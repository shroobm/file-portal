{
  "_README": [
    "docs/29 §5.2 — the signed-silence record. A key the glass detector cannot find in any",
    "renderer is reported as a GLITCH until it appears here with a disposition AND a reason.",
    "That is the whole mechanism: silence signed, never defaulted. Adding a line here is a",
    "judgment you are putting your name to, not a way to quiet the tool.",
    "",
    "Disposition is one of GLASS / EVIDENCE / REPORT / INTERNAL / DEAD / REPAIR (§5.2 + NUM-7). Reason answers",
    "docs/29 §5.3 step 4 — 'does this already have a home, anywhere, live or archival?' — and",
    "names that home. DEAD means the producer should be deleted; a DEAD entry that outlives",
    "the session that wrote it is itself a defect.",
    "",
    "Keys are `<lane>:<key>`. Run: python observability/glass_detector.py",
    "NUM-7 (signed Rab 2026-08-31): `census_dispositions` (below, SEPARATE from this detector-validated dict) holds docs/51's numeration rows — they key on census ids no lane harvests, so they must never sit in `dispositions` (69 of them once did and armed a permanent stale-signature red on the close gate; review C1 2026-08-31). REPAIR = computed correctly, consumer broken — fix the wiring, then retire the entry. The 13 contested census rows are deliberately ABSENT until Rab signs them."
  ],
  "lanes": [
    {
      "name": "bench",
      "_note": "The Repair Bench serves its own UI. This is the lane SYM-026 was found in: state() projected `runs` (18 omission runs on the S76 Beer) and bench.html referenced it zero times.",
      "producers": [
        "prototypes/repair-bench/bench.py"
      ],
      "renderers": [
        "prototypes/repair-bench/bench.html"
      ]
    },
    {
      "name": "widget",
      "_note": "Rust computes, JS renders. The `chunks_resumed` / `resumed` / `eta_s` heartbeat died on this wire (docs/29 §7.5).",
      "producers": [
        "windows-widget/src-tauri/src/*.rs"
      ],
      "renderers": [
        "windows-widget/src/main.js",
        "windows-widget/src/room.js",
        "windows-widget/src/index.html"
      ]
    },
    {
      "name": "converter",
      "_note": "The converter writes the manifest and the receipts. Its readers are downstream CODE rather than a surface, so a hit here means 'something consumes it', which is weaker than 'a human sees it' — the bench and widget lanes then re-check whether the consumer projects it onward. `manifest.chunking.seams[]` (docs/29 §7.3) is the case this two-hop shape is built to catch.",
      "producers": ["windows-converter/convert_and_ship.py", "windows-converter/fidelity_audit.py", "windows-converter/analyst.py", "windows-converter/table_shape.py", "windows-converter/figure_text.py", "windows-converter/page_geometry.py", "windows-converter/marker_blocks.py", "windows-converter/fixes.py", "windows-converter/ligature_repair.py", "windows-converter/variants.py"],
      "renderers": [
        "prototypes/repair-bench/bench.py",
        "prototypes/repair-bench/bench.html",
        "windows-widget/src-tauri/src/*.rs",
        "windows-widget/src/*.js",
        "linux-converter/converter/*.py"
      ]
    }
  ],
  "dispositions": {
    "converter:windows_total": {
      "disposition": "EVIDENCE",
      "reason": "S175 E4 (2026-09-17, his word Desk 8ca8279b; SYM-057 FIXED): the analyst block's window count — the DENOMINATOR of fidelity.analyst.doc_survival, written on both branches of audit_analyst (0 when the reference built no window and doc_survival is None/UNREAD; the count otherwise). docs/29 §5.3 step 4: its home is the manifest beside the ratio it explains, read by whoever reads the fidelity block (the bench, a reaudit, the index's own SYM-057 row); no surface renders the analyst denominator today (the widget prints the ratio, `—` for a null) — the day the Dock's analyst sub-block names 'n of N windows' it becomes rendered; until then EVIDENCE, the same disposition as its sibling reference_masked and the convert block's pages_total (S144)."
    },
    "converter:latex_structure": {
      "disposition": "EVIDENCE",
      "reason": "S175 E4 (2026-09-17, his word Desk 8ca8279b; SYM-056's validator wired): manifest['latex_structure'] — latex_structure.check()'s COUNTS over the pre-analyst Marker body (valid, begin, end, delta, unterminated, unopened, misordered, runaway, max_spec_cols, mode 'flag'), written by convert_and_ship._latex_structure_safe before the analyst. A warn-only flag: the verdict does not read it; what a flag does (reject / strip / bench) is his design. Its home is the manifest beside the fidelity block, read by whoever opens the bundle (the bench, a reaudit, SYM-056's row); no surface renders it — the day the Dock's card shows 'LaTeX: N unterminated' it becomes GLASS. Entered BY HAND: glass's census harvests dict literals that leave a function and its --since regex reads \"key\": in added lines — a key written by subscript is invisible to both (register row observability/glass-subscript-blind, S175)."
    },
    "converter:blank_assets": {
      "disposition": "EVIDENCE",
      "reason": "S188 E7 (2026-09-17; SYM-053's tripwire, report-only): manifest['blank_assets'] — blank_assets.scan()'s record over the bundle's assets/ against the pre-analyst body (assets, read, unread + unread_total, blank[] with name/width/height/extrema/sd/referenced, blank_total, blank_shown_cap, blank_referenced, blank_sd, mode 'flag'), written by convert_and_ship._blank_assets_safe beside latex_structure. A warn-only flag: the verdict does not read it; what a blank crop does (a page flag, a re-crop, the bench) is his gate. Its home is the manifest beside the fidelity block, read by whoever opens the bundle (the bench, a reaudit, SYM-053's row) and by the CLI blank_assets.py <bundle_dir>; no live surface renders it — the day the assay card shows 'blank crops: N referenced' it becomes GLASS (the widget's rebuild, his). The same disposition and reason-shape as converter:latex_structure (S175)."
    },
    "converter:verdict_phases": {
      "disposition": "EVIDENCE",
      "reason": "S188 E6 (2026-09-17): fidelity['verdict_phases'] — every phase whose FAIL signal fired, in pipeline order (fidelity_audit.fail_phases), written by build_fidelity_block and by subscript at convert_and_ship's three hand-set sites (the analyst audit's two branches, the reaudit path) beside the unchanged verdict / verdict_phase (S175). Its home is the manifest's fidelity block, read by whoever reads the verdict (the bench, a reaudit, the held sidecar's reader); the assay badge renders verdict_phase alone today (assay.rs:159, main.js:996) — the day it renders the list it becomes GLASS (the rebuild, his). Entered BY HAND for the subscript sites, as latex_structure was (register row observability/glass-subscript-blind)."
    },
    "converter:from_lane": {
      "disposition": "EVIDENCE",
      "reason": "S191 E1 (2026-09-17; read at convert_and_ship.py:1108, estimate_from_ledger): the lane whose ledger rows made the promise, carried in the estimate's dict into .convert-estimate.json (the promise receipt, docs/29's A4 note) and never emitted on the wire — the Room renders eta_s / s_per_page / basis ×samples (room.js:345), not the lane. Its home is the receipt file a reader opens to audit a promise (promise_audit.py reads the events, the file names the lane); a render on the Room's promise line is the rebuild, his."
    },
    "converter:page_coverage": {
      "disposition": "EVIDENCE",
      "reason": "S191 E1 (2026-09-17; read at fidelity_audit.py:512, audit_convert): tripwires.page_coverage = {with_text, surviving} — the counts behind the page-level survival, in the manifest's fidelity.convert block beside doc_survival (docs/22 §634 names it; the mission-control prototype reads it as a numerator/denominator pair). Read by whoever opens the fidelity block (the bench, a reaudit); no live surface shows the pair — the Room shows the ratio. The same disposition as its siblings pages_total / windows_total (S144, S175)."
    },
    "converter:with_text": {
      "disposition": "EVIDENCE",
      "reason": "S191 E1 (2026-09-17; read at fidelity_audit.py:512): the denominator inside tripwires.page_coverage — pages with witness text — the same home and readers as converter:page_coverage; a denominator never travels without its ratio (docs/34)."
    },
    "converter:dict_hit": {
      "disposition": "EVIDENCE",
      "reason": "S191 E1 (2026-09-17; read at fidelity_audit.py:519): `dict_hit: None` — the slot of docs/15's dictionary-hit gate (flag below 0.80; N-267), whose input was never built (wordfreq absent; the comment says so) — the scan lane's live substitute is garbage_rate beside it. The null in the manifest is the RECORD that the input does not exist: a reader reads it as 'unbuilt', never as 0, and docs/29 §7 called it 'a dead gate' by that name. Deleting the slot or feeding it is docs/15's signed threshold — his; until then the null is evidence of an unfed gate, filed with every book. The acceptance harness's row for this key flips GLITCH → evidence with this reason (S161's pattern)."
    },
    "converter:garbage_rate": {
      "disposition": "EVIDENCE",
      "reason": "S191 E1 (2026-09-17; read at fidelity_audit.py:520 and :634): the scan lane's garbage-token rate (N-267), null on the clean lane — READ by verdict_with_phase in the producer's own module (`gr = tw.get(\"garbage_rate\")`, a localiser: flag above SCAN_GARBAGE_FLAG), which the detector cannot see (its renderer list is other files). Its home is the manifest's fidelity.convert tripwires beside the verdict it helps decide; the Room shows the verdict, not the rate."
    },
    "converter:generationConfig": {
      "disposition": "INTERNAL",
      "reason": "S191 E1 (2026-09-17; read at analyst.py:277, _generate_gemini): the Gemini request body's generationConfig ({temperature: 0.2}) — an OUTBOUND API parameter, the same class as converter:num_predict (S180) and bench:keep_alive: control flow of a request, never a value a human wants back."
    },
    "converter:gpu_vram_mib": {
      "disposition": "EVIDENCE",
      "reason": "S191 E1 (2026-09-17; read at analyst.py:959, preflight, and convert_and_ship.py:2144): the card's VRAM in use at preflight, written into the pending gate card's `preflight` block beside gpu_busy — main.js:318 renders the busy FLAG ('⚠ GPU busy' / 'GPU free'), not the MiB. Its home is the pending card's JSON (pending/<id>.json), opened deliberately when a reader asks what 'busy' meant in numbers; the MiB on the card's face is the rebuild, his."
    },
    "widget:acked": {
      "disposition": "EVIDENCE",
      "reason": "S191 E2 (2026-09-17; read at algedonic.rs:243 and room.js:136–160): the per-occurrence acknowledgement flag in algedonic_state's alerts[] — its HOME is the ack ledger algedonic-acks.jsonl (algedonic.rs:58 ack writes it, :77 state reads it back), and the Room shows it by ABSENCE: the banner lists only `escalated` = !acked && age ≥ M, so an acked occurrence leaves the list the moment the ⚑ ack button writes the ledger. A separate 'acked' mark on the glass is the rebuild, his."
    },
    "widget:unacked": {
      "disposition": "INTERNAL",
      "reason": "S191 E2 (2026-09-17; read at algedonic.rs:255 and room.js:1035): the count of unacknowledged alerts INCLUDING those younger than M — below the signed M line no action is owed (docs/18 §6, docs/19 §6 provisional), so the banner and the Wall show the `escalated` count by design; the young count is derivable from the alerts[] the same payload carries (each with acked and age_min). A 'quiet pain' counter is a design Rab has not asked for; if he does, it is one line of JS at the rebuild."
    },
    "widget:linux_host": {
      "disposition": "EVIDENCE",
      "reason": "S191 E2 (2026-09-17; read at config.rs:19 and main.rs:50 status::fetch_events(&cfg.linux_host, &cfg.remote_user)): a config-file field the HUMAN wrote (the Tailscale name of the Linux box), read by the transport to act — an input, not a computed value; its home is the config file itself (docs/06's security model; S87's rule: verify config from the widget's BOOT LOG, never the writing surface). A surface that prints the host is the rebuild, his; the boot log and the file are where a reader looks today."
    },
    "widget:remote_user": {
      "disposition": "EVIDENCE",
      "reason": "S191 E2 (2026-09-17; read at config.rs:21 and main.rs:50): the SSH user the transport signs in as (never root — docs/06); a human-written config field whose home is the config file; the same disposition and readers as widget:linux_host."
    },
    "widget:remote_inbox_root": {
      "disposition": "EVIDENCE",
      "reason": "S191 E2 (2026-09-17; read at config.rs:23): the remote base directory the inbox/<category> folders live under — a human-written config field read by the transport when it ships; its home is the config file; the same disposition as widget:linux_host."
    },
    "widget:vault_library_dir": {
      "disposition": "EVIDENCE",
      "reason": "S191 E2 (2026-09-17; read at config.rs:27): the local path of the vault's Library clone (Decision #4); EMPTY hides the Add-to-Library button — the Dock renders the field by presence (the button exists or not), which is the only render a path needs; the value's home is the config file."
    },
    "widget:gpu_pipeline_dir": {
      "disposition": "EVIDENCE",
      "reason": "S191 E2 (2026-09-17; read at config.rs:37 and main.rs:59/71/84/105/124/135/145, algedonic.rs, assay.rs, line.rs): the pipeline root every Rust command reads its files from (pending/, drop/, anchor/, events.jsonl, the boot log at main.rs:427) — a human-written config field; empty hides the pre-flight card (rendered by presence). Its home is the config file; every surface the widget has is downstream of it."
    },
    "widget:gpu_python_exe": {
      "disposition": "EVIDENCE",
      "reason": "S191 E2 (2026-09-17; read at config.rs:41 and assay.rs:289): the marker-env interpreter the card's route buttons spawn the converter with — a human-written config field read to act; both it and gpu_converter_dir are required for the buttons to act (the buttons' presence is the render); its home is the config file."
    },
    "widget:gpu_converter_dir": {
      "disposition": "EVIDENCE",
      "reason": "S191 E2 (2026-09-17; read at config.rs:43 and assay.rs:288–): the repo's windows-converter folder the spawn runs in — a human-written config field; the same disposition and readers as widget:gpu_python_exe."
    },
    "widget:reader_obsidian": {
      "disposition": "EVIDENCE",
      "reason": "S191 E2 (2026-09-17; read at config.rs:47 and the boot log's `reader_config: {obsidian: true, zennotes: true}` line, main.js:535): the launcher path/URI of a reader (docs/13 'the dock has doors'); the config file is the allowlist; the Dock renders it by PRESENCE (the door icon exists or not) and the boot log prints the presence flag. Its home is the config file."
    },
    "widget:reader_zennotes": {
      "disposition": "EVIDENCE",
      "reason": "S191 E2 (2026-09-17; read at config.rs:49, main.js:535): the same as widget:reader_obsidian — a reader's launcher, rendered by presence, the config file its home."
    },
    "widget:convert_frac": {
      "disposition": "INTERNAL",
      "reason": "S191 E2 (2026-09-17; read at line.rs:328 and room.js:330–352): the current Marker stage's fraction (the progress file's `frac`), copied into the line payload beside stage / n / total — room.js renders `stage · n/total` as text (:351) and draws the bar from elapsed ÷ the promise BY DESIGN (S42: the bar is monotonic; a per-stage fraction resets at every stage boundary and would run backwards). The number is derivable from n/total, which are rendered; it is control flow that the face chose not to draw. A per-stage bar is a design question for the rebuild, his."
    },
    "widget:shipped_ts": {
      "disposition": "EVIDENCE",
      "reason": "S191 E2 (2026-09-17; read at line.rs:471 last_receipt and main.js:512–523): the ship event's timestamp inside the receipt the ⚓ station click opens — the status line prints the bundle, the convert line, the analyst line and the audit verdict, not the time; its home is the `ship/shipped` event in events.jsonl (and the receipts file) that the receipt is a projection of. 'shipped HH:MM' beside the bundle is one line of JS at the rebuild."
    },
    "bench:total_hits": {
      "disposition": "GLASS",
      "reason": "S191 E3 (2026-09-17; read at bench.py:851 find and bench.html:1426–1435): the sum of the per-page hit counts of a text search — the page renders every hit with its own count (`p12 · 3×`), which sum to it; the total's home is the /api/find response the page opened by the operator's own query. A 'N hits on M pages' head is one line at the bench's next build. S192 E1: GLASS — the search head prints `Search (M pages · N hits)` from r.total_hits (bench.html:1442; test_bench_page.py TestS192OwedPrints)."
    },
    "bench:needles": {
      "disposition": "GLASS",
      "reason": "S191 E3 (2026-09-17; read at bench.py:1132 locate_zone and bench.html:1260–1265): how many text needles the locator tried; the page renders `method`, `page` and `confidence` — and confidence = votes[best] / needles, so the count is the denominator behind a number the page shows (docs/34: a ratio prints both sides — owed at the bench's next build). Its home is the /api/locate response. S192 E1: GLASS — the locate status prints `confidence 0.75 = 3/4 needles` (bench.html:1273; the claim names both sides, test_bench_page.py)."
    },
    "bench:votes": {
      "disposition": "GLASS",
      "reason": "S191 E3 (2026-09-17; read at bench.py:1134 and bench.html:1260–1265): the per-page vote map behind the locator's choice (the numerator of `confidence` and the runners-up); the page shows the chosen page and the confidence; the map is the evidence a reader opens when the location looks wrong. Its home is the /api/locate response. S192 E1: GLASS — the chosen page's votes are the numerator of the same line (bench.html:1273: r.votes[String(r.page)])."
    },
    "bench:seq": {
      "disposition": "EVIDENCE",
      "reason": "S191 E3 (2026-09-17; read at bench.py:1214 _write_body): the write's sequence number in the repair ledger (repairs.jsonl, the chokepoint's fsynced record) — read back by ledger_audit and the ≡ ledger view (bench.html:976–981: chain intact · N write(s)); the same home and disposition as bench:unsplit / bench:truncated (S180)."
    },
    "bench:context_before": {
      "disposition": "EVIDENCE",
      "reason": "S191 E3 (2026-09-17; read at bench.py:1221 _write_body): the text before a changed region, written into the ledger record so a reviewer can place the change without the body; the ledger (repairs.jsonl) is the surface, read by the ≡ ledger view and the reaudit; never live."
    },
    "bench:context_after": {
      "disposition": "EVIDENCE",
      "reason": "S191 E3 (2026-09-17; read at bench.py:1223): the text after a changed region — the same record, home and readers as bench:context_before."
    },
    "bench:sha_before": {
      "disposition": "EVIDENCE",
      "reason": "S191 E3 (2026-09-17; read at bench.py:1226 and :1314–1326 ledger_audit, :1296 undo_ledger): the body's sha before a write — the ledger's CHAIN (each write's sha_before must equal the previous sha_after; ledger_audit reads it in-module and the ≡ ledger view prints `chain intact` / `chain BROKEN`). Its home is repairs.jsonl; the chain verdict is what the page shows."
    },
    "bench:sha_after": {
      "disposition": "EVIDENCE",
      "reason": "S191 E3 (2026-09-17; read at bench.py:1226, :1296 undo_ledger, :1314–1326 ledger_audit — eight in-module reads): the body's sha after a write; the other half of the chain and the key the undo reverts by; the same home and readers as bench:sha_before."
    },
    "bench:regions": {
      "disposition": "GLASS",
      "reason": "S191 E3 (2026-09-17; read at bench.py:1301 undo_ledger and bench.html:2066–2069): how many regions an undo restored — the page's status line says `↩ undone — byte-identical (N more available)` (undo_depth), not the region count; the count's home is the /api/undo response and the ledger's undo event. One more number on the status line at the bench's next build. S192 E1: GLASS — the undo status prints `R region(s), C char(s) restored` (bench.html:2079)."
    },
    "bench:chars_restored": {
      "disposition": "GLASS",
      "reason": "S191 E3 (2026-09-17; read at bench.py:1302): the characters an undo put back (the sum of the reverted events' chars.removed) — the same response and home as bench:regions; the status line prints the depth. S192 E1: GLASS — the same undo line (bench.html:2079)."
    },
    "bench:matches_disk": {
      "disposition": "GLASS",
      "reason": "S191 E3 (2026-09-17; read at bench.py:1314 (the empty-ledger branch, None) and :1326 ledger_audit, bench.html:976–981): whether the newest sha_after equals the body on disk — the second half of the chain check; the ≡ ledger view prints `chain intact` from `intact` (the per-write chain) and the write count; matches_disk is the disk half a reader opens the /api/ledger response for. Printing it beside `intact` is owed at the bench's next build (a chain intact but a body written around it is exactly the case it names). S192 E1: GLASS — the ≡ ledger view prints `disk matches the chain` / `⚠ DISK DIFFERS FROM THE CHAIN` beside the chain half (bench.html:983–988); null (an empty ledger) prints nothing, never 'matches'."
    },
    "bench:delta_lines": {
      "disposition": "EVIDENCE",
      "reason": "S191 E3 (2026-09-17; read at bench.py:1861 and :1882 collapse): the signed line delta of a collapse, written into the repair record (repairs[] in the manifest / repairs.jsonl) beside `delta` — bench:delta's own disposition (S168) names the home: the record the bench reads back at every restart (_record_shift); the same home here."
    },
    "bench:para_lines": {
      "disposition": "EVIDENCE",
      "reason": "S191 E3 (2026-09-17; read at bench.py:1857 and :1882 collapse): the paragraph's line count before the collapse — the other half of delta_lines' ratio; the same record and home."
    },
    "bench:zones_with_repairs": {
      "disposition": "EVIDENCE",
      "reason": "S191 E3 (2026-09-17; read at bench.py:2055 rescore_preview and bench.html:2197–2210): how many of the original zones carry a repair — the ◎ preview's status line prints the degeneration verdict, the zone count and the coverage (addressed/shown), from `coverage`; this count sits beside `repairs` in the same response as the provenance half's other reading. Its home is the /api/rescore response the operator opens; one more term in the status line at the bench's next build."
    },
    "bench:chars_added": {
      "disposition": "REPORT",
      "reason": "S191 E3 (2026-09-17; read at bench.py:2128 repairs_report and bench.html:992–996): characters the repairs added — a field of the /api/report response whose `markdown` is REPAIRS.md, the ledger's final report (docs/28 §3), previewed in a <pre> and written beside the bundle on request; an end-of-run artifact, never polled — docs/29 §5.2's REPORT."
    },
    "bench:entropy_pct": {
      "disposition": "REPORT",
      "reason": "S191 E3 (2026-09-17; read at bench.py:2129 repairs_report): the share of the original body the repairs removed (removed / (body + removed)) — the same response and report as bench:chars_added; REPAIRS.md is its home."
    },
    "bench:unsplit": {
      "disposition": "EVIDENCE",
      "reason": "S180 E3 (2026-09-17; read at bench.py:1692, unsplit_tables): the timestamp the repair ledger's record gains when an unsplit-tables move is placed back — a field of the ledger entry (repairs.jsonl), read by whoever reads the ledger (the reaudit, a reviewer); the same disposition as bench:truncated. Never rendered live; the ledger is the surface."
    },
    "converter:blocks_path": {
      "disposition": "INTERNAL",
      "reason": "S180 E3 (read at convert_and_ship.py:1682 and :1755): the path of the merged blocks.json in the chunk stats — control flow only; it feeds _attach_blocks_safe, which writes the manifest's `blocks` COUNTS (bytes, sha, records). The path itself is a temp-dir location a human never wants."
    },
    "converter:ceiling_mib": {
      "disposition": "EVIDENCE",
      "reason": "S180 E3 (read at convert_and_ship.py:1233/1246, _run_marker): the VRAM ceiling the run was under when Marker was killed, written into the death certificate (the GPU signature `sig`, S146 E3) that the stall/kill event carries — read by a human at the post-mortem of a stalled book (events.jsonl); never live."
    },
    "converter:ceiling_top": {
      "disposition": "EVIDENCE",
      "reason": "S180 E3 (read at convert_and_ship.py:1234/1247): the process that held the card's top when the ceiling was hit — the death certificate's `who` (S146 E3: 'the death certificate names WHO held the card'); the same reader as ceiling_mib."
    },
    "converter:gpu_top": {
      "disposition": "EVIDENCE",
      "reason": "S180 E3 (read at convert_and_ship.py:532, _gpu_signature): the card's committers at the signature's moment, on the death certificate only (with_processes=True) — the post-mortem's reader; the Dock shows the card's live state from nvidia-smi, not from this."
    },
    "converter:inflation": {
      "disposition": "EVIDENCE",
      "reason": "S180 E3 (read at analyst.py:697, process): the per-reason rejection count `rejections.inflation` (J34) in the manifest's analyst block — opened on the assay card's analyst line as the rejected count; the per-reason split is read in the manifest and the chunk journal by a reviewer (S180 E1 read it to place the 2026-08-31 specimen)."
    },
    "converter:think_leak": {
      "disposition": "EVIDENCE",
      "reason": "S180 E3 (read at analyst.py:674): the per-reason rejection count `rejections.think_leak` (SYM-074) — the same block and readers as converter:inflation."
    },
    "converter:max_spec_cols": {
      "disposition": "EVIDENCE",
      "reason": "S180 E3 (read at convert_and_ship.py:816, _latex_structure_safe): the lever's value carried inside the manifest's `latex_structure` record so a reader knows which width the runaway count was judged against — the same disposition and readers as converter:latex_structure (S175)."
    },
    "converter:num_predict": {
      "disposition": "INTERNAL",
      "reason": "S180 E3 (read at analyst.py:121/196/203/619): the ollama request option — the generation bound per chunk (_num_predict_for) — an outbound parameter, the same class as bench:keep_alive and bench:stream; it is carried into the chunk journal for a rejected chunk, which is the reviewer's surface, not a live one."
    },
    "converter:table-geometry": {
      "disposition": "EVIDENCE",
      "reason": "S180 E3 (read at analyst.py:542/544, process): the edit whitelist's tally for the table-geometry layer (J46 + S150) in the manifest's analyst `edits.accepted` / `edits.reverted` — the same block as the other whitelist classes (escape, hyphen, ligature, link, markup), which are written by a loop over the tally and were never literal keys either; read in the manifest and the chunk journal by a reviewer."
    },
    "converter:reference_masked": {
      "disposition": "EVIDENCE",
      "reason": "S131 (Rab signed 2026-09-12; docs/15 §12.1): the analyst block's record of the reference blocks the near-exact gate masked before its windows were built (per block: line, line_end, chars, zlib, max_trigram, excerpt; plus the words removed). A verdict that leaned on the mask must be readable as such, and it is: docs/29 §5.3 step 4 — HOME = the manifest's fidelity.analyst / fidelity.final.analyst block, the card the assay and the Repair Bench open (fidelity_audit.py audit_analyst; first specimen the Zero to One reaudit, vault 8d7cc85, one block of 408 words). Not GLASS: a masked block is Marker's disease, already surfaced by the convert gate's degeneration tripwire and its damage map; this key only says the analyst gate did not count the same disease twice. If a second book needs it at a glance, a 'reference masked: N words' chip on the assay card is the next home."
    },
    "bench:truncated": {
      "disposition": "EVIDENCE",
      "reason": "The Repair Bench's repair ledger (bench.py _write_body, the `margin` record of one edit): whether the kept context lines around a hunk were cut at LEDGER_CONTEXT — a fact inside the ledger entry, read by whoever reads the ledger, never rendered live. Surfaced at S146 E5 only because the analyst's new rejection reason `truncated` (SYM-129) made the key name new since the pin — a generic name's incidental match, the detector's own docstring's case. Evidence, same home as the ledger entry."
    },
    "bench:stream": {
      "disposition": "INTERNAL",
      "reason": "An outbound request parameter on the bench's ✦ assist call (bench.py:1134) — it tells Ollama not to stream. docs/29 §5.3 step 1: it exists only to feed another computation, so it is correctly silent and no human ever wants it on a surface. SCOPE NOTE, and it is the point of this entry: bench.py was NOT touched this session. This key entered the --since scope from `analyst.py`'s request body, which S79 edited for the residency fix, because the filter harvests key NAMES across the whole *.py diff and then attributes them to whichever lane has a producer using that name. That is the identical mis-scoping S79 diagnosed in the `bench:reason` entry below at 21:00 and then reproduced at its own close six hours later. docs/31 §5.2 item 7 — whether outbound request bodies are 'measured, stored values' at all — is UNSIGNED, and this is the second piece of evidence that they are not."
    },
    "bench:keep_alive": {
      "disposition": "INTERNAL",
      "reason": "Outbound request parameter (bench.py:1134): the bench's assist deliberately sends keep_alive 0 so the model leaves VRAM after a single interactive call — correct there, unlike the analyst, whose calls are back-to-back (S79 residency fix). Control flow, never a surface. Same scope note as bench:stream: it arrived from analyst.py's diff, not from any change to the bench."
    },
    "bench:prompt": {
      "disposition": "INTERNAL",
      "reason": "Outbound request parameter (bench.py:1135) — the text sent to the model. Its content is already visible to the operator: they typed it into the AI bar and the proposal comes back on the glass. Showing the assembled prompt would be chrome nobody asked for (docs/29 §5.3 step 5). Same scope note as bench:stream."
    },
    "bench:reason": {
      "disposition": "EVIDENCE",
      "reason": "The five-outcome triage's free-text reason (bench.py:667 triage, :678/:682 coverage). Its home is the opened zone/run card, not the chip row — docs/29 §5.3 step 3: a localizer is EVIDENCE, revealed when the card is open, never promoted to ambient chrome. NOTE ON SCOPE: this key surfaced under `--since 60acd77` only because dispositions.json's own `reason` fields entered the diff as added `\"reason\":` lines — a false positive of name-based scoping (the key was not added to a producer this session). Recorded as the honest disposition anyway rather than waved away, since the question it forces is the right one. The scoping weakness itself belongs on docs/31 §5.2's sheet."
    },
    "bench:options": {
      "disposition": "INTERNAL",
      "reason": "Outbound request parameter on the bench's assist call (bench.py:1135) - the ollama options envelope. docs/29 §5.3 step 1: it exists only to configure another process, and carries nothing a human would act on. Same class as bench:stream and bench:keep_alive. They are NOT new to the bench: bench.py:1135 has carried them unchanged, and they surfaced now only because windows-converter/backend_parity.py added the same key NAMES in its own outbound ollama body this session. That is docs/31 §5.2 item 7, the detector's --since mis-scoping, recurring for the third recorded time (S78, S79, S81) and still unsigned."
    },
    "bench:num_ctx": {
      "disposition": "INTERNAL",
      "reason": "Outbound request parameter (bench.py:1135) - the context window handed to ollama for the assist call. A knob sent OUT, not a value computed for a reader; when its size matters to a human it reaches them as a failure (a truncated answer), not as a number. They are NOT new to the bench: bench.py:1135 has carried them unchanged, and they surfaced now only because windows-converter/backend_parity.py added the same key NAMES in its own outbound ollama body this session. That is docs/31 §5.2 item 7, the detector's --since mis-scoping, recurring for the third recorded time (S78, S79, S81) and still unsigned."
    },
    "bench:think": {
      "disposition": "INTERNAL",
      "reason": "Outbound request parameter (bench.py:1135) - tells qwen3 not to emit a reasoning block. Correct there: the operator wants the answer, not the deliberation. S80/S81 measured what this flag is worth (thinking ON costs 6.5x the tokens on the analyst's workload), and that measurement has its home in docs/34 and the CHANGELOG, not on the bench surface. They are NOT new to the bench: bench.py:1135 has carried them unchanged, and they surfaced now only because windows-converter/backend_parity.py added the same key NAMES in its own outbound ollama body this session. That is docs/31 §5.2 item 7, the detector's --since mis-scoping, recurring for the third recorded time (S78, S79, S81) and still unsigned."
    },
    "widget:from_verdict": {
      "disposition": "INTERNAL",
      "reason": "The docs/15 SS14.2 supersede-marker field, unchanged in assay.rs (:251, the JSON! macro authoring drop/.supersede/<name>.json — the widget's ⟳/⟲ click), reachable to a human already: it rides into manifest.supersede.from_verdict (convert_and_ship.py's _stamp_supersede_safe) and the audit/supersede event, both of which ARE surfaced (the Assay card, room.js). It surfaced NOW only because J31 (S115) added a second Python-side site using the SAME key name (reaudit()'s own supersede marker, mirroring reanalyze()'s pre-existing one) — assay.rs itself was not touched. docs/31 SS5.2 item 7's name-based --since mis-scoping (see widget:source_sha256 above; fifth recorded recurrence, S78/S79/S81/S93/S115)."
    },
    "widget:requested_at_epoch_s": {
      "disposition": "INTERNAL",
      "reason": "The docs/15 SS14.2 supersede-marker field, unchanged in assay.rs (:254), same shape and same cause as widget:from_verdict directly above — surfaced by J31's new reaudit() site reusing the pre-existing key name, not by any assay.rs edit. Reaches a human through manifest.supersede (opaque epoch, the Assay does not render it directly) exactly as it did before this session; docs/31 SS5.2 item 7 recurrence."
    },
    "widget:source_sha256": {
      "disposition": "INTERNAL",
      "reason": "The widget's outbound intent-marker payloads carry the source's full sha256 as an identity GUARD, not a display value: the remedy marker (assay.rs:219 - its own comment: the converter drops the intent if the file it hashes isn't this one, docs/15 SS14.2 consume-once) and the bless marker (assay.rs:358). A downstream process VERIFIES these fields; the human-facing identity everywhere is the sha16 that names bundle folders. The two remaining sites (assay.rs:458, :622) are test fixtures. SCOPE NOTE: assay.rs was NOT touched in S93 - the key name entered --since scope from T3's .done record and the session's prose. docs/31 SS5.2 item 7, name-based mis-scoping, fourth recorded recurrence (S78, S79, S81, S93), still unsigned."
    },
    "converter:engine_args": {
      "disposition": "INTERNAL",
      "reason": "Resume-admission identity input, born S93 (T3/F-02): written into .chunk-work/<sha16>/slice-*/.done and read back ONLY by _done_identity_mismatch to decide whether a finished slice may be resumed under the current job. Control flow by design (docs/29 SS5.3 step 1). When it matters to a human - a mismatch - the field NAME reaches the operator through the STALE print and the slice_stale event's mismatch list in events.jsonl, whose raw tail the widget's events surface renders. The .done record itself is transient: the whole slice tree is deleted once the merge succeeds."
    },
    "converter:marker_version": {
      "disposition": "REPORT",
      "reason": "Two homes, both real: (1) the manifest stamp (convert_and_ship.py convert()) - the bundle's papers, an end-of-run artifact documented for the operator in docs/35 SS4, riding every anchor/ship/held copy of the book forever; that durable copy is why this is REPORT rather than INTERNAL. (2) Since S93 also a .done resume-admission identity input with the same gate role as converter:engine_args - a Marker upgrade mid-book now re-converts stale slices loudly instead of silently splicing mixed-version output into one book."
    },
    "widget:tripwires": {
      "disposition": "INTERNAL",
      "reason": "Not a measurement and not production code: `tripwires` appears as a CONTAINER key inside two test fixtures in assay.rs (:695, :730 — the P-0 asset-ledger tests, S101), where it holds the manifest shape the projection reads. Its children are the measurements and they are rendered: asset_delta/embedded_images/assets_out on the Assay card (room.js assetLedger, main.js assetLedgerLine, S101) and degeneration on the same card since S31. Nothing computes a value called `tripwires`, so there is nothing to surface. Recorded rather than repaired: the first repair attempted was inlining the Rust index, which did not move the count because the detector was reading the test data, not the binding — the honest classification is INTERNAL, not a code change that games the scan. If the P-0 tests are ever deleted this entry becomes DEAD and must go with them."
    },
    "converter:slice_size": {
      "disposition": "REPORT",
      "reason": "Reaches a human via REPORT, indirectly: figure_coverage.py:295 compares it to SYM050_SLICE and the verdict line ('REPAIRED MAP (SYM-050)' + sym050_doubled_offset) renders in every coverage report and sym050-quarantine.txt. The raw integer is machinery for that verdict, not a separate measurement. Signed S108 at the close's first GLASS red; revisit if the detector line is ever removed."
    },
    "converter:invisible_spans": {
      "disposition": "EVIDENCE",
      "reason": "NUM-5 (census N-054): the OCR routing vote's numerator — home is manifest.probe_evidence + the convert/probe event (convert_and_ship.py:698-699, :1340); no ambient renderer by design, the Assay drill is the eventual reader."
    },
    "converter:seams": {
      "disposition": "EVIDENCE",
      "reason": "S161 E2 (2026-09-16), register row B16: the seam pages of a chunked conversion, written by _convert_chunked (convert_and_ship.py:1633) into manifest.chunking.seams and READ by code — the slices count (convert_and_ship.py:996, :1756) is len(seams)+1 on both the ship and the estimate paths. docs/18 SS5.2 promised them findable forever by the audit and the Repair Bench, not rendered: the Bench opens the manifest (bench.py:237) and a seam lookup is one key away when a repair needs it. No renderer by design; the count reaches the operator as slices (glass). Signed under the autonomy experiment (Fable), reversible by editing this entry."
    },
    "converter:reverse_sample": {
      "disposition": "EVIDENCE",
      "reason": "S161 E2 (2026-09-16), register row B16: the anti-hallucination containment sample (fidelity_audit.py:350 reverse_sample, written at :512 into fidelity.convert.tripwires) — the census already dispositioned it EVIDENCE as N-266 (docs/51 SS163: a 200-window sample that stands in for the whole output; the sample size never travels with it, so docs/34 rule 7 forbids promoting it to a headline) and N-328 names its rendering hazard (a bare fraction beside doc_survival). Home: the manifest tripwires (docs/15:159). Rendering it as a headline would be the promotion docs/34 forbids; rendering it with its n is a widget change for his rebuild slot. Signed under the autonomy experiment (Fable), reversible by editing this entry."
    },
    "converter:total_spans": {
      "disposition": "EVIDENCE",
      "reason": "NUM-5: the vote's denominator, same home as invisible_spans (manifest.probe_evidence + convert/probe event)."
    },
    "converter:invisible_ratio": {
      "disposition": "EVIDENCE",
      "reason": "NUM-5: the decided ratio itself, manifest.probe_evidence + convert/probe event — auditable routing, not an operator gauge."
    },
    "converter:ocr_font_trigger": {
      "disposition": "EVIDENCE",
      "reason": "NUM-5: which font short-circuited the vote (or absent) — manifest.probe_evidence + convert/probe event."
    },
    "converter:probe_evidence": {
      "disposition": "EVIDENCE",
      "reason": "NUM-5: the container object travelling with the book in manifest.json (convert_and_ship.py:1340)."
    },
    "converter:runs_capped_at": {
      "disposition": "EVIDENCE",
      "reason": "NUM-3: names the [:25] cap beside runs_total in the fidelity block (fidelity_audit.py) — a cap that declares itself, per SYM-066's repair; renderers read runs_total, the cap constant is record-keeping."
    },
    "converter:runs_reorder": {
      "disposition": "EVIDENCE",
      "reason": "S209 E10 (2026-09-20; SYM-138): REPORT-ONLY count of the analyst block's omission runs whose every word occurs, as a bag, within one span of the analyst's output (fidelity_audit._reorder) — a reordering (TD Q3 2026: a stacked table header the analyst merged into one row, read by the windows as a 36-word omission that FAILED the document), not an omission. The verdict still counts the run: docs/15 §12's gate is signed and the rescue waits for his word (then a lever). Read in the manifest beside runs_total; the row-level flag is `reorder` on each run."
    },
    "converter:tables": {
      "disposition": "EVIDENCE",
      "reason": "S209 E11 (2026-09-20; the frontier's item 1, the bank tables): the structural-table measure `fidelity.convert.tables` (table_shape.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks handed in). The block: the source's own table geometry (pymupdf find_tables inside each Marker table box) against Marker's rows and width, with the witness's reliability beside every number."
    },
    "converter:tables_total": {
      "disposition": "EVIDENCE",
      "reason": "S209 E11 (2026-09-20; the frontier's item 1, the bank tables): the structural-table measure `fidelity.convert.tables` (table_shape.py) — REPORT-ONLY beside survival. tables_total: every Table block Marker wrote with a page and a box — the population beside tables_witnessed_lines / _text / tables_disagree / tables_unread (docs/34: a count says its population; the four parts sum to it)."
    },
    "converter:figures": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-20; B41, the frontier's item 9 — chart text as text): the measure `fidelity.convert.figures` (figure_text.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). The block: the words the source's layer carries inside each Marker figure box against the words Marker shipped for the figure (the AI Index: 10,551 inside 479 figures, 680 shipped, 67 silent; the MPR 409 vs 8)."
    },
    "converter:figures_total": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-20; B41, the frontier's item 9 — chart text as text): the measure `fidelity.convert.figures` (figure_text.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). Every Figure / Picture block Marker wrote with a page and a box — the population beside the counts (docs/34)."
    },
    "converter:figures_with_layer_words": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-20; B41, the frontier's item 9 — chart text as text): the measure `fidelity.convert.figures` (figure_text.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). Figures whose box holds at least one layer word — the figures that carry text the reader could get."
    },
    "converter:layer_words_in_figures": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-20; B41, the frontier's item 9 — chart text as text): the measure `fidelity.convert.figures` (figure_text.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). The layer's words inside the figure boxes, summed — what the source has as text in its charts."
    },
    "converter:marker_words_for_figures": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-20; B41, the frontier's item 9 — chart text as text): the measure `fidelity.convert.figures` (figure_text.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). The words Marker shipped for those figures (captions, alt text), summed — what the reader gets of it."
    },
    "converter:figures_silent": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-20; B41, the frontier's item 9 — chart text as text): the measure `fidelity.convert.figures` (figure_text.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). Figures with layer words and no shipped words — the chart the reader gets as a picture only."
    },
    "converter:figures_unread": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-20; B41, the frontier's item 9 — chart text as text): the measure `fidelity.convert.figures` (figure_text.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). Figures the library could not read (a page past the document, an empty box, the scan lane) — UNREAD, never a negative."
    },
    "converter:layer_words": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-20; B41, the frontier's item 9 — chart text as text): the measure `fidelity.convert.figures` (figure_text.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). On each worst page: the layer's words inside its figures — the specimen beside the count."
    },
    "converter:marker_words": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-20; B41, the frontier's item 9 — chart text as text): the measure `fidelity.convert.figures` (figure_text.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). On each worst page: the words Marker shipped for its figures — the specimen beside the count."
    },
    "converter:sample": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-20; B41, the frontier's item 9 — chart text as text): the measure `fidelity.convert.figures` (figure_text.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). On each worst page: up to eight of the layer's words inside its figures, in reading order — what the reader never gets, shown."
    },
    "converter:page_geometry": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; B40, SYM-142 / SYM-143 — Stanford CIFE): the measure `fidelity.convert.page_geometry` (page_geometry.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks) or a source that could not be opened. The block: the source's page rotation and its symbol glyphs per page — where Marker's shards on a rotated table page and a recogniser's one word repeated for a glyph are born, named in the manifest before a page is opened. (Not `geometry`: that key is S150's analyst table-geometry layer.)"
    },
    "converter:pages_rotated": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; B40, SYM-142 / SYM-143 — Stanford CIFE): the measure `fidelity.convert.page_geometry` (page_geometry.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks) or a source that could not be opened. Pages whose /Rotate is not 0 (a landscape table stored in a portrait file) — CIFE 16 of 113, eight of them shipped as two-letter shards."
    },
    "converter:rotated_pages": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; B40, SYM-142 / SYM-143 — Stanford CIFE): the measure `fidelity.convert.page_geometry` (page_geometry.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks) or a source that could not be opened. Their 1-based page numbers, listed up to rotated_pages_capped_at; the count is always whole."
    },
    "converter:rotated_pages_capped_at": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; B40, SYM-142 / SYM-143 — Stanford CIFE): the measure `fidelity.convert.page_geometry` (page_geometry.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks) or a source that could not be opened. The list's cap (50), said beside the list so a cut reads as a cut (docs/34)."
    },
    "converter:rotated_with_tables": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; B40, SYM-142 / SYM-143 — Stanford CIFE): the measure `fidelity.convert.page_geometry` (page_geometry.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks) or a source that could not be opened. Rotated pages on which Marker drew a Table or TableGroup block — the shape SYM-142 was born in."
    },
    "converter:rotated_flagged": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; B40, SYM-142 / SYM-143 — Stanford CIFE): the measure `fidelity.convert.page_geometry` (page_geometry.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks) or a source that could not be opened. Rotated pages survival flagged — the crossing of the two lists (CIFE 12 of 16)."
    },
    "converter:symbol_pages": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; B40, SYM-142 / SYM-143 — Stanford CIFE): the measure `fidelity.convert.page_geometry` (page_geometry.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks) or a source that could not be opened. Pages whose layer carries symbol_min or more symbol glyphs (unicode Sm / So: check marks, radicals, arrows) — the glyphs a recogniser has no word for; unread on the scan lane."
    },
    "converter:symbol_glyphs_total": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; B40, SYM-142 / SYM-143 — Stanford CIFE): the measure `fidelity.convert.page_geometry` (page_geometry.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks) or a source that could not be opened. The layer's symbol glyphs over the document; unread (None) on the scan lane, whose layer is the OCR's."
    },
    "converter:symbol_min": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; B40, SYM-142 / SYM-143 — Stanford CIFE): the measure `fidelity.convert.page_geometry` (page_geometry.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks) or a source that could not be opened. The line (10 glyphs) above which a page counts as a symbol page, said beside the count."
    },
    "converter:symbols_reason": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; B40, SYM-142 / SYM-143 — Stanford CIFE): the measure `fidelity.convert.page_geometry` (page_geometry.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks) or a source that could not be opened. Why the symbol counts read None on the scan lane — the reason beside the UNREAD, never a zero."
    },
    "converter:pages_unread": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; B40, SYM-142 / SYM-143 — Stanford CIFE): the measure `fidelity.convert.page_geometry` (page_geometry.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks) or a source that could not be opened. Pages the library could not read for their rotation or text — UNREAD, never a negative."
    },
    "converter:rotation": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; B40, SYM-142 / SYM-143 — Stanford CIFE): the measure `fidelity.convert.page_geometry` (page_geometry.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks) or a source that could not be opened. On each worst page: its /Rotate in degrees."
    },
    "converter:flagged": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; B40, SYM-142 / SYM-143 — Stanford CIFE): the measure `fidelity.convert.page_geometry` (page_geometry.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks) or a source that could not be opened. On each worst page: 1 when survival flagged the page, 0 when not — the page's two readings side by side."
    },
    "converter:symbol_glyphs": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; B40, SYM-142 / SYM-143 — Stanford CIFE): the measure `fidelity.convert.page_geometry` (page_geometry.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks) or a source that could not be opened. On each worst page: the layer's symbol glyphs there (None on the scan lane)."
    },
    "converter:repeated": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; SYM-143 — CIFE p.32, `second` × 291 for 48 check marks): the inventions audit's REPEAT class (`fidelity.convert.inventions`) — REPORT-ONLY, unseen by compute_verdict. One invented word written repeat_min or more times on a page — its word, count and page — its own class beside invented_total, which still counts every copy."
    },
    "converter:repeated_total": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; SYM-143 — CIFE p.32, `second` × 291 for 48 check marks): the inventions audit's REPEAT class (`fidelity.convert.inventions`) — REPORT-ONLY, unseen by compute_verdict. Every copy of every repeated word, summed — invented_total net of it is the rest of the inventions."
    },
    "converter:repeat_min": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; SYM-143 — CIFE p.32, `second` × 291 for 48 check marks): the inventions audit's REPEAT class (`fidelity.convert.inventions`) — REPORT-ONLY, unseen by compute_verdict. The line (20 copies) above which a word reads as a repeat, said beside the count."
    },
    "converter:word": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; SYM-143 — CIFE p.32, `second` × 291 for 48 check marks): the inventions audit's REPEAT class (`fidelity.convert.inventions`) — REPORT-ONLY, unseen by compute_verdict. The repeated word itself — the specimen beside its count."
    },
    "converter:count": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; SYM-143 — CIFE p.32, `second` × 291 for 48 check marks): the inventions audit's REPEAT class (`fidelity.convert.inventions`) — REPORT-ONLY, unseen by compute_verdict. How many times the word was written on that page."
    },
    "converter:symbol_worst": {
      "disposition": "EVIDENCE",
      "reason": "S209 E13 (2026-09-21; SYM-143 — CIFE p.32): the page-geometry measure's symbol pages, by their glyphs (cap 10), each with its rotation, Table, flagged bit and a sample of the glyphs — SYM-143's list beside `worst` (the rotated pages, SYM-142's); REPORT-ONLY, unseen by compute_verdict; empty on the scan lane, where the symbols are unread. Born of the measure's first live reading: one list sorted by glyphs ranked the whole rotated tables of pp. 96–100 (their + signs) above the shard pages and dropped p.32 past the cap; ASCII symbols are no longer counted."
    },
    "converter:classes": {
      "disposition": "EVIDENCE",
      "reason": "S209 E14 (2026-09-21; B40 BUILT — the SEU's three ways and SHEG's fourth): the inventions audit's classes of invented word (`fidelity.convert.inventions`) — REPORT-ONLY, unseen by compute_verdict. Every invented word in one of four classes, counted per copy: joined / fragment / dropped_letter / garble — SHEG 0/8/60/186, CIFE 0/549/0/325 (the shards are fragments), NBC 1/7/0/16."
    },
    "converter:class_specimens": {
      "disposition": "EVIDENCE",
      "reason": "S209 E14 (2026-09-21; B40 BUILT — the SEU's three ways and SHEG's fourth): the inventions audit's classes of invented word (`fidelity.convert.inventions`) — REPORT-ONLY, unseen by compute_verdict. Up to eight specimens per class, each with its page, word and count — the reader's evidence beside the counts."
    },
    "converter:joined": {
      "disposition": "EVIDENCE",
      "reason": "S209 E14 (2026-09-21; B40 BUILT — the SEU's three ways and SHEG's fourth): the inventions audit's classes of invented word (`fidelity.convert.inventions`) — REPORT-ONLY, unseen by compute_verdict. Two neighbouring layer words run together in Marker's text (`electronicsand`, NBC p.13 `creditlosses`) — a space the converter dropped."
    },
    "converter:fragment": {
      "disposition": "EVIDENCE",
      "reason": "S209 E14 (2026-09-21; B40 BUILT — the SEU's three ways and SHEG's fourth): the inventions audit's classes of invented word (`fidelity.convert.inventions`) — REPORT-ONLY, unseen by compute_verdict. A piece of a lost word (`cred` of `credentials`; CIFE's two-letter shards) — a word the converter broke."
    },
    "converter:dropped_letter": {
      "disposition": "EVIDENCE",
      "reason": "S209 E14 (2026-09-21; B40 BUILT — the SEU's three ways and SHEG's fourth): the inventions audit's classes of invented word (`fidelity.convert.inventions`) — REPORT-ONLY, unseen by compute_verdict. One letter off a lost word on the same page (`feld` of `field`, `diferent` — SYM-148 at the extractor) — a real reader loss no fold repairs."
    },
    "converter:garble": {
      "disposition": "EVIDENCE",
      "reason": "S209 E14 (2026-09-21; B40 BUILT — the SEU's three ways and SHEG's fourth): the inventions audit's classes of invented word (`fidelity.convert.inventions`) — REPORT-ONLY, unseen by compute_verdict. None of the three — OCR noise, a picture's words (SHEG's cover p.1: 179 of its 186), a recogniser's invention."
    },
    "converter:numbers": {
      "disposition": "EVIDENCE",
      "reason": "S210 E1 (2026-09-21; SYM-147's row-level loss, B36's next cut — the duplicated-figure tell): `fidelity.convert.numbers` (audit_numbers) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). The block: per page, the number tokens (grouped thousands, or four digits and more) Marker's blocks carry MORE often than the source's layer, and the layer's the blocks lack — NBC p.57: 1,040 three times over where a row's figures moved onto the row above, a loss survival and the tables' geometry could not see."
    },
    "converter:extra_total": {
      "disposition": "EVIDENCE",
      "reason": "S210 E1 (2026-09-21; SYM-147's row-level loss, B36's next cut — the duplicated-figure tell): `fidelity.convert.numbers` (audit_numbers) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). Copies of number tokens Marker carries beyond the layer's, summed over the pages — moved, duplicated or OCR'd figures."
    },
    "converter:missing_total": {
      "disposition": "EVIDENCE",
      "reason": "S210 E1 (2026-09-21; SYM-147's row-level loss, B36's next cut — the duplicated-figure tell): `fidelity.convert.numbers` (audit_numbers) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). The layer's number tokens the blocks lack, summed — dropped figures and figures the converter mis-read into non-tokens (63,242 as 62 242)."
    },
    "converter:pages_with_extra": {
      "disposition": "EVIDENCE",
      "reason": "S210 E1 (2026-09-21; SYM-147's row-level loss, B36's next cut — the duplicated-figure tell): `fidelity.convert.numbers` (audit_numbers) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). Pages with at least one extra copy — the pages to open first."
    },
    "converter:extra": {
      "disposition": "EVIDENCE",
      "reason": "S210 E1 (2026-09-21; SYM-147's row-level loss, B36's next cut — the duplicated-figure tell): `fidelity.convert.numbers` (audit_numbers) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). On each worst page: the extra copies there."
    },
    "converter:missing": {
      "disposition": "EVIDENCE",
      "reason": "S210 E1 (2026-09-21; SYM-147's row-level loss, B36's next cut — the duplicated-figure tell): `fidelity.convert.numbers` (audit_numbers) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). On each worst page: the layer's tokens missing there — the two readings of one page side by side."
    },
    "converter:specimens": {
      "disposition": "EVIDENCE",
      "reason": "S210 E1 (2026-09-21; SYM-147's row-level loss, B36's next cut — the duplicated-figure tell): `fidelity.convert.numbers` (audit_numbers) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks). On each worst page: up to six of the extra tokens, most-duplicated first — the evidence beside the count."
    },
    "converter:pages_with_missing": {
      "disposition": "EVIDENCE",
      "reason": "S210 E2 (2026-09-21; SYM-154's next cut — Scotia Q3 p.51's second `Secured funding` row dropped whole from a rendered table): `fidelity.convert.numbers` (audit_numbers) — REPORT-ONLY beside survival, unseen by compute_verdict. Pages whose missing figures reach NUMBERS_MISSING_MIN (3) — the pages that enter `worst` on their missing side alone, so a page that lost a row is listed even when nothing was duplicated."
    },
    "converter:missing_rows": {
      "disposition": "EVIDENCE",
      "reason": "S210 E2 (2026-09-21; SYM-154's next cut — Scotia Q3 p.51's second `Secured funding` row dropped whole from a rendered table): `fidelity.convert.numbers` (audit_numbers) — REPORT-ONLY beside survival, unseen by compute_verdict. On each worst page: the layer's ROWS that lost figures — the label on the figure's own band (the page's geometry, pymupdf words) or, without the page, the words before the first figure on the layer's line — most figures lost first, at most NUMBERS_ROWS (4): the reader is pointed at the row, not only the page."
    },
    "converter:row": {
      "disposition": "EVIDENCE",
      "reason": "S210 E2 (2026-09-21; SYM-154's next cut — Scotia Q3 p.51's second `Secured funding` row dropped whole from a rendered table): `fidelity.convert.numbers` (audit_numbers) — REPORT-ONLY beside survival, unseen by compute_verdict. A lost row's label (`Secured funding`; `Cash and cash equivalents at end`)."
    },
    "converter:missing_years": {
      "disposition": "EVIDENCE",
      "reason": "S210 E1 (2026-09-21): the numbers measure's year split — a bare four-digit year (1900–2099) the blocks lack is a running head Marker drops (Scotia's 212 missing were mostly its head's 2026), counted here apart from missing_total so a missing FIGURE reads as one; REPORT-ONLY, unseen by compute_verdict."
    },
    "converter:cell_agree_floor": {
      "disposition": "EVIDENCE",
      "reason": "S209 E11 (2026-09-20; the frontier's item 1, the bank tables): the structural-table measure `fidelity.convert.tables` (table_shape.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks handed in). The floor on Marker's non-empty cells found in the witness's joined text (CELL_AGREE); below it the witness is none — a constant that declares itself beside the counts it gates."
    },
    "converter:tables_witnessed_lines": {
      "disposition": "EVIDENCE",
      "reason": "S209 E11 (2026-09-20; the frontier's item 1, the bank tables): the structural-table measure `fidelity.convert.tables` (table_shape.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks handed in). Tables whose geometry the lines strategy read with agreeing cells and a credible shape — the witness for rows AND columns (ruled statements)."
    },
    "converter:tables_witnessed_text": {
      "disposition": "EVIDENCE",
      "reason": "S209 E11 (2026-09-20; the frontier's item 1, the bank tables): the structural-table measure `fidelity.convert.tables` (table_shape.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks handed in). Tables the text strategy found with agreeing cells — a witness that a table is there, no number taken from it (it over-splits both ways)."
    },
    "converter:tables_disagree": {
      "disposition": "EVIDENCE",
      "reason": "S209 E11 (2026-09-20; the frontier's item 1, the bank tables): the structural-table measure `fidelity.convert.tables` (table_shape.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks handed in). Tables whose geometry disagreed with Marker's cells or shape (rows far off, narrower than Marker's width: RBC's sparse rulings) — no witness, counted apart so a zero never reads as clean."
    },
    "converter:tables_unread": {
      "disposition": "EVIDENCE",
      "reason": "S209 E11 (2026-09-20; the frontier's item 1, the bank tables): the structural-table measure `fidelity.convert.tables` (table_shape.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks handed in). Tables the library could not read (no geometry either way, a page past the document, the scan lane) — UNREAD, never a negative."
    },
    "converter:columns_lost": {
      "disposition": "EVIDENCE",
      "reason": "S209 E11 (2026-09-20; the frontier's item 1, the bank tables): the structural-table measure `fidelity.convert.tables` (table_shape.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks handed in). The columns the lines witness has that Marker's table does not, summed over witnessed tables — the reader's number (NBC Q3 2026: 77 over 19 pages)."
    },
    "converter:columns_gained": {
      "disposition": "EVIDENCE",
      "reason": "S209 E11 (2026-09-20; the frontier's item 1, the bank tables): the structural-table measure `fidelity.convert.tables` (table_shape.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks handed in). The columns Marker's table has beyond the lines witness's — the document-level tell of partial rulings (RBC 176), kept beside columns_lost so the witness's own reliability is readable."
    },
    "converter:rows_lost": {
      "disposition": "EVIDENCE",
      "reason": "S209 E11 (2026-09-20; the frontier's item 1, the bank tables): the structural-table measure `fidelity.convert.tables` (table_shape.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks handed in). The rows the lines witness has that Marker's table does not, summed over witnessed tables."
    },
    "converter:pages_with_columns_lost": {
      "disposition": "EVIDENCE",
      "reason": "S209 E11 (2026-09-20; the frontier's item 1, the bank tables): the structural-table measure `fidelity.convert.tables` (table_shape.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks handed in). Pages carrying at least one witnessed table with a lost column — the population beside columns_lost (docs/34)."
    },
    "converter:shapes": {
      "disposition": "EVIDENCE",
      "reason": "S209 E11 (2026-09-20; the frontier's item 1, the bank tables): the structural-table measure `fidelity.convert.tables` (table_shape.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks handed in). On each worst page: the witness's rows×cols against Marker's rows×width per table (`25x7->26x7`; `text->21x11` for the text witness) — the specimen beside the count."
    },
    "converter:reason": {
      "disposition": "EVIDENCE",
      "reason": "S209 E11 (2026-09-20; the frontier's item 1, the bank tables): the structural-table measure `fidelity.convert.tables` (table_shape.py) — REPORT-ONLY beside survival, unseen by compute_verdict; None = not measured (no blocks handed in). Present when every table reads unread: why (the scan lane's layer is no witness for geometry; the source could not be opened)."
    },
    "converter:words_reorder": {
      "disposition": "EVIDENCE",
      "reason": "S209 E10 (2026-09-20; SYM-138): the words inside the runs runs_reorder counts — the size of what the rescue would give back, beside the run's own words (docs/34: a count says its population). Report-only, as runs_reorder."
    },
    "converter:reorder": {
      "disposition": "EVIDENCE",
      "reason": "S209 E10 (2026-09-20; SYM-138): on each run of the analyst block — True when the run's words all occur in one span of the output as a bag (a reordering), False when a word is missing anywhere (an omission), None on the CJK path (not re-tested; unread, never a negative). Report-only; the verdict reads the run's words, not this flag."
    },
    "converter:worst_capped_at": {
      "disposition": "EVIDENCE",
      "reason": "NUM-3: names the [:10] exemplar cap beside blocks_total in degeneration_detail — same declared-cap discipline."
    },
    "converter:table_rows_stripped": {
      "disposition": "EVIDENCE",
      "reason": "J29 / SYM-067 (signed Rab 2026-09-04): the number of markdown pipe-table rows blanked before degeneration() measured the body, beside blocks_total in degeneration_detail (fidelity_audit.py). docs/34: a gate that excludes something says how much it excluded. Renderers read flagged/worst; the count is record-keeping like worst_capped_at."
    },
    "converter:tokens_prompt_total": {
      "disposition": "EVIDENCE",
      "reason": "NUM-6 (census N-026): aggregated backend prompt tokens, manifest.analyst — forensic record beside goodput; tokens_prompt_counted_calls names its partial-sum denominator."
    },
    "converter:tokens_output_total": {
      "disposition": "EVIDENCE",
      "reason": "NUM-6: aggregated backend output tokens, manifest.analyst."
    },
    "converter:tokens_accepted_output": {
      "disposition": "EVIDENCE",
      "reason": "NUM-6: goodput's numerator (THIS-run accepted chunks only, per goodput_conditions), manifest.analyst + implicit in the analyst/done goodput field."
    },
    "converter:goodput_conditions": {
      "disposition": "EVIDENCE",
      "reason": "NUM-6: the docs/34 conditions string riding manifest.analyst AND the analyst/done event so the rate never travels bare."
    },
    "converter:tokens_counted_calls": {
      "disposition": "EVIDENCE",
      "reason": "NUM-6 (review M5): the output-counter denominator — how many paid calls actually reported eval_count; manifest.analyst."
    },
    "converter:tokens_prompt_counted_calls": {
      "disposition": "EVIDENCE",
      "reason": "NUM-6 (review M5): the prompt-counter denominator (ollama omits prompt_eval_count on fully cached prefills, so the prompt sum is a partial total over exactly this many calls); manifest.analyst."
    },
    "bench:unseen": {
      "disposition": "GLASS",
      "reason": "M6-R1 typed completeness field (57a5da6, Codex lane). RENDERED SERVER-SIDE: bench.py:1826 gates rescore eligibility on it and bench.py:1834-1835 sums partial-count unseen into the refusal text the operator reads. The detector is lexical over renderer files and cannot see producer-side string composition - the sentence this key feeds IS on the surface. Signed by Fable at the S113 close (close-gate catch-up for a peer-lane commit, docs/29 same-commit rule); verification: both consumer sites read the key by name."
    },
    "bench:remedy": {
      "disposition": "GLASS",
      "reason": "M6-R1 A1 field - the refusal's named door. Folded into rendered operator strings at bench.py:1427 and bench.py:1847 (unread-count remedies joined into the message shown with the refusal). Server-side composition, same lexical-detector blind spot as bench:unseen. Provenance 57a5da6 (Codex lane); signed by Fable at the S113 close."
    },
    "bench:capped_at": {
      "disposition": "EVIDENCE",
      "reason": "The producer cap that applied when the completeness record was built (25 runs / 10 zones / display caps). No downstream consumer reads it today - measured: only the record constructor writes it, and the *_capped_at reads at bench.py:327/:332/:1858 are the MANIFEST'S input keys feeding the constructor, not this record field. Retained so a later reader of a stored record can tell which cap shaped it: audit-trail semantics. Promote to GLASS if a renderer ever shows caps. Provenance 57a5da6 (Codex lane); signed by Fable at the S113 close."
    },
    "bench:zones_total": {
      "disposition": "INTERNAL",
      "reason": "Raw total in the bench state dict (bench.py:513). The rendered projection of the same fact is the completeness record's label ('N of M' / the UNREAD phrase) shown by the chips; bench.html reads no zones_total by name (measured, 0 hits). Kept as the machine-readable twin for tests and API consumers (test_bench_page reads it). Provenance 57a5da6 (Codex lane); signed by Fable at the S113 close."
    },
    "bench:runs_total": {
      "disposition": "INTERNAL",
      "reason": "Raw total in the bench state dict (bench.py:519); same shape as bench:zones_total - rendered via the completeness label, kept raw for machine consumers. Provenance 57a5da6 (Codex lane); signed by Fable at the S113 close."
    },
    "bench:schema": {
      "disposition": "EVIDENCE",
      "reason": "OK-15 report envelope self-description ('fp-ok15-evidence/v1', bench.py:582): the version stamp a later reader needs to parse a stored evidence report. Provenance 77d0361 (Codex lane, OK-15); signed by Fable at the S113 close."
    },
    "bench:collected_at": {
      "disposition": "EVIDENCE",
      "reason": "OK-15 report provenance timestamp (bench.py:584): when the evidence collection ran - the audit-trail field of a stored report. Provenance 77d0361 (Codex lane, OK-15); signed by Fable at the S113 close."
    },
    "bench:remedies": {
      "disposition": "INTERNAL",
      "reason": "Aggregate deduped remedy list in the coverage payload (bench.py:1426-1427,1433). Each remedy ALREADY renders individually via the refusal composition at bench.py:1847; this list is the machine-readable duplicate for API consumers. Promote to GLASS if the UI ever shows the aggregate. Provenance 57a5da6 (M6-R1). Peer-lane key signed by Fable at the S113 close (close-gate catch-up); Codex may re-class next sitting."
    },
    "bench:original_zones_shown": {
      "disposition": "EVIDENCE",
      "reason": "rescore_preview's before-count (bench.py:1872): the shown-zone count at preview time, kept so a reader can verify the preview's delta claim against the current recount beside it. bench.html renders the recommendation, not this raw pair (measured: 0 hits). Provenance 57a5da6 (M6-R1). Peer-lane key signed by Fable at the S113 close (close-gate catch-up); Codex may re-class next sitting."
    },
    "bench:original_zones_total": {
      "disposition": "EVIDENCE",
      "reason": "rescore_preview's before-total twin (bench.py:1873), same audit purpose as bench:original_zones_shown. Provenance 57a5da6 (M6-R1). Peer-lane key signed by Fable at the S113 close (close-gate catch-up); Codex may re-class next sitting."
    },
    "bench:sha256": {
      "disposition": "EVIDENCE",
      "reason": "OK-15 report envelope source binding (bench.py:585): the source PDF sha the collector verified before and after collection - the field that makes a stored evidence report attributable to exact bytes. Provenance 77d0361 (OK-15). Peer-lane key signed by Fable at the S113 close (close-gate catch-up); Codex may re-class next sitting."
    },
    "widget:mtime_ns": {
      "disposition": "INTERNAL",
      "reason": "Queue row ordering key (line.rs:148, conveyor state): filename-head dispatch sorts by it; the RENDERED projection is the row order itself plus wait_s. No JS reads it by name (measured, 0 hits in main.js/room.js). Provenance f045a66 (Conveyor State). Peer-lane key signed by Fable at the S113 close (close-gate catch-up); Codex may re-class next sitting."
    },
    "widget:intake_card_state": {
      "disposition": "INTERNAL",
      "reason": "Conveyor diagnostic in the line payload (line.rs:319): the card-mutex state feeding the positive-convert-claim rule (receipt + mutex busy + matching active). The operator-facing projection is intake_state/phase+age; this raw field stays machine-readable for debugging. Provenance f045a66. Peer-lane key signed by Fable at the S113 close (close-gate catch-up); Codex may re-class next sitting."
    },
    "widget:convert_progress_schema": {
      "disposition": "INTERNAL",
      "reason": "Schema version guard (line.rs:331) so consumers can branch on progress v1 vs v2; a version stamp, never an operator surface. Provenance f045a66. Peer-lane key signed by Fable at the S113 close (close-gate catch-up); Codex may re-class next sitting."
    },
    "widget:writer_pid": {
      "disposition": "INTERNAL",
      "reason": "The stop-targeting key (line.rs:601 in .intake-state.json; :697 in a test fixture): watcher stop kills the REAL receipt-writer PID and verifies descendant death (Codex S113 record). Feeds the stop computation; never a surface. Provenance f045a66. Peer-lane key signed by Fable at the S113 close (close-gate catch-up); Codex may re-class next sitting."
    },
    "widget:card_state": {
      "disposition": "INTERNAL",
      "reason": "Field of .intake-state.json (line.rs:602), declared watcher-only atomic state by its own design (S113 Codex record); malformed/stale reads render as UNREAD upstream. Provenance f045a66. Peer-lane key signed by Fable at the S113 close (close-gate catch-up); Codex may re-class next sitting."
    },
    "widget:frac": {
      "disposition": "INTERNAL",
      "reason": "Hardcoded v2 progress sample in line.rs's test fixture writer (line.rs:698); the LIVE producer is the converter and the live frac renders through convert_frac (line.rs:329 projection). Test scaffolding, not a surface. Peer-lane key signed by Fable at the S113 close (close-gate catch-up); Codex may re-class next sitting."
    },
    "widget:attempts": {
      "disposition": "INTERNAL",
      "reason": "Same test fixture writer (line.rs:698); the live attempts value renders in room.js:352 as 'attempt N/M'. Test scaffolding twin of a rendered live field. Peer-lane key signed by Fable at the S113 close (close-gate catch-up); Codex may re-class next sitting."
    },
    "converter:lever_batch": {
      "disposition": "EVIDENCE",
      "reason": "OK-17's honesty field in the slice .done record (convert_and_ship.py:1187): the LEVER'S asked batch beside the ACTUAL batch used, recorded because the ladder can run a slice at 4 while the lever said 8 and every record used to say 8. Resume/audit trail; the rendered batch story is the slice event's batch field. Fable lane, 3d6775b."
    },
    "converter:latex_balance": {
      "disposition": "EVIDENCE",
      "reason": "SYM-056 (S109; semantic count corrected cross-vendor by MSG-CDX-0014, 2026-08-27 - 61 unmatched opens, not the strict-literal 60): the per-environment \\begin/\\end balance block, produced at fidelity_audit.py:373 and riding the convert block at :497 BESIDE the tripwires, deliberately not among them. REPORT-ONLY BY SIGNATURE (Rab, 2026-09-03, S114 Ticket E): it never fails the line, never changes the markdown, never changes a verdict - compute_verdict cannot see the key, and latex_balance_selftest.py L6 is the tripwire that keeps that true (watched failing: wiring it into compute_verdict turns a 61-unterminated body from pass to fail, 3 red). HOME: manifest fidelity.convert.latex_balance. docs/29 5.3 step 4 walked and answered honestly: NO live surface reads it today. The intended reader is the Repair Bench - an unterminated environment plus its line is exactly a repair zone (error-structure protocol: reason = the env and its counts, highlight = the line) - and THAT WIRING IS NOT BUILT. Named as residue in the S114 Ticket E report rather than implied. Nothing renders it yet, and nothing should gate on it."
    },
    "converter:inventions": {
      "disposition": "EVIDENCE",
      "reason": "S209 E8 (2026-09-20), register row B35 — THE AUDIT'S BLIND SIDE: fidelity_audit.audit_inventions (the words Marker's page-labelled blocks carry that the source's own text layer does not — a re-OCR'd clean page invents; RBC Q3 0.25 % of ten pages' words, CIBC 0 as the control), written by audit_convert into fidelity.convert.inventions BESIDE survival, unseen by compute_verdict (report-only; the threshold is Rab's). Its home is the manifest and the record that reads it (sessions/S209 E5, E8; the sourcing line's post-mortem); no renderer projects it yet — a card row is the next cut once the number has a signed threshold, and this entry retires then."
    },
    "converter:pages_witness_blank": {
      "disposition": "EVIDENCE",
      "reason": "S209 E8 (2026-09-20), B35, inside fidelity.convert.inventions (fidelity_audit.py audit_inventions, written by audit_convert; report-only, unseen by compute_verdict): the pages whose witness had fewer than PAGE_MIN_WORDS words (a chart baked into an image) — not judged, counted apart: Marker's words there are its OCR of the picture, text the layer never had (the SEU's p.73: 74 of 77 'invented' before this line was drawn). Home: the manifest and the records that read it; no renderer yet — retires with converter:inventions when a card row exists."
    },
    "converter:blank_marker_words": {
      "disposition": "EVIDENCE",
      "reason": "S209 E8 (2026-09-20), B35, inside fidelity.convert.inventions (fidelity_audit.py audit_inventions, written by audit_convert; report-only, unseen by compute_verdict): how many Marker words sit on those unjudged pages — the size of what the measure declines to call invented. Home: the manifest and the records that read it; no renderer yet — retires with converter:inventions when a card row exists."
    },
    "converter:latex_commands": {
      "disposition": "EVIDENCE",
      "reason": "S209 E12 (2026-09-20; Waterloo, a PhD thesis): the LaTeX commands inside Marker's <math> elements, counted apart by audit_inventions — an equation is neither an invented word nor a lost one (the layer has glyphs, Marker writes \\hat, \\boldsymbol, \\frac): 1,810 of 32,843 Marker words had read invented on 147 pages, every worst page an equation page. Report-only beside invented_total; the reader sees how much of the document is equations."
    },
    "converter:pages_measured": {
      "disposition": "EVIDENCE",
      "reason": "S209 E8 (2026-09-20), B35, inside fidelity.convert.inventions (fidelity_audit.py audit_inventions, written by audit_convert; report-only, unseen by compute_verdict): the count of witness pages that had Marker blocks — the base every ratio below stands on (docs/34: the denominator rides beside the numerator). Home: the manifest and the records that read it; no renderer yet — retires with converter:inventions when a card row exists."
    },
    "converter:invented_total": {
      "disposition": "EVIDENCE",
      "reason": "S209 E8 (2026-09-20), B35, inside fidelity.convert.inventions (fidelity_audit.py audit_inventions, written by audit_convert; report-only, unseen by compute_verdict): the words Marker's blocks carry that the witness page does not, summed over the measured pages — the numerator of the audit's blind side. Home: the manifest and the records that read it; no renderer yet — retires with converter:inventions when a card row exists."
    },
    "converter:marker_words_total": {
      "disposition": "EVIDENCE",
      "reason": "S209 E8 (2026-09-20), B35, inside fidelity.convert.inventions (fidelity_audit.py audit_inventions, written by audit_convert; report-only, unseen by compute_verdict): the words in Marker's blocks over the measured pages — invented_total's base. Home: the manifest and the records that read it; no renderer yet — retires with converter:inventions when a card row exists."
    },
    "converter:invented_ratio": {
      "disposition": "EVIDENCE",
      "reason": "S209 E8 (2026-09-20), B35, inside fidelity.convert.inventions (fidelity_audit.py audit_inventions, written by audit_convert; report-only, unseen by compute_verdict): invented_total ÷ marker_words_total (None when nothing was measured, never 0) — the one number a card row will show once Rab signs its threshold. Home: the manifest and the records that read it; no renderer yet — retires with converter:inventions when a card row exists."
    },
    "converter:lost_total": {
      "disposition": "EVIDENCE",
      "reason": "S209 E8 (2026-09-20), B35, inside fidelity.convert.inventions (fidelity_audit.py audit_inventions, written by audit_convert; report-only, unseen by compute_verdict): the witness's words Marker's blocks lack, summed — survival's own view riding beside the inventions so the two are never confused. Home: the manifest and the records that read it; no renderer yet — retires with converter:inventions when a card row exists."
    },
    "converter:witness_words_total": {
      "disposition": "EVIDENCE",
      "reason": "S209 E8 (2026-09-20), B35, inside fidelity.convert.inventions (fidelity_audit.py audit_inventions, written by audit_convert; report-only, unseen by compute_verdict): the witness pages' words over the measured pages — lost_total's base. Home: the manifest and the records that read it; no renderer yet — retires with converter:inventions when a card row exists."
    },
    "converter:pages_with_inventions": {
      "disposition": "EVIDENCE",
      "reason": "S209 E8 (2026-09-20), B35, inside fidelity.convert.inventions (fidelity_audit.py audit_inventions, written by audit_convert; report-only, unseen by compute_verdict): how many measured pages carried at least one invented word — the spread (one garbled page or every page). Home: the manifest and the records that read it; no renderer yet — retires with converter:inventions when a card row exists."
    },
    "converter:bars": {
      "disposition": "EVIDENCE",
      "reason": "S209 E8 (2026-09-20), B33's dial, RECORD-ONLY (convert_and_ship.py ocr_dial / _note_ocr_bar; the converted event, the manifest's `ocr` block, the ledger row; estimate_from_ledger names heavy neighbours and excludes none until Rab signs the threshold): the totals of every 'Recognizing Text' bar Marker showed in the process, in order — the raw reading behind ocr_lines (a bar re-run over the same lines double counts, and this list is how a reader sees it). Home: the manifest and the ledger; no renderer yet."
    },
    "converter:lines_per_page": {
      "disposition": "EVIDENCE",
      "reason": "S209 E8 (2026-09-20), B33's dial, RECORD-ONLY (convert_and_ship.py ocr_dial / _note_ocr_bar; the converted event, the manifest's `ocr` block, the ledger row; estimate_from_ledger names heavy neighbours and excludes none until Rab signs the threshold): ocr_lines ÷ pages — the per-page density the estimator reads from a ledger row to name a re-OCR'd neighbour (RBC 132, C-31 64, the SEU 5, CIBC 0). Home: the manifest's ocr block, the ledger row's ocr_lines_per_page; no renderer yet."
    },
    "converter:ocr_heavy_neighbours": {
      "disposition": "EVIDENCE",
      "reason": "S209 E8 (2026-09-20), B33's dial, RECORD-ONLY (convert_and_ship.py ocr_dial / _note_ocr_bar; the converted event, the manifest's `ocr` block, the ledger row; estimate_from_ledger names heavy neighbours and excludes none until Rab signs the threshold): how many of the estimate's neighbours were re-OCR'd runs (> OCR_HEAVY_LINES_PER_PAGE); named on the estimate event and printed as ESTIMATE WARN — the promise still carries their pace (the SEU's 6.29 promised vs 1.52 measured). Home: the estimate event and the watcher log; a card row after his signature."
    },
    "converter:unterminated_total": {
      "disposition": "EVIDENCE",
      "reason": "SYM-056: the NUMERATOR of converter:latex_balance - \\begin tokens left unmatched, summed over environments (fidelity_audit.py:431). docs/34 forbids a bare count, so it travels with its denominator begins_seen and never alone. Same report-only signature, same intended reader (Repair Bench, unwired) as converter:latex_balance; see that entry for the full step-4 walk."
    },
    "converter:begins_seen": {
      "disposition": "EVIDENCE",
      "reason": "SYM-056: the DENOMINATOR of converter:latex_balance - \\begin tokens seen in the body as audited, balanced ones included (fidelity_audit.py:431). Without it '61' is a number with no population, which is the SYM-066 shape. NOTE, on the record because a later reader will look for them: the per-environment detail keys (begin / end / unterminated / stray_end / line) are NOT signed here because the glass detector cannot see them - they are assigned through a Subscript target (envs[env] = {...}), which its own docstring names as invisible to the harvester. A disposition for a key no producer harvests would arm a permanent stale-signature red on the close gate (NUM-7 precedent, review C1 2026-08-31), so they are named as residue in the S114 Ticket E report instead. If a refactor ever moves that dict into the returned literal, those five keys need entries the same day."
    },
    "converter:blocks_engine": {
      "disposition": "EVIDENCE",
      "reason": "J24 (S114, 2026-09-01): names WHICH engine produced the block records that ride beside the bundle (marker_blocks.py sidecar over marker-pdf 1.10.2). It is provenance for the blocks.json file, the same class as converter:runs_capped_at - a record that declares its own producer. Written into .done as FORENSIC ONLY and deliberately excluded from _done_identity_mismatch (convert_and_ship.py, _convert_chunked ~1395): adding it would declare every pre-J24 cached slice stale and re-pay a 3,834 s convert to collect an ADDITION. No renderer reads it BY DESIGN - a human acts on blocks.json's contents (page + bbox), never on the engine name. Renamed from 'engine' at build time because that key already means the converter (marker) in the manifest. Signed by Fable 2026-09-02 at the same sitting the detector named it; steps 1-4 of docs/29 section 5.3 walked: no live home, no archival home beyond the manifest itself, and a human would not act on it."
    },
    "converter:extraction": {
      "disposition": "EVIDENCE",
      "reason": "S210 E2 (2026-09-21; SYM-155 — Marker discards the text layer of 49–72 % of RBC's pages and the bundle never said so): `blocks.extraction` (marker_blocks.extraction_of, per slice; summed by merge_block_records; copied whole into the manifest's blocks block by summarize) — REPORT-ONLY; None on a pre-S210 cached slice (UNREAD, never 0). The block: which extractor Marker kept per page, read from each page's own text_extraction_method."
    },
    "converter:fixes": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z — the fixes proved on copies in S210 as VARIANTS, every failed version kept, a whitelist by the verdicts and the lessening of errors, faithful): `fixes` — the literal names of the fixes the lever (roots.json `fixes`, `for: <drop file name>` then one name per line) applied to THIS job ([] when none): overlap-fraction-gate, charbox-lift, offpage-clip, lane-share-rule, degen-rule-line, table-batch-ceiling, ligature-repair. Written by convert() into the manifest and by marker_blocks into the blocks record; read by variants.py (a variant's identity) and by whoever reads the manifest. REPORT of what ran — it moves no verdict itself; the fixes it names change how Marker read the document, and the measures say by how much."
    },
    "converter:fixes_stats": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z — the fixes proved on copies in S210 as VARIANTS, every failed version kept, a whitelist by the verdicts and the lessening of errors, faithful): `fixes_stats` — the pdftext wrapper's counters for the job (chars_seen, chars_dropped by the off-page clip, chars_lifted by the char-box lift), as the marker_blocks child recorded them; None when the record has no key (a stock run, or a chunked book — UNREAD, never 0). docs/34: a fix that does something says how much."
    },
    "converter:chars_seen": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z — the fixes proved on copies in S210 as VARIANTS, every failed version kept, a whitelist by the verdicts and the lessening of errors, faithful): fixes_stats.chars_seen — every char the wrapper saw."
    },
    "converter:chars_dropped": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z — the fixes proved on copies in S210 as VARIANTS, every failed version kept, a whitelist by the verdicts and the lessening of errors, faithful): fixes_stats.chars_dropped — chars outside the page box the off-page clip dropped before dedup (SYM-160: C-30 dropped 131,082 on the proof, its 36 glued pages → 0)."
    },
    "converter:chars_lifted": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z — the fixes proved on copies in S210 as VARIANTS, every failed version kept, a whitelist by the verdicts and the lessening of errors, faithful): fixes_stats.chars_lifted — loose char boxes whose top sat below their ink, lifted (SYM-155's zero ascent)."
    },
    "converter:ligature_repair": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z — the fixes proved on copies in S210 as VARIANTS, every failed version kept, a whitelist by the verdicts and the lessening of errors, faithful): `ligature_repair` — the ligature repair pass's record (SYM-148; the lever `ligature-repair`): the words whose dropped fi / fl / ff letters were restored from the source's own layer (a word absent from the layer with ONE ligature expansion in it), every repair listed. NOT the bench's `repairs` (a human's digest — Lane C caught the collision). {note: UNREAD…} when the pass could not run."
    },
    "converter:words_repaired": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z — the fixes proved on copies in S210 as VARIANTS, every failed version kept, a whitelist by the verdicts and the lessening of errors, faithful): ligature_repair.words_repaired — distinct words restored."
    },
    "converter:occurrences": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z — the fixes proved on copies in S210 as VARIANTS, every failed version kept, a whitelist by the verdicts and the lessening of errors, faithful): ligature_repair.occurrences — occurrences replaced across the body."
    },
    "converter:skipped_ambiguous": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z — the fixes proved on copies in S210 as VARIANTS, every failed version kept, a whitelist by the verdicts and the lessening of errors, faithful): ligature_repair.skipped_ambiguous — words with more than one expansion in the layer, left alone and named ({from, candidates}): the pass never guesses."
    },
    "converter:vocabulary_words": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z — the fixes proved on copies in S210 as VARIANTS, every failed version kept, a whitelist by the verdicts and the lessening of errors, faithful): ligature_repair.vocabulary_words — the layer vocabulary's size (the denominator the repair is judged against)."
    },
    "converter:candidates": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z — the fixes proved on copies in S210 as VARIANTS, every failed version kept, a whitelist by the verdicts and the lessening of errors, faithful): ligature_repair.skipped_ambiguous[].candidates — the expansions that competed."
    },
    "converter:ocr_font_spans": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z — the fixes proved on copies in S210 as VARIANTS, every failed version kept, a whitelist by the verdicts and the lessening of errors, faithful): probe()'s evidence `ocr_font_spans` — how many spans of the document are in an OCR font (the share's numerator beside total_spans; the stock rule trips on the FIRST such span — Scotia AR: 160 of 25,152 sent a whole report to the scan lane, SYM-156); the lever `lane-share-rule` decides by the share (fixes.SHARE 0.05). Recorded in the converted event beside ocr_font_trigger; read by lane_rule_census.py and whoever audits the lane."
    },
    "converter:ocr_dial": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; S210 CORRECTIONS row 11): `ocr_dial` — S209 E8's dial {lines, bars, lines_per_page}: what Marker chose to OCR on this run (the totals of every Recognizing Text bar), under its OWN key at last. From 2026-09-20 to S211 the dial was bound to `ocr`, which the lane's boolean rebinds a few lines later, so the manifest's `ocr` carried False and every ledger row's ocr_lines read None. The ledger row and the estimator read this key; no verdict does. ocr_dial_selftest 8/8 (the row carries the number; a boolean-only manifest reads None)."
    },
    "converter:pdftext_workers": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; CORRECTIONS row 5): `pdftext_workers` — Marker's OWN config key, returned by fixes.config_overrides and applied to the converter's config before it is built: 1 whenever a get_chars fix (offpage-clip, charbox-lift) is on, because pdftext reads a book over ten pages in spawned worker processes that import it fresh and unpatched (Bill C-30, 42 pp, re-OCR'd its 2,066 lines under the clip; C-288's five pages, in-process, kept every page). Not a manifest key: it is printed on the job's console ('fixes: config overrides …') and its effect is read in the manifest's blocks.extraction (pages kept) and fixes_stats (chars dropped / lifted)."
    },
    "converter:applied": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21): fixes.apply's return — the record of what a job's lever installed: `applied` — the fix names actually installed in this process (a subset of the lever's names is never silent: an unknown name refuses the lever)."
    },
    "converter:stats": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21): fixes.apply's return — the record of what a job's lever installed: `stats` — the wrapper's counters at install time (zeros); the LIVE counters go to the record as fixes_stats (fixes.stats())."
    },
    "converter:root": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `root` — anchor | held, from the bundle's own path."
    },
    "converter:words": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `words` — Marker's word total from the inventions measure (marker_words_total); None when the measure did not run."
    },
    "converter:survival_convert": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `survival_convert` — fidelity.convert.doc_survival (a faithfulness constraint: a candidate may not read lower than the baseline)."
    },
    "converter:survival_analyst": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `survival_analyst` — fidelity.analyst.doc_survival (a rank tie-breaker)."
    },
    "converter:inventions_total": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `inventions_total` — inventions.invented_total (a constraint: not more than the baseline's)."
    },
    "converter:words_lost": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `words_lost` — inventions.lost_total (a constraint: not more than the baseline's — no text removed)."
    },
    "converter:numbers_missing": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `numbers_missing` — numbers.missing_total (in the error sum)."
    },
    "converter:numbers_extra": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `numbers_extra` — numbers.extra_total (in the error sum)."
    },
    "converter:extraction_surya": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `extraction_surya` — pages Marker re-OCR'd (blocks.extraction.surya), so a variant's page-level gain is read from the registry."
    },
    "converter:assets": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `assets` — files under assets/ (a constraint: a candidate may not carry fewer — no image lost)."
    },
    "converter:path": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `path` — the bundle's absolute path (selected_dir resolves it without fp_paths)."
    },
    "converter:original": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `original` — the earliest converted_at of the sha (the first baseline)."
    },
    "converter:selected": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `selected` — the whitelisted variant's dir (the verdict rank, then the fewer errors, then survival, then the newest)."
    },
    "converter:refused": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `refused` — [{dir, violations, unread}] the unfaithful variants and why; UNREAD constraints named, never violations."
    },
    "converter:selected_at": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `selected_at` — UTC of the selection."
    },
    "converter:fixes_effective": {
      "disposition": "EVIDENCE",
      "reason": "S211 E1 (2026-09-21; CORRECTIONS row 5): the variant registry's `fixes_effective` — did the job's named get_chars fix (offpage-clip, charbox-lift) actually read the document: False when the wrapper's chars_seen is 0 (Bill C-30's ~160: pdftext's workers read the pages, the wrapper saw none), True when it saw chars, None when no get_chars fix was named or the record has no counters (UNREAD, never False). A reading beside the selection, never a constraint; read by whoever audits a variant (`variants.py show`)."
    },
    "converter:columns_lost_population": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): the tables measure's `columns_lost_population` — how many tables the column comparison actually covered (the LINES-witnessed tables, the only ones it is computed from); `columns_lost` reads None when this is 0 (RBC AR: 5 of 223; BMO: 0 of 159 — the zeros were unsupported). Read beside columns_lost by the registry (variants.summarize) and whoever audits a bundle."
    },
    "converter:rows_lost_population": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): `rows_lost_population` — identical in value and meaning to columns_lost_population: rows_lost is summed only inside the same lines-witness branch (the old meaning line's 'rows from both' was itself false — Lane A corrected the prose to the code); `rows_lost` reads None when this is 0."
    },
    "converter:tables_agree_population": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): `tables_agree_population` — how many tables BOTH witnesses (lines and text) covered: NOT derivable (the witness returns at the first strategy that finds a table), so the key is present and always None — a reader sees the question was asked and could not be answered, never a fabricated 0."
    },
    "converter:missing_in_figures_total": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): the numbers measure's `missing_in_figures_total` — figures the layer carries inside a Figure/Picture block's bbox that Marker's text lacks (a chart's axis ticks: RBC p.41's 100,000 … 250,000, p.58's ten), counted APART from missing_total, never folded in; None (UNREAD) when the blocks carry no figure box or no pdf_path was given. Report-only, unseen by compute_verdict. AMENDED S211 E3 (RBC p.122): unlabelled-band figures only (the ticks by construction); RBC AR re-measured: missing 22 · in figures 104 · labelled 14 (the capital table's rows, named)."
    },
    "converter:missing_in_figures": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): per worst page, the figures reclassified into the chart split (the page-level twin of missing_in_figures_total). AMENDED S211 E3 (RBC p.122): a figure inside a box counts as a tick ONLY when its band carries no row label inside that box — a partition by an observable, never a threshold; a labelled one stays under missing and is named under missing_in_figures_labelled. The label must lie inside the box: RBC p.41's ticks share their bands with the table's row labels printed outside the chart and are still ticks (inventions_selftest 26b/26c)."
    },
    "converter:missing_in_figures_labelled_total": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, the row-label gate — RBC p.122): the numbers measure's `missing_in_figures_labelled_total` — figures the layer carries inside a Figure/Picture box ON A LABELLED BAND (the band opens with words inside the same box): a table the layout model boxed as a picture (RBC p.122's capital table — Credit / Market / Operational, 14 figures) or a chart's labelled bar; the measure cannot tell which, so they STAY under missing_total (the loss the reader must see; the registry's error weight kept) and are named apart here. Containment alone read them as ticks — a conversion that boxed every table as a picture would have read 0 missing (S2's family). None (UNREAD) exactly when missing_in_figures_total is."
    },
    "converter:missing_in_figures_labelled": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, the row-label gate — RBC p.122): per worst page, the labelled figures inside a box on that page (the twin of the total; read beside missing_rows, which names their rows)."
    },
    "converter:numbers_missing_in_figures_labelled": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, the row-label gate — RBC p.122): the registry's reading of missing_in_figures_labelled_total, beside numbers_missing and numbers_missing_in_figures (variants.summarize)."
    },
    "converter:lost_hyphen_joined": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): the inventions measure's `lost_hyphen_joined` — witness words that are a line-end fragment (the last word on its raw line, trailing nothing, a hyphen or a soft hyphen) which, joined with the next witness word, spell a word Marker's blocks DO carry (Bill C-288: circons + tance → circonstance): Marker rejoined the layer's own line-wrap, nothing was lost. BOTH halves count (E3). lost_total keeps its old meaning."
    },
    "converter:lost_hyphen_joined_specimens": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): up to 6 specimens of lost_hyphen_joined: page, fragment, joined_with, joined — so a reader can see the join, not only its count."
    },
    "converter:joined_with": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): inside a lost_hyphen_joined specimen: the next witness word the fragment was joined with."
    },
    "converter:lost_total_excl_joined": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): `lost_total_excl_joined` = lost_total − lost_hyphen_joined: the honest count of what is still lost. The registry compares THIS between variants when both carry it (variants.faithful, rank)."
    },
    "converter:invented_hyphen_joined": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): `invented_hyphen_joined` — Marker's words that ARE such a rejoined pair (class joined, at a line-end fragment position): the same join read on the invented side (Bill C-30 ~160c: 167 invented on the layer path against 27 on the OCR path, which keeps the layer's breaks). invented_total keeps its old meaning."
    },
    "converter:invented_total_excl_joined": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): `invented_total_excl_joined` = invented_total − invented_hyphen_joined: the honest rest on the invented side; compared by the registry when both variants carry it."
    },
    "converter:inventions_excl_joined": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): the registry's reading of invented_total_excl_joined (None on a manifest audited before the key existed — UNREAD, never 0; faithful then reads the constraint UNREAD and names the side)."
    },
    "converter:words_lost_excl_joined": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): the registry's reading of lost_total_excl_joined — the same UNREAD rule."
    },
    "converter:numbers_missing_in_figures": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): the registry's reading of missing_in_figures_total, beside numbers_missing."
    },
    "converter:tied": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): the registry bucket's `tied` — the variants equal to the incumbent on every measured number (verdict rank, errors, survival_convert, survival_analyst): the incumbent stays selected, each tied one listed with its dir and note; converted_at never breaks a selection tie (Bill C-288's ~160b: equal numbers, a worse page, chosen as the newest under the retired rule). Printed by `variants.py show` and the ship line."
    },
    "converter:excluded_fields": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-22, the error accounting's finding on Waterloo's Kamalzadeh thesis — the reason read 'errors=2482 [None treated as 0: numbers_missing, numbers_extra]', so a variant whose numbers measure never ran ranked as having 0 missing figures): the registry bucket's `excluded_fields` — per candidate, the fields excluded from its pairwise comparison with the incumbent because one side reads None (never read as 0; the comparison runs on the fields both carry: verdict, the shared error sum on the honest rests, both survivals, degeneration). Named in the reason sentence too. Printed by `variants.py show`."
    },
    "converter:analyst_confounded": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-22, SYM-163 — McGill-1 ~148): the registry bucket's `analyst_confounded` — the candidates that beat the incumbent on convert survival and lost on the verdict alone, failed at the ANALYST phase (the local model's own sampling; three runs of exactly the threshold at page anchors): the incumbent stays, each such candidate named with its note so the reader sees the convert fix worked and what failed was the analyst's pass — an analyst re-run on the same Marker body owed. Printed by `variants.py show`."
    },
    "converter:blocks_occurrences": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-22, McGill-1 ~148): `ligature_repair.blocks_occurrences` — how many words the repair's decided map touched in the bundle's blocks record (the record the inventions measure counts), so the record and the shipped body describe the same words; the measure read IDENTICAL numbers on the repaired and unrepaired bundle before this. None (UNREAD) when the record is absent or unreadable — the body's repair stands either way."
    },
    "converter:blocks_note": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3: `ligature_repair.blocks_note` — the reason the blocks record could not be repaired (UNREAD: the exception's name), beside blocks_occurrences None."
    },
    "converter:note": {
      "disposition": "EVIDENCE",
      "reason": "S211 E3 (2026-09-21, Rab's word 23:1xZ — a measure may never assert a number it cannot support): a tied entry's `note` — the tie rule's sentence, verbatim: 'tied on every measured number — the incumbent stays; what no measure sees is not a reason to switch'."
    },
    "converter:violations": {
      "disposition": "EVIDENCE",
      "reason": "S211 (2026-09-21; Rab's word 19:05Z): the VARIANT REGISTRY variants.json (roots.json `variants`; variants.py) — every final bundle of a source sha, its fixes, verdict and measures, the SELECTED one and the refused with their reasons; nothing deleted. Read by convert_and_ship._register_variant (prints the selection), by PORTAL's lookup (the tenant's _find_bundle consults selected_dir first) and by whoever audits a re-run. A flat copy of manifest measures, keyed by LITERAL names: `refused[].violations` — the faithfulness constraints a variant broke, each with both numbers."
    },
    "converter:pdftext": {
      "disposition": "EVIDENCE",
      "reason": "S210 E2 (2026-09-21; SYM-155 — Marker discards the text layer of 49–72 % of RBC's pages and the bundle never said so): `blocks.extraction` (marker_blocks.extraction_of, per slice; summed by merge_block_records; copied whole into the manifest's blocks block by summarize) — REPORT-ONLY; None on a pre-S210 cached slice (UNREAD, never 0). Pages whose provider (text-layer) lines Marker kept."
    },
    "converter:surya": {
      "disposition": "EVIDENCE",
      "reason": "S210 E2 (2026-09-21; SYM-155 — Marker discards the text layer of 49–72 % of RBC's pages and the bundle never said so): `blocks.extraction` (marker_blocks.extraction_of, per slice; summed by merge_block_records; copied whole into the manifest's blocks block by summarize) — REPORT-ONLY; None on a pre-S210 cached slice (UNREAD, never 0). Pages Marker re-OCR'd because a page gate refused the layer (no lines, the OCR-error model, layout coverage, or the line-overlap test) — the RBC pattern as a number the bundle carries."
    },
    "converter:unread": {
      "disposition": "EVIDENCE",
      "reason": "S210 E2 (2026-09-21; SYM-155 — Marker discards the text layer of 49–72 % of RBC's pages and the bundle never said so): `blocks.extraction` (marker_blocks.extraction_of, per slice; summed by merge_block_records; copied whole into the manifest's blocks block by summarize) — REPORT-ONLY; None on a pre-S210 cached slice (UNREAD, never 0). Pages whose method Marker did not name."
    },
    "converter:pages_surya": {
      "disposition": "EVIDENCE",
      "reason": "S210 E2 (2026-09-21; SYM-155 — Marker discards the text layer of 49–72 % of RBC's pages and the bundle never said so): `blocks.extraction` (marker_blocks.extraction_of, per slice; summed by merge_block_records; copied whole into the manifest's blocks block by summarize) — REPORT-ONLY; None on a pre-S210 cached slice (UNREAD, never 0). The re-OCR'd pages, 1-based, so a reader is pointed at them (capped)."
    },
    "converter:pages_surya_capped_at": {
      "disposition": "EVIDENCE",
      "reason": "S210 E2 (2026-09-21; SYM-155 — Marker discards the text layer of 49–72 % of RBC's pages and the bundle never said so): `blocks.extraction` (marker_blocks.extraction_of, per slice; summed by merge_block_records; copied whole into the manifest's blocks block by summarize) — REPORT-ONLY; None on a pre-S210 cached slice (UNREAD, never 0). The cap on that list (docs/34: a cut says how much it cut)."
    },
    "converter:sources": {
      "disposition": "EVIDENCE",
      "reason": "J24 (S114, 2026-09-01), signed S210 E2 (2026-09-21) when marker_blocks.py joined the producers (its record was unseen glass for three weeks — the S7 family, PRODUCED-BUT-NEVER-PROJECTED): the blocks record (`blocks.json` beside the bundle; its summary in the manifest's `blocks` block) — REPORT-ONLY, read by the bench and the auditing readers. The per-slice source files the record was merged from (the slice names), so a partial merge names what it had."
    },
    "converter:page_info": {
      "disposition": "EVIDENCE",
      "reason": "J24 (S114, 2026-09-01), signed S210 E2 (2026-09-21) when marker_blocks.py joined the producers (its record was unseen glass for three weeks — the S7 family, PRODUCED-BUT-NEVER-PROJECTED): the blocks record (`blocks.json` beside the bundle; its summary in the manifest's `blocks` block) — REPORT-ONLY, read by the bench and the auditing readers. Marker's own page_info map (page sizes and ids) carried through the merge."
    },
    "converter:pages_with_blocks": {
      "disposition": "EVIDENCE",
      "reason": "J24 (S114, 2026-09-01), signed S210 E2 (2026-09-21) when marker_blocks.py joined the producers (its record was unseen glass for three weeks — the S7 family, PRODUCED-BUT-NEVER-PROJECTED): the blocks record (`blocks.json` beside the bundle; its summary in the manifest's `blocks` block) — REPORT-ONLY, read by the bench and the auditing readers. How many distinct pages carry at least one block — against `pages`, the coverage of the record."
    },
    "converter:page_min": {
      "disposition": "EVIDENCE",
      "reason": "J24 (S114, 2026-09-01), signed S210 E2 (2026-09-21) when marker_blocks.py joined the producers (its record was unseen glass for three weeks — the S7 family, PRODUCED-BUT-NEVER-PROJECTED): the blocks record (`blocks.json` beside the bundle; its summary in the manifest's `blocks` block) — REPORT-ONLY, read by the bench and the auditing readers. The lowest absolute page a block names (the merge's honesty check: slices are disjoint ranges)."
    },
    "converter:page_max": {
      "disposition": "EVIDENCE",
      "reason": "J24 (S114, 2026-09-01), signed S210 E2 (2026-09-21) when marker_blocks.py joined the producers (its record was unseen glass for three weeks — the S7 family, PRODUCED-BUT-NEVER-PROJECTED): the blocks record (`blocks.json` beside the bundle; its summary in the manifest's `blocks` block) — REPORT-ONLY, read by the bench and the auditing readers. The highest absolute page a block names."
    },
    "converter:page_field_raw_disagreements": {
      "disposition": "EVIDENCE",
      "reason": "J24 (S114, 2026-09-01), signed S210 E2 (2026-09-21) when marker_blocks.py joined the producers (its record was unseen glass for three weeks — the S7 family, PRODUCED-BUT-NEVER-PROJECTED): the blocks record (`blocks.json` beside the bundle; its summary in the manifest's `blocks` block) — REPORT-ONLY, read by the bench and the auditing readers. How often Marker's own `page` field disagreed with the id-derived page (expected ~every block on marker-pdf 1.10.2; a sudden 0 means upstream fixed json_to_chunks)."
    },
    "converter:page_field_note": {
      "disposition": "EVIDENCE",
      "reason": "J24 (S114, 2026-09-01), signed S210 E2 (2026-09-21) when marker_blocks.py joined the producers (its record was unseen glass for three weeks — the S7 family, PRODUCED-BUT-NEVER-PROJECTED): the blocks record (`blocks.json` beside the bundle; its summary in the manifest's `blocks` block) — REPORT-ONLY, read by the bench and the auditing readers. The sentence that says why `page` is corrected from the block id — carried so a reader of the file needs no source."
    },
    "converter:block_type": {
      "disposition": "EVIDENCE",
      "reason": "J24 (S114, 2026-09-01), signed S210 E2 (2026-09-21) when marker_blocks.py joined the producers (its record was unseen glass for three weeks — the S7 family, PRODUCED-BUT-NEVER-PROJECTED): the blocks record (`blocks.json` beside the bundle; its summary in the manifest's `blocks` block) — REPORT-ONLY, read by the bench and the auditing readers. Per block: Marker's block type (Text, Table, TableGroup, Figure, …) — what every measure keys on."
    },
    "converter:page_field_raw": {
      "disposition": "EVIDENCE",
      "reason": "J24 (S114, 2026-09-01), signed S210 E2 (2026-09-21) when marker_blocks.py joined the producers (its record was unseen glass for three weeks — the S7 family, PRODUCED-BUT-NEVER-PROJECTED): the blocks record (`blocks.json` beside the bundle; its summary in the manifest's `blocks` block) — REPORT-ONLY, read by the bench and the auditing readers. Per block: Marker's uncorrected `page` field, kept beside the corrected one so the correction is auditable."
    },
    "converter:section_hierarchy": {
      "disposition": "EVIDENCE",
      "reason": "J24 (S114, 2026-09-01), signed S210 E2 (2026-09-21) when marker_blocks.py joined the producers (its record was unseen glass for three weeks — the S7 family, PRODUCED-BUT-NEVER-PROJECTED): the blocks record (`blocks.json` beside the bundle; its summary in the manifest's `blocks` block) — REPORT-ONLY, read by the bench and the auditing readers. Per block: Marker's section path for the block (the heading chain)."
    },
    "converter:image_refs": {
      "disposition": "EVIDENCE",
      "reason": "J24 (S114, 2026-09-01), signed S210 E2 (2026-09-21) when marker_blocks.py joined the producers (its record was unseen glass for three weeks — the S7 family, PRODUCED-BUT-NEVER-PROJECTED): the blocks record (`blocks.json` beside the bundle; its summary in the manifest's `blocks` block) — REPORT-ONLY, read by the bench and the auditing readers. Per block: the asset file names the block's images resolve to (the bundle's assets/)."
    },
    "converter:timing_s": {
      "disposition": "EVIDENCE",
      "reason": "J24 (S114, 2026-09-01), signed S210 E2 (2026-09-21) when marker_blocks.py joined the producers (its record was unseen glass for three weeks — the S7 family, PRODUCED-BUT-NEVER-PROJECTED): the blocks record (`blocks.json` beside the bundle; its summary in the manifest's `blocks` block) — REPORT-ONLY, read by the bench and the auditing readers. The record's own cost per run — build_document, markdown_render, chunk_render seconds and pages — so the 'no second GPU pass' claim is re-measurable on every book (docs/34)."
    },
    "widget:program": {
      "disposition": "INTERNAL",
      "reason": "The analyst PROGRAM name (docs/13 'program slot': readability etc.) forwarded by line.rs:484 into the status json. It identifies WHICH prompt file re-tasked the fenced model; the human reads the analyst's counts and verdict, never the slot name. Pulled into --since scope by F3 (S114, 2026-09-03) because the inline path now emits it beside the resume path's identical key; pre-existing since S113. Rendering it is a widget change beyond F7's signature - carried as residue, not wired."
    },
    "converter:chunks_resumed": {
      "disposition": "EVIDENCE",
      "reason": "analyst.py:424 - chunks carried over from a prior run's journal (S61 resume). Census N-005 names it SILENCED ON EVERY HUMAN CHANNEL, and on the 2026-09-01 maiden voyage it was 641 of 957 (67 %) - the number that makes duration_s and chunks_passed honest together (N-007/N-013). F3 (S114) now emits it on analyst/done from BOTH paths so it reaches the event stream and the manifest; the Dock/Room do not yet render it - that is the N-005 repair and a signed widget change, carried as residue. Signed EVIDENCE at the same sitting the detector named it (docs/29 s5.4 same-commit rule)."
    },
    "converter:pages_flagged": {
      "disposition": "EVIDENCE",
      "reason": "fidelity_audit.py:473 (audit_convert) - the per-page LOCALIZER behind doc_survival: the list of source pages whose witness windows failed (241 of 1,372 scored on the 2026-09-01 maiden voyage). Pre-existing since docs/15; pulled into --since scope by SYM-056's latex_balance edit to the same function (S114, 91973a4). The COUNT reaches the glass through doc_survival and the verdict; the LIST is a drill-down, not an alarm - EEMUA 191 and docs/52 CONTRACT.md both put localizers in the Bench's page rail, never on the strip. The Bench does not yet read it (unwired; the natural home is bench.html's thumbnail rail). Signed at the same sitting the detector named it. Method note for the ritual: glass_detector --since diffs COMMITTED changes only, so a key an uncommitted edit brings into scope is invisible until the commit - run the detector AFTER committing, or it reads clean on work it cannot see."
    },
    "converter:normalisation": {
      "disposition": "EVIDENCE",
      "reason": "J32-A (docs/54-repair-road/README.md SS2, signed: Proposal A, 2026-09-05): audit_analyst's returned block now names EXACTLY which normalisation ladder ran (fidelity_audit.py's audit_analyst, docs/15 SS3/SS9.4) - {unescape, punct_free, space_free, regex_id}. HOME: manifest fidelity.analyst.normalisation (and, once J32-B's per-chunk survival guard reads the same ladder, analyst.py's chunk-level bookkeeping). The human reads doc_survival and the verdict; this sub-block is the audit trail proving WHICH regex pair produced them, so a future change to the ladder is a provable regex_id bump, never a silent drift in what near-exact means. Nothing renders it as its own surface today - report-only, like latex_balance."
    },
    "converter:unescape": {
      "disposition": "EVIDENCE",
      "reason": "J32-A: sub-key of converter:normalisation (fidelity_audit.py audit_analyst) - always True/False naming whether the unescape rung of the ladder ran. See converter:normalisation for the full walk; same HOME, same report-only signature."
    },
    "converter:punct_free": {
      "disposition": "EVIDENCE",
      "reason": "J32-A: sub-key of converter:normalisation - always True/False naming whether the punct-free rung ran. See converter:normalisation."
    },
    "converter:space_free": {
      "disposition": "EVIDENCE",
      "reason": "J32-A: sub-key of converter:normalisation - always True/False naming whether the final containment test was space-free (it always is, docs/15 SS3 step 9's unification of the CJK and word paths onto one rule). See converter:normalisation."
    },
    "converter:regex_id": {
      "disposition": "EVIDENCE",
      "reason": "J32-A: sub-key of converter:normalisation - the version tag for the exact regex pair (text_norm._UNESCAPE / _PUNCT) that produced this comparison, currently \"j32a-v1\". A future change to either regex must bump this string in the same commit - the docs/34 discipline of naming conditions applied to a MEASUREMENT METHOD rather than a number. See converter:normalisation."
    },
    "converter:rejections": {
      "disposition": "EVIDENCE",
      "reason": "J32-B/SYM-074 (signed Rab 2026-09-05): chunks_rejected's breakdown by reason - {fence, survival, think_leak, inflation} (the fourth bucket is J34, signed Rab 2026-09-05 '1.5x reject': output/input word ratio > ANALYST_CHUNK_INFLATION_MAX after survival passes; the shipped frontmatter gets a second derived line, rejections_inflation, under the same one-line rule) - produced by analyst.py:process (HOME: manifest.analyst.rejections, both analyst/done emits per T17's parity rule, and one derived line in the shipped frontmatter, rejections_survival, the coordinator's default 'yes, a human reads it without opening manifest.json'). Before this ticket chunks_rejected meant fence violations only; it now means ALL rejections and this key is the only way to recover the old fence-only meaning. Sums to chunks_rejected on every run except a resumed chunk from a journal written before either ticket, where the reason-less old record is attributed to 'fence' (the only reason that existed then) rather than dropped - see analyst.py's resume-branch comment. Not rendered as its own surface yet (report-only, like converter:normalisation); the Bench/Room render chunks_rejected today and would need a signed widget change to break it out."
    },
    "converter:chunk_scores": {
      "disposition": "EVIDENCE",
      "reason": "J41 (signed Rab 2026-09-09, S118, 'J41 = manifest chunk_scores'): the ticket's proposal for the S61 journal's per-chunk survival/ratio/reason columns, which previously died with shutil.rmtree(work_dir) at the end of every run that PASSED (S115 could only calibrate J32-B/J34 because the 2026-08-30 run had CRASHED and left an orphan journal behind, C:/Users/Bndit/ml/library/.analyst-work/d58db211c41b0e17/chunks.jsonl). Produced by analyst.py:process, HOME: manifest.analyst.chunk_scores ONLY - deliberately not on either analyst/done event emit (T17 pins that key set, kept lean) and not in the shipped frontmatter. One row per finished chunk, in chunk order, short keys on purpose: {i: 1-based chunk index, s: survival or absent, r: ratio or absent, x: reject reason or absent on a passed row} - roughly 20-40 bytes/chunk, so a 492-chunk book costs roughly 15-20 KB in manifest.json. Rows come from both the live loop (a backend-FAILED chunk gets {i, x:\"failed\"}, nothing else - no survival/ratio was ever computed) and the resume branch (an old-shape journal record with no survival/ratio key yields a row with those two keys absent, x defaulting to \"fence\" exactly as the existing resume branch already attributes a reason-less rejected record). Nothing renders it yet - report-only, same shape as converter:normalisation and converter:rejections; a future Bench/Room surface would need its own signed widget change."
    },
    "converter:edits": {
      "disposition": "EVIDENCE",
      "reason": "J46 (signed Rab 2026-09-12, S140, C+A): the diff-whitelist acceptor's record on manifest.analyst - edits ACCEPTED and REVERTED by class over the book (escape, link, markup, hyphen, ligature, reflow accepted; substitution, deletion, insertion, numeral, punctuation/case reverted) and the whitelist in force. Forensic evidence beside the audit: a book whose reverted deletion count is high was rewritten by the analyst and saved by the acceptor; measured on DDIA at promotion, after the S140 review (1,914 accepted / 1,397 reverted; survival 0.9822/24 shipped ladder, 0.9969/1 v3c). Not rendered yet - the bench is the natural home (a per-chunk e column beside survival) and is a separate ticket; the analyst/done event's key set stays pinned (T17), so this is manifest-only like chunk_scores."
    },
    "converter:chunks_reconciled": {
      "disposition": "EVIDENCE",
      "reason": "J46 (S140): inside manifest.analyst.edits - the number of PASSED chunks that lost at least one edit to the whitelist's revert; the denominator is chunks_passed. Evidence, same home as converter:edits."
    },
    "converter:geometry": {
      "disposition": "EVIDENCE",
      "reason": "S150 E3 (Rab 2026-09-14, 'build the new analyst, test that, and see what we gained, and lost'): the table-geometry LAYER's record on manifest.analyst - tables a renderer reads, proposals, applied / refused / unresolved, every label recovered (rows, the letters the OCR read, the word, how it was resolved), every caption lifted, stray bullet glyphs fixed, the invariant's checks, the resolver's calls, the grid program. None when the layer is off (ANALYST_TABLES, a lever-waiver: off in the pipeline until Rab's word). The detector reads the key as rendered by an incidental match on the word 'geometry' elsewhere - it is manifest-only like converter:edits, so this entry says so; the class's own counts ride converter:edits under the name table-geometry. The AFTER half of the S150 measurement is written by the act itself."
    },
    "converter:gpu_util_pct": {
      "disposition": "REPORT",
      "reason": "S146 E3: a field of _gpu_signature() - the three facts of the death certificate (timeout/stalled events carry **sig; the morning's kill row '100 %, 9,515 of 10,240 MiB' was read from them) and of the 30-s samples that drive the peak and the ceiling moment. Since S45; the detector sees a store now because the signature is built in a local dict before gpu_top is added. Reported through the events, read by the record and the Room's algedonic line."
    },
    "converter:gpu_mem_used_mib": {
      "disposition": "REPORT",
      "reason": "S146 E3: the card's resident total at the sample (nvidia-smi memory.used) - the death certificate's second fact and the input of _note_ceiling(); reported through the timeout/stalled events and the per-slice peak_mib. Twin of gpu_util_pct."
    },
    "converter:gpu_mem_total_mib": {
      "disposition": "REPORT",
      "reason": "S146 E3: the card's size at the sample - the denominator of the ceiling fraction (CEILING_FRACTION 0.80) and of every 'x of 10,240 MiB' sentence in the records; reported through the timeout/stalled events. Twin of gpu_util_pct."
    },
    "converter:mtime_before": {
      "disposition": "EVIDENCE",
      "reason": "S146 E2 (pipeline/audit-mode-provenance, SYM-131): a field of the pipeline/audit_mode_set event - the lever file's mtime before set_audit_mode() replaced it. The SYM-131 gap was a value with no writer and no time; the pair of mtimes proves the write replaced the exact file the reader had read and dates the previous value (the first row dated report at 2026-09-12 02:59:52Z). Read live by nobody; the record quotes it. Evidence, same home as the event row."
    },
    "converter:mtime_after": {
      "disposition": "EVIDENCE",
      "reason": "S146 E2 (pipeline/audit-mode-provenance, SYM-131): the twin of mtime_before - the lever file's mtime after the atomic replace, so a later reader can match the file on disk to the event that wrote it (mtime == mtime_after means no unrecorded write since). Read live by nobody; evidence, same home as the event row."
    },
    "converter:chunk_journal_dump": {
      "disposition": "EVIDENCE",
      "reason": "S141 (unread-surfaces/orphan-chunk-journal-dump-before-rmtree): manifest.analyst.chunk_journal_dump - the DUMPED id (or the UNREAD reason) of the chunk journal ledgered by dumps/dump.sh before the analyst rmtree's its work dir; dumps/ D0001 was this journal snapshotted by hand minutes before an rmtree, and SURF-12 (S140 second reading) found one orphaned 12 days. Evidence: a pointer to bytes + a ledger row, never a verdict."
    },
    "bench:placed_after_table": {
      "disposition": "EVIDENCE",
      "reason": "S149 (Rab's Obsidian find): a repair record's note that its lines were placed after a table's last row instead of directly after the zone's line, with the table's [first,last] rows — stored in manifest.repairs and on the ledger event so a reader of the record (and unsplit_tables) knows why the embed sits where it does. docs/29 §5.3 step 4: its home is the record itself and the ledger; the bench's ◫ view shows the outcome (the table whole) rather than the field."
    },
    "bench:at_line_orig": {
      "disposition": "EVIDENCE",
      "reason": "S149: the body line a repair's lines followed, in the zone's own numbering — the drift ledger (_adjusted_line) compares it instead of zone_line once an insertion is placed elsewhere (below a table). Read by the bench's own arithmetic on every zone lookup; stored in manifest.repairs; not a number a human acts on — its effect is the zone landing on the right line."
    },
    "bench:from_after_line": {
      "disposition": "EVIDENCE",
      "reason": "S149: on the unsplit-table ledger event, where a trapped repair block sat (after which line of the clean body) before the maintenance pass moved it — the pass prints the same list to the operator who ran it (--unsplit-tables) and the record's at_line_orig carries the destination; the event is the archival twin."
    },
    "bench:to_after_line": {
      "disposition": "EVIDENCE",
      "reason": "S149: on the unsplit-table ledger event, where the block went (after the table's last row, clean-body numbering) — printed by the maintenance pass at run time, kept on the event and in the record's at_line_orig; docs/29 §5.3 step 4: the record and the printed report are its homes."
    },
    "converter:sampler": {
      "disposition": "EVIDENCE",
      "reason": "S157 E6 (J49's mechanical half, the lane's own task under the autonomy experiment; the VALUES are Rab's word): manifest.analyst.sampler - the readability pass's sampling conditions as sent (temperature, seed, num_ctx, think) or, when nothing was sent (today: ANALYST_SAMPLER = {temperature: None, seed: None}), the note that the model's own Modelfile recipe applied (qwen3:8b under ollama 0.33.2 read temperature 0.6 / top_p 0.95 / top_k 20 at S119 R2 - Historical, not re-read by the pipeline). docs/34: every survival score in chunk_scores was decoded under these conditions and the record did not say so. Gemini: the request's pinned 0.2. Manifest-only like chunk_scores (the analyst/done event's key set stays pinned by T17); nothing renders it - evidence beside the audit, never a verdict."
    },
    "converter:backend_failures": {
      "disposition": "EVIDENCE",
      "reason": "S157 E17 (B12 / SYM-034, the lane's own task under the autonomy experiment): manifest.analyst.backend_failures - one row per failed generate call {i, error, detail, ollama_log}: the chunk, the exception's class and its first 120 chars, and the DUMPED id (or the UNREAD reason) of the tail of Ollama's own server.log captured at that moment and ledgered through dumps/dump.sh (`ollama-<i>-<ts>.log` beside the work dir). SYM-034's row said only the client side of a hung call existed; this is the server's half, taken when it happens. The Gemini route records the error only (n/a). Manifest-only like chunk_scores; nothing renders it; evidence beside the audit, never a verdict; an empty list is the honest reading of a run with no failures."
    },
    "converter:ollama_log": {
      "disposition": "EVIDENCE",
      "reason": "S157 E17 (B12 / SYM-034): the field inside each manifest.analyst.backend_failures row - the DUMPED id (or the UNREAD reason) of the tail of Ollama server.log captured at the failed call and ledgered through dumps/dump.sh; the bytes live under the analyst work root _captures/ and in the ledger; evidence for the next SYM-034, never rendered, never a verdict."
    },
    "converter:error": {
      "disposition": "EVIDENCE",
      "reason": "S157 E17 (B12): the field inside each backend_failures row - the exception CLASS of the failed generate call (TimeoutError, ConnectionRefusedError, RuntimeError from an ollama 5xx); the shape a stall wears on the client side, beside the server side (ollama_log). Manifest-only; evidence."
    },
    "converter:detail": {
      "disposition": "EVIDENCE",
      "reason": "S157 E17 (B12): the field inside each backend_failures row - the exception text, first 120 characters, as the client saw it. Manifest-only; evidence beside error and ollama_log."
    },
    "bench:delta": {
      "disposition": "INTERNAL",
      "reason": "S168 (2026-09-16), the close's GLASS row: `bench:delta` (prototypes/repair-bench/bench.py:1875, the collapse repair record's SIGNED line delta) surfaced as an unsigned glitch only because windows-converter/latex_structure.py added the generic string literal \"delta\" and added_keys_since matches key TEXT (the detector's own docstring: generic key names find an incidental match). The key HAS a home inside the bench: bench.py:1343 `_record_shift` reads `r.get(\"delta\")` to shift every later zone's line by a collapse's removed lines (the docstring there says why `delta`, not `lines`). HOME: the bundle manifest's repairs[] record, read back by the bench at every restart."
    }
  },
  "census_dispositions": {
    "_note": "docs/51 numeration rows, signed under NUM-7. Documentation-grade: the glass detector does NOT enforce these (its census is lane-key-based); enforcement wiring from docs/51 rows is named residue. 69 filed; 13 contested absent.",
    "census:N-006": {
      "disposition": "REPORT",
      "reason": "HOMED TODAY by NUM-6 (a524763): the honest per-run rate denominator is now named chunks_generated and rides both the analyst/done event and the manifest. HOME: analyst/done event + manifest.analyst.chunks_generated (convert_and_ship.py:1449-1451)"
    },
    "census:N-014": {
      "disposition": "INTERNAL",
      "reason": "A process-global pacing clock that exists only to space outbound requests — the same class as the already-signed bench:keep_alive entry — and no human would act on its value."
    },
    "census:N-015": {
      "disposition": "REPORT",
      "reason": "No home at all (it is folded into a RuntimeError string that is then swallowed at analyst.py:342-345), yet it is the only witness of real API request consumption and the number that falsifies FREE_TIER_WINDOW_CHUNKS, so the run's request total belongs in the analyst's own record. HOME: analyst/done event + manifest.analyst (beside chunks_generated, NUM-6's new fields)"
    },
    "census:N-023": {
      "disposition": "GLASS",
      "reason": "Its derived boolean already has a home on the Dock pre-flight card (main.js:313) but the MiB value and the -1 unknown sentinel reach no surface, so a failed probe renders as the positive claim 'GPU free'. HOME: Dock pre-flight card (windows-widget/src/main.js:313) — the MiB reading and an explicit UNREAD state for -1"
    },
    "census:N-024": {
      "disposition": "INTERNAL",
      "reason": "A configured constant, not a measurement — it belongs in source like the signed converter:slice_size entry — and the number that actually needs a home is the measured largest chunk emitted, which nothing computes."
    },
    "census:N-025": {
      "disposition": "INTERNAL",
      "reason": "An outbound request parameter sent to Ollama, the identical class as the already-signed bench:num_ctx disposition, and when its size matters it reaches the human as a failure rather than a number."
    },
    "census:N-026": {
      "disposition": "EVIDENCE",
      "reason": "HOMED TODAY by NUM-6 (a524763): prompt_eval_count/eval_count are aggregated instead of discarded and ride the book's manifest, with honest None when a backend reports no counters. HOME: manifest.analyst.tokens_prompt_total / tokens_output_total / tokens_accepted_output (analyst.py:427-430)"
    },
    "census:N-038": {
      "disposition": "GLASS",
      "reason": "The headline quantity of the vault lane is computed nowhere, while a surface already claims to show it — the widget's vault_count tile, which silently substitutes the local anchor count when the clone is unreachable (N-116/N-180). HOME: widget vault_count tile (vault.rs → Room/Wall), backed by a real walk of the bare repo's Inbox tree"
    },
    "census:N-039": {
      "disposition": "GLASS",
      "reason": "No home — Exporter.sweep() iterates the directory at exporter.py:147 without ever counting it — yet this is the pipeline's only true backlog signal and the honest denominator the restart-inflated `failed` and `supersede-held` receipt tallies lack. HOME: Room queue/KPI band, as a labelled staging-depth gauge distinct from drop/ queue depth"
    },
    "census:N-041": {
      "disposition": "REPORT",
      "reason": "It exists only as loop iterations with no counter variable, so a bundle that verified 1 of 40 files and one that verified all 40 emit the identical 'blob-verified' log line — the number belongs on the receipt that records the deletion gate's proof. HOME: the `exported` / `exported-supersede` receipt in receipts.jsonl (exporter.py:376-381, :525-532)"
    },
    "census:N-043": {
      "disposition": "REPORT",
      "reason": "It is the quantity that decides whether the vault is written at all, and all three branches log only qualitatively — the exported-supersede receipt carries reason and from_verdict but no magnitude, so a reader cannot tell a one-asset supersede from a two-hundred-path rewrite. HOME: the `exported-supersede` receipt in receipts.jsonl (exporter.py:525-532)"
    },
    "census:N-044": {
      "disposition": "INTERNAL",
      "reason": "It is a first-free-name search index whose only meaningful output — the chosen filename — already reaches the operator through status.json's `dest` field and the ALLOCATED log line, and publishing it as a count would assert a cumulative history it provably does not have."
    },
    "census:N-045": {
      "disposition": "INTERNAL",
      "reason": "Same first-free-name search index as the allocator's, already legible as the ` (n)` suffix in the published bundle name and the CONVERTED/REROUTED/REJECTED log lines; the defect worth fixing is that this meaningless ordinal becomes a permanent vault slug, not that the number is unrendered."
    },
    "census:N-049": {
      "disposition": "GLASS",
      "reason": "The lane's only human-facing surface carries no count at all, so a disabled category and an empty one render identically and a date-filtered photo grid reads as 'no photos'. HOME: dashboard sidebar category rows as a badge (linux-dashboard/dashboard/window.py:69-82), with the active filter named"
    },
    "census:N-050": {
      "disposition": "INTERNAL",
      "reason": "A per-process memory bound that is never presented as a photo count and dies with the process — an LRU occupancy is machinery, and rendering it would invite exactly the misreading (cached ≠ distinct photos) the row warns about."
    },
    "census:N-054": {
      "disposition": "EVIDENCE",
      "reason": "HOMED TODAY by NUM-5 (a524763): the routing vote's ratio, span counts and short-circuiting font now survive the decision and travel with the book, so a 50.1 % and a 100 % book no longer leave identical records. HOME: manifest.probe_evidence + convert/probe event (ocr_invisible_ratio / ocr_invisible_spans / ocr_total_spans / ocr_font_trigger)"
    },
    "census:N-065": {
      "disposition": "EVIDENCE",
      "reason": "No home — it lives only inside cost_s at convert_and_ship.py:1121 — yet the split of a book's cost into 'paid today' and 'paid earlier' is a per-book truth that must travel with the book, and its silent-zero fallback currently makes an unparseable .done look like a free slice. HOME: the conversion ledger row + manifest (beside ledger.cost_s / wall_s)"
    },
    "census:N-072": {
      "disposition": "REPAIR",
      "reason": "The number is written correctly to disk and read by nothing: the resume path at convert_and_ship.py:1191-1194 sums only prior wall_s and never retry_wall_s, which is the exact defect by which ledger.cost_s understates every resumed book."
    },
    "census:N-090": {
      "disposition": "INTERNAL",
      "reason": "A 1-or-abort correctness gate whose only human-relevant outcome — the RuntimeError — already reaches the operator through the watcher's FAILED log line and the intake/failed event, so the count itself is control flow."
    },
    "census:N-091": {
      "disposition": "REPORT",
      "reason": "The book's byte size is recorded nowhere in the pipeline and a file that never settles inside 120 s produces no record at all, so the settled size belongs on the intake event and the abandonment needs an event of its own. HOME: intake/detected event (add bytes) + a new intake/stalled event at the 120 s timeout (watch_and_convert.py:66-79,:166)"
    },
    "census:N-092": {
      "disposition": "REPORT",
      "reason": "One intake/deferred event per PDF per episode is emitted and nothing ever says when the hold cleared, so how long the assistant stalled the conveyor is unrecoverable — the hold's duration belongs on the event stream as per-run narration. HOME: intake/deferred event + a resume event carrying held_s (watch_and_convert.py:152-158)"
    },
    "census:N-099": {
      "disposition": "GLASS",
      "reason": "No renderer reads it, yet 'this PDF has sat in drop/ for three hours' is SYM-024's scenario exactly and OPEN-TASKS J16 already argues it is neither INTERNAL nor DEAD; note the dispositions.json entry J16 claims was written is NOT in the file today. HOME: Room queue panel rows (windows-widget/src/room.js:167-169, which today renders only name and bytes)"
    },
    "census:N-104": {
      "disposition": "GLASS",
      "reason": "Zero readers across main.js/room.js/index.html while the Room draws its progress bar from elapsed ÷ ETA instead — the real fraction Marker reports sits unrendered in the payload beneath an estimate. HOME: Room convert progress bar (room.js:329-334), which should read convert_frac rather than the ETA estimate"
    },
    "census:N-110": {
      "disposition": "INTERNAL",
      "reason": "It is the arithmetic product of two values already rendered side by side in the Dock receipt line ('Npp @ Xs/p', main.js:490), so it carries nothing a reader cannot already see."
    },
    "census:N-130": {
      "disposition": "GLASS",
      "reason": "Computed every poll on both surfaces' behalf and rendered by neither, so a fresh unacknowledged failure is invisible for M minutes until `escalated` catches up — the count that would surface it exists and is discarded. HOME: algedonic banner and Dock chip, beside `escalated` (room.js:148, main.js:1033)"
    },
    "census:N-135": {
      "disposition": "INTERNAL",
      "reason": "A uniqueness nonce masked into an opaque 28-hex token — a generation counter by definition — whose only job is non-repetition within a process."
    },
    "census:N-143": {
      "disposition": "DEAD",
      "reason": "Assigned on every render and read by nothing anywhere in the file — the S22 arithmetic sizing model it fed was replaced by scrollHeight reflow (main.js:281-289) — so it and its dead constants BASE_HEIGHT/CARD_HEIGHT/LINE_HEIGHT should be deleted rather than homed, since a stepping quantity with no reader looks wired and is not."
    },
    "census:N-147": {
      "disposition": "GLASS",
      "reason": "The operator is asked to consent to a standing routing rule whose number never appears — 'big docs' is the whole label — and the tick state is derived from 'is any rule set' rather than from this card's own estimate. HOME: Dock pre-flight card checkbox label (main.js:333-344) — name the threshold and compare it to THIS card's est_chunks"
    },
    "census:N-169": {
      "disposition": "GLASS",
      "reason": "The only witness of how long a book has been stuck never becomes a number — it only selects CSS classes — and because it is a max over the capped 12-alert list the glow silently steps DOWN as the pain grows. HOME: Assay card / Wall (room.js:73-77, :224, :964) — print the oldest held age beside the glow"
    },
    "census:N-170": {
      "disposition": "INTERNAL",
      "reason": "A CSS-tier selector computed from held.length, which is itself rendered honestly at room.js:453, so it must stay unrendered — its '+1' double-count under the enforce lever is a code defect to fix at source, not a number to publish."
    },
    "census:N-202": {
      "disposition": "GLASS",
      "reason": "Computed, shipped over the wire and dropped by the client — it is the one number that could expose the 40-page search cap, and it belongs on the tab that currently states a capped total as if it were the book's. HOME: bench Search tab label (bench.html) as `N of M (cap 40 pages)`"
    },
    "census:N-204": {
      "disposition": "GLASS",
      "reason": "A silent cap on evidence the operator is being asked to look at: the sidebar can say 'p12 · 83×' while only 60 boxes ever paint and nothing says which 23 occurrences are unmarked. HOME: bench search sidebar hit rows (bench.py:774 → bench.html:945-953) — disclose painted-of-found"
    },
    "census:N-205": {
      "disposition": "GLASS",
      "reason": "A second, tighter, undeclared cap on the same surface the primary path caps at 60, and the operator cannot even tell which matcher fired — both the ceiling and the path must be stated where the boxes paint. HOME: bench search sidebar hit rows (bench.py:600,:626) — disclose the cap and name the matcher"
    },
    "census:N-207": {
      "disposition": "REPAIR",
      "reason": "The defect is a name collision, not a missing home: bench.py:1951 answers `len(bench._undo)` — a restart-zeroed stack capped at 20 — under the field name every sibling route answers with the ledger-derived uncapped count."
    },
    "census:N-209": {
      "disposition": "REPAIR",
      "reason": "It exists as a permanent over-count because a defect eats its decrement: undo_ledger (bench.py:1081-1129), the only undo the UI can reach, reverts the body and pops the manifest repair record but never pops the drift entry."
    },
    "census:N-211": {
      "disposition": "GLASS",
      "reason": "Correct as computed and surfaced nowhere: the one gesture that silently rewrites the body reports neither how much it moved nor how many places it touched, while the toast that should carry it prints 'undefined more available'. HOME: the ↩ undo toast (bench.html:1643), reading the keys undo_ledger already returns"
    },
    "census:N-212": {
      "disposition": "REPAIR",
      "reason": "The number has an archival home in repairs.jsonl but is not a sequence: seq = len(ledger()) counts only READABLE lines (bench.py:1027 (ledger() at :976) drops torn ones), so one torn append re-issues an already-used ordinal — the fix is max(seq)+1, not a surface."
    },
    "census:N-219": {
      "disposition": "REPAIR",
      "reason": "One field name carries two incompatible quantities — the frozen convert-time manifest value or the live body count (bench.py:357-359) — while bench.html:1252/1263 prints the live count from the same body, so two disagreeing line totals sit on one screen unlabelled."
    },
    "census:N-223": {
      "disposition": "GLASS",
      "reason": "The needle count IS the stated denominator of the `confidence` ratio already rendered on the ⌖ locate readout, and docs/34 requires a denominator to travel with its ratio — 0.08 must read as 'one of twelve'. HOME: the ⌖ locate confidence readout on the bench (bench.py:854-870 → bench.html)"
    },
    "census:N-225": {
      "disposition": "EVIDENCE",
      "reason": "It is the localizer behind the ⌖ verdict — the signed bench:reason precedent puts a localizer on the opened card, never in ambient chrome — but it must first be sorted by vote instead of page number, since today the winning page can be absent from its own vote table. HOME: the opened ⌖ locate card on the bench (payload already ships at bench.py:880)"
    },
    "census:N-227": {
      "disposition": "EVIDENCE",
      "reason": "It already has a home: it rides manifest.repairs[] with the book and is deliberately kept off the repairs chip, because reusing the `lines` key would let a negative number reach the UI's `r.lines || 0` — a numeration whose name was chosen to prevent a display lie. HOME: manifest.repairs[] (bench.py:1409,:1432; consumed by _record_shift at :1088-1089)"
    },
    "census:N-233": {
      "disposition": "INTERNAL",
      "reason": "A payload sanity field whose operator-relevant fact — whether the page has a text layer at all — already renders honestly at bench.html:1005 via `searchable`."
    },
    "census:N-234": {
      "disposition": "INTERNAL",
      "reason": "A deliberately reasoned memory bound ('a 1,377-pp book must never hold 1,377 pages of rects', bench.py:200-201) — an LRU occupancy is machinery and nothing is misstated by its silence."
    },
    "census:N-235": {
      "disposition": "INTERNAL",
      "reason": "A per-process memoization occupancy that states nothing to anyone; the real issue it exposes is that it is UNBOUNDED where its neighbour _textlayer is deliberately LRU-capped, which is a memory defect to fix rather than a number to render."
    },
    "census:N-236": {
      "disposition": "INTERNAL",
      "reason": "Its cost is already narrated on the glass ('the first search indexes the whole book', bench.html:1000) and its one load-bearing derived fact — `searchable` — is a MEASURED claim the UI states honestly at bench.html:1005."
    },
    "census:N-238": {
      "disposition": "INTERNAL",
      "reason": "The marks themselves are rendered as a list (bench.html:1971-1974) so the population is already visible, and the defect worth fixing is the silent localStorage try/catch that loses the trim setting and reading position on a quota failure, not the missing count."
    },
    "census:N-240": {
      "disposition": "GLASS",
      "reason": "The wrap toast asserts the operator reached the end of the book's hits when it is the end of a 40-page-truncated list, and 'hit 7 of 40' is printed nowhere — so there is no surface on which the truncation could ever become visible. HOME: the bench search bar (bench.html:1028-1035) — print position of total with the cap named"
    },
    "census:N-241": {
      "disposition": "INTERNAL",
      "reason": "A staleness token used only to strand in-flight responses — a generation counter by definition — and nothing downstream would act on the number of searches a session ran."
    },
    "census:N-242": {
      "disposition": "INTERNAL",
      "reason": "The same staleness-token shape as searchGen, read only to strand stale placeholder timers, trim fits and scroll restores."
    },
    "census:N-247": {
      "disposition": "INTERNAL",
      "reason": "A per-assist token index used as an exact multiset gate whose only meaningful outcome already reaches the operator immediately — 'the model damaged an image embed — change REFUSED (link-fence)' at bench.html:1633."
    },
    "census:N-249": {
      "disposition": "REPAIR",
      "reason": "Its manifest half is sound and its drift half permanently over-counts because of N-209's defect (undo_ledger never pops the drift entry), compounded by two coexisting line arithmetics where repair() takes the shift unconditionally and _resolve_zone_line must not."
    },
    "census:N-250": {
      "disposition": "GLASS",
      "reason": "Correct as computed and entirely unsurfaced on the one gesture that can delete the most text in a single action — docs/28 built this chokepoint precisely because the manual ✎ save left no trace, and the receipt it now produces still never reaches an eye. HOME: the ✎ save toast (bench.html:1738), which today prints only '✓ saved (backup kept as .bench-bak)'"
    },
    "census:N-253": {
      "disposition": "EVIDENCE",
      "reason": "It already has a home and is the census's control specimen: it rides repairs.jsonl and the /api/ledger payload declaring the exact characters dropped plus the full-region sha256, and undo REFUSES outright on a truncated write rather than reconstructing approximately. HOME: repairs.jsonl + /api/ledger payload (bench.py:917-932)"
    },
    "census:N-260": {
      "disposition": "EVIDENCE",
      "reason": "docs/15:87-88 promises the count is kept and nothing keeps it — no manifest key, no event — so normalization noise is indistinguishable from zero noise in every book's audit record. HOME: manifest fidelity.convert block, beside runs and pages_flagged"
    },
    "census:N-261": {
      "disposition": "EVIDENCE",
      "reason": "Two books scoring 0.98 are not the same book if one passed on exact containment and the other on ≥90 fuzzy throughout, and neither the §7 schema nor the event vocabulary carries the split, so the audit's own strictness cannot be trended per book or across the corpus. HOME: manifest fidelity.convert block (windows matched exact / fuzzy, shipped with doc_survival)"
    },
    "census:N-262": {
      "disposition": "EVIDENCE",
      "reason": "A correct and deliberate filter whose effect is uncounted anywhere: doc_survival's denominator has already been trimmed by an unreported amount, so books with and without heavy running furniture publish ratios over differently-sized witnesses. HOME: manifest fidelity.convert block (lines and words stripped from the witness) — the doc-side twin of N-332"
    },
    "census:N-266": {
      "disposition": "EVIDENCE",
      "reason": "It already has an archival home in the manifest fidelity tripwires (docs/15:159) but zero renderers read it (OPEN-TASKS B16) and the 200-window sample size never travels with it, so docs/34 rule 7's 'sampling never promotes' cannot be honoured by any reader. HOME: manifest fidelity.convert.tripwires.reverse_sample (existing) — ship n and scope with it, reveal on the Assay drill"
    },
    "census:N-272": {
      "disposition": "REPORT",
      "reason": "A doc-promised measurement (docs/15:196) with no emitter anywhere, and it is load-bearing for a signed decision — docs/41:146 argues P-1's host choice on audit seconds extending the card lock, an argument about a quantity nothing has ever stored. HOME: audit/scored event (add wall_s) + the manifest fidelity block"
    },
    "census:N-285": {
      "disposition": "EVIDENCE",
      "reason": "A real count collapsed to a boolean at the only place it is observed, so the fence can say a chunk was rejected but never how badly — one token lost versus twelve — and the magnitude is a per-book truth that belongs beside chunks_rejected. HOME: manifest.analyst (fence tokens sent/returned aggregated beside chunks_rejected)"
    },
    "census:N-286": {
      "disposition": "REPORT",
      "reason": "HOMED TODAY by NUM-6 (a524763): goodput_accepted_tok_s is emitted with its docs/34 conditions in the record and rides the analyst/done event, with honest None when the backend reports no counters. HOME: analyst/done event goodput_accepted_tok_s + manifest.analyst.goodput_conditions (analyst.py:430-433)"
    },
    "census:N-297": {
      "disposition": "GLASS",
      "reason": "Same quantity as N-099 and same answer — OPEN-TASKS J16 argues it is neither INTERNAL nor DEAD because it is SYM-024's readout — and note that the dispositions.json GLASS entry J16 records as written is absent from the file today. HOME: Room queue panel rows (windows-widget/src/room.js:167-169)"
    },
    "census:N-312": {
      "disposition": "EVIDENCE",
      "reason": "It already has a home in the manifest and is the rare case where numerator and denominator ship in one object; the only gap is that assay.rs:153-170 does not forward it to the Assay drill. HOME: manifest fidelity.convert.tripwires.page_coverage (existing) — forward it through assay.rs to the Assay drill"
    },
    "census:N-315": {
      "disposition": "EVIDENCE",
      "reason": "No home at all — it never enters the block, manifest, event or surface — while doc_survival is published as a bare ratio, which is docs/34's exact prohibition on a denominator being destroyed at return. HOME: manifest fidelity.convert block, shipped in the same object as doc_survival"
    },
    "census:N-317": {
      "disposition": "EVIDENCE",
      "reason": "HOMED TODAY by NUM-3 (a524763): SYM-066 is repaired at the source — runs_total and runs_capped_at now ride the audit block in both phases and runs_total rides the audit/scored event, so '25' can no longer masquerade as the count. HOME: manifest fidelity.{convert,analyst}.runs_total / runs_capped_at + audit/scored event runs_total"
    },
    "census:N-322": {
      "disposition": "EVIDENCE",
      "reason": "HOMED TODAY by NUM-3 (a524763): blocks_total and worst_capped_at now ride degeneration_detail beside the ten-exemplar list, so a 60-zone book no longer reports ten. HOME: manifest fidelity.convert.tripwires.degeneration_detail.blocks_total / worst_capped_at (fidelity_audit.py:312-314)"
    },
    "census:N-331": {
      "disposition": "EVIDENCE",
      "reason": "It is the denominator of fidelity.analyst.doc_survival and it ships nowhere, so the analyst-stage ratio travels with no way to reconstruct what it was measured over — the same defect as N-315 one stage later. HOME: manifest fidelity.analyst block, shipped with doc_survival"
    },
    "census:N-332": {
      "disposition": "EVIDENCE",
      "reason": "The code-side twin of N-262: neither the number of stripped lines nor the words removed is ever reported, so on a book with heavy running heads the witness silently shrinks and the published doc_survival has a denominator nobody downstream can reconstruct. HOME: manifest fidelity.convert block (lines and words stripped from the witness; fidelity_audit.py:137-143)"
    },
    "census:N-333": {
      "disposition": "EVIDENCE",
      "reason": "The cap itself can stay a source constant, but it MANUFACTURES failed windows — lowering doc_survival, raising pages_flagged and creating omission runs — and the count of windows declared missing on cap exhaustion is named in no block, event, surface or report. HOME: manifest fidelity.convert block (windows declared missing by cap exhaustion; fidelity_audit.py:211)"
    },
    "census:N-334": {
      "disposition": "EVIDENCE",
      "reason": "Nothing records which windowing path a book took, so a misdetection silently changes windowing, matching and the reverse sample for the entire book and the block gives no way to tell — the ratio and the path chosen must travel with the audit. HOME: manifest fidelity.convert block (cjk ratio + windowing path; fidelity_audit.py:172-175)"
    }
  }
}
