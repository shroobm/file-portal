# SYMPTOM INDEX

**Retrieve by what the system is doing, not by which session produced it.** You are here because
something looks wrong and you don't know why. Grep the Symptom column.

Read this file at every session open (it is part of MUSTER). **A defect rediscovered is a MUSTER
failure, not bad luck.** Per `docs/21-session-closeout-contract.md` §5: keyed on symptom, terse,
append-only in spirit, session number always present but never the retrieval key. Fixed rows stay
— the recurrence is what you're guarding against.

**Seeding note (2026-08-09, receiver lane):** rows below were transcribed from the `CLAUDE_README.md`
Change Ledger, `docs/19`'s laws, and `coordination/messages/`. They are therefore **Historical** —
recorded by the cited session, faithful to its own account, not re-observed by the seeding session
— *except* rows explicitly tagged `Observed` with a date. Guards named in the "Guard lives at"
column were confirmed present in the working tree at seeding time (`Observed 2026-08-09`).

| ID | Symptom — what you notice | Root cause | Session | Status | Guard lives at |
|---|---|---|---|---|---|
| SYM-001 | Tests and harness all green, but the shipped behaviour is wrong | The fake/stub shared the same assumption as the code under test. Two checks that share an assumption are one check | S60 | `Historical` — class is permanent, no mechanical guard possible | `docs/21` §1 (`Verified` requires a differently-shaped second method) |
| SYM-002 | Page numbers above the first slice point at pages that don't exist (assets numbered ~2553 on a 1,356-pp book). Nothing is lost — navigation simply lies | Marker already numbers slice assets by ABSOLUTE page; the added offset double-counted | S60 (fix `6d0a560`) | `fixed` | `windows-converter/convert_and_ship.py` (absolute-page asset renumbering) |
| SYM-003 | Audit verdict `fail` with degeneration on table-heavy books; excerpts are repeated table rows, no seam nearby | "Table-loop disease" — the model loops on tabular structure. Seen on Beer, Valentine, Damodaran, Cybernetics | S60, S61, earlier | **OPEN** — `Observed` repeatedly; the Repair Bench is the response, not a fix | `prototypes/repair-bench/` (human-in-the-loop repair) |
| SYM-004 | An `ssh`/`scp` from a windowless or background process hangs forever; zombie ssh/scp pairs accumulate in Task Manager | System32 OpenSSH keeps its OWN `known_hosts` (git ships a separate ssh) → invisible host-key prompt with nowhere to answer. Nine zombie pairs before diagnosis | S56 | `fixed` — hang class extinct | Standing options `-o BatchMode=yes -o StrictHostKeyChecking=accept-new -o ConnectTimeout=10` (`docs/19` law 6) |
| SYM-005 | `publickey` denied, and **no passphrase prompt** appears | Public key silently truncated in transit by clipboard paste (73 of 106 bytes); the line-start check still accepted it | S47 | `fixed` | Law: key material travels by file (`scp`), never clipboard (`docs/19` law 5). Diagnose by byte length + `ssh-keygen -lf` match |
| SYM-006 | The GPU stays held / VRAM unreleased after a kill; a process you killed appears gone but work continues | `proc.kill()` on a single pid leaves the venv console-script launcher's real python alive | S48, S52 | `fixed` | `_kill_tree` helper / `taskkill /pid N /t /f` (`docs/19` law 7) |
| SYM-007 | A write "succeeded" but the system behaves as though it never happened; verifying from the same place shows it present | MSIX-packaged desktop-app session's AppData/registry writes are silently virtualized | S29, S45, S48 (three separate disasters) | `fixed` by rule, not by code | `docs/19` law 3: never verify a write from the surface that made it; adoption is Rab's hand; system config only from elevated unpackaged shells or over SSH |
| SYM-008 | A venv launcher dies immediately with exit `0x67` | The base CPython under uv-managed `%APPDATA%\uv\python\…` is missing again | S48 | `Historical` | Reinstall the uv-managed base; see `docs/19` §1 |
| SYM-009 | A human's repair work vanishes after a re-run | `_enforce_hold` rmtree'd a held bundle that carried repairs, to park the incoming one | S65 | `fixed` | `windows-converter/convert_and_ship.py` — repairs-bearing occupant keeps its slot; incoming parks beside, timestamped |
| SYM-010 | A test harness appears to drive the real running app; live guards refuse test payloads | Hardcoded port (7078) reached the LIVE bench via `SO_REUSEADDR` | S65 | `fixed` | `prototypes/repair-bench/acceptance.py` — OS-assigned ports |
| SYM-011 | The converter never fires on a file the allocator just moved in | inotify reports the allocator's cross-watch hop as an **unpaired `IN_MOVED_TO`** → arrives as a plain watchdog `created` event, never `moved`/`closed` | recorded 2026-07-10 (`c718ed2`) | `fixed` | `linux-converter/converter/main.py:11-16` — load-bearing comment + `on_created` with size-stability wait |
| SYM-012 | OCR output still contains the source's bad prior OCR text | pymupdf4llm `force_ocr=True` means `FORCE_KEEP_OLD` — the opposite of what the name implies | recorded 2026-07-10 | `fixed` | `linux-converter/converter/engines.py` — Scan lane uses `OCRMode.FORCE_DROP_OLD` |
| SYM-013 | `ENOENT` naming `.part-<underscored_stem>/assets/…` on a file whose name contains spaces | Assembly dir was keyed on the stem, which the engine's sanitizer rewrote | L13 (2026-07-12) | `fixed` | `linux-converter` — assembly dir keyed on source SHA-256 (`.part-<sha256[:16]>`), sanitizer-proof by construction |
| SYM-014 | A bundle's interior filenames exceed Windows MAX_PATH on the desktop side | Unclamped published bundle names | L15 (2026-07-12) | `fixed` | `bundle.clamp_name` — 200 utf-8 bytes, codepoint-safe |
| SYM-015 | `receipts.jsonl` records the same `supersede-held` for one bundle on every boot | A retained fail-verdict bundle sits in `library/staging/`; the converter's startup sweep re-processes it each time. Guard is fail-closed and correct — the cost is repetition, not risk | `Observed 2026-08-09` (3rd firing: 08-03, 08-06, 08-09) — bundle `claude-code-up-and-running`, sha `5998f114ae93f65c` | `fixed` 2026-08-09 (S67) — bundle deleted on Rab's instruction after the four safety checks below | `linux-converter/converter/exporter.py` supersede branch (worked as designed throughout) |
| — | *(SYM-015 evidence, preserved because the artifact was deleted and the numbers are the only part worth keeping)* | The bundle was S58's **analyst-only re-run** of *Claude Code: Up and Running* (104 pp, converted 2026-07-30), carrying `supersede: {reason: analyst-rerun, from_verdict: fail}` — generated to replace the vaulted note. Convert phase scored **0.9913** (1 page flagged, p31); **analyst phase scored 0.9547 with 12 repeated runs** — degeneration in a *clean-lane, text-layer* book, not a scan. qwen3:8b/readability, 34 chunks passed, 3 rejected, 906 s. Verdict `fail` ⇒ correctly held | S58 → S67 | reference | **Note for SYM-003:** this is degeneration on a clean-lane book with a real text layer, so the table-loop framing (Beer/Valentine/Damodaran) does not cover every case — an analyst pass can degenerate prose too. Deleting is safe **only** because no `repairs` key was present; a bundle carrying human repairs must never be deleted (SYM-009) |
| SYM-016 | A Change Ledger row cites a SHA that isn't in the history | The closing commit was `--amend`ed after the row was written, orphaning the SHA | recorded 2026-07-10 | `fixed` by rule | `CLAUDE_README.md` §4: ledger row is a separate follow-up commit, NEVER amend. Detect with `git merge-base --is-ancestor <SHA> HEAD` |
| SYM-017 | `ps -W` in git-bash reports a PID that doesn't match reality | The PID column lies under git-bash | S65 | `Historical` | Cross-check with Task Manager / `tasklist` |
| SYM-018 | You push code and **no CI status appears** on the commit; lint/test regressions reach the branch unnoticed | The workflow triggered only on push to `master` plus `pull_request`. Work is direct-pushed to `feat/library-pipeline`, so after the last PR-triggered run on **2026-07-13** CI went silent for **205 commits / ~4 weeks**. It also covered only 2 of 5 Python areas, excluding `linux-converter`, which writes the vault | `Observed 2026-08-09` (S67) | `fixed` — trigger now includes the feature branch; converter lint+tests added. **`cargo test` still absent from CI** | `.github/workflows/ci.yml` (trigger note in the header) |
| SYM-020 | `cargo fmt --check` fails across 8 files in `windows-widget/src-tauri/src/` (36 hunks) while `cargo clippy -D warnings` passes | The desktop rebuild ritual ran clippy and **never `cargo fmt`**, so clippy stayed clean and formatting rotted unnoticed for as long as CI was silent (SYM-018) | `Observed 2026-08-09` (S67) | `fixed` 2026-08-10 (S68) — whitespace-only reformat (`b415a2f`); fmt + clippy + `cargo test` (20/20) proven on the reformatted tree; rust CI on the branch, toolchain pinned `1.97.1` (SYM-019's shape, applied before it bit), `cargo test` step added | `cargo fmt --check` now LEADS the rebuild ritual (`docs/19` §1); `.github/workflows/ci.yml` `rust:` job runs on `feat/library-pipeline` |
| SYM-019 | CI lint fails on code that `ruff` passes clean on your own machine | `ruff>=0.4` was unpinned, so CI resolved the newest minor (0.16.2) while local venvs held 0.15.20; the newer default rule set flags older code. First seen the very first time CI ran after SYM-018 was fixed | `Observed 2026-08-09` (S67) | `fixed` — `ruff==0.15.20` pinned in both `requirements-dev.txt`, with the reason inline | `linux-receiver/requirements-dev.txt`, `linux-converter/requirements-dev.txt`. **Trap:** do not silence the resulting DTZ005 in `allocator/rules.py` by making it tz-aware — `{yyyy}/{mm}/{dd}` are local-time folder names by design (comment guards it) |

| SYM-021 | You tree-kill a background command's python; its work continues anyway minutes later | The task was a `cmd1; cmd2` SHELL CHAIN — killing cmd1's process tree doesn't cancel the chain; the shell launches cmd2. The orphaned batch then ran concurrently with its replacement | `Observed 2026-08-10` (S71) | `fixed` by rule | Kill the chain's parent shell (`taskkill /pid <shell> /t /f`), or never chain GPU work with `;` — one command per background task |
| SYM-022 | Windows reboots mid-run: VIDEO_SCHEDULER_INTERNAL_ERROR | TWO concurrent GPU model processes (SYM-021's orphan + its replacement) + a lived-in desktop faulted the display driver | `Observed 2026-08-10` (S71; zero data lost — fsync-per-row calibration + files on disk) | `fixed` by rule | **One lab process on the card, ever** — the serialization law is a MACHINE law: workers check `.gpu-lock`; humans check `nvidia-smi` before any lab launch |
| SYM-023 | Closing the Repair Bench WINDOW silently kills the intake watcher while the widget lives; drops sit unclaimed; no error anywhere (watcher-stderr shows only its startup line; widget-boot.log records `watcher DIED (exit ?)`) | `on_window_event(Destroyed) → watcher::stop` fires for EVERY window and was written in the one-window era (S37); the S63 Bench window made it a latent assassin. The bench SERVER survives (different cleanup path) — the inverted survivor pair is the fingerprint | `Observed 2026-08-13` (S76, first live firing) | `fixed` — label guard (`window.label() == "main"`); any future window inherits it | `windows-widget/src-tauri/src/main.rs` on_window_event (comment names this row) |
| SYM-027 | A value you need is **nowhere on any surface**, yet it turns out to be computed correctly and sitting in a manifest, a `state()` payload, or a log — the disk is perfect and no error was ever raised. Rab's term: a **glitch**, not a bug (the system was DESIGNED observable, so silence is the design's intent failing) | The projection law (docs/13) is one-directional: it forbids the UI to invent numbers and never requires a measured number to reach anyone. Every law in the corpus with teeth governs what may be **written**; none governs what must be **read**, so a producer writes into a shared payload and a consumer pays ZERO cost to ignore a key. Four mechanisms (docs/29 §3): **A** spec-narrowing · **B** grammar placeholder shadowed by a working twin · **C** partial fork · **D** acceptance-complete/UI-deferred. The reactive law-writing engine cannot learn this class — it only fires on failures that announce themselves, and this one raises nothing | `Observed 2026-08-14` (S77 census: **~83 silent fields**; the class named in S76 and then reproduced twice more *during its own investigation*) | **OPEN** — law + procedure written (**docs/29**); the detector is now **built and proven but wired into nothing** (S78: `observability/`, acceptance 13/13, reproduces the S77 hand census 9/9 and independently finds **93** unsigned glitches). It stays OPEN because §5.1 automated is only a net: docs/29 §8.3 — CI, closeout ritual, or both — is **unsigned**, so nothing calls it, and §7's debt is unfixed except the three S76/S77 self-inflicted ones | `observability/glass_detector.py` (§5.1) · `observability/dispositions.json` (§5.2 signed silence) · `observability/acceptance.py` (docs/29 §7 as an answer key) · `docs/29-the-observability-complex.md` (§5.3 six-step procedure · §5.4 same-commit timing) · manual `docs/20` §12 + §14 law 2b. **Diagnose:** `python observability/glass_detector.py`. **Its floor, know it:** a *generic* key name always finds an incidental match and scores glass, and "referenced" ≠ "reaches a human" — `pages_scored` is read by both JS renderers as a positioning *denominator* and never displayed, satisfying §5.1 while §7.2's complaint stands |
| SYM-026 | You find real damage in a bundle **by eye** that the Repair Bench never offered you — a passage simply missing from the markdown, with no zone chip pointing at it. The bench's chip row says "N wreck site(s)" and the book has more damage than N | The audit records **two** kinds of damage: `degeneration_detail.worst[]` (loops) and `fidelity.convert.runs[]` (omission runs — text the witness has that the output lost). `Bench.zones()` reads only the first; `state()` DOES project `runs` (bench.py:233, up to 40) but **`bench.html` never reads `st.runs`** — zero references — so the data reaches the browser and is discarded. On the S76 Beer: 2 zone chips shown, **18 omission runs recorded and invisible**, including `p114 · 60 words · "apart from the command function. r system three v"` — the brace-diagram marginalia Rab found by reading. Same family as the unwired `reverse_sample` (docs/27 §4) and the dead `#st-lib` (docs/26 F9): the instrument is built, computed, stored, and never put on the glass | `Observed 2026-08-14` (S76, found by Rab reading the page, then confirmed against the manifest) | `fixed` 2026-08-14 (S76, `bc6612d`) — runs render as amber `◍` repair sites, page-anchored, with an `anchor_line` where the excerpt's surviving head can be found (6 of 18 on the Beer) and honest `None` where it cannot | `prototypes/repair-bench/bench.html` `renderZones()` + `selectRun()`; anchors in `bench.py` `_resolve_run_line()` |
| SYM-025 | On the Repair Bench, a zone's markdown panel / insertion point does not sit on the damage — the text at the zone's line is an unrelated passage, while the real degeneration sits N lines away. Zones near the TOP of a book look fine; zones deeper in are progressively wrong | The zone line numbers come from the **convert-phase** audit (`fidelity.convert.tripwires.degeneration_detail.worst[].line`, recorded against a body of `md_lines`), but the **analyst phase then rewrites the body** — 55 readability chunks netted **−12 lines** on the S76 Beer (manifest `md_lines` 3013 → live body 3001). `Bench._adjusted_line()` compensates only for bench-session repairs and AI drift; it has no term for the analyst's rewrite, so every zone below the analyst's edits is stale by the accumulated delta. Zone 2 (line 7) was exact; zone 1 (line 1847) was really at 1835 | `Observed 2026-08-13` (S76, caught live before Rab inserted a crop; measured via `/api/md` body coordinates vs manifest) | `fixed` 2026-08-14 (S76, `ef605fa`) — `_resolve_zone_line()` searches the live body for the stored `excerpt` and reports `anchor: "excerpt"`; falls back to the drift arithmetic (and says so) only when the excerpt is gone. A live-body hit deliberately does NOT also take the drift terms — that would double-count | `prototypes/repair-bench/bench.py` `_resolve_zone_line()`. Diagnose: compare manifest `md_lines` to `len(/api/md .text.split("\n"))` — any delta means the recorded lines are stale |
| SYM-024 | A routed gate card produces NO analyst: card stays `"state": "pending"`, no `analyst/start` event, no process, ollama cold — and no error trace exists anywhere. The same `--resume` command run by hand works perfectly | Cause UNPROVEN (one observation): either the invoke rejected (the status line briefly shows `Route failed: …` — easy to miss) or the detached resume died before flipping the card state — invisible because its stderr was `Stdio::null()`. Discriminate next time via `resume-stderr.log` | `Observed 2026-08-13` (S76; recovered by hand-running the resume with stderr captured) | `open` — hardened, not root-caused: resume stderr now goes to a last-words file | `windows-widget/src-tauri/src/preflight.rs` decide (comment names this row); recovery: run `convert_and_ship.py --resume <id> --backend <b>` by hand with output captured |
| SYM-028 | `muster.sh` exits 1 at session open reporting a **SHA mismatch** — "TIME-STATE `<a>` vs ledger `<b>`" — but `<b>` is a real commit, IS an ancestor of `HEAD`, and the working tree is clean. Nothing in the repo or the memory library actually disagrees. Reads exactly like a rewind or a fork, and isn't one | The Change Ledger is **append-below** by convention and nothing enforced it. S75 wrote its row **above** S74's instead of below; S76 and S77 each copied the row above them, producing an ascending table S16→S73 followed by a descending block S77, S76, S75, S74. `muster.sh` took the newest row as `tail -1` — pure position — so for three sessions it returned **S74's** row and compared it against S77's TIME-STATE. An ordering fault wearing a rewind's clothing: the alarm was right that something was wrong and wrong about what | `Observed 2026-08-14` (S78, at open; the drift had been live and undetected since S75) | `fixed` 2026-08-14 (S78) — rows reordered (pure permutation: identical line multiset, byte count unchanged, CRLF intact); `muster.sh` now selects the newest row by **max session number**, never position | `~/.claude/muster.sh` **[3a] LEDGER ORDER** — session numbers must ascend in file order; a violation prints `77->76 …` and is reported as its own fault, distinct from a rewind. Negative-tested against the pre-fix ledger. **Note:** `muster.sh` lives only in `~/.claude` and is **not tracked by any repo** — the hard clock's verifier has no backup |
| SYM-029 | A shell edit to a repo markdown file "succeeds", the line count is right, `diff` shows the lines as identical — but the file is a few bytes shorter and git reports every touched line as changed. Scales with how much you touched: 4 lines edited → 4 bytes lost; a whole-file pass → one byte per line | Repo markdown (`CLAUDE_README.md` at least) is **CRLF**, and this Git Bash's `sed`, `awk`, and `grep` **silently strip the `\r`** when reading. Anything reconstructed from their output is written back LF-only. `grep -c $'\r'` returns **0** on a CRLF file, so the obvious check for this reports the file is clean — the diagnostic shares the defect | `Observed 2026-08-14` (S78; caught by byte-count accounting during the SYM-028 repair, not by any tool warning) | `fixed` by rule | **Slice repo markdown with `head`/`tail` only** — both are binary-safe. Verify any such edit three ways: `wc -c` unchanged, `file -b` still says CRLF, and `tr -cd '\r' \| wc -c` equals `tr -cd '\n' \| wc -c`. For a reorder, prove it is a pure permutation with `diff <(sort old) <(sort new)`. Do **not** trust `grep` to see CR |

## Adding a row

From your closeout's §15. Write the Symptom column **for someone who has the symptom and not the
cause** — what they'd see on screen or in a log, in their words, not the mechanism's name. If you
cannot describe it without naming the cause, you don't yet understand the failure well enough to
index it.
