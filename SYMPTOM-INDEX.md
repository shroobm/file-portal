# SYMPTOM INDEX

**Retrieve by what the system is doing, not by which session produced it.** You are here because
something looks wrong and you don't know why. Grep the Symptom column.

Read this file at every session open (it is part of MUSTER). **A defect rediscovered is a MUSTER
failure, not bad luck.** Per `docs/21-session-closeout-contract.md` §5: keyed on symptom, terse,
append-only in spirit, session number always present but never the retrieval key. Fixed rows stay
— the recurrence is what you're guarding against.

**Seeding note (2026-08-09, receiver lane):** rows below were transcribed from the `CLAUDE_README.md`
Change Ledger, `docs/19`'s laws, and `coordination/messages/`. They are therefore **Historical** —
recorded by the cited session, faithful to its own account, not re-observed by the seeding session
— *except* rows explicitly tagged `Observed` with a date. Guards named in the "Guard lives at"
column were confirmed present in the working tree at seeding time (`Observed 2026-08-09`).

**How to read a status cell (2026-09-16, S162 E3 — the second Circle's R2-S1 and R3-S1):** the cell's FIRST marker is the row's status (`fixed` / **FIXED** / `open` / **OPEN** / `Historical`); the muster's counter reads that marker and nothing after it. A cell that carries `Was:` continues with the SUPERSEDED cell, kept verbatim (the S157 form), so its last clause may read 'this remains OPEN' or 'the fix is owed' while the row is fixed — the tail is history, the head is the status. Dated `Update` and `Scope note` clauses inside a cell are read in date order; the newest dated clause is the current reading of what the marker says.

| ID | Symptom — what you notice | Root cause | Session | Status | Guard lives at |
|---|---|---|---|---|---|
| SYM-001 | Tests and harness all green, but the shipped behaviour is wrong | The fake/stub shared the same assumption as the code under test. Two checks that share an assumption are one check | S60 | `Historical` — class is permanent, no mechanical guard possible | `docs/21` §1 (`Verified` requires a differently-shaped second method) |
| SYM-002 | Page numbers above the first slice point at pages that don't exist (assets numbered ~2553 on a 1,356-pp book). Nothing is lost — navigation simply lies | Marker already numbers slice assets by ABSOLUTE page; the added offset double-counted | S60 (fix `6d0a560`) | `fixed` | `windows-converter/convert_and_ship.py` (absolute-page asset renumbering) |
| SYM-003 | Audit verdict `fail` with degeneration on table-heavy books; excerpts are repeated table rows, no seam nearby | "Table-loop disease" — the model loops on tabular structure. Seen on Beer, Valentine, Damodaran, Cybernetics | S60, S61, earlier | **OPEN** — `Observed` repeatedly; the Repair Bench is the response, not a fix | `prototypes/repair-bench/` (human-in-the-loop repair) |
| SYM-004 | An `ssh`/`scp` from a windowless or background process hangs forever; zombie ssh/scp pairs accumulate in Task Manager | System32 OpenSSH keeps its OWN `known_hosts` (git ships a separate ssh) → invisible host-key prompt with nowhere to answer. Nine zombie pairs before diagnosis | S56 | `fixed` — hang class extinct | Standing options `-o BatchMode=yes -o StrictHostKeyChecking=accept-new -o ConnectTimeout=10` (`docs/19` law 6) |
| SYM-005 | `publickey` denied, and **no passphrase prompt** appears | Public key silently truncated in transit by clipboard paste (73 of 106 bytes); the line-start check still accepted it | S47 | `fixed` | Law: key material travels by file (`scp`), never clipboard (`docs/19` law 5). Diagnose by byte length + `ssh-keygen -lf` match |
| SYM-006 | The GPU stays held / VRAM unreleased after a kill; a process you killed appears gone but work continues | `proc.kill()` on a single pid leaves the venv console-script launcher's real python alive | S48, S52 | `fixed` **Tripwire (S163 E1, 2026-09-16):** `convert_and_ship_selftest.py` T23 (a)–(c) — a real three-deep process tree; `_kill_tree(child)` reaches the GRANDCHILD; the NEGATIVE CONTROL is the bare `proc.kill()`, which leaves the grandchild alive (the S48 orphan class), then reaped by the test. Suite 254 → 262. | `_kill_tree` helper / `taskkill /pid N /t /f` (`docs/19` law 7) |
| SYM-007 | A write "succeeded" but the system behaves as though it never happened; verifying from the same place shows it present | MSIX-packaged desktop-app session's AppData/registry writes are silently virtualized | S29, S45, S48 (three separate disasters) · **4th firing `Observed 2026-08-16` (post-S86 chat, filed S87):** S85 keyed the live `config.toml` from the packaged surface — the write virtualized into the app container (probe-proven: the surface's %APPDATA% writes redirect; mirage copy carried the key, mtime 02:48:21), the real Roaming file never got it, and the Assistant button stayed hidden until Rab's hand re-inserted the key | `fixed` by rule, not by code | `docs/19` law 3: never verify a write from the surface that made it; adoption is Rab's hand; system config only from elevated unpackaged shells or over SSH. **S87 corollary: `config.toml` is Rab's-hand/widget-verified territory — a packaged session reads its own mirage on that path, permanently; verify config from the widget's boot log (`chat_config:`/`reader_config:` lines), never from the writing surface** |
| SYM-008 | A venv launcher dies immediately with exit `0x67` | The base CPython under uv-managed `%APPDATA%\uv\python\…` is missing again | S48 | `Historical` | Reinstall the uv-managed base; see `docs/19` §1 |
| SYM-009 | A human's repair work vanishes after a re-run | `_enforce_hold` rmtree'd a held bundle that carried repairs, to park the incoming one | S65 | `fixed` **Tripwire (S163 E2, 2026-09-16):** `convert_and_ship_selftest.py` T25 (a)–(f) — under enforce (the lever set through its writer, restored after) a repairs-bearing occupant keeps its slot and the incoming parks beside it as `<sha16>--superseded-<stamp>`; a `.bench-bak` protects the same way; the NEGATIVE CONTROL: a bare occupant IS replaced in place (the branch discriminates); report mode parks nothing. Suite 262 → 268; everything under the quarantine, the live `held/` untouched. | `windows-converter/convert_and_ship.py` — repairs-bearing occupant keeps its slot; incoming parks beside, timestamped |
| SYM-010 | A test harness appears to drive the real running app; live guards refuse test payloads | Hardcoded port (7078) reached the LIVE bench via `SO_REUSEADDR` | S65 | `fixed` | `prototypes/repair-bench/acceptance.py` — OS-assigned ports |
| SYM-011 | The converter never fires on a file the allocator just moved in | inotify reports the allocator's cross-watch hop as an **unpaired `IN_MOVED_TO`** → arrives as a plain watchdog `created` event, never `moved`/`closed` | recorded 2026-07-10 (`c718ed2`) | `fixed` **Tripwire (S166 E2, 2026-09-16):** `linux-converter/tests/test_main.py` `TestAllocatorHop` (5 cases) — a plain `created` event (the shape watchdog hands the handler for an unpaired IN_MOVED_TO) converts the hop, the size-stability wait observed to run on it first; a directory event converts nothing; `_wait_until_stable` returns only once the size holds across one interval (a thread appending meanwhile) and at once for a missing file; NEGATIVE CONTROL: with the wait stubbed out a half-written hop is acted on as partial (quarantined) — the wait is load-bearing. Proved on Linux by CI's ubuntu log (run 35152701408: the five ids PASSED, read by `ci_step_log.py --grep`); locally in a scratch venv 4 of 5 (the control is Linux-only — Windows' file lock); a source mutant (`on_created` returning at once) red on the hop case. | `linux-converter/converter/main.py:11-16` — load-bearing comment + `on_created` with size-stability wait |
| SYM-012 | OCR output still contains the source's bad prior OCR text | pymupdf4llm `force_ocr=True` means `FORCE_KEEP_OLD` — the opposite of what the name implies | recorded 2026-07-10 | `fixed` **Tripwire (S166 E2, 2026-09-16):** `linux-converter/tests/test_engines.py` `TestOcrMode` (3 cases) — `pymupdf4llm.to_markdown` captured: the Scan lane passes `use_ocr` IDENTICAL to `OCRMode.FORCE_DROP_OLD` with `ocr_dpi`/`ocr_language` from settings; the Clean lane `SELECT_KEEP_OLD` and no `ocr_dpi`; the discriminator stated (`FORCE_KEEP_OLD is not FORCE_DROP_OLD` — the plan doc's `force_ocr=True` spelling). Proved on Linux by CI's ubuntu log (run 35152701408, the three ids PASSED); locally 3 of 3; a source mutant (`FORCE_KEEP_OLD` planted back) red: `assert <OCRMode.FORCE_KEEP_OLD: 4> is <OCRMode.FORCE_DROP_OLD: 3>`. | `linux-converter/converter/engines.py` — Scan lane uses `OCRMode.FORCE_DROP_OLD` |
| SYM-013 | `ENOENT` naming `.part-<underscored_stem>/assets/…` on a file whose name contains spaces | Assembly dir was keyed on the stem, which the engine's sanitizer rewrote | L13 (2026-07-12) | `fixed` | `linux-converter` — assembly dir keyed on source SHA-256 (`.part-<sha256[:16]>`), sanitizer-proof by construction |
| SYM-014 | A bundle's interior filenames exceed Windows MAX_PATH on the desktop side | Unclamped published bundle names | L15 (2026-07-12) | `fixed` | `bundle.clamp_name` — 80 utf-8 bytes (MAX_PATH recalibration; the docstring carries the ≤160-byte vault-relative worst case). *Guard text said 200 — the L13 ext4-era number — until 2026-08-16: caught by ThinkPad S79's familiarization pass (SYM-039), corrected S86 against `bundle.py:72`* |
| SYM-015 | `receipts.jsonl` records the same `supersede-held` for one bundle on every boot | A retained fail-verdict bundle sits in `library/staging/`; the converter's startup sweep re-processes it each time. Guard is fail-closed and correct — the cost is repetition, not risk | `Observed 2026-08-09` (3rd firing: 08-03, 08-06, 08-09) — bundle `claude-code-up-and-running`, sha `5998f114ae93f65c` | `fixed` 2026-08-09 (S67) — bundle deleted on Rab's instruction after the four safety checks below | `linux-converter/converter/exporter.py` supersede branch (worked as designed throughout) |
| — | *(SYM-015 evidence, preserved because the artifact was deleted and the numbers are the only part worth keeping)* | The bundle was S58's **analyst-only re-run** of *Claude Code: Up and Running* (104 pp, converted 2026-07-30), carrying `supersede: {reason: analyst-rerun, from_verdict: fail}` — generated to replace the vaulted note. Convert phase scored **0.9913** (1 page flagged, p31); **analyst phase scored 0.9547 with 12 repeated runs** — degeneration in a *clean-lane, text-layer* book, not a scan. qwen3:8b/readability, 34 chunks passed, 3 rejected, 906 s. Verdict `fail` ⇒ correctly held | S58 → S67 | reference | **Note for SYM-003:** this is degeneration on a clean-lane book with a real text layer, so the table-loop framing (Beer/Valentine/Damodaran) does not cover every case — an analyst pass can degenerate prose too. Deleting is safe **only** because no `repairs` key was present; a bundle carrying human repairs must never be deleted (SYM-009) |
| SYM-016 | A Change Ledger row cites a SHA that isn't in the history | The closing commit was `--amend`ed after the row was written, orphaning the SHA | recorded 2026-07-10 | `fixed` by rule | `CLAUDE_README.md` §4: ledger row is a separate follow-up commit, NEVER amend. Detect with `git merge-base --is-ancestor <SHA> HEAD` |
| SYM-017 | `ps -W` in git-bash reports a PID that doesn't match reality | The PID column lies under git-bash | S65 | `Historical` | Cross-check with Task Manager / `tasklist` |
| SYM-018 | You push code and **no CI status appears** on the commit; lint/test regressions reach the branch unnoticed | The workflow triggered only on push to `master` plus `pull_request`. Work is direct-pushed to `feat/library-pipeline`, so after the last PR-triggered run on **2026-07-13** CI went silent for **205 commits / ~4 weeks**. It also covered only 2 of 5 Python areas, excluding `linux-converter`, which writes the vault | `Observed 2026-08-09` (S67) | `fixed` — trigger now includes the feature branch; converter lint+tests added. ~~`cargo test` still absent from CI~~ *(superseded 2026-08-10 by SYM-020's fix — the `ci.yml` rust job runs `cargo test`; noted stale by ThinkPad S79, annotated S86)* | `.github/workflows/ci.yml` (trigger note in the header) |
| SYM-019 | CI lint fails on code that `ruff` passes clean on your own machine | `ruff>=0.4` was unpinned, so CI resolved the newest minor (0.16.2) while local venvs held 0.15.20; the newer default rule set flags older code. First seen the very first time CI ran after SYM-018 was fixed | `Observed 2026-08-09` (S67) | `fixed` — `ruff==0.15.20` pinned in both `requirements-dev.txt`, with the reason inline | `linux-receiver/requirements-dev.txt`, `linux-converter/requirements-dev.txt`. **Trap:** do not silence the resulting DTZ005 in `allocator/rules.py` by making it tz-aware — `{yyyy}/{mm}/{dd}` are local-time folder names by design (comment guards it) |
| SYM-020 | `cargo fmt --check` fails across 8 files in `windows-widget/src-tauri/src/` (36 hunks) while `cargo clippy -D warnings` passes | The desktop rebuild ritual ran clippy and **never `cargo fmt`**, so clippy stayed clean and formatting rotted unnoticed for as long as CI was silent (SYM-018) | `Observed 2026-08-09` (S67) | `fixed` 2026-08-10 (S68) — whitespace-only reformat (`b415a2f`); fmt + clippy + `cargo test` (20/20) proven on the reformatted tree; rust CI on the branch, toolchain pinned `1.97.1` (SYM-019's shape, applied before it bit), `cargo test` step added | `cargo fmt --check` now LEADS the rebuild ritual (`docs/19` §1); `.github/workflows/ci.yml` `rust:` job runs on `feat/library-pipeline` |
| SYM-021 | You tree-kill a background command's python; its work continues anyway minutes later | The task was a `cmd1; cmd2` SHELL CHAIN — killing cmd1's process tree doesn't cancel the chain; the shell launches cmd2. The orphaned batch then ran concurrently with its replacement | `Observed 2026-08-10` (S71) | `fixed` by rule | Kill the chain's parent shell (`taskkill /pid <shell> /t /f`), or never chain GPU work with `;` — one command per background task |
| SYM-022 | Windows reboots mid-run: VIDEO_SCHEDULER_INTERNAL_ERROR | TWO concurrent GPU model processes (SYM-021's orphan + its replacement) + a lived-in desktop faulted the display driver | `Observed 2026-08-10` (S71; zero data lost — fsync-per-row calibration + files on disk) | `fixed` by rule | **One lab process on the card, ever** — the serialization law is a MACHINE law: workers check `.gpu-lock`; humans check `nvidia-smi` before any lab launch |
| SYM-023 | Closing the Repair Bench WINDOW silently kills the intake watcher while the widget lives; drops sit unclaimed; no error anywhere (watcher-stderr shows only its startup line; widget-boot.log records `watcher DIED (exit ?)`) | `on_window_event(Destroyed) → watcher::stop` fires for EVERY window and was written in the one-window era (S37); the S63 Bench window made it a latent assassin. The bench SERVER survives (different cleanup path) — the inverted survivor pair is the fingerprint | `Observed 2026-08-13` (S76, first live firing) | `fixed` — label guard (`window.label() == "main"`); any future window inherits it | `windows-widget/src-tauri/src/main.rs` on_window_event (comment names this row) |
| SYM-024 | A routed gate card produces NO analyst: card stays `"state": "pending"`, no `analyst/start` event, no process, ollama cold — and no error trace exists anywhere. The same `--resume` command run by hand works perfectly | Cause UNPROVEN (one observation): either the invoke rejected (the status line briefly shows `Route failed: …` — easy to miss) or the detached resume died before flipping the card state — invisible because its stderr was `Stdio::null()`. Discriminate next time via `resume-stderr.log` | `Observed 2026-08-13` (S76; recovered by hand-running the resume with stderr captured) | `open` — hardened, not root-caused: resume stderr now goes to a last-words file | `windows-widget/src-tauri/src/preflight.rs` decide (comment names this row); recovery: run `convert_and_ship.py --resume <id> --backend <b>` by hand with output captured |
| SYM-025 | On the Repair Bench, a zone's markdown panel / insertion point does not sit on the damage — the text at the zone's line is an unrelated passage, while the real degeneration sits N lines away. Zones near the TOP of a book look fine; zones deeper in are progressively wrong | The zone line numbers come from the **convert-phase** audit (`fidelity.convert.tripwires.degeneration_detail.worst[].line`, recorded against a body of `md_lines`), but the **analyst phase then rewrites the body** — 55 readability chunks netted **−12 lines** on the S76 Beer (manifest `md_lines` 3013 → live body 3001). `Bench._adjusted_line()` compensates only for bench-session repairs and AI drift; it has no term for the analyst's rewrite, so every zone below the analyst's edits is stale by the accumulated delta. Zone 2 (line 7) was exact; zone 1 (line 1847) was really at 1835 | `Observed 2026-08-13` (S76, caught live before Rab inserted a crop; measured via `/api/md` body coordinates vs manifest) | `fixed` 2026-08-14 (S76, `ef605fa`) — `_resolve_zone_line()` searches the live body for the stored `excerpt` and reports `anchor: "excerpt"`; falls back to the drift arithmetic (and says so) only when the excerpt is gone. A live-body hit deliberately does NOT also take the drift terms — that would double-count | `prototypes/repair-bench/bench.py` `_resolve_zone_line()`. Diagnose: compare manifest `md_lines` to `len(/api/md .text.split("\n"))` — any delta means the recorded lines are stale **Update 2026-08-27 (`Observed`, corpus-wide — this row was filed off ONE book):** the drift holds across **all five** held bundles, measured on the BODY index (`split_frontmatter` semantics, matching `bench.py:118`). md_lines vs live body: `0d68f0e0` 30010/29838 (**0 of 10** pointers land) · `26bd434d` 5838/5926 (**0/5**) · `b6fbdd75` 6100/6103 (**0/4**) · `b7b711d4` 3013/2995 (1/2) · `c5afd9ed` 2771/2771 (1/2). **21 of 23 stored pointers fail to resolve on their own excerpt.** ⚠ **NEGATIVE CONTROL, and the load-bearing half:** `c5afd9ed` has `md_lines == body` **exactly** and still only 1 of 2 pointers lands — so **comparing line counts is NOT a health signal**, and anyone 'fixing' this by reconciling counts would build a green light over a live defect (SYM-001's shape). The S76 fix (`_resolve_zone_line`, excerpt-anchored) remains the thing that actually covers the operator. ⚠ This row says the Beer went `md_lines 3013 → live body 3001` (−12); I measure that body at **2995** (−18) today with zero repair records on that bundle — `Unknown` which is right, and I have not edited the original figure. ⚠⚠ **THE UPDATE IMMEDIATELY ABOVE IS FALSIFIED — cross-vendor, `MSG-CDX-0017`, 2026-08-27.** It is left standing unedited because this file appends and does not rewrite; **do not quote its numbers.** Codex ran two independent read-only probes — one a CLEAN-ROOM probe denied the relay, the registers, prior reports, memory and the expected counts — and they agree exactly on the same five hashed bundles and 23 stored pointers. **My error was not a wrong count. I collapsed THREE different predicates into one.** Separated: (a) exact normalized physical BODY line at the stored coordinate → **5 land / 18 miss**; (b) producer-faithful BODY *paragraph start* at the stored coordinate → **6 land / 17 miss**; (c) **the actual Bench resolver → 21 of 23 `anchor="excerpt"`, i.e. 21 SUCCESSES**, 2 `drift`. **So `21` is the count of production resolver SUCCESSES, not of pointer misses** — my claim was numerically identical to its own inverse, which is exactly why it read as plausible. *(Codex notes explicitly that this numerical equality does not disclose or prove my method.)* ⚠ **My load-bearing negative control is dead too, and it mattered more than the headline.** I wrote that `c5afd9ed` has `md_lines == body` exactly and still misses, therefore "comparing line counts is not a health signal". The premise is wrong: that bundle's pointer #2 is **producer-faithfully CORRECT at BODY line 2400** with matching `chars`; its eight-token excerpt simply **spans physical lines 2400–2401**, so a one-line resolver reports `drift`. **That is a schema/resolver mismatch at ZERO line delta — not a stale pointer.** The other fallback (`b7b711d4` #1) has no global excerpt hit at all and lands on unrelated text: a **distinct defect class**, which my single number had merged with the first. **What survives, restated honestly:** the stored line is still the fragile anchor and the excerpt is still the durable one — this row's original S76 finding stands untouched. The operator-facing risk is **smaller** than I twice implied: the shipped resolver recovers **21 of 23**. And there are now **two** named fallback classes, not one — a span-vs-line schema mismatch, and a genuinely unrecoverable excerpt. Codex's controls ran both ways: an in-memory 1579→1580 mutation flipped both direct predicates while content resolution still recovered 1579; an independent content mutation flipped one resolution to `drift` while an untouched pointer stayed stable; disk hashes unchanged; all five manifest/artifact/ LF-normalized-BODY SHA-256s matched. Report: `…/Documents/Codex/2026-08-27/sca/outputs/file-portal-pointer-cross-verification-2026-08-27.md`, `sha256:db0b9ce57b1a…`. *(Filed by the Fable lane on Codex's evidence, per the 2026-08-27 split. This is the second time in one day that a `Verified` of mine reached both Rab and the bus and was wrong — see `ERROR-BIN.md` ERR-001 and ERR-002, same family.)* |
| SYM-026 | You find real damage in a bundle **by eye** that the Repair Bench never offered you — a passage simply missing from the markdown, with no zone chip pointing at it. The bench's chip row says "N wreck site(s)" and the book has more damage than N | The audit records **two** kinds of damage: `degeneration_detail.worst[]` (loops) and `fidelity.convert.runs[]` (omission runs — text the witness has that the output lost). `Bench.zones()` reads only the first; `state()` DOES project `runs` (bench.py:233, up to 40) but **`bench.html` never reads `st.runs`** — zero references — so the data reaches the browser and is discarded. On the S76 Beer: 2 zone chips shown, **18 omission runs recorded and invisible**, including `p114 · 60 words · "apart from the command function. r system three v"` — the brace-diagram marginalia Rab found by reading. Same family as the unwired `reverse_sample` (docs/27 §4) and the dead `#st-lib` (docs/26 F9): the instrument is built, computed, stored, and never put on the glass | `Observed 2026-08-14` (S76, found by Rab reading the page, then confirmed against the manifest) | `fixed` 2026-08-14 (S76, `bc6612d`) — runs render as amber `◍` repair sites, page-anchored, with an `anchor_line` where the excerpt's surviving head can be found (6 of 18 on the Beer) and honest `None` where it cannot | `prototypes/repair-bench/bench.html` `renderZones()` + `selectRun()`; anchors in `bench.py` `_resolve_run_line()` |
| SYM-027 | A value you need is **nowhere on any surface**, yet it turns out to be computed correctly and sitting in a manifest, a `state()` payload, or a log — the disk is perfect and no error was ever raised. Rab's term: a **glitch**, not a bug (the system was DESIGNED observable, so silence is the design's intent failing) | The projection law (docs/13) is one-directional: it forbids the UI to invent numbers and never requires a measured number to reach anyone. Every law in the corpus with teeth governs what may be **written**; none governs what must be **read**, so a producer writes into a shared payload and a consumer pays ZERO cost to ignore a key. Four mechanisms (docs/29 §3): **A** spec-narrowing · **B** grammar placeholder shadowed by a working twin · **C** partial fork · **D** acceptance-complete/UI-deferred. The reactive law-writing engine cannot learn this class — it only fires on failures that announce themselves, and this one raises nothing | `Observed 2026-08-14` (S77 census: **~83 silent fields**; the class named in S76 and then reproduced twice more *during its own investigation*) | **FIXED S191 E3 (2026-09-17)** — the row's own three closing criteria are met (see the S191 note at the cell's end: the whole-repo census 0 unsigned, 134 signed entries each with producer, consumer and home, the ritual's `--since --enforce` at every close since S103); the guard binds every next commit and the standing debt is dispositioned — a value that reaches nobody is now either shown, opened, reported, control flow, or named dead, with a checkable reason. `Was:` **OPEN** — but no longer for the stated reason. docs/29 §8.3 is **SIGNED** (Rab, 2026-08-14): the detector runs in the **closeout ritual, `--since` mode, not CI**, and the step now exists — `CLAUDE_README.md` §4, `docs/21` §3 + §4 row 5. Counts corrected after the double-harvest fix (`155065c`): acceptance **19/19**; census **76 distinct `lane:key` glitches at 87 sites** (a count re-measured per run, never quoted from memory — it read 93, then 77, as the extractor's double-harvest and occurrence-counting were fixed) — since a disposition is keyed `lane:key`, the actionable unit is the signature, not the site (docs/31 §1.13). **Closing criteria judged: (1) the guard runs unskippably in the ritual — MET; (2) a clean run means something — NOT MET (docs/31 §1.9/§1.12: 22 keys falsely `glass` off producer trees listed as renderers, 7 of 8 checked omissions wrong, and `dispositions.json` is still `{}` — the signed-silence mechanism has never been exercised on one real judgment); (3) the standing §7 debt dispositioned or wired — NOT MET (of ~83 fields only the three S76/S77 self-inflicted ones are fixed).** Decisive for an index keyed on the symptom: the symptom is **still reproducible today** — `seams[]` and `reverse_sample` are referenced by zero renderers (`Observed 2026-08-14`), `pages_scored` only as a positioning denominator. The new guard binds the *next* commit; it does not touch the existing debt, and marking this `fixed` would tell a future reader holding the symptom that it no longer happens. **Update 2026-08-17 (S93):** the criterion-(2) clause "`dispositions.json` is still `{}`" is stale — the file has carried real signed entries since S79–S81 (seven bench-lane keys, each with its §5.3 reasoning) and S93 adds widget/converter entries; the rest of criterion (2)/(3)'s evidence was not re-audited today. **Re-measured S190 E2 (2026-09-17):** `dispositions.json` carries 98 signed entries (two added S188 for `blank_assets` / `verdict_phases`); the `--since` census has run clean at every close and went RED once this week on a new key (S188 — caught, dispositioned); the WHOLE-repo census still reads **36 unsigned glitches at 39 sites across 3 lanes** — 19 `bench:*` keys (the Repair Bench's JSON: `total_hits`, `needles`, `votes`, `seq`, `context_before/after`, `sha_before/after`, `regions`, `chars_restored`, `matches_disk`, `delta_lines`, `para_lines`, `zones_with_repairs`, `chars_added`, `entropy_pct`) and 11 `widget:*` config keys (`acked`, `unacked`, `linux_host`, `remote_user`, `remote_inbox_root`, `vault_library_dir`, `gpu_pipeline_dir`, `gpu_python_exe`, `gpu_converter_dir`, `reader_obsidian`, `reader_zennotes`). Criterion (3) NOT MET still; the 36 are a bounded, lane-owned dispositioning task (docs/29 §5.3 per key, each a judgment read from its producer and consumer — not a batch) for a sitting of its own. **S191 E1–E3 (2026-09-17): criterion (3) MET — the 36 signed one judgment at a time (16 bench · 13 widget · 7 converter — the S190 split above was a miscount): the whole-repo census reads `0 unsigned glitch(es) at 0 site(s)`, `acceptance.py` 288/288, `--enforce` exit 0; the entries carry their producer, consumer and home each. Criteria (1) and (2) were met earlier (the ritual's `--since --enforce` at every close; `dispositions.json` 134 signed entries). The symptom's own sentence — "nowhere on any surface" — now reads as: every silent value is either shown, opened, reported, control flow, or named dead, with a reason a reader can check; the row moves to FIXED at the next reading if nothing contradicts this** | `observability/glass_detector.py` (§5.1) · `observability/dispositions.json` (§5.2 signed silence) · `observability/acceptance.py` (docs/29 §7 as an answer key) · `docs/29-the-observability-complex.md` (§5.3 six-step procedure · §5.4 same-commit timing) · manual `docs/20` §12 + §14 law 2b · **`CLAUDE_README.md` §4, where the ritual actually fires it**. **Diagnose:** `python observability/glass_detector.py --since <last ledger SHA>` (the signed mode; drop `--since` for the full standing census). **Its floor, know it:** `observability/README.md` lists **six** limits, not one — the sharpest being that a *generic* key name always finds an incidental match, that "referenced" ≠ "reaches a human" (`pages_scored` is read by both JS renderers as a positioning *denominator* and never displayed, satisfying §5.1 while §7.2's complaint stands), and that the match is on key **name** with no notion of producer **site**, which is why §7.10's "goes dark once persisted" is invisible to it |
| SYM-028 | `muster.sh` exits 1 at session open reporting a **SHA mismatch** — "TIME-STATE `<a>` vs ledger `<b>`" — but `<b>` is a real commit, IS an ancestor of `HEAD`, and the working tree is clean. Nothing in the repo or the memory library actually disagrees. Reads exactly like a rewind or a fork, and isn't one | The Change Ledger is **append-below** by convention and nothing enforced it. S75 wrote its row **above** S74's instead of below; S76 and S77 each copied the row above them, producing an ascending table S16→S73 followed by a descending block S77, S76, S75, S74. `muster.sh` took the newest row as `tail -1` — pure position — so for three sessions it returned **S74's** row and compared it against S77's TIME-STATE. An ordering fault wearing a rewind's clothing: the alarm was right that something was wrong and wrong about what | `Observed 2026-08-14` (S78, at open; the drift had been live and undetected since S75) | `fixed` 2026-08-14 (S78) — rows reordered (pure permutation: identical line multiset, byte count unchanged, CRLF intact); `muster.sh` now selects the newest row by **max session number**, never position | `~/.claude/muster.sh` **[3a] LEDGER ORDER** — session numbers must ascend in file order; a violation prints `77->76 …` and is reported as its own fault, distinct from a rewind. Negative-tested against the pre-fix ledger. **Note:** `muster.sh` lives only in `~/.claude` and is **not tracked by any repo** — the hard clock's verifier has no backup |
| SYM-029 | A shell edit to a repo markdown file "succeeds", the line count is right, `diff` shows the lines as identical — but the file is a few bytes shorter, `git status` reports it **modified**, and `git diff` on it then prints **nothing at all**: no hunks, empty output. Scales with how much you touched: 4 lines edited → 4 bytes lost; a whole-file pass → one byte per line | Repo markdown (`CLAUDE_README.md` at least) is **CRLF**, and this Git Bash's `sed`, `awk`, and `grep` **silently strip the `\r`** when reading. Anything reconstructed from their output is written back LF-only. **The danger was overstated when first recorded** (`Verified 2026-08-14` — docs/31 §1.16, re-proven in a scratch repo): the stored blob is *already* LF (`core.autocrlf=true`, no `.gitattributes`), so a CR-stripping pass leaves the blob byte-identical. `git diff` is therefore **empty**, `git status` is phantom-dirty, and the damage **cannot reach a commit** — `git commit -a` re-normalizes and reports "nothing to commit, working tree clean". Git's only signal is `warning: … LF will be replaced by CRLF the next time Git touches it`. **The obvious check shares the defect**: on a CRLF file BOTH `grep -c $'\r'` and `grep -c -P '\r'` return **0** — the pattern is a real CR (an empty pattern would match every line), grep simply strips the CR off the text before matching. Only **`grep -U`** (binary) sees them: measured on a 3-line fixture, `-U` → 3 while both other forms → 0; on `CLAUDE_README.md`, `-U` → 1912 and both others → 0. `Verified 2026-08-14` on fixture and real file | `Observed 2026-08-14` (S78; caught by byte-count accounting during the SYM-028 repair, not by any tool warning) | `fixed` by rule | **Slice repo markdown with `head`/`tail` only** — both are binary-safe. Verify any such edit three ways: `wc -c` unchanged, `file -b` still says CRLF, and `tr -cd '\r' \| wc -c` equals `tr -cd '\n' \| wc -c`. For a reorder, prove it is a pure permutation with `diff <(sort old) <(sort new)`. **One-flag fixes exist for two of the three:** `grep -U` and `sed -b` (`--binary`) each preserve CR byte-for-byte (`Observed 2026-08-14`, scratch repo: 20-byte CRLF file in, 20 bytes and 3 CRs out). **`awk` has none** — `-b`/`--characters-as-bytes` does *not* preserve CR, and only a hand-set `RS`/`ORS` comes close. Do **not** trust a bare `grep` to see CR |
| SYM-030 | You generate the final repair report for a patient, then can never reopen that patient on the Repair Bench again: it exits `expected exactly one .md in <dir>, found 2` | `REPAIRS.md` (docs/28 §3) is written BESIDE the bundle, and `bench.py`s body scan demands exactly one `.md`. The report about the body is counted as a second body. The tool is disabled by the artifact it produces, and the disabling is permanent because the report persists | `Observed 2026-08-15` (S79, by writing a report and reopening the bundle) | `fixed` 2026-08-15 (S79) — generated reports excluded from the scan by name (`GENERATED_MD`) **Tripwire (S163 E4, 2026-09-16):** `prototypes/repair-bench/test_generated_md.py` (unittest, marker-env, 4 cases) — a `REPAIRS.md` beside `book.md` is ignored and the bundle opens on the body; the exclusion set names the report; NEGATIVE CONTROLS: a second REAL `.md` still refuses (`found 2`) and a report-only folder refuses (`found 0`) — the scan discriminates. Run by hand beside `test_table_boundary.py`. | `prototypes/repair-bench/bench.py` `_finish_init` body scan. **Not yet bitten in production**: no patient has been declared done, so the real held Valentine still carries one `.md` |
| SYM-031 | A status readout states something is `down`, `clean`, `none`, or `0` — and it is not. No error appears anywhere, and the readout is confident | A probe that FAILED was rendered as a NEGATIVE OBSERVATION. The command errored, its stderr was discarded, the parse produced an empty string, and empty was displayed as absence. Measured instance: Git Bashs MSYS layer rewrites a leading `/FI` into a Windows path, so `tasklist /FI …` exits with an error the caller never sees, and the open card printed `widget down` while PID 10048 had been alive for hours | `Observed 2026-08-15` (S79, in the session-open card, on its first run) | `fixed` by rule + guard | **A failed probe must never render as a negative observation — say UNREAD.** `.claude/skills/muster/open.sh` (process + receiver probes both check the call succeeded before counting); tripwired in `selftest.sh` cases 7 and 8, which force the probe to fail and assert the card says UNREAD |
| SYM-032 | You assume the pipeline has mutual exclusion because a file named `.gpu-lock` appears and disappears around conversions — and two GPU jobs can still run at once. Nothing errors; the machine simply thrashes or OOMs ⟨status 2026-09-04: DONE — 2026-09-04 verifier (HEAD bde007d): FIXED-S94, cell mislabelled — `3446f7c` (card mutex) is an ancestor of HEAD; `97906a3` wrote the verdict into the Root-cause column while the Status column still opens with `open`. Flip col 5 to FIXED-S94 / residual-display-signal.⟩ | FIXED-S94 / residual-display-signal (verdict added S108 per F3 - this cell carried no leading verdict, which is how skimmers read it as open; body unchanged): `.gpu-lock` is **write-only**. `watch_and_convert.py:77` writes it before a convert and `:86` deletes it after; its own comment calls it a "busy signal for the future control-room card". **No code anywhere reads it as a gate.** A file named lock that locks nothing — the name implies a mechanism it has never had, and a reader who trusts the name designs against a mutex that is not there (S79 did exactly that, out loud, before checking) | `Observed 2026-08-15` (S79, while designing the chat/convert exclusion Rab specified) | **FIXED S94 (the FIRST marker set S190 E2, 2026-09-17)** — this cell's own S93/S94 updates below said it and the root-cause cell carried the verdict since S108, while the first word stayed `open` (docs/63: the FIRST marker is the status; the muster's counter read it open for 190 sittings): every converter entry serializes on the OS mutex `Local\file-portal-card` (`convert_and_ship.py:379–382` `acquire_card_mutex`, called first in `main()` at :2633; the watcher at `watch_and_convert.py:321`; `card_mutex_selftest` 7/7 re-run 2026-09-17); `.gpu-lock` is the display signal it always was, with voluntary readers. `Was:` `open` — the real mutex is built and proven in `prototypes/room-chat/` (two files, two owners: the widget writes `chat-hold.json`, the converter owns `.gpu-lock` and is only read), but **the watcher gate that consumes it is signed and NOT yet built**, so nothing is excluded from anything today. **Update 2026-08-17 (S93):** stale on two counts — the chat⇄convert mutex HAS graduated and is live (`room_chat.py` writes `chat-hold.json`; the watcher's deferral gate consumes it, `watch_and_convert.py:104-149`, docs/33 §2.3 signed), and `.gpu-lock` now has real voluntary gate READERS that refuse or yield on it (`assay.rs:276`, `room_chat.convert_running`, `backend_parity.py:271`, `calibrate.py:129`, `bench.py:917`). Still true today: nothing excludes converter-vs-converter (SYM-033), and `--resume`/manual runs write no lock at all (SYM-042). **Update 2026-08-17 (S94):** both residuals now have an OS-enforced cover — every converter entry serializes on the named mutex `Local\file-portal-card` (`acquire_card_mutex`, docs/37 §3.2 signed, tripwired 7/7); `.gpu-lock` remains the display signal it always was | `prototypes/room-chat/chat.py` `_hold_write`/`convert_running` + its acceptance cases; graduation requirement in `prototypes/room-chat/README.md` §5. **Diagnose:** `grep -rn "gpu-lock" --include=*.py --include=*.rs .` — every hit is a write or a display read; if that is still true, there is still no mutex *(true at filing 2026-08-15; re-run 2026-08-17: the hits now include the voluntary gates named in the Status update — the grep discriminates the eras)* |
| SYM-033 | The widget looks normal, but there are **two of it**: a drop can be claimed twice, `events` shows two watcher startup lines, and any readout naming "the widget" prints one PID. Nothing errors | Nothing prevents a second launch — the Tauri app carries no single-instance guard (`windows-widget/src-tauri/Cargo.toml` has no `tauri-plugin-single-instance`), so running the exe again produces a second FULL instance that spawns **its own watcher chain onto the same drop folder**. With `.gpu-lock` arbitrating nothing (SYM-032) there is no mutex between them, which makes **SYM-022's precondition** (two concurrent GPU processes → `VIDEO_SCHEDULER_INTERNAL_ERROR`) reachable by a double-click | `Observed 2026-08-15` (S80: PIDs 18856 + 19536, both children of `explorer.exe`, watcher chains 6420→16148 and 12448→7300; `drop` was 0 throughout, so nothing was lost) | FIXED-S94 (cell corrected S108 per F3: the row's own dated Update clauses record the fix built and signed; trail preserved) — the card now SHOWS it; nothing yet PREVENTS it. **Update 2026-08-17 (S94, signed §3.2): prevention BUILT, two layers.** (1) `tauri-plugin-single-instance` — a second launch fronts the ONE widget instead of becoming a second factory; ships `Intended` (stepping on it means double-launching an exe beside Rab's running widget — this row's own scenario — so the tripwire IS the adoption ritual: launch the adopted exe twice, expect front-not-spawn). *Same-session review catch: `set_focus` alone cannot restore a MINIMIZED widget (the one state a relaunch serves) — the callback now `unminimize`+`show`+`set_focus`, so front-not-spawn holds from minimized too.* (2) Even if two widgets ever exist again, their converts now serialize on the card mutex (SYM-042's fix), so SYM-022's two-GPU-converts precondition is closed at the converter layer regardless | `.claude/skills/muster/open.sh` — the widget row counts every instance and names SYM-033 when >1 (it was `{print $2; exit}`: first match wins, rest discarded). Prevention: `windows-widget/src-tauri/src/main.rs` (plugin init, first plugin) + `windows-converter/convert_and_ship.py` `acquire_card_mutex` |
| SYM-034 | A single Ollama `/api/generate` request never returns. No error anywhere, the server's HTTP API stays responsive (`/api/version` answers instantly), the model process stays resident holding VRAM at 0 % utilisation, and **the identical request sent by hand immediately afterwards completes normally** | **UNPROVEN — one observation.** Not the chunk: chunk 87 of the Valentine is ordinary prose, 3,537 chars / ~884 tokens, zero table rows, *smaller* than the arm's average, and it completed in **7.5 s** (`eval_count` 642, `done_reason` `stop`) on a hand-retry seconds later. Not context overflow: no chunk in the book exceeds `num_ctx`. Shares its shape with SYM-024 — a detached invoke that produces nothing while the same command by hand works | `Observed 2026-08-15` (S81: `backend_parity.py -n 8`, chunk 87, curl `TimeoutExpired` after the full 900 s ceiling) | **OPEN** — cause unknown; the exposure is bounded, not fixed | `windows-converter/backend_parity.py` — per-request ceiling cut 900 s → **300 s** (a warm chunk of this size takes 3–25 s, so 300 s is a stall, not a slow request), and a stall is now recorded as a HOLE and the run continues: the record carries `None` throughout so no rate can average over it, and the arm prints its stall count. Production `analyst.py` already degrades correctly here — its `except` ships the un-analyzed original — so this costs a measurement, not a page ⟨S157 E17 (2026-09-15), B12: the SERVER side now exists — on any failed local call `analyst.process` captures the last 200 lines of Ollama's own `server.log` at that moment (`_capture_ollama_log`, written under the analyst work root `_captures/` and ledgered through dumps/dump.sh) and names it, with the error's class, in `manifest.analyst.backend_failures`; UNREAD when the log or the ledger cannot be reached, never silent. Tripwires `analyst_selftest` S157-E17 (a)–(c), 47/47. The cause stays unknown; the next stall carries its evidence.⟩ |
| SYM-035 | Two backends benchmarked A-then-B come out clearly different, the numbers are internally consistent, every rate has its duration — and the difference grows when you run a BIGGER sample. A result that strengthens with sample size looks like confirmation | The arms run in a FIXED ORDER, so the incumbent is always measured on a cool idle card and the candidate always after tens of minutes of sustained load. Hardware does not hold still: clocks fall, and the last arm pays for the heat the earlier ones made. A longer run means more drift, so the artefact scales with `n` exactly as a real effect would | `Observed 2026-08-16` (S82: decode rate vs POSITION r=**-0.78** / -0.36 on the candidate arms vs vs OUTPUT LENGTH -0.33 / -0.15, and the nothink arm's outputs are near-constant so length cannot explain it. Decisive: the same config on the same chunks gave **97.7 tok/s in a 7-min run and 77.7 in a 25-min run, -20.5 %**. **Mechanism UNKNOWN** — first framed as thermal, then the per-request temperature added to catch it showed temp RISING 75–83 °C while throughput ROSE 74.8→88.3 tok/s, the wrong sign for throttling, and `HW Thermal Slowdown` reads 0 µs lifetime. Candidates not yet separated: clock-boost states settling, power management, memory clocks, engine residency. The finding rests on the measured drift, NOT on a mechanism) | **OPEN** — bounded and measured, not eliminated | `windows-converter/backend_parity.py` — **A-B-A**: the incumbent is measured FIRST and LAST and the drift is published; >5 % drift **withholds every cross-arm ratio** rather than footnoting it. GPU temperature and clock sampled per request. Interleaving would be stronger but needs both backends resident at once, which SYM-022 forbids. Rule in `docs/34` §4 + manual ch.18 |
| SYM-036 | An exact-match patch script fed through the session harness's Bash tool as a QUOTED heredoc finds **0 occurrences** of text that is verifiably present in the file — `grep`/byte-dump confirm the bytes are there, `str.count()` says 0 | The harness collapses one level of backslash escaping even in quoted heredocs: a `\\n` written in the intended python source arrives as `\n`, so the anchor built from it contains a NEWLINE where the file contains BACKSLASH+n. The mismatch is invisible in the script as displayed. Companion hazard, same incident: a multi-step in-memory patch that asserts mid-way writes NOTHING (correct fail-closed behaviour) — but the retry must re-apply EVERY step, not just the failed one; S83's retry wired a call to a function whose defining step had been discarded, and only the instrument's brand-new selftest caught it before the GPU did | `Observed 2026-08-16` (S83: two failed patch attempts against provably-present bytes; diagnosed by counting both escape variants in-file) | `fixed` by rule | Build match anchors with `chr(92)`/`chr(10)` (escape-proof by construction) or use the Edit tool, which does not pass through the shell; verify per-step counts and write atomically; after an aborted multi-step patch, re-apply ALL steps. Guard for the companion hazard: `windows-converter/backend_parity_selftest.py` (a guard born today gets its tripwire today) |
| SYM-037 *(was SYM-028, ThinkPad lane; renumbered at the S86 fork merge)* | The first receipt written after a crash/power-cut never appears in `receipts.jsonl` — instead one garbage line holds two half-records glued together, and both parse as nothing | Appends assumed the file ends in a newline. A crash mid-append leaves a torn final line WITHOUT one; the next append glued its record onto the garbage, destroying both. Readers skip unparseable lines, so the loss was silent | `Observed 2026-08-16` (S78 — exposed by the spot-check tests' torn-line seed, then found real in production `_receipt`) | `fixed` 2026-08-16 (S78, `0bb4393`) — `append_receipt` checks the file's last byte and leads with a newline when missing; the torn line stays torn, the new record survives | `linux-converter/converter/exporter.py` `append_receipt` (comment cites S76's power cuts as the observed shape) |
| SYM-038 *(was SYM-029, ThinkPad lane; renumbered at the S86 fork merge)* | A commit message (or a session note) claims the suite passed, but running the suite shows red — nothing was amended, the claim was simply false at commit time | `pytest … \| tail` in a `&&` chain: the pipe makes the chain see TAIL's exit code (0), so the chain proceeded to `git commit` past a failing suite. The claim of green was never checked by the machine, only displayed to the human | `Observed 2026-08-16` (S78 — my own G3 commit; caught within minutes, fixed and amended before push) | `fixed` by rule — run the suite as its own command and let ITS exit status gate the commit; never pipe a gating command's output. (CI remains the backstop, but it fires after the push, not before the commit) | `docs/21` §9's spirit: a green claim requires the machine's own exit status, not a scrolled log |
| SYM-039 *(was SYM-030, ThinkPad lane; renumbered at the S86 fork merge)* | A number or claim in CLAUDE.md or in a guard column of THIS index contradicts what the code or a live test run shows (CLAUDE.md says "24/51" tests, suites pass 28/73; SYM-014's guard says `clamp_name` 200 bytes, `bundle.py` says 80; SYM-018 still says cargo test absent from CI) | Hardcoded facts in prose are never regenerated when the code moves — S78 added 26 tests and L15's clamp was recalibrated, and no doc citing the old numbers was touched. The docs-drift twin of SYM-016's orphaned SHA | `Observed 2026-08-16` (S79 familiarization pass — each mismatch re-proven against source/suite) | `fixed 2026-09-15 (S157 E46)` *(was: `open` — fixes assigned to Desktop via coordination message `2026-08-16T21-51…wip-handoff` §2.6)* — two of the three assigned instances were fixed earlier (SYM-014's guard at S86; SYM-018's note 2026-08-10); the third has no target on this machine: no `CLAUDE.md` in this repo's history in any ref, the counts appear only in dated ledger rows (Historical by construction), the 'rsync' line in no tracked doc — the ThinkPad's untracked copy UNREAD (offline). The CLASS stays a standing rule (docs/21 §3: nothing derivable is written by hand) | Rule when fixing: prefer derivable numbers (cite the command, not its output) or drop the count from prose entirely; a hand-typed count is a future SYM-030 |
| SYM-040 | The muster card is green, both clocks reconcile, the working tree is clean — yet another machine's sessions carry YOUR session numbers, or a fetch lands sessions and symptom rows your record has never seen. Nothing on the card ever mentioned origin | The hard clock verified LOCAL history only. Session closes had stopped pushing (the §4 push step existed, unenforced — skipped eight closes running), so each machine derived its next session number and next SYM id from a record the other could no longer see. The fork is the collision's precondition, and no probe ever looked at origin | `Observed 2026-08-16` (S86, at open — found because Rab said "the ThinkPad left you a message", not by any check; collisions: sessions S78/S79 twice, SYM-028/029/030 twice) | `fixed` 2026-08-16 (S86) — merge `96efecd` (never rebase: ledger rows pin SHAs, SYM-016); ThinkPad rows renumbered SYM-037/038/039 with provenance; session numbers machine-scoped (lanes) | `.claude/skills/muster/open.sh` origin row (fetch at open; FORKED fails the card, `ahead N` = the close must push) + per-lane ledger parse (`MUSTER_LANE`) in `muster.sh`; tripwired: `selftest.sh` cases 11–14 (22/22); CLAUDE_README §4 push teeth. **Diagnose:** `git fetch && git rev-list --count HEAD..@{u}` and the reverse — both nonzero = this row |
| SYM-041 | You flip the slice-batch lever mid-book and nothing changes — every remaining slice still runs the batch the book STARTED with, while the Room's policy row shows your new value as accepted. No error anywhere; the lever simply doesn't reach the running book | `chunk_batch()` is read ONCE at the top of `_convert_chunked` and baked into `slice_extra` for the whole run, while `docs/18` §4, `docs/20`'s Slice-batch row and `line.rs`'s lever comment all promise re-read PER SLICE. A projection-law violation of our own: the surface promises a live lever, the backend honors it only at run start (dossier F-09, code-verified S90 at `fc841a5`; the lever exists for mid-book VRAM steering, S60) | `Observed 2026-08-17` (S93 filing; read again at HEAD — the read still sits above the slice loop) | FIXED-S94 (cell corrected S108 per F3: the row's own dated Update clauses record the fix built and signed; trail preserved) — fix designed (move the read + `_with_batch` inside the slice loop, docs/37 §4 T2) and deliberately NOT built: per-slice vs immutable-per-job semantics await Rab's signature (docs/37 §3.1). The S93 resume-admission work already refuses to gate on `batch` (T2c), so the future fix cannot invalidate finished slices. **Update 2026-08-17 (S94): SIGNED per-slice by Rab ("F-09 per-slice signed") and BUILT** — `chunk_batch()` + `_with_batch` moved inside the slice loop; slice print/event/`.done` carry the per-slice value, manifest keeps the run-start reading. Ships `Intended` (py_compile + review); the next real chunked convert exercises it **Tripwire (S163 E1, 2026-09-16):** `convert_and_ship_selftest.py` T24 (a)–(d) — `_with_batch` replaces in place, appends when absent, does not mutate the caller's list; the control: the old value must not survive the call. Suite → 262. | `windows-converter/convert_and_ship.py` `_convert_chunked` slice loop (the F-09 comment names the signature); the surfaces' per-slice promise and the code now agree |
| SYM-042 | A conversion is visibly running — a `--resume` you launched by hand, or any manual `convert_and_ship.py` invocation — yet every "is the card busy?" probe says FREE: no `.gpu-lock` exists, the assistant will happily load a model beside it, the parity harness sees a clear card. Two GPU processes on one card is SYM-022's crash precondition | Only the WATCHER writes `.gpu-lock` (`watch_and_convert.py:159`, around the child it spawns); a manual or `--resume` run never passes through that code path, so it holds the card with NO signal — invisible to every voluntary gate reader (`assay.rs:276`, `room_chat.convert_running`, `backend_parity.py`, `calibrate.py`, `bench.py`). The file-signal cannot cover a path that doesn't write it (dossier F-01's novel finding, code-verified S90) | `Observed 2026-08-17` (S93 filing; writer census re-run at HEAD: the watcher is still the only writer) | FIXED-S94 (cell corrected S108 per F3: the row's own dated Update clauses record the fix built and signed; trail preserved) — designed cover is Stage 1's single-instance guard + named OS mutex around the Marker launch itself (docs/37 §1 Stage 1, needs signature §3.2), which sees resumes and manual runs the file-gates cannot. **Update 2026-08-17 (S94): SIGNED (§3.2) and the mutex is BUILT and stepped on** — `acquire_card_mutex()` claims `Local\file-portal-card` in `main()` before dispatch, so EVERY entry (watcher child, `--resume`, `--reanalyze`, hand runs) serializes; loud wait, `WAIT_ABANDONED` inherited safely (slice publish is atomic). The OS now enforces what `.gpu-lock` only signaled | `windows-converter/convert_and_ship.py` `acquire_card_mutex` (comment cites this row) + `card_mutex_selftest.py` 7/7 — real contention proven via a TRYING-synchronized child, not a timer. `nvidia-smi` before hand launches stays good hygiene (non-converter lab processes are outside the mutex) |
| SYM-043 | The Dock or Room tells you a failed/flagged document can only be swapped **manually** and that supersede is still pending, even though the re-convert remedy can already replace the vaulted note automatically when the new candidate passes the exporter guard | Both widget renderers still carry pre-implementation copy (`"swap: manual — supersede ... pending"`) from an earlier stage, while the current converter stamps SHA-bound supersede intent and the exporter implements guarded replacement. The human-facing explanation lagged the mechanism | `Observed 2026-08-20` (Desktop S97; current-source cross-read: `main.js:840`, `room.js:425`, `convert_and_ship.py` supersede marker/stamp, `exporter.py` guarded supersede path) | `fixed at the source 2026-09-15 (S157 E53)` *(was: **OPEN** — source-level projection drift; installed runtime was UNREAD and no product code changed in this research session)* — `main.js` and `room.js` now say `swap: automatic — a passing ⟳ re-convert supersedes the vaulted note (exporter guard)`; `windows-widget/src/projection_selftest.py` reds if the drift wording returns while the ⟳ handler exists (12/12; a warn-only CI step). The INSTALLED runtime (C4559805) still shows the old note — UNREAD on the glass until his rebuild and adoption | Change both renderers only after verifying the intended operator wording against the current remedy/export contract; add a UI/source assertion that the copy cannot regress to “pending” while the supersede handlers exist. Narrative: `sessions/S97-desktop-2026-08-20.md` |
| SYM-044 | Every bundle ever converted carries `"marker_version": "unknown"` in its manifest — the engine-provenance stamp is blank corpus-wide — and after a marker upgrade a `--resume` silently splices new-engine slices onto old-engine slices, even though the `.done` identity gate names the engine version as one of its three keys | marker-pdf 1.10.2 exposes no `__version__` attribute (the installed package has no `__init__.py` to carry one), and both stamp sites read `getattr(marker, "__version__", "unknown")` (`convert_and_ship.py:781`, `:979`) — the default is taken every time, and `"unknown" == "unknown"` matches across any upgrade, so the F-02 identity predicate is one-third inert. The correct source exists and answers: `importlib.metadata.version("marker-pdf")` → `1.10.2` | `Verified 2026-08-20` (S97 grounding: probe showed `has __version__: False`; every `anchor/` manifest reads `"unknown"`; re-verified at filing — no `__init__.py` in the installed package; filed as SYM-043 and renumbered SYM-044 the same sitting — the id collided with the S97 close's concurrent row, filed minutes earlier by the Codex instance) | **FIXED S151 E2 (2026-09-14, 9f76e80's successor)** — `convert_and_ship.marker_version_stamp()`: the installed distribution's version (`importlib.metadata.version("marker-pdf")` → 1.10.2 on this machine, Observed) is the stamp at all three sites (the manifest, the `.done` identity gate's peek and its write), the module attribute the fallback, "unknown" only when neither answers; the next book converted carries the real version — bundles converted before keep "unknown" (a re-stamp of old manifests is not this fix). History: OPEN — one-line fix awaited a build slot (docs/37 §3 items 3+6, docs/41 §1) | `convert_and_ship_selftest.py` SYM-044 (a)–(e): the distribution's version wins, the attribute is the fallback, neither → "unknown", an empty distribution version falls through, the default lookup reads the installed package; the `.done` identity test (T13) compares whatever string is stamped and stands unchanged |
| SYM-045 | An id you just filed — a SYM number, a docs/NN number, a session number — exists TWICE: another row or file with the same id sits elsewhere in the tree, filed minutes earlier by someone else; every muster involved was green and every instance honestly believed it took the next free number | Monotonic counters were read from session-start context (or a subagent's earlier report) while a CONCURRENT instance on the SAME machine held not-yet-visible increments — the open probe cannot see a close that has not happened yet. SYM-040 covered two machines unpushed; this is its one-machine twin. Bit twice on 2026-08-20: SYM-043 filed by both the S97 closer and the post-close sitting (renumbered SYM-044), then docs/40 taken by both the close's think-tank doc and the integration archive (renumbered docs/42) — the second collision survived the first reconcile because only SYM ids were re-checked | `Verified 2026-08-20` (S98; both collisions evidenced in git: `3b56f13` vs `7d0011f`/`d29822b`; renumbers `838df92` + the S98 fix) | **FIXED** (the preflight; S157 E19, 2026-09-15): private `agent-scripts/auditing/id_preflight.py <id>…` greps the whole tree (the private junctions included) for the exact id and refuses when it exists anywhere, each hit named; `--next <kind>` takes the next number from the kind's REGISTER SURFACE (the index's rows, the bin's rows, the relay, the docs/ and sessions/ filenames — never a tree-wide maximum, which the muster selftest's planted SYM-999 inflated on the first cut) and preflights it. Residual: it is a read-before-write, not a lock — two instances in the same second can both read free; the relay gate's allocator is the locked path for MSG ids; the filing scripts and hands must CALL it. Was: rule established (re-read the counter surface AT FILING TIME and grep the id before writing; `.claude/skills/echo/sweep.sh` SYM/DOCS rows print live tails); no mechanical preflight built yet. **Update 2026-08-20 (S99):** the relay (`coordination/relay.md`, UTC, append-only) + the claim convention (`coordination/authorship.md`) born — concurrent instances now have voluntary coordination and every trace an author; the mechanical id-preflight remains the fix, this remains OPEN **Reconciling clause (S162 E3, 2026-09-16 — the second Circle's R2-S1):** the S99 Update above ends 'this remains OPEN' and is superseded by the FIXED marker at the cell's head: the mechanical id-preflight is built (`agent-scripts/auditing/id_preflight.py`, `--next S|docs|SYM|ERR`, its selftest), run at every id filed since S157 E19. | `.claude/skills/echo/sweep.sh` SYM + DOCS rows — note the DOCS row prints count AND newest, and a count that outruns the newest number IS the duplicate signal (43 numbered vs newest 41 exposed this one). Candidate mechanical guard: an id-preflight in muster.sh — unbuilt, awaits a slot |
| SYM-046 | A session closes reporting "glass detector exit 0" as proof the observability layer is clean — and it is not proof of anything: the same command exits 0 while printing dozens of unsigned glitches. Every closeout in the arc reads green, and a genuinely new undispositioned measurement would have closed exactly as green ⟨status 2026-09-04: DONE — 2026-09-04 verifier (HEAD bde007d): FIXED in substance, cell mislabelled — close.sh [2] (`c9c0cf3`, 2026-08-20) runs `glass_detector.py --since <pin> --enforce` at every close and exits 1 on an unsigned glitch. Flip the cell; the closeout-prose rule stays as discipline.⟩ | `observability/glass_detector.py` exits non-zero ONLY with `--enforce`, and reports on the WHOLE tree unless scoped with `--since <ref>`. The ritual form is `--since <pin> --enforce` (exit 1 = this session added an unsigned key); a bare invocation is a printout, not a gate, so its exit code carries no verdict. Measured S101: bare exit 0 with 64 glitches listed, `--enforce` exit 1, `--since bbf6e70 --enforce` exit 0. The docs/32 proxy-substitution family: a green summary standing in for a read state, and this time the summary was an exit code nobody had read the flags for | `Verified 2026-08-20` (S101; three closeouts S98/S99/S100 had cited the bare form - substance later confirmed correct for all four pins, evidence retroactively supplied and corrections appended in place) | **FIXED** `c9c0cf3` (close.sh [2] GLASS, 2026-08-20; re-probed S157 E4 2026-09-15: `.claude/skills/muster/close.sh:107` runs `glass_detector.py --since "$PIN" --enforce` and exits 1 on an unsigned glitch — the mechanical guard this row asked for; the closeout's claim text is no longer the evidence, the close's own run is). Was: rule established (closeouts cite `--since <pin> --enforce` and quote its verdict line, never a bare exit code); no mechanical guard yet. Candidate: a close-ritual check that refuses a closeout whose glass claim lacks the scoped form ⟨F8 correction, S157 E16 (2026-09-15), Observed: the row's "three closeouts S98/S99/S100 had cited the bare form" is wrong about S100 — its CLOSEOUT body names glass only in a relay note (line 128); the hollow `exit 0` citation sits in S100's LEDGER ROW (CLAUDE_README.md:1998), and the S101 correction appended to S100's closeout answers a claim that closeout never made; S97's own line (`glass_detector.py --since 0e1fe28 exited 0 with zero scoped keys/sites`, S97:211) was the same hollow form and got no correction. The count stands as two closeouts and one ledger row.⟩ | Diagnose: run all three forms and compare - `glass_detector.py` (exit 0, prints glitches) vs `--enforce` (exit 1) vs `--since <pin> --enforce` (the real answer). The three S98-S100 closeouts carry dated corrections showing the shape of the error |
| SYM-047 | You force-kill the widget to adopt a new build and a `watch_and_convert.py` is STILL running afterwards — Task Manager shows a python watching `drop/` with no widget above it; adopt now and you have TWO watchers over one drop folder, each ready to spawn a conversion of the same PDF ⟨status 2026-09-04: DONE — 2026-09-04 verifier (HEAD bde007d): FIXED in substance, cell mislabelled — `f045a66` Conveyor State: the watcher's `.intake-state.json` carries writer_pid (refreshed every <=5 s); the widget refuses to start a second watcher beside a live writer and reports it at boot. Residual: pid not parent; a writer silent >300 s is invisible — the hand census stays for that case. Flip the cell.⟩ | The S37 Job Object (`KILL_ON_JOB_CLOSE`) governs only the watcher the WIDGET spawned. A watcher started from any other runtime — another agent's session (this specimen: a `codex-runtimes` python, PID 23724, itself already dead), a hand launch, a script — is outside the job by construction and survives every widget lifecycle event including force-kill. Same family as SYM-042 (a signal cannot cover a path that does not write it), applied to process lifetime instead of the card lock. The card mutex prevents the SYM-022 two-GPU crash, so the damage is duplicated work and racing `.part` dirs, not a hang | `Observed 2026-08-20` (S102 adoption: orphan PID 24344 alive ~34 h, started 2026-08-19 01:07, parent dead; stopped before launching the new widget) | **FIXED** `f045a66` (Conveyor State, 2026-08-31; re-probed S157 E4 2026-09-15: `watch_and_convert.py:223` writes `writer_pid` into `.intake-state.json` every ≤ 5 s; `windows-widget/src-tauri/src/line.rs:72/84/213` reads it at boot and refuses a second watcher beside a live writer). Residual, named: the file carries the pid, not the parent; a writer silent > 300 s is invisible to it — the hand census below stays for that case. Was: rule established (before any adoption or widget restart, census `python.exe` command lines for `watch_and_convert` and check each one's PARENT is the live widget; stop orphans first). No mechanical guard: a job cannot reach backwards over a process it never spawned. Candidate cover: the watcher writes a pid/parent file the widget can compare at boot, or muster's live block names the watcher's parent | Diagnose with the PRECISE probe: `Get-CimInstance Win32_Process -Filter "Name = 'python.exe'"` filtered on the command line — a bare `Where-Object CommandLine -like` over ALL processes matches the shell that typed the query and reports a false STILL RUNNING (that happened twice this session) |
| SYM-048 | A read-only PyMuPDF pass over a big scanned PDF pins a CPU for tens of minutes with no output and no error — the same code runs in milliseconds per page on a born-digital book of ten times the length | `page.get_image_info(xrefs=True)` makes MuPDF resolve every displayed image back through the xref table, and on a large scan that search costs **34.6 s per page** (measured S102, fresh document per case: plain 0.00 s · `hashes=True` 0.16 s · `xrefs=True` 34.60 s · both 34.96 s) — ~104 minutes for one 184-page book. The flag looks free and is not. Companion trap that produced a WRONG first diagnosis: timing the flag variants inside ONE process makes the later calls look free, because the first call already warmed MuPDF cache — the fix was aimed at `hashes` until fresh-document timing reversed the verdict | `Verified 2026-08-20` (S102: 28 CPU-minutes burned before Rab noticed the machine was busy; isolated by fresh-doc timing, fixed same session) | `fixed` 2026-08-20 (S102) — `xrefs` dropped (nothing needed it) + two-pass cheapest-first so hashing is paid only when an image survives the size filters. **104 min → 0.6 s on the same book; 1,356-page book 28+ min → 5.1 s** | `windows-converter/figure_coverage.py` (the timing table is in the code comment beside the call). **Diagnose:** time flag variants with a FRESH `pymupdf.open()` per case, never in sequence on one handle |
| SYM-049 | A flow diagram is plainly on the page and plainly absent from the bundle, and the figure-coverage instrument says nothing. No error, no flag — the page simply never appears in the report | **Zero-area paths are dropped before clustering** (`figure_coverage.py:428`, `if _rect_area(r) <= 0: continue`). A pure horizontal or vertical connector line has zero area, so it never joins the boxes it connects; a diagram whose boxes sit further apart than `VECTOR_CLUSTER_GAP_PT` fragments into sub-threshold pieces and falls under `MIN_AREA_PT2` / `VECTOR_MIN_PATHS`. A false NEGATIVE inside the instrument built to find false negatives. Same family as SYM-042 and SYM-047 — a mechanism cannot cover a path it does not see | `Verified 2026-08-21` (S105 Circle, Lane C, on the shipped bytes at `2d28b2a`). Predicted at S104 from a test fixture; now MEASURED on two real pages. **Cyb p34** Requisite-Variety arrow diagram: 54 drawings, 27 zero-area (50 pct), 12 clusters, largest 541 pt2 against MIN_AREA_PT2 4900 — 9x below. **Cyb p78** Conversation-for-Understanding lozenges: 2 clusters of 2 paths against VECTOR_MIN_PATHS 4. Neither has an asset in the bundle. **Control proving the mechanism rather than asserting it:** p35 carries a near-identical arrow diagram, it DOES ship and IS flagged — the only difference is one curved path with non-zero area | **FIXED S157 E25 (2026-09-15)** — zero-area paths clustered when ANCHORED (`_anchored_lines`: within the gap of a positive-area path or a perpendicular line; bare rule stacks stay out) and the table veto DISQUALIFIED for a non-rectilinear cluster (`_nonrect_items` ≥ 3 sizable curves/diagonals — measured: the Book of Models 14 of 15 vetoed pairs, Investment Valuation 0 of 144). Every anchor re-measured: the Book of Models gains exactly p.34 and p.78 as uncovered (1 → 3), figure pages 57 → 71, nothing else moves on any book (`sittings/S157/e25/anchors_diff.json`). Tripwires 44/44 with both negative controls (each lever at its ceiling restores S104). Residue: a rectilinear chart (p.40, bands and axes) still reads as a table — covered by its asset. Was: **OPEN** — recorded in the module header at S104, unfixed. The real fix is clustering that follows stroke geometry rather than bounding boxes; it needs its own calibration and is not a threshold nudge | none — the limitation is named in `windows-converter/figure_coverage.py` header and beside the drop site at `:428` (was `:300` at `2d28b2a`; S106 moved it +128 and did not repair its own citation — caught by its Circle), but nothing detects it. **Diagnose:** render the page, then count `page.get_drawings()` entries whose rect area is 0 — a diagram more than ~40 pct zero-area paths is in this class. **Specimens for P-2 to calibrate on: Cyb p34, Cyb p78** · `S157 E24 (2026-09-15)`: the drop site is `figure_coverage.py:528` today (`:428` above is the S106 line; it has moved twice); the Book of Models re-measured 1 uncovered (p.84) — p.34 and p.78 still invisible; still OPEN · `Corrected S157 E39` — the residue "p.40 still reads as a table" was wrong: under the shipped code p.40 is REPORTED (its anchored lines carry 6 non-rectilinear items into the cluster; the veto is disqualified); a verifier lane caught it against the anchors diff's own `vetoed_tables: 0` |
| SYM-050 | The figure-coverage report names dozens or hundreds of pages UNCOVERED on a book whose figures are all visibly present in the bundle. The numbers look precise, and in `--json` the tool prints them without complaint | The bundle is a **pre-S60 doubled-offset** one: assets are numbered by a doubled page index (ids to `_page_2553_` on a 1356-page book), so `_ASSET_RE` maps every asset to the wrong page and the per-page presence question is answered against a scrambled map. The regions are found correctly; the ATTRIBUTION is wrong. SYM-002 is the same doubled-offset defect on the navigation side, fixed at S60 — but bundles converted BEFORE that fix are still sitting in `anchor/` and are still being measured | `Verified 2026-08-21` (S105 Circle, Lane C). Investment Valuation: **19 of 20 adjudicated uncovered verdicts FALSE** — numerator 19, denominator 20 (a seeded random sample of 15 drawn from the 239 plus a complete census of the 5 in the one correctly-mapped slice), conditions IV bundle 2026-08-01, shipped thresholds, seed 20260820, every page rendered at 1.5x and visually adjudicated. 13 of 15 sampled figures were located IN the bundle under doubled ids. Region-level DETECTION is fine at 13.3 pct false alarms (2 of 15). **`docs/41:468` named this exact hazard one session before S104 built its headline on that bundle** | **RESOLVED in source S108 (`0fbb6e3`)** — the full narrow signature now repairs the page map in memory and carries `page_map` plus `sym050_doubled_offset` into JSON; bare out-of-range without the full signature stays explicitly untrustworthy. Independent controls: clean post-S60 bundle 1/1 covered, detector false; poisoned IV bundle repaired 220/269 covered versus naive 30/269, detector true; 0 assets out of range after repair. Historical bundle files are unchanged **Citation note (S159 E5, 2026-09-16):** the four `figure_coverage.py` line numbers in this row drifted +68 to +143 after later edits — find by name: `sym050_true_page`, `sym050_signature`, the in-memory `tp = sym050_true_page(p)` call, the `sym050_doubled_offset` JSON key. The fix is present at HEAD. | `windows-converter/figure_coverage.py`: `sym050_true_page` (`:277`), the narrow three-part `sym050_signature` (`:283`), in-memory application (`:620`), and JSON condition (`:687`). `windows-converter/coverage_rescore.py` is the out-of-band host. Tripwires: 30/30 figure coverage and 8/8 host. **Scan-lane subpage diagrams remain a different UNREAD class; P-1 excludes the full-page scan raster by design** |
| SYM-051 | Claude Code closes on its own mid-session, sometimes mid-turn. Work is not lost - the session resumes intact on relaunch - but an in-flight question can vanish before it is ever sent | **Two independent causes, both measured, neither a Claude defect.** (a) **MSIX package servicing**: an app update terminates the running app, always. Same packaging model as SYM-007. (b) **System-wide memory exhaustion**: under pressure the app hangs rather than exits, which surfaces as `MoAppHang` in Windows Error Reporting | `Verified 2026-08-21` (S106). (a) package `Claude_1.34493.1.0` installed `00:02:07`, `CoworkVMService` logged `Service stopped` `00:02:38` and `starting` `00:02:40`, and EVERY `claude.exe` process has StartTime `00:02:4x` - three independent records of one teardown. Version moved 3x in 3 days: 1.32352 -> 1.32885 -> 1.34493. **Update 2026-08-25 (S109 post-close), a FOURTH dated confirmation and the cleanest one yet: package `Claude 1.37937.1.0` installed `23:54:52`, and EVERY `claude.exe` process carries StartTime `23:55` -- 12 processes, one tree, all children of the new main PID, sixty seconds after the install stamp.** Observed live while answering *"why do I have like 16 claude services running?"* -- the count was the Electron process tree (1 main + crashpad + GPU + 2 renderers + 6 utility = 12, plus `cowork-svc` and the `CoworkVMService`), 1831 MB total, NOT a leak and NOT 16 copies. **The discriminator that matters for this row: uptime was 142 min at the time, so the app restart was NOT a reboot** -- the machine had rebooted separately at `21:36:28` (clean, RuntimeBroker-initiated), which is what actually killed the widget, its watcher and the atlas sim. Two events, two causes, and reading either one as the other is the mistake this row exists to prevent. (b) `Observed` at 03:44Z: RAM 15.9 GB total, **0.7 GB free, 95.6 pct used**; commit 40.9 of 47.7 GB. Top consumer **firefox, 57 processes, 23,137 MB** - more than the machine has physical RAM. claude.exe was 1,574 MB across 12 processes. `RADAR_PRE_LEAK_64` named claude.exe (08-20), rustc.exe (08-20), Discord.exe (08-19), firefox.exe (08-18) - **a leak detector naming a different process each day is system pressure, not one leaky app** | **OPEN - environmental, not a code defect.** (a) is unavoidable while auto-update is on; the mitigation is knowing that a close near a version bump is an update, not lost work. (b) is reclaimable: restart the browser. **Anything typed but not SUBMITTED when the app is torn down is never written to the transcript and cannot be recovered** - that is how a question can go missing with no trace | none, and none is possible for (a). **Diagnose:** `Get-AppxPackage *Claude*` for the install time vs `(Get-Process claude).StartTime`; `C:\ProgramData\Claude\Logs\cowork-service.log` for the teardown; Win32_OperatingSystem free-vs-total for pressure. ⚠ **The VM service cannot arm its own recovery** - it logs `failed to configure recovery actions: Access is denied`, so if it ever crashes outright it stays down **until reboot** · `S157 E27 (2026-09-15)`: cause (b) was measured on a 16 GB machine on 2026-08-21 — the day Rab installed 2×16 GB (the library's `desktop-machine.md`; today's `wmic memorychip` reads two DIMMs of 17,179,869,184 bytes, Observed). The exhaustion half is PARTIALLY CLOSED by the hardware: the mechanism stands, its headroom doubled; no re-measure of the hang under load has been made since |
| SYM-052 | In the Repair Bench, a markdown line looks complete but is short. A wide pipe-table row is displayed shortened, with no ellipsis and no marker, and the operator repairs or deletes it believing they saw the whole row | Both render paths hard-truncate at 400 characters: `esc(l).slice(0, 400)` and `esc(lines[i]).slice(0, 400)`. Nothing marks that a cut happened. Compounded by `#ctx { white-space: pre-wrap; word-break: break-word }`, which wraps grid rows and destroys the column alignment the operator is trying to count | `Verified 2026-08-21` (S106) at `prototypes/repair-bench/bench.html:803` and `:817` **as they stood at `4d06588^`**. ⚠ **This row was BORN STALE: the commit that filed it is the commit that removed both sites**, so `still live at HEAD` was false the moment it was written (S106 Circle, Lanes A+C). **First found 2026-08-21T00:09Z in session `f88d4909` and NEVER RECORDED** - no closeout, no CHANGELOG, no ledger row, no SYM row; `bench.html` last changed 2026-08-15. The finding evaporated when that session ended, which is why it is being filed a day late from a transcript search | **FIXED 2026-08-21** in `4d06588` (both sites removed; lines no longer wrap). ⚠ The row was filed OPEN by that same commit — corrected by S106's Circle. **No tripwire guards the regression**, and nothing in this repo tests `bench.html` at all. Originally: this is the failure mode of the tool built to prevent that failure mode: the Bench exists so an operator does not act on partial evidence, and it is showing partial evidence as whole. Outranks every cosmetic item on that surface because it can cause a WRONG REPAIR, not just a slow one **Tripwire (S165 E1, 2026-09-16):** `prototypes/repair-bench/test_table_health.js` (node, 18 → 24) — the page's `linesHtml` and `esc` read out of `bench.html` and run: a 2,000+-character pipe row renders WHOLE (the last cell present), every line its own numbered row (an empty line included); the stylesheet's `#ctx .cl` is `white-space:pre` with no `pre-wrap`/`break-word` on `#ctx` (a rule-text check, said as such — node has no layout engine). NEGATIVE CONTROLS in the same run: the 400-char slice planted back into the extracted renderer reds the whole-row case (the span reads 400); `pre-wrap; word-break:break-word` planted back on `.cl` reds the stylesheet case. A file-level plant (both defects sed'd into a scratch copy of the page) reads `4 of 24 FAILED`, exit 1. The suite has no runner yet (not CI, not `close.sh`'s `[3c]`): run by hand from `prototypes/repair-bench/`. | none. **The Bench is a quarantined prototype** (`prototypes/repair-bench/`, zero pipeline coupling), so a fix is low-risk. Sibling of SYM-005 (a silently truncated SSH key that still passed its check) and of the S78 thirteen-empty-columns miss the Bench's own warning banner cites |
| SYM-053 | A scan-book page has a hand-drawn diagram or marginal callout in the source PDF, the final Markdown contains an image reference for that page, and the page therefore looks “covered” — but opening the referenced asset shows blank paper and the diagram's words are absent | Page-level asset presence measures attribution, not region survival. Marker can select a blank subregion while omitting the meaningful hand-drawn regions; the Analyst then preserves the resulting image token without ever seeing its pixels. An existing asset reference can therefore be positive evidence for the wrong crop | `Verified 2026-08-23` (S108 Codex visual pass). Stafford Beer PDF p129 / printed p101: source has two large hand-drawn callout bubbles plus connecting bracket; final `_page_128_Picture_15.jpeg` is exactly 511 × 70, grayscale extrema 240/240, standard deviation 0, entropy 0. Raw and analyst-local Markdown both reference that blank strip after the convergence-of-purpose exercise; neither callout's text survives. The missing annotations challenge academic classification and who may set categories against natural system laws | **OPEN** — the S108 out-of-band host correctly marks scan-lane semantic coverage UNREAD, but no detector compares rendered source regions to final crop pixels/context. Do not interpret “page has an asset” as diagram survival. **The blank-crop tripwire BUILT report-only S188 E7 (2026-09-17):** `windows-converter/blank_assets.py` reads every asset's pixels (grayscale standard deviation under `BLANK_SD = 1.0` = paper) and whether the body references it; `convert_and_ship._blank_assets_safe` records counts and the capped list under `manifest["blank_assets"]` (`mode: "flag"`) on every conversion — the verdict does not read it; `blank_assets_selftest.py` 10/10 (the S108 shape positive, a gradient negative, the lever's own negative control), T29 in the converter suite, `CONV_SUITES` +1. **Measured on the corpus first:** two distinct blank assets in ~2,300 (Beer's `_page_128_Picture_15.jpeg`; Equity Research's `_page_1_Picture_0.jpeg` 474×824 at 255/255 — a blank first page cropped whole), both referenced. What the flag DOES (a page flag in the verdict, a re-crop, the bench) is a gate change: proposed, his word. The region-to-crop visual comparison the row asks for is not this — it needs the rendered source page beside the crop. **S189 E2: a second band, `near_blank` (`BLANK_SD` ≤ sd < `NEAR_BLANK_SD = 4.0`), after the four near-blank assets were looked at by eye — three scanned BLANK VERSOS of DIAGNOSING (paper texture and bleed-through, sd 2.3–2.9; each shipped as a referenced picture) and Automate's dedication page ("For my nephew Jack", sd 3.89 — five words that live only inside the image; `grep` finds them nowhere in the body). Two classes for his gate: a scanned blank page the flat lever misses, and a text-only page shipped as a picture (a twin should carry the words — an OCR-on-asset rung or a page-class rule, his). 15/15; the CLI prints both bands (DIAGNOSING blank 1 + near-blank 3, Automate near-blank 1, DLA neither); `BLANK_SD` untouched** | `windows-converter/coverage_rescore.py` carries the fail-closed UNREAD rule only. Needed guard: rendered-page/region-to-final-asset visual coverage, blank/degenerate-crop tripwire, and image-adjacency protection through Analyst. Diagnose by rendering the source page, opening every page-mapped final asset, and reading the adjacent final Markdown together |
| SYM-054 | You finish an agent-fleet run, every suite is green and every file accounted for — and a handful of HTTP servers the agents started are still listening. Nothing errored, no port conflict surfaced, and the harness that audits the run reports CLEAN | A fleet's verification lanes start real servers to exercise real routes (the honest choice — a mocked socket proves nothing about a socket) and nothing kills them when the lane returns. Measured S109: **seven orphaned servers** from the relay-room token-gate lane — `room.py serve` on 7199/7211 with test tokens, plus `hostile.py` on 7222/7223/7224 — still alive hours later. **The harness checks FILES, not PROCESSES**: `selftest.py`'s L5.6/L5.7 re-measure the coordination tree and the state tree by digest and are blind to a live process by construction. Same family as SYM-047 (an orphan watcher outliving the Job Object that was supposed to govern it), one level up: there, one process outlived its spawner; here, a whole fleet did, and the audit designed to catch what the fleet left behind could not see it. A quarantine that measures only the filesystem is not a quarantine of the machine | `Observed 2026-08-24` (S109 — found only because Rab asked "whats still working?"; no probe in the session would have surfaced it) | **FIXED** (the census; S157 E9, 2026-09-15): `agent-scripts/auditing/fleet_proc_census.py` — the tracker's receipt against the live process table for a window, every leak named, exit 1; run at every closeout. Residual: the sentence in docs/47 that makes it a fleet LAW is Rab's to sign; until then it is the lane's protocol. Was: the fleet standard's S1 (bus isolation) is digest-verified and held; there is no equivalent clause for PROCESSES. A fleet law needs a process census at the end of a run, the way it already has a digest census | `.claude/skills/*/selftest.py` L5.6/L5.7; any Workflow whose lanes bind a port |
| SYM-055 | A protocol's digest function can be swapped from **sha256 to md5** — while still labelling its output `"sha256:"` — and the 83-case selftest passes **83/83, exit 0**. Every message on the bus reads digest-verified; the board is green; the confirmation layer the two-model protocol rests on is checking a value nothing has ever validated | `gate.py:133` `digest()` returns `"sha256:" + hashlib.sha256(canonical(text)...)`. **No case in `selftest.py` compares `digest()` against an independently computed sha256, or even asserts its length.** Every digest assertion is self-referential — it compares gate.py's output to gate.py's output, so any algorithm agrees with itself. The suite tests that digests MATCH, never that they are the digest they claim to be. Same family as SYM-046 (an exit code cited as a verdict nobody had read the flags for) and docs/32's proxy substitution: a label standing in for the property it names | `Verified 2026-08-25` (S109 second sitting, by hand, in a scratch copy — NOT a subagent report). Applied the one-line mutation, ran the suite: `ALL TRIPWIRES FIRED — 83/83, exit 0`. Non-equivalence proven by observable divergence: mutant `digest("hello relay")` = `sha256:acc77fb37d3a408f8e23caa793e65dfa` (**32 hex chars**) vs true `sha256:d6d73b3e899f235e4c4540a978ac34c5bcd2dea1437991da046282f41844692b` (**64**). Live tree verified byte-identical before and after (`ab26a4f1…`) | **FIXED S160 E2 (2026-09-16)** — four cases in `.claude/skills/relay-gate/selftest.py` check the PROPERTY the label names: T1 the shape (`sha256:` + 64 hex), T2 `digest(text)` == an independent `hashlib.sha256(canonical(text))`, T3 `digest('hello relay')` == the vector pinned apart from gate.py, T4 the S109 md5 mutant in-process FAILS T1–T3 (the negative control). Suite 204/204 before → 208/208 after (exit 0). Stepped on in a scratch copy of the skill at the same relative depth (private `sittings/S160/e2_mutation.py`): the unmutated copy 208/208 (the relocation control); the S109 md5 mutant now fails 10 earlier cases and crashes the suite at line 319 (exit 1) — `gate.py:803`'s `sha256:[0-9a-f]{64}` shape check (6258e24, 2026-08-28) rejects a 32-hex digest since, so the row's '83/83 with the mutant' is Historical; a **blake2s mutant (64 hex, the shape check passes it) fails ONLY T2 and T3 — 2 of 208, exit 1** — which is the property the shape check never had. Was: **OPEN** — found by mutation-testing the suite (21 mutants, 3 killed, **18 survived**; that 14.3 % score is `Inferred` and carries a named methodology defect, see `sessions/S109` — but THIS row is `Verified` independently of it). The fix is one test case; the finding is that **83 green cases did not include it** | Fix site: `.claude/skills/relay-gate/selftest.py`. The missing case, stated so it can be written from this sentence alone: **assert `gate.digest(s) == "sha256:" + hashlib.sha256(gate.canonical(s).encode("utf-8")).hexdigest()` for a fixed input, computing the expected value independently of gate.py** — plus a length assertion (64 hex). **Diagnose:** apply the mutation in a scratch copy and run the suite; if it still reads 83/83, the gap is live |
| SYM-056 | The analyst sits on ONE chunk for ~15 minutes against a ~14 s mean, at full GPU draw. Nothing errors, no timeout fires, the chunk eventually completes, and **the audit passes it**. The book's Markdown carries math that cannot be parsed by anything | **The converter emits structurally invalid LaTeX at scale.** `\begin{array}` environments are opened and never closed, with degenerate column specs of 35-36 identical characters (`\begin{array}{cccccccccccccccccccccccccccccccccccc}` -- 36 `c`s, no `\end{array}`). A structure with no valid completion is exactly what an LLM runs long on. **MECHANISM, `Verified 2026-08-25` from source:** the chunk does NOT complete -- `analyst.py:185` calls ollama with `urllib.request.urlopen(req, timeout=900)`, and **900 s is exactly the 15 minutes measured** (specimens 918 s and 882 s). The call raises, `analyst.py:341` takes the exception path, and three things follow **by design, with a written rationale**: the ORIGINAL un-analyzed chunk is kept in the output (`out.append(chunk)`), it is counted as `chunks_failed` and **does reach the frontmatter meta**, and it is DELIBERATELY NOT JOURNALLED so a later resume retries it rather than baking a transient failure into the book. **THEREFORE: NO DATA LOSS AND NO OBSERVABILITY GAP** -- a hypothesis to the contrary was raised and FALSIFIED by reading the source, and the code is better than the alarm was. **CONFIRMED BY PRE-REGISTERED PREDICTION, not by hindsight:** with a chunk mid-timeout, the prediction *"when n advances, that chunk's index will be ABSENT from the journal"* was written down BEFORE the outcome (docs/28) and then held -- `MISSING i: [103, 106, **115**]`, three for three, the third ran exactly 900 s. Two differently-shaped methods now agree (source read + live predictive test), which is what `Verified` costs. **THE COST, FINAL (these supersede the mid-run figures of 40 unterminated / 62 %, both computed on a partial run -- do not quote those):** the book COMPLETED. Frontmatter reads `chunks_passed 173 · chunks_rejected 6 · chunks_failed 3 · duration_s 5413.3`. **`chunks_failed: 3` EQUALS the journal-derived missing list [103, 106, 115]** -- a third independent confirmation, pre-registered before the bundle existed. 3 x 900 s = **2700 s produced NOTHING**; the other 179 chunks ran 2713 s = **15.2 s/chunk**. So **49.9 % of the analyst lane went to 1.6 % of the chunks.** End to end: convert 1089 s + analyst 5413 s = **108 min** for 156 PDF / 295 printed pages. **And the defect SHIPPED:** the same one-line diagnostic run on the bundle now sitting in `anchor/` reads `begin=126 end=66` -- **DELTA 60 unterminated environments in the delivered Markdown.** That is what makes the guard below cost-justified: it is not a tidiness fix, it is most of the runtime. **The audit cannot see it** -- it scores content, not structural validity, so all 28 affected chunks carry `status: passed`. Sibling of SYM-046 and docs/32's proxy-substitution family: a green verdict standing in for a property nothing measured | `Verified 2026-08-25` (S109 second sitting, live during C0's analyst lane). In Ashby's 112 chunks: **28 chunks (25%) unbalanced or runaway**, corpus totals `\begin{array}`=**79** vs `\end{array}`=**39**, **DELTA 40 unterminated**. Specimen `i=105`: `$$\begin{array}{c*36}$$` with no close. Two ~15-min chunks measured against a 13.9 s/chunk mean (**60x**), GPU pinned 93%/72C/286W throughout, `expires_at` proving the request never returned. **NOT Ashby-only -- it is in the SHIPPED corpus**: the anchored `Investment Valuation` bundle reads begin=43 end=40 (**delta 3, already vaulted**); `Cybernetics_Book_of_Models` reads 0/0. **Full corpus re-measured at filing (do not quote the n=2 sample): 2 of 23 anchored bundles carry a non-zero delta, and both are variants of the SAME book (Investment Valuation, base + `[analyst-local]`); corpus totals begin=85 end=79, **DELTA 6**. Ashby alone is 40.** So the shipped damage is small and real; the exposure is proportional to math density, and Ashby is the worst case seen | **OPEN.** **Corrects an S109 attribution**: this was first called a live **SYM-003** (table-loop disease) specimen. It is not -- SYM-003 is degeneration on *pipe-table-heavy* books, and one of the two measured specimens here contains **zero** pipe characters. Different cause, same family. `sessions/S109` §19.6 and OPEN-TASKS **J14** carry the superseded wording; this row is the correction · `Observed 2026-09-16 (S168 E3)` — THE SHELF RE-MEASURED with the validator below: 33 bundles (anchor + held), begin 1337 · end 1138 · unterminated 212 · **9 INVALID** — Ashby (both copies + the held one) `array` +61 whitespace-aware / +60 strict-literal (the Codex MSG-CDX-0014 numbers reproduced to the digit: 127/66 vs 126/66) with 10 runaway specs; Investment Valuation 4e (both) `aligned` −2/−1 with misorders; the University Edition (both + held) `align*` +1 and 3 misorders. THE CAUSAL CLAIM, 2 of 3: the anchored `(1)` copy re-chunks to exactly the run's 182 chunks (173 + 6 + 3); chunks 103 and 115 each carry one unterminated `array`; chunk 106 carries none — its 900-s timeout has another cause. 43 of 182 chunks INVALID (24%; S109 read 28 of 112 on the partial run). | **None, and none exists anywhere in the pipeline.** Needed: a **structural validator on converter output, before the analyst ever sees it** -- balance every `\begin{X}` against its `\end{X}`, and flag any column spec above a sane width. Cheap, mechanical, and it catches the defect at the seam where it is born rather than 15 minutes downstream. NOTE FOR A44: **hOCR would NOT fix this.** hOCR supplies text and bounding boxes, not LaTeX validity -- this needs its own guard. **Diagnose on any bundle:** `grep -c 'begin{array}' FILE` vs `grep -c 'end{array}' FILE`; a non-zero delta is the defect, and it is a one-line check that has never been run. **LIMIT OF THE CAUSAL CLAIM, stated rather than glossed:** the unterminated arrays are `Verified` and the 900 s timeouts are `Verified`, but that the *same chunks* carry both is **`Inferred`** -- the timed-out chunks (i=103, i=106) are precisely the ones NOT written to the journal, so their text could not be read. It is not closable from the journal by construction. **What would close it:** re-derive the chunking from the finished bundle markdown with `analyst._chunks(fence(md)[0])` and inspect indices 103 and 106 for an unbalanced `\begin{array}`. Until then the two findings are correlated, not causally linked **CROSS-VENDOR CORRECTION 2026-08-27 (Codex lane, `MSG-CDX-0014`, independently reproduced from primary bytes):** the figure **60** is the **strict-literal** delta only (`\begin{array}` = 126 minus `\end{array}` = 66) and must be labelled as such wherever it appears. The **semantic** count is **61 unmatched opens** — a whitespace-aware byte-order stack finds 127 opens, 66 closes, **zero stray closes**. The literal rule misses one opener written as `\begin` + **CRLF** + `{array}` while still counting its closer, so it under-reports by one. **"60 unterminated arrays" as a semantic claim is FALSIFIED; the honest number is 61, which is worse by one.** Controls run by Codex: balanced canonical and CRLF-split fixtures each return zero unmatched; a planted missing close returns one; removing one real close moves 61→62 and appending one moves 61→60; a second independent implementation reproduced the hashes, the counts, the anomalous token at body line 4,183 / raw line 4,200, and all five controls. ⚠ **The causal link to the timed-out analyst chunks 103/106/115 is `UNREAD`, not established** — those chunks are absent from the journal by design. ⚠ Codex also asks that `held`, `anchor`, `delivered`, `shipped` and `vaulted` not be used interchangeably as phase claims without separate evidence. **Its standing note on the guard: the guard stays cost-justified, but its test must include whitespace-separated TeX commands or it inherits the same blind spot.** Body denominator: 691,965 bytes / 5,926 lines, sha256 `5880bed650…`. *(Filed by the Fable lane on Codex's evidence, per the 2026-08-27 split: Codex verifies, Fable edits its own registers. Same defect family as `ERROR-BIN.md` ERR-004 — a probe whose assumption about token adjacency was narrower than the data, on a CRLF file.)* · **Tripwire (S168 E3, 2026-09-16):** `windows-converter/latex_structure.py` — the validator this cell asked for, BUILT and UNWIRED: `check(md)` balances every `\begin{X}` against its `\end{X}` per environment (whitespace-aware, the strict-literal count kept beside it), walks the nesting (an `end` that closes nothing or the wrong environment is named with its line), flags runaway column specs above `MAX_SPEC_COLS = 20` (a lever, his); a CLI over files (exit 1 on any INVALID). `latex_structure_selftest.py` (12): the S109 specimen, the split opener, crossed nesting the counter alone would pass, the lever's NEGATIVE CONTROL. Wiring into `convert_and_ship.py` before the analyst is the restart window's act (a running file). |
| SYM-057 | **The audit reports PERFECT analyst fidelity on a book it could not measure.** `doc_survival` comes back `1.0` — the best possible score — from the branch that runs when there was nothing to compare | `windows_analyst_audit`: `fidelity_audit.py:403` returns `{"doc_survival": 1.0, "runs": []}` when `make_windows(ref, cjk)` yields an empty list, i.e. when the Marker reference is too short or empty to build a single comparison window. An unmeasurable comparison renders as a flawless one | S110 | **FIXED S175 E4 (2026-09-17, his word Desk 8ca8279b: the restart window)** — `fidelity_audit.py` `audit_analyst` returns `doc_survival` **None** (UNREAD) with `windows_total` 0 when the reference builds no window, and carries `windows_total` on the measured branch (the denominator travels with the ratio); `compute_verdict` and `convert_and_ship.py`'s fallback branch read None as not-measured — it FAILS nothing and FLAGS nothing (the verdict's two signals are docs/15 §12's; a flag for an unmeasured stage is a gate change, his). T26 flipped to `EXPECT_FIXED_SYM057 = True` in the same commit (272 → 273: (e) the measured block's `windows_total`, a None through `compute_verdict` reads pass, the run gate still fails beside it); the runner's gates green. The widget's readers already print `—` for a null (main.js:873, 1001); `room.js:203`'s `?? 0` would print `0.00` for a None on a pass — the rebuild's (his). A live PDF through the restarted watcher is this sitting's proof run (his word c2eb377f). Was: **OPEN** — ⚠ **LATENT, NOT LIVE, and the distinction is the finding.** `Observed 2026-08-27`: no bundle in the corpus currently reports `analyst.doc_survival = 1.0` (9 manifests read; values 0.9303–0.9791), so nothing is lying today. **But the manifest records NO WINDOW COUNT** — `grep -oU '"windows'` over a held manifest returns nothing — so a genuine 1.0 and a not-measured 1.0 are **byte-identical and indistinguishable forever after the run**. The defect is not that it lies; it is that any future lie is unrecoverable. ⚠ **1.0 does not discriminate**: a real perfect containment produces the same number, which is why no reader could ever catch this downstream — the same shape as SYM-001. The honest return is `None`/UNREAD, and the manifest needs the window count so the two cases separate. Found by the Codex lane's completion audit (item U07), which reached no register and no relay entry until today; **not** found by any guard of ours. Verified at source by the Fable lane before filing | none — the audit is the producer, and it has no check on its own certainty · **Tripwire IN WAITING (S169 E2, 2026-09-16):** `windows-converter/convert_and_ship_selftest.py` T26 (268 → 272; the CONVERTER row runs it at every converter-touching close) — behind `EXPECT_FIXED_SYM057 = False` it asserts the defect is PRESENT as filed (`audit_analyst("", …)` reads `doc_survival` 1.0, `runs []`, no `windows_total`), so the day `fidelity_audit.py:564` changes without the flag flipping the suite goes red; flipped to True IN THE SAME COMMIT as the fix (the restart window's) it asserts the honest return — `doc_survival` None and `windows_total` 0. Controls (c)/(d): a real reference against itself reads a MEASURED 1.0 (`runs_total` 0), against an output that dropped it < 1.0 with runs. |
| SYM-058 | The Dock reports a bare `survival N` value. The operator cannot tell which phase survived or which evidence the number describes | The Rust status export supplies only converter `doc_survival` (`assay.rs:127,159`), and the renderer drops even that phase name when it prints bare `survival` (`main.js:940`) | S111 | **FIXED AT THE SOURCE S161 E4 (2026-09-16) — renders at his next rebuild** — `main.js`'s assay card names the phase: the verdict span reads `convert survival N` (the value IS `conv["doc_survival"]`, assay.rs's export), never a bare `survival`; `projection_selftest.py` case + a planted negative control (12 → 17 cases, 6/6 controls). The installed exe (C4559805) still prints the bare word until the rebuild — his hand. Was: **OPEN — Verified from source 2026-08-28.** This is a presentation defect, not evidence that the underlying converter score is wrong | none. Guard the card copy: the value must be labelled **convert survival** (or another explicit phase); a bare `survival` label must fail |
| SYM-059 | A Dock card places the overall verdict beside converter survival, visually implying one explains the other. A bad verdict can therefore look like a bad conversion even when the analyst caused it | `st.verdict` is composite: `fidelity_audit.py:427-450` can select an analyst or converter result, while `assay.rs:159` independently exports converter `doc_survival`; `main.js:935-941` renders them together without cause or phase. They **can**, but need not, describe different evidence lanes | S111 | **OPEN — Verified from source 2026-08-28.** Live bundle causality was not re-adjudicated and remains `UNREAD`; the source-level ambiguity is sufficient **Scope note (S161 E4, 2026-09-16):** the phase the composite verdict selected cannot be named honestly from the Dock's side — the thresholds (`ANALYST_DOC_FAIL`, `ANALYST_RUN_WORDS`) live in `fidelity_audit.compute_verdict` and a JS copy would be the hard-coded-threshold class (SYM-096, docs/13). The writer must carry it: a `verdict_phase` (`analyst` / `convert` / null) beside `verdict` in `build_fidelity_block`, then assay.rs exports it and the badge reads `fail · analyst`. `fidelity_audit.py` is a file the live pipeline imports — the edit lands at a restart window (his), with S158's docs/15 amendment shape. Until then SYM-058's and SYM-060's renders (this sitting) put both survivals side by side, which is the evidence the operator lacked, not the attribution. **Read S190 E2 (2026-09-17):** the writer carries the attribution now — `verdict_phase` since S175 (the badge reads `fail · analyst`, `assay.rs:159` / `main.js:996`), and `verdict_phases` since S188 E6 (every failing phase in pipeline order — Automate: `convert`, `analyst`; eight of twelve held bundles name a phase the single column hid); the badge shows the single phase until the rebuild (his). The row stays open on the widget half only. | none. Render a phase/cause field, or split the analyst and converter verdicts; tripwire a fixture where analyst fails while converter survival remains high |
| SYM-060 | The status payload carries analyst evidence that the Dock never shows. A producer can change the field without any visible effect, and an operator cannot inspect the lane the composite verdict may have selected | `assay.rs:165` exports `analyst`, but `main.js` has no `st.analyst` read. The `r.analyst` reads at `main.js:492-495` belong to a different receipt object and are a negative control, not a consumer of this field | S111 | **FIXED AT THE SOURCE S161 E4 (2026-09-16) — renders at his next rebuild** — `main.js` gains `analystLine(st)`: `st.analyst` (assay.rs:165's export, read by nothing since S111) renders as `analyst survival N (n of total runs[, k reference words masked])` under the verdict — both sides travel (NUM-3's runs_total, S131's reference_masked.words), nothing invented, nothing when the block is absent or null; exercised under node on five payload shapes; `projection_selftest.py` case + a planted control. Was: **OPEN — Verified from source 2026-08-28.** This is a dead status-payload field at the Dock boundary | none. Add a consumer/contract test that fails when an exported status field intended for operator evidence has no renderer, then render or remove it deliberately |
| SYM-061 | Every failed, flagged, or held Dock card offers both “re-convert” and “re-analyze,” even when the evidence identifies only one phase. The UI asks the operator to guess the remedy | `main.js:904-912` conditions both actions on the same aggregate state; held cards repeat the same pair at `:923-931`. No branch binds the offered action to the evidence phase | S111 | **OPEN — Verified from source 2026-08-28.** The buttons may both be technically available; the defect is that the card presents both as equally evidenced remedies **Scope note (S161 E4, 2026-09-16):** binding the primary remedy to the failing phase needs the same `verdict_phase` SYM-059's note describes; the JS half (⟲ primary on `analyst`, ⟳ primary on `convert`, the other secondary) follows it in the same rebuild slot. **Read S190 E2 (2026-09-17):** the converter half is written twice over — `verdict_phase` (S175) and `verdict_phases`, every failing phase in pipeline order (S188 E6; a book failing both phases names both, so the primary remedy can be bound to the UPSTREAM phase); the JS half is the rebuild, his. | none. Bind the primary action to a named failing phase and make any cross-phase retry explicitly secondary; tripwire converter-only and analyst-only fixtures |
| SYM-062 | A model can type any plausible sentence of at least ten characters into `gate.py resolve --decision`, and the runtime converts that model-authored transcript into a resolved escalation and `state=idle`. The protocol says no model may clear `blocked-on-rab`, but the command has no independent evidence that Rab made the decision it records | `cmd_resolve` calls `announce_bus("resolve")`, finds an open escalation, checks only `len(a.decision.strip()) >= 10`, writes that string as the decision, marks the escalation resolved, then sets the sidecar state to idle (`gate.py:2090-2111` in the committed S111 B2 repair). Its own docstring says the transcript “is not authority” while its state transition treats it as sufficient authority | S111 | **OPEN — Observed from source 2026-08-28.** This is not repaired by B2's preserved-disagreement terminal state: disagreement explicitly leaves blockers unchanged, while `resolve` is the existing mechanism that lifts them. No authenticated human-authority artifact or signed design exists, so Codex did not invent one | none. The acceptance boundary belongs to Rab: define an independently verifiable human-authority artifact or keep resolution human-operated outside the model runtime. A test that merely supplies another model-authored string would reproduce the bypass, not guard it |
| SYM-063 | `close.sh` prints `MEMORY UNREAD — memory library is not a git repo` even though the canonical memory path is a clean Git repository and the SOFT clock is durable | The probe collapses every nonzero `git rev-parse --is-inside-work-tree` result into “not a git repo.” On this checkout Git refused the read because repository ownership was not trusted until an exact per-command `-c safe.directory=<memory path>` was supplied; the gate reported the wrong cause instead of identity/ownership `UNREAD` | S111 | **FIXED S157 E42 (2026-09-15)** — the MEMORY probe reads git's stderr: `dubious ownership` / `safe.directory` renders "UNREAD — git REFUSES the memory library (ownership / safe.directory unresolved: <git's line>)", anything else "not a git repo"; both UNREAD, never clean. Tripwire: muster selftest CASE 54 (a git shim answering dubious ownership for one path; the plain non-repo negative control). Was: **OPEN — Observed 2026-08-28.** The mechanical close printed the false cause; an independent per-command safe-directory probe then read clean memory HEAD `d9d1e7a`. This does not make the original card green—it proves its diagnosis was over-broad | none. Preserve stderr/exit cause, distinguish absent/non-repository from ownership refusal, and add a dubious-ownership negative control. Do not mutate global safe-directory configuration merely to make the card green |
| SYM-064 | An S111 close prints through RUST and then appears frozen indefinitely; no CI row arrives, while `git-askpass` waits off-screen for interaction | `close.sh` bounds the later GitHub request with `curl --max-time 25` but does not bound the earlier `git credential fill`. In a noninteractive close that credential call launched `git-askpass` and waited about fourteen minutes until Codex interrupted the exact process tree | S111 | **FIXED S157 E42 (2026-09-15)** — `git credential fill` runs under GIT_TERMINAL_PROMPT=0, GCM_INTERACTIVE=never, GIT_ASKPASS=/bin/true and `timeout ${FP_CRED_TIMEOUT_S:-20}`; a helper that does not return renders "UNREAD — the credential lookup did not return in Ns" (timeout's exit 124), never a wait. Tripwire: CASE 53 (a helper that sleeps 30 s is bounded in 3 s and named; a helper that answers at once is not called a hang). The live close still reads CI through the stored credential inside the bound. Was: **OPEN — Observed 2026-08-28.** The first attempt changed no repository bytes. A rerun with `GIT_TERMINAL_PROMPT=0`, `GCM_INTERACTIVE=Never`, and `GIT_ASKPASS=/usr/bin/false` completed and honestly printed CI `UNREAD — no stored credential` | none. Bound credential acquisition itself and add a negative control whose helper never returns; the close must terminate with CI UNREAD inside a declared ceiling |
| SYM-065 | VW-E2 hermetic selftest reads 53/54 after any new commit — test_symbolic_loose_and_worktree_commondir_packed_refs asserts REPOSITORY_SHA == live HEAD | The anchor is frozen inside hash-frozen producer bytes, so the equality can only hold at the anchor commit itself; every later commit turns the suite red by construction | S112 | **FIXED 2026-08-31 (S113, `583f752`)** — reproduced 53/54 at `e8a01bc`; the resolver now compares its live result with an independent exact-Git live-HEAD reading, while the existing bounded-Git test retains the contract's anchor-ancestor assertion. The suite passed 54/54 before commit and again after HEAD advanced to `583f752`. Producer, packet, hashes, runtime gates, and calibration authority are unchanged | `windows-converter/visual_witness_capture_selftest.py:799-839`; contract rule `docs/contracts/visual-witness-e2-packet-r2.json:129` |
| SYM-066 | Every audited big book reports exactly "25 omission runs" (Ashby x2, Damodaran 4e x2) | `fidelity_audit.py:378` stores `sorted(runs)[:25]` and the event emits the LIST LENGTH — the surface number is `min(true, 25)`; re-run with the cap removed measured **634 true runs** on the old 4e (21,189 words, 4.4% of witness), and the stored list's min-words is 72 vs the 18-word floor | S112 | **FIXED** — `Verified S114, 2026-09-01`: the NUM-3 repair fired LIVE, twice, on the same real book (Damodaran 4e, held `14c66834bdfeaa2e`). `events.jsonl` 22:35:15Z convert audit `runs 25 of runs_total 531`; 04:56:32Z analyst audit `runs 25 of runs_total 404`; the manifest carries `runs_capped_at 25` beside both totals — the first non-legacy manifest on disk. Independently: M6-R1's `evidence_count` resolves it to `25 of 531 · partial · unseen 506` (convert) and `25 of 404 · unseen 379` (analyst), so the cap can no longer masquerade as the population on any glass that reads the grammar. The S113 close called this row "dead in production" but left the status OPEN; struck here with the live evidence. *(Was:* **OPEN** — `Observed` (recomputation reproduced the stored 0.9270/1351/255 exactly before the claim; hand-checked)*)* **Citation note (S159 E5, 2026-09-16):** `fidelity_audit.py:378` drifted; find `"runs_total": len(runs)` by name (twice in the file at HEAD). The fix is present; the tripwire is `analyst_audit_selftest.py`'s `runs_total` assertions. | `windows-converter/fidelity_audit.py:378` |
| SYM-067 | Convert-stage `degeneration=true` (and therefore the whole `fail` verdict) on table-heavy books can fire with zero decoder loops | `degeneration()` runs on RAW markdown; empty-cell table grids (single-sentence callouts promoted to 30-row empty tables, phantom trailing columns) drive `max_trigram` to 205 via pipe tokens; `repeated_lines`=0 in both 4e audits; counterfactual with `_strip_markdown` first → 0 paragraphs trip **on the 2025 4e anchor (26 → 0); on the held University 4e `14c66834bdfeaa2e` it is 25 → 1, and the 1 is real: raw line 8776, a 21,870-char `\begin{array}{lll}` runaway (`{1 - t}` × 441, zlib 0.011) — SYM-056's unterminated array, which MUST stay flagged; Beer anchors (max_trigram 2,267 / 143) stay flagged under every variant** (`Verified` 2026-09-04 with the repo's own `degeneration()`, `docs/53-lead-hunt/scripts/verify_sym067_pair.py`). Real loops (Beer class) still fire correctly — precision failure on sparse tables only. Degeneration is the ONLY convert-stage path to `fail` (`fidelity_audit.py:436`) | S112 | **FIXED** `8aa8936` (J29, signed Rab 2026-09-04 "Signed: J29, SYM-072, push 7bcf001") — pipe-table rows are blanked before `degeneration()` measures the body, line terminators kept so `line`/`md_lines` still address the shipped markdown, the count rides as `table_rows_stripped` (disposition EVIDENCE). `Verified` on the whole anchor + held corpus (34 md files, CPU): exactly 3 verdict flips, all Damodaran 2025 4e (26 → 0 blocks); held University 4e keeps 1 (line 8776, the `{1 - t}` × 441 runaway = SYM-056's array); Brain of the Firm still trips (×2,267 / ×143). `degeneration_selftest.py` 22/22 with the decoy asserted on the old path (D2) and the declared blind spot (D5: a loop that emits only table rows). Was: OPEN — `Observed` both bundles + hand-checked zone 8219 **Scope note (S159 E5, the Circle, 2026-09-16):** the fix is the WINDOWS lane's (`fidelity_audit.py`); the Linux port `linux-converter/converter/degeneration.py` carries the thresholds and not the blanking — a dense pipe table there can still false-fire (OPEN-TASKS `linux-converter/port-the-table-row-blanking`, his word). FIXED reads Windows-only. | `windows-converter/fidelity_audit.py` (`_blank_table_rows`, `degeneration`), `degeneration_selftest.py`, `docs/15` §9.3 |
| SYM-068 | You click the watcher's stop/pause button, the card says stopped — and PDFs dropped afterwards still convert; Task Manager shows a python watching drop/ under the live widget | `watcher::stop` killed only the DIRECT child; with a venv-launcher `gpu_python_exe` the real interpreter is a grandchild and survives inside the S37 job until widget exit (measured live 2026-08-31: shim 32068 / real 3532) | post-S112 night | **FIXED 2026-08-31 (WAT-2, signed)** — stop now `taskkill /T /F` on the tree + belt-and-braces kill/wait; `Inferred→built` from source + live census, first live stop-click under the new exe is the remaining observation **Cite drift (S162 E3, 2026-09-16):** the row's `watcher.rs:184-205` is now `stop()` at `:268` with the two `taskkill` calls at `:279` and `:290`; the fix is present. | `windows-widget/src-tauri/src/watcher.rs:184-205` |
| SYM-069 | You close the widget's main window while a Bench or Chat window is open; the control room is gone but conversions keep running and bench/chat servers stay up — a half-dead factory nothing is steering | tauri exits the PROCESS only on the LAST window, and the S37 job's KILL_ON_JOB_CLOSE fires only at process exit; `Destroyed("main")` stopped the watcher's direct child and nothing else | post-S112 night | **FIXED 2026-08-31 (A3, signed)** — main's death now closes every other window so the process exits and the job fires; `Inferred→built` from source, unexercised live **Cite drift (S162 E3, 2026-09-16):** the row's `main.rs:687-703` is now the `WindowEvent::Destroyed` handler at `:771`; the fix is present. | `windows-widget/src-tauri/src/main.rs:687-703` |
| SYM-070 | You triage every wreck site and omission visible in Repair Bench; re-score says “nothing left open” and recommends the vault even though the audit lists were capped and hundreds of located defects never appeared | `state()` projected only the stored capped arrays; `coverage()` called their length the total; `rescore_preview()` required only clean current degeneration plus zero open entries in that displayed subset. A reproduced 25-of-634 runs + 10-of-37 zones fixture therefore returned `eligible=true` after all 35 shown sites were dispositioned | S113 | **FIXED 2026-08-31 (M6-R1, `57a5da6`)** — completeness is typed separately for runs/zones; known hidden, legacy-at-cap, missing, malformed, and contradictory totals fail closed and name their remedy. Legacy under-cap stays exact. Read-only corpus census: 11/33 unique existing manifests are now held pending reconversion (7 anchor, 4 held); no patient changed | `prototypes/repair-bench/test_bench_page.py:266-422` (future/legacy/malformed/capped + HTTP); 78/78 suite; real sandbox acceptance 85/85; widget 35/35 |

| SYM-071 | `open.sh` reports `SOFT clock DRIFT tally=<n> hook=` with the hook field EMPTY, and the two clocks are then read as forked - when in fact both records agree and nothing rewound | The guard is line-wise over prose that gets re-wrapped. `muster.sh:75` matches `received [0-9]+ / given [0-9]+` with `grep -oE` over the TIME-STATE block, so the phrase must sit contiguous on ONE line. The S113 close re-wrapped that sentence in `MEMORY.md` so line 11 ended `cookies **received` and line 12 began `84 / given 3`; a line-wise regex cannot see across the break and returns empty. An EMPTY parse and a REAL drift are then rendered by the same branch and the same message | S114 | **FIXED** - S114 2026-09-03: the soft-clock hook is now read off the TIME-STATE block as JOINED text (newlines/indentation/'>' markers collapsed before matching), so a wrapped `received N / given M` parses correctly; an EMPTY hook after that join renders UNREAD, distinct from a real DRIFT. selftest.sh CASE 38 (a phrase wrapped across continuation lines, tally == hook, exits 0) and CASE 39 (a contiguous mismatch fires DRIFT / no `received` phrase at all fires UNREAD, each excluding the other) tripwire both directions. (Was: **OPEN** - `Observed 2026-09-01`. Both records read directly and agreed in substance (`cookie-tally.md:13` = `**Received from user: 84**`; TIME-STATE = `received 84 / given 3`); HARD clock was green throughout (`14a526b`, ledger-equal, ancestor of HEAD). Verdict CONFIG, not rewind. Reconciled by re-wrapping the phrase contiguous; re-run returned `SOFT clock ... OK 84 (tally == hook)` and exit 0. **The dangerous direction is the converse**: the same edit shape can make a genuine drift read as a benign empty parse, and nothing distinguishes `hook=` (could not read) from `hook=<wrong number>` (really forked) - the failed-probe-vs-negative-observation rule (SKILL.md rule 4) applied to muster's own soft clock. Filed by the session that tripped it) | `muster.sh:74-83`. Needed: parse the TIME-STATE block as JOINED text (strip newlines before matching) so wrapping cannot defeat it, and report an unreadable hook as UNREAD distinctly from a mismatched hook. Tripwire must step on BOTH: a wrapped-but-agreeing TIME-STATE must exit 0, and a contiguous-but-DISAGREEING one must still exit 1 |
| SYM-072 | `open.sh` exits 1 with `[3] PIN: COLLISION - a closeout for S<n> exists under a different machine/date` although the session is the same session on the same machine - every gate above it reads clean | `open.sh:371-375` decides by FILENAME ONLY: it expects `sessions/S<n>-<machine>-<today>.md` and calls any other `sessions/S<n>-*` a collision. Two legitimate shapes trip it: (a) a MULTI-DAY session - the closeout is named by its OPEN date per SKILL.md Phase 6 (`S114-desktop-2026-08-31.md`, opened 08-31, still open 09-03), so from day two the guard sees its own file as foreign; (b) a PEER-LANE RECORD under the same number (`S114-ledger-diag-codex.md`, the S113 `-fable-lane-` precedent). The guard cannot tell a collision from continuity or from a second lane | S114 | **FIXED** `ba036f9` (signed Rab 2026-09-04) — [3] PIN now reads each `sessions/S<n>-*` file's own `⟨claimed: … S<n> …⟩` stamp and the machine in its name: this machine + past-or-today date + stamp = OPEN (a later day of the same session); another shape with the stamp = LANE RECORD, named, exit 0; a closeout-shaped name for another machine (stamp or not), a same-machine name without the stamp, a future date, or a stamp for a different number (S430 ≠ S43) = COLLISION, exit 1. Tripwires CASE 40–42 in `selftest.sh` (65/65), including the negative controls a name-only fix would pass. The real repo's open reads `absent` for S115 (clean, exit 0). Was: OPEN - `Observed 2026-09-03` by the S114 fleet's Fable verifier and reproduced by the operator's Fable: outer exit 1 on [3] PIN on every open from day two of S114 **Cite drift (S162 E3, 2026-09-16):** the row's `open.sh:371-375` is now the `⟨claimed: …⟩` stamp logic at `:477-493`; the fix is present. | `open.sh:371-375`. Needed: decide by the file's own `claimed:` stamp (machine + lane) and its §1 presence, not by the date in its name; treat a second lane's record under the same S<n> as a named lane, not a collision. Tripwire must step on all three: same-session-later-day exits 0, peer-lane record exits 0 and is NAMED, a genuine foreign closeout still exits 1 |
| SYM-073 | The same held bundle scores differently on re-audit: the maiden-voyage manifest's convert-stage `doc_survival` is 0.9334, a fresh `fidelity_audit.py --pdf <source> --md <held md> --lane clean` on the byte-identical pair gives 0.9271 (pages_scored 1372 both; 257 pages flagged fresh; verdict `fail` both) | Unknown. The manifest records `marker_version: "unknown"` and the audit stamps only `SCHEMA_VERSION = 1`, so nothing in the bundle says which audit code or witness produced 0.9334. Candidates, none measured: audit code drift between the run (S113, `14a526b`-era) and HEAD (`latex_balance`, `runs_total` and the NUM-3 repair landed since), a pymupdf/witness version difference, or a run-time argument the CLI default does not pass | S114 post-close (docs/53 lane D + the coordinator, 2026-09-04) | **FIXED 2026-09-05** (S115: J33 `bbcff64` retains the Marker body beside the bundle as `<name>.marker.txt`; J31 `d3f69e8` records `fidelity.final.text_audited` + `reference` on every re-audit, so two audits of two texts can never again read as one; verified GO_AMENDED by `wf_6fca68fd-b1d`) — **was: OPEN — CAUSE FOUND 2026-09-04** (`Verified`): the manifest's 0.9334 / 241 flagged / 531 runs is the audit of the PRE-analyst Marker body (`convert_and_ship.py:1616`); the fresh 0.9271 / 257 / 570 is the same audit of the POST-analyst held md (`:1633` rebinds `body` to the analyst output, `:1670` writes it). The rebuilt Marker body from the slice cache reproduces the manifest exactly (`sym073_cause.py`, 55 s). Consequence: every "fresh audit of a held md" in the record measured the analyst output against the PDF, not what the manifest measured. Remedy = J33 (retain the body) + J31 (a re-audit names `text_audited`). Was: OPEN — `Observed` both numbers, cause Unknown | `windows-converter/convert_and_ship.py` (`_write_marker_body_safe`, `reaudit()`); `windows-converter/fidelity_audit.py` (`audit_analyst`) |
| SYM-074 | `</think>` control tags inside shipped book markdown — the local analyst backend (qwen3:8b, a thinking model) leaked its reasoning close tag into the text: held Univ 4e `14c66834bdfeaa2e` line 8779, immediately after the line-8776 math-OCR runaway (chunk 296, the chunk that blew the context), 2 occurrences in the held md and in both anchor copies, 0 in the other 31 corpus files | `Verified` in `analyst.py:183-215` (`_generate`): the local path asks ollama for `"think": False` and returns `reply["response"].strip()` with NO filter of any kind — there is nothing to truncate; qwen3:8b emits a bare `</think>` on its own (0 `<think>` opens, 2 closes). The second leak (line 13744, page 661, ordinary prose, longest nearby line 660 chars) follows a `/no` fragment on the heading line before it — the model's reasoning-control vocabulary leaking into text, not a context overflow; the first (line 8779) follows the runaway chunk. The ⟦IMG⟧ multiset fence passes both (no image token changed). Was: Inferred — a truncated well-formed think block | S114 post-close (fleet lane B residue, 2026-09-04; re-measured by Fable: `grep -c '</think>'`) | **FIXED 2026-09-05** (`72a6ba9`, SYM-074 FIXED: `analyst.process` rejects any candidate carrying `<think>` or `</think>` BEFORE the fence — reason `think_leak`, the original ships, both backends; tripwires `analyst_selftest.py` SYM-074 (a)–(d) incl. the watched-failing control; corpus sweep re-measured by the verifier fleet: 34 files, hits only the Univ 4e held md + its 2 anchor copies, 2 each; scope is the two literal ASCII tags only — `<thinking>`, `</THINK>`, a tag split across a newline or HTML-escaped are NOT caught, documented in docs/15 §6, not widened without Rab) — **was: OPEN** — no guard. Needed: reject a chunk whose output carries `<think>` or `</think>` (the fence check's shape, ships un-analyzed), an analyst selftest tripwire, and a sweep of the corpus (measured: only this book) | `windows-converter/analyst.py` (`process()`, the think-leak check before the fence; `analyst_selftest.py`) |
| SYM-075 | A CI step you are about to arm reads `conclusion: success` in the jobs API, the job is green — and its LOG ends `Found 50 errors.` / `##[error]Process completed with exit code 1` | GitHub reports a `continue-on-error: true` step's `conclusion` AFTER the override (`steps.<id>.outcome` is the pre-override value; the jobs API exposes only the post-override `conclusion`), so a masked failure is indistinguishable from a pass at the API level. The S108 arming condition — "observed green" — was one sentence away from being satisfied by the wrong meter (METER-CONFUSION's shape, SYM-001's class: the API and the job verdict share the override) | S116 | **OPEN** — `Observed` 2026-09-05: run 33990130578 / job 101370772727, step `Lint (windows-converter, warn-only)` API success, log exit 1, 50 ruff errors; the two other S108 steps read green IN THE LOG (30/30, 139/139) and were armed; this one was left warn-only. Guard: for any `continue-on-error` step, read the job LOG (or `outcome`), never the API `conclusion`. The lint debt itself is J39. *Update 2026-09-06 (S117): J39 cleared the 50 and armed the step (`47cd440`); CI run 197 on `5248329` — the step's LOG reads `All checks passed!` (line 269). No `continue-on-error` step remains in `ci.yml`, so the trap has no live instance today; the row stays OPEN as the class (the next warn-only step re-arms it) — the guard is a reading rule, not a mechanism* **Update 2026-09-17 (S192 E2): the trap had TEN live instances — the A29 governance suites (S157 E49) are `continue-on-error: true`, and the close's own reader `ci_observe.py` printed their post-override `success` at every close since. The guard is a MECHANISM now: the observer downloads the run's logs zip and prints each warn-only step's LOG verdict (`exit N` / `clean` / `UNREAD`) beside the API's (`ci_observe_selftest.py` 7/7). The first reading: 3 of 10 red on the S191 row's run (circle, coordination, muster — platform-shaped causes in A29's cell); every close since S157 had reported them green. The row stays OPEN until the LOG reading is the close's gate (arming — his)** |
| SYM-076 | The analyst-stage audit (and the per-chunk survival guard) scores a chunk as LOST where the model merely removed Marker's backslash before an underscore: `within\_recursive` vs `within_recursive` reads survival **0.0** on a 15-word probe; a technical book (DDIA 2e: Cypher/Datalog identifiers, DOIs) bleeds ≈0.4 % of its document survival to this alone | Order of operations in `text_norm`'s ladder: `chunk_survival`/`audit_analyst` run `punct_free(unescape(prepare_output(x)))` — `prepare_output` strips the `_` as MARKDOWN first, so by the time `unescape` runs the input reads `within\recursive` (backslash before a LETTER, which R5 deliberately protects for `\rm`) while the output reads `withinrecursive`; the `_UNESCAPE` regex also excludes `_` (it is `\w`). The J32-A ladder was measured on prose where `\_` is rare | S118 | **OPEN** — **BUILT behind a lever, OFF (S182 E1, 2026-09-17): `text_norm.prepare_for` v3 = escape-first (incl. `_`) + cite-anchor on both sides; `ladder.txt` absent = v2 byte for byte; the real audit on the held DDIA pair v2 0.9718/581 failed/49 → v3 0.9845/319/33 (docs/15 §9.6); 20/20 tripwires; the ligature rung waits for its gate; FIXED the day his line `j32a-v3` lands in the lever file — the manifests' `regex_id` say which ran.** `Observed` 2026-09-09 on held DDIA `fc1f068c3a8eeb63`: v3 (unescape incl. `_` BEFORE the markdown strip) re-scores the document 0.9718 → 0.9756 and runs 49 → 46 with `\rm` vs `rm` still 0.0; a ligature-blind rung (Marker drops fi/fl/ff glyphs, the model repairs them) adds 0.9808 / 33. Neither clears 0.995 — the rest is content (S118 §8 F3). Fix = **J44**; `fidelity_audit` binds `prepare_output` BY NAME, so any fix must land in `text_norm` itself, not be monkeypatched | `windows-converter/text_norm.py` (`prepare_output`, `_UNESCAPE`, `chunk_survival`); `fidelity_audit.py:39` (the by-name import); probes: S118 scratchpad `failing_windows.py`, `ladder_v3_probe.py`, `ladder_v3b_probe.py` | `.github/workflows/ci.yml` (the Lint step's comment names this row); `sessions/S116-desktop-2026-09-05.md` §4; ERROR-BIN ERR-2026-09-05-059 |
| SYM-077 | `coordination/selftest.sh` T3 reports the concordance amendment ABSENT (`FAIL 3 — concordance amendment present`) while every one of its four strings is in `relay.md` (`tr -d '\r' < relay.md \| grep -ci` reads 3 / 2 / 2 / 2); the fixture cases beside it stay green | `norm FILE \| grep -qi -- RULE` under `set -o pipefail`: `grep -q` exits at its first match and closes the pipe, `tr` is killed by SIGPIPE, the pipeline's status is **141**, and `\|\| amend=1` fires — a present string reads absent. The fixtures are small enough that `tr` finishes before `grep` closes, so the tripwire never saw its own hazard (docs/32 §5). Reproduced 2026-09-09T19:27Z: 141 with pipefail, 0 without, 0 in count form | S120 | **fixed** `79b08a5` (S120 L1): count form; a >200 KB fixture with the strings at the top passes, the old form shown returning 141 as the negative control; `coordination/selftest.sh` 20/20 | `coordination/selftest.sh` (the large-fixture case) |
| SYM-078 | `gate.py stage --as Fable` on the real checkout prints `REFUSED: UNREAD - HEAD:coordination/relay.md is not a byte-prefix of the working copy` although nothing in the log was edited — the same command passes its whole fixture suite | `core.autocrlf=true` (this machine) stores a text file as LF and checks it out as CRLF, so `git show HEAD:<file>` (LF) can never be a byte-prefix of the working bytes (CRLF prefix + LF appends); the fixtures were LF-only temp repos and could not reach it. Any byte-exact comparison between a blob and a working copy of a TEXT file is wrong under autocrlf — compare line-ending-blind, build the blob in the blob's own convention (the `.gitattributes -text` family of S115/S117, seen from the other side) | S120 | **fixed** `8355d40` — tripwire: an autocrlf fixture (LF blob, CRLF checkout, both lanes' appends) in `relay-gate/selftest.py` (S120 B12 block) | `.claude/skills/relay-gate/gate.py` `cmd_stage` |
| SYM-079 | `gate.py stage` (or any HEAD-vs-working-copy comparison of the append-only log) refuses with `edited`/`not a prefix` on a log nobody edited | Byte- or id-keyed equality fails on real history: `core.autocrlf` stores LF and checks out CRLF (B12); a header id occurs twice (MSG-FAB-0018, a 2026-08-24 double-post older than the id-uniqueness refusal); stray CRs. The digest law's own `canonical()` equality and an ordered-subsequence match of HEAD's entries in the working copy are the only comparisons that survive the log as it is | S120 | **fixed** `8355d40` (line-ending-blind), `c055661` (entry-aware), `08af2e2` (subsequence by canonical text); relay-gate selftest 198 | `.claude/skills/relay-gate/gate.py` `cmd_stage` |
| SYM-080 | `schtasks /Create …` under Git Bash prints `ERROR: Invalid argument/option - 'C:/Program Files/Git/Create'` | MSYS rewrites a leading-slash argument into a Windows path before the program sees it — the `open.sh` `/FI` family (SYM-family, S78), now a second instance. Any Windows tool that takes `/Flag` arguments must be called from PowerShell (or with `MSYS_NO_PATHCONV=1`) | S121 | **fixed** by route: the per-logon watch task was registered through PowerShell's ScheduledTasks module (`Register-ScheduledTask`); Git Bash never calls `schtasks` | `sessions/S121-desktop-2026-09-09.md` §8 F2 (no code path; a rule) |
| SYM-081 | Codex's J53 Python suite reports `115/115 OK` in its notes but `FAILED (errors=9)` here — every error `RuntimeError: laboratory_start_identity_mismatch` from `test_lab_driver.py` | `lab_driver.py:258` spawns its lab actor with `sys.executable` and requires the child's first probe pid to equal `Popen().pid`; under the marker-env `Scripts/python.exe` (a 262,144-byte uv venv LAUNCHER that execs the real interpreter as a second process) the pids differ, so the guard fires on the launcher, not on an impostor. On the interpreter Codex's `VERIFICATION.md:175` pins (`uv/python/cpython-3.12.13…/python.exe`, 91,648 bytes, no launcher) 115/115 and 86/86 reproduce. A claim that depends on an interpreter must name it in the claim, not in a footnote | S122 | **OPEN** — Codex's defect to fix (resolve the launcher, or compare process identity by the actor's own digest handshake rather than pid); `Verified` both ways by two lanes + Fable | `prototypes/whole-program/old-new/lab_driver.py` (Codex's branch `codex/operations-studio` at `e5eff0d`) |
| SYM-082 | A published file manifest (sha256 + bytes per path) mismatches the checkout for some files and matches for others — 7 of 25 sampled rows in Codex's `sessions/S121-codex-reading-and-ticket-origins.md` | The mismatching rows carry the hash/size of the LF-normalised git BLOB (`git show <sha>:<path>`), the matching rows the CRLF working-copy bytes — one manifest built from two byte sources under `core.autocrlf=true` (SYM-078's family, the fourth instance: schemas.json S115, contracts S117, `stage` S120, this). A manifest must say which bytes it hashed | S122 | **OPEN** — Codex's to restate; the reader's own coverage (547 hashed / 621 enumerated) is a different, consistent number | `sessions/S121-codex-reading-and-ticket-origins.md` on `codex/operations-studio` |
| SYM-083 | An embedding run crawls at ~2 passages/s and one Python process balloons past 9 GiB RSS (the box swaps on zram); a smaller batch runs the same model 25x faster | fastembed's `passage_embed`/`embed` default batch is **256**, and every sequence in a batch is padded to the model's window (512 tokens for bge/e5/multilingual models): 256 x 512 x 12 heads of attention scratch per layer. Not a model fault -- MiniLM at 256 tokens merely hid it | `Observed 2026-09-09` (S123, ThinkPad): bge-small 1.8/s at 9.9 GiB with the default; 2.0/s at 1.8 GiB and MiniLM 56/s at 420 MiB with `batch_size=32` | **FIXED** in the station (`EMBED_BATCH = 32`, waived) -- the class stays OPEN for any other fastembed caller | `linux-indexer/indexer/embed.py` (`EMBED_BATCH`); the bench that measured it is in the S120 closeout. **Diagnose:** `ps -o rss` on the embedding process while it runs; if it climbs past ~1 GiB for a sub-100M-parameter model, look at the batch size before the model |
| SYM-084 | `tailscale file cp <file> desktop-bndit:` refuses with `can't send to desktop-bndit: cannot send files: peer is owned by a different user`, though both machines are on the tailnet, online, and used by the same person | Taildrop only moves files between devices owned by the **same tailnet user**. The ThinkPad is a tagged node (`tag:home-server`, UserID = the node itself); the Desktop is user-owned (`rabiullahlotfullah@`). A tagged node has no user, so it can neither send to nor receive from a user's device | `Observed 2026-09-10` (S123, ThinkPad): `tailscale status --json` Self.Tags `['tag:home-server']`, Peer DESKTOP-BNDIT Tags None | **OPEN** by design (the tag is docs/06's server posture); the workaround is documented | `tailscale serve` of a loopback `http.server` (tailnet-only, no root: `tailscale serve --bg --http=8080 http://127.0.0.1:8081`) or the widget's own `tailscale ssh rab@archlinux "cat <path>"` from the Desktop -- both Observed 2026-09-10. Serving a *path* directly needs root and is not this lane's way |
| SYM-085 | The peer lane's uncommitted bytes in the shared checkout (its relay hunk, its sidecar writes) are simply GONE after a fleet run — `git status` clean, no commit of theirs in the log, `close.sh` DIFF reads `peer-owned 0` | A lane without its own worktree ran `git reset --hard` in the main checkout (its brief pointed it at a ground written for worktree lanes); the shared checkout has no protection against a destructive git verb from any process that can reach it. `git reflog` shows the reset; the bytes are unrecoverable unless the peer re-posts from its own transcript | S123 | `fixed` (S124, J56 signed 2026-09-10) — `.claude/hooks/guard_git.py`: a PreToolUse hook on the Bash and PowerShell tools denies the working-tree verbs against the shared checkout for everyone and restricts a subagent lane to read-only git there (a `deny` holds even in bypass-permissions mode); red-teamed by six lanes + a source critic — one breach (a shell variable in `--work-tree`) found on a sacrificial clone and closed; selftest 129/129. **Not covered:** Codex's own harness (docs/47 §9 binds it by law; its lanes need worktrees), a git inside a script file, a runtime-assembled target | `.claude/hooks/guard_git.py` + `guard_git_selftest.py`; docs/47 §9; `coordination/fleet-ground-template.md`; `coordination/private/git-guard.log` |
| SYM-086 | A fleet/subagent worktree starts from weeks-old code — files you committed are absent, `windows-converter/` missing, byte-exact tests failing — while your own HEAD is current | The workflow harness spawns each lane's worktree from `origin/master`, not HEAD (S115: `7c006f2`, 2026-07-13; the orphan `worktree-wf_*` branches share the base); recurred S117 §8-F8 and S119 | S115 | `Historical` — **OPEN** (a harness default); the MODE CHECK preamble tells a worktree lane to reset to the branch tip first | `coordination/fleet-ground-template.md`; docs/47 §9; filed S124 (J55) |
| SYM-087 | Byte-exact fixture tests (`docs/contracts/*.json`, `schemas.json`) pass in the main checkout and drift or fail in a fresh git worktree (S117: 6 of 54) though the files look identical | `core.autocrlf=true` over raw-CRLF blobs checks the fixture out as `\r\r\n` in a fresh worktree — the third EOL instance in two sessions (S115 `schemas.json`, S116 §8-F8, S117 §8-F7) | S117 | `fixed` (S117) — `.gitattributes` pins `docs/contracts/*.json -text` | `.gitattributes`; SYM-078/082 family (S1); filed S124 (J55) |
| SYM-088 | A convert receipt or Room line shows a triumphant rate for a run that measured nothing — `1377pp @ 0.0s/p` after a cache-resume; `wall_s 0.0 · s_per_page 0.0` beside an honest `cost_s` | A resumed convert has `pages_converted_this_run 0`, so the rate branch returns 0.0; `main.js:482` rendered `s_per_page` raw while `room.js` already spoke the honest vocabulary — SYM-057's sibling on another surface | S114 | `fixed` (S114 `1db7266`: UNREAD when pages-this-run is 0) | census field N-059; filed S124 (J55) |
| SYM-089 | During a long inline (non-`--resume`) analyst run `events.jsonl` is silent for hours — no `analyst/start`/`analyst/done`, no goodput on any glass — while the same phase on the `--resume` path emits them (S114: a 4 h 40 m dark phase) | `emit("analyst",…)` lived only in `apply_analyst()` (the `--resume`/card path); the inline path called `analyst.process()` behind a bare `print()` — SYM-042's shape, a signal writer on one entry path and the work on another | S114 | `fixed` (S114 `eee951d`: both paths emit parity events) | `convert_and_ship.py`; filed S124 (J55) |
| SYM-090 | The analyst-stage survival guard passes a chunk the model DUPLICATED — a verbatim 2× (or 7×) copy scores `survival 1.0` and ships; the guard flags deletion only, never inflation | `ANALYST_CHUNK_SURVIVAL` measures how much of the input survives in the output, so a superset scores ≥ 1.0 by construction — a one-directional guard | S115/S116 | `fixed` (S116: J34 BUILT `50242a8` — the inflation/ratio guard, `rejections["inflation"]`) | `analyst.process` inflation guard; docs/58 S16; filed S124 (J55) |
| SYM-091 | A book fails the analyst-stage audit for real deletions that each PASSED the per-chunk guard — a chunk that loses one paragraph of five scores ~0.8 and passes (DDIA `0.9683/61`; Univ 4e before it) | `ANALYST_CHUNK_SURVIVAL_MIN` has a blind zone between obviously-bad and perfect: partial deletions above the cut pass. The level is a lever (0.50 → 0.80 in S118 `aa93818`); the blind zone is by construction | S117 | **FIXED S140 (J46, signed Rab 2026-09-12) — read S183 E2 (2026-09-17)**: the diff-whitelist acceptor reconciles every ACCEPTED chunk against its input and REVERTS deletions, so a partial deletion the guard lets through no longer SHIPS — `analyst_selftest` S183 (SYM-091): one paragraph of five dropped, `chunk_survival` reads 0.8 and the guard PASSES it (the blind zone, real and UNCHANGED — its lever `ANALYST_CHUNK_SURVIVAL_MIN` is his), the shipped chunk carries all five paragraphs, `reverted {deletion: 1}`; J46 (c) the watched negative control (reconcile replaced by identity → the edit ships); `edit_whitelist_selftest` (deletion of a paragraph reverted with its paragraphing; at the chunk's start/end) — 51/51 and 54/54 run 2026-09-17. What closed is the defect (a deletion shipping), by the acceptor; the guard's resolution is what it was. Was: **OPEN** by construction — the lever is Rab's (J32); the remedy is J46's diff-whitelist acceptor | `ANALYST_CHUNK_SURVIVAL_MIN`; J32, J46; filed S124 (J55) |
| SYM-092 | The per-reason rejection breakdown (`rejections{fence, survival, think_leak, inflation}`) reaches the status/events payload but no widget surface shows it; an operator sees only the aggregate `chunks_rejected` | `events.rs`/`line.rs`/`room.rs` read `chunks_rejected` via dynamic JSON; no renderer reads the `rejections` sub-object — the PRODUCED-BUT-NEVER-PROJECTED cluster (SYM-058…061) | S116 | **OPEN** — Codex's M6-R1 territory (MSG-FAB-0072) | `windows-widget` renderers; filed S124 (J55) |
| SYM-093 | The figure-coverage report keeps FLAGGING a page that is a framed worked-example — a boxed illustration with a border, a few labels and a caption — as an uncovered figure, a false alarm the vetoes do not remove (IV p63, p73 survive all three vetoes; S104 §8). *(corrected S124 04:5xZ — the first filing of this row had the direction reversed; the consolidator lane caught it against S104's own text)* | The vetoes REMOVE flagged pages (706 → 477 → 352 → 269 figure pages on IV); a veto sums the region's partial text coverage (0.319) and table coverage (~0.21) = 0.53 against a 0.60 threshold, so the frame's contents read as under-covered and the page stays flagged. Lowering the threshold to 0.50 would catch these two and would be tuning to a sample of two with no held-out set; the fix is clustering that does not let a frame border swallow its contents (S104's own words) | S104 | **OPEN** — P-1's vetoes; shipped is 239 uncovered (S104) | `figure_coverage` vetoes; docs/58 S16's first member; filed S124 (J55) |
| SYM-094 | The figure-coverage table-veto calls a page a 'table' (and passes it) when the page is an illustration, chart or flow diagram; on a near-identical sibling page it reports no table at all (Cybernetics: 0-for-11 non-tables at ~100 % 'table coverage'; p74 1.0 vs p84 0.0) | The veto trusts MuPDF `find_tables()` as a table discriminator, but it keys on grid-like layout, not semantic tables; the one true positive (p84) survived with no margin. Re-derive the verdict through a second-shaped check before the veto acts | S104 | **FIXED S157 E25 (read S189 E2, 2026-09-17)** — the second-shaped check this row asked for exists: the table veto is DISQUALIFIED for a non-rectilinear cluster (`_nonrect_items` ≥ `VETO_TABLE_MAX_NONRECT` sizable curves + diagonals, `figure_coverage.py:151–160`), measured over every (cluster, vetoing table) pair — the Book of Models 14 of 15 pairs non-rectilinear, Investment Valuation 0 of 144; after: the Book of Models' vetoed tables 15 → 0, figure pages 57 → 71. The field 2026-09-17 (`sittings/S189/e2_cyb_json.txt`): `vetoed_table_regions 0 · pages_with_source_figures 71 · uncovered 3 (34, 78, 84)` — p84, this row's true positive, is reported, not vetoed. The p74/p84 page swing is `find_tables()`'s own and decides nothing now. The row was never re-read after S157. `Was:` **OPEN** (`Inferred` that the source is unchanged; not re-run S124) | `figure_coverage` table veto; family S12 (its root: `find_tables()` means grid, not table; the page-swing is the S15 surface — corrected S124); filed S124 (J55) |
| SYM-095 | The caption triage adjudicates figures on one book and silently does nothing on another: its 5/6 precision holds only for books that caption figures as `FIGURE N.N`; on any other convention every figure lands in 'the rest' with no warning | The triage keys on the literal `FIGURE N.N` string — an unstated input-shape assumption; Cybernetics uses another convention, so the triage is inert there while emitting no signal about its inertness. An instrument declares when its convention assumption is unmet | S106 | **FIXED S189 E1 (2026-09-17)** — the instrument declares its convention's state: `coverage()` reports `triage_convention` (`off` / `met` / `unmet` — figure pages exist and none carries `FIGURE N.N`) and `triage_captioned_pages` beside `triage_mode`; the human branch prints `TRIAGE INERT: … READ FIRST is empty by convention, not by coverage` under `mode=caption` when unmet; the partition and the lists unchanged (`figure_coverage_selftest` 51/51: the `Fig. 3` book reads unmet with its page in `uncovered_other`, the `FIGURE 1.1` book met, `mode=off` off, the CLI's line present on one and absent on the other, the JSON form carries it); on the field the Cybernetics models book — the row's specimen — reads `TRIAGE INERT` (0 of 3 uncovered captioned) and Investment Valuation reads 8 of 49 as before; the suite joins `close.sh`'s runner list. Still report-only and unwired from the pipeline (that is the instrument's design, not this row's defect). `Was:` **OPEN** — report-only, unwired | P-1 caption triage; filed S124 (J55); FIXED S189 (`figure_coverage.py` `triage_convention`) |
| SYM-096 | `close.sh`'s LEVERS gate reports no unlevered threshold constants while a hard-coded threshold sits in the code — inside a dict literal, a call default, or a tuple | The gate is regex-shaped (`NAME = <number>` in Python/Rust/JS) and cannot see a constant written any other way; it under-reports the class it guards (docs/18 §2: every autonomous behaviour has a lever). Fix: an AST walk, or declare the syntactic classes the gate cannot see | S106 | **FIXED S184 E1 (2026-09-17) — the blind classes LISTED beneath the row, warn-only:** `observability/lever_census.py` walks the ADDED lines' Python AST for a numeric literal as a comparison operand, a parameter default, a dict literal's value or a tuple/list/set element (0/1/-1/2 never; the gate's own `lever-waiver:` form honoured only with substance after the colon; selftests exempt — POLICY law 2); `close.sh` [5] prints its count as a sub-row under LEVERS (`--count`; the lines on a bare run) — a failed probe reads UNREAD; the exit code untouched (arming his). `lever_census_selftest.py` 11/11 — one case per class, the skip values, the waiver both ways, a syntax error, the added-lines restriction on a throwaway repo, the selftest exemption, and the NEGATIVE CONTROL that `NAME = 0.42` (the regex's class) is NOT this census's; `selftest.sh` 127/127 after the wiring. The historical measure (`--all`): 439 such literals across the tree (compare 210 · default 35 · dict 52 · sequence 142; 39 selftests exempt) the regex never saw — a listing, the judgment the author's; since `32dd0df` 0 added. Limits said in the docstring: a number reached through a NAME, a comparison against a call, an f-string. Was: **OPEN** by design (declared in S106 §10 f) | `.claude/skills/muster/close.sh` LEVERS; `observability/lever_census.py` + its selftest; filed S124 (J55) |
| SYM-097 | The caption-adjacency probe reports a figure as 'lost' when its image ships — an image reference sitting AFTER its caption, or further than ~41 characters away, is called missing though it is in the bundle (5 of 181 IV sites) | The probe searches a 2500-character window BEFORE each caption; the measured adjacency signal is ~41 chars (median) — 2500 is 60× that and one-directional; the single control (12.2 at −40 chars) sits at the mode and exercises neither the boundary nor the direction | S106 | `Historical` — the 2500-character probe was a one-off S106 Circle adjudication instrument, not shipped code: `git grep 2500` over `windows-converter/`, `observability/`, `linux-*/` finds no such constant (S124). The row stands as the calibration lesson for the next adjacency probe, not as a live defect *(corrected S124 04:5xZ from OPEN/Inferred)* | the S106 correction's probe; filed S124 (J55) |
| SYM-098 | A close/CI guard that scans a fixed recent window of commits for a known SHA silently stops firing — the red-path case reports success — once history grows and the target scrolls out of the window; nothing errors | `close.sh` fetched `?per_page=20` and scanned that page for the SHA; the known-red reference run (`534a6c0`) had scrolled off, so the guard could no longer see the case it exists to catch. A bounded search window is a frozen assumption about how far back the target sits | S109 | `fixed` (S109: full `head_sha` comparison, proven both ways) | `close.sh` CI check; SYM-071's kin; filed S124 (J55) |
| SYM-099 | A subagent or fleet lane's relay write lands on the LIVE bus — a real escalation appears in Rab's decision queue — though it was meant for a scratch surface; nothing errors and the entry is permanent (S109 17:25Z, MSG-FAB-0018) | The coordination target resolves to the production `coordination/` tree when its selector (`FP_COORD`) is unset — a fail-OPEN default; SYM-010's fixture-reaches-live family one layer up. S108 built the `FP_PIPELINE` scratch-root tripwire for the pipeline path, not this one | S109 | **OPEN by declared default** — `gate.py` prints `LIVE BUS` vs `quarantined bus (FP_COORD)` on every write (its docstring names the fail-open default and why it was kept); the default still resolves to live | `.claude/skills/relay-gate/gate.py` (`on_live_bus`); filed S124 (J55) |
| SYM-100 | The open/close card shows the SOFT clock green — the cookie/TIME-STATE values read as current — while those bytes were never committed to the memory repository; a whole clock move sat uncommitted for two days behind a green card (S109 → S110) | The gate measured PRESENCE of the value in the working files, not its DURABILITY (committed + pushed). S110 B1/B3: `close.sh` prints the memory repository's HEAD/tree durability line | S110 | `fixed` (S110 B1/B3) | `close.sh` memory durability line; filed S124 (J55) |
| SYM-101 | `git check-attr` reports `eol: lf`, so a CRLF-shebang fix looks handled — but a fresh clone gets neither the attribute nor the fix, because the `.gitattributes` carrying it was itself gitignored (a broad `dumps/*` rule) | `git check-attr` reads the WORKING TREE, not what a clone receives; its answer green-lit an attribute that would not exist for anyone else. Probe the shipped state (a fresh clone, `git ls-files`), not local disk | S110 | `fixed` (S110: the ignore rule narrowed; `.gitattributes` tracked) | `.gitignore`, `.gitattributes`; filed S124 (J55) |
| SYM-102 | On a `convert/chunk_batch_invalid` event the Room/Dock shows a blank where the fallback batch size should be | The renderer read `batch`, a key the producer never emits for that event; the producer emits `fallback` — a consumer/producer key mismatch at the projection boundary | S113 | `fixed` (S113 A4 `74f0d20`) | the schema registry (A4); filed S124 (J55) |
| SYM-103 | The Room renders off event fields (`tag`, `msg`) that no producer declares — a shadow schema — so uncontracted fields drive the surface and no gate can catch drift in them | The consumer invented structure from undeclared keys instead of ignoring or failing on them; A4's schema registry made the declared fields the only ones read | S113 | `fixed` (S113 A4 `74f0d20`) | the schema registry (A4); filed S124 (J55) |
| SYM-104 | A chunk passes the per-chunk survival guard (0.80–0.82) and the analyst audit, yet a paragraph is missing from its output or a numeral was silently rewritten (`100` → `10`) | The guard scores 12-word window overlap: one dropped paragraph in a many-window chunk barely moves the fraction and one changed digit changes no window's membership — the guard's resolution is coarser than the defect. SYM-076 is the same instrument's opposite error | S118 | **FIXED S140 (J46, signed Rab 2026-09-12) — read S183 E2 (2026-09-17)**: the acceptor REVERTS a changed numeral and a deletion at accept time, so neither ships — `analyst_selftest` S183 (SYM-104): `100` → `10` in a nine-window chunk, ONE window fails, the guard reads 0.8889 and PASSES (a digit moves the fraction less than the lever — the row's "changes no window's membership" was imprecise: the window that holds the digit does fail, the fraction barely moves), `100` ships, `reverted {numeral: 1}`; S183 (SYM-091) the paragraph; J46 (a) both in one chunk; J46 (c) the watched negative control; `edit_whitelist_selftest` ("a changed numeral is reverted and named") — 51/51 and 54/54 run 2026-09-17. J45's per-chunk digit multiset was never needed for this: the acceptor sees the digit as a substitution. Was: **OPEN** — J45 (per-chunk digit multiset) + J46 (the diff-whitelist acceptor) | `ANALYST_CHUNK_SURVIVAL`; docs/58 S16; filed S124 (J55) |
| SYM-105 | A converted book's Markdown reads `rst`, `proles`, `%ere`, `so$ware` where the source says first, profiles, There, software; the analyst 'repairs' the garble and the audit then scores the repair as loss | Subset fonts name their ligature glyphs (f_i, f_f_i, f_l, T_h, f_t) at codes 0x21–0x29 with no ToUnicode entry, and pdfium's `FPDFText_GetUnicode` (`pdftext/pdf/chars.py:18`) returns the raw code; MuPDF resolves the same glyphs to U+FB00–FB04 (`Verified` S119, two extractors) | S119 | **OPEN** — J48 (repair upstream, or a vocabulary-gated repair before the analyst). **Measured S188 E4 (2026-09-17, read-only, `sittings/S188/e4_ligature_footprint.py` over the 44 bundle markdowns in `anchor/` and `held/`, ~20 distinct books): the shape-detectable form (a lowercase letter, one of `! " # $ % &`, a lowercase letter; or `# $ % !` opening a word — `Di"cult`, `e!ectively`, `#rst`) occurs in ONE book — Designing Data-Intensive Applications, 16 hits in 12,766 lines (the S119 specimen; its dropped-glyph form `proles` is not shape-detectable and is uncounted); the other 50 "hits" are HTML entities in a Python book (`&lt;p&gt;`), R's `dfr$age`, one missing space. One book in twenty on this corpus: J48's build waits for a second specimen; the row's cost stands measured, not built.** | Marker/pdftext; J48; SYM-012's sibling; filed S124 (J55) |
| SYM-106 | The Room's convert-station progress bar reads 95 % for two hours while the book is still being analysed; the honest per-stage counts sit only in the numerations panel below it | The bar is `min(95, elapsed / Marker's promised eta_s)`, capped until `converted`, but the station stays RUNNING through the analyst and a cache-resumed convert gives a 0 s promise (`room.js:327-339`) | S118 | **OPEN** — J43 (drive the bar from slices x/total, then chunks n/total; a widget change on Rab's sign) | `room.js`; J43; filed S124 (J55) |
| SYM-107 | Most entries on the relay bus lack the RECAP / FOR RAB / SUGGESTED PROMPT slots Rab signed as mandatory (84 of 146 at S120), and the selftest meant to enforce them can never go green | CR-CDX-0002 (2026-08-24) made the slots mandatory, but the only check was a selftest over the whole append-only history — which append-only can never repair — and nothing refused a non-conforming entry at write time: the guard sat off the path every write takes | S120 | `fixed` (S120 B3: `gate.py post` refuses a missing slot; the selftest counts legacy entries before a cutoff) | `.claude/skills/relay-gate/gate.py` post; filed S124 (J55) |
| SYM-108 | `coordination/selftest.sh`'s header check (T2) reports every gated relay header as malformed — 13 of 145 match — while the headers are the current, correct form | The header regex predates the gate's `· <msg: …>` suffix: a check pattern typed once and never regenerated when the entry format grew, so a check that should pass fails by construction — SYM-071's kin | S120 | `fixed` (S120: accepts the gate's header; six 2026-08-24 headers grandfathered by id) **Cite drift (S162 E3, 2026-09-16):** `coordination/selftest.sh` labels the case `B2`, not `T2` (79b08a5 renamed it); the case is present. | `coordination/selftest.sh` T2; filed S124 (J55) |
| SYM-109 | A Rust build in a fleet worktree fails to link with `LNK1104` (cannot open file) though the same build passes in the main checkout | The worktree lives under the scratchpad's long path and MSVC's linker path-length limit is exceeded; a short `CARGO_TARGET_DIR` is the workaround (S122). Any fleet Rust lane under `AppData\Local\Temp\claude\…` recurs | S122 | `fixed` by convention — fleet Rust lanes set a short `CARGO_TARGET_DIR` (S122's own re-run is the record's line) | fleet briefs (the ground names it); filed S124 (J55) |
| SYM-110 | A read-only command in the shared checkout is DENIED by the git guard with a reason that names a git verb the command never had — three shapes so far: `sed -n "$((L+1)),$p"` (a `$p` head manufactured from arithmetic), a PowerShell `Where-Object { $_.TaskName -like … }` (a `$_` expression head read as an invocation), `timeout … ssh … grep -E` (grep's `-E` read as PowerShell's `-EncodedCommand`) *(fourth, S126 06:2xZ: a PowerShell expression `[math]::Round($w.WorkingSet64/1MB)` — the head's basename lost its `$` to the path split, and `[math]::` was read as a process start; v4.4, 141/141)* *(fifth, S127 06:2xZ: a PowerShell `switch` whose case labels are quoted GUID strings — a quoted-string head is an expression there; the `{` inside it tripped the bash brace-expansion rule; v4.5. The fix's own negative control found that bash `{git,} reset --hard` had been splitting into `git,` and PASSING — a real bypass, closed in the same version: braces split only at word boundaries)* *(sixth, S128 17:xxZ, filed S129: an inline `python -c "…"` whose code carries braces — UNREAD, denied; the case is owed as J64; the remedy meanwhile was ERR-009's own, the script in a file)* *(seventh–ninth, 2026-09-10/11, filed S130: `%es` inside a `time -f` format string; an inline PowerShell `if { … }` block; a `[void][System.…]::` head — each denied, each re-run as a script in a file; a `$VAR` head is the designed deny and is not counted)* *(tenth, 2026-09-12, filed S131: a `find … -printf '%TY-%Tm-%Td'` format string — `%ty-%tm-%td` read as the head; re-run as a script in a file)* | The guard reads the command line with one grammar for two shells and fails closed on anything it cannot read; each shape was a construct outside that grammar, rendered UNREAD and denied. Fixed one at a time, each as a selftest case FIRST (v4.1, v4.2, v4.3), never by loosening the rule | S124–S126 | `fixed` for the five shapes (guard selftest 144/144); the class stays open by construction — a fourth shape gets the same treatment: a case, then the grammar *(S137: shapes 6–11 fixed by 17 cases — nine false-deny regressions, eight negatives; guard selftest 172/172. The mechanism was not the brace rule but the wrapper recursion reading every wrapper's quoted argument as shell, and heredoc bodies split into commands; the same construct hid a bypass, SYM-116)* *(twelfth, S138 16:1xZ: `sed -n "$(grep -n … \| cut -d: -f1),\$p" file` — the text after a `$(…)` substitution opens a new segment whose first token `,\$p"` reads as a `$p` head; the first shape's family through a different door; not retried, `tail -n +N` instead; a case owed)* *(S142 E2, 2026-09-13: the twelfth shape FIXED — `subst_end()` keeps a `$(…)` inside double quotes in its enclosing word while its body is still emitted as its own segment and scanned; two false-deny cases and two negatives (`"$(git reset --hard),\$p"` still denied; a verb after the quoted substitution still denied); measured RED 178/181 before the fix, 181/181 after. Not touched: the unquoted sibling `echo $(x),$p` and backticks inside quotes keep the old split — no live deny on record; a case first)* | `.claude/hooks/guard_git_selftest.py` (the FALSE-DENY REGRESSION cases); docs/47 §9; filed S126 |
| SYM-111 | The process tracker's events-mode receipt lists most processes with an EMPTY command line, shows processes that never "exited", hangs children under a parent that had already died (its pid re-used), and pairs a wrapper with a bundle from minutes earlier (one elevated run, 2026-09-11: 514 of 546 rows without a command line, 131 missed exits) | The events engine subscribes to process start/stop events and reads the command line AFTER the event arrives: a short-lived process is gone before the read; a stop event can be dropped under load; a parent is resolved by pid at read time, so a re-used pid lands under the wrong tree; and the wrapper's claim of a bundle has no time bound. Poll mode reads everything in one snapshot and has none of these — events mode traded completeness for timing and did not say so | S130 | **FIXED S196 E2 (2026-09-17)** — the row's own done-when (the S195 Update clause below) is met: the first receipt written by the new library — S196's `receipt-20260917-152328.partial.jsonl` (private `proc-feed/proc_events/`, checkpointed from 19:23:28Z) — carries `cmdline_state` on 1,239 of 1,239 records and NO empty command-line cell (310 read `UNREAD (no command line captured)`, 929 `read`; the md row 386 UNREAD lines); the S195 receipt from the OLD library, the negative control: 0 of 990 with the key, 271 EMPTY cells (Verified: the survey fleet's verifier, S196 E1, and the coordinator's own count, `sittings/S196/e2_receipt_read.py`, both controls OK). THE S195 PREMISE WAS WRONG AND IS CORRECTED HERE: the boot service does not run the old library until his next boot — `proc_tracker_service.ps1:46` spawns a fresh `proc_events.ps1` child at every session-gate open and the child dot-sources `proc_events_lib.ps1` from disk (`proc_events.ps1:52`); the service started once (2026-09-15 22:51Z, service.log) and was never restarted; the build landed at S196's gate open. Residue, said: the service records in poll mode, so the claim window, the identity cache and the ancestor refusal (events-mode mechanisms) are proved by the selftest's s1–s7 (42/42) and not yet by a live events receipt — an events run is elevated, his; the S196 FINAL receipt is written at this sitting's gate close and S197 reads it. `Was:` **OPEN** — poll mode is the mode of record; events mode must render what it could not see as UNREAD, never as an empty cell or a zero; owed: a command-line cache keyed by pid + creation time, a bounded claim window, parents resolved by creation time · `Update 2026-09-17 (S195 E1)` — THE FOUR OWED MECHANISMS BUILT in `proc-feed/proc_events_lib.ps1` + `proc_events.ps1` (private): an uncaptured command line renders `UNREAD (no command line captured)` in the md row and the jsonl (`cmdline_state`), never an empty cell; the Win32_Process read after a start trace is CLAIMED only inside `$CmdClaimWindowSeconds` (a lever, 5 s — `Test-CmdlineClaim`, pure), else UNREAD naming the reused pid; the identity cache `$identity[pid · `Update 2026-09-17 (S197 E2)`: the FIXED reading holds on a FINAL — the S196 receipt finalized at the gate's close (`receipt-20260917-152328.jsonl`, header `partial: false`, window end 20:30:19Z, two seconds after S196's marker was removed): 1,281 of 1,281 records carry `cmdline_state`, 0 empty command-line cells, 350 read `UNREAD (no command line captured)`, 931 `read` (the md row 401 UNREAD lines); the S195 receipt beside it, the negative control, unchanged (0 of 990, 271 empty) — `sittings/S197/e2_receipt_read.py`, both controls OK |ticks]` beside `$known[pid]` (`Get-ProcKey`); `Get-AncestorChain` refuses a parent created after its child (`parent_state` = `UNREAD (pid reused)`). Tripwires s1–s7 in `proc_events_selftest.ps1` (a positive and a negative control each): 42/42 green (34/34 before). **The marker stays OPEN:** his boot service loaded the OLD library at boot and runs it until his next boot — the row moves to FIXED when a receipt written by the new library reads UNREAD where it once read empty | the tracker's library in `proc-feed/` (private repo, branch `workshop`); filed S130 |
| SYM-112 | A boot log written by a Windows PowerShell 5.1 script reads as garbage, or as every other character a NUL, in every other tool (`cat`, Python, a page's own reader) | The script appends stderr with `2>>`; Windows PowerShell's file redirection writes UTF-16LE with a BOM, and appending it to a file that began as UTF-8 yields a mixed-encoding file. The remedy is an explicit encoding on every write (`Out-File -Encoding utf8` / `Add-Content -Encoding UTF8`), never the redirection operator | S130 | **FIXED — S141 E7/E8 (2026-09-12), the row not updated until S160 E7 (2026-09-16)** — the redirections are gone: `desk_boot.ps1` starts the server with `--files` (the server opens `serve.log`/`serve.err` itself as UTF-8; 2d1e45a) and `desk_watch_boot.ps1`'s `Log` writes `-Encoding Ascii` (fe6a6cd); the UTF-16 specimen was set aside as `serve.err.utf16-until-2026-09-13`. S160 E7 gave `desk_boot.ps1`'s own `Log` the same explicit `-Encoding Ascii` (one token; PS 5.1's `Add-Content` default is the ANSI code page — right for ASCII, wrong the day a message carries a non-ASCII byte), the file parsed by PowerShell's own parser; it takes effect at the next boot, the running task reads nothing after its start. Observed after the 2026-09-15 reboot: `boot.log` 1,029 bytes · `watch-boot.log` 85 · `serve.log` 474 · `serve.err` 0 — **0 NUL bytes in all four**. Was: **OPEN** — the two-line fix is owed in the Desk's boot script (private repo) | `relay-desk/` boot script (private repo, branch `workshop`); filed S130 |
| SYM-113 | The open after a clean close reports `UNPARSEABLE ROW IN THE LAST 5: rowN(no-sha)` and `session mismatch: TIME-STATE S<N> vs ledger S<N-1>` — an INCIDENT — though the close ran green and the row was pushed | The ledger row is written AFTER `close.sh` runs (by design: the row names the closing SHA), so no close-side gate ever parses it; the only parser (`muster.sh` [3b]: the closing SHA must be the LAST non-empty cell, bare hex) sits on the OPEN path. A malformed row is invisible until the next session, which then opens on an incident | S130 | `fixed` (S132, J69: `.claude/skills/muster/row_check.sh <N>` — the same awk, the SHA checked against the repo, exit 1 before the push; selftest 48–52 incl. the awk-identity assertion) | `.claude/skills/muster/muster.sh` [3b] (the reader); `close.sh` (no row step yet); filed S130 post-close |
| SYM-114 | A held bundle cannot be shipped by any repair: cut the converter's repetition loop out of the body and the re-audit reports a loss of hundreds of words and stays `fail`; leave it in and the degeneration tripwire fails it; the bless button refuses it either way | The analyst stage's near-exact reference is Marker's OWN body (J33's sidecar), and Marker's loop is in it. The gate treated the reference as ground truth, so removing the disease read as removing text; the convert gate treated the same bytes as disease. Two signed signals, each right alone, closed every exit — and no path re-read the reference with the detector that already existed | S130–S131 | `fixed` (S131, J70, signed 2026-09-12): `mask_degenerate_reference` blanks every reference block `degeneration()` flags before the windows are built, and `reference_masked` in the analyst block says so; docs/15 §12.1; `analyst_audit_selftest.py` (h)–(k). The repeated-line signal is NOT masked — no specimen yet | `windows-converter/fidelity_audit.py`; `analyst_audit_selftest.py`; filed S131 |
| SYM-115 | A converted book carries the literal token `/no_think` at the end of a heading or a paragraph, and the paragraph it sits on is cut short there (the rest of the sentence, or the next paragraph, is gone) | The local analyst (qwen, `readability` program) emitted its own control token in the output and the chunk's text ended at it; nothing in the fence, survival or ratio checks names a control token as a rejection, so the chunk shipped with the token and without its tail (*Zero to One*: `**SECRETS** /no_think`; `…Once PayPal dominated this segment /no_think` with the 16-word tail dropped). SYM-074's kin (a bare `</think>` leaked, S115) — the same hazard, a second token | S131 | `fixed` (S135, 2026-09-12): the `/no_think` and `/think` soft switches join SYM-074's `think_leak` rejection (the same reason key — the key set is pinned; a new key would break T17 for nothing), word-boundaried so a URL's `/think` is prose; the original chunk ships; `analyst_selftest.py` (d)–(g) incl. a negative control that removes only the switch test, 26/26 | `windows-converter/analyst.py` (the accept branch); `text_norm`; J46; filed S131 |
| SYM-116 | A Bash heredoc whose body is fed to a PROGRAM (`python - <<EOF`, `node - <<EOF`) and shells out to git (`os.system('git reset --hard')`, `execSync('git clean -fdx')`) PASSES the git guard in the shared checkout, while the same shape with a harmless format line (` - [%s] %s`) is DENIED — the guard was wrong in both directions on one construct | The guard split every heredoc body into command segments (a body line's first word became a head: `%s` → UNREAD → deny) and applied the wrapper's mentions-git rule to the OPENER line only, so a git verb inside the program's text had no git head and no rule read it. Fixed S137: `fold_data_heredocs()` — a body is shell only when its receiver is a shell (`bash <<EOF`, `… \| sh`); a body fed to a non-shell wrapper that mentions git is denied as UNREAD when the cwd is guarded or the body names a guarded root; any other body is data and leaves the scan. Measured on HEAD vs the fix by `j64_hole_baseline.py` (S137 §8 F3) | S137 | `fixed` (guard selftest 172/172: the J64 block's negatives `python - <<EOF` shelling out and `bash <<EOF` both deny; the false-deny regressions allow) | `.claude/hooks/guard_git.py` `fold_data_heredocs`; `guard_git_selftest.py` J64 cases; docs/58 S16; S137 §8 F3 |
| SYM-117 | After a clean lockstep the memory library's TIME-STATE line carries the wrong date — the day the lockstep script was written, whatever today is | The library's `memory_timestate.py` (private, called by `lockstep.sh`) rewrote the header's date with a TYPED constant (`"· 2026-09-12 (S%s)." % N`) — correct on the day it was promoted (S134), wrong from the next day on; S8's shape (a fact frozen while the world moved). Found S138 by `brief_check.py`'s orphan rule (the script had no brief; writing one meant reading it). Fixed: today's UTC date from the clock; usage → exit 2; the brief forbids passing a date | S138 | `fixed` (private `3496990`; the S138 lockstep is the first run with the clock date — read the header after it) **Tripwire (S166 E1, 2026-09-16):** private `agent-scripts/developing/memory_timestate_selftest.py` (12 cases; `library_smoke` discovers it) — the script run WHOLE as a subprocess on a planted MEMORY.md reached through the new `FP_MEMORY_MD` override (unset, the library's own index; `lockstep.sh` never sets it): the header's date is TODAY's UTC date, never the typed 2026-09-12; the SHAs on line two; the block above the previous newest; no `--go` writes nothing; `already`; the 17,510 guard refuses with nothing written; usage exits 2. NEGATIVE CONTROL: a copy with `today = "2026-09-12"` planted back runs and the date case FAILS on it — the check discriminates. A midnight straddle tolerated (the date read before and after). | `agent-scripts/developing/memory_timestate.py` + brief; S138 §8 F5 |
| SYM-118 | CI goes RED on a ledger-row commit that changed no code: `acceptance.py` `FAIL — 139/140`, `a stale signature is caught in the SIGNED (--since) mode`, while the same script passed locally minutes earlier (run 263, S138) | The case took its `--since` ref from `_exercising_ref()` — the newest of the last 40 commits whose diff introduces an extractable key — and returned False (a FAIL, not a SKIP) when none did. Two record-only commits (the close, the row) pushed the last keyed commit out of the 40-deep window; the property itself holds against ANY non-empty diff (measured: HEAD~1/2/5 all True). The docstring above `_exercising_ref` described this exact shape from S78. Fixed: the keyed ref when there is one, else `HEAD~1`; None → the harness prints SKIP; 140/140 at the pushed HEAD | S138 | `fixed` (`observability/acceptance.py`; CI on the fix commit named in S138 §20) | `observability/acceptance.py` `_stale_caught_under_since`; ERR-092; S138 §20 |
| SYM-119 | A library script called with a bad or missing input (a malformed regex, a missing file, a 404 run id, an empty ledger, a non-integer argument, an unresolved ref) prints a Python traceback and exits 1 — the SAME code its brief documents for a clean negative ("nothing matched", "no results", "no lanes") — so a chain or a cold agent gating on the exit code reads a crash as an empty result | Eighteen instances across ten scripts of the private library (S139: four Sonnet lanes exercised 123 documented paths in fixtures; a refuter reproduced 18/18): the scripts guarded the documented happy and CONFIG paths and let every other failure fall through as an unhandled exception, whose exit is 1 by Python's convention. S4's shape — a failed probe rendered as a negative. Fixed: every such path is a `CONFIG: …` / `usage` line and exit 2 (POLICY §5); `run_gates.sh` verifies `--pin` before any gate; `procs.ps1` sets exit 0; `library_smoke.py` runs every tool's bare-call path at every open/close | S139 | `fixed` (private `6fbe0f0`; re-verified 15/15 by `verify_fixes_s139.py`; the smoke 32/32) | `agent-scripts/testing/library_smoke.py`; `SEQUENCES.md`; S139 §8 F3 |
| SYM-120 | `cd -` (or `cd ~`) followed by a tier-1 git verb in the same command was ALLOWED by the guard from an unguarded cwd — `cd - && git reset --hard` passed while `cd $VAR && git reset --hard` was denied | `guard_git.scan()` treated `-` and `~` as no-ops for its tracked cwd (`if target in ('-', '~'): continue`): the guard stayed at the caller's directory, which was unguarded, so the verb read as aimed outside the shared checkout. `-` is the shell's previous directory — unknowable to a guard that is not a shell — and must read UNREAD like `$VAR`; `~` is the profile and resolves. Found by the S140 second reading's instruments lane (I6), reproduced by its refuter with a different verb and a different variable name, in a lane folder with the log redirected | S141 | fixed (S141 E6: `~` resolves to `%USERPROFILE%`; `-` sets the cwd UNREAD; `cd - && git reset --hard` from an unguarded cwd denied; +3 selftest cases, 177/177) | `.claude/hooks/guard_git.py` scan(); `guard_git_selftest.py` cases 96–98 |
| SYM-121 | A `git push` from a branch with NO upstream carried a changed ledger unread: `row_check_missing()` diffed `@{upstream}..HEAD` and treated every non-1 return (including 'no upstream configured') as 'nothing to judge' → allowed | 'No evidence of a change' became 'no change' — the S4 family (a failed probe rendered as a negative). The row reader itself needs no upstream: it reads the working tree's newest row. Found by the second reading's I9; not reproducible read-only (it needs `--unset-upstream`), so it was fixed with a fixture case instead | S141 | fixed (S141 E6: with no upstream the row is checked unconditionally and the deny says so; +2 selftest cases on a fixture repo whose upstream is unset) | `.claude/hooks/guard_git.py` row_check_missing(); `guard_git_selftest.py` cases 162–163 |
| SYM-122 | `deferral_gate_selftest.py` could not run beside the live watcher for 13 days: its test watcher logged `another real File Portal watcher already owns Local\FilePortalWatcher` and exited 3 before `watching`, so every check went BAD - while the CHANGELOG's last reading of it (12/12) stood as its reputation | f045a66 (2026-08-31) gave the watcher one fixed OS mutex name with no env override, unlike the card mutex's `FP_CARD_MUTEX`; the tripwire's isolation (FP_PIPELINE, FP_CONVERT) never covered the mutex, and nobody ran the suite after the mutex arrived (AGENTS.md says not to run it casually). A guard whose tripwire cannot fire beside it is docs/32 §5's proxy with a reputation; its first run in 13 days found SYM-123 | S141 | fixed (S141 E7: `FP_WATCHER_MUTEX` honoured like `FP_CARD_MUTEX`, production unchanged; the selftest isolates BOTH mutexes per run - 19/19 with the live watcher up) | `windows-converter/watch_and_convert.py` _claim_single_watcher(); `deferral_gate_selftest.py` env |
| SYM-123 | A PDF dropped under a chat-hold was deferred (the card idle, the stub never ran, `.intake-state.json` phase `deferred`) but `watcher.log` had no DEFERRED line and `events.jsonl` no `intake deferred` event - the state file knew, the event stream did not | f045a66's main loop pre-empts the dispatch under a hold (rows -> `deferred` for the receipt) so `convert_one`'s DEFERRED log + event branch became unreachable except in a dispatch/hold race; the signed docs/33 §2.3 property ('prove it defers') was only visible in the receipt. The S7 shape: produced (the phase) and never projected (the event) | S141 | fixed (S141 E7: `_note_deferred()` in the loop - one line + one event per PDF per episode via the same `_deferred` set; the tripwire's held checks 3-5 read it) | `windows-converter/watch_and_convert.py` _note_deferred(); `deferral_gate_selftest.py` Phase A |
| SYM-124 | On 2026-08-31 the watcher's move of a converted source to `drop/done/` died with a FileNotFoundError traceback under `watcher loop error (continuing)`; `events.jsonl` had `converted` then an audit `error` (`No such file` at a `\\?\` path) and no intake event about the source at all - the book was later read as 'abandoned' and the traceback blamed on the full-width `：` in its name | The source had left `drop/` by another hand 20 s into its conversion (`drop/failed/` written 07:37:14Z, between `estimate` 07:36:55Z and `converted` 07:37:39Z; the same name had moved on 07-19 and twice on 07-22); `shutil.move` raised out of `convert_one` and no `emit` sat on that path. The name was never the cause - the tripwire's Phase D moves that shape | S141 | fixed (S141 E7: `_move_source()` - a failure is one `MOVE FAILED` line + an `intake move_failed` event naming source, outcome, dest and reason (missing vs the OSError); the worker's catch-all emits `intake worker_error`; the DONE/FAILED line prints NOT MOVED; Phase E of the tripwire removes the source mid-conversion and reads the event; no bare traceback) | `windows-converter/watch_and_convert.py` _move_source(); `deferral_gate_selftest.py` Phases D-E |
| SYM-125 | `watcher.log` crashed a UTF-8 reader (`'utf-8' codec can't decode byte 0x85`) and shows `�` in an editor at the `…` of one book's name, while every other non-ASCII char in the same line reads as `\uff1a`-style escapes | `logging.basicConfig(filename=…)` without `encoding` writes the locale's cp1252 with `backslashreplace`: a char cp1252 CAN encode (U+2026 -> 0x85, U+2019 -> 0x92) lands as a raw byte, everything else as an escape - a file no single decoder reads. S1's shape (the layer below chose the bytes) | S141 | fixed (S141 E7: `encoding="utf-8"` on the watcher's log from its next start; readers of the existing file decode with `errors="replace"` - the old bytes stay as they are) **Tripwire (S163 E3, 2026-09-16):** `watch_and_convert_selftest.py` SYM-125 (a)–(d) — the source proxy (the watcher's `basicConfig` carries `encoding="utf-8"`, the planted removal reds it) and the class itself in a subprocess with UTF-8 mode off: with the encoding U+2026 lands as `e2 80 a6`; WITHOUT it the locale writes the raw `0x85` and a UTF-8 reader raises — the row's symptom, watched (or said UNREAD where the locale is not cp1252). Suite 18 → 22 checks; run by hand (no automatic runner — the register row `close/run-the-hermetic-converter-suites`). | `windows-converter/watch_and_convert.py` main() logging.basicConfig |
| SYM-126 | CI red on the E7 close push (run 34729685347 / num 276, `Schema registry parity (A4, hard gate)` 17/18: `checked-in schemas.json is current` FAIL) — while `schema_registry.py --check` had never been run for the new `intake/move_failed` event; and after `--write` the sibling event `intake/worker_error`, emitted from a class METHOD (`_Worker._run`), was still absent from the registry with `--check` green 18/18 | Two holes: (1) the E7 partial closeout ran no registry check before its push (ERR-101); (2) `schema_registry._functions()` collected only `tree.body` FunctionDefs — an `emit()` inside a class method was on a writer path the extractor never walked (S5: a guard off the path the work takes), so a whole event could ship unregistered under a green gate | S141 | fixed (S141 E7 post-close: `_functions()` includes every class's methods as `Class.method`; `schemas.json` regenerated with both events; selftest +2 — a planted emit inside a class method trips the stale check and appears in the projection — 20/20) | `observability/schema_registry.py` _functions(); `schema_registry_selftest.py` case 1b |
| SYM-127 | An episode's denies cross-check reads `guard_record main: journal 0 != log 1 · lanes: journal 0 != log 1 ✗` in a window where no tool call was denied for a lane or by the record guard; the guard log shows two DENY rows at the same second with `tool=? agent=- reason=unreadable-payload` and `tool=? agent=-/- guard=record cmd={}` | `library_smoke.py --public` runs the live hooks as probes with an empty stdin (`PUBLIC_HOOKS … stdin=""`); the hooks log those refusals into the LIVE `git-guard.log` like any deny, and the cross-check (S141 E5) reads the log by agent field — `agent=-` is not `-/-`, so it counts as a lane; `guard=record` counts as the record guard — while the journal, read from the transcript, has no such call. The probe writes to the surface the check reads. | S143 | **FIXED S160 E3 (2026-09-16)** — private `agent-scripts/auditing/journal_check.py` `denies_check` skips the hooks' smoke rows (a guard-log DENY whose third field begins `tool=? ` — an unreadable or empty payload fed by the smoke, in both shapes the live log carries: `agent=- … reason=unreadable-payload` and `agent=-/- … guard=record cmd={}`), counts them apart and says so on the line (`smoke rows skipped N`). `journal_check_selftest.py` 25/25 → 27/27: two smoke rows inside the window leave the three counts ✓ with N=2, and a REAL deny added beside them still moves the count to ✗ (the negative control — the skip is by `tool=?`, not by the window). The live specimen, S143's whole receipt: HEAD's bytes `guard_record main: journal 0 != log 1 · lanes: journal 1 != log 2 ✗` → `0 == 0 · 1 == 1 ✓ … smoke rows skipped 2`. The receipts already written keep their ✗ lines as written (a receipt is never regenerated); the reader's subtraction this row prescribed is now the instrument's. Was: `open` — recorded, not fixed (the instruments are frozen S142–S145): the check is right about the log and wrong about the cause; a reader of the cross-check subtracts the smoke's rows by their `tool=?` shape until the smoke logs its probes elsewhere or the check skips `tool=?` rows | `library_smoke.py` PUBLIC_HOOKS; `journal_check.py --content` (the denies cross-check); `git-guard.log` 2026-09-13T06:41:20Z–21Z |
| SYM-128 | A re-audit reship prints `SHIPPED … -> staging/` and the ThinkPad's exporter answers `EXPORT-SUPERSEDE-HELD … incoming verdict 'fail' is not pass` — the verdict of the copy ALREADY in staging, not the one just shipped; `ls` on the ThinkPad shows the fresh bundle as `.part-<sha16>` INSIDE the old bundle dir (its files dated by the archive, the old manifest still at the top) | `convert_and_ship.py ship()` assembles into `staging/.part-<sha16>` and publishes with `mv part staging/<name>`; when `<name>` already exists — a held staging copy, which the exporter keeps in place after a failed supersede (`staging copy kept`) — `mv` moves the assembly INTO the existing dir instead of replacing it; the exporter skips dot-dirs and re-reads the old top-level manifest. The docstring says "atomically rename"; the existing-name case was never on its path | S144 | `fixed` S146 E4 (2026-09-13 18:1xZ): `ship()`'s remote command moves an existing staging name aside as `.held-<sha16>-<UTC stamp>` BEFORE the final mv (or exits 97 — never nests), deletes nothing in staging, and the `shipped` event carries `replaced_staging` (T22 a–e). Not yet read back on a live ship (the lever `enforce`; no ship this sitting). Was: `open` — recorded; staging repaired by hand this once (the old copy renamed to a dot-dir and KEPT, the assembly moved up, the exporter's on_moved then ran); the fix (`rm -rf` the existing name before the `mv`, or refuse and say so) is a converter change on Rab's word — `transport/ship-replaces-held-staging-copy` | `ship()`; the exporter's `EXPORT-*` line names the verdict it read — compare it with the reaudit's `from_verdict` before believing `SHIPPED` |
| SYM-129 | The analyst stage's progress file stops moving for a quarter of an hour on one chunk while the card sits at 95 %; ollama's slot shows a single request with `n_gen` in the tens of thousands (Ashby, chunk 103: 30,228 tokens by 10:30Z at 57 tok/s, 143 context shifts on one task, prompt 1,596 tokens) and the stage's ETA (17.9 s/chunk) is wrong by that quarter hour | `analyst.py` sends each chunk with `options: {num_ctx}` and `think: false` and no `num_predict`; the only bound is the client's `urlopen(req, timeout=900)` — a thinking model asked not to think can still loop, llama.cpp shifts the context (n_discard 4,093 a shift) and keeps generating past the window, and the analyst waits the full 900 s before the chunk fails over. Family S13 MODEL-OUTPUT-HAZARD, beside SYM-074/SYM-115 (the think leaks). OUTCOME (Observed 10:37Z): the client timeout fired at 10:36:34Z — ollama released the slot (`stop processing: n_tokens = 7030, truncated = 1`, the request having generated 43,000+ tokens) — and the analyst moved to chunk 104 (passed); chunk 103 left NO row in the chunk journal (101 `think_leak`, 102 `fence`, 104 passed): a timed-out chunk is invisible to the resume journal and to the record except as the manifest's `chunks_failed` count; the stage's rate re-read 26.4 s/chunk after it | S144 | `fixed` S146 E5 (2026-09-13 18:2xZ): the local request carries `options.num_predict` = 2x the chunk's estimated tokens (floor 512, cap NUM_CTX) — above the 1.5x inflation reject, so no acceptable chunk is lost; a reply that stopped on the bound (`done_reason` length) is rejected as `truncated` with the original shipped (analyst_selftest S146-E5 a–d). Not yet read back on a live analyst run. Was: `open` — recorded; the fix (a `num_predict` near 2× the chunk's tokens, so a runaway costs a minute, not fifteen; the failure named as a rejection reason) is an analyst change on Rab's word — `analyst/bound-chunk-generation` | `analyst.py` line ~231 (the request's options); ollama's `server.log` (`n_gen`, `context shift`); the chunk journal `.analyst-work/<hash>/chunks.jsonl` |
| SYM-130 | A re-conversion whose audit reads `verdict fail` (degeneration TRUE) prints `SHIPPED … -> staging/` and the ThinkPad's journal answers `EXPORTED … (commit … pushed + blob-verified)` — a failing book is a new vault note (Ashby, 54b471d7, 11:31:10Z) | two guards, neither on this path: the converter ships every verdict unless `audit-mode.txt` reads `enforce` (it read `report`), and the exporter's verdict guard ("pass, or flag with bless", S56/S117) sits on the SUPERSEDE branch only — a FIRST ingest of a bundle carries no verdict check. Family S5 A-GUARD-OFF-THE-PATH-THE-WORK-TAKES | S144 | `fixed` S146 E6 (2026-09-13 18:3xZ): the exporter's FIRST-ingest path reads `fidelity.verdict` and refuses an explicit `fail` (`EXPORT-INGEST-HELD`, receipt `ingest-held`, staging kept, vault untouched) — `flag` and a missing block still ingest; four tests, 38 passed on the ThinkPad; deployed: the ThinkPad's checkout pulled to cc64dd2 (from S126's 119db7d), its suites 38 and 90 passed on the real checkout, `file-portal-converter.service` restarted 18:28:43Z and watching staging again (MainPID 29195); the undo is a checkout of 119db7d and the same restart. Was: `open` — recorded; the vault note stands until Rab decides (keep as ship-with-losses-named, or revert); the fix (the ingest branch reads `fidelity.verdict` and refuses a fail like the supersede branch does; a tripwire ingests a fail fixture and asserts the refusal) is `exporter/ingest-verdict-guard`, ThinkPad code on Rab's word | the exporter's `EXPORTED` line vs the bundle's `fidelity.verdict`; the converter's `verdict_fail` + `shipped` events in the same second |
| SYM-131 | Books hold or ship depending on a one-word file (`audit-mode.txt`: `report` or `enforce`) whose current value no record names: it has read `report` since 2026-09-12 02:59:52Z (its mtime) with no event row, no session record and no Desk post saying who set it or why; the S142 triage and S144's re-conversions assumed `enforce` (the S109 and 08-25 holds) | the lever is read at verdict time (`audit_mode()`, `convert_and_ship.py`) and written by nothing that records — a machine fact without provenance; the open card does not print it. Family S7 PRODUCED-BUT-NEVER-PROJECTED (the ERR side of it is M4, ERR-102) — CORRECTED S145 (C-024): the card DOES print it (`levers audit=report …`, line 37 of the S142/S143/S144 cards); the reader read the plan, not the card — family S9 THE-WRONG-REFERENCE-OBJECT; the provenance gap (no record of the writer) stands | S144 | `fixed` S146 E2 (2026-09-13 17:45Z): `set_audit_mode()` is the one sanctioned writer of `audit-mode.txt` — every write emits `pipeline/audit_mode_set` (old · new · writer · reason · mtimes), a value outside the vocabulary or a write without a writer refuses before writing; `shipped`/`held` events carry `audit_mode=`; the card prints the mode and its consequence (S145 E1); the lever moved to `enforce` through it, the first write on record with a writer (T12 a–h, 224/224). Was: `open` — recorded; the fix (the card prints the mode at every open; a write to the file emits an event with the writer; the converter emits the mode it read beside every `shipped`/`held` event) is `pipeline/audit-mode-provenance` (Rab's word) **Cite drift (S162 E3, 2026-09-16):** the row's `T12 a–h` is `T20 (a)–(h)` in `convert_and_ship_selftest.py:1713` (T12 is slice retention); the eight cases are present. | `ls -la audit-mode.txt` for the mtime; `events.jsonl` for the absence |
| SYM-132 | A Marker slice dies at the card's ceiling (2026-09-13 09:01Z: 4,003 s, 100 % util, 9,515 of 10,240 MiB) and nothing in the record can say WHOSE memory the other ~4 GB was — the same slice inside the same converter re-ran at 1,698.6 s and 5.6 GB (S145 E8) | `_gpu_signature()` reads the card's total (`nvidia-smi memory.used`) and the `timeout`/`stalled` events carry only that; the per-process split (the WDDM `GPU Process Memory` counters' `Total Committed`) is never sampled — a death at the ceiling is unattributable by construction. Family S7 PRODUCED-BUT-NEVER-PROJECTED (the OS produces the split; the converter never reads it) | S145 | `fixed` S146 E3 (2026-09-13 18:xxZ): `_gpu_top_committers()` (WDDM `Total Committed` per process via typeperf; names from tasklist; EMPTY = UNREAD) + `_note_ceiling()` at 0.80 of the card on every 30-s sample (the worst moment's split kept); the `timeout`/`stalled` death certificates carry `gpu_top`, `ceiling_mib`, `ceiling_top`; the `slice` event carries `peak_mib` and the ceiling moment (T21 a–k, 235/235). Was: `open` — recorded; the fix is `converter/gpu-signature-per-process` (Rab's word: product code) | `wddm_sampler.ps1` (private `measurement/evidence/e8-converter-run/`) beside a run; the `timeout` row's fields |
| SYM-133 | The table layer under the lever ON rewrites a matrix's single-glyph ROW LABELS into a word: Ashby's transition table rows `3 4 5 6` become `ELASTIC` over blanks; `1 2 D 3 4 5` → `IDEAS`; `a<br>b` / `c` → `ABC`. Nothing errors; the invariant admits it (the letters fit, the word is the book's); the census reads a clean table | The rail signature (S150: ≥ 2 letter cells, four in five filled cells letters, blanks between or a stack) fires on any column of single glyphs, and the lexicon word route's glyph readings (S151: `6` → C, `3` → e, `4` → a, `5` → s — the OCR's misreads of rotated letters) let a column of DIGITS fit any word; "zero wrong labels" (S151 E1) was measured on one book's seven rails and carried into the lever ON (S154) for every book | S157 | **FIXED** S157 E15 (2026-09-15): `_index_run` refuses a run before any word is sought — more than half digits; every glyph a column heading of the same table (a matrix index); a stacked cell whose row is stacked alike with short parts (merged rows) — each on the record in `unresolved_rails`; Ashby 4 → 0 false labels, the shelf's other 30 bundles and Valentine's fourteen truths unchanged | `table_geometry_selftest.py` §15 (the three refusals + two positive controls, 175/175); the shelf probe `sittings/S157/e5_shelf_probe.py` (read its `labels` column, not only the trims); docs/15 §12.4.13 |
| SYM-134 | A table in a shipped note has lost its header row: the column names are fused into the first data row as `| d1 | a 1 | explain 0 | hard 0 | … |`, the separator and the other data rows intact; the analyst-stage audit flags the book on that table's words (a 36-word run) while the acceptor's table rung reports the table ACCEPTED | `table_geometry.grid_invariant`'s S152 E3 header-fold rule: a fold of the header row with the first body row "may join filled cells" (a stacked heading — `Standard` over `Error`), and `_fold_allowed(header=True)` returns True UNCONDITIONALLY — so the model's fusion of the header with a DATA row (`a` over `1`) is admitted as a stacked heading; `reconcile(FULL_TABLES)` ships the fused table while `reconcile(FULL)` (no table rung) would have reverted it as six insertions (private `sittings/S186/e1_grid_probe.txt`). The rung built to protect tables is the one that let this through | S186 | **FIXED S187 E2 (2026-09-17)** — `_fold_allowed(header=True)` refuses a lower row whose filled cells outside column 1 are all numeric ("the lower row is data — not a stacked heading (SYM-134)"); `table_geometry_selftest` 211/211 (the specimen refused with the reason naming data; the regression's stacked heading still admitted — the positive control; the rule named); `edit_whitelist_selftest` 55/55 (the acceptor restores the fused table whole); the field's before/after over DSB's 50 tables: table 31 (the specimen) ACCEPTED → RESTORED, table 6 (a caption case) ACCEPTED both times, 47 identical; the book's road now `--reanalyze` (J42), his word. Was: **OPEN** — `Observed` 2026-09-17 on held *Data Science for Business* `5414cc78e6b8a6c6`, Table 10-2 (the sidecar's table vs the note's, `e1_dsb_table_read.txt`); the fix designed: a header fold whose lower row's filled cells outside column 1 are ALL numeric is a DATA row, not a heading — refused with the reason; the regression fixture's `Standard`/`Error` + `Lower`/`95%` heading (letters among its cells) stays admitted (the positive control); the specimen joins `table_geometry_selftest` and `edit_whitelist_selftest`. `table_geometry.py` is the analyst child's import — built when the card is free (S187). **Reach (S186 E2, read-only):** 1 fused header in 204 tables across the day's six notes (DSB's table 36); the vault's three books (112 tables) carry none | `windows-converter/table_geometry.py` `_fold_allowed`, `grid_invariant`; `edit_whitelist.py` `reconcile`; filed S186 |
| SYM-135 | A long book's run ends with `intake/failed exit_code 1` right after the analyst-stage audit's events — no `held`, no `shipped`, no anchor copy, the PDF in `drop/failed/`, the analyst's 75-minute pass gone — and the watcher's log ends in `UnicodeEncodeError: 'charmap' codec can't encode character` at a `print(…, flush=True)` | The child's `sys.stdout` is cp1252: its spawner's environment carried no `PYTHONIOENCODING`. The widget's Rust spawn sets it for the watcher (`watcher.rs:250`) and the child inherits it; the lane's S175 WMI respawn (`Win32_Process.Create`, a bare command line) did not, and six books ran through that watcher before one (*Automate the Boring Stuff*, a clock emoji in `analyst_meta`) reached a print with a character cp1252 cannot encode — at `ANALYST done:` (line 1933), before the bundle's md and manifest are written | S187 | **FIXED S187 E3 (2026-09-17), both sides:** `convert_and_ship.py` `_console_utf8()` at import reconfigures stdout/stderr to UTF-8 with `replace` whenever they are not UTF-8 (whoever spawned the child — his by-hand run included); `watch_and_convert.py` passes `PYTHONIOENCODING=utf-8` in the child's env (live from the S187 restart, `e4_respawn_utf8.ps1` — `Win32_ProcessStartup.EnvironmentVariables`); the tripwire (`convert_and_ship_selftest` SYM-135): a child spawned WITHOUT the variable, utf8 mode off, imports the module and prints the clock emoji — exit 0, UTF-8 bytes; the NEGATIVE CONTROL reproduces the crash (rc 1); 289/289. The run lost is re-run on his standing word (the slices resumed from cache in 0 s; the analyst again). The error is the lane's — ERR-2026-09-17-110 | `windows-converter/convert_and_ship.py` (`_console_utf8`), `watch_and_convert.py` (the child's env), `convert_and_ship_selftest.py`; private `sittings/S175/e4_respawn_detached.ps1` (the cause), `sittings/S187/e4_respawn_utf8.ps1` (the cure) |
| SYM-136 | A convert killed by the stall monitor (or the outer timeout) leaves `watcher.log` and `events.jsonl` a frozen-seconds count and the card's GPU signature, but not one line of what Marker last printed — the stage it was in and the batch it was on die with the drained pipe (Ashby and Shannon & Weaver, 2026-09-19, both `frozen 903s`; that they froze at `Detecting bboxes` was known only because PORTAL's page happened to be watching the progress file) | `_run_marker` keeps `captured` (every line the drain read) and used it only on the non-zero-exit path; the stall and timeout raises carried the numbers and dropped the words | S209 | **FIXED S209 E2 (2026-09-20):** `_marker_last_words(captured)` — the last three non-empty lines Marker printed, each trimmed to 160 chars, joined by ` || ` (tqdm's bars carry ` | ` themselves) — appended to both messages (`marker's last words: …`), so the watcher's `FAILED` line names the stage; no new event key (the certificate's shape unchanged, glass untouched). Unit: `sittings/S209/last_words_check.py` (a bar-glyph tail, an empty drain, a one-line drain); ruff at CI's pin clean; `py_compile` clean | `windows-converter/convert_and_ship.py` (`_marker_last_words`, the stall and timeout raises) |
| SYM-137 | a conversion finishes — Marker, the audit, the analyst's whole pass — and the SHIP dies on `[WinError 3] The system cannot find the path specified` copying the bundle's `.md`; the pdf lands in `drop/failed/`, the bundle folder under `anchor/` exists and is EMPTY, PORTAL's row reads HELD (the Spring Economic Update, 2026-09-20 19:30Z, after 270 s + 28 min) | the bundle name ended in a space: PORTAL's `safe_stem` strips then cuts to 80 chars (the cut re-created the trailing space), and `clamp_name` rstripped only the clamped branch — a name ≤ 80 bytes passed through as it was; Windows DROPS trailing spaces and dots when it creates a directory, so `anchor/<name >/` was created as `anchor/<name>/` and the copy into `<name >/…` found no such path | S209 E8 | FIXED S209 E8 — `clamp_name` rstrips both branches (`bundle_name_selftest.py` 6/6); PORTAL's `safe_stem` rstrips after its cut (the tenant's S13); the SEU re-dropped | S12 | `windows-converter/convert_and_ship.py` clamp_name; `bundle_name_selftest.py`; the tenant's `app/server.py` safe_stem |
| SYM-138 | a document HELD with `verdict fail · phase analyst` while the analyst audit's doc_survival reads 0.9994 and its convert survival is the batch's best (TD Q3 2026: 0.9147, 15 of 81 pages flagged) — the manifest's `fidelity.analyst.runs` holds ONE run of 36 words that reads like a table header (`1 to 3 3 to 6 6 months up to …`) | Marker rendered the maturity table's header as two stacked rows (`Less than / 1 to 3 / 3 to 6 / 6 months / Up to 1 …` over `1 month / months / months / to 1 year / year …`); the analyst's readability pass merged them into one row (`Less than 1 month · 1 to 3 months · … · 6 months to 1 year · …`) — an accepted table-geometry edit and the BETTER table for a reader (the source, p.44, stacks the header because its columns are narrow) — and the analyst audit's near-exact windows over the raw first row no longer find that sequence, so the improvement counts as a 36-word omission and the run rule fails the document: the instrument manufactured the finding; S152's stacked-heading rule is not applied at the analyst-stage audit | S209 E10 · S209 E11 — the SECOND specimen: Stanford HAI's 2026 AI Index Report (425 pp, job 2d148dd1) HELD at analyst doc_survival 0.998 on two 36-word runs, the KaBLE table's two stacked header rows merged into one (`¦ ¦ ¦ ¦ GPT ¦ ¦ Cla ¦ ude ¦ Ge ¦ emini ¦…` over `¦ Task ¦ ¦ 01 ¦ o3 -mini ¦ 40 ¦…` → `¦ Task ¦ ¦ 01 ¦ GPT o3 -mini ¦ 40 ¦ Cla 3.7-S ¦…`; the pipes drawn as ¦ here for the register's cells); the sentence before it byte-identical in both files; the manifest predates the rung (`reorder: None`) | open — recorded; a FAIL cannot be blessed by design, so TD stays held; the fix is the audit's: a merged-header rung in the analyst ladder (J32-A/J44) or the window drawn per header GROUP, with TD's row as the fixture — THE RUNG BUILT REPORT-ONLY S209 E10: `fidelity_audit._reorder` re-tests each omission run as a bag of words within one span of the analyst's output; a reordering is marked `reorder` on the run and counted as `runs_reorder` / `words_reorder` on the block (signed EVIDENCE); the verdict still counts the run — the rescue needs his word (docs/15 §12), then a lever. `analyst_audit_selftest` (v): TD's stacked header merged reads reorder, deleted reads omission; 21/21; on CI | S15 | `windows-converter/fidelity_audit.py` audit_analyst (the windows), `text_norm.py` (the ladder); the fixture: `held/b1b9b7eddec3b2dc` (`.marker.txt` l.2218 vs `.md` l.2226) |
| SYM-139 | every manifest the watcher writes on this machine reads `analyst.chunk_journal_dump: "UNREAD: dump.sh did not run ([WinError 2] The system cannot find the file specified)"` (RBC, Scotia, C-31, the SEU — all of S209's; and every WATCHER-run manifest — NOT every run: `dumps/LEDGER.md` carries D0049 and D0050, chunk journals dumped 2026-09-17 by hand-run converters from a shell with bash on PATH; the inference 'every run since S141' corrected S209 E11, CORRECTIONS 18) — the chunk journal, the only place a rejected chunk's text lives, is never ledgered, and the ollama log capture at a failed call (`_capture_ollama_log`) dies the same way | `_dump_journal` and `_capture_ollama_log` spawn `["bash", dumps/dump.sh, …]` by bare name; the drop-folder watcher runs with an environment whose PATH has no `bash` (Git's is at `C:/Program Files/Git/bin/bash.exe`), so `subprocess.run` raises WinError 2 before the script runs; the except branch says UNREAD, never fatal — and nobody read the key until the frontier asked which half the model drops on a bilingual chunk | S209 E10 | FIXED S209 E10 — `_bash()` resolves the interpreter by name, then by Git's known install (`BASH_CANDIDATES`); both sites use it and say `UNREAD: no bash …` when neither resolves; the next watcher run's manifest is the proof (a `DUMPED …` id) — PROVED S209 E11: the AI Index's manifest (the first watcher-run subprocess started after the fix) reads `DUMPED D0051 929029 bytes` — the 13 rejected chunks' texts kept for the first time from the watcher | S12 | `windows-converter/analyst.py` `_bash`, `_dump_journal`, `_capture_ollama_log`; `analyst_selftest.py` (the resolver's case) |
| SYM-140 | NBC's Q3 2026 report shipped with 25 of 84 page anchors and 11 of those on ANOTHER page's text (`^p13` on p.17's footnote line, `^p17` on p.21's, `^p25` at line 3605 of 3873 — p.80's paragraph); measured afterwards on the other seven bundles of the batch: CIBC 2 wrong of 78, TD 2 of 81, Scotia 1 of 92, PBO/RBC/MPR/Nilsson 0 — and Scotia's `92/92` and TD's `81/81` had been reported as the port's proof: a count of anchors placed, which cannot see a wrong page | `anchor_markdown` takes each candidate key's NEAREST instance after the cursor; a key of MIN_KEY (24) chars or more is never checked for recurrence, and a bank report's segment pages carry the same footnote (`(1) See the Financial Reporting Method section on pages 5 to 12…`, ×11) and the same headings (`Third quarter of 2026 versus third quarter of 2025`, ×12) on every page; when a page's own instance is absent from the markdown (p.13's highlights table: its footnotes survive only at p.3's) the nearest instance is a LATER page's, the cursor overshoots it, and every page after reads no-hit (NBC: p.25 → p.84) | S209 E10 (NBC, `anchor_cursor.py` / `anchor_keys.py` / `anchor_truth.py`, private) | FIXED S209 E10 — a once-only key leads and is searched from the top, its position being its page's wherever the cursor stands; the TRUSTED once-only keys are the longest run whose positions increase with the pages (`_increasing_run`), because a once-only key can itself stand on the wrong page (p.13's footnote at p.3's line fenced pages 2–12 out on the first cut); and no hit — text or table row — may pass the first trusted text of a later page (the fence). `page_anchors_selftest` 20/20 (cases 18–20 the three shapes). The truth measure against the same trusted keys, before → after: wrong NBC 11 → 0, CIBC 2 → 0, TD 2 → 0, Scotia 1 → 0; right Scotia 59 → 82 of 92, TD 51 → 75 of 81, NBC 4 → 69 of 84 (84/84 anchored); the tenant's copy `app/vaultlinks.py` carries the same rule (S14). The lever stays OFF (B34, his signature) | S10 | `windows-converter/page_anchors.py` `anchor_markdown` (`trusted`, `fence`, `_increasing_run`); `page_anchors_selftest.py` 18–20; tenant `app/vaultlinks.py` |
| SYM-141 | Desjardins' 2025 annual report (297 pp, job c2643160, 2026-09-21 02:07Z) HELD at the analyst phase on five runs, three of them carrying a lone backslash token (`began on march 22 2025 backslash the`, `role on march 31 2025 backslash the tenure of serge`) — the sentences byte-identical in `.marker.txt` and the `.md`; the rung's bag test read the backslash as a missing word and left the runs `reorder: False` | Marker writes a literal asterisk inside a bullet as backslash-asterisk (`- backslash-asterisk Lesser effect…`, p.35) and a rule line as a run of backslash-underscore; the audit's J32-A v2 unescape set covers backslash-paren and backslash-dollar but not these, punct_free deletes the asterisk and (by v2's R5, which keeps backslashes) leaves the orphaned backslash as a TOKEN — 15 in Marker's text, 6 in the analyst's, because the analyst's whitelisted `escape` edit removes the backslash-asterisk; every vanished token fails its window and the run counts as an omission: the instrument manufactured the finding (S15) | S209 E13 (`backslash_probe3.py`, `ladder_probe.py`, `reorder_rerun.py`, private) | PARTLY FIXED S209 E13 — the rung's bag counts WORDS only (`_reorder`: a token with no word character is structure), so the runs read `reorder: True` (Desjardins 2 → 4 of 5; CIBC AR 4 of 5 unchanged) — report-only, the verdict still counts them; the ladder's own artifact (the orphan backslash) is the LADDER's to fix — a v3/v4 rung adding backslash-asterisk and the rule line to the unescape set, behind his ladder lever; `analyst_audit_selftest` (w) carries the raw shape | S15 | `windows-converter/text_norm.py` (`unescape`, `punct_free` R5); `fidelity_audit.py` `_reorder`; `analyst_audit_selftest.py` (w) |
| SYM-142 | Stanford CIFE's technical report (113 pp, job 18780692, 2026-09-21 02:32Z) HELD at the analyst phase on one 36-word run of letter shards (`a 2 f j ic ip co co pr pa`); convert 0.8038 with 37 pages flagged, and nine of the inventions measure's ten worst pages are ROTATED pages: on eight of the document's sixteen Rotate-90 pages (28, 29, 56–59, 80, 81) Marker's one Table block per page holds only two-letter shards (p.58 `in in ig En d De P ha g ee r g an`, 270 tokens; p.80 1,213 tokens — the manifest's `runs_top`) | the source's layer reads those pages whole (pymupdf p.58: 2,199 chars, `Engineering and Design Phase / Schematic Design / Design Development…`) and so does Marker's OWN extractor (pdftext p.58: 2,049 chars, the same words) — the break is inside Marker's later passes on a rotated table page; the rotated tables on pp. 96–100 read whole (containment 0.88–1.00), and spans and fonts do not separate the two sets; which pages Marker sent to its OCR is in no file (`page_info` carries bbox and polygon only) — Inferred: the OCR-error pass re-OCR'd them and the recogniser cut the rotated text into shards; the shipped text carries the shards, survival flags the pages, the analyst drops the run — the hold honest, the page lost | S209 E13 (`rot_probe.py`, `rot_census.py`, `cife_spans.py`, `cife_pageinfo.py` — scratch; `sourcing/pdftext_probe.py` p.58, private; `sittings/S209/e13_notes.md` ## 3) | OPEN — the class named on B40 (fragments): the stage is a rotation census in the manifest (`page.rotation` per page and the flagged share of rotated pages; report-only, the lane's to build) and, behind his lever, a pre-pass that bakes the Rotate attribute into the page before Marker sees it; what would settle the inference: Marker on p.58 with its OCR-error model off (the card) | S12 | `windows-converter/fidelity_audit.py` (`audit_inventions` worst list; `audit_convert`) |
| SYM-143 | The same report's p.32 — a project table with 48 check marks (√ in TimesNewRomanPSMT, the layer's own glyphs) — shipped with the word `second` written 291 times and `checkmark` once (`Case Projects … the second second second second…`): the inventions measure's worst non-rotated page, 292 invented of 430 Marker words, 0 lost | pdftext reads the 48 marks as √ (the extractor exonerated); Marker's Table block holds the repeated word — Inferred: the table's cells re-OCR'd, and the recogniser, shown a glyph it has no word for, emits one word and repeats it (291 is about six per mark); the reader gets `second` where the source shows a check mark; no measure of ours names a REPEAT — the inventions count reads 292 with the specimen `second` and nothing says one word made them all | S209 E13 (`cife_p32c.py`, `cife_spans.py` — scratch; private; `sittings/S209/e13_notes.md` ## 3) | OPEN — the stage: `audit_inventions` names a repeated invented word as its own class (`repeated: word, count, page` beside `invented_total`) and a symbol-glyph census per page from the layer (non-letter glyph runs) as the predictor; report-only, the lane's to build; a converter rule that keeps a symbol cell's glyph is his lever | S13 | `windows-converter/fidelity_audit.py` (`audit_inventions`) |
| SYM-144 | CIBC's 2025 annual report (200 pp) HELD at the analyst phase with one REAL dropped table row beside four reorders (E12's reading): `12,031` six times in Marker's text, five in the analyst's — the segment-results row (Total revenue / Provision for credit losses / Amortization and impairment / Other non-interest expenses, marker line 8672) gone from the shipped table | the loss is the analyst's own CPU pre-pass, not the model's: `analyst.fence` then `table_geometry.geometry_pass` with a resolver that refuses every call (0 calls) drops the row — the pass's split at lines 8672–8673 (`a stacked heading of 11 headings`) took the DATA row for the upper half of a stacked heading because `_is_num` tested the whole BR-stacked cell (`12,031<br>1,764<br>231<br>5,836` is not one number token) and `_join_heading` then dropped the upper piece (no letters) under a two-word lower piece; found read-only by the Codex lane (MSG-CDX-0086, 03:02Z: 358 of 358 chunk input hashes matching D0053), replayed by this lane (`cibc_ar_geometry_probe.py`: 6 → 5 → after the fix 6) | S209 E13 (Codex MSG-CDX-0085/0086; `sittings/S209/cibc_ar_geometry_probe.py`, private) | FIXED S209 E13 — `_has_num`: a cell is numeric when any of its `<br>` parts is a number (a bare percent excepted), used by `_heading_pair` and `_heading_row_br`; `table_geometry_selftest` 215/215 with the real rows (four SYM-144 cases, a positive control); the hold was RIGHT and the bundle stays held until re-run; the tenant (PORTAL) carries NO geometry pass (grep `_heading_pair` / `geometry_pass` under its app, 03:2xZ: nothing) — nothing to port, my earlier assumption struck (E13 later) | S16 | `windows-converter/table_geometry.py` (`_has_num`, `_heading_pair`, `_heading_row_br`); `table_geometry_selftest.py` |
| SYM-145 | The tables' shape measure (E11) read NBC's Q3 report as 77 columns lost over 19 pages, and this lane reported the number to Rab as the reader's loss (E11's Desk line and the desktop note) — on physical p.32 (block /page/31/Table/3) the source and Marker both carry nine columns with every figure of the mortgages row in place, yet the measure counted ten lost there | pymupdf's `lines` strategy splits a column at every ruling it finds: the witness read 20 × 19 with NINE columns empty in every row (the shading's edges), CELL_AGREE passed because Marker's cells all appear in the witness's text, and 19 − 9 became columns lost; the width rule did not fire (the witness box 442 pt of the block's 551) — the instrument manufactured the number; Codex's read-only counterexample (MSG-CDX-0085, 03:00Z), replayed by this lane (`nbc_p32_probe.py`: cells filled per column 15, 0, 16, 0, 16, 0 …) | S209 E13 (Codex MSG-CDX-0085; `sittings/S209/nbc_p32_probe.py`, `ts_whole` scratch) | FIXED S209 E13 — the witness counts the columns that carry text in any row and the rows that carry text in any cell (a ruling is not a column); NBC 77 → 14 over 4 pages, CIBC AR 3 → 0, Desjardins 11 → 1; `table_shape_selftest` 10/10 (a ruled table with a phantom ruling inside every column reads 0 lost); correction 20 in the lane's log; the residue (NBC p.26 `11x16->11x9`, p.74) unverified by eye **S211 E3 (the accounting's mechanism reading, NBC AR p.65): a SECOND shape — a ratios table whose column headers wrap unevenly (one column five physical lines, its neighbour one) reads as columns and a row lost though every cell is present; the lines witness counts bands from physical text lines. The population keys (S211) make the population visible; the witness's header handling is owed.** | S15 | `windows-converter/table_shape.py` (`_witness`); `table_shape_selftest.py` |
| SYM-146 | MIT OCW's Real Analysis lecture notes (92 pp, job 291a44ff, 2026-09-21 02:52Z) read 211 of 8,094 Marker words invented (2.6 %) and 646 lost, the specimens `definition`, `finite`, `coefficients`, `first`, `refinement`, `differentiable` — every one a word with fi, fl or ff | the LaTeX-typeset layer carries LIGATURE glyphs (U+FB00–FB06: 211 in the layer, on 66 pages — exactly the count invented) that Marker writes as plain letters; `audit_inventions` compared `deﬁnition` to `definition` as two words; survival is untouched by the fold (0.45, 86 flagged, 195 runs before and after: MIT's flags are the equations class, B43); the census over the catalog: Waterloo's second thesis 1,843 glyphs on 169 of 183 pages, MIT 211, Budget 2026 53, every bank 0 | S209 E13 (`lig_census.py`, `lig_rerun.py`, private) | FIXED S209 E13 — both sides folded (NFKC) before the words are counted: MIT 211 → 10 invented, 646 → 436 lost; `inventions_selftest` 13/13 (case 13: three ligature words, 0 invented, 0 lost, the real invention beside them counted) | S15 | `windows-converter/fidelity_audit.py` (`audit_inventions`); `inventions_selftest.py` |
| SYM-147 | NBC's Q3 2026 report (84 pp) SHIPPED (done, the vault lane) with physical p.57's TableGroup damaged: the repurchase-agreements-and-securities-loaned row carries `62 242` four times where the layer has `63,242` four times (and no `62 242`), the derivative-financial-instruments row beneath carries `63,242 15,810` in its cells, `1,040` appears three times over, `13,010` and `1,540` once more than the layer — a row's figures moved onto the row above and a figure mis-read, in a shipped bundle | the tables' shape measure never looked at it (TABLE_TYPES was `Table` only; the block is /page/56/TableGroup/33) and survival cannot see a moved figure (the numbers exist on the page); found read-only by the Codex lane (MSG-CDX-0085, 03:00Z), replayed by this lane (`nbc_p57_probe.py`: the layer's numbers against Marker's, multiset difference) | S209 E13 (Codex MSG-CDX-0085; `sittings/S209/nbc_p57_probe.py`, private; `e13_notes.md` ## 5) | PARTLY FIXED S209 E14 — TableGroup added to TABLE_TYPES so the measure reads them (NBC 14 → 15 columns lost over 5 pages, Desjardins 1 → 10 over 2 — the TableGroups' captions lower the cell agreement, disagree 76 → 123, honest); the damage itself is a ROW-LEVEL loss no measure names yet: the stage is a per-page 'numbers Marker carries more often than the layer' count (the duplicated-figure tell: `1,040` × 3) beside columns_lost — report-only, the lane's; Marker's table model on a spanning financial table is his lever | S15 | `windows-converter/table_shape.py` (`TABLE_TYPES`) |
| SYM-148 | Stanford SHEG's report (29 pp, job 310054e7, 2026-09-21 03:31Z; done, FLAG 0.8163) shipped `feld` (×2, p.3), `diferent` (p.3), `identifes` (×3, p.11), `imited` (p.11) where the source's layer reads `field`, `different`, `identifies`, `limited` — the inventions measure's specimens on p.3 and p.11, each an invented word beside a lost one (254 of 3,904 invented on the document, 179 of them p.1's OCR of a cover picture). **The class at the scale of a whole thesis (S210 E4, 2026-09-21 13:18Z — McGill's `Toward scalable photonic computing`, 192 pp, job 61d74afb, shipped as flag):** 660 dropped letters of 30,379 Marker words (`coefcient`, `defne`, `confguration`, `diference`), **93 of 192 pages flagged, survival 0.8169**, the analyst rejecting 48 of 92 chunks over a text it could not read; the loss on LAYER pages (`blocks.extraction`: 187 pdftext, 5 surya — none of the ten worst). Read at the char level (`sittings/S210/ligature_probe.py`): on p.111 MuPDF reads 19 words carrying fi / fl / ff, pdftext reads the same 207 words with none — no ligature code point, no empty or replacement char: pdfium returns the ligature glyph as its first letter alone. The same provider layer as SYM-155's zero ascent (pdfium's reading of the font), and the same family of lever | the letter is lost at Marker's OWN extractor: pdftext (pypdfium2) reads p.3 as `feld` / `diferent` and p.11 as `identifes` while pymupdf reads the same glyphs whole — the font (Source Sans Pro) draws fi / ff / li as one glyph and the two libraries resolve its ToUnicode differently; the layer carries NO ligature code points (0 of U+FB00–FB06), so SYM-146's fold cannot touch it: this is a real reader loss, not a measure's artefact | S209 E14 (`sittings/S209/sheg_lig_probe.py`, private; `e14_notes.md` ## 2) | OPEN — the fix is upstream (pdftext's glyph mapping) or a converter rule that takes the layer's word where Marker's is the layer's word minus one letter — his lever; the measure's cut (the lane's, report-only): `audit_inventions` names a DROPPED-LETTER class — an invented word at edit distance one from a lost word on the same page — beside `joined` / `fragment` / `garble` (B40) | S12 | `windows-converter/fidelity_audit.py` (`audit_inventions`) |
| SYM-149 | Two gates in the verdict path answer PASS or SHIP when they have nothing to read: `compute_verdict({}, None)` returns `pass` and a convert block with `pages_scored` 0 and `doc_survival` 1.0 returns `pass` (`doc_survival` None raises a TypeError instead); `_enforce_hold` ships the bundle, verdict unread, when the manifest cannot be read or parsed | the first is the function trusting its caller — through the pipeline's own path the empty measurement FLAGS (`audit_convert` on an empty markdown against a one-page source: doc_survival 1.0, pages_scored 0, verdict flag/convert; a blank source against real markdown the same), so the reading holds at the function, not at the ship; the second is BY DESIGN and documented in the function (docs/30 §5.4: "enforcement must never cost a conversion", `audit/error` emitted) — a signed rule that sits against the lane's doctrine that UNREAD never renders as a negative (docs/32); both retained from the Codex lane's 102-question audit (MSG-CDX-0088, 03:30Z) and replayed by this lane (`verdict_empty_probe.py`; the code read of `_enforce_hold`) | S209 E14 (Codex MSG-CDX-0088; `sittings/S209/verdict_empty_probe.py`, private; `e14_notes.md` ## 5) | OPEN — a gate's failure mode is Rab's to change (warn-first): (a) `compute_verdict` on a block with no `pages_scored` or a `doc_survival` of None reads `fail` (or raises, as the None path already does) — a one-line change with a selftest; (b) `_enforce_hold` holding on a manifest it cannot read — a reversal of docs/30 §5.4 he signed; the Desk line r24 on 49125cad asks | S4 | `windows-converter/fidelity_audit.py` (`compute_verdict`, `verdict_with_phase`); `convert_and_ship.py` (`_enforce_hold`) |
| SYM-150 | `reaudit()` re-audited a held bundle against drop/done/<manifest.source> — the file under that NAME today — with only `is_file()` between the name and `audit_convert`, while the Marker sidecar beside it is hash-checked and the slice-cache fallback is not: a same-named re-drop (the day's NBC and CIBC were re-dropped; every re-ship rewrites drop/done) would have produced a verdict about a file the bundle was never made from | the verb trusted a filename for identity (S10) where the ship path trusts a sha; retained from the Codex lane's 102-question audit (MSG-CDX-0088, 03:30Z, its lines 2518/2578 named), replayed by code read (`reaudit`, 2519–2525) | S209 E14 (Codex MSG-CDX-0088; `e14_notes.md` ## 6) | FIXED S209 E14 — the bytes are hashed before any witness is read; a mismatch REFUSES with `reaudit_refused reason=source sha mismatch` on the bus and the two shas in the exit line; `convert_and_ship_selftest` 297/297 with the mismatch case and its positive control (the held fixture now carries the REAL sha of the file it plants; the R1 traversal control likewise) | S10 | `windows-converter/convert_and_ship.py` (`reaudit`); `convert_and_ship_selftest.py` (`make_held_bundle`, the R1 control, SYM-150) |
| SYM-151 | The analyst's resume journal is keyed by `_resume_key(fenced, backend, program)` — the fenced text, the backend, the program's NAME (+tables) and the chunk size — so a journal written by one MODEL, or under a prompt whose TEXT was edited under the same name, was resumed as if nothing had changed: the resumed chunks carry the old model's or the old prompt's output under the new run's provenance | the key bound a name where the output depends on the text behind the name and on the model (S10); retained from the Codex lane's audit (MSG-CDX-0088: "the resume key binds text/backend/program NAME/chunk size, not model/prompt"), replayed by code read (`_resume_key`, 410–417) | S209 E14 (Codex MSG-CDX-0088; `e14_notes.md` ## 6) | FIXED S209 E14 — the key binds the model (`MODEL`, or `GEMINI_MODEL` on that backend) and the sha of the program's text (`load_program`; an unreadable program keys as `no-program-text` and fails the run itself); existing journals are orphaned once (a resume costs one re-run per in-flight document, none in flight); `analyst_selftest` 53/53 (SYM-151: another model or an edited prompt under the same name never resumes the same journal; the key returns once restored) | S10 | `windows-converter/analyst.py` (`_resume_key`); `analyst_selftest.py` |
| SYM-152 | BMO's Q3 2026 Report to Shareholders (69 pp, job 5a427f2e, 2026-09-21 04:17Z) HELD at the analyst phase (analyst 0.9993, 127 of 138 chunks passed) on ONE 36-word run — `for each method used in calculating rwa 5963 7680 …` — the rung reading `reorder: False`; in the shipped markdown rows 14 and 15 of the Basel disclosure index are one row (`14. Analysis of capital requirements … calculating RWA 15. Tabulate credit risk …`, the page references `59-63, 76-80` moved behind the joined label) with a header separator drawn under them | the analyst's CPU-only geometry pass (`table_geometry.geometry_pass`, replayed with a refusing resolver: 0 model calls, `splits` 1 at marker lines 89–90 `a stacked heading of 5 headings (5 stacked pairs)`) took two INDEX rows for a second table's stacked heading: their first cell is empty (the item's number sits in the label), a data row sits above (row 13: `63`), and NO cell read as a number — the page references (`59-63,<br>76-80`, `22-49,<br>51-67,91-93`) are not `_NUMTOK` tokens and the rest are dashes; SYM-144's rule, a second blind spot — and the join DROPPED row 14's page-reference cell (`16,22-49,<br>55-67,<br>70-71,78-81,<br>84-85,<br>88-93`: `_join_heading` drops an upper piece with no letter under a two-word lower piece), so the reader lost seven page references and the rung's `reorder: False` was right; the lane's first reading ("no word lost", the Desk r27) struck — correction 22 | S209 E14 (`sittings/S209/geometry_probe.py`, private; `e14_notes.md` ## 9) | FIXED S209 E14 — `_has_num` splits a cell on `<br>`, whitespace and commas and counts a page reference or range (`_PAGEREF`: a digit run with an optional `-` range) as a number-shaped token; the probe reads `splits` 0 on BMO after; `table_geometry_selftest` 218/218 (the real index rows, `_has_num` on five shapes, the regression's heading a positive control); the hold was honest by the rule and the bundle stays held until a re-run (his lever) | S16 | `windows-converter/table_geometry.py` (`_has_num`, `_PAGEREF`); `table_geometry_selftest.py` |
| SYM-153 | S209's lockstep (2026-09-21 06:16Z) printed `already`, exited 0, committed and PUSHED the memory library (0226df1) with MEMORY.md unchanged, and its own closing clocks then read `HARD clock ✗ session mismatch: TIME-STATE S208 vs ledger S209 · VERDICT ✗ INCIDENT` — a green step over an unwritten TIME-STATE | `memory_timestate.py`'s guard was `("**S%s (" % N) in raw` — meant to see a block already inserted (`>   **S209 (…`), it matched the index entry for `sourcing-line.md`, which begins `**S209 (Rab, 2026-09-20): …` — a guard coarser than the defect (S16) rendering a green without a measurement (S2); the lockstep's clocks caught it in the same run, which is the only reason it was seen | S209 close (`sittings/S209/lockstep_run.txt`, the first run; `lockstep_run2.txt`, the second; private) | FIXED S209 close — the guard reads the block's own line form (`>   **S<N> (` at a line start); the second run wrote (17,484 bytes, memory 61bec32, the clocks CLEAN); `memory_timestate_selftest` gains the case (an index line naming the sitting is not `already`; a block line is); correction 24 | S2 | `agent-scripts/developing/memory_timestate.py` (private); `lockstep.sh` |
| SYM-154 | Scotiabank's Q3 2026 Report to Shareholders (92 pp, SHIPPED 2026-09-20 as flag) carries on p.51 the wholesale-funding maturity table (T34) as one Table block with 159 number tokens where the layer's box holds 172 — 14 figures ABSENT from the shipped table (`4,763`, `10,540`, `8,310`, `3,569`, `7,241`, `34,423`, `11,249`, …; `missing_probe.py`: ABSENT, not rendered in another form); CIBC's Q3 report p.35 the same measure's other shape — 22 of 24 figures inside a FigureGroup shipped with no words (a chart whose numbers are text: B41's class, the figures measure's silent figure) | cells Marker drops inside a table it renders — a loss no measure named before: survival's windows are prose-shaped and a figure is one token; the tables' shape measure had no ruled witness (Scotia's tables are unruled — presence only); the numbers measure's `missing` side (S210 E1, b5ea910 / bef2be9), years apart, reads it as 14 on that page; the source read cell by cell in the layer's box (`absent_table_probe.py`: 172 layer tokens, 169 inside the Table box, 0 outside any block) | S210 E1 (`sittings/S210/missing_probe.py`, `absent_table_probe.py`, private; `e1_notes.md` ## 6) | OPEN — the measure names the page and the figures; the converter's loss (Marker's table cells) is his lever (a re-render of flagged table pages from the layer's own cell grid — B36's stage); the next cut of the measure: the missing figures' cells located in the Table box so the row is named (the tables' shape measure's rows_lost for unruled tables). **THE NEXT CUT BUILT (S210 E2, `45b3885`):** `audit_numbers` takes the source path and names the ROW each missing figure sits on from the page's own geometry (pymupdf words: the label on the figure's band, left of the first number-shaped token) as `missing_rows` on the worst entry, a page entering `worst` on its missing side alone from NUMBERS_MISSING_MIN (3); the measure's blind spot closed — a page whose blocks carried no number at all was skipped, the one page that lost every figure being the page it could not see (Scotia Q3 measured 90 → 92 pages). Live: Scotia Q3 p.51 → `Secured funding` 10 (the second panel's row, dropped whole — `missing_rows_probe.py`: the label appears twice on the page, Marker's table keeps it once), p.65 → `Cash and cash equivalents at end` 4 (a second row named); CIBC Q3 p.35 → the silent chart's series rows (`Average deposits`, `Credit risk`, `Operational risk`, 5 each); NBC Q3 no page under the floor. The converter's loss stands — his lever (B36's stage); the measure now points at the row | S12 | `windows-converter/fidelity_audit.py` (`audit_numbers`) |
| SYM-155 | Both of RBC's documents fail on the line for one reason: Marker discards the text layer of most of their pages and re-OCRs them — the Q3 2026 Report to Shareholders (76 pp, job 4c95e332) re-OCR'd 10,067 lines and looped (`deposit deposit deposit` ×117 in a footnote paragraph → degeneration → HELD); the 2025 Annual Report (246 pp, job 5604cd31, 2026-09-21 07:11Z) re-OCR'd 1,804 lines, then froze 904 s at `Detecting bboxes 1/20` with the card at 9,797 of 10,240 MiB, Marker itself the holder (8,554 MiB) → FAILED. Marker's own page gate replayed on the CPU (`sittings/S210/marker_page_gate.py`): provider lines present, the OCR-error model says `good`, layout coverage ok — `check_line_overlaps` FALSE on 2 of 3 sampled pages. **The specimen the bundle names itself (NBC's 2025 Annual Report, 254 pp, shipped as flag 2026-09-21 11:15Z — the first manifest with `blocks.extraction`, 4cb3c75):** pdftext 240 · surya 14 (pp. 22, 27, 34, 65, 98, 140, 172, 173, 195, 209, 212, 218, 219, 238); the gate replayed on the fourteen: thirteen are its (a superscript marker's line intersecting more than one neighbour; p.98 forty-five lines), p.140 an image page with no provider lines (OCR right there). Those fourteen hold five of the inventions' ten worst pages (p.195 `aired all allo credi imp wances`, p.173 `het iiicoiiie`), five of the numbers' ten worst (its top four), two of the tables' ten worst and eight of the 28 flagged pages (`sittings/S210/extraction_crosscheck.py`) — the fraction gate keeps all thirteen on the layer. **At the fleet's scale (S210 E3, `sittings/S210/gate_vs_worst_census.py`, private) — CORRECTED 11:32Z:** over the eight shipped clean-lane bundles whose inventions measure postdates the equations fix (S209 E12 — a manifest carrying `latex_commands`), 13 of the 63 pages the measure ranks worst are pages the overlap gate throws to OCR (21 %): NBC AR 5 of 10 (the manifest's own `surya 14` agrees), MIT OCW 3 of 10, TD AR 3 of 10, Waterloo Fakhraai 1 of 10, SHEG 1 of 10; the UofT theses 0 of 12 and the PBO 0 of 1 (their invented words are the ligature and dropped-letter classes). STRUCK, written at 0bc9103 and wrong by construction: 33 of 92 (36 %) over eleven bundles — Waterloo's Kamalzadeh thesis 10 of 10, NBC Q3 6 of 9 and Finance SEU 4 of 10 came from manifests written before the fix, which rank their EQUATION pages worst (LaTeX command names counted as invented words), and equation pages carry the sub- and superscripts the gate trips on — two classes read as one (the File Portal's CORRECTIONS.md, S210 row 8). One worst page in five across the line's measured bundles is a page the layer would have read right | RBC's PDFs set line boxes taller than the row pitch (a 15-pt box every 10 pt on table rows; a superscript footnote marker does the same to prose), so each row intersects both neighbours; Marker's `LineBuilder.check_line_overlaps` counts a line with more than one intersecting neighbour and compares `matrix_intersection_area` — an ABSOLUTE area in pt² — against `provider_line_provider_line_min_overlap_pct = 0.1`, a number named as a percentage: any two lines touching by a tenth of a square point count as overlapping, and one such line discards the whole page's layer. Over every page (`overlap_census.py`, pdftext lines, no model): RBC Q3 55 of 76 pages (72 %), RBC AR 121 of 246 (49 %); TD Q3 1 of 81, TD AR 7 of 244, Scotia Q3 4 of 92, NBC AR 13 of 254, BMO AR 10 of 207, CIBC AR 12 of 200, Desjardins 12 of 297, BoC FSR 0 of 42. The same test as a FRACTION of the line's own box: at 0.1 RBC Q3 51 / RBC AR 39; at 0.34 9 / 1; at 0.5 0 / 0 — TD and Scotia 0 at every fraction. **THE ROOT, PER DOCUMENT (S210 E2, `span_offset_probe.py`, `font_metric_census.py`, `line_grouping_probe.py`):** on the Q3 report, 843 of 7,046 spans matched exactly to MuPDF's (same text, x0 within 2 pt) sit 6.8 pt lower in pdftext, on 34 pages — every one in the embedded font RBCTextTabular-Bold (size 8.5), each with its pdftext top AT its baseline: pdfium reads that font's ascent as zero; the bold figures are the current-period columns, and a line's union of a right box and a low one is the 15-pt band. The annual report has NO such defect (29 of 17,914) — its 121 failing pages are the area test alone on 10–11-pt boxes at a 9–10-pt pitch | S210 E1 (`sittings/S210/marker_page_gate.py`, `line_overlap_probe.py`, `overlap_census.py`, `layer_probe.py`, private; `TERRAIN.md`) | OPEN — Marker's gate, upstream. **PROVED ON A COPY 08:07Z (`sittings/S210/overlap_fix_proof.py`, RBC Q3 p.20 on the CPU, 35 s), and the first fix REFUSED by its own proof:** a LineBuilder whose overlap test is the fraction its name says (at 0.5, injected through `marker.converters.pdf.LineBuilder`) keeps the page on pdftext — and the table comes out scrambled: the first and fourth columns shift down one row (`Net interest income` loses its `$ 1,215` to the row below; `Total revenue` carries `2,997` and `8,425`, `Non-interest income`'s) and two rows vanish — the same overlapping boxes mis-assign cells in the table pass; the gate guarded a real hazard on these pages, badly. The boxes measured (`line_overlap_probe.py`): 15.0-pt line boxes on a 10.0-pt row pitch, the spans' font size reading 1.0 (matrix-scaled type). The fix is the BOXES: pdftext's line boxes clipped to the row pitch (the midpoint of the gap to each vertical neighbour, or the span's rendered size) before the overlap test and the cell assignment — a provider patch, his signature; the re-run of both RBC documents after it, the copy's p.20 as the fixture (its layer rows: `Non-interest income (1) 2,997 2,628 2,471 8,425 7,335`, `Total revenue (1) 4,212 3,943 3,758 12,173 10,815`). **PROVED, PER DOCUMENT (08:45Z–08:49Z, `overlap_fix_proof.py` on the CPU):** RBC AR p.100 with the fraction test alone — pdftext kept, every table column right against the layer (`Regulatory Capital · 140,356 · – · – · 12,006 · 152,362`; `Stable deposits (3) · 106,148 · 54,260 · 28,305 · 29,640 · 208,918` — the cells drawn with · here, the register's own separator being the pipe), 38 s — the annual report's fix is the gate's test as a fraction at 0.5, for his signature. RBC Q3 p.20: the Tabular-Bold lift alone still goes to surya; the lift + the fraction test keeps pdftext (43 spans lifted) and the bold columns STILL sit one row down — because `marker/processors/table.py` (l.13, l.472) re-reads every cell's text with pdftext's `table_output` on pdfium's raw char coordinates: no LineBuilder polygon reaches the cells. The Q3 fix is one layer down — a pdftext-level char-metric correction (a char whose box top sits at its baseline lifted by 0.8 × size) proved on this page next; the OCR path stays right meanwhile (the control: surya, the table right, 740 s). **THE Q3 PAGE PROVED (09:14Z, `overlap_fix_proof_p20_metric3.txt`; `metric_patch_check.py`):** `pdftext.pdf.pages.get_chars` wrapped — for each char pdfium's TIGHT box (the ink) read beside the LOOSE box pdftext keeps; a loose box whose top sits below the ink's top by more than 0.3 × size (size = the larger of the font size and the box height — this producer draws at size 1.0 through a scaled matrix) is lifted to clear the ink by 0.15 × size, no font name: on p.20 226 chars lifted per read (every one RBCTextTabular-Bold), the spans off their ink 28 → 0 of 124; 0 chars touched on the AR's p.100 and TD's p.5. With the fraction gate beside it, the page keeps pdftext and every table row reads right against the layer (`Net interest income (1) · $ 1,215 · $ 1,315 · $ 1,287 · $ 3,748 · $ 3,480`; `Total revenue (1) · 4,212 · 3,943 · 3,758 · 12,173 · 10,815`; the cells drawn with ·), 83 s of CPU against the OCR path's 740 s; with the stock gate alone the page still goes to surya (the area test trips on the superscripts). **THE PROPOSAL, complete:** (1) the gate's overlap test as the fraction its name says (0.5) — a LineBuilder injected through `marker.converters.pdf.LineBuilder` (frees the annual report's 121 pages alone); (2) the get_chars wrapper (frees the Q3 report with (1)); both installed by `marker_blocks.py` before the converter is built — his signature, the engine policy; upstream reports to marker-pdf (the area named as a pct) and pdftext (the zero ascent) after; the re-run of both RBC documents then **S211 E3 (the accounting's mechanism reading, BMO AR p.97): a page the gate threw to OCR on a clean-layer document ships table labels garbled — `peposits field at other illialitial illistitutions` for `Deposits held at other financial institutions` — while pymupdf reads that page's layer whole: the GUARD OWED is a report-only measure comparing an OCR'd page's text against its own intact layer (the layer is right there on the clean lane), so a wrong OCR routing is seen at the page, not inferred from the dial.** | S3 | Marker 1.10.2 `builders/line.py` (`check_line_overlaps`), `util.py` (`matrix_intersection_area`); `windows-converter/marker_blocks.py` (the injection point) |
| SYM-156 | Scotiabank's Q3 2026 report (92 pp, job 68133c0c, born-digital: 25,152 spans, 0 invisible) went through the SCAN lane — `--strip_existing_ocr`, every page OCR'd (`Recognizing Text 551/551`), survival 0.7298 as an agreement witness, 46 of 92 pages flagged, and on p.51 a whole table row dropped (SYM-154's Scotia half: the `Secured funding` row of the second panel, ten figures) — because 160 of its spans on pages 11–20 carry `GlyphLessFont` (`sittings/S210/font_census.py`: 0.64 % of the spans; the other 24,992 in Scotia's own faces). **The second document (09:15Z):** Scotia's 2025 Annual Report (236 pp, job 01a8b342) went through the SCAN lane by the same trigger — 202 of 73,206 spans in GlyphLessFont on 10 pages (0.28 %), invisible ratio 0.023 — force-OCR'd on the card while this was written (`Recognizing Text 1548/1766` at 09:27Z). **Its cost (09:43:51Z):** that conversion FAILED — the scan lane's full OCR of 236 pages reached Marker's table-cell detection pass with the card at 9,664 of 10,240 MiB (Marker's child 8,646) and froze 902 s (SYM-159's certificate, the same as RBC AR's); a born-digital annual report with a good text layer was lost to one span's font | `convert_and_ship.py` 1066: `ocr_layer = ocr_font_trigger is not None or (total_spans > 0 and ratio > 0.5)` — the font-name check, called a secondary in its own comment, decides the lane ALONE on the first span it meets, document-wide, against its own evidence (invisible ratio 0.0) | S210 E1 (`sittings/S210/font_census.py`, private; `terrain.py`) | OPEN — the lane rule is his (docs/18, the engine policy; warn-first): the trigger scoped to the share of spans (or to the page), with Scotia Q3 as the fixture and Stanford SRI (a real scan: 24,470 spans, no OCR font, the ratio rule) as the control; Scotia's re-run on the clean lane after. **PROVED ON A COPY (S210 E2, 09:26Z, `sittings/S210/lane_rule_census.py`, private — the converter's own `probe` for the lane and its evidence, a pymupdf span census for the OCR-font share):** over the catalog's 33 PDFs plus Ashby and Shannon, the proposed rule (scan when the invisible ratio > 0.5 OR the OCR-font share of spans >= 5 %; clean otherwise) moves exactly TWO documents to the clean lane — Scotia Q3 (0.64 %) and Scotia AR (0.28 %) — the three real scans stay scan by the ratio (Ashby 1.000, Shannon 1.000, SRI 1.000) and every born-digital document is unchanged (CIBC AR invisible 0.062, TD AR 0.009, no OCR font). Both Scotia documents re-run on the clean lane after his signature | S16 | `windows-converter/convert_and_ship.py` (the probe at 1045–1076) |
| SYM-157 | Desjardins' 2025 annual report (297 pp, job c2643160) reads `degeneration: true` — the convert phase's only FAIL signal — on one paragraph: line 1335 of Marker's body, 348 chars, `**\_\_\_…\_**`: a signature line of 172 escaped underscores, three distinct glyphs (`sittings/S210/degen_lines.py`, the paragraph read the detector's way: table rows blanked, split on blank lines); the other five specimens on the line are true loops (AI Index `the lowest score for` ×59; RBC Q3 `deposit` ×117; Ashby `A B` ×487 and `b c x` ×92 and an equation ×162; Visual Display `SHOWN TO VOTING` ×88) | `fidelity_audit._degenerate_blocks`: a block with fewer than five space-separated tokens falls to the CJK path (characters as tokens), where a rule line's `\ _ \` trigram repeats 171 times and zlib crushes it — the instrument manufactures a loop from a line that carries no words | S210 E1 (`sittings/S210/degen_lines.py`, private) | OPEN — a gate's definition (docs/15 §9, signed; warn-first): the space-free fallback exempts a block with three or fewer distinct non-space glyphs (a rule, a leader), with the Desjardins line as the fixture and the Brain-of-the-Firm anchor as the positive control; Desjardins' re-audit after. **PROVED ON A COPY (S210 E2, 09:25Z, `sittings/S210/degen_clause_proof.py`, private):** the stock `_degenerate_blocks` beside a copy carrying the one clause (a paragraph with three or fewer distinct non-space glyphs is skipped), over the marker bodies of 34 distinct documents — the clause silences exactly ONE block in the corpus, Desjardins' line 1335 (3 glyphs), and every true loop still fires (AI Index 2327; RBC Q3 3304; Ashby 3438, 3770, 5621; Visual Display 345); 28 documents with no block either way unchanged. The selftest for the change is that run's fixture list: the Desjardins line must not fire, the five loops must | S15 | `windows-converter/fidelity_audit.py` (`_degenerate_blocks`) |
| SYM-158 | Bill C-31 (346 pp, job bc8bf802, 2026-09-20 17:22Z) HELD as `ENGINE-CRASH` fifty-six minutes into its conversion, at `Recognizing Text 16373/22002`: `PermissionError: [WinError 5] Access is denied: …\\bc8bf802\\job.json.tmp -> …\\job.json` — the tenant's store could not rename over `job.json` for two seconds (40 tries × 0.05 s) while a reader held the handle | the tenant's `on_progress` save is guarded (an `OSError` is logged as `STORE-WRITE deferred` and the engine keeps running — SYM-013's lesson) but `_station()`'s save is not: the same rename, raised from a station note, propagates out of `engine.run` as the engine's crash — a bookkeeping write with the job's whole outcome as its blast radius | S210 E1 (`terrain.py`; the tenant's `server.py` 92–126, 286–293, 329–337) | OPEN — a tenant fix (its own repo and record, S16): `_station()` defers on `OSError` like `on_progress`, the terminal saves stay strict; needs the tenant's restart at a quiet moment; C-31's re-run after | S11 | the tenant `app/server.py` (`Store.save`, `_replace_with_retry`, `_station`, `on_progress`) |
| SYM-159 | A long document on the OCR path dies at the same place every time: `Recognizing tables N/N` completes, `Detecting bboxes` begins (Marker's table-cell detection pass), and progress freezes at batch 0 or 1 of ~20 with the card at its ceiling — the kill-early policy (docs/18 §5.1) then fires at 902–904 s frozen. Four specimens with one certificate (SYM-132's death certificate at work): Ashby (306 pp, 2026-09-19, 9,735 MiB, Marker 7,168), Shannon & Weaver (132 pp, 09-19, 9,775 / 8,712), RBC's 2025 Annual Report (246 pp, 09-21 07:11Z, 9,797 / 8,554; 1,804 lines re-OCR'd first), Scotia's 2025 Annual Report (236 pp, 09-21 09:43Z, 9,664 / 8,646; the whole document OCR'd on the scan lane). Two documents that reached the pass with their text layers (TD AR 244 pp, 9,726 MiB peak; NBC AR) passed it | the detection model's batch over the table cells of a long OCR'd document is sized for a card with room; at 9.4–9.8 GB of 10,240 MiB the first batch never returns (a wedge or paging at the ceiling — the certificate cannot say which), and the watchdog reads a frozen pipe. The banks reach this pass by SYM-155 (RBC: half the pages thrown to OCR by the overlap gate) and SYM-156 (Scotia: the whole document on the scan lane by one span's font) — their fix is to keep the layer; the real scans (Ashby, Shannon) have no layer to keep | S210 E2 (`terrain.py`'s certificates; `e2_notes.md` ## 10) | OPEN — the kill-early is right (a frozen batch at the ceiling never returns); the levers are his: (a) SYM-155's and SYM-156's fixes take the banks off this path; (b) for real scans, Marker's `detection_batch_size` / `table_rec_batch_size` lowered when the card sits above 0.85 of its memory at the table pass (the engine policy, docs/18) — a proof on a copy with Ashby's pages before it is proposed; (c) the certificate could carry the batch sizes in force (a producer key) so the wedge is read, not inferred | S11 | Marker 1.10.2 `processors/table.py` (the cell detection over `table_output`), `builders/line.py` (`get_detection_batch_size`); `windows-converter/convert_and_ship.py` (`_run_marker`, the stall watchdog) |
| SYM-160 | A bilingual bill from the House of Commons (Antenna House XSL Formatter 7.4) with a clean text layer is thrown to OCR almost whole: Bill C-30 (45-1) 39 of 42 pages re-OCR'd (the three kept are its near-empty title, notes and back pages), Bill C-288 4 of 5 — at 11.2 and 11.4 s/page against 7.0 and 1.6 promised. The OCR reads the printed columns right (anchors 39 / 39 and 5 / 5, the analyst 1.0) but the inventions measure counts 27 invented / 479 lost words on C-30 (joined 9 · fragment 3 · dropped letter 1 · garble 14, every one of its ten worst pages an OCR'd page) and the numbers measure 5 extra figures on OCR'd pages | the producer's pages carry a hidden duplicate of both columns OFF the page (x 1038–1284 on a 612-pt page); pdfium's text page keeps it (MuPDF clips it: TEXT_MEDIABOX_CLIP), so the duplicate reaches pdftext. pdftext 0.6.3's `deduplicate_chars` drops a word whose rounded box, text and font were seen before — 740 of the duplicate's newline chars on C-30, their point at x 1284 colliding line for line — and `get_spans` joins consecutive chars of one font with NO vertical test, breaking only after a newline, so the newline-less lines glue two and three deep (249 glued spans on 36 of 42 pages; a 21–33 pt box for a 9.7-pt font; 'havecome', 'isamended', 'ofwhich'). Marker's `check_line_overlaps` then finds each glued off-page line intersecting two others and returns False. Its off-page filter cannot catch the duplicate: `polygon.expand(5, 5)` takes FRACTIONS of the page — five page-widths a side, an eleven-page box under a comment calling it a small margin (SYM-155's unit family) — and had the margin been 5 pt an off-page line returns False too: the gate condemns the page either way; the visible columns' own layer is whole (font census 7 of 7,107 span pairs off — not SYM-155's root) | S210 E5 (`bill_glue_probe.py`: the four pdftext steps replayed, the dropped words and the glued spans named, pdfium's own chars at the join; `bill_flatten_probe.py`: flatten on and off the same — ruled out; `marker_page_gate.py`: 6 of 6 body pages by the overlap condition alone; `e5_notes.md` ## 3) | OPEN — B45 decision (8): the provider drops chars outside the page box before dedup, as MuPDF does — the duplicate gone, no collision, no glue, no overlap — HIS signature, a proof on a copy owed before it is proposed; until then every bill from this producer takes the OCR path (right text, eleven seconds a page, the inventions measure reading OCR garble as the layer's loss). **S211 E1–E2 (2026-09-21, Rab's word 19:05Z — the proofs as VARIANTS beside the failed originals): BUILT as the lever `offpage-clip` (fixes.py; chars outside the page box dropped before pdftext's dedup; one pdftext worker under a get_chars fix — CORRECTIONS row 5), PROVED on a copy (C-30 36 of 42 glued pages → 0, every gated page RE-OCR → kept, the FSR control 0 dropped), its reach measured over the shelf (clip_census: 61 PDFs, 46 untouched; the three Antenna House bills' glue to 0 — C-30 94,532 chars dropped, C-288 8,158, C-31 108,518, whose 22,002 re-OCR'd lines in S209 were this mechanism too; partial on the Finance SEU, NBC AR and one book), and its first variant read: C-288 ~160b kept all 5 pages on the layer (the original 1 of 5; the registry selected it, the original beside it); C-30 ~160c on the line. The lever is off unless a job's lever names it; the whitelist decides which variant pops up.** | S16 | pdftext 0.6.3 `pdf/chars.py` (`get_chars` keeps every char; `deduplicate_chars`), `pdf/pages.py` (`get_spans`); Marker 1.10.2 `builders/line.py` (`check_line_overlaps`), `schema/polygon.py` (`expand`, fractions) |
| SYM-161 | A variant of a document lands with every measured number equal to its original's (survival, invented, lost, numbers, tables, degeneration) and reads WORSE on the page: headings demoted to body text, bold section numbers unbolded, the margin line numbers glued to sentence ends, two column headers fused into one cell — and the registry selected it as the newest (Bill C-288 ~160b over ~160: all 5 pages on the layer, equal numbers, the layout worse). Nothing in the manifest moves when structure is lost | No measure reads STRUCTURE. The inventions measure counts words (a demoted heading keeps every word), the numbers measure counts figures by band, the tables measure counts rows and columns, the degeneration measure counts loops and rule lines — a heading rendered as body, bold dropped, a margin number joined to a sentence, two headers fused, change none of them. The whitelist ranked on (verdict rank, fewer errors, survivals) and then converted_at, so equal numbers chose the newest: a tie on what the instruments see was read as improvement. A KNOWN INCAPACITY, named under his word (23:1xZ: anything falsified secured, or known prior than later) | S211 E2–E3 (e2_notes ## 5: the two C-288 bundles' markdown read side by side; e3_notes ## 3; `variants.py show <sha>`) | OPEN — the tie rule SECURED this sitting (variants.select: a candidate equal on every measured number never displaces the incumbent; it is listed under `tied` with its note, converted_at no longer breaks a tie; variants_selftest's negative control is the old rank tuple choosing the newest). The STRUCTURE MEASURE OWED, report-only beside the others: a heading census per page (Marker's SectionHeader count and level distribution against the original's), a bold/emphasis census, the margin-number join count, a column-header fusion check — each None where it cannot read, never 0. Until it exists a variant equal on every number stays beside its original, pending review, and the whitelist keeps the incumbent | `windows-converter/variants.py` (`select`: the tie rule; `variants_selftest.py`); the structure measure — nowhere yet |
| SYM-162 | A House of Commons bill converted on the TEXT-LAYER path (Bill C-30 ~160c under `offpage-clip`: 40 of 42 pages on the layer, survival 0.9467 against the OCR original's 0.9193) reads WORSE by the inventions measure — 167 invented / 574 lost against 27 / 479 — and the words are visibly broken in the markdown: `provi-** 10 **sions`, `modi-** 40 **fiée`, `pen` … `sions`, `com` … `plaints` (fragment 65, garble 102 by class); the registry refuses the variant. On C-288 ~160b the same numbers sit glued to sentence ends | The bill prints a bold LINE NUMBER in the margin every fifth line. On the layer path pdftext returns those numbers as spans on the same baseline as the text line; Marker's line merge takes them into the text flow, so a number lands BETWEEN the halves of a hyphenated word (`provi-` · `10` · `sions`) and the dehyphenation cannot join what a bold token separates — two fragments where the witness (pymupdf, `pen-\nsions`) holds one word. On the OCR path (the original) surya reads the columns as blocks and the numbers stay apart (joined 9 / fragment 3 / garble 14). A REAL loss of the fixed variant, named by the secured measure: the hyphen-join exemption fires 0 times here by design (Marker's word is not whole) | S211 E3 (e3_notes ## 5: `reaudit_convert.py` dry on ~160c and the original — survival re-measures equal, hyphen-joined 0 on both; the marker.txt read against pymupdf's p.28; CORRECTIONS row 10: the cause first attributed to the join artifact without reading, corrected) | OPEN — the registry keeps the OCR original selected (~160c neither promoted nor, until the shelf carries the honest rests, refused; then refused on words_lost_excl_joined 574 > 479 for the right reason). The lever that would fix it is OWED: the margin line-number spans kept out of the text lines (a rule on pdftext's spans — a bold integer alone in the margin band left of the column — dropped or parked as their own block before Marker's line merge; proved on a copy of C-30 and C-288 against the witness's whole words before it is proposed). Until then a bill's layer-path variant is not whitelisted | `windows-converter/fidelity_audit.py` (audit_inventions: classes fragment / garble; invented_hyphen_joined 0 says the split is not a join) · the lever — nowhere yet |
| SYM-163 | A variant re-run under a convert-stage lever reads BETTER on every convert measure than its original — McGill-1 ~148 under `ligature-repair`: 660 dropped-letter words restored (`frst` → `first` ×35, `efcient` → `efficient` ×34), convert survival 0.8496 against 0.8169, the same pages OCR'd — and lands with verdict FAIL where the original read flag; the registry keeps the original and the reader never sees the repair. The job's errors carry three ANALYST-RUN lines of exactly 24 words each; the SAME lottery with the sign reversed (S211 E5, TD Q3 ~r2 read live): a PLAIN re-send with no lever, whose every convert-stage number ties with the held original (survival 0.9147, inventions 5, lost 464, missing 24, rows 3, columns 18 on both — the same Marker body), was WHITELISTED over it because the analyst's own sampled rewrite differed (survival 0.9994 against 1.0) and flipped the verdict fail → flag at the analyst phase: the held verdict was the analyst's sample, not the document, and the choice between two identical bodies was a coin | The verdict's only two roads to fail are degeneration and the ANALYST near-exact gate (a run ≥ ANALYST_RUN_WORDS or survival below ANALYST_DOC_FAIL). The analyst is the local model rewriting the Marker body chunk by chunk; its output is sampled, so two runs over near-identical bodies differ — the original's pass produced 0 runs, the variant's three (each a window straddling a page anchor: `… 31 41page1723 offer deterministic …`), 5 of 92 chunks rejected (fence 3, survival 1, think_leak 1). The verdict compares a convert-stage fix through an analyst-stage lottery — S14 CONFOUNDED-COMPARISON: the difference measured is not the lever's. Read from the two manifests and the job's errors, not inferred | S211 E3 (e3_notes ## 8: the two manifests' fidelity blocks and analyst records, the job's ANALYST-RUN excerpts; variants_selftest case 23 — a candidate failed at the analyst phase with a better convert survival is NAMED, one failed at the convert phase is not) | OPEN — the registry now NAMES the case under `analyst_confounded` (the incumbent stays; the note says the convert fix worked and the analyst's own pass failed it) so the loss is never silent. OWED: (1) the analyst re-run on the SAME Marker body as a station of its own (the chunk journal makes it resumable) so a convert-stage variant is judged on its convert measures plus a fresh analyst pass, not a lottery; (2) the near-exact window's page-anchor tokens excluded from the run count (the three runs were exactly the threshold, each at an anchor) — proved on a copy against McGill-1's two bundles first; (3) his signature on ANALYST_RUN_WORDS if it moves (a threshold). Until then a variant that fails at the analyst phase is re-sent once before it is called worse AMENDED S211 E5 (this commit): the win case is named too — `analyst_decided` holds the SELECTED candidate when the analyst phase alone decided it (every convert-stage number tied, `_convert_stage_ties`: the error fields with their honest rests preferred, a None skipped and never read as 0, and degeneration), and the reason carries 'THE ANALYST PHASE DECIDED THIS: …'. The SELECTION is unchanged — a better verdict is Rab's signed criterion — so this is a naming fix, not a threshold: a reader can no longer read a lottery as a convert-stage win. variants_selftest 25 → 27/27 (the case; the two negative controls: a moved convert-stage number is the lever's win, a convert-phase verdict win is not named). The analyst re-run station is still the act owed | `windows-converter/variants.py` (`select`: `analyst_confounded`; `variants_selftest.py` case 23) · the analyst re-run station — nowhere yet · `analyst_decided` + `_convert_stage_ties` (`variants_selftest.py` cases 26–27) |
| SYM-164 | A capital or ratio table whose row labels (Credit / Market / Operational) AND figures are real extractable text in the source ships as ONE IMAGE with no numbers — the reader gets a picture where the source has a labelled, machine-readable table (BMO AR p.65: 10 labelled figures; RBC AR p.122: the capital table, 14 labelled figures — Credit 6 · Operational 5 · Market 2) | Marker's layout model boxes the whole labelled region as one Figure/Picture block and rasterizes it; everything inside the box is dropped from the text. Read on both pages with pymupdf: the labels and the figures are text on the page, the block on that page is a single Figure whose bbox covers them | S211 E3 (2026-09-22; the error accounting's mechanism-reading fleet — six Sonnet readers at the worst pages, an adversarial verifier each; accounting/lanes/mech-*.json, verify/mech-*.json); e3_notes ## 6 (RBC p.122, the row-label gate) | OPEN — NAMED by the numbers measure since 01f3fd5 (missing_in_figures_labelled: a figure inside a Figure box on a labelled band stays under missing). The fix owed: a lever that RESCUES the text inside a Figure box whose bands carry labels and figures — the layer's word bands re-read as a table (rows by band, cells by x-gap) and shipped beside the picture; proved on a copy of BMO AR and RBC AR against the layer before it is proposed | `windows-converter/fidelity_audit.py` (audit_numbers: missing_in_figures_labelled) · the rescue lever — nowhere yet |
| SYM-165 | A financial table Marker DID render ships with its rows wrong in one of several shapes: a wide two-year row split into two rows at the year boundary, the second unlabelled (BMO Q3 p.60); a row's label and four segment values pulled out of their row and merged into a garbled trailing row with another row's figures (BMO Q3 p.67); multi-word column headers cut MID-WORD across two header cells at a dollar-sign column boundary — Tra / ansfer, Rea / alized (CIBC AR p.143); two adjacent rows interleaved into one row, each cell two stacked numbers, a thousands comma read as a decimal point (CIBC AR p.162); a full-width note line cut by the data rows' column grid into scattered fragments (Finance SEU p.95); a table continued onto the next page cut in two with the straddling row's back half orphaned as a stray paragraph (UofT p.343); rotated row headers broken into reversed fragments (McGill Subcarrier p.117); a corrupted ECHO of each row emitted after the real one (Desjardins AR p.249); a ranked list rendered as a table with the whole list accumulating in each row's first cell (AI Index p.168); a tenth shape (S211 E5, read on RBC AR p.147 and p.150, TD AR p.49 and p.123 under the cell-level witness): the WHOLE table swallowed by its first cell — 30 to 39 rows' labels and figures in row 0's first cell, the 39 to 47 rendered rows after it empty | The table pass — table_rec's grid, assign_text_to_cells, split_combined_rows — assigns the layer's text lines to cells by a fixed column grid and by row bands: a line that does not fit the grid (a note, a wrapped header, a rotated label, a row wider than its neighbours, a row straddling a page break) is cut, merged, moved or echoed. Every shape is present in blocks.json BEFORE the analyst (the convert stage's own); pdftext/pymupdf read every one of these rows whole and in order. SYM-154 named the drops, SYM-147 the moves; these are the other shapes of one mechanism | S211 E3 (2026-09-22; the error accounting's mechanism-reading fleet — six Sonnet readers at the worst pages, an adversarial verifier each; accounting/lanes/mech-*.json, verify/mech-*.json); the verifier's reading of BMO Q3 p.67 (the reader said dropped; the page shows merged) | OPEN — the cell-level WITNESS is the act owed (SYM-154's, generalized): per Table/TableGroup block, the layer's row bands against the rendered rows (count, and each band's label against a row's first cell), a <br>-joined label cell as the signature of a merge, a row near-identical to its predecessor as an echo, a header cell whose text is a word's tail; report-only first, on copies of the nine pages named here, before any model or Marker pass is touched; the reference for the banks is their own supplementary spreadsheets AMENDED S211 E5 (this commit): the cell-level witness LANDED report-only as `fidelity.convert.tables_cells` (windows-converter/table_witness.py; table_witness_selftest 24/24) — per Table/TableGroup box the layer's words clustered into row bands and read against the rendered rows: matched / dropped / merged (two figured rows in one cell) / collapsed (the table in one cell) / split / echo (a row the rendering repeats; the OCR echo with digits swapped, layer-anchored) / header_cut / label_joined, and UNREAD by name where it cannot read (rotated text, a box short of its table, no labelled figured band, the scan lane). Its first reads over four bank ARs: RBC AR ~155 collapses 40 tables (361 rows), TD AR 10 (102), Desjardins 1 (24); the fix on the table pass still owed | `windows-converter/table_shape.py` (populations only today) · the cell-level witness — nowhere yet |
| SYM-166 | A long run of consecutive OCR'd pages that all repeat the same running head (a thesis's 16 spec pages) carries NO page anchors of its own — a reader cannot navigate to any page inside the run, and every one of those pages' losses lands under one earlier anchor (McGill Subcarrier 3c177d5fb368f82c: anchors 0-based 130–145 absent; P12 171 of 188 pages anchored) | Anchor placement (SYM-140's fix) leads on the longest run of trusted once-only text keys; a run of pages whose only text is repeated boilerplate has no page-unique key anywhere, so every page in it collapses under the last anchor before the run. Read from the shipped markdown (no page-N-0 anchors in the run) and the manifest's pages_anchored | S211 E3 (2026-09-22; the error accounting's mechanism-reading fleet — six Sonnet readers at the worst pages, an adversarial verifier each; accounting/lanes/mech-*.json, verify/mech-*.json) | OPEN — the fix owed: where no unique key exists for a page, an anchor placed by page ORDER (the pages before and after are anchored; the run's pages are the ones between) — proved on a copy of the Subcarrier thesis; the guard exists (P12 pages_anchored, the manifest's `anchors`) and already reads the gap | `windows-converter/page_anchors.py` · P12 in `sourcing/postmortem.py` |
| SYM-167 | The inventions measure names a word as lost and two as invented on a page where the shipped markdown carries the word exactly as printed — a genuinely hyphenated compound wrapped at its own hyphen (the PBO's Robert-Ouimet; the MIT CIO report's automation-vulnerable) — and the worst pages' specimens name words the source spells correctly, split only by its own soft hyphens (Wiener's Cybernetics) | The witness's dehyphenation deletes EVERY line-end hyphen, so a permanent compound hyphen at a wrap fuses the two halves into one witness token Marker — right to keep the hyphen — never has: one lost, two invented. And the worst[] specimens were built from the RAW lost/invented lists before the hyphen-join exemption ran (the exemption itself matches U+00AD), so a page's specimens could name the layer's own line-wrap. Both the instrument's | S211 E3 (2026-09-22; the error accounting's mechanism-reading fleet — six Sonnet readers at the worst pages, an adversarial verifier each; accounting/lanes/mech-*.json, verify/mech-*.json) | FIXED S211 E3 (this commit): a lost witness word equal to two ADJACENT Marker words counts under lost_compound_hyphen and its halves under invented_compound_hyphen — out of the honest rests (lost_total_excl_joined, invented_total_excl_joined), never out of the raw totals; worst[] specimens and the per-page invented_net / lost_net are net of both exemptions. inventions_selftest 31 → 36/36 (cases 30–31; the negative control: the same two words with no hyphen count nothing) | `windows-converter/fidelity_audit.py` (audit_inventions) · `inventions_selftest.py` cases 30–31 |
| SYM-168 | A dot-leader table-of-contents page reads as an almost total omission — 216 of 220 words lost, the page flagged — though every title, section number and page number is present, in order, in the shipped markdown (Waterloo Kamalzadeh p.7); the PBO's largest OMISSION-RUN is its own contents page for the same reason | The survival witness keeps the source's literal dot-leader runs (`Introduction ........ 1`); Marker rightly renders the contents as a table with no dots, so every sliding word window that spans a dot-leader run can never match, and the whole page reads as omitted. The instrument's, on the PRIMARY measure | S211 E3 (2026-09-22; the error accounting's mechanism-reading fleet — six Sonnet readers at the worst pages, an adversarial verifier each; accounting/lanes/mech-*.json, verify/mech-*.json) | OPEN — the fix owed is on the survival witness (collapse a run of three or more periods before windowing, as the witness already strips other furniture): it moves doc_survival on every document that carries a contents page, so it is proved on copies over the whole shelf first (every survival before and after, every verdict that flips named) and ships on HIS WORD — a change to the primary number is not the lane's alone | `windows-converter/fidelity_audit.py` (prepare_witness) · the proof over the shelf — owed |
| SYM-169 | The numbers measure reports an EXTRA figure that is present the same number of times in the source and the shipped markdown (McGill-1 p.188: `VLSI 2023,` written by Marker as italics closing before the comma), and a figure inside its own Figure box is counted MISSING outside charts when a unit is glued to it on the page (Waterloo p.98: `1000nm`) | The measure stripped every html tag to a space, so an inline tag closing between a figure and its comma let the number regex take `2023` where the layer's own `2023,` refuses it; and the containment test keyed the page word verbatim (`1000nm`) against a digits-only missing token (`1000`), so the two never met and the band test read the word as a label | S211 E3 (2026-09-22; the error accounting's mechanism-reading fleet — six Sonnet readers at the worst pages, an adversarial verifier each; accounting/lanes/mech-*.json, verify/mech-*.json) | FIXED S211 E3 (this commit): inline TEXT tags (i, b, em, strong, span, a, u, code, mark — the name ended by a lookahead, so `<br>` is never a b) strip to nothing, every other tag to a space (a <sup> footnote mark never fuses onto its figure); the number inside a page word keys the missing set (_num_key) and a word that begins with a digit is a figure, never a label's word (_numeric_word, the three band loops). inventions_selftest cases 32–33 with their negative controls | `windows-converter/fidelity_audit.py` (audit_numbers, _num_key, _numeric_word) · `inventions_selftest.py` cases 32–33 |
| SYM-170 | A scan-lane document (its lane decision: the embedded OCR layer is untrusted) reads thousands of words lost and invented and hundreds of figures missing (Shannon & Weaver: lost 4,403 · invented 2,332 · missing 706; Stanford Research de61c326873be673), the registry summing them as errors and the accounting reading them as losses — while the measure's own meaning line says `disagreement, not invention` | On the scan lane the convert audit's witness IS the PDF's own embedded OCR layer, which the lane already judged untrustworthy; the inventions and numbers measures diff Marker's fresh OCR against it (kind `agreement`). The counts are two OCR passes disagreeing, not a measured loss — the label existed at the aggregate and nothing carried it into the registry's error sum or the tickets | S211 E3 (2026-09-22; the error accounting's mechanism-reading fleet — six Sonnet readers at the worst pages, an adversarial verifier each; accounting/lanes/mech-*.json, verify/mech-*.json) | FIXED S211 E3 (this commit) in the registry: variants.summarize reads the word and figure counts as None (UNREAD) when the block's kind is `agreement`, carries `witness_kind`, and _compare excludes them by name (variants_selftest 23 → 24/24; the negative control: kind `fidelity` keeps its numbers); tickets_auto files one known-incapacity ticket instead of loss tickets for such a bundle. OWED: the page-level worst[] carrying the kind so a reader of the manifest sees it there too | `windows-converter/variants.py` (summarize: witness_kind) · `variants_selftest.py` case 24 |
| SYM-171 | A page that is one or two full-page figures reads both figures as SILENT (source words inside the box the shipped markdown lacks) though the figures carry no text of their own — the only words inside the box are the page's running head (Waterloo Zahra 102e4a1ebfc813e0 p.182) | The layout model drew the Figure block's bbox up to the running head's own y-band, so the head's words were never carved into their own PageHeader block; the figures measure (figure_text.py) counts every layer word inside a Figure box as the figure's and has no test for page furniture. The instrument's | S211 E3 (2026-09-22; the error accounting's mechanism-reading fleet — six Sonnet readers at the worst pages, an adversarial verifier each; accounting/lanes/mech-*.json, verify/mech-*.json); Lane B's residue at 79382b7 (a furniture exemption needs a witness — a running head recurring across pages, or a margin-band position — never a guessed rule) | OPEN — the furniture witness owed (the same one the inventions measure owes for C-288's footer URL): a word run that recurs on 40 % of pages, or sits in the top/bottom margin band, is furniture, not a figure's word; proved on a copy of the Zahra thesis and C-288 before it is proposed | `windows-converter/figure_text.py` (figures_silent) · the furniture witness — nowhere yet |
| SYM-172 | A re-run of a bank's annual report under the overlap-gate pair (NBC AR ~155: OCR'd pages 14 → 1, invented 37 → 13, extra figures 20 → 0, missing 34 → 30, survival 0.9413 → 0.945) is REFUSED by the whitelist for columns lost 29 against 23 over equal populations — the whole difference one page, p.212, where the original's OCR rendering read 17 columns and the variant's clean layer rendering 11: the clean table (every figure whole and in its cell, `24,324 · 5,781 · 11,987`) lost more columns than the garbage one ('•', 'S', '_', '1.1.1.1111' as cells, `2.8 %` split across two, `5,781` read as `5,78`) | The tables witness (pymupdf's ruled-lines strategy) found p.212's table ruled into 20 sub-columns: the `%` and `$` signs sit in their own ruled gutters and carry text in every row, so SYM-145's empty-column rule kept them as columns. Marker's layer rendering merges `2.7 %` into one cell (11 columns); the OCR rendering's junk cells happened to fill 17 of the 20 slots and matched the ruling count better. The witness counted the gutter as a column and the measure rewarded garbage — S15, the instrument manufacturing the finding. Read from both bundles' blocks.json rows and the layer's own bands on p.212 | S211 E4 (e4_notes ## 1: the two renderings' rows 3–9 beside the layer's bands; the witness re-run under the rule: 43×10 for both) | FIXED S211 E4 (this commit): a column whose every non-empty cell is a UNIT SIGN (%, $, €, £, ¥, ¢, ×, bps, pts, pp, mm, bn, k) is the unit gutter of its neighbour, not a column; a dash is a value and stays. table_shape_selftest 11 → 13/13 (the gutter case; the negative control: the same gutter holding a figure in one row is a column). p.212 now reads 43×10 to the witness against Marker's 17 and 11 — both wider than the layer, both DISAGREE, no columns_lost taken from that page for either. The shelf's next re-audit pass carries the rule to every bundle; NBC AR ~155 is compared again on the honest numbers | `windows-converter/table_shape.py` (`_witness`, `_UNIT_ONLY`) · `table_shape_selftest.py` cases 12–13 |
| SYM-173 | More than half of every word a chart carries never reaches the shipped markdown. Across the clean-lane shelf the source layer holds 15,646 words inside Marker's own figure boxes and 7,287 of them ship — 46.6 % — and 386 of 1,134 figure boxes ship as an image carrying NO words at all. Read on three pages of the AI Index: p.321 the layer carries 674 words and Marker wrote 5 blocks holding 28 (one FigureGroup, a header, a footer); p.301 620 against 53; p.65 517 against 27 — each a full-page chart whose axis labels, tick values, series names and data labels are real text in the PDF. The same shape on every bank annual report (RBC 45 silent boxes, Desjardins 43, Scotiabank 39, BMO 33) and on the Bank of Canada's Monetary Policy Report, whose invented and missing counts are both 0 | Marker's layout model classes the whole chart region as Figure / FigureGroup / Picture, and the pipeline ships a picture: the region is rasterized to assets/ and the words inside its bbox are never emitted. Nothing is broken — it is the box's SEMANTICS: a figure is an image, so its text is image. The words exist in the layer the whole time (figure_text.py measures exactly this: layer_words_in_figures against marker_words_for_figures), which is why survival counts them lost and why no converter lever recovers them — the fix is not a setting, it is emitting the box's own words beside the image. Read from the bundles' blocks.json against the PDF layer with pymupdf | S211 E5 (his Desk word 15:48Z, 'how are we losing so much fidelity'; the attribution fleet's six lanes named it on five documents independently, 5 of 31 new-mechanism attributions; the shelf totals and the three AI Index pages re-read by hand before this row was written) | OPEN — the measure exists and is honest (figures_silent, layer_words_in_figures, marker_words_for_figures, REPORT-ONLY since S209 E13); the RESCUE is owed and is a build, not a lever: read the layer words inside a Figure/Picture bbox, cluster them into their own bands and cells, and ship them beside the image as structured caption text. Named in the reconciler's charter as the first arm operation; on the present shelf it would return 8,359 words in 386 boxes. Rab's call, and his to read before it ships | `windows-converter/figure_text.py` (the measure) · `fidelity.convert.figures` on every manifest · the reconciler's first operation (gatekept, outside the repos) |
| SYM-174 | A document that converted completely is filed as a conversion failure, and its source is moved to drop/failed where every later tool reads it UNREAD. Waterloo's Kamalzadeh thesis: intake done, convert done (147 pages), gate done (fidelity flag, survival 0.6951), assay done (analyst 17 min 25 s) — then the ship's tailscale dial answered 502 and the watcher logged FAILED … -> drop/failed/ (exit 1). PORTAL renders it CONVERT-FAILED with the solution line 'a timeout or a Marker crash'. Three ship-stage failures exist in the whole event history (2026-08-03, 09-20, 09-21), all the same 502 dial to a ThinkPad that was asleep or unreachable; the two recent ones are exactly the two shelf bundles whose source cannot be resolved for a re-audit. The PBO document of the same night was re-shipped 7 min 31 s later and shipped fine — the job record, sealed at the failure, still shows it failed | The line's exit code is ONE scalar for a multi-leg pipeline: convert_and_ship raises on the ship leg, the watcher treats any non-zero child exit as a conversion failure (watch_and_convert moves the source to FAILED_DIR), and PORTAL's engine renders that intake/failed event as CONVERT-FAILED with a convert-shaped remedy. Nothing distinguishes 'the document could not be converted' from 'the converted bundle could not be carried', and the job record is a sealed tail that a later successful re-ship never reaches. Read from watcher.log, events.jsonl (3 ship failures / 61 shipped), the job.json stations and the two files still sitting in drop/failed | S211 E5 (the attribution fleet, lanes L1 and L4, four tickets; its own verifier REFUTED the harsher half — the PBO bundle DID ship at 02:17:37Z — and that correction is carried into this row; the event counts re-read by hand) | OPEN — three parts owed, each cheap: (a) the watcher distinguishes a convert-stage failure from a post-convert transport failure and leaves a completed source in drop/done, recording SHIP-FAILED apart; (b) PORTAL stops naming a transport failure CONVERT-FAILED and stops advising a Marker remedy for it; (c) the re-audit's source resolver falls back to drop/failed under its own sha gate (SYM-150's hash check makes that safe), so a stranded document is re-measurable. Until (a), a re-ship is invisible to the job record it belongs to | `windows-converter/convert_and_ship.py` (the ship leg's raise) · `windows-converter/watch_and_convert.py` (the FAILED_DIR move) · the tenant's `app/portal_engine.py` (the CONVERT-FAILED rendering) · `sittings/S211/reaudit_convert.py` (find_pdf) |
## Adding a row

From your closeout's §15. Write the Symptom column **for someone who has the symptom and not the
cause** — what they'd see on screen or in a log, in their words, not the mechanism's name. If you
cannot describe it without naming the cause, you don't yet understand the failure well enough to
index it.
