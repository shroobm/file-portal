# BRIEF-S153-OPUS — the handoff (2026-09-15, S153; from the Fable lane, Claude Fable 5.1 ⟨claimed: Fable⟩)

**Parties.** Rab (shroobm), the principal — every decision below is his. Fable, the lane; Claude Fable 5.1, its occupant S141–S153. Codex, the peer lane, asleep on Rab's word since 2026-09-10 (door-brief pointers MSG-FAB-0095…0102 wait for it). **You**, Opus, whom Rab is bringing in: this brief is what you would need to run the eight open items persistently, written before you arrived, checked against the repos by verifier agents (E3 of `sessions/S153-desktop-2026-09-14.md` carries their findings). It is a record, not authority: nothing here signs anything.

**Read first, in this order.** (1) `~/.claude/CLAUDE.md` and the memory library's `MEMORY.md` (they load in every session; the MUSTER and the TIME-STATE clocks live there). (2) `sessions/S152-desktop-2026-09-14.md` §18 and §10, then `S151`'s, then `S150`'s. (3) `OPEN-TASKS.md` — the register is the roll call; read it before asking what is next. (4) The private library's `agent-scripts/developing/handoff/README.md` — the sitting's sequence with every tool named.

## 0. The state at handoff (Observed 2026-09-15 01:3xZ)

- S152 closed at `8b2b7b5` (row `ece0293`, CI green run 34897781810; post-close `d68f716` and errata `7369a17`, CI green). S153 open for this brief only (pin `8b2b7b5`). Both trees clean but for S153's own files; the session marker `coordination/private/session.current` = S153 until this sitting's post-close removes it.
- The pipeline untouched by S150–S153: the table-geometry lever `ANALYST_TABLES = False` ([analyst.py:135](../windows-converter/analyst.py)) and the shipped acceptor set `FULL` unchanged. Nothing shipped; no bundle moved; the vault at `f489189`.
- The autonomy experiment stands (S146 §1, Desk e34253e4 / 1f355f41 / bdc9415c): the Fable occupant signs its own runs until Rab says the experiment has stopped. Whether that grant extends to you is his word, not mine.
- The Desk (private `relay-desk`, Tailscale `100.108.102.101:7150`) is where Rab posts; a Desk post is answered on the Desk only. My session watched it with a Monitor; start your own.
- The card (the GPU): no model of mine ran S151–S153; `.gpu-lock` absent; the seat Active (his). The GPU law is his: `.gpu-lock`, `nvidia-smi`, `query user` before any GPU work; one GPU process; never chain GPU work with `;`; the evening card is his — launch only with the card idle and the seat empty, or on his explicit word.

## 1. The eight items that are Rab's, and what a persistent run of each needs

Each: what it is · its state · what it needs from Rab · the steps · the measure · where it lives. The measure everywhere is S151's ground truth (private `measurement/evidence/s151-e4-truth/truth/majority/`, 14 tables on 10 Valentine pages by a three-reader panel) and the scorer (`measurement/probes/table_truth_score.py`, 18 tripwires), run through `handoff/score_truth_after.py`.

**1.1 The lever — `table-geometry` into FULL.** The table layer (`windows-converter/table_geometry.py`: caption · rail · dots · fold, every repair held to `grid_invariant`) and the acceptor class `edit_whitelist.FULL_TABLES` run only in dry runs today. *Needs:* Rab's word ("table-geometry into FULL", or "leave it"). *Steps on his word:* set [analyst.py:135](../windows-converter/analyst.py) to `True` (the analyst's `process(tables=None)` reads it), run `table_geometry_selftest.py`, `analyst_selftest.py`, `edit_whitelist_selftest.py` (marker-env) and `uvx ruff@0.15.20 check .`, commit by pathspec with the record; the next book the watcher converts carries it (the watcher spawns `convert_and_ship.py` per book — no restart). Not retroactive. *Measure after the first shipped book:* the census (`table_census.py`) BEFORE/AFTER and the scorer on any table the panel truthed; the evidence is one book, both copies, so far. *Register:* `analyst/table-geometry-lever` (S150 E5).

**1.2 The clear for the un-split of the held Valentine copy `b6fbdd75f6242f53`.** S149 found the bench's own crop had split Exhibit 8.2 (continued) — three lines between the header and the delimiter since 08-06; the un-split moves each repair embed out of the table and re-anchors the record, one ledger event per move. *Needs:* Rab's "clear" (the condition: nobody has the bundle open at a bench). *Steps:* close any bench window on that bundle; from `prototypes/repair-bench/` run `C:/Users/Bndit/ml/marker-env/Scripts/python.exe bench.py b6fbdd75f6242f53 --unsplit-tables` (prints the moves as JSON and exits; no server, no token); he reopens the bench. *Measure:* the scorer's p.133 row for the held copy reads "table" instead of "NO TABLE"; the census's orphan runs 2 → 0. *Register:* `bench/repair-inside-a-table` (S149).

**1.3 The list-cell convention.** A cell that prints a bullet list (p.239's Exhibit 17.3; p.72's cells): the truth is one cell holding the list (the copy's shape) or one row per item (the page's layout)? The panel chose rows; the scorer then counts "merged" cells that are a convention. *Needs:* Rab's word only. Fable's recommendation: one cell. *Steps on his word:* one rule added to `measurement/truth/FORMAT.md`; the majority truths for p264-t1 (and the readers' files) re-cut to the rule; `score_truth_after.py` re-run so the merged count on p.239 reads 0 or names a real defect. *Register:* `truth/list-cell-convention` (S151 E4).

**1.4 The vision route.** Two calls no text rule can make: where a rotated rail's rows begin and end when the OCR left no blank row between labels (`analyst/rail-boundary-unread` — the lexicon route split COSTS · MGMT · VALUATION by the words, the residue is where words do not decide), and whether a quadrant like p.342's is a figure or a table (`census/figure-and-index-read-as-tables`, the figure half; the block record calls it `Table`). *Needs:* Rab's decision — a vision model on his card (the GPU law applies), or Sonnet readers on rendered pages as in S151 E4 (API cost, no card), or neither. *Steps on his word:* an episode per call, the page rendered by `measurement/probes/render_pages.py` (pypdfium2, scale 4; the PNGs stay out of the repos), the reading under the orchestration law (GROUND · deviation-is-the-report · tag every claim · a negative control · declare the residue), the answer admitted only through `grid_invariant`. *Measure:* the scorer's rails column (word / letters / wrong / absent) and the panel's figure/table call.

**1.5 The p.175 table lost to prose (`converter/lost-table-p175`).** Exhibit 12.7's coefficients table (PDF page 200) is not a table in the anchor copy — its numbers sit as prose lines (body lines 2442–2444 in the census's and the records' convention, which count from the end of the frontmatter; the file's own lines 2453 and 2455 — a verifier read the raw file and the two conventions differ by the eleven frontmatter lines), the label `index)` as `indev)`; Marker's block record has it as `/page/200/Table/3`. *Needs:* the card — a re-conversion of Valentine with Marker's `--html_tables_in_markdown` flag (the S149 §18 probe, never run) and a comparison against the truth. *Steps on his word, the seat empty:* the GPU law; convert to a scratch output, never the live bundle; `table_census.py` on the result; `score_truth_after.py` against the truth for p200-t3 and the other thirteen; the record. *Measure:* p200-t3 "not located" → located, and no other table worse. *Register:* the row above.

**1.6 D1 — `ship-with-losses-named` versus `audit-must-be-green` (S110-D1, `docs/50-ticket-board.md`, blocked-on-rab).** The north star for what the pipeline ships. Under losses-named, a book whose audit names a loss (the held Valentine: one real loop, `the purpose of` × 115 on printed p.117) ships with the loss recorded; under must-be-green it stays held until the loss is repaired. Every other audit signal only flags (docs/15 §12). *Needs:* Rab's word; it moves nothing until he gives it. *Steps on his word:* `gate.py resolve` with his decision in his terms; the lever `audit=enforce|report` (`convert_and_ship.audit_mode()`) follows his reading; the held bundles re-audited (`--reaudit`, J31) under it. *Measure:* the held count and the vault's tip.

**1.7 The experiment's end.** *Needs:* his sentence ("the experiment has stopped"). Until then the Fable occupant signs its own runs. Whether you inherit the grant is his word.

**1.8 The Damodaran bundle `0d68f0e02293970c` (4e).** S149 §10 read it as gone from `held/` with no pipeline event. **S153 E3 found it (Observed, a verifier lens's residue re-read by the lane): not gone — MISPLACED.** It sits nested inside another held bundle's folder, `C:/Users/Bndit/ml/library/held/c5afd9edcf620fc6--reshipped-20260905-211727/0d68f0e02293970c/`, intact — its markdown, its `.bench-bak`, its `assets/`, its manifest (converted 2026-08-01, lane clean, verdict fail); the nested folder's own mtime 2026-08-06, the parent folder's 2026-09-14 03:01Z (the time S149 saw on `held/`); `events.jsonl` carries no event for either bundle at that hour (the Damodaran's last events 2026-08-03: a ship failure, then held; the Beer bundle's a re-audit and re-ship on 2026-09-05). A folder moved into a sibling folder is an Explorer drag's shape, not the pipeline's. *Needs:* his yes or no — was the move his hand? — and, if it should go back, his word. *Steps on his word:* one reversible command, `mv` of the nested folder up one level to `held/0d68f0e02293970c`, nobody holding the bundle open; then the census of the held count (7 → 8) and the register row closed. If it was not his hand: an incident — the widget's log and the folder's history read before anything is written.

**Owed by the Fable lane, not by him:** a `# lever-waiver:` comment or a lever for `SEGMENT_MIN_SCORE` and `GAP_COST` in `table_geometry.py` (close.sh's LEVERS line at S151's close; docs/18 §2: a number that decides something is a lever).

## 2. The protocol as it is actually run (the persistent method, POLICY §9)

Rab's words: "protocol" alone = the persistent method — each category an episode with a partial closeout, receipts and a Desk line; "run persistent" = keep sitting; "pace yourself"; "record all receipts". The sequence, every tool named, is `handoff/README.md`. The spine:

1. **Open**: `open.sh` → the card; the record with §1 (his words verbatim, unedited afterwards), §2 the card verbatim, §3 the pin (the newest ledger row's closing SHA), §9 the plan — committed FIRST (the guard hooks refuse a tracked commit until `sessions/S<N>-*.md` exists for the marker's session; creation and commit are TWO calls); the tracker (`start_tracker_TEMPLATE.ps1`, label `S<N>-receipt`, detached); the smoke ALONE in the foreground (`agent-scripts/testing/library_smoke.py`, uv python, nothing else writing).
2. **An episode**: the build with its tripwire → ruff at CI's pin → the dry run on both copies (`table_dryrun.py --resolver none --chunks none` — no model; `--resolver local` is the card) → the census → the scorer → the record section (numbers read from files) → the register strike with the residue named → docs/15 addendum → the Desk line → the public commit by pathspec and the private evidence commit → the receipt (`close_ep.py <N> <K> <since> …`, `<since>` = the previous receipt's window END, verbatim — the receipts tile).
3. **The close**: gained / lost / missing; §7 §8 §10 §15 §18 (§15 probed at write time); the journal entry (`sessions/JOURNAL-autonomy-2026-09-13.md`, LF); the content commit; the four close files from the templates (the row has FIVE cells); `close_driver.sh <N> <pin> <content_sha> <since>`; the post-close in `README.md` §5 order; the marker removed LAST.
4. **After a compaction**: the plan file before, the section after — the record is the memory.

## 3. The standing constraints (his words, as they bind)

- Scripts are NEVER public: the private repo `C:/Users/Bndit/Projects/file-portal-private` (branch `workshop`); the public `C:/Users/Bndit/Projects/file-portal` (branch `feat/library-pipeline`). C-026: `prototypes/repair-bench/` is PUBLIC tracked code; only `moonlight-phone`, `proc-feed`, `relay-desk` are junctions into the private half. No secret, no token in either repo; the bench server's `--token` is never pasted.
- Fleets: lanes on Sonnet, at most five, one Fable-class verifier last; Opus only when he names it. Agents get NO internet — only material the coordinator fetched and checked. Every subagent gets the orchestration law (`docs/47`; the memory's `subagent-orchestration-law`).
- The record before any tracked write; never edit a file a running lane is reading; the shared checkout's guard denies a `$VAR` command head and any wrapper argument containing "git"; scripts by literal absolute path; an expected deny gets its own call; never a heredoc carrying backslashes (ERR-009); commit messages inline or `-F <file>`; commit by pathspec after `git add` of new files; never restart the live watcher from a shell.
- Repo markdown is CRLF and append-only (the journal is LF); `PYTHONIOENCODING=utf-8` on every Unicode print; the memory index `MEMORY.md` under 17,510 bytes (fold the previous two blocks before a new one); the memory library pushed after every lockstep; CI's ruff is pinned 0.15.20 and the desktop has none — `uvx ruff@0.15.20 check .` before every push; the close chain's row needs FIVE cells; Desk → Desk, chat → chat; Rab's own Gmail only for drafts to himself, never sent.
- The tag law (docs/21): Observed · Verified · Inferred · Intended · Unknown · Historical — a consequential act needs an Observed premise; sampling never promotes; a number is re-measured, never quoted; a failed probe never renders as a negative observation (UNREAD). Every measured number names numerator, denominator and conditions (docs/34).
- A guard born today gets its tripwire today (docs/32 §6). A rule is measured into shape on the book, never reasoned into it (S152 §8.2: the fold rule folded nine index entries at its first run; the index signature named 6 of 18 at its first cut — both kept).

## 4. The instruments and where they live

- Public `windows-converter/`: `table_geometry.py` (the reading half: `fence_mask`, `table_blocks`, `cells`, `health`, `read_table` → `TableReading` with `title_row`, `title_fragments`, `title_pieces`, `letter_column`, `wrapped_labels`, `index_like`; the repair half: `lexicon`, `letters_fit`, `best_word`, `lexicon_segments`, `words_from_stream`, `best_phrase`, `propose` → caption / rail / dots / fold, `apply_table`, `grid_invariant`, `apply_admitted`, `geometry_pass`) with `table_geometry_selftest.py` (119/119 at handoff); `edit_whitelist.py` (`FULL_TABLES`, the `table-geometry` rung; 53/53); `analyst.py` (`process(markdown, backend, program, tables=None, resolver=None)`, `_word_resolver`; 38/38); `convert_and_ship.py` (`marker_version_stamp`; 248/248); `fidelity_audit.py` (`degeneration(strip_table_rows=True)`, `compute_verdict`); `prompts/grid-word.txt`.
- Private `measurement/`: `probes/table_census.py` (docling-env), `probes/table_dryrun.py` (marker-env), `probes/render_pages.py`, `probes/table_truth_score.py` + selftest, `truth/FORMAT.md`; the evidence `evidence/s150-valentine-{before,after0,after}/`, `s151-valentine-after-{lexicon,titles}/`, `s151-e4-truth/` (the readers, the majority, the scores, the packets with the plant, the agreement), `s152-valentine-after-{fold-run1,fold,pieces,stacked}/`; the notes `ANALYST-AS-IT-STANDS-2026-09-14.md`, `READING-DAS2002-BATEMAN2008-2026-09-14.md`; `baseline/`.
- The library `agent-scripts/`: `auditing/` (script_journal, cost_receipt, scripts_used, code_journal, proc_receipt, baseline, ci_observe, ci_step_log, occupant, brief_check), `developing/` (close_chain.sh, close_parts.py, handoff/), `journal/` (the receipts, LEDGER), `testing/library_smoke.py`.
- The book: `C:/Users/Bndit/ml/library/anchor/Best Practices for Equity Research Analysts - James J Valentine (2011) (1)/` (the anchor copy + `blocks.json`), `C:/Users/Bndit/ml/library/held/b6fbdd75f6242f53/` (the held copy, the split in it), the PDF `C:/Users/Bndit/ml/library/drop/done/…Valentine (2011).pdf`. Read only, never written by a probe.

## 5. The lexicon (his words)

"protocol" = the persistent method · "run persistent" = keep sitting, episodes with partial closeouts · "the card" = the GPU · "the seat" = his desktop session (`query user`) · "his slot" = a decision only he signs (the lever, `FULL`, punctuation/case in the acceptor) · "the lever" = a constant that decides something (docs/18 §2) · "clear" = his go-ahead for the un-split · "the vision route" = reading the page image, his decision · "D1" = ship-with-losses-named vs audit-must-be-green · "the bench" = the Repair Bench prototype · "the Desk" = the relay-desk board · "papers under the door" = a door-brief pointer for a sleeping lane · "a cookie" = his mark of approval (the tally in MEMORY.md's header, nowhere else).

## 6. Navigation

`sessions/S150-desktop-2026-09-14.md` (the analyst learns tables) · `S151` (the tickets half; E4 the panel) · `S152` (the panel's findings turned into rules) · `S153` (this brief) · `docs/15-survival-audit.md` §12.4–§12.4.7, §9.3.1 · `docs/60-the-factory-that-audits-itself.md` · `sessions/JOURNAL-autonomy-2026-09-13.md` (entries 18–20) · `OPEN-TASKS.md` rows named above · `SYMPTOM-INDEX.md` (SYM-044 fixed S151; SYM-067 the table-loop family) · `coordination/relay.md` MSG-FAB-0100…0102 (0103, this brief's own pointer, is posted at S153's close — a verifier lens read the file before it existed and said so) · the memory library: `autonomy-experiment-2026-09-13`, `persistent-partial-closes`, `segment-analyst` (S151 and S152 paragraphs), `file-portal-private-workshop`, `subagent-orchestration-law`, `pace-and-known-errors`.

## 7. What this brief cannot see

It cannot see Rab's intent for you (which lane, which grant); it cannot see the ThinkPad (UNREAD at every S15x open); it was written by the lane that did the work, so its residue list is that lane's — the S152 §10 list stands; a reader who finds a path or a command here wrong should fix the brief and say so in the record, not work around it.

## Erratum (S155 E2, 2026-09-15, the Fable lane's occupant Claude Opus 5)

§0's GPU-law line and §1.5's steps say to take `.gpu-lock` for a probe on the card. Measured: `.gpu-lock` is a busy SIGNAL only the watcher's own convert may hold (SYM-032) — a probe's `.gpu-lock` was REAPED by the watcher as stale 14 s after it was written (`watcher.log` 2026-09-15 00:02:42 local). For a probe: read the lock (present = a conversion is running, do not start), `nvidia-smi` and `query user`, then ONE process; nothing can hold the signal for you. §1.5's probe was run on his word `move back and run`: `--html_tables_in_markdown` does not recover p.175's table (`OPEN-TASKS.md` `converter/lost-table-p175` carries the measure and the route).
