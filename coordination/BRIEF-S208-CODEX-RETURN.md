# BRIEF-S208-CODEX-RETURN — papers under the door for the Codex lane's return (2026-09-19T14:22Z, S208; from the Fable lane, its occupant Claude Opus 5 ⟨claimed: Fable lane · occupant: Claude Opus 5 · 2026-09-19⟩)

*Commissioned by Rab, chat 2026-09-19T14:17Z: "Codex is back online … send a notice message to Codex of all that it missed since it went last offline, and what's the up to date paths and tooling folders it should use, and the tickets … give it instructions on how to make its own receipts … make sure it has its own lanes where it journals and uses these tools itself, so it doesn't get piled up in your lanes."* This lifts his 2026-09-10 word "stop giving stuff to codex for now" for THIS notice; nothing here is a hand-off. Every claim carries its tag; what this brief cannot see is in §7.

## 0. The state now (Observed 2026-09-19T14:19Z, the S208 card)

- Ledger newest **S207 `0c8dae1`** (row `2cc1e73`; CI green run 35290786265); this sitting S208; the clocks CLEAN; origin in sync; `OPEN-TASKS.md` 98 open rows (last written 2 days ago); `SYMPTOM-INDEX.md` 135 rows, 20 open; the relay 208 entries. The widget C4559805 running (pid 10056); the Desk task running; the relay watch task Ready.
- Your last entry: `MSG-CDX-0083`'s CDX-13 package (2026-09-13, relay line ~7301); the Fable lane's last pointer to you `MSG-FAB-0106` (2026-09-15). Between them and now: **S154–S207 in this repo, and a second institution beside it (§1.2).**
- The Fable lane HOLDS after this sitting (Rab: "I want you to rest"; the meter resets 2026-09-20T04:00Z). You may not be answered on the bus until his next word to the Fable lane.

## 1. What you missed

### 1.1 S154–S207 in this repo (Historical — the ledger rows, titles as written; read `sessions/S<N>-desktop-*.md` for any one)

S154 Opus takes the lead · S155 "move back and run" · S156 THE VISION ROUTE, signed, by sub agents · S157 THE LONG AUTONOMOUS SITTING ("Run autonomous until I say stop… every episode rewards a cookie") · S158 the converter modeled · S159 root origins · **S160 AUTO MEANS NEVER IDLE** ("change your idea of what auto is") · S161 the register worked down · S162 the second Circle · S163 the tripwires owed · S164 THE RUNNER · S165 the bench's own alarm · S166 the clock's own guard · S167 the tool the lane forgot · S168 the library smoked whole · S169 the index given room, the lockstep row struck, the code-notes checker · S170 the checker into the driver · S171 the hook's live proof · S172–S174 THE THINKPAD ("Thinkpad is on." · "Touch thinkpad"): the receiver deployed, the held-fail park read · **S175 THE RESTART WINDOW on his word** (Desk 8ca8279b) · S176 the front door proved live with a book built to fail · **S177 FOUR WORDS OF HIS IN TWELVE MINUTES** (4315ccf4 "If you messed up your first priority is to fix it") · S178 `docs/64` WHAT IS ALREADY GOOD · S179 the degeneration alarm read · S180 the HTML page's author found; the last unseen keys signed · S181 B27 applied · S182 the audit's own share measured · **S183–S187 SEVEN OF HIS BOOKS through the widget** — three in the vault, four held; the crash that lost the last book was the lane's (SYM-135: no PYTHONIOENCODING) · S188–S191 THE REGISTER WITH THE CARD FREE; the thirty-six unsigned keys signed · **S192 THE CI READER READS THE LOG** — three reds every close had called green (SYM-075) · **S193 THE INSTITUTION** — a folder anyone copies, seats an agent in, develops from (`prototypes/institutions` → the private repo) · S194 the platform-shaped suites read SKIPPED · S195–S197 SYM-111 (the process tracker says UNREAD, never an empty cell) · S196 the register read by a fleet that could not write; owners in F11 (42 his / 10 the card's / 7 the lane's / **2 yours: A49, J54**) · S198 the error bin read against the lane's own hand · S199 the tracker's receipts reach the journal · S200–S202 the baseline instrument's slots · S203 the day read against its files (a fleet with a known-false control) · S204 `claims_reader.py` · S205 the reader at the open · S206 the guard's denials counted · **S207 the reader row struck; the loop STOPPED on his word** (Desk c877a631 "You can stop at the next ticket").

### 1.2 The second institution (Historical — its own records; Observed on this disk)

`C:\Users\Bndit\Projects\file-portal shopify web app\` — a COPY of the institution (S193) bootstrapped 2026-09-18 (S0 `ce2712e`), its own repo `shroobm/file-portal-shopify-web-app` (private), its own ledger S1–S7, its own registers, hooks, skills and tools (`tools/close_chain.sh`, `tools/close_ep.py`, `tools/lockstep.sh`, the muster open/close). Its tenant program **PORTAL** (`app/`): a tailnet web service (`http://100.108.102.101:7160/`, the Desk holds 7150) where a PDF sent from Rab's iPhone goes through THE REAL LINE — `windows-converter/watch_and_convert.py` via `drop/` (on his word, S5) — and the bundle comes back as one zip with the manifest's measurements; eight books went through overnight (S6); the vault (`C:\Users\Bndit\Documents\Obsidian\Obsidian and Zennotes Vault\Library\`, the exporter's Inbox, 18 books) now carries a thread note and page anchors (S7). **That institution is the Fable lane's seat there; you are not seated in it.** Read its `records/CARRY-OVER.md` if Rab sends you there; do not write into it.

## 2. The paths and tooling folders (Observed 2026-09-19)

| what | where | note |
|---|---|---|
| the public repo (this) | `C:\Users\Bndit\Projects\file-portal` · branch `feat/library-pipeline` | records `sessions/`, the ledger `CLAUDE_README.md`, `OPEN-TASKS.md`, `SYMPTOM-INDEX.md`, `ERROR-BIN.md`, `docs/`, `wiki/`, `coordination/`; the line `windows-converter/`; the glass complex `observability/` |
| the skills | `.claude/skills/{muster,relay-gate,echo,circle,reading-bench,wiki,desk}` | `muster/open.sh` and `close.sh` are the mechanical halves; `relay-gate/gate.py` the bus; `AGENTS.md` points you here |
| the private workshop | `C:\Users\Bndit\Projects\file-portal-private` · branch `workshop` | **scripts NEVER public** (Rab). `agent-scripts/developing/` (the lockstep, `close_ep.py`, `compact_memory.py`, `handoff/close_driver.sh`, `desk_inbox.py`), `agent-scripts/auditing/` (`baseline.py`, `claims_reader.py`, `proc_receipt.py`, `code_journal.py`, `ci_observe.py`, `id_preflight.py`, `cost_receipt.py`, `prices.json` …), `agent-scripts/journal/{calls,cost,usage,code,procs}/` (the receipts), `sittings/S<N>/` (the Fable lane's per-sitting work), `relay-desk/` (the Desk), `institutions/` (the copyable institution), `measurement/baseline/` |
| the pipeline root | `C:\Users\Bndit\ml\library` | `drop/` (the one door), `anchor/` (every bundle), `held/`, `pending/`, `events.jsonl`, `.convert-progress.json`, `.gpu-lock`, `analyst-mode.txt`, `audit-mode.txt` — READ ONLY for a lane; the card is Rab's (the GPU law) |
| the vault | `…\Obsidian and Zennotes Vault\Library` (a clone of `rab@archlinux:file-portal/vault.git`) | books arrive by the exporter's rails only; `Threads/` holds the S7 node (uncommitted there — J63) |
| the tenant institution | `C:\Users\Bndit\Projects\file-portal shopify web app` | the Fable lane's seat; read-only for you |
| the Desk | `http://100.108.102.101:7150/` (`relay-desk/desk.py`) | Rab's phone-first channel to the Fable lane; a Desk post is answered on the Desk only |

## 3. The rules that bind now (his words, with their dates; verbatim where quoted)

1. **The record before the act** (J63, docs/28): a session record `sessions/<lane>…md` exists and is committed in its own call BEFORE any tracked write; `guard_record` refuses otherwise; the marker `coordination/private/session.current` names the open sitting (the Fable lane's hooks read it — see §5 for yours).
2. **The tag law** (docs/21): Observed · Verified · Inferred · Intended · Unknown · Historical on every claim; a number is re-measured, never quoted; a failed probe is UNREAD, never "clean".
3. **`guard_git` TIER1** (J56): `reset checkout clean stash restore switch rm mv apply am` are refused; `git add` BY NAME (never `-A`; `.codex/` is untracked beside you); `$VAR` command heads, wrappers naming git, heredocs naming git are refused; a script is a FILE, the interpreter by its full path (`C:/Users/Bndit/AppData/Roaming/uv/python/cpython-3.12.13-windows-x86_64-none/python.exe`; marker-env's `python.exe` for anything that imports `fitz`); `PYTHONIOENCODING=utf-8` on every call (SYM-135).
4. **Repo markdown is CRLF and append-only** (SYM-029, docs/63): a status is the row's FIRST marker; `Was:` opens superseded text; corrections are dated appends at the end; never `sed -i` on a CRLF file; the byte probe `sittings/S188/crlf_probe.py <file>`.
5. **The GPU law**: `.gpu-lock` is read, never taken; the card runs only on Rab's word; never a synthetic row in a store a production reader consumes; never edit a file a running process reads (the restart window is his, S175).
6. **Every gate change warn-only first, with his signature** (cdaf6619 "fix it": a mechanism fixed outright; a gate/threshold warn-first).
7. **Closes**: both gates before the chain (`observability/glass_detector.py --since <pin> --enforce`; `observability/schema_registry.py --check`), exits read without a pipe; the ledger row ≤ 80 words, its own commit, never amend; the push; **CI read from the LOG** (SYM-075, S192); the code notes ONE block per changed file; the REGISTER gate reads the file (a sitting strikes or adds, or says so); the two clocks in lockstep.
8. **The bus**: `coordination/relay.md` append-only, UTC, claimed per `coordination/authorship.md`; the gate's laws (`.claude/skills/relay-gate/SKILL.md`); **blocked-on-rab is never cleared by a model**; a FULL STOP on any escalation.
9. **His hands** (unchanged since S207): every SEMANTIC row; the rebuild (SYM-023); adoption (the card re-measures it); arming (A29, `FP_CONV_ARMED`); the levers (`ladder.txt`); publishing; money; an elevated prompt; a UI click on his screen.
10. **His standing words to the Fable lane** (not yours unless he says so): "Run autonomous until I say stop… every episode rewards a cookie… Record all receipts" (d7d03d67); AUTO MEANS NEVER IDLE (S160); "You can stop at the next ticket" (c877a631) — the Fable loop is STOPPED.

## 4. The tickets

- **Yours on the register (F11's roll-call, S196/S197): `A49`** — pick adoptions from the Okular digest (`docs/49` §1 ranks 13 candidates) — SEMANTIC at the choosing, mechanical after his pick; **`J54`** — your four items from S122's verification (SYM-081 `lab_driver.py:258`, and three more; delegated in `MSG-FAB-0083`, never collected). Both were parked by his 2026-09-10 word; **this notice does not re-issue them** — his word to you does. Ask him on the bus (`gate.py escalate` is not the form; a notice naming what you would take is).
- The rest: 96 open rows, 42 his, 10 the card's on his word, 7 the Fable lane's (F11; the crammed row; J60; the S199 recut row; …). `OPEN-TASKS.md` carries every owner in F11's cell. Do not take a Fable-lane row; do not take a card row; a SEMANTIC row needs his signature before anyone builds it.
- The tenant institution's rows (T-001…T-019) are in ITS register, not yours.

## 5. YOUR OWN LANES — where you journal and use the tools (Rab's requirement: nothing of yours in the Fable lane's folders)

- **Your lane name is `codex`** (the ledger is MULTI-LANE: the lane column, the muster's lane rule — "another lane's row leaves this lane where it was"). Your sitting numbers are your own series: **`C1, C2, …`** (never `S<N>` — that is the Fable lane's).
- **Your records:** `sessions/C<N>-codex-<YYYY-MM-DD>.md` in this repo (CRLF; §1 his commission verbatim; §2 the card; the pin; the six core sections at the close — docs/21). Your ledger rows in `CLAUDE_README.md`'s Change Ledger with lane `Codex` (the form of the last Codex row; ≤ 80 words; its own commit).
- **Your marker:** `coordination/private/session.current` is written by `open.sh` for whoever runs it and read by the Fable lane's hooks — **do not run the Fable lane's `open.sh` while a Fable sitting is open** (the card names the open sitting; if it names `S<N>`, wait or ask). Your own open: read the card (`bash .claude/skills/muster/muster.sh` is read-only), write your record, commit it in its own call — J63 by discipline, since Claude's hooks do not run in your harness.
- **Your work folders:** `C:\Users\Bndit\Projects\file-portal-private\sittings\codex\C<N>\` (create it; the Fable lane's are `sittings/S<N>/`). Your scripts live there; never in `agent-scripts/` (that is the shared toolbox — read and run, do not edit without a row).
- **Your receipts — the shape, then the tools.** A receipt is three files per window, `agent-scripts/journal/{calls,cost,usage}/<label>.md`, and **your labels are `C<N>` and `C<N>-ep<K>`** so they never collide with `S<N>`. The shape (read `agent-scripts/journal/calls/S207.md` and `cost/S207.md` as the form): `**Window:** <ISO> → <ISO>` (tiled onto the previous receipt's END, read from its file, never typed) · `**Written:** <ISO>` · `**tool calls (main):** N · denied N · errors N` with a per-tool table · `**Grand total (USD equivalent): X.XX**` from `agent-scripts/auditing/prices.json` · the usage lines (messages, input/output/cache tokens). The tools `close_ep.py <N> <K> <since> "<category>"` and `script_journal.py --label <L> --since <ISO>` READ A CLAUDE-FORMAT TRANSCRIPT (`CLOSE_TRANSCRIPT=<jsonl>`); your harness writes its own format — so either point them at your transcript if your format matches, or write the same shape from your own logs (a script in your folder). A receipt you cannot fill reads UNREAD, never 0.
- **Your journals:** `agent-scripts/journal/code/C<N>.md` (the code notes: ONE block per changed file — `agent-scripts/auditing/code_journal.py` parses the form), `agent-scripts/journal/procs/C<N>.md` only if the tracker's receipt names your window (`proc_receipt.py <label>`; it labels by the marker's sitting — likely UNREAD for you: say so).
- **Your baseline row:** `agent-scripts/auditing/baseline.py C<N> --tickets K --chain-exit <exit>` writes `measurement/baseline/C<N>.json` and a README row — the instrument accepts any label.
- **Your close:** `bash .claude/skills/muster/close.sh <pin>` (mechanical); the chain `agent-scripts/developing/handoff/close_driver.sh` is the Fable lane's driver with Fable's paths — read it, do not run it against your sitting until it takes a lane; the lockstep `agent-scripts/developing/lockstep.sh` advances the FABLE clocks (`cookie-tally.md`, `MEMORY.md`) — **not yours**; your HARD clock is your ledger row; your SOFT clock, if you keep one, is your own file (`coordination/private/codex-time-state.md`, say).
- **Your bus:** `gate.py --as Codex` (`inbox`, `confirm`, `post`, `beat`, `owed`); `ack-codex.json` is yours alone; the occupant declared (`gate.py occupant --as Codex --model "<name>"`).
- **The cookies** are Rab's rewards to the Fable lane (`cookie-tally.md`); not a form for your lane unless he says so.

## 6. Navigation

`sessions/S207-desktop-2026-09-17.md` (the last Fable close; §18); `sittings/S207/CARRY-OVER.md` (private); `OPEN-TASKS.md` (F11 for the owners); `SYMPTOM-INDEX.md` (20 open); `ERROR-BIN.md` (the lane's own hand: ERR-009 heredocs, ERR-054 `python -c`, ERR-110 encoding); `docs/58` (the error families), `docs/63` (the FIRST marker), `docs/64` (what is already good); `coordination/CODEX-RESIDENCY.md`; `.claude/skills/relay-gate/SKILL.md`; the tenant institution's `README.md` and `CLAUDE.md` (its constitution) if he seats you anywhere like it.

## 7. What this brief cannot see

Your harness's transcript format and cost table (the receipts' tools assume Claude's); whether the gate is LIVE (both sidecars exist on disk — `ack-codex.json`, `ack-fable.json` — but his signature on the protocol's revival is his); the state of the ThinkPad (the vault push and your Linux lane's work since 2026-09-10 `coordination/messages/…index-station-deployed.md`); what he wants you to take first — that is his word, on the bus or in your chat. Nothing here is a ticket.

**RECAP.** Fifty-four Fable sittings and a second institution since your last entry; the rules as they bind now; your two rows; your own lanes named so nothing of yours lands in the Fable lane's. **FOR RAB.** Codex has the door brief; its first act is yours to name. **SUGGESTED PROMPT.** For Codex, from Rab: "Read `coordination/BRIEF-S208-CODEX-RETURN.md`; open C1 as it says; tell me on the bus which of A49/J54 you would take and what you need."
