# docs/63 — The second Circle: every FIXED row of the symptom index against the code at HEAD (S162, 2026-09-16)

*Written against commit **110dbc3** on `feat/library-pipeline` (the frozen-commit rule: the tree the lanes read; nothing of the lane's landed on it until they returned). Written by Fable (Claude Opus 5) as Phases 5–8 over four Sonnet lanes' Phase 4; the receipts are private (`sittings/S162/e1/`: the brief, the packets with plants, `results.{json,md}`, `adjudication.json`). Nothing run; the card untouched; no code changed by this Circle — its gate touched status cells, a document's pointers and one register row.*

## §0 Substrate (Phase 0)

The muster's open card at 18:48:20Z: clocks reconciled at d0034f3 (S161), the tree clean, 133 symptom rows with 29 open by the muster's counter, 96 register rows. The first Circle's record (`docs/62` §8) named this one's inheritance: "~90 further fixed/resolved rows never triaged" and "§4.2/§4.4/§4.6–§4.9's ~30 numbered cells not swept against docs/34". The day's own lesson shaped the question: four register rows (B32, SYM-112, F2, F9) had first sentences that outlived their fixes — nobody re-read them.

## §1 Intent (Phase 1) — the falsifiable questions

Assessment, not change. (1) For each of the 104 rows of `SYMPTOM-INDEX.md` whose status does not read open: is the fix the row names present in the code at HEAD, at or near the place the row names, and does a tripwire for it exist in a suite a close or CI runs? (2) For the numbered cells of `docs/61` §4.2, §4.4, §4.6–§4.9: does each name its numerator, denominator and conditions (docs/34), and is each tagged as what it is?

## §2 Reference (Phase 2) — what "good" was defined to mean, quoted

docs/21 §5: the symptom index is "the load-bearing artifact"; a row marked fixed is "a `Historical` claim about a guard". The muster skill: "If today's work depends on that guard, step on it — violate the property and watch the alarm fire. A guard nobody has watched fire is a proxy with a reputation (docs/32 §5)." docs/32 §6: "a guard born today gets its tripwire today … A new guard without a test that violates its property is a proxy with a birth certificate." docs/34: every measured number names its numerator, denominator and conditions; a ratio prints both sides; a duration nobody reported renders UNREAD. docs/21 §1's tag law. **A contradiction in the criteria, found while reading:** the index's own header says "Fixed rows stay — the recurrence is what you're guarding against" and the S157 form keeps a superseded cell verbatim after `Was:` — so a fixed row's LAST clause can lawfully read "this remains OPEN". Nothing said which end of the cell a reader trusts. (Resolved mechanically in §7.)

## §3 Observe (Phase 3) — anomalies logged before delegation, and the seeded suspicions

Observed at the pin: 104 not-open rows; their first markers by shape — `fixed` 21 · `fixed S…` 15 · `fixed SEE…` 12 · `fixed by rule` 8 · `FIXED` 7 · `FIXED SEE…` 6 · `Historical` 5 · the rest single. Seeded, verdict-free: the inverse of the day's lesson — a fix may not outlive its row; line numbers cited at S7x–S12x have had ten sittings to drift; the rows fixed "by rule" have no code to test; the rows fixed in the private repo cannot be read from the public ground. The cells: §4.2's "Beer" sits beside §4.1's "Diagnosing" for one book; §4.7 still says "pre-acceptor" after §10.5 retracted it.

## §4 Decompose (Phase 4) — the lanes

Four Sonnet lanes, read-only, no internet, the brief (private `e1_circle2_brief.md`) checked by `fleet_law_check.py --brief` (5 of 5) and committed before the launch (05c13a4). **R1 / R2 / R3** — the 104 rows quoted whole in the file's order (36 · 36 · 35 entries), one planted row each written to look real, its fix at a file absent from the tree (`SYM-021b` → `gpu_lock.py`; `SYM-136` → a `pages_meta` fallback; `SYM-140` → `table_layer.py`). **C3** — eleven seeded cells of `docs/61` §4 (the plant `C3-11`: a Diagnosing sentence that lives in docs/60 §3, not §4.8) and the open inventory of the six sub-sections. Workflow `wf_dc2b3d8a-3f2`, 14 min 49 s, 264 tool uses, 702,986 lane tokens.

## §5 Reconcile (Phase 5)

**The law after the return (`fleet_law_check.py --results`): 21 checks · 2 red.** Every plant caught by id (4 of 4: R1 `DEAD/VIOLATION — not in the file`, R2 `DEAD`, R3 `DEAD`, C3 `VIOLATION`); every residue declared (2,174–2,347 characters); every entry tagged. The two reds are NULLS: **R1 returned 33 of 36** (SYM-021, SYM-022, SYM-023 dropped; the "padded" three — SYM-037/038/039 — are the packet builder's own artefact: the ThinkPad-lane rows' first cell carries `*(was SYM-028 …)*` and the lane normalised the id) and **R2 returned 34 of 36** (SYM-073, SYM-074 dropped). The five dropped rows were checked by the lane itself (§6). No lane contradicted another on a shared row (none was shared). The lane's own re-check (`e2_adjudicate.py`: every backticked path and symbol of every non-WITHSTOOD row's cells tested at HEAD) confirmed each cite drift the lanes named and every "present" they claimed that it could reach.

## §6 Judge (Phase 6) — most-severe-first, in the Circle's vocabulary

1. **VIOLATION (C3-S1, severity 1; Observed by the lane, re-read by Fable):** `docs/61` names one anchor "Beer" (§4.2 row 1, §4.3, §4.4 row 1) and "Diagnosing" (§4.1, §10.2, §10.9), lists neither in §3.3's binding table, and cites Stafford Beer's *Brain of the Firm* in the same document — the document that exists to fix terminology carried an unbound word for one of its own nine books. Failure: a reader cross-checks §4.4's "Beer at batch 8" against §4.1's table, finds no Beer, and files it on the wrong book. **Gate: mechanical** — §10.12 binds it (§7).
2. **EROSION (C3-01, C3-07/C3-S2, C3-08; severity 2–3):** §4.2 cites SYM-132 for a figure that is S146 E8's (SYM-132 is the 9,515 MiB death); §4.7 rows 1–2 keep "pre-acceptor" after §10.5's retraction with no inline pointer; §4.7 row 2's 669 stands 100 lines above §8.2's 670. All three the append-only rule's known shape (the first Circle's "pointer gap"); each now has its line in §10.12.
3. **HONEST-BUT-CONFUSING (R2-S1 severity 3, R3-S1 severity 1 as the lane ranked it; C3-05; C3-S3):** SYM-045's and SYM-112's cells END with the superseded text's last words ("this remains OPEN"; "the two-line fix is owed") because the S157 form keeps the old cell after `Was:` — true data, the wrong end of the cell read as the status. §4.6's "10 real losses" beside §10.10's "five on the 4e" are two denominators never related on the page; §4.8's "survival 0.69–0.99" is the pre-§10.2 framing. **Gate: mechanical** — a "how to read a status cell" paragraph at the index's head, a reconciling clause in SYM-045, §10.12's items 4–5.
4. **EROSION — cite drift (R2: SYM-068, SYM-069, SYM-072; R3: SYM-108, SYM-131; the lane's check: SYM-047's `line.rs:72` → `:84`):** every fix present, every cited line moved (watcher.rs 184–205 → `stop()` at 268; main.rs 687–703 → 771; open.sh 371–375 → 477–493; `T2` → `B2`; `T12 a–h` → `T20 (a)–(h)`). **Gate: mechanical** — dated cite-drift notes inside the five cells.
5. **FRAGILE — a fix with no tripwire (15 rows):** nine name CODE at HEAD with no case that plants the defect back — SYM-006 (`_kill_tree`), 009 (`_enforce_hold`), 011 (the inotify route), 012 (`FORCE_DROP_OLD`), 023 (the `main` label guard), 030 (`GENERATED_MD`), 041 (`_with_batch`), 052 (the bench's `slice(0, 400)` removed), 125 (utf-8 logging); six are RULES with no code to test (SYM-004, 005, 018, 019, 021, 022 — docs/19's laws, the muster's standing orders). docs/32 §6 names the class. **Gate: mechanical, owed** — one register row for the nine (§7); the six rules are not owed.
6. **BLOCKED → WITHSTOOD by the lane (SYM-045, 054, 112, 117, 119, 127):** their fixes live in the private repo (`id_preflight.py` + its selftest; `fleet_proc_census.py`; `desk_boot.ps1`, fixed S160 E7; `memory_timestate.py`; `library_smoke.py` — itself the smoke; `journal_check.py` + its selftest 27/27) — outside the lanes' GROUND by the brief's own line, read by Fable, present. SYM-117's tripwire UNREAD (no selftest found beside `memory_timestate.py`).
7. **EROSION, the row's own words (SYM-086; SYM-036; SYM-028; SYM-038):** SYM-086's marker reads `Historical` and its body "OPEN (a harness default)" — a class the project cannot fix and says so; SYM-036 and SYM-038 are "fixed by rule" and the lane held them to the tripwire bar; SYM-028's muster.sh check is intact (`[3a]` at `muster.sh:177-189`), its birth in another repo. Left as written; named here.
8. **WITHSTOOD — 79 of the 104 rows** (R1 19; R2 25 + SYM-073/074 by the lane, both with their selftests; R3 27; the six BLOCKED re-read; the five rows the lanes dropped placed by the lane: SYM-021/022 rules, SYM-023 FRAGILE, SYM-073/074 WITHSTOOD): the fix present at or by the name the row gives, a tripwire in a running suite — among them the day's own three (SYM-055's four cases and two mutants, SYM-127's 27/27, SYM-058/060's projection cases) and S157's nine. Absence of findings on these is a finding: the index's fixes largely outlive their rows; what drifts is the CITE, not the fix.
9. **The Circle's own errors, first-class:** the packet builder carried the ThinkPad rows' `*(was …)*` suffix into their ids (three false "padded" reds); two lanes dropped five rows between them (re-checked by hand, none DEAD); the lane's adjudicator flagged C3's cell ids as "rows not in file" (they are cells) — instrument noise, read as such.

## §7 Gate (Phase 7)

**Mechanical, done this sitting (S162 E3):** `docs/61` §10.12 (the six items above, with §10.11's pointer); `SYMPTOM-INDEX.md` — the "How to read a status cell" paragraph in the header (the FIRST marker is the status; `Was:` opens the superseded text; dated clauses read in date order), cite-drift notes inside SYM-068/069/072/108/131, a reconciling clause in SYM-045; `OPEN-TASKS.md` — one named row `symptom-index/tripwires-owed-for-fixed-rows` for the nine code fixes without a tripwire (the register's net this sitting +1, justified: the Circle's one owed build). **Semantic, his:** nothing this Circle found needs a signature; the `docs/15` §12.5 escalation from the first Circle stands. **Evidence-insufficient:** SYM-117's tripwire (UNREAD — the private file has no selftest beside it; the next sitting looks).

## §8 Close (Phase 8) — the answers, and what the next Circle inherits

**(1)** The fixes outlive their rows: 79 of 104 WITHSTOOD (a fix present and a tripwire in a running suite; six of them read by the lane in the private repo), 15 FRAGILE (nine owed a tripwire, six rules), 10 EROSION (six cite drifts, four by the row's own wording), 0 DEAD among the real rows — the only DEAD verdicts were the four plants. What drifts is the line number, not the code. **(2)** The cells of `docs/61` §4 mostly withstand docs/34 (the lane inventoried 28; 14 of its 21 judged cells withstood); what fails is a word (Beer) and the pointer gap the first Circle named. **What the next Circle inherits:** the nine tripwires (the register row; when built, the rows read WITHSTOOD); SYM-117's selftest; the 29 open rows (never a Circle's — they are open); the S157 `Was:` form's tail (now read by the header rule — a later Circle may ask whether the form should end with a terminator); §4.1/§4.3/§4.5's cells re-swept once the S159 corrections settle.

*Provenance: S162 E1–E3 (private `sittings/S162/e1/`; `e2_adjudicate.py`'s probes in `adjudication.json`). Written by Fable (Claude Opus 5); nothing run; the card untouched.*
