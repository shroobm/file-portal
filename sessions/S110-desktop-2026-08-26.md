# S110 — Desktop — 2026-08-26

⟨claimed: Codex lane · occupant: OpenAI Codex (GPT-5) · S110 · 2026-08-26⟩

## §1 Session Intent

Rab's commission, preserved as the two invoked skill names and left unedited after this opening
commit:

> `relay-gate` · `muster`

The repo contract makes `.claude/skills/` canonical; the supplied `.agents/` locations are the
stale-mirror hazard recorded in OPEN-TASKS B2.

## §2 Starting State — `open.sh` card, verbatim

```
════════ MUSTER · OPEN · 2026-08-26T04:32:04Z ════════
[0] IDENTITY
    ledger newest    S109  ab544d1
    this session     S110  ·  desktop  ·  2026-08-26
    advisory         S110 is named in the INHERITED tree (ab544d1) — read each, do not blanket-correct:
                     coordination/BRIEF-S109.md 
[1] GROUND
    verifier         8C8A748E  (repo == ~/.claude)
──────── MUSTER · 2026-08-26T04:32:04Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 82 (tally == hook)
[3a] LEDGER ORDER  ✓ 92 Desktop-lane rows ascend (S16 → S109)
[3b] LEDGER PARSE  ✓ 92/117 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 4 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79
[3] HARD clock ... ✓ ab544d1 (S109) == ledger ab544d1, ancestor of HEAD
    WORKING TREE . 0 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           UNREAD — fetch failed; NOT a statement that origin matches local
[2] LIVE
    pipeline         held 5 · anchor 24 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=16
    gpu-lock         absent
    events           147 line(s)
    open-tasks       94 item(s) · last written 23 hours ago
    symptoms         56 row(s), 4 open · last written 31 minutes ago
    relay            59 entries · run `gate.py status` for the board
    processes        UNREAD — process-table probe failed (rc=1); NOT a statement that anything is down
    installed exe    4DCB73E2  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip ?
    thinkpad         UNREAD — host did not answer; NOT a statement that it is down or clean
[3] PIN
    --since          ab544d1
    closeout         sessions/S110-desktop-2026-08-26.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

Pinned close reference: **`ab544d1`**.

## §3 Inherited — all `Historical`

- S109 closed 2026-08-25 at `ab544d1`; the separate ledger-row commit is `a1c40d2`.
- S109 §21 said to read `coordination/BRIEF-S109.md` §6 first; its nine seams remain reported
  evidence until independently re-observed.
- S109 §20's relay adoption, digest-test gap, and S108 sign sheet remained open at that close.
- The newest relay pointer, `MSG-FAB-0030`, asks only that Codex read the brief; it opens no ticket
  and asks Rab for nothing.

## §4 Promoted State and Registers

`Observed 2026-08-26` from the card and the canonical gate board:

- HARD and SOFT clocks reconcile; the worktree was clean at open.
- OPEN-TASKS has 94 items; SYMPTOM-INDEX has 56 rows with 4 open; relay has 59 entries.
- Relay board: both lanes `idle`; no escalation and no FULL STOP. Fable's beat and Codex's stored
  beat are stale; that is a reading, not a verdict about either occupant.
- Codex inbox: two non-ticket notices, `MSG-FAB-0028` and `MSG-FAB-0029`. The later
  `MSG-FAB-0030` pointer explicitly needs no ACK.
- `gate.py owed --as Codex --enforce` reports 8 D2 records OWED. That is record debt: it does not
  prove the historical work was not performed.

Rows intersecting today's intent: OPEN-TASKS B2 and J9/J15/J17/J18; SYM-045, SYM-046, SYM-054,
and SYM-055. No fix is commissioned in this session.

Still `UNREAD`: origin parity, process-table state, ThinkPad state, vault tip, and the nine
single-lane seams in BRIEF-S109 §6 unless separately promoted.

## §5 Standing Orders in Force

- Verify current source before instructing Rab; read a page before editing it.
- Adoption remains Rab's hand.
- One process on the GPU card; inspect `.gpu-lock` and `nvidia-smi` before GPU work.
- Kill process trees, never single PIDs; never chain GPU work with `;`.
- The markdown link fence is mandatory; repo markdown CRLF is handled without lossy rewrites.
- A live status sentence carries its probe; every measured number follows docs/34.
- A guard born today gets a negative tripwire today.
- Cookies are logged before other work by the soft-clock writer.
- Relay is carried at open and answered before close, with UTC and claimed authorship.
- Deep or ambiguous commissions trigger `/echo` before interpretation.

## §7 Work Performed

Opening record only. Relay acknowledgments follow after this file's opening commit.

## §8 Findings and Evidence

Pending.

## §10 Known Failures / Open

- `origin`, process-table state, ThinkPad state, and vault tip are `UNREAD`.
- SYM-055's independent digest assertion remains open; a green relay suite alone does not prove
  its algorithm is SHA-256.
- The D2 `owed` board reports eight undisclosed Codex outcomes.

## §15 Verification

Pending close gates against `ab544d1`.

## §17 Current State

Opening state only; no relay sidecar has yet been changed by S110.

## §18 Next Entry Point

Verify the live gate's digest implementation independently, then acknowledge the two waiting
non-ticket notices with faithful restatements. Do not take or invent a ticket.

---

# S110 — THE FABLE LANE'S HALF

⟨claimed: Fable lane · occupant: Claude Opus 5 · S110 · 2026-08-26⟩

*Rab: "ADD INTO S110." S110 is therefore a SHARED session, as S108 was (`⟨Fable⟩+⟨Codex⟩`). Codex
owns §1–§18 above and I have touched none of them. Everything below §19 is mine. Single writer per
section, append-only — the discipline this session spent the day testing.*

## §19 How this lane got here

S109 closed properly at `ab544d1` (row `a1c40d2`, CI `completed success` observed by hand). What
follows is post-close work that continued past the close and into Codex's S110 open. It moves
NEITHER clock; the ledger still names `ab544d1`.

## §20 What this lane did

**The Dock, mapped to source.** A 6-agent fleet (`wf_9f6bf179-e79`, 289 tool calls, 18 min) traced
every element of the Dock to its producer and renderer. The tiles are config data
(`config.rs:9-14` → `list_portals` → `main.js:56-65`), not hardcoded. There is exactly ONE drop
handler for the whole webview, hit-testing pointer against tile rects — and `main.js:70-71` divides
by `devicePixelRatio` because the drop payload is in PHYSICAL pixels while `getBoundingClientRect()`
is in CSS pixels. Without that division any display scaling ≠ 100 % hits the wrong tile.

**The survival number — and my own `docs/34` violation.** The Dock reads
`manifest.json → fidelity.convert.doc_survival`, hardcoded to the convert phase
(`assay.rs:127`, `:159`). It is NOT stale: the manifest is phase-keyed and
`convert_and_ship.py:66-69` merges additively, so both numbers coexist permanently.

**I subtracted two numbers with different denominators and called it a recovery.** I reported "the
analyst recovered 8.70 points of survival — it worked." Wrong. `convert 0.8582` measures source→Marker
with a FUZZY window (0.76–0.96 is ordinary reflow, flag at 0.97). `analyst 0.9452` measures
Marker→analyst with NEAR-EXACT containment (fail at 0.995). **On its own gate 0.9452 is a
catastrophic fail**, and the manifest records a 492-word missing run against a threshold of 25. The
analyst did not recover anything; it deleted a passage. Corrected in J14, BRIEF §2.7 and the Gmail
draft.

**Four genuine Dock defects**, SYM-043 class: the lane is unlabeled (`main.js:940` renders bare
`survival 0.858`) · **the badge and the number come from different lanes** (`compute_verdict`
tests the ANALYST block FIRST at `fidelity_audit.py:429-432` and returns on it, so the red `fail`
is analyst-driven while the number beside it is convert) · `assay.rs:165` ships `fid["analyst"]`
and `main.js` never reads it — **dead payload, `widget:age_s`'s family** · and it bites exactly the
decision the card exists for, since the card offers both re-convert and re-analyze on one-lane
evidence.

**`AGENTS.md` fixed** (`70ce4af`). It prescribed three relay-gate commands beginning with bare
`python` — the Microsoft Store shim. The file written FOR the Codex lane handed it the command that
cannot run. Both interpreters now named with full paths in a section that LEADS the file; zero
bare-`python` commands remain. Codex hit bash, then Python, ten minutes apart, and tagged both
`UNREAD` rather than calling a red muster. **The readings were correct; the document was at fault.**

**`dumps/`** — a home for heavy things (`126d959`, `bd2c993`). Ledger + README + `dump.sh` +
`.gitattributes` TRACKED; the bytes gitignored. Tracked bytes would break `close.sh`'s
`0 uncommitted` forever; ignored-only would make a delete unrecoverable. So the LEDGER is tracked
and git history preserves the RECORD of every dump even after the bytes are gone. `D0001` is real:
the SYM-056 chunk journal `analyst.py:393` deleted.

**Relay.** Beat refreshed with a probe (the tool refuses a `--verified` without one). `MSG-FAB-0031`
posted; lane `blocked-on-ack`. Codex's `MSG-CDX-0010` confirms it read `MSG-FAB-0030` AND
`BRIEF-S109.md` — **the door brief was picked up.**

## §21 The three mechanism experiments

**Bakeoff** (`wf_00aae4a2-2e3`, 6 agents racing, 8 messages each). Measured on disk, not from
agent self-reports:

| mechanism | questions retrievable | answers retrievable |
|---|---|---|
| one box, delete-on-read | **0/8** | **0/8** |
| two files, single-writer, append-only | 8/8 | 8/8 |
| directory queue, atomic rename (maildir) | 8/8 | 8/8 |

The single box ended at **zero bytes**. Two failure modes, and the second was not predicted: the
truncate is UNCONDITIONAL (`TRUNCATE iter1 nothing-consumed` — it destroys unread content), and
**the reply channel eats itself** (`consumed-Q05-and-destroyed-A03`). Three answers written, zero
survived. **Delivery 3/8; useful round-trips 0/8.** A single slot cannot hold a question and its
answer at once.

**Daisy chain** (`wf_425a081c-ed1`, 4 links in series, maildir transport). Circuit closed in 2m16s.
The digest chain is unbroken — each link's in-hash equals the previous link's out-hash:
`d1f6ea7c → 1a1e733a → a843dd22`, `links_seen 4`. Zero torn `.tmp`, zero unclaimed. **4/4.**

## §22 What went wrong, this lane's half

1. **I subtracted across denominators** (§20) — the exact violation `docs/34` exists to prevent.
2. **I misattributed my own authorship on six artifacts.** Bare `⟨claimed: Fable⟩` where the
   convention says `⟨claimed: Fable lane · occupant: Claude Opus 5⟩`. **Rab had already made this
   correction on 2026-08-24, in `coordination/authorship.md` §Correction — the file governing the
   thing I was doing.** I regressed a correction that was sitting in the rulebook. Fixed in
   `docs/47`, `BRIEF-S109`, `sessions/S109`, the S109 ledger row, and both relay-room docs.
3. **I handed a fleet a GROUND that had gone stale.** Measured the board at 04:31Z, dispatched at
   04:42Z; Codex woke and posted at 04:39Z. The relay agent HALTED on it — and its halt prevented a
   false-clean, because `MSG-CDX-0010` was posted `--no-ack` and could never have entered the inbox.
   25 polls would have returned a null that read as "Codex is slow." **GROUND has a shelf life and
   `docs/47` does not say so.**
4. **My own audit had a probe defect.** First pass counted a log verb (`SAW`) instead of disk state
   and reported "B: 8 LOST" for a mechanism sitting on disk complete.
5. **`dumps/` shipped carrying the hazard its own README names.** `dump.sh` was headed for a CRLF
   shebang; then my own `dumps/*` rule ate the `.gitattributes` that fixes it — and `git check-attr`
   said `eol: lf` anyway, because it reads the WORKING TREE. A fresh clone would have gotten
   neither.

## §23 Left open

- **`MSG-FAB-0031` is unacknowledged**; this lane is `blocked-on-ack`.
- **The single-lane discount is undischarged.** Everything after 2026-08-24T19:00Z has been read by
  one vendor. The one ask made of Codex — independently re-observe SYM-055 — is outstanding.
- **The four Dock defects are unfiled** as register rows.
- **`docs/47` needs the shelf-life clause** (§22.3).
- **Q&A box design: NOT built.** The bakeoff says the specified delete-on-read is terminal, not
  lossy. The change is one word — never delete, MOVE. Rab's decision, not a lane's.

---

## §24 THE FABLE LANE'S CLOSE — and why NO ledger row is written

⟨claimed: Fable lane · occupant: Claude Opus 5 · S110 · 2026-08-27⟩

**THE RELAY WAS ANSWERED BEFORE THE CLOSE**, per the standing order.

`MSG-CDX-0011` arrived carrying **T-009** — one bounded communication-only round trip. Confirmed at
`sha256:75ae353ea076…` by independent re-digest, with a substantive restatement. Answered as
`MSG-FAB-0032` (`sha256:267244a8c7ec…`), delivering the four fields T-009 asked for. Beat refreshed
with a probe.

**SYM-055 IS NOW CROSS-VENDOR, AND THAT IS THE HEADLINE OF THIS CLOSE.** Codex independently
re-observed it: canonical suite `83/83 exit 0`; a disposable scratch copy with **only** `digest()`
changed sha256→md5 while retaining the `"sha256:"` label **also** returned `83/83 exit 0`; an
independent SHA-256 over fixed bytes rejected the mutant. That is the exact experiment `MSG-FAB-0031`
asked for, run by the other vendor, on the other side of the bus.

**The single-lane discount on SYM-055 is DISCHARGED.** It was filed by one lane and is now confirmed
by two. Everything else after `2026-08-24T19:00Z` remains single-lane and must still be held as
reported evidence until independently re-observed.

### Why this close writes NO ledger row — deliberately

S110 is SHARED. At the moment of this close, **Codex's half is not finished**: its lane sits
`blocked-on-ack` on `MSG-FAB-0032`, T-009's round trip is mid-flight, and its own closeout sections
are unwritten.

**A ledger row would close a session whose other lane is live.** That is SYM-045's shape one level
up — the failure I warned Rab about earlier in this very session, when he asked whether the S109
first sitting should be reopened to take the close. Refusing it there and taking it here would be
the same defect with my name on it.

So: **neither clock is advanced.** The ledger's newest row remains **S109 / `ab544d1`**, and the next
muster will correctly derive **S110** again, because S110 is genuinely not done. That is the honest
reading and it costs nothing — the work is committed, pushed and recorded in these sections whether
or not a row names it.

**The row belongs to whoever closes LAST**, and it should carry both claims — `⟨Fable⟩+⟨Codex⟩` — as
S108's row does.

### CI — GREEN, observed by hand after the push

`Observed 2026-08-27T04:35:20Z`. Workflow **CI**, `status=completed conclusion=success`, for the
close commit **`48edad3`**, queried through the stored git credential.

It read `in_progress` on the close card and was NOT called a green then. That distinction is the
whole reason this line exists: `close.sh`'s CI check cannot fire in this cadence (`docs/45` F16),
and **S101, S102 and S103 all closed reporting clean on top of a red** because nobody looked after
the push. The watch was written to emit on EVERY terminal state — success, failure, cancelled,
timed_out — so silence could not be mistaken for health.

*(This record itself lands as a post-close commit and will trigger its own CI run. That regress is
accepted: the close's CI is the close commit's, and bookkeeping after it is bookkeeping.)*

### What this lane leaves behind

- **Nothing running.** No fleet, no monitor, no server, no port. All workflows returned; all
  monitors were stopped or ended.
- **The tree is committed and pushed.**
- **`MSG-FAB-0032` is unacknowledged** — my lane is `blocked-on-ack` by design, waiting on Codex's
  one closing reply. That is T-009 completing, not a fault.
- **The registers hold everything outstanding**, not this session's memory: `OPEN-TASKS.md` §A for
  Rab's signature, §B/§J for anyone. The four Dock defects found today are still **unfiled** and are
  the first thing worth doing next.
- **`docs/47` §7 now lists SEVEN defective probes** this occupant shipped, up from the three it was
  written with. Two arrived *after* the section documenting them was committed.
