# S77 — Desktop lane — 2026-08-14

**Closeout under `docs/21`.** Tier: EXTENDED — docs, widget and prototype source changed; an
exe is staged.

## §1 Session Intent
`Observed` — Rab, immediately after the S76 close: *"there needs to be a law, but this is less
of a law and more of a complex. How do we efficiently determine what should be wired to the
glass… these aren't bugs, they should be considered glitches, because the system was designed
to be highly observable."* Plus: use agents for a deeper-than-`/circle` review of all the laws,
and *"you have the manual, that should be updated as well."*

## §5 Observable Contract
`Intended` — Rab, after adopting **`DB82AE3F`**:
1. Room → 🗁 engineering menu — **fails if** `resume-stderr.log` is absent (its siblings are there).
2. Bench → ◎ re-score — **fails if** the toast does not state MEASURED, COVERED and VAULT
   separately; the vault recommendation was previously fetched and discarded.
3. Bench chips — **fails if** a collapsed or dismissed site shows no quiet mark, or if any mark pulses.
4. `docs/20` §14 — **fails if** law 2b (the converse projection law) is absent.

## §7 Implementation Delta
`Observed` — **docs/29 NEW** (the complex). `docs/20`: §12 converse-projection note + the
ledger/`resume-stderr.log` rows; §14 law 2b. `SYMPTOM-INDEX`: **SYM-027** files the class;
SYM-025 and SYM-026 corrected to `fixed` (both were stale). `bench.html`: rescore renders
coverage + vault recommendation; `outcomeTag()` + `.zo` show the five outcomes on zone and run
chips. `room.js` + `line.rs`: `resume-stderr.log` joins the 🗁 allowlist (both halves).

## §8 Decision Ledger
1. **Glitch, not bug** — Rab's distinction, adopted throughout. It relocates the fault from the
   coder to the unrealized design intent, which is where the guard belongs.
2. **The law is a disposition requirement, not "show everything."** Wiring everything is the
   wall-of-text failure this project fights on every surface.
3. **Timing over diligence** (§5.4) — same-commit disposition. Retrospective sweeps find
   glitches; only concurrency prevents them. Mode D exists precisely because "done" was defined
   as a green suite.
4. **Fixed our own three before publishing the law.** Writing the document while leaving the
   instances would have been the exact failure it describes.
5. **The outcome marks do not pulse** — a decided site is the opposite of "your hand is
   required" (docs/13's one law).

## §9 Evidence
`Observed` — three read-only lanes, all cross-checked by grep against the live tree.
**Census:** ~83 COMPUTED-AND-SILENT fields (≈14 converter · ≈17 linux · ≈25 widget incl. one
fully dead command `audit_mode_get` · ≈27 bench incl. three dead routes). **Laws:** ~40 laws
extracted; every law with teeth governs writing, none governs reading; the 80-word ledger cap
broke at S74/75/76 (145/149/**177**); `dict_hit` is a dead gate (signed threshold, input
hardcoded `None`); `runs` truncated `[:25]` at source with no record of the discard.
**Etiology:** four modes, predictive — two lanes independently named `page_coverage` as the
next instance. Ritual: fmt · clippy · 20/20 · node · **acceptance 85/85** · build → `DB82AE3F`.

## §10 Known Failures
1. **The investigation reproduced the pattern twice while measuring it** — the rescore verdict
   and the triage outcomes were both built S77 and silent the same night; `resume-stderr.log`
   was S76's. Not embarrassing: it is the evidence that the mechanism is structural rather
   than attentional. All three fixed here.
2. **My own ledger rows broke the 80-word cap three sessions running** (S74–S76), the checking
   command existing and unrun the whole time. This row is inside it.
3. **The detector (§5.1) is specified and NOT automated.** The class stays open until it is.
4. The census's §7 debt is otherwise unfixed — `page_coverage`, seams, `chunks_resumed`,
   `recent_audits`, `reverse_sample`, receipts `note`.

## §15 Symptom Signatures
**SYM-027** — a value is nowhere on any surface yet computed correctly and sitting on disk;
the disk is perfect and nothing raised. `OPEN` (law written, detector not automated).

## §16 Standing Context
docs/18's algedonic escalation names three channels — Room banner, morning note, Gmail draft.
**Only the banner exists**, it cannot represent silence (it iterates over events), `died` is
absent from the matcher because no such event is emitted, and the default lever position
disarms it. The escalation built to make five silent days impossible terminates inside the one
surface that requires a human to already be looking. **This deserves its own session.**

## §17 Current State
`Observed at close` — widget down; bench down; `.gpu-lock` absent; held 4; vault 6; tree clean.
Installed exe `C3D277F7`; **`DB82AE3F` staged, unadopted.**

## §18 Next Entry Point
1. MUSTER; newest row S77, ancestor-verified.
2. **The guided Valentine session** — deferred three times now, still the commission.
3. **docs/29 §8's five signatures**, then automate the §5.1 detector (that is what closes SYM-027).
4. The docs/26 signature sheet → slice 3 (Dock) with the F3 colour policy.
5. The algedonic channels (§16) — its own session.
