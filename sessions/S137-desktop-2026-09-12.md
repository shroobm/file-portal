# S137 — Desktop — 2026-09-12 — J64: the guard's false-deny shapes as cases, then the grammar

*Record before the act (docs/28; J63's guards). Written at the open, before any tracked write.*

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

> Work on the next tickets and tell me all you've worked on since we started using desk

**Standing:** nothing to Codex until lifted; the record before any tracked write (its creation and its commit are two
calls); a probe in the shared checkout is a file run by its literal path; `row_check.sh <N>` and CI's own gates before
any push; `--census` at every close.

## §2 Starting State — `open.sh`, 2026-09-12T15:29:26Z (verbatim)

```
════════ MUSTER · OPEN · 2026-09-12T15:29:26Z ════════
[0] IDENTITY
    ledger newest    S136  567163d
    this session     S137  ·  desktop  ·  2026-09-12
    session marker   coordination/private/session.current = S137 (guard_record / guard_git read it)
    advisory         S137 is named in the INHERITED tree (567163d) — read each, do not blanket-correct:
                     sessions/S136-desktop-2026-09-12.md 
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-12T15:29:27Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 86 (tally == hook)
[3a] LEDGER ORDER  ✓ 119 Desktop-lane rows ascend (S16 → S136)
[3b] LEDGER PARSE  ✓ 119/145 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ 567163d (S136) == ledger 567163d, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 9 · anchor 30 · pending 0 · drop 0
    levers           audit=report · analyst=local · batch=8
    gpu-lock         absent
    events           341 line(s)
    open-tasks       143 item(s) · last written 34 minutes ago
    error-bin        88 row(s) — read the CLASS table before you probe · last written 7 hours ago
    symptoms         115 row(s), 43 open · last written 62 minutes ago
    relay            167 entries · run `gate.py status` for the board
    widget           13196
    widget autostart task 'File Portal widget (J57)' Ready — AtLogOn · auto-logon ON (Bndit)
    python procs     7
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 50896d1
    thinkpad         converter=active · vault tip 8d7cc85 · staging 0 · receipts 17
[3] PIN
    --since          567163d
    closeout         sessions/S137-desktop-2026-09-12.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

## §3 The pin

`--since 567163d` (the previous session's closing commit, from the card).

## §9 The plan

1. **J64 (the ticket, my ranking's first — the biggest cause of this month's HARNESS-MISUSE rows):** the guard's
   false-deny shapes as FALSE-DENY REGRESSION cases in `guard_git_selftest.py`, each reproducing the deny FIRST (a red
   case is the specimen), then the grammar, never a loosening; the RED-TEAM negatives re-run after every change (the
   fifth shape's fix found a real bypass by its negative control). The open shapes: (6) an inline `python -c "…"` whose
   code carries braces; (7) a `%es`-style format string inside quotes (`time -f`); (8) an inline PowerShell `if { … }`
   block; (9) a `[void][System.…]::` head; (10) `find -printf '%TY-…'`; (11) a HEREDOC BODY line read as a command
   (tonight's ` - [%s] %s:%s %s`). A `$VAR` head stays the DESIGNED deny and is a case in the other direction.
2. The mechanism, if the specimens agree: quoted strings and heredoc bodies are DATA, never heads — the scanner
   skips `<<TAG … TAG` bodies and does not read a token inside quotes as a command head; a `%`-led token is never a
   head; a `[type]::` / `[void]…` token is an expression head in PowerShell (the `[math]::` fix's family).
3. Selftest green with the new cases; the guard's live tripwire: the shapes re-run in the real checkout must ALLOW.
4. SYM-110 annotated (fixed for the shapes, the class stays open by construction); J64 struck; the record; the close
   with the receipts; lockstep; §20; the tracker (a new one for S137) closed at the end.

## §8 Findings

- **F1 — five of the seven shapes reproduced, all with ONE reason** (`Observed`, `j64_specimens.py` over HEAD's guard in
  a guarded fixture, 15:3xZ): shapes 6 (`python -c` with braces), 7 (`time -f '%es'`), 10 (`find -printf '%TY…'`), 11
  (a heredoc body's ` - [%s] …` line fed to `python -`) and 11c (a heredoc body fed to `cat > file`) were each denied
  with *a command head the shell expands (…) is UNREAD*. Shapes 8 and 9 (a PowerShell `if { }` block, a
  `[void][System.…]::` head) already passed on HEAD — fixed by the v4.4/v4.5 family without a case; they have cases
  now. The register's mechanism for J64 was `Inferred` as the brace rule; the observed mechanism is the WRAPPER
  RECURSION: `scan()` recursed into EVERY quoted argument of EVERY wrapper as shell text (a Python payload, a `time`
  format, a `find` format), and a heredoc body was split into command segments whoever received it.
- **F2 — the fix is two narrowings, neither a loosening** (`Observed`, 172/172): (a) `SHELL_WRAPPERS` — the recursion
  into a quoted argument happens only for wrappers whose argument IS shell (`bash -c`, `sh -c`, `cmd /c`,
  `powershell -Command`, `eval`, `xargs`, …); every wrapper keeps the mentions-git rule over its segment (the R4
  red-team case `python -c "os.system('git reset --hard')"` still denies). (b) `fold_data_heredocs()` — a heredoc body
  is shell only when its receiver is a shell or the opener line pipes into one; a body fed to `python -`, `cat > file`,
  `tee`, `git commit -F -` is that program's DATA and is removed before the scan.
- **F3 — the old grammar had a BYPASS where it had the false deny** (`Observed`, `j64_hole_baseline.py`, HEAD's guard
  vs the working copy): `python - <<'EOF'` with `os.system('git reset --hard')` in the body **passed** on HEAD (the
  body's lines had no `git` head; the wrapper's mentions-git rule saw only the opener line), and so did `node - <<EOF`
  with `execSync('git clean -fdx')` — while the harmless `%s` line was denied. The fold's third clause closes it: a
  body fed to a non-shell WRAPPER that mentions git, with the cwd in a guarded root or the body naming one, is denied
  as UNREAD. Filed as SYM-116 (fixed, this session; S16 beside SYM-110).
- **F4 — the suite's shape** (`Observed`): 17 J64 cases — nine FALSE-DENY REGRESSION (allow expected; each a live deny
  before the fix, per F1) and eight NEGATIVE CONTROLS (deny expected: heredoc to `bash`, heredoc piped to `sh`,
  heredoc to `python` that shells out, `python -c` that shells out, `bash -c`, `sh -c` with a `cd` into the main,
  `%VAR%` and `$VAR` heads). The deny-log census is derived, and it caught the first run's extra deny (F5).
- **F5 — my patch broke the live guard for one call** (`Observed`): the fold referenced `guarded`, a closure inside
  `decide()`, from module level → `NameError` → *internal error — fails closed; denied* on my own next heredoc call
  (the repair sent through the very shape under repair). The suite had shown it red (J64/11) with the reason printed;
  the specimens run named it. Repaired through `Edit`, suite 172/172, specimens 15/15. ERR-089 (ORDERING).
- **F6 — a designed deny, mine** (`Observed`): I asked the shared checkout for `git stash` to lint a baseline; the guard
  refused it as tier 1 (J56/ERR-068's law). Baseline via `git show HEAD:<path> > scratch` instead. ERR-090.
- **F7 — the live tripwire** (`Observed`, the real checkout, live hook, 15:3xZ): the three shapes denied on S133–S136 —
  a heredoc with `%s` to `python -`, `python -c` with braces, `find -printf '%TY…'` — all ALLOWED, read-only.

## §10 Known failures / residue

- **`fold_data_heredocs` runs for the Bash tool only**; PowerShell here-strings (`@'…'@`) keep their old reading — no
  false deny of that shape is on record, so no case; the next one gets one.
- **The heredoc receiver test is by the opener line's heads** (`bash`, `sh`, `zsh`, `dash`, `ksh`, `fish`, `eval`,
  `source`, `.`); a body fed to a shell reached by a `$VAR` head is `$VAR`'s own designed deny, not the fold's.
- **The wrapper clause denies a program-fed body that MENTIONS git** — a Python line that merely prints the word
  "git" in a guarded root is denied as UNREAD. That is the fail-closed side and it is the intended one; the remedy
  is ERR-009's: a script in a file.
- **The `--census` for docs/58's README** still reads S16 as "no symptom row yet" — its table was written S123 and
  `error_families.py` is the source; one line added for SYM-110/116, the table left as the Historical it is.
- **The bench server (pid 15444/22604)** on the renamed held dir: Rab's process.
- Two ERR rows of my own this session (F5, F6); both refused by the tripwires that exist for them.

## §15 Registers

- **OPEN-TASKS:** J64 struck; the dated line. Net −1. Census at close.
- **SYMPTOM-INDEX:** SYM-116 filed (fixed); SYM-110 annotated — the six shapes 6–11 fixed by cases, the class open by
  construction. **ERROR-BIN:** ERR-089 (ORDERING), ERR-090 (HARNESS-MISUSE). **docs/58 + `error_families.py`:**
  SYM-116 → S16.
- **Private (`agent-scripts`):** no new script; `j64_specimens.py` and `j64_hole_baseline.py` stay in the scratchpad
  (their cases are in the suite, which is the tripwire).

## §18 Next entry point

1. **Open:** `open.sh`; the card should read S138. The Desk inbox first. Record through `record_open.py`.
2. **Rab's ranking (S136 F5):** J64 done; next the Readability lane over the library's 19 briefs (≤5 lanes, Sonnet),
   then J46 (the diff-whitelist acceptor). Rab may redirect on the Desk.
3. **The guard from now on:** a heredoc to a program is data unless it mentions git; a `$VAR`/`%VAR%` head is the
   designed deny; a new false-deny shape gets a case FIRST (the suite's J64 block is the template), the grammar second,
   the negatives re-run third.
4. **Standing:** nothing to Codex until lifted; the record before any tracked write (two calls); scripts in files by
   literal path; `run_gates.sh`, `row_check.sh` and the guard before any push; `--census` at every close; the three
   receipts (process, cost, scripts-used) at every close.

## §7 Work log (UTC)

- **15:30** `open.sh` clean (S137, pin 567163d). This record.
- **15:3x** `j64_specimens.py` over HEAD's guard: 5 of 7 reproduced (F1). `guard_git.py` read (`segments`, `head_of`,
  `scan`, the WRAPPERS recursion, the heredoc docstring).
- **15:3x** `patch_j64.py`: `SHELL_WRAPPERS`, `fold_data_heredocs`, 17 cases. Suite 170/172 — J64/11 red (F5) and the
  derived deny census with it. Live guard failed closed on my heredoc (F5). Repaired via `Edit`. 172/172; specimens
  15/15; live tripwire (F7); `j64_hole_baseline.py` (F3). `git stash` refused (F6). ruff: my five E702 cleaned; the
  five baseline findings are S136's (CI does not lint `.claude/hooks`).
- **15:39** the S137 process tracker started (pid 24556, `-Label S137-receipt`) — it covers the close, not the sitting;
  the receipt says so.
- **15:4x** the record; registers; census; close chain.
## §19 Close values — `close.sh 567163d`, 2026-09-12T15:45:36Z (verbatim)

```
    PIN              567163d
    DIFF             10 file(s) since 567163d · dirty 0 = mine 0 · peer-owned 0 (none) · 1 untracked
    GLASS            clean since 567163d (--enforce; bare runs exit 0 regardless — SYM-046)
    RUST             skipped — no windows-widget/ change since 567163d
    CI               no run for d8edef16 yet (unpushed? ahead=2) — NOT a green
    LEVERS           no unlevered threshold constants added since 567163d
    DOCTOR           lever lexical parity: figure-triage.txt names 8 · quoted refs 8 · MISSING 0
                     LEXICAL REF: mode · min_area_pt2 · vector_min_paths · cluster_gap_pt · text_coverage · words_per_line · table_overlap · accounted_for
                     UNREAD — a quoted lexical occurrence does NOT prove the consumer reads the lever; ARMED on MISSING only
    DOCTOR-HEAD      UNREAD — closeout-headline probe re-run not yet mechanized; NOT a statement the numbers reproduce
    CENSUS           selftest.sh: declared 11 = FIRED 11 = banner 11/11 (exit 0)
    CENSUS-SCOPE     UNREAD — broader TEST-STRATEGY fixture census not yet mechanized; NOT a statement all promised tripwires ran
    REGISTER         OPEN-TASKS.md written this session: +2 / -1 lines
                     STRUCK 1 · ADDED 0 — net -1, the register moved DOWN
    MEMORY           clean — soft clock is DURABLE at 30e57e4
    DEBT             open SYM 43, unchanged since 567163d
    PUSH             ahead of upstream: 2 (the close pushes; 0 after)
════════ close exit 0 — values only; the judgment half is SKILL.md ════════
```

- **DIFF:** every changed file is in the content commit `d8edef1` or this closing commit; the row follows, then
  `row_check.sh 137` on it, then the push.
- **Relay:** no entry — nothing to Codex (standing).

