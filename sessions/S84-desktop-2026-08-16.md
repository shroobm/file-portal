# S84 · Desktop · 2026-08-16

## §1 Session Intent

Rab's commission, in his words:

> "the word, S84 is the GLM OCR probe"

and, authorizing the session's first act by name, source and size:

> "yes, pull the files when S84 opens"

**Written before any work. Left unedited for the rest of the session** (docs/21).

## §2 Starting State

`bash .claude/skills/muster/open.sh` — exit 0, verbatim:

```
════════ MUSTER · OPEN · 2026-08-16T05:37:40Z ════════
[0] IDENTITY
    ledger newest    S83  6fa09f4
    this session     S84  ·  desktop  ·  2026-08-16
    advisory         S84 is named in the INHERITED tree (6fa09f4) — read each, do not blanket-correct:
                     CHANGELOG.md sessions/S83-desktop-2026-08-16.md
[1] GROUND
    verifier         344E1277  (repo == ~/.claude)
──────── MUSTER · 2026-08-16T05:37:41Z ────────
[1] MEMORY ....... ✓  [2] SOFT clock ... ✓ 72  [3a] ✓ 68 rows S16→S83  [3b] ✓ 68/89
[3] HARD clock ... ✓ 6fa09f4 (S83) mirrors ledger, ancestor of HEAD
    WORKING TREE . 0 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile · muster exit 0
[2] LIVE
    pipeline         held 4 · anchor 23 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=16
    gpu-lock         absent · events 137 · widget 11280 · python procs 4 · ollama 2
    llama-server     1 — the NAME does not identify it (ollama's engine is also llama-server.exe);
                     the parent PID decides, never the name (S81)
    installed exe    C3D277F7 · vault 6 note(s) · tip 70c60e6
    thinkpad         converter=active · vault tip 70c60e6 · staging 0 · receipts 5
[3] PIN
    --since          6fa09f4
    closeout         sessions/S84-desktop-2026-08-16.md
```

**Advisory resolved:** both `S84` hits (CHANGELOG, S83 §18/§8) are S83's forward references to its
successor. No collision. **The card's new llama-server row is in production for the first time** —
the 1 it counts is Ollama's engine, resident from S83's recheck arm; released below before the
GLM load. **Pinned `--since` = `6fa09f4`.**

## §3 Pinned closeout terms

Unchanged: §7/§8/§10/§15/§18; `git diff --name-only 6fa09f4..HEAD` fully accounted;
`glass_detector.py --since 6fa09f4`; ledger row as a separate follow-up commit; both clocks in
lockstep; §1 unedited.

## §4 In-scope symptom rows

- **SYM-022** — one lab process on the card, ever: Ollama's resident model is released and VRAM
  proven back to baseline before llama-server loads GLM-OCR. The watcher is ALIVE; nothing gets
  dropped during GPU work.
- **SYM-003** — the probe's target hypothesis: GLM-OCR's region-parallel two-stage design may be
  structurally immune to the autoregressive table-loop. Stated as hypothesis; one Beer page tests
  it.
- **SYM-032/SYM-036** — `.gpu-lock` read as signal only; patch anchors built with `chr()` or the
  Edit tool.
- **Sampling never promotes** — 3 pages is `Inferred` about everything, permanently.

## §5 The probe, planned before it runs

1. **Release** Ollama's model (`keep_alive: 0`), prove VRAM at baseline.
2. **Pull** (authorized): `llama-server -hf ggml-org/GLM-OCR-GGUF` — `GLM-OCR-Q8_0.gguf` (950 MB)
   + `mmproj-GLM-OCR-Q8_0.gguf` (484 MB) from HuggingFace, ~1.44 GB, onto the existing
   `C:\Users\Bndit\ml\llama\` install (b10448). Port **7130** — outside room-chat's 7110–7119 and
   the bench's 7077–7096 (S83 §10.4's lesson applied at birth).
3. **First question:** does b10448 load a March-2026 model at all. A clean `/health` answers it;
   a load failure is a finding, not a dead end (the fallback is a newer llama.cpp build, which is
   its own small decision).
4. **Quarantine home** `prototypes/glm-ocr-probe/` — disposable, zero pipeline coupling.
5. **Test material** from the anchor's source PDFs: one Beer table-loop wreck site, one Valentine
   zone, one clean control page, rendered to PNG.
6. **Side-by-side** — GLM-OCR's output against the current bundle markdown for the same regions.
   OCR sampling per ggml-org guidance: temperature ~0.1 / top-k 1.
7. **The gate is content fidelity, judged by Rab at the bench.** The vendor's 94.62 OmniDocBench
   stays `Historical` (docs/34). The probe feeds two separate downstream decisions — bench
   second-reader, scan-lane challenger — each needing his signature, neither taken here.

## §6 Results

**The chain, every link observed:** Ollama released (VRAM 6,959 → 955 MiB) → authorized pull →
**b10448 loads GLM-OCR** (`/health` ok, port 7130) → five Beer pages rendered at 180 dpi from the
`Verified` source (`drop/done/…BEER.pdf`, sha256 = the bundle key `b7b711d4…`) → transcribed →
string-probed against the held bundle → **adjudicated by Rab against the page renders**.

| page | audit record | typeset text | handwritten/figure content | Rab |
|---|---|---|---|---|
| 77 | worst omission run (96 words) | in bundle | ABSENT → recovered ("85 % of public…") | **signed** |
| 76 | run (48 words) | in bundle | ABSENT → recovered (OPERATIONS vignettes) | **signed** |
| 114 | run (60 words) | in bundle | ABSENT → recovered (System Three* V/W/X list) | **signed** |
| 33 | run, table-ish garble | in bundle (prose parity) | diagram labels — not string-adjudicable | seen |
| 50 | control | 2/3 sampled sentences verbatim | one divergence, flagged | seen |

**The signature is mechanistic and consistent: Marker's scan lane keeps the print and drops
Beer's handwriting; GLM reads both.** The garble `r system three v` resolving to *"System
Three\*, V — 'management audit'"* is the cleanest single demonstration. Cost (`Observed`, n=5):
2.5–3.3 s wall/page, prefill 4,052 tok ≈ 1.5 s, decode 196–404 tok at 308–331 tok/s, every
finish `stop`.

**Flags against the model, accepted as flags:** "Mammal corporation" (Mammoth?), "placards it"
(at?) — 0.9B smoothing, exactly what the bench's accept/reject step exists for.

**Stated untested, so it cannot be quietly assumed later:** the SYM-003 **table-loop** hypothesis
— none of the five pages exercised a degeneration zone (the probe adjudicated *omission* runs
only) · whole-book behaviour · hallucination rate at scale · clean-lane books. Five pages promote
nothing beyond themselves.

### §6.1 Deliberate divergence

§5 planned one Valentine zone; all five pages came from the Beer instead. Rationale recorded at
the time: same-book pages hold the scanner constant and isolate the model variable, and the
Beer's audit gave page-keyed omission runs where the Valentine's zone would have needed
bench-side digging first.

## §7 Implementation Delta

| Commit | What |
|---|---|
| `14eaaae` | open plan — commission + download authorization verbatim, `--since` pinned `6fa09f4` |
| *(this close)* | `prototypes/glm-ocr-probe/` — README (probe design, evidence tables, verdicts) + five `glm-pNNN.md` outputs (tracked); page renders ignored as re-derivable; CHANGELOG |

## §8 Decision Ledger — what Rab signed

**Signed, his words:** the download (*"yes, pull the files when S84 opens"*) · **the existence
proof** — *"the handwriting checks out"* (p77) and *"checks out"* (p76, p114): three of three
adjudicated omission runs recovered faithfully, confirmed by the vision authority against the
page renders.

**Explicitly NOT signed here, queued as their own builds:** (1) the **bench second-reader** —
GLM drafting proposals for wreck-site zones with reason·highlight·solution structure; (2) the
**scan-lane challenger** — an engine-policy-table slot, gated by the Survival Audit A-B against
Marker on whole books. Also still pending from S83: the wrapper decision, rule 8's countersign.

## §10 Known Failures — mine, first-class

**10.1 The p33 check could not distinguish recovery from parity.** My string-probes tested GLM's
*prose* against the bundle — and the prose was already in the bundle, so the page's actual
omission (the hand-drawn diagram) was never string-adjudicated. The probe design conflated "GLM
matches the bundle" with "GLM recovered what the bundle lacks" for exactly the page whose damage
was graphical. The bench build must key its checks to the *zone*, not the page.

**10.2 The probe never touched its named target.** SYM-003 — the table-loop — is the symptom the
commission grew from, and none of the five pages exercised a degeneration zone, because I
selected pages from the omission-run list where page numbers were free. The strongest finding
(handwriting recovery) is real but is a finding about a *different* failure class than the one
hypothesised. Recorded so the next session tests the loop deliberately: the degeneration rows
carry line numbers, and the line→page mapping exists in the audit's own machinery.

## §15 Symptom Signatures

- **SYM-003** — hypothesis (region-parallel OCR dodges the loop) **still open, untested** (§10.2).
- **SYM-026's class** — the probe is the first tool that *recovers* omission-run content rather
  than merely detecting it. Three recoveries signed.
- No new rows filed.

## §17 Current State

`Observed at close` — GLM server tree-killed; card **free** (1,040 MiB, 50 °C). GLM-OCR GGUFs
cached locally (llama.cpp `-hf` cache), reload is one command. Widget 1 (11280), watcher alive,
drop 0, `held/` 4 unchanged — **the Beer now has three signed recoveries waiting for a bench
session**. Vault 6, tip `70c60e6`. Ollama serving, nothing resident. *(`Historical` the moment
it is written.)*

## §18 Next Entry Point

1. `bash .claude/skills/muster/open.sh` — expect exit 0, S85, `--since` = this session's SHA.
2. **The bench second-reader build** — awaits Rab's signature and his cadence (survey → design →
   dedicated session). The probe's evidence: recovery works on handwriting omissions at ~3 s/page;
   proposals must carry reason·highlight·solution and land as accept/reject, zone-keyed (§10.1).
3. **Test the table-loop deliberately** (§10.2): map a degeneration zone's lines to its page,
   render, probe. One page answers what this probe did not.
4. **The scan-lane challenger** — bigger surgery, own design, Survival Audit A-B gate.
5. Pending from S83: the **wrapper decision** · **rule 8 countersign**.
6. Standing: a **product session** for `held/` — the Beer's three signed recoveries are its
   natural start · Rab's ten real questions · docs/26 slice 3 · `windows-converter/` outside CI.
