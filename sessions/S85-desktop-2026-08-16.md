# S85 · Desktop · 2026-08-16 · OVERNIGHT AUTONOMOUS

## §1 Session Intent

Rab's commission, in his words, handed at bedtime with cookie #73:

> "Tbh, I'm going to bed, do everything, look out for everything, make sure that the llama app is
> essentially embedded into the file portal app, and so I can talk to models that I have
> downloaded on my device, that are directly connected to a schema regarding the file portal
> knowledge corpus, and manual, which should describe everything regarding the file, and all the
> folders and files within. Do this, use agents if needed, don't fuck it up. Good night. Have a
> cookie 🍪"

Read as: **the room-chat graduation he queued in S79 §18.3, now signed by his words** — the chat
capability moves from quarantine into the widget proper — plus two extensions he names tonight:
a **model picker** over what is already downloaded on this device, and a **corpus schema**: a
manual that describes the portal itself — every folder and file — wired into what the model can
cite. Agents authorized. "Don't fuck it up" = the laws ride: one lab process on the card, the
signed deferral gate built exactly as designed, citation-or-refusal, projection verbatim
(docs/33 §2.1), the link fence, and **adoption is Rab's hand** — the exe is built and staged
tonight, launched by him in the morning; his running widget (11280) is not touched.

**Written before any work. Left unedited for the rest of the session** (docs/21).

## §2 Starting State

`bash .claude/skills/muster/open.sh` — exit 0: S85, `--since` = **`d995b1f`**, clocks clean
(69 rows S16→S84). Live: held 4 · anchor 23 · drop 0 · widget **11280** (Rab's, running, with
watcher chain — LEFT ALONE) · ollama serving, nothing resident · card free (~1,040 MiB, 50 °C)
· GLM-OCR GGUFs cached from S84. **Cookie #73 logged FIRST** per the standing order — tally 73,
TIME-STATE mirror 73, `muster.sh` verified `tally == hook` after.

**Advisory resolved:** `S85` in the inherited tree is S84 §18.1's forward reference. No collision.

## §3 Pinned closeout terms

Standard: §7/§8/§10/§15/§17/§18; every file in `git diff --name-only d995b1f..HEAD` accounted;
`glass_detector.py --since d995b1f` — **and §5's same-commit render rule applies to every field
the new panel projects** (docs/29 §5.4); ledger row as separate follow-up commit; clocks in
lockstep; §1 unedited; **morning note** to
`C:\Users\Bndit\Desktop\CLAUDES DESKTOP MESSAGES FOR BNDIT\2026-08-16 -- *.txt`.

## §4 In-scope symptom rows

- **SYM-022/021/032** — GPU discipline: chat load tests use the hold protocol; no chains; the
  lock is a signal. His widget's watcher is ALIVE — nothing gets dropped, and the deferral-gate
  tripwire runs against an **isolated** watcher instance (temp pipeline root, never the live one).
- **SYM-033** — two widgets is a reachable state; tonight's testing NEVER launches a second full
  widget. The chat server is tested headless; the built exe is staged, not launched.
- **SYM-010** — test harnesses use OS-assigned/isolated ports and dirs, never the live ones.
- **SYM-023's class** — window-event lifecycle: the chat server must die with the widget (Job
  Object), not with a window.
- **SYM-036** — patch anchors via `chr()` or the Edit tool.

## §5 The design, recorded before it is built

**Architecture — graduate the proven thing, don't rewrite it.** The widget spawns
`room_chat.py` (promoted from `prototypes/room-chat/chat.py`, which S79 built and quarantined) as
a managed child **inside the watcher's Job Object** — exactly the watcher's own pattern — and
`room_chat.py` spawns llama-server as its child (grandchild dies with the job; S37 paid for
this). The citation engine, two-phase hold, corpus loader and refusal path are quarantine-proven
code and move, not get rewritten.

**The lanes:**

1. **Corpus schema** (his explicit ask): new `docs/35-portal-schema.md` — the manual of the
   portal itself: every folder and file class in the pipeline root (`drop/ anchor/ held/
   pending/ events.jsonl .gpu-lock analyst-mode.txt audit-mode.txt conversion-ledger.jsonl …`),
   bundle anatomy (`manifest.json` fields, assets, fidelity block), and the repo docs map.
   Curated prose, citable line-by-line. Plus `corpus_schema.py`: a **live tree walk** rendered as
   DATA beside the prose (docs/33 §2.1: the model points at surfaces, never restates them).
2. **The chat server, promoted**: `windows-converter/room_chat.py` — chat.py + `/api/models`
   (enumerate ollama manifests + llama.cpp `-hf` cache GGUFs), `/api/load` takes the picked
   model, corpus = docs/20 + docs/35 + live schema data. Citation-or-refusal extended over the
   new corpus. Headless acceptance tests.
3. **The signed watcher gate** (docs/33 §2.3, README §5): `watch_and_convert.py` reads
   `chat-hold.json` before converting; holds → defer with a log line, re-poll; cleared →
   convert. **Tripwire**: isolated watcher, drop a PDF while held, prove it defers AND converts
   the moment the hold clears.
4. **The widget**: `llama_server_exe` config key (empty-hides-feature, docs/20 §11 pattern) +
   `chat_python` fallback resolution; `chat.rs` spawn/stop commands; Room panel — model picker,
   load/unload, Q&A with citations rendered, pipeline numbers via existing projections only.
   Every projected field rendered or dispositioned in the same commit.
5. **Ritual + staging**: `cargo fmt --check` → clippy `-D warnings` → `cargo test` →
   `node --check` → `npm run tauri build` → SHA-8 printed and staged. His widget untouched.
6. **Close**: morning note → closeout → CHANGELOG → ledger row → clocks in lockstep.

**Priority order if the night runs short** (recorded now, so scope-trimming is visible): lanes
1–3 deliver "talk to downloaded models wired to the schema" even standalone; lane 4 is the
embedding; lane 5 stages it; nothing ships half-tested — a lane that can't finish clean gets
recorded as NOT DONE with its state, per docs/21.

**Agents**: authorized by Rab; used for read-only reconnaissance (an Explore agent maps the
Rust integration points) — doctrine-carrying edits stay with me.

## §6 What was built, and what proved it

**The commission's chain, every link exercised tonight:**

1. **docs/35 — the portal schema** (curated) + `corpus_schema.py` (live half). In the corpus,
   citable; the live tree renders as DATA on the surface, never in the model's mouth.
2. **The signed watcher deferral gate** + stale-hold reap — tripwire **12/12**: held defers
   (once, no spam, isolated telemetry) · cleared converts on the next poll · a dead pid's hold
   is reaped, not obeyed.
3. **room-chat graduates** (git mv, history kept): model picker over BOTH on-device stores
   (ollama manifests → blobs; the HF hub cache) — ids only, never request-supplied paths;
   switching unloads first. Acceptance **20/20** after catching the relocation bug itself.
4. **The widget owns the lifecycle**: `chat.rs` on bench.rs's chassis — `llama_server_exe` key
   (empty hides the feature), Job Object adoption, a `chat_stop` the bench never had, the
   death-certificate mutex, and the certificate RENDERED (reopening after a death names the exit
   code on the Dock's status line). fmt clean · clippy `-D warnings` clean · **cargo test 23/23**
   (the lane commit's message says 20 — it is 23) · `node --check` clean.
5. **End to end, live, on the real card**: picker → `qwen3:8b` loads in 15–16 s (VRAM 8,183 MiB
   at the new 16k context) → *"What is the held/ folder?"* answers **`cited`, resolving into
   BOTH docs/20 §6 and docs/35 §2.3** → *"How many books are held right now?"* refuses honestly →
   a third answer cited docs/33 (not in its corpus) and was **withheld by the guard working
   exactly as designed** → unload clears the hold, VRAM back to 937 MiB.

## §7 Implementation Delta

| Commit | What |
|---|---|
| `e74964b` | open plan — commission + cookie #73 logged first, `--since` pinned `d995b1f` |
| `58ef166` | **the signed gate, built and fired** + docs/35 + events FP_PIPELINE override |
| `2bba88a` | **room-chat graduates** — picker, schema corpus, stale-hold reap, pid_alive |
| `ad94118` | **chat.rs** — the widget embeds the assistant; config key; button; live config.toml keyed |
| *(this close)* | 16k context (corpus measured ~9.5k tok) · display names · certificate rendered · CHANGELOG · morning note · staged exe |

## §8 Decision Ledger — what Rab signed

**Signed, his words, tonight's §1:** the graduation itself, the model picker, the schema corpus,
agents authorized, and cookie #73. The deferral gate was already signed (S79 §8.4); tonight built
it to its own specification.

**My readings of ambiguity, recorded for his morning eyes (docs/21: an assumption is stated,
never silently installed):**
1. *"directly connected to a schema"* — read as: the schema DESCRIPTIONS join the citable
   corpus; live listings render on the surface as data. The signed projection law (docs/33 §2.1)
   was kept intact. If he wants the model itself speaking live listings, that is a doctrine
   amendment only he can sign.
2. The stale-hold reap lets the READER delete a dead writer's file — a bounded exception to the
   single-writer law, keyed on pid liveness, logged, tripwired. Flagged for his countersign.
3. SYM-033's prevention half (single-instance guard) remains unbuilt — still his signature.

## §10 Known Failures — mine, first-class

**10.1 `os.kill(pid, 0)` as a liveness probe would have shipped if the tripwire had not fired
first.** On Windows it is `TerminateProcess` in disguise. The gate's own selftest caught it —
a live holder read as dead, a valid hold reaped. `pid_alive()` now does it honestly.

**10.2 The corpus outgrew its context and I found out from a 500.** docs/35 pushed the corpus to
~9.5k tokens against `-c 8192`; the first full E2E failed at ask-time. Measured, then raised to
16k (VRAM 8,183 MiB, fits). The design comment even warned the 8k fit was docs/20-shaped.

**10.3 My E2E harness violated SYM-006 and then SYM-031 in one evening**: `proc.terminate()`
orphaned the llama child (whose VRAM then OOM'd the next load), and `api("/api/unload")` with no
body silently sent a **GET** — the 404 was discarded unread, and I spent a diagnosis cycle
suspecting the server. The harness, not the product, held every defect of the last hour.

**10.4 The lane commit's message says cargo test 20/20; the suite is 23.** Corrected here.

## §15 Symptom Signatures

- No new rows. SYM-006/031/036 each re-fired **at me** through the night and their remedies
  held. SYM-023's inverted-survivor shape is designed out (no chat window-event coupling); the
  chat server dies by command or Job Object only.

## §17 Current State

`Observed at close` — Rab's widget (11280, pre-graduation exe) still running untouched, watcher
alive, drop 0, held 4, card free (~937 MiB). Live `config.toml` carries `llama_server_exe`. The
new exe is **built and staged, NOT adopted** — hash in the morning note; adoption is Rab's hand
(docs/19 §0.3). No stray llama-server, no stray hold (probed). Cookie #73 in both mirrors.
*(`Historical` the moment it is written.)*

## §18 Next Entry Point

1. `bash .claude/skills/muster/open.sh` — expect exit 0, S86, `--since` = this session's SHA.
2. **Morning adoption (Rab's hand):** close the old widget → copy the staged exe (SHA-8 verified)
   → launch → the Assistant button appears → pick a model, load, ask. The relaunch also brings
   the deferral gate live in the production watcher.
3. **First live session**: his ten real questions (docs/33 §3.3) — the acceptance test that
   decides whether curated context suffices or retrieval is needed, now askable in his own app.
4. The withheld-docs/33 edge from §6.5: consider making docs/35 §3.2 self-contained enough that
   models cite IT for the gate. Small corpus edit, his call whether it matters.
5. Standing: the reap countersign (§8.2) · SYM-033 prevention · the GLM bench second-reader ·
   the wrapper decision · rule 8 countersign · a product session for `held/`.
