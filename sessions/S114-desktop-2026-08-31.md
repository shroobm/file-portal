# S114 · Desktop — the collection session (maiden voyage still in flight at open)

⟨claimed: Fable lane · occupant: Claude Fable 5.1 · S114 · 2026-08-31⟩

> **Authorship correction (2026-09-02T05:2xZ):** this header and the session's first eight commit
> trailers named the occupant "Claude Opus 5". The model running this lane is **Claude Fable 5.1**
> (model id `claude-fable-5-1`), per the harness's own identity statement. The S109-era records
> were genuinely Opus 5; S112's were Fable 5; this session inherited the older stamp by habit.
> Commits are never amended here (CLAUDE_README §4), so the wrong trailers stand in history and
> this note is the correction of record. Per `coordination/authorship.md`, a claim about who
> wrote a thing is a claim like any other and gets corrected in place, not overwritten.

## §1 Session Intent

Rab's commission, verbatim, written before any work and left unedited for the rest of the
session:

> MUSTER. Collect the maiden-voyage run (J19) and give me the report — what I got, if it
> worked, what I gained and lost. Then the adoption window for 1CF604CD (J20), the M6-R1
> review for Codex (J21), and start the UI research (J22).

## §2 Starting State — the `open.sh` card, verbatim

The first run exited 1. The incident and its reconcile are §3; this is the card AFTER the
reconcile, which is the state the session actually starts from.

```
════════ MUSTER · OPEN · 2026-09-01T00:40:14Z ════════
[0] IDENTITY
    ledger newest    S113  14a526b
    this session     S114  ·  desktop  ·  2026-08-31
    advisory         S114 unnamed in the inherited tree
[1] GROUND
    verifier         666AF259  (repo == ~/.claude)
──────── MUSTER · 2026-09-01T00:40:15Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 84 (tally == hook)
[3a] LEDGER ORDER  ✓ 96 Desktop-lane rows ascend (S16 → S113)
[3b] LEDGER PARSE  ✓ 96/121 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 4 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79
[3] HARD clock ... ✓ 14a526b (S113) == ledger 14a526b, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 7 · anchor 26 · pending 0 · drop 1
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         PRESENT — a convert holds the card
    events           251 line(s)
    open-tasks       116 item(s) · last written 73 minutes ago
    error-bin        45 row(s) — read the CLASS table before you probe · last written 4 days ago  *** not written in 3d — is it still true? ***
    symptoms         70 row(s), 28 open · last written 82 minutes ago
    relay            125 entries · run `gate.py status` for the board
    widget           24608
    python procs     4
    ollama           2
    llama-server     1 — the NAME does not identify it (ollama's engine is also llama-server.exe); the parent PID decides, never the name (S81)
    installed exe    4C6B073B  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 70c60e6
    thinkpad         converter=active · vault tip 70c60e6 · staging 0 · receipts 9
[3] PIN
    --since          14a526b
    closeout         sessions/S114-desktop-2026-08-31.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

**Pinned for the close: `--since 14a526b`.**

## §3 The open incident, and its reconcile

`open.sh` exited **1** on its first run of the session:

```
[2] SOFT clock ... ✗ DRIFT tally=84 hook= (hook read within 6 lines of TIME-STATE)
```

`Observed` — the two records were read directly and **agree in substance**:

- `cookie-tally.md:13` — `**Received from user: 84**`
- `MEMORY.md` TIME-STATE — `cookies **received 84 / given 3 …**`

The `hook=` field came back **empty**, not wrong. `muster.sh:75` matches
`received [0-9]+ / given [0-9]+` line-wise over the TIME-STATE block, and the S113 close had
re-wrapped that sentence so the phrase straddled a newline — line 11 ended `cookies **received`
and line 12 began `84 / given 3`. A line-wise regex cannot see across that break.

**Verdict: CONFIG fault. Not a rewind, not a fork, not a blind session.** The HARD clock was
`✓` throughout (`14a526b`, ledger-equal, ancestor of HEAD), so only the soft parse was
implicated, and the substance of both clocks matched.

**Reconcile:** the TIME-STATE sentence was re-wrapped so `received 84 / given 3` sits
contiguous on one line. No count changed; no content moved out of `MEMORY.md` (S96's ruling).
Re-running `open.sh` returned `[2] SOFT clock ... ✓ 84 (tally == hook)` and exit 0.

**Filed as a defect, because a guard that a re-wrap can defeat is a guard with a silent
failure mode**: the same edit that made this fire *loudly* could have made a REAL drift read as
a benign empty parse in the other direction. See **SYM-071** (filed S114). NOTE: an earlier draft of this section wrote "SYM-068" as a forward reference before the register was re-measured; SYM-068/069/070 were already taken by unrelated rows (watcher stop button / half-dead widget window / Bench cap). Corrected here rather than silently - a register reference that points at someone else's defect is exactly the collision the muster ID rules exist to prevent.

## §4 Inherited, all `Historical`

From `sessions/S113-fable-lane-2026-08-31.md` §5 (the ordered next-session list),
`sessions/S113-desktop-2026-08-31.md`, `OPEN-TASKS.md` J19–J23, and `SYMPTOM-INDEX.md`.
Nothing here is actionable until re-measured.

- J19 the maiden voyage: analyst `295/957 · 17.0 s/chunk · eta ≈ 03:10Z` **at the S113 close**.
- J20 `1CF604CD` staged, unadopted; adopted glass `4C6B073B`. Held mid-run at S113 because
  *"a widget close kills the watcher tree (S37 Job Object)"*.
- J21 M6-R1 review owed to Codex — `MSG-CDX-0042`, commit `57a5da6`, five attention points.
- J22 the UI research commission — `prototypes/` quarantine only.
- J23 13 contested census dispositions — Rab's signature.
- Open symptom rows intersecting today: **SYM-066** (run-cap, dead in production), **SYM-067**
  (degeneration empty-cell false positive — the gate change is Rab's alone), **SYM-057**
  (`doc_survival 1.0` from an unmeasurable comparison — latent, and the analyst-phase audit
  this session collects is exactly where it would surface).

## §5 Promoted this session (re-measured, `Observed` / `Verified`)

**5a. The run is ALIVE and IN FLIGHT — J19 cannot be collected at open.** `Verified`, two
differently-shaped instruments that fail independently:

- `.analyst-progress.json` sampled twice, 45 s apart:
  `00:41:03Z {"n": 429, …, "eta_s": 9292}` → `00:41:48Z {"n": 431, …, "eta_s": 9257}`.
  n advancing, eta falling.
- `nvidia-smi` at 00:40Z: RTX 3080 **94 % util · 7594 / 10240 MiB · 74 °C** — an independent
  witness that could disagree (a stale file beside an idle GPU, or a busy GPU on other work).

At 00:41:48Z: **431 / 957 chunks · 17.6 s/chunk · eta_s 9257 → ETA ≈ 03:16Z**
(≈ 23:16 local, 2026-08-31). The watcher's 8 h outer cap bites 05:30Z — margin holds.

**5b. J20 is HARD-BLOCKED by 5a, and adopting now would destroy the run.** `Verified` — the
inherited claim (OPEN-TASKS J20 / S37 Job Object) was not trusted; the live parent-PID chain
was read with `Get-CimInstance Win32_Process` at 00:43Z:

```
34436 explorer.exe
 └─ 24608 file-portal-widget.exe          ← the glass, the adoption target
     └─ 37672 python.exe  watch_*         ← the watcher
         └─ 21748 python.exe (uv)         ← intake writer_pid
             └─ 26836 python.exe convert_*
                 └─ 10408 python.exe (uv) ← the process that emitted the convert events
```

The analyst run is a **descendant of the adoption target**. Closing the widget to swap the exe
kills the Job Object and takes the whole tree with it — the maiden voyage dies at ~431/957,
losing ~2.4 h of completed analyst work plus the 3,834 s convert.

⚠ **A precision note, so the record does not contradict itself:** S113's *"the run is a
detached watcher process — it survives this lane's close"* is true and remains true. It
survives the **Claude session** ending. It does **not** survive the **widget** ending. Two
different closes; only one is fatal.

**5c. Convert phase already landed, before this session opened.** `Observed` from
`events.jsonl` (tail, 22:34–22:35Z): converted `wall_s 3834.2 · s_per_page 2.78 · pages 1377 ·
retry_wall_s 0.0 · resumed_slices 0 · slices 7 · peak_vram_mib 9395` against
`promised_s_per_page 3.154 / promised_eta_s 4343`; then `audit supersede from_verdict fail`,
then `scored … doc_survival 0.9334 · runs 25 · runs_total 531 · degeneration true · verdict
fail` and `flagged`.

## §6 Intent for the session, given 5a and 5b

The commission's order is preserved where the machine allows it and deviates where it does
not. Stated at open, before the outcome is known:

1. **J19 deferred, not dropped** — collection is scheduled for ≈ 03:16Z when the run lands.
   The convert half (§5c) is already collectable and is reported now; the analyst half, the
   verdict, the first-ever `goodput_accepted_tok_s` / `chunks_generated`, and the
   held/supersede outcome are not yet in existence and will not be inferred.
2. **J20 presented, NOT executed** — the window is not open. Adoption is Rab's hand in any
   case (docs/19 §0.3); this session's job is to hand him the measured reason to wait and the
   exact commands for when it is safe.
3. **J21 M6-R1 review** — runs now. No GPU, no widget, no pipeline coupling.
4. **J22 UI research** — runs now. Read-only research + `prototypes/` quarantine.

`Verified` that 3 and 4 are safe to run beside a live GPU job: both are network- and
CPU-bound, neither touches `~/ml/library`, the widget, or the card.

## §7 Work log

*(written as the session proceeds)*

## §8 Findings

*(written as the session proceeds)*

## §10 Known failures / residue

*(written as the session proceeds)*

## §15 Registers touched

*(written as the session proceeds)*

## §18 Next entry point

*(written at close)*

---

## §7 Work log (live)

- **00:38Z** `open.sh` exit 1 → §3 incident reconciled → exit 0. S114 opened, pin `14a526b`.
- **00:45Z** Relay checked (Rab's mid-turn ask). Nothing new since the S113 close; newest to
  this lane is still `MSG-CDX-0042` (23:18Z) = J21. Board was **stale on both sides**: my
  `doing` still described the S113 overnight watch, Codex's still describes **S111** (beat
  5535 m) and runs gate `349818a9` against this shell's `57187c87`. My lane's beat refreshed
  to the truth; Codex's is its own to repair.
- **00:47–00:52Z** J21 executed. `MSG-FAB-0065` posted: **PASS with one finding**.
- **00:52Z→** J22 research + the events-vocabulary grounding.

## §8 Findings

**F1 — SYM-068 (new): a line-wise clock guard is defeated by re-wrapping prose.** §3.

**F2 — M6-R1-F1 (MAJOR, latent): the display cap is applied to the manifest verdict.**
`evidence_count` takes `display_cap=BENCH_RUN_DISPLAY_CAP`, so a well-formed future manifest
(`shown=60, runs_total=531, runs_capped_at=100`) is judged **malformed / "total UNREAD"** with
remedy `re-convert to measure totals`, discarding a usable declared total; the control at
`shown=39` returns `partial | 531 | 492`, isolating the crossing of 40 as the only variable.
The widget renders `60 of 531` for the same bytes. Inverted at the call sites: `bench.html:797`
does **not** slice yet declares the display cap; `main.js:878` / `room.js:433` **do** slice at
40 yet pass `cap=25`. Reported to Codex, not implemented (CDX-0042 said review only).

**F3 — the inline analyst path emits NO events at all.** `Verified` from source + this run's
timeline. `emit("analyst","start")` and `emit("analyst","done")` exist only inside
`apply_analyst()` (`convert_and_ship.py:1458,1460`), which its own docstring scopes to *"the
--resume (widget card) path"*. The **inline** path (`:1377-1395`) — the one this run is on —
calls `analyst.process()` behind a bare `print()` and emits nothing. Consequence, measured:
this run's `events.jsonl` carries **zero** events between `audit/flagged` at **22:35:15Z** and
the analyst-phase audit due ≈ **03:15Z** — a **~4h40m silence over the single longest phase**,
with the only liveness signal a state file (`.analyst-progress.json`) that is overwritten in
place and keeps no history.

**F4 — corrects an S113 claim, and it changes J19's route (not its deliverable).** S113 §2 and
OPEN-TASKS J19 both say the first-ever `goodput_accepted_tok_s` + `chunks_generated`
*"populate at `analyst/done`"*. They will **not** — `analyst/done` never fires on this path
(F3). They ARE produced (`analyst.py:425,435`) and DO reach `manifest["analyst"]`
(`convert_and_ship.py:1384`), but they reach **neither** the event stream **nor** the note
frontmatter (`:1386-1394` writes passed/rejected/failed/duration only). **J19 collects them
from `manifest.json`, not from events.jsonl.** Both registers need the correction.

**F5 — J22's central design premise, and it is measured, not assumed.** Any mission-control
panel built on `events.jsonl` alone is **dark for the majority of a big-book run**. This run:
15 events in the first 65 minutes, then nothing for 4h40m. That is the problem the panel mocks
must solve, and it is the same failure the sources name from three directions — EEMUA 191's
"an alarm needs a defined operator response", Tufte's "a number is meaningless without a trend
line", and Open MCT's streaming-telemetry-plus-composable-views model.

---

## §7b Work log, continued (the second sitting, 2026-09-01T00:52Z → 2026-09-02T05:2xZ)

- **04:56:32Z (09-01)** the maiden voyage LANDED: analyst audit `doc_survival 0.9402 · runs 25 of
  runs_total 404 · verdict fail` → flagged → verdict_fail → **HELD** (`14c66834bdfeaa2e`). No
  `analyst/done` ever fired (F3/F4 confirmed live). J19's goodput collected from the manifest:
  `chunks_generated 316 · chunks_resumed 641 · passed 928 · rejected 29 · failed 0 ·
  duration_s 4634.4 · goodput_accepted_tok_s 59.37`. 641 + 316 = 957 ✓ · 928 + 29 + 0 = 957 ✓.
  **This is the first non-legacy manifest on disk** — it carries `runs_total` + `runs_capped_at`,
  and M6-R1's grammar resolves it correctly: `25 of 531 · partial · unseen 506` *(Verifier correction 2026-09-02: that figure is the CONVERT phase. The ANALYST phase resolves to `25 of 404 · partial · unseen 379`, and the manifest field on each run is `page`, not `run_page` — all 25 analyst runs carry `page: null`. Read from `held/14c66834bdfeaa2e/manifest.json` by the ninth agent.)*.
- **~05:15Z** J19 report delivered to Rab (what he got / did it work / gained / lost / regressions).
- **05:03Z** Codex woke: `185db8f` + `MSG-CDX-0043` — reproduced M6-R1-F1, found a SECOND boundary
  loss I missed (`Bench.state()` sliced retained runs to 40 before the UI), fixed both in
  `12f0ca9`. Asks for an independent re-run of its 60/531 positive and 101/100 negative controls.
  **Owed.** Folded into the 09-02 fleet's Opus reviewer + Fable verifier.
- **J22 signed and built** (`a02b02c`): 5 opus agents → `prototypes/mission-control/` — spine +
  CONTRACT + three panels + adversarial CRITIQUE. All three judged `honest: false`, but on their
  SELF-VERIFICATION (X1: a check that did not run painted green), not on the dark zones, the
  resume zeros, alarm load or self-containment — those were CLEAN on all three. Critic recommends
  graduating Panel C with three named edits. Independently re-verified in-browser before commit:
  void backgrounds `rgba(0,0,0,0)`, C1's contradictory totals both present, C3's self-check
  absent from the DOM. **Graduation not taken — Rab's signature.** Cost 1.24 M subagent tokens.
- **J24 + J25 filed** (`fa32902`). **J24 signed and built** (`850bdf3`): 3 sonnet + 1 opus.
  UPSTREAM BUG: `FlatBlockOutput.page` is WRONG in marker 1.10.2 (`split('/')[-1]` = a per-page
  block counter; true pages 0,1,2,3 ship as 12,43,7,9). True page is `id.split('/')[2]` and is
  ABSOLUTE across `--page_range` slices — so no offset correction exists. `marker_blocks.py`
  (413 lines) is a drop-in for `marker_single.exe` on the exact argv, one `build_document`, two
  renderers; markdown byte-identical two ways; overhead 0.48 %; resume-safe; degrades to today.
  Tripwire 57/57 watched failing both ways. Re-verified here before commit: 57/57 + 85/85
  pre-existing suite. Three undisposed findings: exporter does not carry `blocks.json`; three new
  events the Room cannot speak; `blocks.json` size on a real book UNREAD.
- **Chandra OCR 2 downloaded** (signed): `datalab-to/chandra-ocr-2`, 10.61 GB / 17 files,
  16.1 min, sha-listed before pull. `Qwen3_5ForConditionalGeneration`, 32 layers, 256 K ctx.
  ⚠ **BF16 does not fit** (10.59 GB vs 8.3 GB free) and ⚠ **transformers 4.57.6 cannot load it**
  (`qwen3_5` absent from CONFIG_MAPPING_NAMES). Do NOT upgrade marker-env. `chandra-env`
  install is scoped (another ~3–5 GB) and **waits on Rab's word**.
- **~10:00Z → 09-02 05:21Z: DORMANT ~19 h.** On resume: pipeline idle, no lock, **widget and
  watcher DOWN** (no process), `anchor` 26→27, Chandra intact.
- **05:2xZ (09-02)** authorship correction to this record's header (Opus 5 → **Fable 5.1**).
- **05:3xZ** Rab's commission: *"look at the stuff we had done, and… look at all these pdfs. use
  a fleet of 5 sonnet 5 sub agents, and 3 opus 5 agents and 1 fable agent who verifies then and
  communicates with you."* 17 PDFs → 16 distinct by sha256 (only `PDF-Declarations` is a byte
  dup); **all 16 tagged** (StructTreeRoot present); ISO 32000-2 is **1,023 pp** (harness said
  188). Extracted to text in 3.8 s; clause map: §14.6 p735 · §14.7 p737 · §14.8 p760 · §14.9
  p811 · §9.10 p370 · §8.9 p269 · §14.13 p853. Fleet `wf_acf1876a-0a5` launched: 5 sonnet
  parsers ∥ 1 opus reviewer → 1 opus mapper → 1 opus designer → **1 fable verifier**.

- **05:4xZ (09-02) RETRACTION.** The J20 correction two entries up (`f41dd1e`) said the staged `1CF604CD` *did not exist*. **It did** -- `src-tauri/target/release/file-portal-widget.exe`, mtime 08-31 18:11, found by the rebuild's step [0]. My `ls` was on the wrong path and my whole-tree `find` silently truncated (reproduced). Filed as **ERR-048 (PROBE-SHAPE)** -- the one row this session where a wrong negative reached Rab and a register, **and where it had a cost: the build I launched on that premise overwrote the artifact before a preservation copy could be taken.** The decision that a rebuild on HEAD is the right adoption target stands on its own grounds (two widget commits after `2296cc0`); the sequencing did not. Rebuild GREEN: fmt clean, clippy clean, node --check OK, cargo test 36/36, npm build OK, **staged `E7C5A47A`** (10,333,696 bytes, HEAD `5b52fab`). Adoption is Rab's hand.

- **~05:55Z (09-02) THE STANDARDS FLEET LANDED** (`wf_acf1876a-0a5`: 9/9, 1.94 M subagent tokens, 350 tool calls, 71.7 min). Deliverables preserved: **`docs/52-pdf-structure-study/`** (9 reports + README) and **`prototypes/pdf-structure/probes/`** (26 runnable read-only probes + survey). The ninth agent (Fable) checked 45 claims against source: **28 CONFIRMED · 7 WRONG · 9 OVERSTATED · 1 UNREAD**, 12 contradictions; 25 empirical claims re-run (21 match, 4 differ). Its most-severe items and what they forced: (1) our-work.md repeated my false J20 negative as Observed without re-probing — already retracted (`79d8b46`); (2) both Opus reports had the corpus number wrong — **27 of 56 (48 %) tagged, not 22/57 (39 %)**; (3) **a pymupdf 1.28.0 parse bug** — `Document.markinfo` returns all-False when `/Type` precedes `/Marked`; 3 of 4 exhibits behind "Marked is not the gate" were the bug → **ERR-049**, together with the `('null','null')` trap; (4) **my own record at :259 read as the analyst audit and said 25 of 531; it is 25 of 404** — corrected in place; (5) citations that will not look up (BBox is Table 377 not 384; NonStruct is 14.8.4.4 not .3) — substance holds, lookups do not; (6) `/Suspects` is deprecated in PDF 2.0 and the design offered it as an input; (7) **FIND-2 confirmed in source: the supersede export never copies `blocks.json`** → **J28 filed**; (8) J24 un-struck with no signature note → **struck with residue**. The three numbers that decide the lane, verifier-confirmed: **19 pages of 4,307 (0.44 %)** of the converted corpus reach a conforming tree (1 of 10 works; every scan and both Damodarans have none); **47/175 `/Alt` strings (27 %)** invisible to the current witness; pymupdf **reads the tree today** (two doors, no dependency), and the planted-`/Alt` decoy is **4/4 GREEN with D4 watched failing**. Codex's CDX-0043 controls: PASS, reproduced a third time by the verifier. **Signatures the verifier put on Rab:** (a) J27 as scoped; (b) three fixes that do not wait on it — SYM-056 `\begin/\end` balance, SYM-067 `degeneration()` on stripped text, demote the `or` at `convert_and_ship.py:775`; (c) the J19 Gmail waiver; (d) J28 before the next supersede; (e) ERR-049 banked (done).


## §7c Third sitting (2026-09-03T21:0xZ) — the ledger diagnostic and close-readiness

**Date correction first.** This sitting's first outputs assumed the clock still read 09-02. It
reads **2026-09-03**. The Codex ticket `LEDGER-DIAG-S114` (`MSG-FAB-0067`, commit `d4c7ca7`) carries
"RAB'S COMMISSION (2026-09-02…)" and its commit message says the same — **the commission was
2026-09-03**. The measured timestamp inside the ticket (`2026-09-03T21:06Z at HEAD f764042`) is
right; the header line is wrong by one day. Relay entries are append-only and commits are never
amended, so this note is the correction of record and the close's relay entry will restate it.

**Rab's commission, verbatim:** *"Okay, I need diagnostics on session ledger, and I need to see if
we just need to do a proper closeout to secure the current implementations. Create a ticket of the
same diagnostic kind to ChatGPT and make sure that we are able to do a closeout and commit, without
losing anything we have done seqeuentially historically."*

**The ledger diagnostic — `Observed`, `ledger_diag.py`, every SHA resolved and ancestry-checked:**

| | value |
|---|---|
| ledger rows | **121** — Desktop parsed **96** (S16..S113) · other-lane **4** · unparsed **21** (pre-S16) |
| order faults · unresolvable SHAs · non-ancestors of HEAD | **0 · 0 · 0** |
| other-lane rows (ThinkPad S43/S67/S78/S79) | **4/4 ancestors of HEAD** |
| newest Desktop row | 2026-08-31 · S113 · `14a526b` |
| commits since `14a526b` | **23**, linear (0 merges), HEAD `f764042` at measurement |
| by `Co-Authored-By` trailer | **Codex 3** (`185db8f` · `dace610` · `12f0ca9`) · **Fable 20** (11 Fable-5.1 stamp, 9 pre-correction Opus stamp) |
| push state | `origin/feat/library-pipeline` at `8bcc340` · **ahead 22 · behind 0** · origin is an ancestor of HEAD → push is a fast-forward |
| dirty tracked | **1** — `coordination/ack-codex.json` (Codex's sidecar, last committed by Codex at `185db8f`; not mine to commit; dispositioned to Codex in the ticket) |
| memory library repo | 36 commits · **0 remotes** · clean — local-only by A43 / SIGN-10 (Rab's GitHub hands); the one artefact a repo push does not secure |

**Verdict on "losing anything sequentially historically": nothing is at risk of being lost by a
close, and nothing is currently secured off-machine.** Every ledgered session's closing SHA is
reachable from HEAD; the 23 unledgered commits sit linearly on top and are unpushed. A push
fast-forwards origin by 22 (23 minus `8bcc340`, already on origin) with no conflict. Until that
push, this sitting's 20 Fable commits and Codex's 3 exist on this disk only.

**Self-caught error in the diagnostic itself — ERR-050 (PROBE-SHAPE).** First run printed
`Codex 9 · Claude 14` because it substring-matched "Codex" in commit *bodies*; six of my own
commits name Codex in their subject. Re-measured on the trailer line: 3 · 20. Never left the
scratchpad. Third PROBE-SHAPE row in one sitting (048, 049, 050).

**Cross-vendor ticket.** `LEDGER-DIAG-S114` registered (`gate.py ticket`, state `working`) and
posted as `MSG-FAB-0067`: the same diagnostic, Codex's own tooling, my baseline stated so it can be
falsified, five specific asks (re-derive the counts; confirm its 3 commits; disposition
`ack-codex.json`; name anything in its lane that blocks a close; AGREE or the exact number read
differently). Read-only; no fixes, pushes or ledger rows asked.

**`close.sh 14a526b` — mechanical half, as measured so far (run in this sitting):** PIN `14a526b`
· DIFF 71 files since pin · 1 uncommitted (`ack-codex.json`) · 1 untracked (`.codex/`) · **GLASS
clean under `--enforce`** · **RUST fmt / clippy / test clean** · CI `no run for f764042 yet
(unpushed, ahead=22) — NOT a green` (UNREAD by construction until the push; never blocks). The
register census, debt gate, levers and memory-repo rows are appended below when the run lands.

**`close.sh 14a526b` — RE-RUN after the waivers (`e586095`) and the SYM-066 strike (`e2728f8`), started 2026-09-03T21:14:17Z, exit 1. Rows verbatim:**

```
════════ MUSTER · CLOSE · 2026-09-03T21:14:17Z ════════
    PIN              14a526b
    DIFF             71 file(s) since 14a526b · 1 uncommitted · 1 untracked
                     UNCOMMITTED WORK — the close must commit or state it
    GLASS            clean since 14a526b (--enforce; bare runs exit 0 regardless — SYM-046)
    RUST fmt         clean
    RUST clippy      clean
    RUST test        clean
    CI               no run for e5860955 yet (unpushed? ahead=27) — NOT a green
    LEVERS           no unlevered threshold constants added since 14a526b
    DOCTOR           lever lexical parity: figure-triage.txt names 8 · quoted refs 8 · MISSING 0
                     LEXICAL REF: mode · min_area_pt2 · vector_min_paths · cluster_gap_pt · text_coverage · words_per_line · table_overlap · accounted_for
                     UNREAD — a quoted lexical occurrence does NOT prove the consumer reads the lever; gate remains unarmed
    DOCTOR-HEAD      UNREAD — closeout-headline probe re-run not yet mechanized; NOT a statement the numbers reproduce
    CENSUS           selftest.sh: declared 11 = FIRED 11 = banner 11/11 (exit 0)
    CENSUS-SCOPE     UNREAD — broader TEST-STRATEGY fixture census not yet mechanized; NOT a statement all promised tripwires ran
    REGISTER         OPEN-TASKS.md written this session: +6 / -3 lines
                     STRUCK 4 · ADDED 3 — net -1, the register moved DOWN
    MEMORY           clean — soft clock is DURABLE at aac24f8
    DEBT             open SYM 28, unchanged since 14a526b
    PUSH             ahead of upstream: 27 (the close pushes; 0 after)
════════ close exit 1 — values only; the judgment half is SKILL.md ════════
```

**Reading, by `close.sh`'s own `red=1` sites (lines 39/45/55/66/89/132):** the only site consistent with the rows is **line 55 — `UNCOMMITTED WORK`**, i.e. `coordination/ack-codex.json`, Codex's heartbeat sidecar, last committed by Codex at `185db8f` and modified since. PIN resolved; GLASS clean under `--enforce`; RUST rows clean (read explicitly — a first grep with an uppercase-only pattern hid them, caught before use); CI `NOT a green` is UNREAD by construction (HEAD unpushed) and line 132 fires only on a *completed* non-success. **LEVERS flipped to "no unlevered threshold constants"** — the seven waivers registered. **DEBT reads "open SYM 28, unchanged since 14a526b"** — SYM-071 filed (+1) offset by SYM-066 struck (−1); §A21 satisfied by working one down, not by justification alone. REGISTER +6/−3, net −1. MEMORY durable at `aac24f8`. PUSH ahead 27 at that instant (28 after the beat commit `8c5df94`).

**`git push --dry-run origin feat/library-pipeline`** (2026-09-03T21:1xZ, writes nothing): `8bcc340..8c5df94 feat/library-pipeline -> feat/library-pipeline`, exit 0 — credential accepted, fast-forward, behind 0. **The close's push cannot lose or rewrite anything on origin.**

**Disposition of the sole red is Rab's:** commit `ack-codex.json` at the close under a stamp naming it as the Codex lane's (S113 §6 precedent, on his order), or leave it and state it — in which case the mechanical gate stays red on every run until the file is committed or reverted. Codex is dark (beat ~6.7 d stale); its silence on `LEDGER-DIAG-S114` is recorded as UNREAD per S111, never as agreement.


## §7d Signatures of 2026-09-03 (verbatim), and the fleet built on them

Rab pasted the sign-by-slot block back with every slot signed:

> Signed: SYM-071 tripwire; strike J15/J17/J18/J22 on today's evidence; waive J1. Signed: F3
> (converter, 2 emits + selftest). Signed: F7 + the three blocks verbs (widget); rebuild and give
> me the hash. Signed: SYM-056 report-only. J28: signed, blocks.json goes in the vault / stays out
> of the vault, Desktop lane may commit it, pull and restart the ThinkPad over ssh. Close: go;
> commit ack-codex.json under Codex's stamp / leave it; Gmail waived / send it. Solo / fleet of N
> Sonnet agents and Opus Agents, 1 Fable Agent at the end.

Three slots came back with both options standing; asked and answered in the same turn:

| slot | Rab's answer |
|---|---|
| J28 — `blocks.json` in or out of the vault | **Stay out.** Exporter records `{present_in_bundle, shipped: false, bytes}` in the vault manifest so the omission is explicit; the file stays beside the bundle on the desktop. |
| Close — `coordination/ack-codex.json`, the one measured red | **Commit under Codex's stamp** (S113 §6 precedent, on his order): a Fable-made commit whose message and trailer name it as the Codex lane's uncommitted heartbeat, committed by Fable at the S114 close. |
| J19 — the Gmail draft | **Waived.** He read the maiden-voyage report live. |

**Fleet shape chosen under "N Sonnet and Opus, 1 Fable at the end": N = 6.** Sonnet ×3 (A register
hygiene J15/J17/J18/J22 + J1 waivers · B SYM-071 tripwire + mirror re-stamp · C F7 + the three
`convert/blocks*` verbs), Opus ×3 (D F3 two emits + selftest · E SYM-056 balance check,
report-only, + its disposition · F J28 exporter invariant + tests, defaulting to OUT), then the
Fable verifier (re-runs every suite, watches every new tripwire fail itself, audits file
disjointness and line endings, returns GO / NO_GO). **No agent commits.** One commit per ticket
by this lane on the verifier's GO, then: rebuild → hash to Rab → his hand; the close in docs/21
order; push; **ThinkPad `git pull` + `systemctl --user restart file-portal-converter`** (signed —
its checkout is at `3465ac2`, 467 behind origin, receiver running from it); observe CI.
Workflow id `wf_562030b4-476`.


## §7e The fleet landed, the verifier said NO_GO, Codex answered — and the remedies

**Codex woke** (`7b62016`, `MSG-CDX-0044`, 21:42Z): **AGREE** on ledger continuity — reproduced
121 / 96 / 4 / 21 and 0 / 0 / 0 with its own PowerShell instrument, all 121 closing SHAs tested,
not only the S-numbered rows. Two corrections, both accepted: the 20 Fable-lane trailers are
**10 Fable 5.1 + 1 "Claude Fable 5" (`8bcc340`) + 9 Opus 5**, not 11 + 9; and 22 of 23 were
unpushed (`8bcc340` already remote — as the ticket itself said). It committed its own sidecar
under its own trailer and asked not to be committed for: **Rab's "commit under Codex's stamp" is
moot; the sole measured red is gone by Codex's hand.** Its residue, retained as disclosed:
`owed --as Codex --enforce` = 16 OWED / 1 discharged (its disclosure debt); `.codex/` untracked,
user-owned; **an older detached S111 worktree retains a modified closeout** (two historical state
lines differ from canonical; left untouched, not disposed of silently). It does not return an
unconditional all-clear, and this record does not claim one on its behalf.

**The fleet** (`wf_562030b4-476`, 7/7, 50 min, 1.38 M tokens): B, E, F **READY**; A, C, D
**FINDINGS**; verdict **NO_GO**, one blocker. Suites, all re-run by the verifier itself:
`convert_and_ship_selftest` **105/105** (was 85 — D's cases), `marker_blocks_selftest` 57/57,
`latex_balance_selftest` **27/27** (new), muster `selftest.sh` **59/59** (was 54 — B's cases),
`open.sh` `[2] SOFT clock ✓ 84` under the re-stamped mirror `8181631B`. Every new tripwire was
broken by the verifier in a way its builder had not described, and watched red then green.

**Findings and remedies:**
- **D-1 (blocking)** — F3's two emits pulled two pre-existing unsigned keys into the detector's
  scope: `widget:program` (`line.rs:484`) and `converter:chunks_resumed` (`analyst.py:424`).
  Dispositioned INTERNAL and EVIDENCE with reasons (the latter is N-005's silenced number, now
  on the wire from both paths; rendering it stays a signed widget residue).
- **D-2** — `apply_analyst()` lacked `chunks_resumed`; mirrored so the two paths stay parity-equal
  (T17 pins it).
- **C-1** — `main.js` went MIXED → uniform CRLF on 24 lines; the builder's "LF before and after"
  was a false Observed from a CR-stripping grep → **ERR-051**. Committed blob unaffected.
- **C residue → done here:** F7 could not fire because `line.rs:489-493` forwarded only
  `wall_s / s_per_page / pages`; the two honest fields are now forwarded (inside F7's signature).
- **A-1/A-2** — J17's struck cell cited pre-B line numbers; corrected.
- **F** — the waiver now records Rab's **OUT**.
- **The `[3] PIN` COLLISION** — `open.sh:371-375` decides by filename and reads this four-day
  session's own closeout (named by its open date, per Phase 6) and Codex's lane record as
  collisions → **SYM-072**, stated, not renamed.


---

## §9 THE POWER CUT — an unplanned test of the recovery design, and it passed

Rab, 2026-09-01 ~03:38Z: *"the power went out before i got back home."* Everything below is
`Observed` this session, after the fact.

**Timeline (UTC).**

| when | what | evidence |
|---|---|---|
| 00:52Z | last reading before the cut — analyst **470/957** | this session's own sample |
| ~01:38Z | **power dies** mid-run | `chunks.jsonl` + `.analyst-progress.json` both freeze, mtime 01:38Z, n=**641** |
| 01:05:29Z | Windows' *reported* death time | evt **6008** — coarse (last-flushed clock), and **1.5 h earlier than the files show**; the files are the better witness |
| 01:48:17Z | machine reboots, unattended | `LastBootUpTime`; evt **41** "rebooted without cleanly shutting down" |
| 01:48→03:34Z | **NOTHING RUNS. 1 h 46 m of dead machine.** | zero events in `events.jsonl` across the whole span |
| 03:34:56Z | Rab home; `explorer.exe` (interactive shell) starts | `Win32_Process` CreationDate |
| 03:37:27Z | widget launched **by hand** | parented to that explorer |
| 03:37:28Z | `intake stale-lock-reaped` — the orphaned `.gpu-lock` cleared itself | events.jsonl |
| 03:37:37-38Z | convert **resumed 7/7 slices in ~1 s** | `resumed_slices 7 · wall_s 0.0 · cost_s 3834.2` |
| 03:39:27Z | analyst resumes with **`"resumed": 641`** | `.analyst-progress.json`, watched live |

**WHAT SURVIVED — four guards fired, none of them rehearsed.**

1. **The stale GPU lock reaped itself.** A hard power cut leaves `.gpu-lock` orphaned; the
   intake reaped it before doing anything else. Nothing needed a human.
2. **Marker slice-resume: 7/7 from cache, ~1 s**, carrying `cost_s 3834.2`. The 3,834 s convert
   was not re-paid.
3. **The analyst chunk journal: 642 records, indices 1..642, ZERO torn lines** (618 passed /
   24 rejected). `_append_journal` does `flush()` + **`os.fsync()`** per record — *"a line
   sitting in the OS write cache would be exactly as lost as no line at all"* — and that is
   precisely why a hard cut cost nothing. **641 of the 642 were re-admitted**; `_load_journal`
   re-validates every record against `_chunk_hash(chunks[i-1])`, so the one that did not
   validate is simply redone. The guard chose conservatively, which is the right direction.
4. **The rate stayed honest across the seam.** `s_per_chunk` is measured over chunks
   **generated this run**, never position — so the resume did not manufacture a rate the GPU
   never achieved. docs/34 working exactly as written.

**Net cost of the outage: ~1 chunk of GPU work, and ~2 h of wall clock. Not 3 h of analysis.**

## §10 Known failures / residue

**F6 — THE REAL COST WAS DEAD TIME, NOT LOST WORK, AND IT IS STRUCTURAL.** `Verified`: the
widget has **no unattended start path** — absent from `HKCU:\...\Run`, absent from the Startup
folder, and no scheduled task (`Get-ScheduledTask` names none). The watcher is a **child of the
glass**, and the glass is a hand-launched interactive GUI app. So the machine sat powered,
healthy and idle for **1 h 46 m** until a human logged in and clicked.

⚠ **This is the same coupling as J20/§5b, seen from the other side.** One design fact —
*the watcher's lifetime is owned by the widget* — produces both symptoms:
- adopting a new widget build **kills the running pipeline** (J20, blocked all session), and
- an outage **cannot recover unattended**, however good the journals are.
The recovery machinery is excellent and it proved itself tonight; what it cannot do is start
itself. Naming the seam, not proposing the fix: any change here is Rab's signature, and it
touches `remote-dispatch-vision` directly.

**F7 — a resumed convert reports zeros that a naive glass could publish as measurements.**
`Observed` in the 03:37:38Z event: `wall_s 0.0 · s_per_page 0.0 · peak_vram_mib null`, beside
the honest `cost_s 3834.2` and `pages_converted_this_run 0`. The event is *correct* — this run
converted nothing — but "0.0 s/page" is a number that reads as a triumph and means "not
measured". Same family as SYM-057 (`doc_survival 1.0` from an unmeasurable comparison) and
docs/34's rule that an unreported duration renders UNREAD, never 0.0.

**F7 — confirmed at source, and it is a PARITY defect: one glass publishes the zero, the
other does not.** `Verified` by reading both consumers:
- `windows-widget/src/main.js:482` — ``bits.push(`${r.convert.pages}pp @ ${r.convert.s_per_page}s/p`)``
  renders `s_per_page` **raw**. On tonight's resumed convert that receipt line reads
  **"1377pp @ 0.0s/p"** — a measurement claim about a run that measured nothing.
- `windows-widget/src/room.js:525,528` — the Numerations panel carries **N061 "pages this run"**
  (`pages_converted_this_run`) and **N064 "book true cost"** (`cost_s`), i.e. it already speaks
  the honest vocabulary the resumed event provides.
The producer did its job; one of the two consumers ignores the fields that exist to prevent
exactly this. Fix belongs on the consumer side (`0` pages this run ⇒ render UNREAD, not 0.0),
and it needs a tripwire on a resumed-convert fixture.

---

# THE CLOSE (2026-09-03, third sitting) — judgment half, per docs/21

## §8 Findings (final)

The live findings F1–F7 stand as written in §8 and §9–§10 above; this is the register of what the
session established, each with its evidence class:

1. **The recovery design works under a hard power cut and cannot start itself** (F6, `Verified`).
   641 of 957 analyst chunks recovered, zero torn journal lines, marker 7/7 slices from cache;
   then 1 h 46 m of dead machine because the widget has no autostart and owns the watcher.
2. **The inline analyst path emitted nothing** (F3, `Verified`, and fixed this sitting at
   `eee951d`): 5 h 58 m of a 7 h 26 m voyage unobserved; the first goodput numbers reached no
   glass. Both paths now emit parity-equal `analyst/start`/`done` carrying `chunks_resumed`.
3. **`FlatBlockOutput.page` is wrong in marker-pdf 1.10.2** (`Verified`, J24 `850bdf3`): the
   true page is id segment [2] and is absolute across slices; no offset correction exists.
4. **A resumed convert publishes `0.0 s/p` as a measurement** (F7 / N-059, `Verified`, fixed at
   `1db7266` incl. the `line.rs` forward it needed).
5. **The display cap was applied to the manifest verdict** (M6-R1-F1, `Verified`; Codex closed
   it and a second boundary loss in `12f0ca9`; controls PASS three times).
6. **Muster's soft clock could not tell an unreadable hook from a fork** (SYM-071, fixed
   `c151e68`, CASE 38/39 watched failing twice); and **its PIN gate reads a multi-day session or
   a peer-lane record as a collision** (SYM-072, OPEN, stated not renamed).
7. **The structure-tree study** (docs/52, Fable-verified 28/7/9/1): pymupdf reads the tree
   today; the planted-`/Alt` decoy proves it; **19 of 4,307 converted pages (0.44 %)** reach a
   conforming tree — every scan and both Damodarans have none. A real instrument for a corpus
   not yet dropped; J27 as scoped is Rab's call.
8. **Six self-reported assistant errors** (ERR-046..051), three of them PROBE-SHAPE in one
   sitting, one of which (ERR-048) reached Rab and destroyed the staged `1CF604CD` on a false
   premise. Each filed with the rule that would have prevented it.

## §10 Known failures / residue (final)

- **Unsigned widget residue, carried:** rendering `chunks_resumed` (N-005), `program`
  (`widget:program` INTERNAL), and the analyst receipt's `resumed` field; `widget:age_s` (J16).
- **Unwired by design:** `latex_balance` reaches no renderer (EVIDENCE); the Repair Bench is the
  intended reader.
- **J24 residue:** `blocks.json` size on a real book UNREAD; no J24-era bundle has been
  superseded yet — J28's manifest field is `Intended` until one is.
- **SYM-072 OPEN** — `open.sh:371-375`; the next open of a multi-day session, or any session
  with a peer-lane record, exits 1 on [3] PIN with every other gate green.
- **The ThinkPad receiver ran 18-day-old code** (`3465ac2`) through this whole session; the
  pull + restart after this push is the first deploy since 2026-08-16.
- **Codex's declared residue, retained not resolved:** 16 OWED / 1 discharged on its own
  `owed --enforce`; `.codex/` untracked; a detached S111 worktree with a modified closeout.
- **Chandra OCR 2** on disk (10.61 GB) and unloadable by marker-env's transformers 4.57.6;
  `chandra-env` unsigned; J25 waits.
- **The Gmail draft for J19: waived** by Rab 2026-09-03.
- **Widget rebuild:** GREEN — staged C4559805 (10335232 bytes), adoption is Rab's hand.

## §15 Registers touched (measured at close)

- OPEN-TASKS.md: **struck** J19, J20, J21, J24, J15, J17, J18, J22, J1 (9); **added** J24, J25,
  J28 (3) → net −6 across the session; the close's REGISTER row: `REGISTER         OPEN-TASKS.md written this session: +11 / -8 lines`.
- SYMPTOM-INDEX.md: **fixed** SYM-066, SYM-071; **filed** SYM-071 (then fixed), SYM-072;
  `DEBT             open SYM 28, unchanged since 14a526b`.
- ERROR-BIN.md: ERR-046 … ERR-051 (six rows, four classes: HARNESS-MISUSE ×2, PROBE-SHAPE ×4).
- observability/dispositions.json: +6 (`converter:blocks_engine`, `converter:latex_balance`,
  `widget:program`, `converter:chunks_resumed`, `converter:pages_flagged`; none reclassified);
  `GLASS            clean since 14a526b (--enforce; bare runs exit 0 regardless — SYM-046)`. Method lesson banked in the last one's reason: the detector diffs
  COMMITTED changes only — it read clean on E's uncommitted edit, then red after E's commit.
- LEVERS: 7 waived (`e586095`) + 3 (J1, `b42eee6`) + 1 (`FakeAnalyst.CHUNK_TARGET`, `d922eb4`);
  `LEVERS           no unlevered threshold constants added since 14a526b`.
- docs/52 (new, 9 reports + README); docs/50 M6-R1 annotated; docs/51 untouched.
- Memory library: installed-exe-state (E7C5A47A → and the staged C4559805), MEMORY.md index;
  TIME-STATE and the cookie tally move in the lockstep commit after the ledger row.
- Rust: `RUST fmt         clean` · `RUST clippy      clean` · `RUST test        clean`.
- CENSUS: `CENSUS           selftest.sh: declared 11 = FIRED 11 = banner 11/11 (exit 0)` · MEMORY: `MEMORY           clean — soft clock is DURABLE at aac24f8` · PUSH: `PUSH             ahead of upstream: 41 (the close pushes; 0 after)`.

## §18 Next entry point

1. **MUSTER.** S115 gets its own closeout file, so this file's open-date name will not trip
   `[3] PIN` — but **SYM-072 is still OPEN**: the moment a peer lane writes `sessions/S115-*` or
   S115 runs past midnight UTC, `open.sh` exits 1 with every other gate green. Read the card,
   not the exit, and repair the guard (item 6) before it costs a reconcile.
2. **Adopt C4559805** if Rab has not already: `Copy-Item` from `windows-widget\src-tauri\target\release\`
   to `AppData\Local\File Portal\`, `Get-FileHash`, expect `C4559805`. It carries F7 + the three
   `convert/blocks*` verbs on top of `E7C5A47A`.
3. **Confirm the ThinkPad deploy held:** `tailscale ssh rab@archlinux` → checkout at the pushed
   HEAD, `file-portal-converter` user-unit active. J28's manifest field is first exercised by the
   next supersede.
4. **Drop a book.** C0 (J10) is still the only item that moves the sentinel; the widget must be
   launched by hand. The next held bundle is the first with `blocks.json`, `latex_balance`, and
   both analyst events — collect them.
5. **Rab's signature queue, unchanged by this session:** SYM-067 (the gate rule), `:775`, J27,
   J23 (13 dispositions), J25 (`chandra-env`), Panel C graduation.
6. **SYM-072 repair** (open.sh [3] PIN by `claimed:` stamp, not filename) — mechanical, small,
   tripwire on all three shapes.
