# S94 · Desktop · 2026-08-17 · STAGE 1 — THE HARDENING SLICE (+ F-09 BUILT ON SIGNATURE)

## §1 Session Intent

Rab's commission, in his words: **"F-09 per-slice signed, Stage 1 GO with the mutex"**
*(given in-chat after the fourth ask landed cited — the two §3 signatures docs/37 was
waiting on).*

Read as: (a) record both signatures in docs/37 §3; (b) build F-09 per-slice (the docs/37
§4 T2 spec, deferred by S93 for exactly this signature); (c) execute Stage 1 (docs/37 §1):
CSP + devtools out of release, bless via config, spot_check / degeneration_flagged /
fixity-check receipts rendered, recent_audits rendered or dispositioned, single-instance
guard + named OS mutex around the Marker launch. ONE widget build; adoption is Rab's hand.
Exit: ritual green · SHA-8 staged · every new guard stepped on.

## §2 Starting State

`open.sh` exit 0, card verbatim:

```
════════ MUSTER · OPEN · 2026-08-17T20:55:27Z ════════
[0] IDENTITY
    ledger newest    S93  0872f22
    this session     S94  ·  desktop  ·  2026-08-17
    advisory         S94 is named in the INHERITED tree (0872f22) — read each, do not blanket-correct:
                     sessions/S93-desktop-2026-08-17.md
[1] GROUND
    verifier         1E2BE296  (repo == ~/.claude)
──────── MUSTER · 2026-08-17T20:55:28Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 73 (tally == hook)
[3a] LEDGER ORDER  ✓ 76 Desktop-lane rows ascend (S16 → S93)
[3b] LEDGER PARSE  ✓ 76/101 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 4 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79
[3] HARD clock ... ✓ 0872f22 (S93) mirrors ledger, ancestor of HEAD
    WORKING TREE . 0 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 4 · anchor 23 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=16
    gpu-lock         absent
    events           137 line(s)
    widget           20348   (Rab's post-S93 restart — the fourth ask ran on this instance)
    python procs     4
    ollama           2
    llama-server     1 — parent PID decides, never the name (S81)
    installed exe    6CA0DEF0  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 70c60e6
    thinkpad         converter=active · vault tip 70c60e6 · staging 0 · receipts 7
[3] PIN
    --since          0872f22
    closeout         sessions/S94-desktop-2026-08-17.md
════════ mechanical half: clean (exit 0) ════════
```

The S94-in-inherited-tree advisory is S93 §18's forward reference ("expect S94") — read,
not a collision. **Pinned `--since`: `0872f22`.**

Index rows intersecting today: SYM-041 (F-09 — today's build closes it), SYM-042 +
SYM-033 + SYM-032 (the mutex is their designed cover), SYM-022 (the crash the mutex
prevents), SYM-027 (recent_audits is its named specimen), SYM-020/019 (ritual order,
pinned toolchain), SYM-007 (adoption/config laws), SYM-029 (CRLF), SYM-038 (suite exit
gates commits).

Standing orders in force: one widget build, adoption is Rab's hand (SHA-8 printed, his
copy+launch) · every new guard gets its tripwire today (docs/32 §6) · verify before
instruct · the running widget (PID 20348) and Rab's model are untouchable · kill trees,
never PIDs · CPU-only except where the ritual's cargo test needs nothing from the card.

## §7 Implementation Delta

| Commit | What |
|---|---|
| `8b025c9` | session open — commission + card + pin, before work |
| `8f8c0ed` | Rab's signatures recorded in docs/37 §3.1/§3.2 (his words quoted) |
| `220e7ae` | **F-09 built** (SYM-041): `chunk_batch()` per slice; slice print/event/`.done` carry the value; banner/manifest keep run-start (T2a); batch still never gates (T2c) |
| `3446f7c` | **the card mutex** (SYM-042; SYM-022 precondition closed): `Local\file-portal-card` at `main()` dispatch, every entry; loud wait; abandonment inherited; tripwire 7/7, contention proven real |
| `ae3bd4e` | **widget hardening**: tauri.conf CSP (deny-by-default, survey-verified against every main-window behavior) · room_chat.py CSP header (the model-output surface) · devtools out of release · `tauri-plugin-single-instance` 2.4.3 · bless via config |
| `b65ef0c` | **the missing renders**: spot_check / degeneration_flagged / fixity-check phrases + the `recent_audits` Room panel (SYM-027 specimen); docs/16 row → REAL |
| *(close)* | CHANGELOG + this closeout |

Verification: fmt clean · clippy `-D warnings` clean · `cargo test` 23/23 ·
`card_mutex_selftest.py` 7/7 · `room_chat_acceptance.py` 24/24 · `node --check` clean ·
glass detector `--since 0872f22` no unsigned glitches · six-reader survey workflow before
the edits · adversarial review: 3 of 5 finder dimensions by workflow + 2 solo, findings
re-judged inline where verifiers died (§10.2) — 3 real, fixed; the review earned its keep.

Honest status per guard: card mutex **stepped on** (7/7, real contention) ·
single-instance **`Intended`** (tripwire = adoption double-launch, incl. from minimized)
· config CSP **`Intended`** (never enforced against a live webview until adoption — the
§10.2(3) label; eyes-on checks in §18) · chat-page CSP header **`Intended`** (live at the
chat server's next spawn) · renders **`Intended`** · F-09 **`Intended`** (next chunked
convert) · resume identity gate predicate **exercised** (6/6, S93).

**Build (`Observed`, the §10.2(1) rebuild): `target\release\file-portal-widget.exe`
SHA-8 `C3C05D49`, 10,254,848 bytes.** The first build `AFDB8355` is SUPERSEDED — never
adopt it (§10.3).

## §8 Decision Ledger

1. **CSP split across two surfaces**: the survey proved the config CSP governs only the
   main window (bench/chat are External-URL documents), so F-14's real surface — the chat
   page rendering model output — got its CSP as a room_chat.py response header. The bench
   prototype's server got none (quarantined `prototypes/`, outside Stage 1's bullet);
   candidate for Stage 2.
2. **Bless path**: user@host from config (the signed bullet); the staging path stays a
   literal — it is the ThinkPad repo-layout contract, not a per-machine value. The
   `by: "rab"` author string kept: blessing IS Rab's hand via the widget.
3. **Renders are render-only**: no algedonic escalation added for spot_check /
   degeneration / fixity-fail — pain semantics (terracotta = hand required) are a policy
   Rab has not signed; the survey's notes on self-retiring alarms are on file for when he
   does. Recommendation: a fixity FAIL deserves a pain arm; awaiting his word.
4. **Single-instance ships `Intended`**: stepping on it live means double-launching an exe
   beside his running widget — SYM-033's own scenario. Its tripwire IS the adoption
   ritual: launch the adopted exe a second time, expect front-not-spawn.
5. **Mutex-wait vs watcher timeout**: a watcher child that waits on a held card burns its
   21600 s backstop budget; accepted. *(§10.2 corrected this entry's first justification:
   the child's CARD BUSY prints are CAPTURED by the watcher — `capture_output=True`,
   watch_and_convert.py:165 — and reach watcher.log only at DONE/FAILED, so during the
   wait watcher.log says only "CONVERTING x". The live signals that DO exist during a
   wait: the `card_mutex` `state:wait` event in events.jsonl, which no widget surface
   renders yet, and `.gpu-lock` naming the file. Both the render and a live-wait log line
   are filed for Stage 2; the acceptance stands on the mutex's correctness, not on an
   observability claim that was false.)*

## §10 Known Failures — mine

**10.1** The mutex tripwire's first draft "proved" contention with a timer while the
child was still importing pymupdf — it tested two different mutexes and a startup delay
(SYM-001's shape, twice over: shared-assumption and proxy-measurement). Its own red
caught it; fixed with TRYING-synchronization and the name-inheritance rule, and the
defect is now named inside the selftest as a comment.

**10.2** The adversarial review workflow half-died on a session limit (3 of 5 finders ran;
13 of 19 agents failed, resets 8 pm). Two hazards inside that, both caught: (a) my
workflow script's aggregation counted a finding whose verifiers ALL died as "refuted" —
a failed probe rendered as a negative, SYM-031's shape in my own orchestration; every
unverified finding was re-judged inline instead. (b) The two dead finder dimensions
(renders, docs-truth) were covered solo. Verdicts: **6 raw findings → 3 real, all fixed
this session** — (1) the single-instance callback could not restore a MINIMIZED widget
(`set_focus` no-ops there; the one state a relaunch serves) → `unminimize`+`show`+focus,
rebuilt; (2) §8.5's "the wait is loud in watcher.log" was false prose (child stdout is
captured; corrected in place, render + live-log filed for Stage 2); (3) the config CSP
shipped as the one guard with no honest status label → now labeled `Intended`, and the
adoption checklist below carries its eyes-on checks. 2 findings refuted on live votes
(WAIT_FAILED unreachable by construction; the mutex's process-lifetime hold is inside the
signed §3.2 scope and recorded). 1 partially wrong (the bench-CSP deferral IS on the
record, §8.1) — its substance (the bench renders untrusted book content with no CSP)
stands as the Stage 2 item §8.1 already names.

**10.3** The first staged build `AFDB8355` was superseded within the hour by the §10.2(1)
fix — Rab was told to hold adoption before copying. The adoption target is the rebuild's
hash in §7/§18, and only that one.

## §15 Symptom Signatures

No new failure class. SYM-041 → built · SYM-042 → covered · SYM-033 → prevention built
(two layers, one `Intended`) · SYM-032 → residuals closed by the mutex. All dated in
place, histories kept.

## §17 Current State

`Observed at close` — Stage 1 exit criteria: ritual green (fmt/clippy/test 23/23, twice —
once per build) · SHA-8 `C3C05D49` staged (AFDB8355 superseded pre-adoption, §10.3) ·
guards stepped on where stepping is possible without creating the guarded-against
scenario (mutex 7/7; the rest carry explicit `Intended` labels in §7). Rab's widget
PID 20348 still runs the OLD exe `6CA0DEF0` — none of tonight's widget work is live
until his adoption. The python-side work (F-09, mutex, chat CSP header) is live at the
next converter run / chat server restart respectively. *(`Historical` the moment it is
written.)*

## §18 Next Entry Point

1. `open.sh` — expect S95, `--since` = this close's SHA.
2. **Rab's hand: adopt `C3C05D49`** (close the widget · copy from `target\release\` to
   `%LOCALAPPDATA%\File Portal\` · launch). Then step on the guards: minimize it, launch
   the exe a SECOND time — expect the ONE widget to restore and front, never a twin.
   Eyes on: the Room renders with its styling intact (the CSP's first live enforcement),
   the Recent audits panel and receipt phrases appear, the chat page loads and answers
   (its CSP header's first live test — the chat server respawns with the widget).
   Fable then verifies from outside: installed-exe hash == `C3C05D49`, boot log, single
   PID (the anti-S45 checks).
3. His word, still open on the register: asset posture (§3.3) · Stage 2 GO · Stage 3 GO ·
   the standing queue (docs/26 sheet, reap countersign, GLM second reader, wrapper,
   SPOT_CHECK_EVERY).
4. Stage 2 = converter tests + CI (the decision with two witnesses) — the dossier's
   negative cases are enumerated in docs/37 §1.
