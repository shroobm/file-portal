# S73 — Desktop lane — 2026-08-10

**Closeout under `docs/21`.** Tier: EXTENDED — widget source changed; an exe is staged.

## §1 Session Intent
`Observed` — plan `f9f1be6`, Rab's morning words: "sign slice 0+1, the Wall… send a gmail draft
of the manual so I can open it on my phone + everything you did this session." Cookie #58.

## §5 Observable Contract
`Intended` — Rab, after YOUR adoption (one Copy-Item + hash + launch, law 3):
1. Copy `target\release\file-portal-widget.exe` (SHA-8 **48BCB4A6**) over the installed exe,
   relaunch, switch to the Wall — **fails if** it looks like a different product instead of the
   same product built heavier (mass on the dots, deeper background, bigger verdict).
2. With a `fail` verdict or waiting Gate cards — **fails if** no terracotta bloom/underglow; with
   everything viable — **fails if** ANYTHING glows (green never blooms; that's the law).
3. Windows' reduce-motion on — **fails if** the underglow still breathes.
4. The big screen at 3–4 m — the docs/25 reel-frame test. Your eyes are the gate; slices 2–5
   wait on this verdict.

## §7 Implementation Delta
`Observed` — `styles.css`: +`--fp-*` token block (dark+light) · Wall block wears them (depth
wash, verdict clamp 48–130px + `wv-attn` bloom, keycap dots + `wl-glow` underglow + breathe,
hero clamp 38–76px + shadow mass, algedonic bloom, reduced-motion guard). `room.js`: two class
derivations in `renderWall` from existing fields (`sv.color`, `s.on`, `assay.verdict`) — zero
new numbers. Plus CHANGELOG/CLAUDE_README/this file. **Nothing else touched.**

## §8 Decision Ledger
1. **Bloom gated by semantics, not color-happiness** — `wv-attn` only when the verdict string
   is clay; underglow only Gate-with-cards / Assay-fail; Convert gets mass, never glow
   (activity ≠ summons). Rejected: currentColor bloom on every verdict (my own concept page
   did this — the charter overruled the concept).
2. **Circles stay circles** — keycap treatment via face+elevation, not shape (ISOTYPE identity).
3. **Slice 0 provably inert** — token-consumer grep is the audit; unconsumed tokens are the
   contract, not dead code.
4. **Render acceptance deferred to Rab's adoption** — the Wall needs the real backend; a
   harness screenshot without live data would be SYM-001-shaped comfort. The ritual + audits
   are the machine's half; the big screen is his.

## §9 Evidence
`Observed` — `cargo fmt --check` silent · clippy clean 23.9s · `node --check` OK · 20/20 tests ·
`npm run build` → 2 bundles · staged exe SHA-8 `48BCB4A6` · token audit: 1 consumer line, Wall
block only · Gmail draft `r1202885857142346313` created (Drive link
`1N05qD8uPi7aKqG_O0Fwfymigndx9ZIqs` + the S71–S73 report) · cookie #58 logged (58/3).

## §10 Known Failures
None found. One transient: the Gmail MCP 503'd once; retry succeeded.

## §15 Symptom Signatures
No new rows.

## §17 Current State
`Historical` — running widget still `F28C58A8`; **`48BCB4A6` staged, unadopted**. Pipeline idle;
vault `70c60e6`; held/ = 4. Unsigned: slices 2–5 (pending his big-screen verdict) · transcribe
thresholds · audit credit · the standing queue.

## §18 Next Entry Point
1. MUSTER; newest row S73, ancestor-verified.
2. **Rab adopts `48BCB4A6`** (§5), judges the Wall on the big screen → his verdict gates
   slices 2–5.
3. Standing queue unchanged (Decision 5 · the four · Valentine zones 2–4 · Cybernetics-stale ·
   Textor · parked scan-lane locate).
