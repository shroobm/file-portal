# S135 — Desktop — 2026-09-12 — SYM-115: the analyst's leaked /no_think is a think-leak rejection; the library's .gitattributes

*Record before the act (docs/28; J63's guards). Written at the open, before any tracked write.*

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

> Do another ticket, good morning!

**Standing:** nothing to Codex until lifted; the record before any tracked write (its creation and its commit are two
calls); a probe in the shared checkout is a file run by its literal path; `row_check.sh <N>` and CI's own gates before
any push; `--census` at every close.

## §2 Starting State — `open.sh`, 2026-09-12T14:22:49Z (verbatim)

```
════════ MUSTER · OPEN · 2026-09-12T14:22:49Z ════════
[0] IDENTITY
    ledger newest    S134  73d8be4
    this session     S135  ·  desktop  ·  2026-09-12
    session marker   coordination/private/session.current = S135 (guard_record / guard_git read it)
    advisory         S135 is named in the INHERITED tree (73d8be4) — read each, do not blanket-correct:
                     sessions/S134-desktop-2026-09-12.md 
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-12T14:22:50Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 86 (tally == hook)
[3a] LEDGER ORDER  ✓ 117 Desktop-lane rows ascend (S16 → S134)
[3b] LEDGER PARSE  ✓ 117/143 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ 73d8be4 (S134) == ledger 73d8be4, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 9 · anchor 30 · pending 0 · drop 0
    levers           audit=report · analyst=local · batch=8
    gpu-lock         absent
    events           341 line(s)
    open-tasks       144 item(s) · last written 5 hours ago
    error-bin        88 row(s) — read the CLASS table before you probe · last written 6 hours ago
    symptoms         115 row(s), 44 open · last written 6 hours ago
    relay            167 entries · run `gate.py status` for the board
    widget           13196
    widget autostart task 'File Portal widget (J57)' Ready — AtLogOn · auto-logon ON (Bndit)
    python procs     7
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 50896d1
    thinkpad         converter=active · vault tip 8d7cc85 · staging 0 · receipts 17
[3] PIN
    --since          73d8be4
    closeout         sessions/S135-desktop-2026-09-12.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

## §3 The pin

`--since 73d8be4` (the previous session's closing commit, from the card).

## §9 The plan

1. **SYM-115 (the ticket):** the local analyst leaked its `/no_think` control token twice into *Zero to One* and the
   chunk was cut at it (S131 §8 F2). SYM-074's `think_leak` rejection already refuses a leaked `<think>` / `</think>`
   and ships the original chunk; the `/no_think` and `/think` soft switches are the same hazard and join the same
   check — the same reason key (the key set is pinned by T17 and three asserts; a new key would break them for
   nothing). A word-boundary test keeps a URL path like `example.com/think` out of it.
2. `analyst_selftest.py`: (d) a candidate ending in `/no_think` → rejected `think_leak`, the original ships; (e)
   `/think` alone → rejected; (f) a URL containing `/think` in prose → passed (not over-broad); the hand-typed
   `22/22` banner becomes a computed count (SYM-039).
3. **S134's residue:** a `.gitattributes` in the private repo so the library's scripts stay LF on disk
   (`agent-scripts/** text eol=lf`), the index renormalised, pushed.
4. `run_gates.sh --pin 73d8be4 --converter`; the record; `close_chain.sh 135`; lockstep; §20.

## §8 Findings

- **F1 — the leak is a sibling, not a new class** (`Observed`): SYM-074's `think_leak` check refused `<think>` /
  `</think>`; the `/no_think` soft switch that cut two chunks of *Zero to One* is the same model's think-control token.
  It joins the same condition under the same reason key — `rejections`' key set is pinned by T17 and three asserts, and
  a new key would have broken them for a distinction no reader needs. `_THINK_SWITCH` is word-boundaried: `/think`
  inside a URL path is prose (case f).
- **F2 — the suite's own negative control caught my edit first** (`Observed`): SYM-074 (d) removes the guard by an exact
  text match of the ONE condition line; my condition spans two lines now, so (d) read "guard line not found" — the
  control doing its job. It targets the whole condition now, and (g) removes only the switch test and asserts the
  `/no_think` candidate passes while `</think>` still rejects. My first (g) swallowed the closing `):` with a trailing
  comment — a SyntaxError inside the exec'd copy, caught on the next run. **26/26**, the count computed (SYM-039).
- **F3 — the pre-repair body carries exactly two `/no_think`** (`Observed`, the bench backup): the two the check would
  now reject at the chunk, shipping Marker's original in their place.
- **F4 — the library, LF by attribute** (`Observed`): `.gitattributes` `agent-scripts/** text eol=lf` in the private
  repo, the index renormalised; the S134 residue paid.


## §10 Known failures / residue

- **The rejection is at the chunk, not the token:** a leaked switch costs the whole chunk's rewrite (docs/12: the
  analyst may be rejected, never edited). On a book with many leaks the analyst's yield falls; the count is on the
  `analyst/done` event's `rejections.think_leak`. A per-token strip is a design change for Rab, not a fix.
- **Not re-run on a live book** — the check is exercised by the selftest and the specimen's counts only; the next
  conversion is the live observation.
- **Owed still:** J71 (the push hook); a Readability lane over the library's briefs; Rab's calls (`audit=report`?, the
  widget outside its task, the bench server on the renamed dir, J46, J65/J67/J68).

## §15 Registers

- **SYMPTOM-INDEX:** SYM-115 → `fixed`. **OPEN-TASKS:** the dated line (no J row struck or filed). Census at close.
- **Private:** `.gitattributes` for the library.

## §18 Next entry point

1. **Open:** `open.sh`; the card should read S136. The Desk inbox first. Record through
   `agent-scripts/developing/record_open.py`; close through `close_chain.sh`.
2. **Next ticket:** J71 (wire `row_check.sh` into `guard_git.py`'s push path, with a planted three-cell row as the
   case), or the Readability lane over the briefs — Rab's pick.
3. **Standing:** nothing to Codex until lifted; the record before any tracked write (two calls); a probe in the
   shared checkout is a file by its literal path (tripped once again this open); `run_gates.sh` and `row_check.sh`
   before any push; `--census` at every close.


## §7 Work log (UTC)

- **14:23** `open.sh` clean (S135, pin 73d8be4). This record.
- **14:2x–14:4x** Rab on the Desk: "Do another ticket, good morning!" — answered. `_THINK_SWITCH` + the condition;
  cases (d)–(g); the control's target and my `):` slip (F2); 26/26; ruff clean; `run_gates.sh --pin 73d8be4
  --converter` green. SYM-115 fixed; the register line; the private `.gitattributes`.
- **14:27** the content commit; `close_chain.sh 135`; lockstep; §20.
