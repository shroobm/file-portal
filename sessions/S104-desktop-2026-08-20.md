# S104 · Desktop · 2026-08-20 · THE TEXT-DENSITY VETO, AND THE HANDOFF

⟨claimed: Fable · S104 · 2026-08-20⟩

## §1 Session Intent

Rab's commission, in his words: **"Fix P-1's text density veto, and then can we carry context
into the next session, so muster then circle as the session start."**

Aligned reading, two parts:

1. **Build the veto** that makes P-1's number mean something. Today it reports coverage 0.1147
   on *Investment Valuation* — 625 "uncovered" pages that are dominated by shaded callout boxes
   and table grids, not lost figures (p45 was rendered and read: a text sidebar whose box fill
   clustered into a 277,307 pt² "figure"). The veto must separate **prose-shaped** text from
   **label-shaped** text, because the naive "contains text" test would destroy the one true
   positive we have — Cybernetics p84 is a flow diagram *full of* short labels.
   **Acceptance is fixed in advance, before any threshold is chosen:** p45 must be vetoed,
   p84 must survive. A veto that cannot pass both is not shipped.
2. **Carry context into the next session**: a handoff that makes S105 open **muster → circle**,
   with the circle's commission written out rather than left to be reconstructed. This answers
   his own decision from the previous exchange — he chose the rigorous route over my
   self-report for the systemic question about S97–S103.

*(This section is written before work and is not edited afterwards.)*

## §2 Starting State

`open.sh` clean: ledger newest S103 `6466262` · this session S104 · SOFT 76 · HARD mirrors
ledger, ancestor of HEAD · origin in sync. **Pinned `--since`: `6466262`.** Relay: nothing new
for Fable. `docs/44` verified free at filing. Noted for the handoff: **`/circle` is a
user-level skill, not vendored in this repo** (`.claude/skills/` holds only `echo` and
`muster`), so it travels with the account, not the clone.

## §3 Work Plan

1. This closeout; commit the open.
2. Build the veto in `figure_coverage.py`; measure the two known specimens FIRST (p45 must
   veto, p84 must survive); only then choose thresholds.
3. Re-run the whole corpus; report the before/after honestly, including whatever it costs.
4. Extend the selftest with veto cases, both directions.
5. Write `docs/44` — the handoff + the circle commission for S105.
6. Close with `close.sh 6466262`; push; observe CI.

## §4 Scope and Evidence Boundary

- Writes: `windows-converter/figure_coverage.py` + its selftest, `docs/44` (new), docs/37,
  docs/41 §2 P-1, CHANGELOG, this file.
- **The module stays unwired and report-only** — P-1's host variable is still Rab's, and
  nothing here changes a verdict or a manifest.
- No GPU, no pipeline run, no vault write. Any measurement that runs long gets a hard cap and
  is killed rather than left running (S102's 28-CPU-minute lesson).

## §5 Sections Deferred to Close
