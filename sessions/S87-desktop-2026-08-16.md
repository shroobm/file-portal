# S87 · Desktop · 2026-08-16 · LATE EVENING — THE CORPUS WIDENS, THE MAPPINGS LAND

## §1 Session Intent

Rab's commission, in his words:

> "Signed, add it and measure the fit, should I close the widget? Also finish the WIP that
> thinkpad needed you to do, because the job was to provide a deliverable once you collected
> its information and your side, into several mappings, im not sure if you got that in your
> memo from it."

Read as three things: (1) **docs/36 joins the assistant's corpus** — signed — with the fit
MEASURED against the 16k context before it ships (S85 §10.2's lesson, applied forward);
(2) the widget-restart question answered honestly from source, not recall; (3) **the handoff's
item 8 finished to its actual specification** — the deliverable is SEVERAL MAPPINGS merging
the ThinkPad's collected knowledge with the Desktop's, not the prose skeleton v1 filed.
Context he carries from earlier tonight: his first live assistant question ("what is this
program about, what should an engineer know") was correctly WITHHELD for want of a citable
source — docs/36 is that source, born after the corpus was signed.

**Written before work. Left unedited for the rest of the session** (docs/21).

## §2 Starting State

`open.sh` exit 0: S87 · `--since` = **`6a3a7dc`** · clocks clean · **origin in sync** (the
S86 guard's first routine pass). Live: **installed exe `6CA0DEF0` — RAB ADOPTED tonight**,
widget 2628 running, python procs 4, ollama 2, llama-server 1 resident (his loaded model —
name-is-not-identity caveat, S81); pipeline calm (held 4 · drop 0 · pending 0); gpu-lock
absent; vault 6 notes tips matched. Advisory: S87 in the inherited tree is S86 §18.1's
forward reference — benign.

**Inherited from the post-S86-close chat (the addendum this session must FILE):** SYM-007's
4th firing — S85 keyed the live config.toml from the packaged surface, the write virtualized
into the app container (probe-proven this evening: this surface's %APPDATA% writes redirect;
the mirage copy carries the key, mtime 02:48:21), the real Roaming file never got it, Rab's
hand re-inserted, Assistant button then CONFIRMED live by him. Also: Notepad's off-screen
registry position (X=−1920, dead-monitor coords) diagnosed; the one-liner fix was given to
Rab — application UNCONFIRMED.

## §3 Pinned closeout terms

Standard core sections; every file in `git diff --name-only 6a3a7dc..HEAD` accounted;
`glass_detector.py --since 6a3a7dc` (uv interpreter); ledger row as separate follow-up
commit; clocks in lockstep; the close pushes (origin row to zero).

## §4 In-scope symptom rows

- **SYM-007** — this session files its 4th firing on the row itself; the config-mirage rule
  is the session's inherited fact, not a new observation.
- **SYM-022/032** — llama-server is RESIDENT (Rab's live assistant). Tonight's measurement is
  CPU-only (`llama-tokenize.exe`); NOTHING this session launches touches the card.
- **SYM-039** — the mappings must cite commands for derivable numbers, not restate counts.
- **SYM-036** — room_chat.py edits go through the Edit tool.

## §5 Standing orders

In force as S86 stated them; specifically tonight: one lab process on the card (and the one
resident process is RAB'S — untouchable), a status sentence carries its probe, config.toml is
Rab's-hand/widget-verified territory (this session's own §2 inheritance).
