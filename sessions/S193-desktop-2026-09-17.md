# S193 — Desktop — 2026-09-17 (15:41Z) — HIS WORD: THE INSTITUTION IN A FOLDER — "prototype-institutions … the full thing … auto persistent … complete from top to bottom. nothing missed." (Claude Opus 5's fortieth sitting in the Fable lane; the autonomy experiment's forty-eighth)

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

In CHAT, 2026-09-17 ~15:30Z, after the chat he asked for at 14:08Z (S192 §20):

> "If you were to make a folder environment, and then set all of the agents environment, so that you can just copy that folder environment, put that as a folder project in claude code, and anyone can start developing from there? A contained nested institution, that needs a program and an agent to occupy inside."

and, after the lane's design and its four-question echo (where it lives · constitution vs bylaws · memory in the folder or global · who signs in a copy):

> "prototype-institutions. I need you to do the full fucking thing im so sorry. auto persistent, and it to be the complete from top to bottom. nothing missed. before you do, partial closeout, compact, then do the task at hand. Thank you."

Read as: (1) the place is `prototypes/` — the name `institutions`; (2) the whole design, built, not a sketch — every part the S192 harvest and the design answer named; (3) run as the persistent loop (episodes, receipts, partial closes, the carry-over — through compactions); (4) FIRST a partial closeout of the chat window and the plan written for the compaction, THEN the build. The three unanswered echo questions fall to the lane's stated defaults (the memory in the folder with the card verifying the pin; the A-list laws immutable in a copy, the rest amendable by a signed row; the human's contract a template with a solo-developer default) — said here, his to overrule. His earlier words stand (AUTO MEANS NEVER IDLE; the standing grant; "if you messed up your first priority is to fix it"; the proposal form; the GPU law; scripts never public).

## §2 Starting State — the `open.sh` card, verbatim (2026-09-17T15:41Z)

```
════════ MUSTER · OPEN · 2026-09-17T15:41:20Z ════════
[0] IDENTITY
    ledger newest    S192  7541156
    this session     S193  ·  desktop  ·  2026-09-17
    session marker   coordination/private/session.current = S193 (guard_record / guard_git read it)
    advisory         S193 is named in the INHERITED tree (7541156) — read each, do not blanket-correct:
                     sessions/S192-desktop-2026-09-17.md 
[1] GROUND
    verifier         07BD3BA7  (repo == ~/.claude)
──────── MUSTER · 2026-09-17T15:41:21Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 270 (tally == hook)
[3a] LEDGER ORDER  ✓ 175 Desktop-lane rows ascend (S16 → S192)
[3b] LEDGER PARSE  ✓ 175/201 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · unchanged since the close 7541156 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ 7541156 (S192) == ledger 7541156, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 12 · anchor 32 · pending 0 · drop 0
    intake           waiting 0 · oldest 0s · active null · written 1s ago by pid 13256
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    events           544 line(s)
    open-tasks       98 item(s) · last written 2 hours ago
    error-bin        110 row(s) — read the CLASS table before you probe · last written 6 hours ago
    symptoms         135 row(s), 21 open · last written 2 hours ago
    relay            208 entries · run `gate.py status` for the board
    widget           10056
    widget autostart task 'File Portal widget (J57)' Running — AtLogOn · auto-logon ON (Bndit)
    task             'File Portal desk (relay)' Running LASTRUN=2026-09-15T22:51Z RESULT=267009
    task             'File Portal relay watch (Fable)' Ready LASTRUN=2026-09-15T22:50Z RESULT=0
    task             'File Portal tracker' Running LASTRUN=2026-09-15T22:51Z RESULT=267009
    task             'File Portal desk watch' Running LASTRUN=2026-09-15T22:51Z RESULT=267009
    python procs     9
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    adoption         C4559805 (2026-09-04T02:3xZ, by rab) · installed C4559805 — MATCH
    vault            7 note(s) · tip f489189
    thinkpad         converter=active · vault tip bf3a0be · staging 0 · receipts 31
[3] PIN
    --since          7541156
    closeout         sessions/S193-desktop-2026-09-17.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

**The pin:** `--since 7541156` (S192's closing SHA; the row `b22c3b1`; CI green run 35231531064 / num 539). The marker = S193. The card's `1 uncommitted` is the untracked `.codex/`; the card idle, `.gpu-lock` absent; the watcher the lane's (writer 13256). No record exists under S193 before this file.

## §3 Inherited, stamped Historical — the chat window (14:16Z → 15:41Z) had no record; it is closed here

- **The chat (Historical, from this window's own transcript):** he asked how things were going, how efficient the loop had been, what impressed the lane, which agent system it preferred, the project's value inside and outside the market, then commissioned a comprehensive list of what to reproduce elsewhere. The lane answered from files, not memory: `sittings/S192/chat_efficiency_probe.py` (52 sittings S141–S192, 5,290 USD-eq, 18,930 calls; ~180/sitting the first twenty, ~53/sitting the last thirty-two; 68 register rows struck vs 4 added; 13 SYM rows stamped fixed since S141; 63 Desk posts from him); the plan meter read (Weekly Fable 96 %, all models 77 → 79 %); one web search for the parsing market's prices; and **the harvest workflow** `wf_97131da7-067` (eleven agents — seven read-only readers, three evaluators, one critic; 304 tool uses; 2.18 M subagent tokens; 26 min): **398 candidate practices, 232 keep · 140 adapt · 26 leave, every citation re-opened by an evaluator (398/398); the critic's 21 missing, 34 too-bespoke, 53 duplicates** — the full list `sittings/S192/TAKE-WITH-ME.md` (660 KB), the digest `chat_take_with_me_digest.txt`. No receipts were cut for the chat window (the S192 close's whole receipt ended 14:05:11Z); **this sitting's first episode receipt is tiled onto that end and therefore CONTAINS the chat's calls — said so it is not mistaken for the build's cost.**
- **S192's post-close CI** (0a8420e): GREEN, run 35232262059 / num 540 — and the observer's LOG lines: the same three governance suites red (circle, coordination, muster), seven clean.
- **The index (Observed 15:41Z):** MEMORY.md `c2dc7ac` at 17,443 of 17,510 — 67 bytes; **the fold at this sitting's E0**.
- **The apparatus (in force):** the record before any act; two calls at the open; a script by path; the Edit/Write tools over heredocs; `git add` by NAME beside `.codex/`; the clock read before a heading; both gates before the chain; the per-episode costs read from the receipts; the subagent law for every agent (GROUND · deviation-is-the-report · the tag law · a negative control · residue); scripts NEVER public; the GPU law (nothing here touches the card); a plan file before a compaction, a section after.

## §4 The echo — THE BUILD, complete

**Where (Observed `prototypes/`: real dirs beside JUNCTIONS into the private repo — `moonlight-phone`, `proc-feed`):** the institution is built in the PRIVATE repo at `C:/Users/Bndit/Projects/file-portal-private/institutions/` and junctioned as `prototypes/institutions` in the public repo — the lane's tooling is what it generalises and "scripts should NEVER BE PUBLIC" (S130) holds until his word publishes a copy. A copy is `cp -r` of the private folder.

**What (the design he read, every part):** `CLAUDE.md` (the constitution — the A-list laws, the MUSTER, the tag law); `HUMAN.md` (the principal's contract — template + solo-developer default); `OCCUPANT.md` (the seat); `MACHINE.md` (generated by probes at bootstrap, grows only from incidents); `.claude/settings.json` (hooks: SessionStart → the open card; PreToolUse → the git guard, the record-first guard, the error-family advisories; PostToolUse → the byte/CRLF probe) + `.claude/skills/` (muster: open · close · selftest; echo: the proposal form; circle) + `.claude/hooks/` (guard_git · guard_record · error_families — generalised, no absolute paths, the repo root derived); `registers/` (LEDGER.md with lanes and the occupant column; SYMPTOMS.md with the FIRST-marker rule; ERRORS.md with families; TASKS.md strike-or-add; dispositions.json; schemas.json generated); `records/` (sessions/ from a template; journal/; JOURNAL.md; CARRY-OVER.md); `memory/` (MEMORY.md as pointers + the TIME-STATE line, a single machine-readable line; git-tracked); `observability/` (glass_detector · lever_census · schema_registry · promise_audit, reading the tenant's manifest); `tools/` (script_journal · baseline · lockstep · close_ep · ci_observe · id_preflight — generalised); `tests/` (the institution's OWN selftests, positive + negative controls, the runner list — and `bootstrap.sh` refuses to write S1 until every tripwire fires); `docs/` (the rationale per law, numbered, each citing its incident — generalised from 21, 28, 29, 32, 34, 47, 58, 63, 18); `workload/CONTRACT.md` + `workload/institution.toml` (the tenant's manifest: lanes/producers/renderers, suites, lever files, the scarce resource and its lock, the consequential acts, the events); `bootstrap.sh` ("muster init": names the lane, pins the memory, generates MACHINE.md, runs tests/, writes S1's record and the first row); a **light close** for sittings that touched no code; a `README.md` (how to copy, occupy, and grow it — the incident → row → guard → doc loop). **Left out by design, each with its replacement named in the README:** the reward token; the byte-capped narrative index and its folds; the two-model relay bus and stamps (an opt-in module); the Desk envelope; the machine quirks (MACHINE.md's).

**How (the persistent loop, through compactions):** E0 the fold; E1 the skeleton and the constitution (CLAUDE.md, HUMAN.md, OCCUPANT.md, the README, the CONTRACT, the layout) — the record before it; E2 the registers and record templates; E3 the hooks and skills generalised, with their selftests (a workflow may write files ONLY under `institutions/`, one agent per subtree, in a worktree or with disjoint paths; every agent under the subagent law); E4 the observability layer and tools generalised with their selftests; E5 `bootstrap.sh` + `tests/` — the copy proved on a throwaway copy of the folder in a temp dir (the negative control: a broken copy refuses S1); E6 docs/ and the light close; E7 the reference tenant: this converter's manifest as `workload/institution.toml.example`, the contract proved against it (read-only on the converter). Each episode: the record before, the receipt after, a cookie, a Desk line; the close through the tool; the carry-over; **`sittings/S193/PLAN.md` written BEFORE the compaction and re-read after it — the state the next context needs.** **Not this sitting:** any change to the converter, the pipeline, the widget; publishing the folder (his word); the estimator; the lever.

## §5 The method

Episodes with receipts (`close_ep.py 193 <K> <since> "<category>"` — the first `<since>` = S192's whole receipt END 2026-09-17T14:05:11.038Z, which folds the chat's calls into E1's receipt — said), a cookie per receipt (E0 none), a Desk line per episode, the plan file before the compaction; the close through the tool (both gates first); the block ≤ ~330 bytes.

## E0 — the fold (15:55Z; a library commit, no cookie; its calls fall in E1's receipt)

Two folds by `compact_memory.py`, one check: the S192 line into the `S192–S171` line (the MUST adds `7541156 b22c3b1 THE CI READER READS THE LOG 3 of 10 platform-shaped take a break and chat`; -139) and **the S117–S104 line cut by judgment** (the phrases dropped live in the registers and records — J34, the fleet-worktree rule, VW-E2-R2/E3, "Codex verifies, Fable repairs", the SIGN-SHEET path; the fourteen kept are in the check's KEPT list; -189): **bytes 17,115 (was 17,443) · room 395 · missing 0 · below `## General` unchanged True** (`e0_fold_check.py`, baseline `c2dc7ac`). Library `953d3ca`, pushed. The compaction happened between the plan file (777064d) and this section; `PLAN.md` re-read first, as it says.

## E1 — the institution's skeleton and constitution (15:56Z; the record before the writes; nothing of the converter touched)

**The place (Observed):** `prototypes/moonlight-phone` and `prototypes/proc-feed` are symlinks/junctions into the private repo and `.gitignore` lines 110–112 (S130) ignore the three junction paths — "Never tracked here, whatever a future close does". The institution follows that rule: built at `file-portal-private/institutions/`, reached here through a junction `prototypes/institutions` (`mklink /J` from PowerShell, a `.ps1` by path), and **the junction path appended to `.gitignore` under a dated S193 comment** — the one public write of this episode besides this record. `git check-ignore -v prototypes/institutions` is the tripwire; `git status` after the mklink is the reading.

**The writes (private, under `institutions/`):** `README.md` (copy · occupy · grow; the incident → row → guard+tripwire → doc loop; what was left out and its replacement); `CLAUDE.md` (THE CONSTITUTION — the A-list laws numbered, each with the incident that made it as provenance; the MUSTER; the tag law as an admission gate; the amendment rule: the constitution is immutable in a copy, the bylaws are signed ledger rows); `HUMAN.md` (the principal's contract — the template and the solo-developer default); `OCCUPANT.md` (the seat); `workload/CONTRACT.md` (what the institution needs from a tenant and promises back); `workload/institution.toml` (the manifest template, every table commented); the folder skeleton (`.claude/{skills,hooks}`, `registers/`, `records/{sessions,journal/{calls,cost,usage,code}}`, `memory/`, `observability/`, `tools/`, `tests/`, `docs/`, `workload/`) with a `.gitkeep` where a directory would otherwise be empty. **Not in E1:** the hooks, skills, observability, tools, tests, docs, bootstrap (E2–E7). Every law's provenance is an S-number, a SYM or an ERR id of THIS repo, written from the harvest (`sittings/S192/TAKE-WITH-ME.md`, 398 practices, each citation re-verified by an evaluator) — Historical by the harvest's date, 2026-09-17.

**Written and read back (Observed, `e1_after.txt`):**

```
16:01Z  the junction: prototypes/institutions -> file-portal-private/institutions (mklink /J; Directory, ReparsePoint)
        .gitignore:121 prototypes/institutions/  (git check-ignore -v on a probe file and on CLAUDE.md: the rule fires; git status shows only .gitignore and the record)
        the constitution CLAUDE.md: 18 sections (§0 the MUSTER … §16, the close), 203 lines, 13,933 bytes, LF
        HUMAN.md 65 lines (the seven kinds; the signature rule; the budget line; the solo-developer default)
        OCCUPANT.md 50 lines (declared never guessed; ten nevers; the change of occupant; the reward token left out)
        workload/CONTRACT.md 50 lines (eleven tables → their readers; eight promises back; the proof with its negative control)
        workload/institution.toml 61 lines (every table commented; a missing table reads UNREAD, never clean)
        README.md 104 lines (copy · occupy · grow; the left-out table with seven rows and their replacements; the layout)
        the skeleton: 18 directories; .gitkeep in the five that would be empty
        line endings: every institution file LF (the manifest's [records].line_ending = "lf"); the source repo's records stay CRLF
        nothing of the converter, the pipeline, the widget, the card or the vault read or touched
```

**E1 closed (16:02Z):** receipt `journal/*/S193-ep1.md` — window 14:05:11Z → 16:01:54Z (contains the chat window and the eleven-agent harvest, said in §3): **71.51 USD-eq (session 31.18 + fleets 40.33) · 83 calls · denied 2 (both guard_git's: the chained commit at the open; a `$PY` command head) · errors 0**; cookie #271 (library `b8b2f25`); the Desk line a80773e3; private `213dfa1` + `8a5fe18` (`.gitattributes` eol=lf — git's own autocrlf warning at the E1 commit was the reading), public `1547dec`.

## E2 — the registers, the record templates, the memory index (16:04Z; the record before the writes)

**The shapes generalised (read this sitting — `SYMPTOM-INDEX.md` lines 1–20, `ERROR-BIN.md` §A the CLASS table lines 115–151, `OPEN-TASKS.md` lines 1–40, `CLAUDE_README.md` lines 1125–1130 and its newest rows):** `registers/LEDGER.md` — the HARD clock: `| date | lane | S<N>: ⟨occupant⟩ summary | records touched | SHA |`, one row per sitting, a SEPARATE commit after the closing commit, never amended, a bylaw row for an amendment (the constitution's amendment rule) and for a signature (HUMAN.md §3); `registers/SYMPTOMS.md` — retrieve by what the system is doing; the FIRST marker of the status cell is the status; `Was:` opens superseded text; ids never reused; `registers/ERRORS.md` — the occupant's own mistakes, retrieved by the SHAPE of the mistake; the CLASS table (the 24 families the source register holds, carried by name and one-line trigger + rule, without the source rows — the rows are that project's evidence, the classes travel); `registers/TASKS.md` — strike-or-add; the tag table (Observed-dated / Historical / SEMANTIC needs the human / MECHANICAL); the header rule; `registers/dispositions.json` — the skeleton (`_README`, `lanes` from the manifest, `dispositions` empty); `records/sessions/TEMPLATE.md` — the opening sections (§1 verbatim intent, §2 the card, the pin, §3 inherited Historical, §4 the echo, §5 the method) and the closing (E-final gained/lost/missing, §7 built, §8 findings, §10 residue, §15 current state Observed, §18 next entry point, receipts, §19 the chain, §20 post-close); `records/JOURNAL.md` — the human-readable journal's five headings; `records/CARRY-OVER.md` — the next wake's first act (the template); `memory/MEMORY.md` — the pointers + ONE machine-readable TIME-STATE line `<!-- TIME-STATE lane=<lane> session=S<N> sha=<sha> -->` (parsed by one regex in open.sh and lockstep.sh — E3/E4) + the standing-rules index; `memory/session-bootstrap.md` (the MUSTER's long form), `memory/laws-short.md` (the constitution's sixteen laws in one line each, for recall), `memory/human-standing-facts.md` (a pointer, theirs). **Not here:** `schemas.json` (generated at E4 by the registry). Every file LF; nothing of the converter touched.

**Written and read back (Observed, `e2_after.txt`):**

```
16:08Z  registers/LEDGER.md 31 lines (the rules in order; bylaw rows; the five-column header, no rows yet — bootstrap writes S1's)
        registers/SYMPTOMS.md 31 lines (the FIRST-marker rule; ids never reused; UNPLACED until a family; step on a fixed row's guard)
        registers/ERRORS.md 97 lines: 26 classes (the source's 24 + EDIT-UNDER-EXECUTION + TYPED-NOT-READ) grouped in 9 METHOD families; 16 MECHANISM families; §C rows empty; §D how it stays true
        registers/TASKS.md 34 lines (the tag table; the strike-or-add rule; three rows ADDED at bootstrap: T-001 HUMAN slots, T-002 the real lanes, T-003 the runner list)
        registers/dispositions.json parses (the six dispositions defined; lanes ["example"]; dispositions {})
        records/sessions/TEMPLATE.md 84 lines (§1 §2 pin §3 §4 §5; the episode shape in a comment; E-final §7 §8 §10 §15 §18 receipts §19 §20)
        records/JOURNAL.md 25 lines (the five parts; the S1 opening entry as the template)
        records/CARRY-OVER.md 21 lines (the nine slots)
        memory/MEMORY.md 23 lines; the TIME-STATE line reads lane=unbootstrapped session=S0 sha=0000000 (one regex, bootstrap rewrites it)
        memory/session-bootstrap.md 44 · laws-short.md 25 (sixteen laws + §0) · human-standing-facts.md 19 (eight empty slots)
        every file LF; nothing of the converter touched
```

**E2 closed (16:09Z):** receipt `journal/*/S193-ep2.md` — window 16:01:54Z → 16:08:37Z: **5.13 USD-eq · 37 calls · denied 1 (guard_git on a `find` that mentioned git) · errors 0**; cookie #272 (library `912b589`); the Desk line c6ac15c6; private `c8ecd99`, public `8ca6ba3`. **A correction by append:** the public E2 commit's message names "private 9d4e…" — a typed placeholder, TYPED-NOT-READ; the private SHA is `c8ecd99`. The rule from here: the private half commits first and its SHA is read into the public message by `git log -1`.

## E3 — the hooks and the skills, generalised, each with its tripwire (16:12Z; the record before the writes)

**The method:** the git guard is 899 lines of battle-tested grammar (J56, J64's eleven false-deny shapes, SYM-110's twelve, J63, J71, S141's holes) and its selftest 397 lines; they are **ported by a script with asserted replacements** (`sittings/S193/e3_port_hooks.py`), not retyped: the project constants become the institution's (`coordination/private/` → `.claude/hooks/state/` for the log, the trace and the roots; the session marker → `records/.session.current`; the record glob → `records/sessions/S<N>-*.md`; "is this a guarded checkout" → `workload/institution.toml` exists; `FP_` → `INST_`; the row reader → `tools/row_check.py` on `registers/LEDGER.md`; the LAW string → the constitution's section); the docstrings rewritten to name the institution's docs. `guard_record.py` and its selftest the same way. **Written fresh:** `tools/row_check.py` (row_check.sh's three rules in Python: no unparsed row in the last five, the newest lane row is S<N> and unique, its SHA a commit and an ancestor of HEAD; exit 0/1/2), `.claude/hooks/error_families.py` (the advisory hook — warn_heredoc's signed over-sensitivity for heredocs with escapes and `python -c` with a backslash, plus `sed -i` on an append-only path, a pipe after a gating command, `git add -A`; and `--census` / `--place` reading the CLASS and FAMILY tables of `registers/ERRORS.md` instead of a hardcoded dict), `.claude/hooks/byte_probe.py` (PostToolUse on Write|Edit: the file's CRLF/LF count against the manifest's `[records].line_ending` for the append-only paths — a reading, never a block), `.claude/settings.json` (SessionStart → open.sh; PreToolUse Bash|PowerShell → guard_git; PreToolUse Write|Edit|MultiEdit|NotebookEdit → guard_record; PreToolUse Bash → error_families --advise; PostToolUse Write|Edit → byte_probe; the interpreter `python3` with `INST_PYTHON` as the override — no absolute path), the skills (`muster/SKILL.md` the judgment half, generalised; `open.sh`, `close.sh`, `selftest.sh` ported from the source's 510/596/1198 lines with the manifest as the source of every path; `echo/SKILL.md`; `circle/SKILL.md`; `close-light/SKILL.md` + `close_light.sh`), and `tests/` for each. **The coordinator runs every selftest itself** and pastes the counts. The source hooks are read-only.

**Written and proved (Observed, `e3_after.txt` — every count from the suite's own tally line):**

```
16:42Z  HOOKS (ported by e3_port_hooks.py — asserted replacements: guard_git 9, guard_record 3, guard_git_selftest 19, guard_record_selftest 9)
        tests/guard_git_selftest.py       178/178 green · fired 178 / skipped 3 / silent 0   (the REAL-checkout cases SKIPPED: the folder is not its own repo here; a bootstrapped copy is)
        tests/guard_record_selftest.py     16/16 green
        .claude/hooks/error_families.py --selftest   13/13 green · --census: classes 26 · METHOD 9 · MECHANISM 16 · ERR rows 0 · SYM rows 0
        .claude/hooks/byte_probe.py --selftest        8/8 green
        .claude/settings.json parses (SessionStart → open.sh; PreToolUse Bash|PowerShell → guard_git; Write|Edit|MultiEdit|NotebookEdit → guard_record; Bash → error_families; PostToolUse → byte_probe; INST_PYTHON or python3, no absolute path)
        SKILLS
        muster/open.sh on the folder itself: CLEAN (GROUND 4 hooks parse; IDENTITY 0 rows → S1; CLOCKS S0/S0; ORIGIN skipped; LIVE gpu lock absent, events UNREAD; REGISTERS 3/0/0 rows; PIN S1 record absent) — the first run found TWO lines naming TIME-STATE (memory/MEMORY.md's prose was a decoy); reworded, the count is 1
        tests/open_selftest.py             20/20 green · fired 20 / skipped 0 / silent 0   (case 0 the positive control; lag / fork / mismatch / stale / decoy / blind / unparsed row / lane rule / order / missing register / FIRST marker / advisory / a hook that does not parse / UNREAD events)
        tests/close_selftest.py            16/16 green   (register censuses STRUCK/ADDED; untouched → RED; uncommitted → RED; a red suite warn-only vs ARMED; SKIPPED as a third tally; LEVERS named vs waived; DEBT +1; unresolvable pin; missing register UNREAD+red)
        tests/row_check_selftest.py        12/12 green   (three cells in prose; a duplicate sitting; not a commit; another branch; a BYLAW row never selects; another lane; usage 2; CONFIG 2; ledger_parse's tally)
        muster/close.sh on the folder (pin 3201084, e3_close_run.txt): DIFF 0 files · 4 modified uncommitted (the E3 skills, RED — committed next) · GLASS/SCHEMA UNREAD (E4) · RUNNER fired 9 / red 0 / skipped 12 (the E4–E5 suites, each named) · LEVERS none · PROMISES UNREAD (no promise event) · REGISTER RED (untouched since the pin — true; the institution's TASKS.md moves at E5) · DEBT 0 → 0 · MEMORY 1,666 bytes · PUSH ahead 1 · CI UNREAD; the runner alone takes > 2 min (the guard selftest's 178 subprocesses)
        echo/selftest.sh 6/6 (the no-hit term assembled at run time so the selftest does not match itself — the S188 liveness-probe trap) · circle/selftest.sh 8/8 (the three imported rules; HELD absent; the subagent law's five parts; a negative control)
        close-light/close_light.sh parses (exit 3 when code changed; the four owed rows; the six sections by heading; the commit by name; the row; row_check; the push; the lockstep) — ITS SELFTEST OWED TO E4 beside tools/lockstep.sh
        every file LF (byte_probe); nothing of the converter touched
```

**E3 closed (16:45Z):** receipt `journal/*/S193-ep3.md` — window 16:08:37Z → 16:44:31Z: **23.17 USD-eq · 104 calls · denied 0 · errors 0**; cookie #273 (library `ba18495`); the Desk line cc42a31f; private `6f27d82` `3201084` `e72241d`, public `b669662`.

## E4 — the observability layer and the tools, generalised (16:46Z; the record before the writes)

**The method:** the source's five observability modules (579 · 197 · 968 · 141 · 425 lines) and eight tools (386 · 343 · 25 · 77 · 86 · 140 · 117 · 30) are converter-shaped in their lanes, writers and answer keys; the institution's are **written fresh against the manifest** — smaller, generic, each with its `--selftest` (a positive control, a negative control, UNREAD on a failed read) — and the source's rules carried by name: `observability/glass_detector.py` (`[[lanes]]` producers → keys harvested from dict literals and subscript writes; renderers searched for each; `--since <pin>` scopes to lines ADDED; `registers/dispositions.json` signs silence; **`--enforce` is the only form whose exit means anything**; a producer glob matching nothing is a lane that passes forever — a red), `lever_census.py` (the AST census of numeric literals in compare / default / dict / sequence positions in added Python lines; tests exempt; the `# lever-waiver:` form; `--count`), `schema_registry.py` (`--write` the producers' key sets to `registers/schemas.json`, `--check` drift → exit 1; a registry generated from source is regenerated, never edited), `promise_audit.py` (any event stream: the promise event and its two fields from `[events]`; the last N; median, within 2×, the worst named; UNREAD when absent), `acceptance.py` (the answer key: every disposition's reason names a file that exists and, for GLASS, a renderer that carries the key); `tools/script_journal.py` (the receipts: calls by tool, denies against the guard log, errors, the window, the cost from the harness transcript when `CLOSE_TRANSCRIPT` names one, else UNREAD — never 0), `baseline.py` (the per-sitting row), `lockstep.sh` (the TIME-STATE line from the newest row; memory committed; HARD an ancestor of SOFT verified), `close_chain.sh` (code-notes check → the gates → close.sh → the closing commit → the row → row_check → push → ci_observe), `close_ep.py` (an episode's receipt tiled onto the previous window's end), `ci_observe.py` (the run for a SHA via the GitHub API with `gh` or a token; every job/step; each continue-on-error step's LOG read from the logs zip: exit N / clean / UNREAD), `id_preflight.py` (the next free SYM-/ERR-/T- id; a forward reference anywhere in the tree is TAKEN), `compact_memory.py` (the fold with its read-back check), and `close-light/selftest.sh` (owed from E3). Read-only on the source; nothing of the converter touched.

**Written and proved (Observed, `e4_after.txt` — every count from the suite's own tally line):**

```
17:04Z  OBSERVABILITY (fresh, manifest-driven; the source's harvester logic carried: dicts that leave a function, Rust pub fields / serde renames, JS object literals)
        observability/glass_detector.py --selftest     8/8   (a rendered key clean; a returned key unrendered → GLITCH; a lookup table not harvested; an EMPTY GLOB is a red; a signed key SIGNED; --since scopes to ADDED lines; an unresolvable --since UNREAD 2; no manifest UNREAD 2; and — found by running it on the folder — NO [[lanes]] reads UNREAD 2, never clean)
        observability/lever_census.py --selftest       9/9   (compare / default / keyword / dict / sequence; trivial 0 1 -1 2 not counted; a waived line; tests/ exempt; --count; UNREAD 2 — the fixture's tally typed 5, the instrument said 6: 3 in a sequence counts; corrected to the instrument)
        observability/schema_registry.py --selftest    5/5   (--write; no drift after; a renamed key is DRIFT with + and -; an orphan read; a missing registry UNREAD 2)
        observability/promise_audit.py --selftest      5/5   (n, median, within 2×, the worst named; unreadable rows counted; --last; absent stream UNREAD; no event UNREAD — never 1.0)
        observability/acceptance.py --selftest         7/7   (a GLASS entry flips with the tree when the renderer drops the key; unknown word; a 3-char reason; a lane not in the manifest; a home that does not exist; missing json UNREAD)
        TOOLS
        tools/lockstep.sh + tools/lockstep_selftest.py 8/8   (the ONE line rewritten; memory committed; both clocks printed; open.sh CLEAN after; no row → red, nothing written; a decoy line → red; a foreign SHA → red after the verify; usage 2)
        tools/script_journal.py --selftest             7/7   (calls by tool in the window; a deny cross-checked against the guard log; errors; the cost from tools/prices.json; the Window line the next receipt tiles onto; no transcript → UNREAD never 0; an unpriced model → FLOOR)
        tools/close_ep.py --selftest                   4/4   (the category; tiled exactly → no warning; an overlap said; a non-ISO since refused)
        tools/id_preflight.py --selftest               4/4   (forward references TAKEN; ERR-001 on an empty register; untracked files outside the namespace; not a git tree UNREAD 2)
        tools/compact_memory.py --selftest             4/4   (the fold with its read-back: a dropped must-phrase refuses; a changed tail refuses; a prefix matching no line refuses)
        tools/ci_observe.py --selftest                 5/5   (read_step_logs on a planted zip: exit 1 / clean / no file UNREAD / a bad zip UNREAD; the FULL-SHA rule)
        tools/baseline.py --selftest                   3/3   (the registers' counts read; no pin → UNREAD; a missing register → UNREAD never 0)
        tools/close_chain.sh + tools/close_parts.py + close-light: tests/close_chain_selftest.py 13/13 (the chain on a complete sitting exits 0: the content commit → close.sh → the whole receipt + the baseline → §19 verbatim → the closing commit → the row → row_check → lockstep; open.sh CLEAN at S2 after; an unexplained file 12; an untouched register → close.sh red 3; no record 4; the light close 0 / a .py changed 3 / a missing §18 red) — the first run found acceptance.py crashing on a manifest with no [[lanes]] and close.sh reading glass's UNREAD (exit 2) as RED: both fixed, the chain's order made the source's (content commit BEFORE close.sh)
        close.sh on the folder (pin e72241d, e4_close_run.txt): GLASS UNREAD (no [[lanes]] — T-002) · SCHEMA no drift (lanes 0) · RUNNER fired 22 / red 0 / skipped 1 (tests/bootstrap_selftest.py — E5) · LEVERS 0 (regex) · 0 (AST census) · PROMISES UNREAD · REGISTER RED (untouched — true until E5) · DEBT 0 → 0 · MEMORY 1,666 · DIFF 21 modified uncommitted (this episode's work, committed next)
        tests/RUNNER.txt 27 lines; .gitignore + records/journal/.work/; every file LF; nothing of the converter touched
```
