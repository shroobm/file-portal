# S78 — ThinkPad / receiver lane — 2026-08-16

**Closeout under `docs/21-session-closeout-contract.md`.** Tier: **FULL** — code shipped on both
linux services, two systemd contracts changed, one new timer unit. Session number S78 is the
sequential next after the ledger's S77; Rab delegated execution mid-session ("you know who you
need to be for this to be done") but did not explicitly number it — **veto the number if wrong.**
One cookie awarded this session (unnumbered here by design; the tally is the Desktop's book).

---

## §1 Session Intent

`Observed` — the session grew in four asks: (1) brief me on the branch; (2) severity + goal;
(3) how does the real world solve our open problems — with an explicit commission to research
USA / Canada / Russia / Japan / China / South America pools; (4) cross-reference the resulting
plan against the codebase and plans, then execute it in one session.

## §2 Starting State

`Observed` — MUSTER at open: checkout **behind origin by 58** (the whole S68–S77 Desktop arc);
both services active; vault tip `70c60e6`, staging empty; receipts 5 lines ending in the
SYM-015 supersede trio. The 58-commit gap's only linux-tree touch was a comment-only
`converter.toml` fix (S69's SYM-012 wording), verified by diff before deciding not to restart.

## §3 Development Chain

`Verified` — each gate its own commit, suite + lint green before each:

- **G0** pull to S77 tip (`8b12e55` → `c3f7e17`); restart skipped, comment-only delta (see §6).
- **G1** `fb3c6bc` — Desktop's calibrated zlib+trigram degeneration tripwire ported to the
  linux lanes, report-mode, verdict in manifest + journal. 10 tests.
- **G2** `9b0d475` — `min_chars_per_page` calibration check: **bar not met** (~10 distinct
  documents, not 30); threshold held, evidence recorded in the config comment.
- **G3** `0bb4393` — spot-check every 10th accepted export + `degeneration_flagged` in the
  exported receipt + torn-line healing in `_receipt`. 5 tests.
- **G4** `89ba82c` — Type=notify + WatchdogSec=90 on both services, observer-liveness
  heartbeats, stdlib sd_notify duplicated per package. 4 tests each side.
- **G5** `e752db4` — weekly vault fixity timer (`git fsck --strict --no-dangling`), receipt
  per run, `append_receipt` extracted and shared. 3 tests.

## §4 Analogue + Boundary

`Verified` (agent-researched, primary sources in the coordination message) — every gate has a
real-world analogue chosen because it was **in this machine's lane**: FADGI 3rd ed. (10-or-10%
batch inspection) → G3; NDSA Levels L3 (fixity at fixed intervals, with logs) → G5; NDL Japan's
stratified acceptance (per-category thresholds) → G1's per-lane framing; systemd watchdog
practice (progress-tied heartbeats, generous first interval) → G4. Explicitly NOT imported
(Desktop lane, filed in the coordination message): ABBYY per-character confidence routing,
Smart Engines ROVER voting, MinerU2.5 layout-conditioned decoding penalties, HathiTrust's
post-publication reopen loop.

## §7 Implementation Delta

`Observed` — `linux-converter/`: `converter/degeneration.py` (new), `converter/fixity.py`
(new), `converter/sdnotify.py` (new), `converter/main.py`, `converter/exporter.py`,
`config/converter.toml`, `systemd/` ×3 units (2 new), `tests/` ×3 new files + 2 touched.
`linux-receiver/`: `allocator/sdnotify.py` (new), `allocator/main.py`,
`systemd/file-portal-allocator.service`, `tests/test_sdnotify.py` (new).

## §8 Decision Ledger

- **Report-mode everywhere** — standing doctrine (enforce is Rab's open lever question); no new
  check blocks anything.
- **G1 thresholds ported, not re-derived** — one calibration with docs/15 provenance; forking
  the numbers would create two truths.
- **Watchdog heartbeat = observer alive, NOT dispatch progress** — a legitimate multi-minute
  conversion must not read as a hang; the cost is honest (§11).
- **Spot-check N=10** — FADGI floor; module constant, Rab's number to tune (§12 argues it may
  be too high at this volume).
- **Fixity weekly, Persistent=true** — NDSA says monthly-or-better; a sleeping laptop fires on
  next boot.
- **G0 restart skipped** — deploy ritual says restart on linux-tree pulls, but the delta was
  comment-only; the G4 restarts made state unambiguous 40 minutes later anyway.

## §9 Evidence

`Verified`, differently-shaped pairs per SYM-001:

- Suites: converter **73/73**, allocator **28/28**; ruff clean both; CI green through S77 at
  open (`gh run list`), this session's push pending at write time.
- **Watchdog live**: both PIDs SIGSTOPped 19:19:04; by 19:21:12 both services active with new
  MainPIDs, `NRestarts=1` — systemd killed and restarted on missed heartbeats. Reaching
  `active` under Type=notify separately proves READY=1 arrives.
- **Fixity live**: first run `ExecMainStatus=0`, real receipt
  `{"outcome": "fixity-check", "result": "pass", "tip": "70c60e61"}` against the actual vault;
  timer listed for Mon 00:09 UTC.
- Corruption path proven by test against real garbled loose objects, not mocks.

## §5 Observable Contract

New machine-readable surface, each field born with a disposition (docs/29):

- `manifest.json` gains `degeneration` {flagged, repeated_lines, md_lines, worst[]} — EVIDENCE.
- `exported` receipts may carry `degeneration_flagged: true` and/or `spot_check: true` — REPORT
  (the seam the Desktop tails).
- New receipt outcome `fixity-check` {result, tip, error?} — REPORT.
- Journal: `DEGENERATION …` WARNING, `SPOT-CHECK …` INFO.
- Both services now speak the systemd notify protocol; a wedged service self-reports by dying.

## §6 Deliberate Divergences

- The detector is a byte-faithful port including its known limits; no "improvements" were
  added, so Desktop recalibrations port cleanly.
- `SPOT_CHECK_EVERY` is a constant, not converter.toml — plumbing Settings into Exporter for
  one number Rab hasn't tuned yet wasn't worth the surface; promote when he tunes it.

## §10 Known Failures (mine, this session)

1. **A commit landed claiming a green suite while a test was red** — `pytest | tail` let the
   pipe eat the exit code and the `&&` chain continued to `git commit`. Caught within minutes;
   fixed and amended before push (unpushed work commit, no row citing it — the SYM-016 rule
   governs close commits, not this). Filed as **SYM-029**: the trap is general.
2. The first fix attempt targeted my test helper; the real defect was production's
   (`_receipt` gluing onto torn lines) — the test was right to fail twice. Filed as **SYM-028**.

## §11 Unproven by Machine

- A **hung conversion subprocess** does not trip the watchdog (heartbeat is observer-liveness
  by design). Discriminating a hang from a legitimately slow conversion needs a per-conversion
  deadline — future work, stated in the unit comment.
- The degeneration tripwire has fired only on synthetic loops and the suite; **no real book
  has crossed the linux lane since it shipped.**
- The spot-check has never fired live (needs 5 more accepted exports; see §12).

## §12 Uncertainty Register

- **N=10 may be wrong for this pipeline's volume.** The vault has taken ~7 ingests in 6 weeks;
  the 10th future accept could be months away, which makes the sampling gate decorative for a
  long time. FADGI's own rule is "10 or 10% whichever is LARGER" — at our batch sizes an
  argument exists for N=3–5. Rab's number.
- Desktop-authored bundles carry `fidelity`, not `degeneration`; their receipts will never
  show `degeneration_flagged`. Correct (their audit already gates them) but worth knowing
  when reading receipts.
- S78 numbering assumed sequential; veto at next Desktop MUSTER if the Desktop's tally says
  otherwise.

## §13 Dependencies / Assumptions

sd_notify is env-gated: outside systemd everything no-ops (tests prove). Timers require
lingering — already enabled (docs/10 Part 1). No new Python dependencies.

## §14 Regression Surface

Receipt consumers (widget tail, MUSTER `tail -3`, Bench 🗁 receipts target) see new outcome
`fixity-check` and new optional keys on `exported`. All are additive; no existing key changed
shape. The unit files changed contract (Type=notify): **any future service that stops sending
READY=1 will hang in `activating` — that is the contract working, not a bug.**

## §15 Symptom Signatures

Two rows appended to `SYMPTOM-INDEX.md` this session: **SYM-028** (first receipt after a crash
vanishes / a garbage line glues two receipts), **SYM-029** (commit claims green, suite is red —
piped exit codes).

## §16 Recovery / Return Log

If the watchdog storms (it should not — 90 s vs 1 s heartbeats): revert the two unit files to
`Type=simple`, delete the `WatchdogSec` line, `systemctl --user daemon-reload && systemctl
--user restart` both. The Python changes are inert without the env vars. Fixity timer:
`systemctl --user disable --now file-portal-vault-fixity.timer`.

## §17 Current State

Both services active under notify+watchdog, `NRestarts=1` (the deliberate live proof, not an
incident). Fixity timer armed. Vault untouched: tip `70c60e6`, 7 notes… `Historical` from
MUSTER — the note count was not re-counted this session, only the tip verified. Staging empty.
Receipts tail = the fixity pass. Branch ahead of origin by 6 at close-commit time; push follows
the ledger row.

## §18 Next Entry Point

1. MUSTER; newest ledger row should be S78 and ancestor-verified; receipts tail should show
   Monday's scheduled `fixity-check` pass — **if it is missing, the timer or lingering broke.**
2. Feed the new tripwire a real book: drop a scanned PDF on the convert lane, confirm the
   manifest's `degeneration` block and (if 10th) the spot-check receipt.
3. Rab's numbers when he wants: `SPOT_CHECK_EVERY` (§12 argues 3–5), fixity cadence, and the
   standing enforce-lever question, now with two report-mode feeds ready to graduate.
4. Desktop lane: read `coordination/messages/2026-08-16T19-30--thinkpad-research-digest…`,
   render `spot_check` / `degeneration_flagged` / `fixity-check` on the glass (docs/29: fields
   exist, dispositions REPORT — a receipt nobody renders is SYM-027's shape).
5. The guided Valentine session remains the standing commission (deferred four times now).
