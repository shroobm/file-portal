# S72 — Desktop lane — 2026-08-10

**Closeout under `docs/21`.** Tier: CORE + selected extended — a documentation/design session;
zero executable pipeline code touched, the widget untouched by design.

## §1 Session Intent
`Observed` — plan `c77ba90`, Rab's sequencing honored: the deployment was live and used, so the
commissioned DESIGN-PLAN phase ran — the big-screen "mechanical-keyboard-reel" brief, PLAN ONLY.
Overnight while he naps; surprise requested; morning note owed (overnight-report protocol).

## §7 Implementation Delta
`Observed` — new: `docs/25-design-plan.html` (the plan + live concept strip) ·
`sessions/S72…` · the morning note (desktop folder, outside repo). Modified: CLAUDE_README
(plan→close), CHANGELOG. **Widget: zero changes. Pipeline: zero changes.**

## §8 Decision Ledger
1. **The thesis: "the thock, not the RGB."** Research says the reel aesthetic is damped mass +
   curation, not flash. Rejected: any treatment that adds a second accent color, glow beyond
   the terracotta grammar, or motion past 400 ms — the refusals are IN the plan (Part 6).
2. **Concept demonstrated on the plan's own page, never on the widget** — Rab's "do not change
   the UI as of yet" is load-bearing. Rejected: a widget branch with the treatment applied
   (tempting, faster to judge — and exactly what he said not to do yet).
3. **Token-first, Wall-first slices.** The Wall is the big-screen surface = the wow he asked
   for; tokens land applied-to-nothing first so slice 0 is provably pixel-identical. Rejected:
   a single big-bang restyle (unreviewable, unrevertable, unsignable).
4. **Sound = a lever, default OFF, slice 5, his signature only.**

## §9 Evidence
`Observed` — HTTP-served probes (localhost:8321): 6 keycap tiles render; click walks the
underglow (active 1→3); the `linear()` spring token computes live; the Wall hero renders;
charter + refusals present; console clean. Research cited in-doc (reel decomposition;
pure-CSS damped springs via `linear()`, Chromium-native — the Tauri WebView qualifies).

## §10 Known Failures
None found this session. The concept strip is dark-fixed by design (it mocks the widget's
dark chrome inside a theme-aware doc) — intentional, noted here so it isn't reported as a bug.

## §15 Symptom Signatures
No new rows.

## §17 Current State
`Historical` — docs/25 open in Rab's browser; morning note on his desktop; widget untouched;
pipeline idle; vault `70c60e6`; held/ = 4. Unsigned queue as listed in the note.

## §18 Next Entry Point
1. MUSTER; newest row S72, ancestor-verified.
2. **Rab's slice signatures on docs/25** — recommended start: Slice 0 (tokens, pixel-identical
   proof) + Slice 1 (the Wall) in one gated session, then his big-screen test.
3. Unsigned from S71: transcribe thresholds · audit credit. Parked: scan-lane locate.
4. Standing queue unchanged.
