# S133 — Desktop — 2026-09-12 — The re-audit's provenance names the bench's repairs; the selftest's codepage line

*Record before the act (docs/28; J63's guards). Written at the open, before any tracked write. The second of the
tickets Rab asked for from bed.*

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

> Complete your close-out but I expected you to do a few more tickets because you were working so well with that previous prompt I used. Don't preemptively finish up for the night. Pace yourself but don't end a put unless you have done like 3-4 tickets, please. I'd appreciate your persistence and dedication. Your focus is needed and your work is paying off. You are doing it well.

**The tickets chosen (S131 §10 / §18, both owed there):** (a) `convert_and_ship.py --reaudit` records
`repairs_digest: null` on a bench-repaired bundle, because it hashes `manifest["repairs"]`, a key no bench writes — the
Repair Bench's ledger is `repairs.jsonl` beside the manifest; the shipped *Zero to One* names the re-audit but not the
fourteen repairs behind it. The fix: hash the ledger file when the manifest key is absent, and say how many entries.
(b) `degeneration_selftest.py`'s docstring: a bare run on this machine's cp1252 console crashes on the script's own
glyphs before the verdict line; the run form is `PYTHONIOENCODING=utf-8` (SYM-112's kin, on stdout). Both small, both
with the information in hand: the code site (`reaudit()`, the `repairs_digest` block), the selftest's own fixture that
already plants a `repairs.jsonl` it never checks, the identity lane's observation of the crash.

**Standing:** nothing to Codex until lifted; the record before any tracked write (its creation and its commit are two
calls); a probe in the shared checkout is a file run by its literal path; `--census` at every close; the row parsed by
`row_check.sh` before the push (J69).

## §2 Starting State — `open.sh`, 2026-09-12T08:3xZ (verbatim)

```
════════ MUSTER · OPEN · 2026-09-12T08:35:38Z ════════
[0] IDENTITY
    ledger newest    S132  700e20a
    this session     S133  ·  desktop  ·  2026-09-12
    session marker   coordination/private/session.current = S133 (guard_record / guard_git read it)
    advisory         S133 is named in the INHERITED tree (700e20a) — read each, do not blanket-correct:
                     sessions/S132-desktop-2026-09-12.md 
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-12T08:35:39Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 86 (tally == hook)
[3a] LEDGER ORDER  ✓ 115 Desktop-lane rows ascend (S16 → S132)
[3b] LEDGER PARSE  ✓ 115/141 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ 700e20a (S132) == ledger 700e20a, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 9 · anchor 30 · pending 0 · drop 0
    levers           audit=report · analyst=local · batch=8
    gpu-lock         absent
    events           341 line(s)
    open-tasks       144 item(s) · last written 7 minutes ago
    error-bin        87 row(s) — read the CLASS table before you probe · last written 53 minutes ago
    symptoms         115 row(s), 44 open · last written 7 minutes ago
    relay            167 entries · run `gate.py status` for the board
    widget           13196
    widget autostart task 'File Portal widget (J57)' Ready — AtLogOn · auto-logon ON (Bndit)
    python procs     7
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 50896d1
    thinkpad         converter=active · vault tip 8d7cc85 · staging 0 · receipts 17
[3] PIN
    --since          700e20a
    closeout         sessions/S133-desktop-2026-09-12.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

## §3 The pin

`--since 700e20a` (the S132 closing commit; `beb97ae` row and `1b09a58` §20 are on origin, CI 250 green).

## §9 The plan

1. `reaudit()`: `repairs_digest` = sha256 of `manifest["repairs"]` when present (unchanged), else sha256 of the bytes of
   `held/<id>/repairs.jsonl` when it exists, else null; add `repairs_source` (`manifest` | `repairs.jsonl` | null) and
   `repairs_entries` (non-empty lines of the ledger) beside it. The `audit/reaudit` event carries the same digest.
2. `convert_and_ship_selftest.py`: the healthy fixture already writes `repairs.jsonl` — assert the provenance's digest
   equals the file's sha256 and `repairs_entries == 1`; a fixture without bench files → null / 0.
3. `degeneration_selftest.py`: the run form named in its docstring.
4. Selftests; `ruff==0.15.20 check`; a 2-lane review (Logic · Test) if the diff warrants it; the record; the close with
   `row_check.sh 133` before the push; memory in lockstep.

## §8 Findings

(filled as they are made)

## §10 Known failures / residue

(filled as they are made)

## §7 Work log (UTC)

- **08:3x** `open.sh` clean (S133, pin 700e20a). This record. The receipt (tracker pid 6672) still running.
