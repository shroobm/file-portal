# S102 · Desktop · 2026-08-20 · THE ADOPTION, AND P-1 — FIGURE COVERAGE

⟨claimed: Fable · S102 · 2026-08-20⟩

## §1 Session Intent

Rab's commission, in his words: **"adopted for me, lets do P-1"** / **"adopt it for me"**.

Aligned reading, two parts:

1. **Perform the adoption myself.** The standing law is *adoption is Rab's hand* (docs/19
   §0.3) — a law he authored, and which he is here explicitly waiving for this instance. It is
   waived by his word, not reinterpreted by mine. The law's actual purpose is technical, not
   ceremonial (the S29/S45 MSIX-mirror ghosts, where a write landed in a sandbox mirror and an
   "adoption" never reached the real exe), so the waiver is honored **with the ghost checks run
   explicitly**: MSIX mirror census, backup of the outgoing exe, sha256 verified at the real
   path after the copy, and the muster card re-read as an independent witness.
2. **Build P-1** — figure coverage — the first real completeness instrument. Its four decision
   variables are recorded below: two are already determined by signed doctrine, one by
   measurement taken this session, and **one remains genuinely his** and is put to him with
   evidence rather than guessed.

*(This section is written before work and is not edited afterwards.)*

## §2 Starting State

`open.sh` clean: ledger newest S101 `06dd19b` · this session S102 · SOFT 76 · HARD mirrors
ledger · origin in sync · **installed exe `4DCB73E2`** (post-adoption; the card is the witness)
· widget PID 29936 · held 4 · anchor 23 · levers audit=enforce · analyst=local · batch=16.
**Pinned `--since`: `06dd19b`.** Relay carried at open: nothing new for Fable.

## §3 The Adoption — done, verified, with one real finding

Sequence, each step measured rather than assumed:

1. **MSIX shadow census: NONE.** No `%LOCALAPPDATA%\Packages\*\LocalCache\Local\File Portal`
   mirror exists — the S29-class ghost's precondition is absent.
2. **Outgoing exe backed up**: `file-portal-widget.exe.bak-C3C05D49-2026-08-20` beside the
   installed one. The adoption is reversible by copy.
3. **Widget PID 14984 stopped** (running since 2026-08-18 23:54).
4. **⚠ FINDING — an orphaned watcher, outside any Job Object.** Killing the widget left
   `watch_and_convert.py` **PID 24344** alive: started **2026-08-19 01:07** (~34 h earlier),
   parented to a **`codex-runtimes` python (PID 23724) that was itself already dead**. The S37
   Job Object governs only watchers *the widget spawns*; a watcher launched from another
   runtime is outside it by construction and survives every widget lifecycle event, including
   force-kill. Card was free and no marker/convert process existed, so it was stopped before
   the new widget could create a two-watcher state over one `drop/`. Filed as **SYM-047**.
5. **Copied and verified at the real path**: `%LOCALAPPDATA%\File Portal\file-portal-widget.exe`
   → sha256 prefix **`4DCB73E2`**, matching the staged build exactly. Adoption landed.
6. **Launched**: widget **PID 29936**, one process, watcher **PID 5388** correctly parented to
   it (its child 9116 is uv's launcher shim re-execing the same script, inside the same job —
   not a second watcher). Boot log healthy: `watcher_start pid 5388`, `reader_config`,
   `chat_config configured`.
7. **S94's single-instance guard EXERCISED and HELD** — a second launch produced **no twin**:
   PID 29936 unchanged, watcher tree unchanged. This guard had been `Unknown` since S94
   ("nobody logged the guard exercise"); it is now **Observed**.

**Still `Unknown` from S94's list**: Room styling under CSP · Recent audits panel · chat page ·
the P-0 figures line rendered on a real book. Those need eyes on the glass, which is Rab's.

## §4 P-1's Decision Variables — what is determined, and what is his

| variable | status | basis |
|---|---|---|
| **report vs gate** | **DETERMINED: report-only** | docs/15 §6:114 is signed doctrine — *"ALL thresholds ship in report-only mode until calibrated"*, and §9 step 3 requires the tool to show its false alarms to Rab verbatim before it may pulse terracotta. Also live-state: `audit-mode.txt` is at `enforce`, so a gating signal would park books on day one. |
| **instrument** | **DETERMINED: PyMuPDF-only** | Admitting a JVM for PDFFigures 2.0 is a new platform dependency with its own adoption ritual, and it is unsigned. PyMuPDF is installed, load-bearing, and covers both rasters and vector drawings. PDFFigures stays available as a later, separately-signed upgrade. |
| **semantics** | **CHANGED BY MEASUREMENT: page-level, not bbox-overlap** | docs/41 §2 P-1 specified *"does each source figure region have some output image whose bbox overlaps it"*. **Not computable on our stack**: a shipped bundle is `.md` + `assets/` + `manifest.json` only (verified), and marker's markdown-mode `_meta.json` carries just `table_of_contents` + `page_stats` — **no block bboxes** (`renderers/__init__.py:117-125`). Output bboxes exist only under a different output format or the Python API, i.e. a converter rewrite. Buildable today: **per-page coverage** — does each source page bearing figure regions have ≥1 output figure asset attributed to that page (asset names carry the page). |
| **host** | **OPEN — Rab's** | And now better informed: since bbox-overlap needs convert-time data that no longer exists afterwards, in-converter placement (a) is the only route to *region-level* coverage later, while out-of-band (b) gets *page-level* coverage over the whole existing corpus for free and costs zero card-lock. This session builds (b)-capable; wiring (a) is his call. |

## §5 Work Plan

1. This closeout; commit the open; file SYM-047.
2. Build `windows-converter/figure_coverage.py` — pure, CLI-driven, report-only, no verdict.
3. **Calibrate on the real corpus** (docs/15 §9 step 3): run every anchored bundle, show the
   flagged pages verbatim, and count the false alarms honestly before proposing anything.
4. Tripwire in the same commit.
5. Close in lockstep from `06dd19b`; push.

## §6 Scope and Evidence Boundary

- New Python module + its test; docs/41 §2 P-1 amended with the semantics finding; docs/37;
  SYMPTOM-INDEX (SYM-047); CHANGELOG; this file.
- **The module is not wired into `convert_and_ship.py`** — that is variable 4, Rab's.
- No GPU, no pipeline run, no vault write, no verdict changed.

## §7 Sections Deferred to Close
