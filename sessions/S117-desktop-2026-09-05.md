# S117 — Desktop — 2026-09-05

⟨claimed: Fable 5.1 · S117 · desktop · opened 2026-09-05T22:3xZ⟩

## §1 Session Intent

Rab, verbatim, on the sign line offered at the S116 close (*"Signed: bless · drop <book name> · J39 · arm REGISTER ·
D-1 fail-closed · W5 not yet · G9 not yet."*): *"absolutely, also this is the next pipeline convert run sample.
designing data intensive applications book martin kleppmann and chris riccomini"* — with the file
`C:\Users\Bndit\Downloads\Designing Data-Intensive Applications_ The Big Ideas Behind -- Martin Kleppmann, Chris
Riccomini -- 2, 2026 -- O'Reilly Media, Incorporated -- isbn13 9781098119065 --.pdf` attached.

Read as: the sign line affirmed in full — (1) the Cybernetics bless is his click, then my restart of the ThinkPad
exporter so the bless is swept; (2) DROP THIS BOOK as the next convert run (C0/J10); (3) J39; (4) arm the REGISTER
gate; (5) D-1 = fail-closed-with-reason; (6) W5 build and G9: not yet, recorded.

*(Written before the drop; left unedited for the rest of the session — docs/21 §1.)*

## §2 Starting State — the `open.sh` card (22:3xZ, exit 0; rows verbatim, the unchanged rows elided)

```
    ledger newest    S116  35f77aa
    this session     S117  ·  desktop  ·  2026-09-05
[2] SOFT clock ... ✓ 85 (tally == hook)
[3] HARD clock ... ✓ 35f77aa (S116) == ledger 35f77aa, ancestor of HEAD
VERDICT .......... ✓ CLEAN — clocks reconcile
    origin           in sync (fetched)
    pipeline         held 7 · anchor 27 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    widget           15308
    python procs     8
    ollama           2
    thinkpad         converter=active · vault tip 70c60e6 · staging 1 · receipts 10
    --since          35f77aa
    closeout         sessions/S117-desktop-2026-09-05.md
```

**Pinned `--since 35f77aa`** (the S116 close). HEAD at open: `f8e6bb4` (S116's §20 pointer).

## §3 Inherited, all `Historical`

- S116 §18: bless → drop a book (J34's first breath; the road is complete: sidecar, four rejection reasons,
  `--reaudit`, bless-before-supersede) → J39 → W5/G9/D-1 as ink. S116 §10: Cybernetics in ThinkPad staging as
  `supersede-held`; J33's vault-OUT rule unexercised; SYM-075 open; the REGISTER gate warn-only since S109.
- S108 sign sheet item 6 (C0): "a small post-S60 born-digital PDF for a clean first breath" was the recommendation;
  Rab chose a 673-page one instead — his call, recorded, the duration estimate below is honest about it.
- The widget's bless status line (main.js:1028, S56-era): *"Blessed … — deploy/restart the ThinkPad exporter to
  vault it."*

## §4 Promoted before the drop (`Observed`, each with its probe)

- **Seat (law 8):** `nvidia-smi` 2676 / 10240 MiB, 57 °C; per-process view hidden (unelevated); `ollama ps` → no
  model resident; room chat `GET 127.0.0.1:7100/api/status` → `loaded: false, hold_file: null, convert_running:
  null` (its 5.23 GB model is NOT on the card); `.gpu-lock` absent; `query user` → bndit console only. So the
  2.6 GB is desktop graphics and the card is free for one lab process.
- **Room chat's yield rule** (`room_chat.py:70-113, 304-313`): it writes `chat-hold.json` before loading and re-checks
  `.gpu-lock` after; the watcher DEFERS a drop while a chat-hold exists (`watch_and_convert.py:405-413`). Both sides
  yield — safe direction. If Rab chats during the convert, the chat refuses the card, not the converter.
- **Intake path** (`watch_and_convert.py:44-51, 414-424`): `MODE_FILE = analyst-mode.txt` reads `local`; a `.pdf`
  landing in `C:\Users\Bndit\ml\library\drop` runs `convert_and_ship.py <pdf> --analyst --backend local` straight
  through (no pending card; `ask` would defer); the watcher writes `.gpu-lock` with the file name; a Marker cap is
  scaled to the page count. Watcher alive: PID 26200 (marker-env python, since 09-03 22:36), 26428 its shim.
- **Bless control** (`windows-widget/src/main.js:928-973, 1021-1031`): the Dock's Assay card renders `✓ bless` on
  `flag` and `fail` rows (`.ac-bless`, `data-src` = the bundle) → `invoke("assay_bless")` → `assay.rs::bless` writes
  `bless.json` into ThinkPad staging over ssh. The exporter's `sweep()` runs at startup (`main.py:311`) and its
  handler watches staging NON-recursively (`main.py:305`), so a marker inside a bundle dir is honoured on the next
  restart — the status line is right.
- **The book** (`probe_ddia_pdf.py`, pymupdf): exists, 20,665,337 bytes; **673 pages** (the tool header that
  delivered the file said 202 — wrong; pymupdf's count stands); title *Designing Data-Intensive Applications, 2nd
  edition*, Kleppmann & Riccomini, produced 2026-04-22 by macOS Quartz; text layer on every sampled page (1,616,521
  chars total, 2,402 per page on average), 152 images → **clean lane, born-digital**. Filename 170 bytes / stem 166 →
  `bundle.clamp_name` clamps to 80 (SYM-014's rule, by design).
- **Duration, `Inferred`** from S114's maiden voyage (1,372 pages, 957 analyst chunks): ≈0.7 chunks/page → ≈470
  chunks here; at the measured 10–25 s per warm chunk that is 1.3–3.3 h of analyst after Marker's convert
  (tens of minutes). A multi-hour run; the widget's card ages the analyst heartbeat.

## §7 Work log

- **22:2xZ** `open.sh` exit 0, pin `35f77aa`. Phases 2–5 read; this file written BEFORE the drop (commit `b96f266`).
- **2026-09-06T00:31:40Z THE DROP** (slot 2): the PDF copied (not moved) into `C:\Users\Bndit\ml\library\drop\`,
  20,665,337 bytes. `+1 s` `intake/detected analyst_mode=local` (watcher PID 26428); `+5 s` `.gpu-lock` written
  with the file name; child PID 16048 = `convert_and_ship.py <pdf> --analyst --backend local` (`Observed`,
  `Win32_Process`). `convert/probe`: 673 pages, `chars_per_page 2402.0`, **lane clean** (`text_layer_present`,
  57,778 spans, 0 invisible). `convert/estimate`: `eta_s 2122`, `s_per_page 3.154`, basis `similar`, 3 samples.
  `convert/chunking`: 4 slices × 200, batch 8.
- **00:35:06Z / 00:37:02Z / 00:39:02Z** slices 1–3 done: `wall_s` 201.1 · 115.2 · 120.7 (pages 0–599) — ≈0.73 s/page
  against the 3.15 estimate (the estimator's three "similar" samples were slower books; `Observed`, the estimate
  is the estimator's, not mine). GPU 6,509 MiB / 24 % / 56 °C during slice 4.
- **00:3x–00:4xZ, while Marker ran** (nothing touched the live converter tree): J39 launched as a Sonnet builder
  in an isolated worktree + a Sonnet refuter (`wf_ffc82c5e-d33`) — the running process lazily imports
  `analyst.py`/`fidelity_audit.py` from the live checkout later, so J39 merges only after the run ends; REGISTER
  gate ARMED `5e3a654` (selftest 73/73); the Atlas D-1/W5/G9 records `e5b1380` (Atlas repo) + J38's note.
- **00:40:10Z `convert/converted`**: `wall_s 504.4`, `s_per_page 0.75` (promised 3.154 / eta 2122 — the estimator's
  three "similar" samples were slower books), **`peak_vram_mib 9015`** of 10,240 — 1.2 GB of headroom; the room
  chat's yield rule is what kept its 5.23 GB model off the card. `convert/blocks`: 5,716 blocks, 673 of 673 pages,
  `page_unresolved 0`, complete. **`audit/scored` (convert phase)**: `doc_survival 0.8559`, `runs 25` of
  `runs_total 299`, `degeneration False` → **verdict `flag`** (ships in enforce mode; fail would hold). `analyst/start`
  00:40:21Z, backend local, 1,989,461 chars.
- **00:46Z Rab's bless click** (slot 1, HIS hand): `bless.json` landed in ThinkPad staging at 00:46 — `by rab`, sha
  `c5afd9edcf620fc6…`, reason *human-bless (figure-heavy ceiling, docs/18 §5.4)* (`Observed` over ssh). No local
  event is emitted by the click (events.jsonl unchanged).
- **00:47:27Z ThinkPad exporter restarted** (my hand, the widget's own status line's instruction; the exporter sweeps
  only at startup, `main.py:311`). Journal, 00:47:28Z: `EXPORT-BLESSED … flag verdict accepted on human bless (by=rab)`
  → `EXPORT-SUPERSEDE … -> Inbox/cybernetics-book-of-models-v4-6b-complete--c5afd9ed (reaudit, fail->pass)` →
  `EXPORTED-SUPERSEDE … (commit 50896d15 pushed + blob-verified, staging removed)`. Receipts +2: `blessed`,
  `exported-supersede`. **Vault tip `70c60e6 → 50896d1`** *"supersede: … (reaudit, fail→pass)"*; staging empty.
  **A41 resolved by the designed path** — the first supersede-with-provenance in the vault's history.
- **Vault manifest, read back** (`git show HEAD:…/manifest.json`): `fidelity.verdict flag`, `fidelity.reaudit.from
  fail` by `convert_and_ship --reaudit`, `blessed {by rab, from_verdict flag}`, `supersede {reason reaudit,
  from_verdict fail}`, `marker_body.present_in_bundle False` (a pre-J33 bundle — J33's vault-OUT rule STILL
  unexercised; the DDIA bundle will be the first). **Two observations:** (a) the exporter's log line and the vault
  commit message render the transition as *"fail→pass"* while the manifest says a blessed **flag** — a status word
  that outruns its record (STATUS-THEATRE's shape; **J40** filed, wording only, the data is right); (b) `bless.json`
  itself was committed into the vault beside the note — harmless, the manifest already folds it, but it is a
  second copy of a control file inside the library; named, not judged.
- **00:48:13Z analyst progress** (`.analyst-progress.json`, age 23 s): chunk **32 of 492**, `s_per_chunk 14.0`,
  `eta_s 6440` (≈ 02:35Z finish); GPU 7,300 MiB / 92 %.
- **00:5xZ J39 returned** (`wf_ffc82c5e-d33`, builder + refuter, 21 min, 175 tool uses): 50 → 0 by hand, every
  rule's method recorded in the J39 row; the worktree had spawned at `7c006f2` (F4 again) and was reset first;
  refuter **GO_AMENDED** (cosmetic: the commit body's error count; the ARMED comment's date is 09-06 UTC vs 09-05
  local — not a slip in this project's UTC convention). **Its one real find:** the fresh worktree's checkout turned
  the byte-exact `docs/contracts/*.json` fixtures into `\r\r\n` (110,341 → 111,213 bytes = one byte per line) and
  `visual_witness_capture_selftest` read GROUND-DRIFT on 6 of 54 there while the main checkout — byte-identical
  to the blob — passed 54/54 (`Observed` here, 00:5xZ). **Merge timing revised, and why:** the plan was "after the
  run"; the census (`Observed`: no `import`/`from` of any J39 module in `convert_and_ship.py`, `fidelity_audit.py`,
  `analyst.py`, `text_norm.py`; the process's only subprocesses are taskkill, Marker, tar and ssh) shows the
  running converter can never load a J39-touched file it has not already loaded, so the cherry-pick went in now:
  **`47cd440`**; main checkout: ruff clean, six suites green (9/9 · 30/30 · 206/206 · 18/18 · 24/24 · 54/54), CRLF
  intact on all 11 files. `.gitattributes` gains `docs/contracts/*.json -text`. Worktree + branch removed, pruned.
- **00:56:29Z analyst progress:** chunk **60 of 492**, `s_per_chunk 15.9`, `eta_s 6868` (≈ 02:51Z).
- **00:58Z push `f8e6bb4..5248329`; 01:02:41Z CI run 34002667639 / num 197 completed success**, both jobs; the
  windows-converter **Lint step, now a HARD gate, read `All checks passed!` in the job LOG** (line 269 — read in
  the log per SYM-075, not the API). **S108-9 is whole**: close.sh DOCTOR + CENSUS (S116) and all three S108
  warn-only CI steps armed and observed green on their first hard run. J39's "strike at the next sweep once CI is
  observed" condition is met.
- **01:02:57Z analyst progress:** chunk **87 of 492**, `s_per_chunk 15.5`, `eta_s 6277` (≈ 02:48Z); GPU 7,355 MiB /
  92 % / 73 °C. A Monitor armed on `events.jsonl` (analyst/audit/ship/held/failed, or PID 16048 vanishing).
- **02:53:08Z `analyst/done`** (the Monitor woke this session): `chunks_passed 481 · rejected 11 · failed 0 ·
  generated 492 · duration_s 7966.6` (2 h 13 min; 16.2 s/chunk over the whole phase) · `goodput_accepted_tok_s
  57.76` (460,181 accepted output tokens / 7,966.6 s wall — docs/34: this-run accepted tokens over whole-phase wall,
  cached prefills uncounted) · **`rejections {fence 8, survival 3, think_leak 0, inflation 0}`** — J34's first live
  breath: zero rejections, zero false rejections on 492 chunks; the survival guard fired three times.
- **02:53:15Z `audit/scored` (analyst phase):** `doc_survival 0.9683`, `runs 25` listed of `runs_total 61`, verdict
  **`fail`** (the `ANALYST_DOC_FAIL 0.995` gate) → `audit/held`. **The book is in `held/fc1f068c3a8eeb63`**, not the
  vault; nothing shipped (ThinkPad staging untouched). `.gpu-lock` released; the PDF in `drop/done`; drop empty.
- **The held bundle, read back:** `fidelity.verdict fail`; convert `doc_survival 0.8559`, `pages_flagged 144` of 673,
  `runs_total 299`, `degeneration False`; analyst-audit `normalisation {unescape, punct_free, space_free, regex_id
  j32a-v2}`; **`marker_body {file …marker.txt, bytes 1,991,297, sha256 3a14d17d…}` — J33's first sidecar, present
  and hash-bound**; frontmatter `rejections_survival: 3` / `rejections_inflation: 0` (J34's line, first time on a
  real note). The five largest runs: 264 words *"derived data systems the systems of record are the primary…"*
  (a dropped paragraph), 108 *"it in figure 38 every change to the state of…"*, then three 96-word runs of index
  text (*"data minimization 25 596 tion 101105 510 data models 65108"*, *"221 stream processors writing to
  databases 528…"*) — the analyst rewrites index/page-number lists into prose or drops them. All runs `page: None`
  (the analyst-stage audit has no page localizer — a Marker-body comparison; known, not new).
- **The analyst journal is gone:** `analyst_work/` holds only the 08-30 Univ 4e dir; this run's `chunks.jsonl` was
  `rmtree`d at assembly (analyst.py's last line), taking the per-chunk `survival` and `ratio` columns J34 was
  built to make readable "off the journal". The counts survive in the manifest; the distribution does not (J41).

## §8 Findings

**F1 — The road was walked end to end, once, on a real book: convert → analyst → audit → held, and on another:
reaudit → ship → bless → supersede → vault.** Both stops were the designed ones. Cybernetics is the first vault note
with `blessed` + `supersede` + `reaudit` provenance; DDIA is the first held bundle that carries a sidecar, four
rejection reasons, and the two frontmatter lines. Nothing in the pipeline had to be touched by hand to get either
book to its stop.

**F2 — DDIA fails the analyst-stage gate for the same reason the University 4e does: real deletions that pass the
per-chunk guard.** 0.9683 means ≈3.2 % of the Marker body's 12-word windows are absent from the analysed text (61
runs). The 0.50 survival guard caught three gross deletions; a chunk that loses one paragraph of five scores 0.8 and
passes, and 264 words of *derived data systems* went that way. This is docs/54 §4 item 4's open policy — a compliant
analyst re-run, a tighter per-chunk threshold (its cost measured on the 08-30 journal: 0.80 catches 5 of 500), or a
repair at the Bench and `--reaudit`. **Rab's call, three ways, all built.** The index-page runs are a second class
(the analyst prosifies page-number lists) — a prompt-program question, not a guard question.

**F3 — J34's first live run: 0 of 492 rejected as inflation, and the number cannot be checked beyond that.** The
guard cost nothing on a normal book (no false rejects) and caught nothing (no runaway). Whether the largest live ratio
sat at 1.1 or 1.4 is unrecoverable because the journal died with the book's assembly. J41: keep the per-chunk
`survival`/`ratio` columns after success (a compact list in the manifest, or the journal renamed beside the bundle),
so the next lever move reads real data — the very thing S115 could only do because the 08-30 run had crashed and
left its journal behind.

**F4 — Marker was four times faster than its own estimate and used 88 % of the card.** 0.75 s/page against 3.15
promised; `peak_vram_mib 9015` of 10,240. The room chat's yield rule (`chat-hold.json` + `.gpu-lock`, both sides
defer) is the only thing between that peak and an OOM if Rab opens a chat mid-convert. Named; the rule held today.

**F5 — Slot 1's last inch needed my hand, and the widget already said so.** After the bless click the exporter
sweeps only at startup (`main.py:311`; its staging watch is non-recursive), so a `bless.json` inside a bundle dir is
honoured on the next restart — the S56 status line *"deploy/restart the ThinkPad exporter"* is accurate. A
re-sweep on bless (a top-level touch, or a recursive watch) would remove the hand; small, unfiled — Rab's product
question whether the bless should be one click or two.

**F6 — The exporter names a blessed flag "pass"** (J40) — a status word outrunning its record on the first real
supersede. Data right, strings wrong. And `bless.json` rode into the vault beside the note.

**F7 — Fresh worktrees corrupt byte-exact fixtures** (`docs/contracts/*.json`, CRLF blobs + autocrlf = `\r\r\n`; 6
of 54 tests drift in a worktree, 54/54 in main). Pinned `-text`. This is the third EOL finding in two sessions (S116
F8's mixed blob, S115's `schemas.json`), all the same root: `core.autocrlf=true` on a repo with raw CRLF blobs.

**F8 — The fleet-worktree harness fault (S115 F4) fired again** (`7c006f2`, not `b96f266`); the brief's own
instruction to reset was followed. Still a harness fault; still named.

**F9 — S108-9 is whole and SYM-075 has no live instance.** No `continue-on-error` step remains in `ci.yml`; the
Lint step's first hard run read `All checks passed!` in its log. The row stays open as the reading rule.

## §10 Known failures / residue

- **DDIA held** at `fc1f068c3a8eeb63`, analyst-stage 0.9683 / 61 runs — Rab's disposition (F2: re-run, threshold,
  or Bench + `--reaudit`). Univ 4e `14c66834bdfeaa2e` still held for the same class. **Held: 8.**
- **J41** (journal columns die on success) filed; **J40** (exporter wording; bless.json in the vault) filed; the
  re-sweep-on-bless question (F5) unfiled, Rab's.
- J33's vault-OUT rule STILL unexercised (nothing with a sidecar has shipped); the DDIA sidecar is the first candidate.
- The analyst-stage audit's runs carry `page: None` — no localizer on that stage (known).
- The Atlas repo has no remote (`e5b1380`); its quarantine untouched.
- Codex's lane quiet since 09-03; `.codex/` untracked.

## §15 Registers touched (measured at close)

- OPEN-TASKS.md: annotated J31 (road complete), J34 (first live), J38 (D-1/W5/G9 signed), J39 (BUILT + MERGED),
  A41 (RESOLVED); added **J40**, **J41**. Struck 0 (the sweep strikes).
- SYMPTOM-INDEX.md: SYM-075 dated note (no live instance; stays OPEN as the class). Open SYM unchanged at 27.
- docs/50: S108-9 → done (whole). wiki/governance.md: S117 line (REGISTER armed).
- `.gitattributes`: `docs/contracts/*.json -text`. `.claude/skills/muster/close.sh` + `selftest.sh`: REGISTER armed
  (73/73).
- Atlas repo `e5b1380`: D-1 signed, W5/G9 not yet (WIDGET-FEATURES §7/§9; the S116 signature record's addendum).
- coordination: MSG-FAB-0075 at close (the landing + the two firsts) + beat.

## §19 close.sh values (2026-09-06T02:56:59Z, pin `35f77aa`, exit 0) — and the judgment on them

- **DIFF** 23 files since the pin · 0 uncommitted · 1 untracked (`.codex/`). **GLASS** clean (`--enforce`). **RUST**
  skipped (no widget change). **CI** success on `5248329` (run 197, the J39 push, observed — the close commit's own run
  is observed after the push, §20). **LEVERS** none (J39 touched no threshold; the nine deferred constants still
  deferred, §18).
- **DOCTOR** 8/8/0 armed green; **CENSUS** 11 = 11 = 11/11 armed green; **REGISTER** (armed today) reads *written this
  session: +8/−6, STRUCK 0 · ADDED 2* — line-accurate now that the blob is LF (S116 F8); the two adds are J40 (a
  status word outrunning its record on the first real supersede) and J41 (the guards' calibration data dying on
  success — found by the first live J34 run having nothing to show for its 0). Both are defects the day's two
  firsts exposed; neither is governance for its own sake. **DEBT** open SYM 27, unchanged. **MEMORY** durable at
  `e306610` (the lockstep write follows the ledger row). **PUSH** ahead 3 → the close pushes.

## §18 Next entry point

1. **MUSTER**; the card will read held 8, drop 0, vault tip `50896d1`.
2. **Rab's disposition of DDIA** (F2), by slot: (a) compliant analyst re-run (`--reanalyze`, same guards), (b) a
   tighter per-chunk survival lever (0.80 catches 5 of 500 on the 08-30 journal — re-measure before signing),
   (c) Bench repair + `--reaudit fc1f068c3a8eeb63`. And the index-page prosification: a prompt-program question.
3. **J41** before the next analyst run, so the next journal survives success.
4. J40's two strings; the bless re-sweep question (F5); J23 · J25/J30 · Panel C; the nine unlevered constants.

## §20 Post-close: CI observed on the row, the clocks advanced together

**Observed 2026-09-06T03:03:09Z.** GitHub Actions run **34007809856 / num 198** on the ledger-row commit `db0bdb1`
→ `completed | success` (both jobs; watched from NO-RUN at 02:58Z through `in_progress` to `completed` — the RUN
conclusion, honest now that no `continue-on-error` step remains). Push `5248329..db0bdb1`, ahead 0. **Lockstep:**
the memory library's TIME-STATE reads close `93c5dc6` / row `db0bdb1` with this green named, the S117 block written,
S116–S79 compacted under the read-limit hook's target (20,807 → 15,623 bytes; full text at `e306610`, `3701abe`,
`7e02980`, `3c759b0`), committed `706aab6` and pushed to the private remote; the cookie phrase stays on one line
(SYM-071). Cookie tally unchanged at 85. `close.sh` exit 0 at §19; the ledger row is its own commit (SYM-016); §1
unedited since `b96f266`; no artifact carries a session number other than S117. Rab was pushed the landing at
02:5xZ as asked ("wake me when the book lands").
