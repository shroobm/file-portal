# S79 — Desktop lane — 2026-08-15

**Fable 5 (Opus 5), Rab present throughout.**
Opened `a22a805` (S78's close). Pinned closeout ref: **`--since a22a805`**.

> **This file was created at OPEN, before the work it describes.** That is the change S79
> makes to the ritual: docs/21 §4 row 1 requires §1 to be *"written before work begins and
> left unedited afterward — the gap between intent and outcome is itself a finding"*, and all
> **12 of 12** prior closeouts were first committed in their own **closing** commit, so intent
> had always been authored by someone who already knew the outcome. `git log --diff-filter=A`
> on this file is the proof, and §1 must be byte-identical at close.

---

## §1 Session Intent

Rab's commission, in his words, in the order he gave it:

1. *"Muster. Check what happened last session, and tell me immediately what protocol I can have
   that would just be a command skill, akin to that of circle, for each session, so it starts
   honestly, exactly, without deviation."*
2. *"a skill based of this chain — Observed / Verified / Inferred / Intended / Unknown /
   Historical."*
3. *"study all of file portal you have access to, study the git… then tell me, how best would
   you design the skill catered to this project here."* — with *"Dont use agents for this
   session, take on the responsibility, be anxious, and investigate thoroughly, into the code
   if you have to, to see how it functions."*
4. *"Sign it, build the skill."*
5. *"take S79, fix the six refs, then error structure"*

And the standing goal he stated behind all of it:

> *"I do want the fucking pipeline to stop being sub-efficient or even sub-par… The goal at the
> end is that AI is efficient for spotting things out, so a human can manually take
> responsibility with zero risk. That's the goal here ultimately, and the system has become
> slightly ineligible to me as of recent because the intuitive has surfaced, and the surface has
> a name that was built on the intuitive, and not the engineering logic it deserves."*

That last sentence is the design constraint for everything this session ships: **a name must
carry a mechanism, or it does not ship.** Anything a command can produce is produced by a
command; prose is reserved for judgment no grep can do (docs/21 §3 and §6).

## §2 Starting State

`Observed 2026-08-15T21:31Z` — the `open.sh` card, verbatim:

```
════════ MUSTER · OPEN · 2026-08-15T21:31:31Z ════════
[0] IDENTITY
    ledger newest    S78  a22a805
    this session     S79  ·  desktop  ·  2026-08-15
    advisory         S79 is named in the INHERITED tree (a22a805) — read each, do not blanket-correct:
                     CHANGELOG.md CLAUDE_README.md docs/31-circle-s78-findings.md docs/32-proxy-substitution.md observability/acceptance.py
[1] GROUND
    verifier         344E1277  (repo == ~/.claude)
──────── MUSTER · 2026-08-15T21:31:31Z ────────
[1] MEMORY ....... ✓ …/memory/MEMORY.md
[2] SOFT clock ... ✓ 71 (tally == hook)
[3a] LEDGER ORDER  ✓ 63 rows ascend (S16 → S78)
[3b] LEDGER PARSE  ✓ 63/84 rows parsed · 21 discarded, newest at row 21 · tail = last 5
[3] HARD clock ... ✓ a22a805 (S78) mirrors ledger, ancestor of HEAD
    WORKING TREE . 6 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
[2] LIVE
    pipeline         held 4 · anchor 23 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=16
    gpu-lock         absent
    events           137 line(s)
    widget           10048
    python procs     4
    ollama           2
    installed exe    C3D277F7  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 70c60e6
    thinkpad         converter=active · vault tip 70c60e6 · staging 0 · receipts 5
[3] PIN
    --since          a22a805
    closeout         sessions/S79-desktop-2026-08-15.md
════════ mechanical half: clean (exit 0) ════════
```

**What was already broken on arrival, and is now cleared:**

- The session number **S79 was already spoken for** in the inherited tree. `Observed`: the
  ledger row, the closeout filename and the README all called the last session S78, while eight
  occurrences across five tracked files called it S79 — and docs/21 §7's prescribed command for
  verifying the number, the one headed *"never by assumption"*, returned **S79** off prose at
  `CLAUDE_README.md:65`. Rab's call: take S79, correct the references. Six were genuine
  misattributions and were corrected; **two in docs/31 were forward-looking and correct as
  written** (*"a future row that omits `S79:`"*, *"test against S79's own work"*) and were left
  alone — a blanket replace would have broken them.
- S78 §17 recorded *widget down · bench down*. `Observed`: both had been **up since 12:59**
  today. Inherited state is `Historical`, and it was false by morning.

## §6 Deliberate Divergences

**The ritual was adopted mid-session, so §1 was written after the skill it describes was
already built.** The skill exists because Rab commissioned it in this session; there was no
Phase 6 to open under until Phase 6 existed. Recorded rather than quietly backdated — the
requirement §1 encodes takes effect from **S80**, and this file is the first instance of the
form, not a clean example of it.

## §5 Observable Contract

Each checkable in under a minute, each with its failure condition stated in advance.

- `bash .claude/skills/muster/open.sh` → **exit 0**, a card of VALUES. Fails if any row reads
  `✓` where a number belongs, or if `widget` reads `down` while the widget is up.
- `bash .claude/skills/muster/selftest.sh` → **ALL TRIPWIRES FIRED — 12/12**, case 0 first.
  Fails if the count drops or if case 0 (the positive control) is the one that goes red.
- `python observability/acceptance.py` → **PASS 27/27**; `glass_detector.py --since a22a805` →
  **✓ no unsigned glitches**. Fails if a key added this session reaches nobody.
- `python prototypes/room-chat/acceptance.py` → **20/20**. Fails if the citation guard stops
  withholding an uncited answer, or starts withholding a cited one.
- `python prototypes/repair-bench/acceptance.py` → **85/85**, including "REAL held Valentine
  untouched".
- Open the Bench on Valentine, click zone 1: the card must carry **reason · highlight · solution**
  and the ⚠ grid caution. Fails if it names a session number instead of explaining the defect.

## §7 Implementation Delta

| Commit | What |
|---|---|
| `988547d` | **`/muster`** — `open.sh` (mechanical), `SKILL.md` (judgment), `selftest.sh` (12 tripwires), `muster.sh` **vendored into the repo** — S78 §10.4 closed |
| `6c8343e` | took S79; corrected 6 misattributions, left 2 correct forward references; closeout created **at open** |
| `cd486d2` | **the error structure** — `signatures.json` A–F, `Bench.diagnose()`, `renderDiagnosis()` |
| `230f305`·`76d0168` | the open reported an incident on its own session, then contradicted itself |
| `81a8e61` | the bank explained the history instead of the defect |
| `3e02ad2` | bench preview → the Beer book |
| `5a34a4f` | **analyst residency** — 47 % of its wall clock was model reloading |
| `9b72961` | **docs/33** — the Circle on the chat surface |
| `105f087` | **`room-chat`** — quarantined assistant, docs/33's five mechanical items |

## §8 Decision Ledger — what Rab signed

1. **The `/muster` skill**, on the epistemic chain, as a repo-resident command.
2. **Take S79 and correct the six references** — the ledger is authoritative over prose.
3. **The projection-law amendment** (docs/33 §2.1): a generated surface is permitted, and live
   values are rendered through the existing projection path verbatim — the model may point at a
   surface, never restate it. *The first non-derived surface in this widget's history.*
4. **The watcher deferral gate** (docs/33 §2.3): the conveyor may refuse to start while the
   assistant holds the card. Signed, **not yet built** — see §18.
5. **Standing design law**, his words: *"the surface has a name that was built on the intuitive,
   and not the engineering logic it deserves."* Applied as: a name must carry a mechanism.

## §10 Known Failures — mine, first-class

Seven defects, every one of them inside a guard, found while building the guard. docs/32 §6
predicted the next instance appears in whatever is built next to enforce a rule; it appeared
seven times in one session.

**10.1 A failed probe rendered as a negative observation.** MSYS rewrote `/FI`, `tasklist`
errored to stderr, the error was swallowed, and the card printed `widget down` while PID 10048
had been alive for hours. → SYM-031, plus a rule and two tripwires.

**10.2 `muster.sh`'s soft clock was held by an editorial convention.** A line-distance window
standing in for "inside the TIME-STATE entry"; a 1-line entry read `hook=99` off an index bullet.
Now bound to the blockquote's structure.

**10.3 The open asked one question twice and disagreed with itself.** I fixed one of two copies,
so the card printed `OPEN` and `COLLISION` together and exited 1.

**10.4 The collision check grepped the working tree**, so the moment the session wrote its own
number it began reporting the session to itself.

**10.5 The signature bank explained the history instead of the defect.** Rab read its first real
output and asked *"What the hell does this mean."* It named a signature letter and cited a
session — the exact lookup the bank exists to abolish, printed by the bank.

**10.6 The citation guard resolved nothing.** `endswith` both ways matched neither, so every
correct answer would have been withheld while the guard looked busy. Found by writing the test.

**10.7 `load_s` reported uptime.** The greeting would have said a model "came up in 73.7 s" that
came up in 33.2.

**And the one that was already there: `.gpu-lock` is not a lock.** Written at
`watch_and_convert.py:77`, deleted at `:86`, read as a gate by nothing. **I told Rab "the signal
already exists" and designed against it** — an inference stated as an observation, in the session
whose subject is that failure.

**Recurred at close:** the detector's `--since` mis-scoping. I diagnosed it in
`dispositions.json` at 21:00 and reproduced it six hours later, when `analyst.py` edits injected
`stream`/`keep_alive`/`prompt` into the **bench** lane. Dispositioned INTERNAL with the mechanism
recorded; docs/31 §5.2 item 7 remains unsigned and now has a second piece of evidence.

## §15 Symptom Signatures

- **SYM-030** — generating `REPAIRS.md` makes its own patient un-openable on the bench. `fixed`.
- **SYM-031** — a status readout says `down`/`clean`/`none` and it is not: a failed probe
  rendered as a negative observation. `fixed` by rule + guard + tripwires.
- **SYM-032** (filed this close) — a file named `.gpu-lock` implies a mutex that does not exist.

## §17 Current State

`Observed at close` — widget **up** (PID 18856, exe `C3D277F7`; `DB82AE3F` still staged and
stale); watcher up; bench down; room-chat down and **unloaded**, `chat-hold.json` absent, card at
**1,106 MiB**. `held/` **4**, all byte-identical to session open — Valentine `616b310f4f48f195`,
Beer `13aadc4685a65b82`. `anchor/` 23, drop 0, pending 0. `audit-mode` = `enforce`,
`analyst-mode` = `local`. Vault **6 notes**, tip `70c60e6`; ThinkPad converter **active**,
staging 0, receipts 5. llama.cpp **b10448 installed** at `C:\Users\Bndit\ml\llama\`, not wired to
anything. *(Historical the moment it is written — S78's §17 said "widget down" and was false by
morning, which is why §2's card is re-measured at every open rather than inherited.)*

## §18 Next Entry Point

1. `bash .claude/skills/muster/open.sh` — expect exit 0, S80, `--since` = this session's SHA.
2. **The `--jinja` parity re-measure**, llama.cpp migration, mid-flight. Restart llama-server
   with `--jinja` and re-measure **token counts, not speed**: the gate is whether a chunk lands
   near **814 tokens and stops on its own**. Ollama produced ~814; llama.cpp produced 1,380–2,048
   and twice hit the cap with `finish_reason: length`. Until that matches, the +27 % tok/s and
   the 1.72× batching are unbankable and the analyst must not be pointed at it — a truncated
   rewrite trips the link-fence or corrupts the chunk.
3. **`room-chat` graduation** (README §5): `llama_server_exe` config key · adopt the spawn into
   the watcher's Job Object (S37 — a hard kill currently orphans llama-server) · **build the
   signed watcher gate** with its tripwire: drop a PDF while `chat-hold.json` is set, prove it
   defers *and* prove it converts the moment the hold clears.
4. **Rab's ten real questions** for the assistant — the acceptance test that decides whether
   curated context suffices or retrieval is needed (docs/33 §3.3). Do not guess the workload.
5. Standing: the omission-class bank entry (18/18 runs unclassified on the Beer — the bank is
   blind to text that *isn't there*) · the wrong-page embed in Valentine · docs/26's slice 3.

## §19 The one thing to take from this session

The session built the thing that opens sessions, and the thing that opens sessions caught the
session building it — seven times. Every defect was in a guard, and every guard was being written
by someone actively holding docs/32 in mind. That is now three sessions of evidence for the same
mechanism, and the only method that has ever worked on it is the one Rab named by instinct: step
on the wire and watch whether it explodes. Nothing here was believed because it looked right;
`.gpu-lock` looked right for a year.
