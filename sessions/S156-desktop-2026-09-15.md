# S156 — Desktop — 2026-09-15 — THE VISION ROUTE, SIGNED, BY SUB AGENTS: a reading of the page feeds the layer (Claude Opus 5's third sitting in the Fable lane; the autonomy experiment's eleventh)

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

Rab, in chat, 2026-09-15 ~04:2xZ, after S155's close report, which had left him three words — the vision route (a model on his card, Sonnet by API per book, or neither), D1, the experiment's end:

> "signed, use sub agents. good luck. persistent. record all receipts."

**The reading (the echo, in the record before the act; on the Desk at E0):** *"signed, use sub agents"* answers the first of the three — **the vision route is signed, and its shape is SUB AGENTS**: Sonnet readers on the rendered page images (S151 E4's and S154 E6's method), never a vision model on his card. Sub agents live in a sitting, not in the watcher's conversion, so the route's shape is: a **reading of the page** produced by a reader panel in a sitting (rotated labels with their words and row spans; figure-or-table calls), written as a sidecar beside the bundle, and **consumed by the layer** whenever the analyst runs on that bundle — the pipeline's own conversion (the lever is ON) picks it up if it is there, the sidecar-aware re-analysis (J42) applies it to a held or anchored copy; a book without a reading gets the text rules (5 of 7). Every claim the reading makes is still admitted through `grid_invariant`. *"persistent. record all receipts."* — the persistent method, the receipts per episode, the Desk lines, private for scripts and nav, the close. *"good luck"* — noted. **D1 and the experiment's end are NOT in this sentence — they stay his.** "Use sub agents" is also the fleet shape for the sitting's own work: Sonnet lanes on preplanned briefs with planted controls, the lane as verifier.

## §2 Starting State — `open.sh`, the S156 card (verbatim)

```
════════ MUSTER · OPEN · 2026-09-15T05:06:45Z ════════
[0] IDENTITY
    ledger newest    S155  2f60a57
    this session     S156  ·  desktop  ·  2026-09-15
    session marker   coordination/private/session.current = S156 (guard_record / guard_git read it)
    advisory         S156 unnamed in the inherited tree
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-15T05:06:46Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 86 (tally == hook)
[3a] LEDGER ORDER  ✓ 138 Desktop-lane rows ascend (S16 → S155)
[3b] LEDGER PARSE  ✓ 138/164 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ 2f60a57 (S155) == ledger 2f60a57, ancestor of HEAD
    WORKING TREE . 2 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 8 · anchor 25 · pending 0 · drop 0
    intake           waiting 0 · oldest 0s · active null · written 1s ago by pid 18844
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    events           391 line(s)
    open-tasks       150 item(s) · last written 59 minutes ago
    error-bin        105 row(s) — read the CLASS table before you probe · last written 35 hours ago
    symptoms         132 row(s), 43 open · last written 10 hours ago
    relay            207 entries · run `gate.py status` for the board
    widget           27548
    widget autostart task 'File Portal widget (J57)' Ready — AtLogOn · auto-logon ON (Bndit)
    task             'File Portal desk (relay)' Ready LASTRUN=2026-09-13T00:59Z RESULT=4294967295
    task             'File Portal relay watch (Fable)' Ready LASTRUN=2026-09-12T19:41Z RESULT=0
    task             'File Portal tracker' Ready LASTRUN=1999-11-30T05:00Z RESULT=267011
    task             'File Portal desk watch' Ready LASTRUN=1999-11-30T05:00Z RESULT=267011
    python procs     6
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            7 note(s) · tip f489189
    thinkpad         UNREAD — host did not answer; NOT a statement that it is down or clean
[3] PIN
    --since          2f60a57
    closeout         sessions/S156-desktop-2026-09-15.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

The seat (`query user`, 04:2xZ): console session Active (his). The card: 1,793 MiB of 10,240 at 27 % (his desktop apps); `.gpu-lock` absent; no model of mine will run this sitting (the route needs no card). The smoke runs ALONE in the foreground after this record's commit.

## §3 The pin

`--since 2f60a57` — S155's closing SHA (row e3a71b8; CI green run 34927859038; the post-close 6044614 and f40416f, CI green runs 34928221387 / 34928519117).

## §9 The plan (each an episode with receipts and a Desk line)

- **E0 — this record; the tracker (`S156-receipt`); the smoke alone; the Desk line with the reading.**
- **E1 — the reading's FORMAT and the layer's hook:** a page reading is a JSON sidecar (`<bundle>/vision.json`; the private `measurement/truth/VISION.md` says its shape): per table (keyed by its header row's text and its first body row, never by a line number), `rails` — each a word and a row span — and `kind` (table / figure); `geometry_pass(..., vision=reading)` takes it the way it takes a resolver: a rail proposal takes its word from the reading where the lexicon and the resolver do not decide (MGMT), its span from the reading over the tiled span; a fragment rail (p.72's phrase in three pieces) is admitted when every fragment is a substring of the reading's label; a table the reading calls a figure gets no repair and the record says why. `grid_invariant` gains the fragment clause with its negatives; the selftest section [12]; ruff.
- **E2 — the pipeline's hook:** `analyst.process(..., vision=None)` reads the sidecar when given; `convert_and_ship` passes `<bundle>/vision.json` when it exists (the conversion and the J42 re-analysis alike); the record rides `meta["geometry"]["vision"]`; the selftests; nothing shipped.
- **E3 — the reading for Valentine, by the panel already sat:** S154 E6's three readers agreed 21 of 21 with the S151 majority — their JSON becomes the first `vision.json` (both copies; the readers' agreement as its provenance); the dry run on both copies with the reading; the scorer: rails 5/7 → ?, MGMT letters → word, p.342's HIGH left alone; the register rows.
- **E4 — the panel promoted for the next book:** the packets, the reader brief and the agreement into the private library (`handoff/` already holds the templates; the reading writer joins them), each with its brief, the smoke after (a promotion is a build).
- **E5 — the verifier fleet on E1–E2's code (Sonnet lenses, a planted control, skeptics), then the close.**

Not this sitting's: D1; the experiment's end; the card; any book but Valentine.

## E0 — the record, the tracker, the smoke (05:0xZ)

- The record committed first (88807cc); the tracker `S156-receipt` (pid 32, 05:07:52Z); the smoke, alone: **66/66**, 82 s; the Desk line 55a3e09c carries the reading of his sentence.

## E1 — the reading's format and the layer's hook (05:1x–05:3xZ)

- **The format** (Observed, private `measurement/truth/VISION.md`): a JSON sidecar `vision-reading/1` — per table an `anchor` (text the block carries; never a line number), `kind` (table / figure), `rails` (the label as printed, the first and last body row it spans, under the real header); `produced_by` names the panel and its agreement.
- **The hook** (Observed, public `table_geometry.py`, `sittings/S156/e1_layer_patch.py` + `e1_restructure.py`): `_fold_text`, `_vision_entry` (the entry whose every anchor appears in the block's text); `propose(..., vision=, notes=)` — the reading step now runs AFTER the letter-column branch (the first cut put it inside, so a fragment rail — no letter column — never met its entry; the selftest caught it): a reading rail lends its WORD to an unresolved run whose letters fit (`how: vision`), its SPAN to the matched run (`span_how: vision`), and a rail with no letter run becomes a FRAGMENT proposal when every rail cell on its rows is a substring of the label; a table the reading calls a figure loses all its proposals (`notes.figures`); `grid_invariant` gains the fragment clause (every fragment a substring of the label, the run blank after — else refused with the pieces named); `geometry_pass(..., vision=)` records `vision` (format, produced_by, tables_matched, words, spans, fragment_rails, figures, unmatched).
- **The tripwires** (Observed): section [12] — MGMT lent its word by the reading where the lexicon refused; the spans from the reading override the tiled ones (REVENUE [6, 11], COSTS [12, 13], MGMT [14, 14]); the record's vision facts; the invariant on the reading's output; a reading matching nothing changes nothing and says so; no reading → `vision` None; p.72's shape — three fragments proposed from the reading, applied on row 1, the pieces blanked, the invariant naming three fragments; **the plant** ("Bonds" in a fragment) never proposed and the table left as it came, and as an invariant negative refused with the pieces named; a figure call silences a table (the axis letters untouched) with its **negative control** (without the reading the same table IS repaired — the figure call is the reading's alone). **142/142**; `analyst_selftest` 39/39; `edit_whitelist_selftest` 53/53; ruff at CI's pin clean.

## E2 — the pipeline's hook (05:3x–05:4xZ)

- **`analyst.process(..., vision=None)`** (Observed): hands the reading to `geometry_pass`; its facts ride `meta["geometry"]["vision"]`; a new case S156 (a) — one table matched, one span from the reading, `vision` None without one — **40/40**.
- **`convert_and_ship`** (Observed): `_vision_sidecar(path)` reads a `vision-reading/*` JSON and refuses aloud anything else (a malformed file, a foreign format — None, never half-read); `convert()` takes `<stem>.vision.json` beside the dropped PDF, hands it to the analyst and copies it into the bundle as `vision.json`; `apply_analyst()` (the J42 re-analysis) hands `<bundle>/vision.json`; both pass `vision=` ONLY when a reading exists (the selftests' analyst fakes keep their signature). Six new cases (the sidecar reaches the analyst and lands in the bundle; the bundle's sidecar reaches the re-analysis; malformed, foreign and absent sidecars are None) — **GREEN 254/254**; ruff clean. docs/15 §12.4.10.
- **Not this episode's:** a reading for any book (E3 writes Valentine's); the census (it reads the copy, not the sidecar — a `kind` from the reading is a later step); the watcher (untouched; it spawns the converter per book, which now reads the sidecar).
