# S91 · Desktop · 2026-08-17 · THE WEATHER FIX — CI STOPS DEPENDING ON A DOWNLOAD

## §1 Session Intent

Rab's commission, in his words (remote from work): **"Buddy, CI/ Rust failed in 1 minute."**

Read as: diagnose and fix. Diagnosed before this open (chat, `Observed` via the Actions API
through the stored git credential): the rust job died at **Set up job** — GitHub's codeload
answered **429** three times downloading the `dtolnay/rust-toolchain` action archive, on both
the original run and the re-run I fired; no repo step ever executed; the same push's python
job passed; every push S86→S89 was green on the identical Rust tree; S90's diff was docs-only;
local `cargo fmt --check` clean. Scope: replace the third-party toolchain ACTION with direct
`rustup` (preinstalled on windows-latest), keeping the 1.97.1 pin and SYM-019's rationale.
Under Rab's standing sign (S88).

**Written before the fix. Left unedited** (docs/21).

## §2 Starting State

`open.sh` exit 0: S91 · `--since` = **`86f9f84`** · clocks clean · origin in sync. Failing
run `32042976659` (head `5503f8d`): rust `failure` at Set up job, 429 ×3 on
`codeload…/dtolnay/rust-toolchain/zip/4360b52…`; re-run via API (HTTP 201) failed identically.
`Swatinem/rust-cache@v2` remains in the job — its archive was not the one throttled; if it
429s next it becomes the same fix's second verse.

## §4 In-scope symptom rows

- **SYM-019's family** (unpinned/moving CI dependencies) — the pin STAYS; only the delivery
  mechanism of the pinned toolchain changes.
- **SYM-031** — the CI verdict is a reading: this closeout records the post-fix run's actual
  conclusion, never "should pass".

## §7 Implementation Delta

| Commit | What |
|---|---|
| `b06dc7b` | open + verse one: toolchain via preinstalled rustup (1.97.1 pin kept); the 429's first victim removed |
| `7738d37` | verse two: the 429 MOVED to `Swatinem/rust-cache` (log-proven) → first-party `actions/cache@v4`, explicit paths, lock-keyed. The rust job now downloads ZERO third-party archives |
| *(close)* | CHANGELOG · this closeout |

**Verdict, `Observed`: run on `7738d37` = `completed success`** (both jobs; watcher poll,
full-SHA query). Verse one's run failed at Set up job on the NEXT archive — the fix was
right and incomplete, which the log named within minutes.

## §8 Decision Ledger

Built under Rab's standing sign; his words this session: "Buddy, CI/ Rust failed in 1
minute" and a mid-fix "Hows it going". Flagged, not built: nothing — cache-key change costs
one cold build, noted in the yml comment.

## §10 Known Failures — mine

**10.1 My first CI watcher rendered a failed query as "still pending" for 24 polls.** The
Actions API's `head_sha` filter requires the FULL sha; I passed the short form, matched
nothing, and printed the empty result as `pending` — SYM-031's exact shape (a failed probe
rendered as a negative observation) inside a guard written the same night that row was
re-read. Caught only because Rab asked "how's it going" and I re-queried honestly. The
replacement poll derives `git rev-parse` full SHA. Row untouched (fixed-by-rule stands);
the refire is this record.

**10.2 Verse one shipped as "the durable fix" and was half of it.** The 429 was diagnosed
per-archive when the operative pattern was third-party-vs-first-party; the second run's log
corrected the model in one line. Cost: one extra red run. The §2 note ("second verse")
had named the risk — naming a risk and scoping the fix to it are different acts.

## §15 Symptom Signatures

- **SYM-031 re-fired at me** (10.1) — remedy held once the probe was made honest; recorded
  here, row unedited (same handling as S88's SYM-029 refire).
- No new rows: the CI-weather class is covered by the yml's own S91 comments and SYM-019's
  pin doctrine.

## §17 Current State

`Observed at close` — CI green on `7738d37` (rust + python), the rust job free of
third-party action downloads. Widget up (Rab's morning launch), assistant still on the
pre-S89 700 budget pending his restart. Pipeline calm. The close push below spawns one more
CI run — expected green per the just-proven fix (`Inferred`, stated). Cookies 73.
*(`Historical` the moment it is written.)*

## §18 Next Entry Point

1. `open.sh` — expect S92, `--since` = this close's SHA; origin row in sync.
2. The docs/37 path stands unchanged: **muster → docs/37 → Stage 0 on Rab's word** (his
   paste-prompt already carries the F-09 signature).
3. Still pending Rab's hands: widget restart → the engineer question's fourth ask.
