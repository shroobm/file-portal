# S95 · Desktop · 2026-08-19 · COMPLETE FILE PORTAL SYSTEM SCOPE

## §1 Session Intent

Rab's commission, in his words: **"Create a work plan that orchestrates a complete and full scope of file-portal, this task needs to be done in completeness, and upon retrieval, create a laid out full scope doc, of the entire system. Please do not represent any falseness in this document, please do not miss out on details that provide a deeper understanding of the program, and cushion the language of it, provide terminology for every term relevant in explanation and in source."**

## §2 Starting State

`open.sh` exit 0, card verbatim:

```text
════════ MUSTER · OPEN · 2026-08-19T05:00:37Z ════════
[0] IDENTITY
    ledger newest    S94  841497d
    this session     S95  ·  desktop  ·  2026-08-19
    advisory         S95 is named in the INHERITED tree (841497d) — read each, do not blanket-correct:
                     sessions/S94-desktop-2026-08-17.md 
[1] GROUND
    verifier         1E2BE296  (repo == ~/.claude)
──────── MUSTER · 2026-08-19T05:00:38Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 74 (tally == hook)
[3a] LEDGER ORDER  ✓ 77 Desktop-lane rows ascend (S16 → S94)
[3b] LEDGER PARSE  ✓ 77/102 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 4 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79
[3] HARD clock ... ✓ 841497d (S94) mirrors ledger, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           UNREAD — fetch failed; NOT a statement that origin matches local
[2] LIVE
    pipeline         held 4 · anchor 23 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=16
    gpu-lock         absent
    events           137 line(s)
    processes        UNREAD — process-table probe failed (rc=1); NOT a statement that anything is down
    installed exe    C3C05D49  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip ?
    thinkpad         UNREAD — host did not answer; NOT a statement that it is down or clean
[3] PIN
    --since          841497d
    closeout         sessions/S95-desktop-2026-08-19.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

**Pinned `--since`: `841497d`.**

Namespace note: the session-level instruction names a `.Codex/.../Codex-Memory-Backup`
library, but that exact path was not present when probed. The tracked muster instead verified
the existing `.claude/.../Claude-Code-Memory-Backup` library and reconciled its TIME-STATE,
cookie tally, and repository ledger. The discrepancy remains `Unknown`; it will not be used to
promote memory-only assertions into current system facts.

Index rows intersecting today: SYM-001 (independently shaped verification), SYM-027
(computed data can remain off-glass), SYM-029 (repository Markdown/CRLF handling), SYM-031
(failed probes render `UNREAD`), SYM-039 (documentation drift), and SYM-040 (origin state is
currently `UNREAD`).

Standing orders in force: verify before instructing; read current source before documenting;
do not treat inherited prose as current; do not convert failed probes into negative claims;
keep epistemic status visible; use measured numbers only with their numerator, denominator,
conditions, and source; preserve the Markdown line-ending contract; do not touch the GPU lane;
and leave the pre-existing untracked `.agents/` directory outside session commits.

## §3 Work Plan

1. Establish the evidence model and document boundary: tracked repository at the pinned base,
   current working tree, historical records, live probes, and explicitly unknown external state.
2. Inventory every tracked subsystem, entry point, configuration surface, persistent artifact,
   protocol, interface, executable workflow, test surface, and operational document.
3. Trace the end-to-end data paths from intake through allocation, conversion, analysis,
   transport, receiving, vault export, repair, control-room projection, assistant, and remote
   operation; distinguish production, prototype, tooling, and historical-only paths.
4. Re-measure derivable facts from source and manifests. Cross-check consequential structural
   claims with a differently shaped method where practical; downgrade anything not fully proven.
5. Read the relevant architecture, schema, operating-law, configuration, and handoff documents;
   reconcile them against source rather than repeating them as authority.
6. Write `docs/38-file-portal-full-system-scope.md` with a reader-first overview, boundary map,
   component catalogue, workflows, state model, interfaces, configuration, safety and failure
   behavior, security/trust boundaries, operations, test strategy, known limitations, glossary,
   and evidence/status appendix.
7. Validate the document: link and path checks, terminology coverage, source-to-section coverage,
   contradiction scan, observability disposition, and a clean documentation diff review.
8. Complete this closeout, account for every changed file since `841497d`, run the required
   observability checks, advance the ledger and soft clock in lockstep, and report any checks
   that remain `UNREAD` rather than implying success.

## §4 Commission Addendum

After the first glossary pass, Rab added: **"Make sure the terminology can be translated for an
engineer to understand, or a developer to understand too. Add the translation into it."** The
scope document therefore carries both a domain glossary (§14) and a separate three-column
engineering translation index (§15): project term, conventional software-engineering analogue,
and its concrete implementation meaning here.

## §7 Implementation Delta

This was a documentation and evidence session; no runtime behavior was changed.

1. Added `docs/38-file-portal-full-system-scope.md`, a 17-section whole-system reference covering:
   evidence vocabulary and work plan; definition and boundaries; machines/authority; eleven
   end-to-end flows; component catalogue; runtime state classes; durable interfaces; configuration
   and dependencies; security/trust boundaries; lifecycle; verification; limitations; navigation;
   glossary; engineering translation; and scope/evidence maintenance rules.
2. Added this S95 closeout before implementation work and then completed its evidence/accounting.
3. Added the S95 `CHANGELOG.md` entry.
4. Did not modify application source, configuration, tests, the attached snapshot, or the
   pre-existing untracked `.agents/` directory.

The supplied attachment at
`C:/Users/Bndit/Downloads/file-portal-feat-library-pipeline(6)/file-portal-feat-library-pipeline`
contained 294 files and no `.git`. After CRLF/LF normalization, every file matched its active
tracked counterpart. There were 123 raw-hash differences attributable to the line-ending
comparison. The active repository had only the newly opened S95 session record beyond that set.
No instruction-like content in the attachment was treated as user authority.

## §8 Decision Ledger

| Decision | Evidence and reason |
|---|---|
| Describe current source separately from live deployment. | Origin, process table, ThinkPad, and vault tip were unread at open; an installed hash alone does not establish behavior. |
| Treat the attachment as corroboration, not authority. | It was a non-Git snapshot and normalized to current tracked content; the user explicitly distinguished attached instructions from the request. |
| Record `convert-gpu` as configuration-dependent. | `transfer.rs` implements the route, while current widget defaults/`portals.json` omit its portal; `send_files` validates configured categories. |
| Keep atomicity, hashing, durability, semantic fidelity, and policy approval separate. | Each is established by a different code path/evidence type; collapsing them would overstate assurance. |
| Preserve the observability acceptance red. | 38/39 is the executed result. Current source renders `recent_audits`, so the single failure is classified as answer-key drift rather than hidden. |
| Mark Linux tests unread, not failed. | The local offline environment could not resolve cached `watchdog`; the suite never executed. |
| Add a separate engineering translation index. | Rab asked that terminology be directly translatable for engineers/developers; analogy and concrete meaning avoid asking them to infer domain language. |
| Do not repair product/detector issues in a scope-document commission. | The authorized outcome was a truthful plan and full-system document; fixes are recorded as follow-up work. |

## §9 Verification Evidence

| Check | Result |
|---|---|
| MUSTER session bootstrap | Exit 0; hard S94/`841497d`, soft tally 74, and existing memory hook reconciled. External origin/process/ThinkPad/vault remained unread as printed. |
| Attachment/source comparison | 294/294 corresponding files normalized equal; no divergent implementation content. |
| Widget Rust tests | 23/23 pass. A path-canonicalization warning was non-fatal. |
| Widget JavaScript parse | `src/main.js` and `src/room.js` pass Node syntax checking. The attempted HTML input was rejected as an unsupported extension and was not classified as a product test. |
| Maintained Python compilation | Pass for Windows converter, Linux receiver/converter/dashboard, Repair Bench, and observability sources. |
| `events_selftest.py` | 7/7 pass. |
| `deferral_gate_selftest.py` | Pass; every encoded tripwire fired. |
| `room_chat_acceptance.py` | 24/24 pass. |
| `observability/acceptance.py` | Red 38/39; stale expected `recent_audits` glitch against source that now renders it. |
| Full glass census | Advisory: 64 unsigned glitches at 70 sites across three lanes; limitations require human classification. |
| `glass_detector.py --since 841497d` | Pass: zero keys/sites and zero unsigned glitches for the documentation-only delta; detector disclaimer retained. |
| Linux unit suites | Unread: offline dependency resolution lacked `watchdog`; no test result was produced. |
| Reference/path validation | Every explicitly navigated repository/document path in docs/38 exists. |
| Markdown/diff hygiene | `git diff --check` passed before closeout completion. |

## §10 Known Failures and Unread Surfaces

- The observability acceptance suite is **red 38/39**, not green. The one red expectation lags S94's
  source rendering of `recent_audits`; fixing that guard is separate work.
- Linux unit suites are **UNREAD**, not failed: the offline environment lacked cached `watchdog`.
- The initial attempt to invoke a remembered bundled-Python path failed because that path did not
  exist in this desktop runtime. The configured dependency locator supplied the valid runtime; the
  scoped detector then completed cleanly.
- A JavaScript syntax-check command was mistakenly pointed at an HTML file. Node correctly rejected
  the extension. The HTML was not called defective and the error was excluded from product evidence.
- Origin fetch, process-table observation, ThinkPad state/configuration/queues, and vault tip remained
  unread. No health or parity inference was made.
- The global bootstrap instruction's `.Codex/.../Codex-Memory-Backup` path was absent while the
  tracked muster used the existing `.claude/.../Claude-Code-Memory-Backup` path. The canonical
  namespace remains unresolved.

## §12 Uncertainty Register

The full limitation register lives in docs/38 §12. Highest-consequence unresolved items are:
configuration-dependent GPU portal availability; no comprehensive Windows-converter CI suite;
no Desktop shipment arrival inventory/digest acknowledgement; incomplete Linux publication
durability contract; image-only DOCX routing weakness; shared Linux status writer race; no hard
timeout for every in-process converter call; advisory rather than universal GPU coordination;
document-level rather than entailment-level assistant citations; unsigned human repair credit;
cross-lane manifest/schema divergence; and unread deployed/runtime/remote state.

## §15 Symptom Signatures

- **SYM-001:** consequential documentation claims were cross-checked with source/tests or explicitly
  downgraded; external deployment was not inferred.
- **SYM-027:** `recent_audits` is currently rendered, but the observability acceptance answer key
  still expects it off-glass, producing the honest 38/39 red.
- **SYM-029:** attachment comparison normalized CRLF/LF before judging content; raw byte differences
  were retained in the accounting.
- **SYM-031:** failed dependency, origin, process, ThinkPad, vault, and runtime-path probes were
  rendered as failures/UNREAD rather than zeros or absence.
- **SYM-039:** stale/outdated document filenames discovered in the first navigation-map pass were
  corrected against the actual `docs/` inventory before close.
- **SYM-040:** origin remains explicitly unread because fetch did not yield a trustworthy result.

## §17 Current State

`docs/38-file-portal-full-system-scope.md` is the new cross-system orientation and evidence map. It
does not replace focused contracts. It translates 100+ File Portal/domain terms in the glossary
and maps the most important ones to conventional engineering concepts and concrete code meaning.
The active product source remains S94 behavior at baseline `841497d`; S95 changes documentation
only. The local tests listed in §9 retain their exact outcomes. No live ThinkPad/vault/origin/process
claim was earned. The pre-existing `.agents/` directory remains untracked and outside session work.

## §18 Next Entry Point

1. Read `docs/38-file-portal-full-system-scope.md` first, then follow its §13 navigation map to the
   owning focused contract/source.
2. For assurance work, update the stale observability `recent_audits` expectation without weakening
   the tripwire, then build the planned Windows-converter fixture/CI stage.
3. For integrity work, specify and test Desktop bundle arrival inventory/digest acknowledgement and
   Linux publication durability/recovery.
4. For deployment truth, fetch origin and perform the installed-widget plus ThinkPad/vault exercise;
   preserve UNREAD if the machines remain unreachable.
5. Reconcile the `.Codex` versus `.claude` canonical memory namespace before migrating or duplicating
   memory data.
