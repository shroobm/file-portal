# S164 — Desktop — 2026-09-16 — THE RUNNER: the converter's hermetic suites at the close, warn-only (Claude Opus 5's eleventh sitting in the Fable lane; the autonomy experiment's nineteenth)

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

No new word of his opens this sitting; it opens on the register under his standing words:

> "Run autonomous until I say stop. Keep doing tasks, be persistent." (Desk d7d03d67, S157)
> "You shouldn't have needed to wait for me to do work… it's better if you're doing something than waiting for a response, in case you're waiting for my signature." (Desk 7663789d, 17:06:27Z)

The row taken: `close/run-the-hermetic-converter-suites` (S163 E4): none of the converter's hermetic selftests is run by CI (marker-env only; `figure_coverage_selftest.py` the one exception) or by `close.sh` — every tripwire born S157–S163 is exercised only when a lane runs it by hand. "A guard nobody has watched fire is a proxy with a reputation (docs/32 §5)." Build: a `[3c] CONVERTER` row in `close.sh` in the RUST row's shape, WARN-ONLY first (the S108 standard: every gate change warn-only until observed; arming is his signature), with a `selftest.sh` case that plants a failing suite and watches the row go red.

## §2 Starting State — the `open.sh` card, verbatim (2026-09-16T20:03:01Z)

```
════════ MUSTER · OPEN · 2026-09-16T20:03:01Z ════════
[0] IDENTITY
    ledger newest    S163  18f2ca5
    this session     S164  ·  desktop  ·  2026-09-16
    session marker   coordination/private/session.current = S164 (guard_record / guard_git read it)
    advisory         S164 unnamed in the inherited tree
[1] GROUND
    verifier         07BD3BA7  (repo == ~/.claude)
──────── MUSTER · 2026-09-16T20:03:01Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 177 (tally == hook)
[3a] LEDGER ORDER  ✓ 146 Desktop-lane rows ascend (S16 → S163)
[3b] LEDGER PARSE  ✓ 146/172 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · unchanged since the close 18f2ca5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ 18f2ca5 (S163) == ledger 18f2ca5, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 8 · anchor 25 · pending 0 · drop 0
    intake           waiting 0 · oldest 0s · active null · written 1s ago by pid 13620
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    events           391 line(s)
    open-tasks       98 item(s) · last written 21 minutes ago
    error-bin        109 row(s) — read the CLASS table before you probe · last written 35 hours ago
    symptoms         133 row(s), 29 open · last written 21 minutes ago
    relay            208 entries · run `gate.py status` for the board
    widget           10056
    widget autostart task 'File Portal widget (J57)' Running — AtLogOn · auto-logon ON (Bndit)
    task             'File Portal desk (relay)' Running LASTRUN=2026-09-15T22:51Z RESULT=267009
    task             'File Portal relay watch (Fable)' Ready LASTRUN=2026-09-15T22:50Z RESULT=0
    task             'File Portal tracker' Running LASTRUN=2026-09-15T22:51Z RESULT=267009
    task             'File Portal desk watch' Running LASTRUN=2026-09-15T22:51Z RESULT=267009
    python procs     7
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    adoption         C4559805 (2026-09-04T02:3xZ, by rab) · installed C4559805 — MATCH
    vault            7 note(s) · tip f489189
    thinkpad         UNREAD — host did not answer; NOT a statement that it is down or clean
[3] PIN
    --since          18f2ca5
    closeout         sessions/S164-desktop-2026-09-16.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

**The pin:** `--since 18f2ca5` (S163's closing SHA; the row `90d2867`; CI green run 35142666886). The marker `coordination/private/session.current` = S164. `id_preflight.py --next S`: `next free S: S164`, free. **This record precedes every write of this sitting** (S163's slip: a script insert ran before its record).

## §3 Inherited, stamped Historical

- **S163's post-close CI** (6b13fc9): GREEN, run 35143320464 / num 478, both jobs — Observed 20:02Z by `ci_observe.py` (docs/21 §9). S163's Gmail draft r7316401978519029830 (never sent).
- **The lessons in force:** the record before any write, whatever writes it; creation and commit two calls; no heredoc for a patch; `grep -cU`; the clock read before a heading; the Desk watcher filtered `from=Rab` (b6ptf7xgb armed 19:52Z); the ThinkPad offline (his word); a gate change is warn-only first and its arming his.
- **The ground (Observed at the open):** `close.sh` runs `[1] DIFF · [2] GLASS · [3] RUST (only when windows-widget/ changed since the pin; fmt leads) · [4] CI · [5] LEVERS · [6] DOCTOR · [7] CENSUS · [8] REGISTERS · [8a] MEMORY LIBRARY · [8b] DEBT · [9] PUSH`; `$PY` is the marker-env interpreter already (the GLASS row uses it); `selftest.sh` is at **113/113** with cases 15–18 on close.sh (a missing pin; an unresolvable pin; a real historical red CI; the UNREAD discipline) and case 44 on DIFF's writer attribution. The suites the row names: `convert_and_ship_selftest.py` (268), `watch_and_convert_selftest.py` (22), `table_geometry_selftest.py` (208), `marker_blocks_selftest.py`, `degeneration_selftest.py`, `analyst_selftest.py`, and the bench's `test_table_boundary.py` (14) + `test_generated_md.py` (4) — each hermetic (a temp `FP_PIPELINE`, stubbed Marker, no GPU).
- **Rab's standing words in force (Historical, unchanged):** the persistent method; AUTO MEANS NEVER IDLE; scripts private; the GPU law (no engine run); no rebuild/adoption/drop; never edit a file a running process reads (`close.sh` is read by nothing running; `~/.claude/muster.sh` is the mirror of `muster.sh`, untouched here); a Desk post answered on the Desk; nothing to Codex.

## §4 The echo (what "warn-only" means here, before the row is written)

The new row runs the hermetic suites under `$PY` when `windows-converter/` or `prototypes/repair-bench/` changed since the pin, prints one row per suite with the suite's own count or its RED, and **does not set `red=1`** — the close's exit code does not move on a red suite until his arming flips one line (`FP_CONV_ARMED=1`, or the line itself). The row says "warn-only" on every RED so nobody reads the close's 0 as the suites' green (SYM-046's family: an exit code cited as a verdict nobody read the flags for). Two readings: **(a)** the list of suites fixed in `close.sh` (the eight named), overridable by `FP_CONV_SUITES` for the selftest's fixtures; **(b)** every `*_selftest.py` under `windows-converter/` discovered by glob — which would also run `deferral_gate_selftest.py` (launches the REAL watcher — ~40 s of process orchestration, excluded by CI for that reason) and `card_mutex_selftest.py` (an OS mutex). **DEFAULT (a):** a named list, hermetic by construction, the two live-process suites out by name. A `selftest.sh` case plants a failing suite through `FP_CONV_SUITES` in a fixture repo with a `windows-converter/` change and asserts the row reads RED while the exit stays 0 (the warn-only property, asserted); a positive control asserts a passing suite reads clean; a third asserts "skipped" when nothing under the two dirs changed. His correction re-aims.

## §5 The method

Episodes with receipts (`close_ep.py 164 <K> <since> "<category>"`; the first `<since>` = S163's whole receipt END 2026-09-16T19:46:09.485Z), a cookie per episode, a Desk line per episode, the carry-over updated. **E1** the row in `close.sh` + the three `selftest.sh` cases; `selftest.sh` before and after (the count read); `close.sh <pin>` run once on this tree to see the row live (`windows-converter/` changed since the pin? — no: this sitting changes the skill, not the converter; the row reads "skipped" here, and the fixture cases are the proof); the register row noted (BUILT warn-only; arming his). Then the close — whose own `close.sh` run carries the new row.

## E1 — THE RUNNER: `[3c] CONVERTER` in `close.sh`, warn-only, with its tripwires (20:03Z → 20:32Z, the clock read)

**The act (Observed; `.claude/skills/muster/close.sh`, LF kept; the record first this time):** after `[3] RUST`, a `[3c] CONVERTER` row in the same shape — `touched_conv` when `git diff --name-only <pin>..HEAD` matches `^(windows-converter|prototypes/repair-bench)/`; `CONV_SUITES` the eight named hermetic suites (`FP_CONV_SUITES` overrides, for fixtures); each suite run from its own directory under `$PY` (the marker-env interpreter close.sh already uses for GLASS) with `PYTHONIOENCODING=utf-8`; one row per suite — `clean — <the suite's own tally>` (the tally grepped from the suite's last lines: `GREEN (n/n)`, `ALL OK: n/n`, `ALL TRIPWIRES FIRED — n/n`, `SELFTEST PASS`, `Ran n tests`) or `RED (warn-only — the S108 standard; arming is Rab's)`; a missing file reads UNREAD, a missing interpreter reads UNREAD; a summary row on any red: `RED but WARN-ONLY — the exit code does not carry this` unless `FP_CONV_ARMED=1`, when it reads `RED — … ARMED: the close stops here` and sets `red=1`. `deferral_gate_selftest.py` (launches the real watcher) and `card_mutex_selftest.py` (an OS mutex) OUT by name, as CI keeps them.

**The tripwires (`selftest.sh`, cases 61–63; the suite 113 → 119, ALL TRIPWIRES FIRED):** a fixture repo with a `windows-converter/` change since its pin, a planted `fail_selftest.py` (exits 1) and an `ok_selftest.py` (prints `GREEN (3/3)`), the fixture's own real interpreter: **61** the failing suite reads RED on its row; the summary row says WARN-ONLY out loud; the exit code is the SAME with the red suite as with the green one (the differential reading — whatever the fixture's other rows say); **61b** ARMED (`FP_CONV_ARMED=1`), the same red stops the close (exit 1, the ARMED row); **62 POSITIVE CONTROL** a passing suite reads `clean — GREEN (3/3)`; **63** an untouched converter reads skipped and runs no suite. **The control earned its keep on the first run:** the fixture's interpreter resolved to `python3` on PATH — the Store stub that exits 49 (the muster skill's own note) — so the planted RED "passed" for the wrong reason and 62 went red; the interpreter now prefers marker-env, then the uv python, then PATH.

**Live (Observed):** `close.sh 18f2ca5` on this tree → `CONVERTER skipped — no windows-converter/ or prototypes/repair-bench/ change since 18f2ca5` (this sitting changes the skill, not the converter); `close.sh bbcaf58` (S163's tests inside the range) → the eight rows clean with their tallies — 268/268 · SELFTEST PASS · 208/208 · 57/57 · 26/26 · 47/47 · 14 tests · 4 tests — in 57 s of `close.sh` end to end (20:30:05Z → 20:31:02Z, the CI probe included). **The register:** the row noted BUILT warn-only, arming his (`e1_record.py --go`); the count unchanged at 98. No `muster.sh` change — `~/.claude/muster.sh` untouched, no re-stamp owed.

## E2 — gained / lost / missing (20:33Z)

**Gained.** The runner: `close.sh` `[3c] CONVERTER` — the converter's eight hermetic suites (268 + 208 + 57 + 47 + 26 + 22 checks and the bench's 18 tests) run at every close that touched `windows-converter/` or `prototypes/repair-bench/`, one row per suite with the suite's own tally on the close card; WARN-ONLY (a RED row says so; the exit unmoved until `FP_CONV_ARMED=1` — arming his); six tripwires in `selftest.sh` (113 → 119) including the differential exit reading and the ARMED path; the positive control that caught the Store-stub interpreter on its first run; live against bbcaf58: eight rows clean in 57 s. The tripwires born S157–S163 are now proof at every close that touches their code, not only when a lane remembers — read on the card, gated on his word.

**Lost.** Nothing shipped; nothing of his moved; no engine, no model, no PDF, no bundle written; the card untouched; no fleet; `muster.sh` untouched (no re-stamp owed).

**Missing.** His words: the runner's ARMING (`FP_CONV_ARMED=1`, or the line in `close.sh`, once he has seen the row on a close card); the `UNFRAME_STUBS` lever; the widget rebuild + adoption (six source fixes); the ThinkPad's restart; the `verdict_phase` restart window; the §12.5 signature; `reanalyse Valentine`; D1; the experiment's end; the Linux port; the `drop_repeated_text` lever; J35's shape; his answer to the question on the Desk. The lane's, next: the four tripwires that wait (SYM-052's JS case beside `test_table_health.js` — runnable under node here; SYM-011/012 for CI's log; SYM-023 with the rebuild); SYM-117's selftest; then the rows one hand of his unlocks.

## §7 What was built or changed

- Public (6 files since the pin 18f2ca5 — one block each in the private `sittings/S164/code_notes_S164.md`): `.claude/skills/muster/close.sh` (the `[3c] CONVERTER` row), `.claude/skills/muster/selftest.sh` (cases 61–63), `OPEN-TASKS.md` (the runner row noted BUILT warn-only), `sessions/S163-…` (S163's post-close §20, inside the pin's range), this record, the JOURNAL. A gate change, warn-only, with its tripwires.
- Private: `sittings/S164/` — the open record's writer, the record appender, the selftest and live-close outputs, the Desk texts, the cookie script, the carry-over.

## §8 Findings

1. A guard is only as real as its runner: five tripwires landed in S163 and none would have run again until a lane remembered; one row in the close makes every close that touches the converter run them — the difference between a test file and a gate.
2. Warn-only is a shape, not a mood: the row prints RED in words, the summary says the exit does not carry it, and one environment variable flips it to a stop — so the S108 standard ("every gate warn-only until observed") is mechanical and his arming is one line, not a rewrite.
3. A positive control catches the wrong reason: the planted failing suite "read RED" on the first run because the fixture's interpreter was the Store stub — every suite would have failed, and the case would have passed. The control (a passing suite must read clean) went red and named it. A negative control alone proves nothing about WHY.
4. The exit code's meaning is measured differentially: in a fixture whose other rows may be red for their own reasons, "warn-only" is asserted as "the exit with the red suite equals the exit with a green one", not as "exit 0" — the assertion survives the fixture.
5. The record before the act, kept this time: the first write of the sitting was the record (S163's slip inverted), and the guard had nothing to catch.

## §10 Known failures / residue (open at close)

- His words, above (§E2 Missing) — the arming first among them.
- The lane's own errors this sitting: the fixture's interpreter resolution (the Store stub) — caught by the control and fixed; a bash quoting slip in a case's label (a nested apostrophe) — a syntax error on the first run, fixed.
- The four tripwires that wait; SYM-117's tripwire UNREAD; the row is warn-only until his arming — a red suite at a close does not stop the close yet.
- SYM open 29 of 133 rows; register 98.

## §15 Current state (Observed at 20:33:48Z)

- The card: `1069 MiB, 10240 MiB, 8 %` (nvidia-smi); `.gpu-lock` absent; no model of the lane's loaded this sitting; no fleet.
- The seat: `>bndit                 console             1  Active      none   2026-09-15 6:50 PM` (query user).
- The registers (the muster's counter): open-tasks 98 · symptoms 133 rows, 29 open.
- The public tree: 0 tracked change(s) before the close chain. The private tree: 1 (the services' own appends).
- The monitors: the Desk watcher (bqozzqzn7, re-armed 20:29Z, filtered `from=Rab`, a 30-minute cap) — stopped by the close; the tracker is his boot service; the ThinkPad offline by his word.

## §18 Next entry point

1. The rule: the next sitting opens on the register at the next wake unless his word arrives first. What needs nothing now: SYM-052's JS case beside the bench's `test_table_health.js` (node; the bench's page-side suite); SYM-117's selftest beside `memory_timestate.py` (private); SYM-011/012 for CI's log (their shape read first in `linux-converter/tests/`). Then the rows one hand of his unlocks.
2. His words: the runner's arming (the first close card that shows the CONVERTER rows is the observation the S108 standard asks for — this sitting's own close reads `skipped`, S165's would run them if it touches the converter); the six-fix rebuild; the `verdict_phase` restart window; the lever; the §12.5; the question on the Desk.
3. The apparatus: a gate row in `close.sh` = the RUST row's shape + a `touched_*` diff test + one row per check with its own tally + WARN-ONLY with an `FP_*_ARMED` line + three `selftest.sh` cases (a planted red with the differential exit, the armed stop, the positive control, the skip).

## The receipts (tiled onto S163's whole receipt's end, 2026-09-16T19:46:09.485Z)

- E1: window 2026-09-16T19:46:09.485Z → 2026-09-16T20:31:55Z · calls 45 · cost 22.23 (fleets 0.00)
- Receipts: 1 over the episodes (the windows are contiguous from S163's end) · main-lane tool calls in the receipts: 45 · cost in the receipts: 22.23 USD-eq (fleets 0.00 — none) · the sitting's whole receipt is the close chain's.
## §19 Close values — `close.sh 18f2ca5`, 2026-09-16T20:36:09Z (verbatim)

```
    PIN              18f2ca5
    DIFF             7 file(s) since 18f2ca5 · dirty 0 = mine 0 · peer-owned 0 (none) · 1 untracked
    GLASS            clean since 18f2ca5 (--enforce; bare runs exit 0 regardless — SYM-046)
    RUST             skipped — no windows-widget/ change since 18f2ca5
    CONVERTER        skipped — no windows-converter/ or prototypes/repair-bench/ change since 18f2ca5
    CI               no run for 7ea53f93 yet (unpushed? ahead=1) — NOT a green
    LEVERS           no unlevered threshold constants added since 18f2ca5
    DOCTOR           lever lexical parity: figure-triage.txt names 8 · quoted refs 8 · MISSING 0
                     LEXICAL REF: mode · min_area_pt2 · vector_min_paths · cluster_gap_pt · text_coverage · words_per_line · table_overlap · accounted_for
                     UNREAD — a quoted lexical occurrence does NOT prove the consumer reads the lever; ARMED on MISSING only
    DOCTOR-HEAD      UNREAD — closeout-headline probe re-run not yet mechanized; NOT a statement the numbers reproduce
    CENSUS           selftest.sh: declared 11 = FIRED 11 = banner 11/11 (exit 0)
    CENSUS-SCOPE     UNREAD — broader TEST-STRATEGY fixture census not yet mechanized; NOT a statement all promised tripwires ran
    REGISTER         OPEN-TASKS.md written this session: +1 / -1 lines
                     STRUCK 0 · ADDED 0 — net 0 — nothing was worked down; say so in the closeout rather than imply it
    MEMORY           clean — soft clock is DURABLE at 6ef4007
    DEBT             open SYM 29, unchanged since 18f2ca5
    PUSH             ahead of upstream: 1 (the close pushes; 0 after)
════════ close exit 0 — values only; the judgment half is SKILL.md ════════
```

- **DIFF:** every changed file is in the content commit `7ea53f9` or this closing commit; the row follows, then
  `row_check.sh 164` on it, then the push.
- **Relay:** no entry — nothing to Codex (standing).

## §20 Post-close (UTC) — CI observed, memory in lockstep

- **CI, `Observed`:** 35147545431 (`ci_observe.py`, on the row commit `48e12df`).
- **Memory, in lockstep:** library commit `e04e8a2` — TIME-STATE → Desktop S164 / row `48e12df` / this CI; 17489 bytes under
  the 17,510-byte read-limit hook; pushed to the private remote.
- **Both clocks:** `muster.sh` clean after the lockstep; the row passed `row_check.sh 164` before the push.
- This §20 is a post-close commit and gets its own CI run, reported in the reply, not here.
- **The register's net, justified (close.sh: `OPEN-TASKS.md written this session: +1 / -1 lines` — STRUCK 0 · ADDED 0, net 0, "nothing was worked down; say so"):** said: 98 at the open, 98 at the close. The one line changed is the runner row `close/run-the-hermetic-converter-suites` noted BUILT warn-only; it stays open because its close is his — the arming (`FP_CONV_ARMED=1`, or the line in `close.sh`) after he has read the CONVERTER rows on a close card. Nothing struck, nothing added; the sitting built a runner for five guards that had none. DEBT `open SYM 29, unchanged`.
- **The first live appearance of the row in a real close (Observed, §19):** `CONVERTER        skipped — no windows-converter/ or prototypes/repair-bench/ change since 18f2ca5` — this sitting touched the skill, not the converter, so the eight suites did not run at this close; they ran once against `bbcaf58` in E1 (eight rows clean in 57 s, private `sittings/S164/e1_close_live_bbcaf58.txt`). The next converter-touching close is the row's first live run inside the chain.
- **The chain's stops before the push:** clean at the first run, exit 0 — `brief_check` 60 briefs · 6 retired · 0 defects; CI's local gates green (schema 0 · acceptance 0); `close.sh` clean — DIFF 7 files since 18f2ca5, dirty 0, 1 untracked (`.codex/`, not mine); GLASS clean (`--enforce`); RUST skipped; CONVERTER skipped (above); CENSUS 11 = 11 = 11; the row `48e12df` (173 of 173), `row_check` ✓; occupant 55 of 55 messages claude-opus-5; push exit 0; CI green on the row commit, observed by the chain.
- **The code receipt, clean at the first run:** `journal/code/S164.md` reads commits 5 · files 7 · explained 9 · unexplained 0. No revision.
- **The record's own slip, said:** the closing sections are headed "E2 — gained / lost / missing" by the receipt count (one receipt, E1); the heading is right this time by construction — one episode, one receipt — and the note stands so the next reader knows the numbering is the receipt count, not the episode count.
- **Receipts (whole sitting, window 2026-09-16T19:46:09.485Z → 2026-09-16T20:35:08.544Z, tiled onto the S163 whole):** `journal/{calls,code,cost,usage}/S164.md` — calls 55 · preloaded 10 · consulted 3 · scratch 0 · inferred 18 · harness 24 · denied 0 · errors 0 · lanes 0 (no fleet) · workflows 0 · cost **27.46 USD-eq** (session 27.46 + fleets 0.00) · commits 5 · files 7. The one episode receipt (contiguous from S163's end). `journal/procs/S164.md`: UNREAD — the tracker is Rab's boot service; the fixture suites of cases 61–63 exit on their own (no process tree to reap).
- **Baseline row S164 (`measurement/baseline/S164.json`, read never typed):** `baseline.py S164 --tickets 0 --chain-exit 0` — calls 55 · denies(log) 0 · cost 27.46 · UNREAD fields: tracker, refuter.
- **Denies:** none this sitting — the record was the first write, so the record-first hook had nothing to refuse; no forbidden verb reached the guard. (The guard did refuse one command AFTER the close, in the post-close: a `$PY` variable as a command head — `guard_git`'s J56 rule; rerun by literal path, no loss; it lands in S165's window.)
- **The relay at the close (Observed):** no entry — nothing to Codex; the Desk carried the E1 line; no post of his this sitting; the close report follows this commit.
- **The memory in lockstep:** MEMORY.md at 17,489 bytes (`e04e8a2`; the fold: the S163 block to one line, the S161 block to one, the S164 block above them — 21 bytes under the limit, so S165's fold must compress a block below S160 before it adds); cookies #178 (E1) and #179 (this closeout) under his standing grant, each committed and pushed on `main`; TIME-STATE → S164 / `854182b` / row `48e12df` / CI 35147545431.
- **CI:** GREEN on the row commit `48e12df` — run 35147545431 / num 481, both jobs (python 20:38:00Z, rust 20:40:45Z), observed by the chain; this §20's own post-close commit gets its run, reported in the next sitting's §3 (docs/21 §9).
- **The card at the close:** 1,069 MiB of 10,240 at 8 % at §15's write (20:33Z) — NOT mine: no engine, no model, no probe of the lane's on it all sitting; `.gpu-lock` absent throughout.
- **The seat:** console session Active (his) throughout; the ThinkPad offline by his word.
- **The monitors after this commit:** the Desk watcher (bqozzqzn7, armed 20:29Z, filtered `from=Rab`) is stopped by the close's last act with the marker; the tracker service is his.
- **Rab's words this sitting:** none. The question on the Desk (broke-first or decide-first) stands; the next sitting opens on the register at the next wake unless his word arrives first — SYM-052's JS case, SYM-117's selftest, SYM-011/012 for CI's log.
