# S83 · Desktop · 2026-08-16

## §1 Session Intent

Rab's commissions, in his words, both verbatim:

> "re-measure with A-B-A on a cooled card"

and, mid-open, after switching the session's model:

> "You are now fable, look past since S77, and take in what might have happened, and implement
> solutions"

Read together: complete the measurement thread honestly (the cooled run, SYM-035 controlled), and
step back over the S78–S82 arc — find the patterns, and build the countermeasures this session
rather than recommending them to a future one.

**Written before any work. Left unedited for the rest of the session** (docs/21).

## §2 Starting State

`bash .claude/skills/muster/open.sh` — **true exit 0**. (A first paste showed exit 141: SIGPIPE
from my own `head -30` on the reader side, not the instrument — re-run captured to a file, exit 0.
A reading of the reader, not of the open.)

```
════════ MUSTER · OPEN · 2026-08-16T04:32:30Z ════════
[0] IDENTITY
    ledger newest    S82  8fb374e
    this session     S83  ·  desktop  ·  2026-08-16
    advisory         S83 is named in the INHERITED tree (8fb374e) — read each, do not blanket-correct:
                     sessions/S82-desktop-2026-08-15.md
[1] GROUND
    verifier         344E1277  (repo == ~/.claude)
[1] MEMORY ....... ✓  [2] SOFT clock ... ✓ 72  [3a] ✓ 67 rows S16→S82  [3b] ✓ 67/88
[3] HARD clock ... ✓ 8fb374e (S82) mirrors ledger, ancestor of HEAD
    WORKING TREE . 0 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile · muster exit 0
[2] LIVE
    pipeline         held 4 · anchor 23 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=16
    gpu-lock         absent · events 137 · widget 11280 · python procs 2 · ollama 2
    installed exe    C3D277F7 · vault 6 note(s) · tip 70c60e6
    thinkpad         converter=active · vault tip 70c60e6 · staging 0 · receipts 5
[3] PIN
    --since          8fb374e
    closeout         sessions/S83-desktop-2026-08-16.md
```

**Advisory resolved:** the single `S83` hit is S82 §18 item 1's forward reference. No collision.

**Provenance:** session opened under Opus 5; Rab switched the model to **Fable 5** (`/model`)
between the mechanical open and this plan commit. Same session, same ledger identity, model
handoff recorded here per the naming discipline in the prompt-crafting protocol.

**Live state notes, `Observed` at open:** the widget is back — **one** instance (11280), the
S80-fixed card row reporting a single bare pid, watcher chain alive (11540 → 22476). Rab's hand
relaunched it after S80–S82's downtime. The intake watcher being alive means a PDF dropped during
the GPU run would put Marker on the card beside the measurement (SYM-022's precondition) — drop
is 0 and the preflight now warns, but nothing should be dropped while the run is on the card.
Ollama's own engine (llama-server.exe, PID 21240, parent `ollama serve`) held 6,959 MiB at open;
released via `keep_alive: 0` — VRAM 6,959 → 739 MiB, 52–53 °C idle before the run.

**Pinned `--since` = `8fb374e`.**

## §3 Pinned closeout terms

Unchanged: §7/§8/§10/§15/§18; `git diff --name-only 8fb374e..HEAD` fully accounted;
`glass_detector.py --since 8fb374e`; ledger row as a separate follow-up commit; both clocks
advanced together; §1 unedited.

## §4 In-scope symptom rows

- **SYM-035** — the reason this run exists. The cooled A-B-A is the control; the drift line is
  the most informative number in the output, and >5 % withholds the ratios by construction.
- **SYM-022** — watcher alive during a GPU run; preflight refuses on `.gpu-lock` (read as the
  converter's busy *signal*, never as a lock — SYM-032) and warns on a live watcher.
- **SYM-034** — ~120 requests this run; the 300 s bound and hole-recording get exercised again.
- **SYM-031 / SYM-029** — as always: failed probes render UNREAD; repo markdown stays CRLF.

## §5 The solutions, planned before they are built

The look-back (full analysis lands in §6 at close) resolves to five patterns; the buildable
countermeasures are listed here so intent precedes work:

1. **The prefill outlier judge becomes the arm's own median** (S82 §10.4's specified fix): the
   S82 draft judged against the single warmup measurement — the thing docs/21 rule 3 forbids
   trusting — and once withheld two legitimate readings against an anomalous warmup.
2. **The instrument gets its own tripwire suite** — `backend_parity_selftest.py`, offline, no
   GPU: every pathology this arc paid for (the ns/ms unit trap, cached prefill, UNREAD-never-0,
   the incumbent bar, the order-drift limit) banked as a fixture that fires.
3. **The card learns the llama-server lesson** — a row counting `llama-server.exe` processes with
   the S81 caveat that the *name* does not identify (Ollama's engine is also llama-server.exe;
   the parent decides), plus a selftest case.
4. **Two standing orders** in the muster skill: *a status sentence to Rab carries its probe*
   (three sessions running, an inference reached him dressed as an observation — always in a
   mid-work sentence, never in a closeout), and *a guard born today gets its tripwire today*
   (docs/32 §6 has held five sessions running).
5. **docs/34 rule 8: a ratio needs two runs** — generalising S82 §7.1: four ratios were published
   on one run each, and all four were withdrawn. Synced to manual ch.18 and the memory law.
   Flagged in §8 for Rab's countersign, since docs/34 is his signed law.

Then the cooled run (`-n 30 --cool-to 55`, same stride and chunks as S82 for comparability),
launched from the committed instrument so the run's code state is pinned in history.

## §6 Results

### §6.1 The cooled A-B-A — the ratios are admissible for the first time

`Observed` — `backend_parity.py -n 30 --cool-to 55`, launched from committed `beb4443`, same 30
chunks as S82. Cool-downs fired as designed (e.g. 79 °C → 55 °C in 231 s before the recheck).

```
ORDER DRIFT: incumbent decode 84.7 tok/s first, 83.4 last = -1.6%  GPU 79C -> 81C
(within the 5% limit - ratios below are admissible)
```

| arm | prefill tok/s (n=30) | decode tok/s (n=30) | e2e |
|---|---|---|---|
| ollama first | 3,950.1 (p50 3,934.0, p95 4,232.2) | **84.7** (p50 84.6, p95 86.1) | 8.7 s |
| llama.cpp `--jinja` | 3,241.6 | 80.9 — FAILED the token gate; speed inadmissible | 39.8 s |
| llama.cpp `+no-think` | **3,507.5** (p50 3,547.7, p95 3,756.9) | **84.8** (p50 84.7, p95 86.7) | 8.4 s |
| ollama recheck (last) | 3,908.7 | 83.4 | 8.9 s |

**Decode 1.00× (84.8 vs 84.7). Prefill 0.89×** — a small real gap consistent with the `-ub`
micro-batch hypothesis, worth ~1–2 % end-to-end at this workload shape. **The token gate held a
fourth time** on a fourth sample: `--jinja` alone FAIL, `enable_thinking:false` PASS.

Two caveats carried with the number, not footnoted away: (1) the whole run sat at ~84 tok/s where
yesterday sat at ~100 — the machine has **day-states**, mechanism still `Unknown` (SYM-035's
column), which is precisely why only within-run ratios are trusted and absolutes across days are
not comparable; (2) under **docs/34 rule 8** this is the *first* controlled run — 1.00× is the
first admissible measurement, not yet THE number. The architectural conclusion needs no more
precision than this.

**Conclusion for the migration thread:** the analyst gains nothing measurable from leaving
Ollama. Recommendation (§8, unsigned): keep the de-facto two-engine state — analyst on Ollama,
room-chat/bench-assist on direct llama-server — and close the thread.

### §6.2 The look-back since S77 — five patterns, five countermeasures, all built this session

1. **docs/32 §6 held five sessions running** (S78 tautology test → S79 seven-in-guards → S80
   partial-observation card → S81 three cache traps inside the document's own instrument → S82
   the forbidden single-measurement reference). Countermeasure: instruments get tripwires at
   birth — `backend_parity_selftest.py` (9 cases, every pathology this arc paid for), and the
   standing order *a guard born today gets its tripwire today*. **The suite fired on its very
   first run**, catching a partially applied patch before the GPU did.
2. **The tag law leaked in live sentences, never in closeouts** (S79 `.gpu-lock`, S81
   "healthy" hung run, S82 "thermal"). Countermeasures: standing order *a status sentence to Rab
   carries its probe*; the card's llama-server row meets the S81 misread at the exact place it
   happened (tripwires 15 → 16).
3. **Rates are fragile, content is robust** — every rate ratio S79–S82 died; the token gate has
   now survived four samples. Countermeasure: docs/34 **rule 8, a ratio needs two runs**
   (+ manual ch.18 + memory law), awaiting Rab's countersign.
4. **The migration outlived its premise** — resolved this session by §6.1: parity, measured
   admissibly. The remaining question is architectural and Rab's.
5. **The factory idled while the lab calibrated** — Cybernetics 26 days in `held/`, Damodaran 10,
   the standing queue unchanged through four closeouts. Not mechanically solvable from here: the
   bench needs Rab's hand. Recommendation recorded in §18; S84 (the GLM-OCR probe) is aimed at
   exactly this — a second reader to speed the bench work that drains `held/`.

## §7 Implementation Delta

| Commit | What |
|---|---|
| `c5d1d00` | open plan — both commissions verbatim, model handoff recorded, `--since` pinned `8fb374e` |
| `beb4443` | **median outlier judge** (S82 §10.4 closed) + `gate_verdict`/`order_drift_verdict` extracted + **`backend_parity_selftest.py` 9/9** |
| `accff3f` | card llama-server row (tripwires **16/16**) + two **standing orders** + **docs/34 rule 8** + manual sync |
| *(this close)* | S83 results + look-back; **SYM-036** filed (and it struck its own row while being filed — §10.2); PORT 7117→7127; CHANGELOG |

## §8 Decision Ledger — what Rab signed

**Signed this session, his words:** *"the word, S84 is the GLM OCR probe"* — S84 commissioned;
*"yes, pull the files when S84 opens"* — the ~1.44 GB download from `ggml-org/GLM-OCR-GGUF`
authorized by name, source and size.

**Put to him, NOT yet signed:** (1) docs/34 **rule 8** (flagged inside the rule text); (2) the
**two-engine steady state** and closing the migration thread — §6.1 gives him the measurement;
the decision is architectural and his.

## §10 Known Failures — mine, first-class

**10.1 The partial patch.** A multi-step in-memory patch aborted on one step and correctly wrote
nothing; my follow-up re-applied only the failed step, wiring a call to a function whose defining
step had been discarded. **The selftest written twenty minutes earlier caught it on its first
run** — the guard-born-today rule proving itself the hour it was written. The record run would
have crashed at its verdict.

**10.2 SYM-036 struck the row that documents SYM-036.** The symptom row explaining the harness's
backslash-collapse was itself mangled by that collapse — the `\n` in its own text arrived as a
real newline, splitting the row (2 insertions where 1 was expected, one lone LF). Caught by the
numstat check, repaired with `chr()`-built anchors — the row's own prescribed remedy.

**10.3 I told Rab "arm 3 of 4, 15–20 minutes."** The quoted chunks were the A-B-A recheck —
arm 4, minutes from done. The probe was cited (the new standing order held) but the
*interpretation* of which arm was printing was inferred from output shape, not from the arm
headers a few lines up. Cost: a wrong ETA. The sentence carried its probe; it must also carry a
correct reading of it.

**10.4 The harness listened inside room-chat's port range** (7117 ∈ 7110–7119) directly beneath
its own do-not-borrow comment, through three sessions of record runs. Never bit — room-chat was
down throughout — but a do-not-borrow comment that borrows is SYM-032 in miniature. Moved to 7127.

## §15 Symptom Signatures

- **SYM-036** (filed) — quoted heredocs through the session harness collapse one escape level;
  exact-match anchors containing backslash escapes silently find nothing (or split lines when
  written). `fixed` by rule: `chr()`-built anchors or the Edit tool; after an aborted multi-step
  patch, re-apply ALL steps.
- **SYM-035** — the A-B-A control's first *admitting* run (−1.6 %). The control now has both
  outcomes observed: withheld (+18.1 %, S82) and published (−1.6 %, S83). Day-state mechanism
  still `Unknown`.
- **SYM-034** — did not fire across ~120 requests. Still one observation, still unproven.

## §17 Current State

`Observed at close` — widget **1** (11280) with watcher chain alive; drop 0, pending 0, `held/`
4 (Cybernetics 26 days). Ollama serving and **holding the model resident** from the recheck arm
(~7 GB) — released at S84's open before the GLM load. llama.cpp `b10448` idle; ollama's bundled
engine is build `0b1bad14f` (probed — one codebase, two builds). Vault 6 notes, tip `70c60e6`.
Installed exe `C3D277F7`. muster tripwires 16/16; parity tripwires 9/9. Session ran on **Fable 5**
from mid-open. *(`Historical` the moment it is written.)*

## §18 Next Entry Point

1. `bash .claude/skills/muster/open.sh` — expect exit 0, **S84**, `--since` = this session's SHA.
2. **S84 = the GLM-OCR probe** (commissioned, download authorized): release ollama →
   `llama-server -hf ggml-org/GLM-OCR-GGUF` (Q8_0 950 MB + mmproj 484 MB; port **outside** both
   reserved ranges) — first question: does `b10448` load it at all → quarantine home
   `prototypes/glm-ocr-probe/` → render one Beer table-loop wreck site, one Valentine zone, one
   clean control from the anchor PDFs → side-by-side vs current bundle markdown → Rab judges at
   the bench. Gate = content fidelity (docs/34: the vendor's 94.62 stays `Historical`). Sampling
   note: 3 pages is `Inferred`, always.
3. **The wrapper decision** awaits Rab: two-engine steady state vs Ollama-off-the-machine. §6.1
   is the measurement; nothing further to measure unless he re-commissions the `-ub` experiment
   (low stakes: ~1–2 % end-to-end).
4. **Rule 8 countersign** awaits Rab.
5. Standing: a **product session** for `held/` (the bench, Rab's hand) — S84's probe feeds it ·
   Rab's ten real questions · the omission-class bank entry · docs/26 slice 3 ·
   `windows-converter/` still outside CI's lint set.
