# S75 — Desktop lane — 2026-08-13

**Closeout under `docs/21`.** Tier: EXTENDED — widget source changed; an exe is staged.

## §1 Session Intent
`Observed` — Rab, after the S74 three-lane mass audit landed in-chat: "Let's Open S75, and
finish that, then do a circle, then once your decisions listed are done absolutely, proceed
with some agents on discovering semantic and visual catering towards the operators
simplicity." Plus the Bench doctrine statement (§16 below) and the standing agent grant
("You agents when needed"). Cookies #62–64. Plan `4b9f653`.

## §5 Observable Contract
`Intended` — Rab, after adoption of **`B7F720C4`** (one Copy-Item, widget closed first if
running):
1. Wall surface — **fails if** any Dock status text or algedonic chip paints over it, or if
   the foot row sits below the fold on launch.
2. Windows reduce-motion ON — **fails if** ANYTHING still moves: no assay pulse (Dock dot,
   rail key, Wall dot), no drill zoom (fade only), no vault glow/spin, no spark spins, no
   keycap travel.
3. Light theme or indigo/teal accent — **fails if** the assay pulse or vault glow still
   flashes the dark-theme terracotta instead of the theme's own clay.
4. The Dock line's ▤ — **fails if** it still reads "—" while the vault bar has state
   (expected: ✓ when up-to-date, terracotta +N when notes await your pull).
5. The card caption on a pre-audit manifest — **fails if** it ever reads "null lane".

## §7 Implementation Delta
`Observed` — `docs/26-mass-audit-findings.md` NEW (the S74 audit filed: 15 graded findings,
gate split, signature sheet, Bench-doctrine note). `styles.css`: rl-fail/wl-fail classes ·
extended rail + Wall reduced-motion blocks · the S75 completion block (drill fade, vault
glow/spin, pf spark, assay dot, rp-bar) · surface-wall hide for #status/#algedonic-chip ·
wall-mode `calc(100vh − 26px)` · token-derived assay-pulse/vault-glow. `room.js`: inline
animation writes REMOVED (rail + Wall) → class derivations · wv-attn from `sv.word` ·
guard comment (circle observation). `main.js`: `escHtml(st.kind)` ×2 · `stSet(stLib, …)`
in every vaultApply branch. Plus CHANGELOG / CLAUDE_README / this file. Commits `d83104b`
(docs/26) · `f270f30` (repairs) · `b909758` (guard comment). **fidelity_audit.py, assay.rs,
docs/25 untouched — the semantic fence.**

## §8 Decision Ledger
1. **The Room keeps #status; only the Wall hides it** — the Room's handlers (bless refusals,
   bench errors) report through that sink; silencing it there would swallow real feedback.
   The Wall has its own ⚑ line, so the chip is redundant only THERE.
2. **Drill under reduced-motion = fade, not nothing** — the overlay appearing with zero
   transition reads as a glitch; opacity-only is the vestibular-safe minimum.
3. **#st-lib wired, not deleted** — the six-station grammar wants its ▤ alive; values mirror
   vault_check (the data the Dock already fetches — projection law, zero new reads); attn
   only on a delivery-to-pull (docs/13: the vault's one hand-required state).
4. **The circle's fragility observation → a comment, not a guard clause** — the invariant
   (convert/vault rows never carry verdicts) is structural to `defs`; a runtime guard would
   be dead code tested by nothing. The comment warns the only person who could break it:
   a future editor of `defs`.
5. **Acceptance by two lanes, not three** — scope-proportional: repairs-vs-findings +
   regression sweep cover claim-completeness and collateral damage; a provenance lane had
   nothing new to trace.

## §9 Evidence
`Observed` — Byte-audit vs `481cd3c`: Room 2100 chars, diffs = clock digits only (i=86–87);
Wall zero; errs []. Harness live: assay rail glyph `rl-glyph rl-glow fp-gv3 fp-ga3 rl-fail`,
inline animation GONE, computed animation-name `assay-pulse` (class-driven ⇒ media-query
killable); #status/#algedonic-chip display none under surface-wall, block under
surface-room; Wall dot `wl-fail` same pattern; verdict `wv-attn` on ATTENTION; presslog
exactly `["convert"]` once, class gone next poll. Circle lane 1: six FIXED-COMPLETE +
fence HELD (diff --stat = sanctioned files only). Circle lane 2: six invariants HOLD +
rig re-proven empirically. Ritual: fmt silent · clippy clean · 20/20 · node ×2 · build →
**staged `B7F720C4`** (9.6 MB, 17:16). Both lanes' full reports summarized in docs/26's
session addendum trail (this file + CHANGELOG).

## §10 Known Failures
None found. Informational (circle lane 1): three pre-existing color-only 150 ms transitions
(.portal, #vault-btn, .pf-btn) remain unguarded — no transform, never cited by F1; left as
recorded observations, not defects.

## §15 Symptom Signatures
No new rows. The F1 inline-animation trap is now extinct in-code AND commented at the site
that could resurrect it.

## §16 Standing Context (Rab, in-chat)
`Observed` — Bench operating doctrine UNDISCOVERED: no canonical good-repair example;
entropy-loss target 0.01–1.00% (minimum-operator-work measure, not guaranteeable); the
markdown structure of a finished good repair is unknown even though words/icons/tools/UI
are individually clear. The operator-simplicity discovery mission (agents, semantic +
visual) runs AFTER the docs/26 signature sheet resolves. Bench-dependent signatures
deprioritized until that doctrine session.

## §17 Current State
`Historical` — installed exe `0454114C` (Rab adopted this morning); **`B7F720C4` staged,
unadopted.** Widget state: Rab's hands (he was on a break at close). Unsigned queue: the
docs/26 signature sheet (5 decisions) · slices 3–5 · transcribe thresholds · audit credit ·
Decision 5 · the four · engine lever · Valentine zones 2–4 · Cybernetics-stale · Textor ·
scan-lane locate.

## §18 Next Entry Point
1. MUSTER; newest row S75, ancestor-verified.
2. **Rab adopts `B7F720C4`** + §5's five checks.
3. **The signature sheet** (docs/26, five decisions with recommendations) — then slice 3
   (Dock; decide the F3 colour policy WITH it), then the operator-simplicity discovery
   mission, then slices 4–5.
