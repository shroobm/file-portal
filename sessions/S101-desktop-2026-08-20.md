# S101 · Desktop · 2026-08-20 · P-0 — THE WIRING SLICE

⟨claimed: Fable · S101 · 2026-08-20⟩

## §1 Session Intent

Rab's commission, in his words: **"lets do items 6-9, start with P-0"** — the
conversion-completeness slate (docs/37 §3 items 6–9, docs/41), beginning with **P-0**.

Aligned reading (a slot-referenced GO is unambiguous by construction — `/echo` Phase 0's own
carve-out — so it is recorded and executed, not echoed): build **P-0 option (a)**, the
recommendation his GO landed on: **render `asset_delta` + `embedded_images` on the Assay
card**, closing their docs/29 glass-glitch status. Zero new measurement — the keys have been
in every manifest since 2026-07-20; this is wiring, not building. P-1 through P-6 are NOT in
this session's scope: their decision variables (host, instrument, semantics, thresholds,
report-vs-gate, back-fill) still need his word, and P-0's rendered numbers are meant to inform
those choices before they are made.

Standing constraint carried in from docs/41: this number is **informational, never a verdict
input** — `Best Practices` (465-page scan, one image per page) shows `−416`, which means "OCR
worked", not "figures lost". The render must carry that caveat or it will teach the wrong
lesson the first time he reads it.

*(This section is written before work and is not edited afterwards.)*

## §2 Starting State

`open.sh` clean: ledger newest S100 `bbf6e70` · this session S101 · SOFT 76 · HARD mirrors
ledger, ancestor of HEAD · origin in sync · pipeline held 4 · anchor 23 · levers
audit=enforce · analyst=local · batch=16 · widget running (PID from the card).
**Pinned `--since`: `bbf6e70`.** Relay carried at open: nothing new for Fable (Codex has still
not written) — checked, not assumed.

**Pre-work measurement (the corpus this render will describe)**, read from the real anchor
manifests + `assets/` counts at open:

| bundle | assets | embedded (uniq xref) | delta |
|---|---|---|---|
| Best Practices for Equity Research | 49 | 465 | **−416** |
| DIAGNOSING THE SYSTEM (Beer) | 91 | 348 | −257 |
| Cybernetics Book of Models | 92 | **0** | **+92** |
| Investment Valuation (Damodaran) | 313 | 232 | +81 |
| BRAIN OF THE FIRM · Designing with Freedom (×2 bundles) | — | — | no `fidelity` block |

*Numerator: files in the bundle's `assets/`. Denominator: distinct image XObject xrefs in the
source PDF (`page.get_images(full=True)`, deduped by xref). Conditions: anchored bundles at
2026-08-20, pymupdf 1.28.0, as recorded by `fidelity_audit.extract_witness`.* This
independently reproduces the S97 grounding's census — same numbers, separate run.

## §3 Work Plan

1. This closeout; commit the open.
2. `assay.rs`: project the asset accounting (both sides + the delta, docs/34).
3. `room.js` + `main.js`: render it with its caveat.
4. Prove it: Rust unit test against a real manifest shape + the scoped glass detector.
5. Build the widget, print the SHA-8; **adoption is Rab's hand** (docs/19 §0.3).
6. The glass-evidence correction (§7 below) + its symptom row.
7. Close in lockstep from `bbf6e70`; push.

## §4 Scope and Evidence Boundary

- Repo writes: `windows-widget/src-tauri/src/assay.rs`, `windows-widget/src/room.js`,
  `windows-widget/src/main.js`, docs/15 §5/§13, docs/37, SYMPTOM-INDEX, CHANGELOG, this file.
- **No converter/Python change** — the projection law's whole point: Python already owns these
  numbers, the widget only renders them. **No pipeline run, no GPU, no vault write.**
- Building the exe is mine; **installing it is Rab's** — I will print the SHA-8 and stop.

## §6 Implementation Delta (`--since bbf6e70`)

Commit per task: open · **the P-0 render** (`assay.rs` projection + `room.js` `assetLedger` +
`main.js` `assetLedgerLine` + one CSS rule + `p0-asset-ledger.test.mjs`) · **the
reconciliation** (glass 64→62, docs/15 §5 amendment, SYM-046, three closeout corrections) ·
docs/37 item 6 GO recorded · this close.

**No Python touched.** The projection law's whole point: `fidelity_audit.py` already owned
these numbers, and P-0 only carries them to a surface.

## §7 What P-0 Did, Measured

- **The two keys stopped being glitches.** Glass census across the P-0 commit, compared at
  `HEAD~1` in a throwaway worktree: `converter:asset_delta` and `converter:embedded_images`
  moved out of the unsigned list. Count **64 → 62** — and the arithmetic only closed after
  chasing the missing 1 (below).
- **The detector caught my own code, same session.** My test fixtures introduced
  `widget:tripwires` (+1): a *container* key in `json!` test data that renders nothing. First
  repair attempt — inlining the Rust binding — did **not** move the count, because the detector
  was reading the fixtures, not the binding. That attempt is recorded in the code comment
  rather than erased. Correct repair: **signed INTERNAL with its reason** (dispositions 10→11),
  because a container key in test data is not a measurement, and changing code to dodge a scan
  would have been the wrong instinct.
- **docs/15 §5's own claim was wrong and is withdrawn**: *"a large delta is a flag"*. Measured
  on the real corpus, `−416` means OCR worked (465-page scan, one image per page) and `+92`
  against zero XObjects means the figures were vector drawings. The delta is a **count, never
  coverage** — which is exactly why `compute_verdict` does not read it, and why P-1 exists.

## §8 Evidence

| check | result |
|---|---|
| Rust suite (widget) | **25/25**, incl. 2 new: both sides projected on the real corpus shapes, and absent-ledger-stays-null |
| `cargo clippy --all-targets -- -D warnings` | clean |
| `p0-asset-ledger.test.mjs` (vendored) | **11/11** — exercises the REAL source functions on the −416 / +92 / +81 / null / partial-null shapes, plus Room↔Dock wording parity |
| Browser harness (`harness-s101-asset-ledger.html`) | **0 console errors**; both themes render; computed CSS: caption 9px, `.dim` opacity 0.70 both themes, values brighter than label, no horizontal overflow; **the null case emits no span at all** (caption reads exactly `survival 0.688`) |
| glass, scoped ritual form | `--since bbf6e70 --enforce` → exit 0, "no unsigned glitches" |
| release build | exit 0, **SHA-8 `4DCB73E2`** staged |
| exe carries the new frontend | asset path keys `/room.js` `/main.js` `/styles.css` present in the binary; `frontendDist: "../src"` embeds at compile time from the edited directory; build ran after the edits. *Plain-text search finds neither new NOR pre-existing frontend strings (assets are compressed) — that probe is inconclusive by construction and is not cited as proof.* |

**A screenshot was not obtainable** — the Browser pane does not composite in this session, so
the visual claim rests on extracted text + computed styles, not on an image. Stated rather
than glossed.

## §9 Decision Ledger

- Built option **(a)**, the render — the recommendation his GO landed on. Option (b)
  (REPORT-silent) was the alternative and is now moot: the number reaches a human.
- **Both sides always travel, never the bare delta**, and the caption says "count" not
  "coverage" — the single most important design choice here, because the delta alone teaches
  the wrong lesson on the very first book an operator opens.
- **Null renders blank, never zero** (docs/34's law applied to a count: a measurement nobody
  made is not a zero). Three anchored bundles carry `fidelity: null` and would have shown
  `0 out / 0 in source`.
- P-1..P-6 deliberately untouched: their decision variables are Rab's and are better decided
  once these numbers have been looked at.

## §10 Known Failures and Unread Surfaces

- **The render is `Intended`, not `Observed`, in the product**: staged exe `4DCB73E2` is NOT
  installed — the running widget is still `C3C05D49`. **Adoption is Rab's hand** (docs/19
  §0.3). Until he adopts and opens the Assay card, the live render is unexercised.
- SYM-046 (the bare-glass-invocation trap) has a rule but no mechanical guard.
- SYM-045's id-preflight still unbuilt; SYM-044's one-line `marker_version` fix still unbuilt
  (it belongs to a converter commit, and P-0 touched no Python by design).
- Codex still has not run: relay entries 1 and 2 remain uncarried, docs/43 §3 unclaimed.

## §11 Current State

Pipeline untouched (held 4 · anchor 23 · enforce/local/16 · vault 6). Glass: **62 unsigned
glitches, 11 signed dispositions** — the honest census, replacing three closeouts' worth of
hollow "exit 0". Register: **item 6 GO with P-0 built**; **P-1's four variables unsigned**;
items 7–9 open; 10–12 built. Tripwires: widget 25/25 · echo 5/5 · coordination 11/11 · muster
22/22.

## §12 Next Entry Point

**Rab's hand first: adopt `4DCB73E2`** (copy to `%LOCALAPPDATA%\File Portal\`), open the Assay
card, and look at the figures line on a real book — that is P-0's acceptance and the input to
P-1. Then his word on **P-1's four decision variables** (docs/41 §2 P-1): host · instrument ·
coverage semantics · report-vs-gate. Everything else in the slate follows P-1.
