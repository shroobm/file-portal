# S150 — Desktop — 2026-09-14 — THE ANALYST LEARNS TABLES: before, after, and what is still missing (Claude Fable 5.1's ninth sitting; the experiment's fifth)

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

Rab, in chat, 2026-09-14 ~16:4xZ, after two questions on the Valentine table (his screenshots of the bench's view and lines at page 134, and "could've this been fixed during conversion and analysis?" — answered from the installed Marker and the acceptor's whitelist: partly at conversion, two levers off, the root cause upstream of both; not at analysis under the signed whitelist; the audit caught it and held the book):

> "Okay, close out and compact, then carry over this. Think about what carries over state changes for the analyst. If you bring in a new class, what information do you want to capture before that change and after that change. If it's an additional layer, what can we now understand more than before, and how does it compare. Once you figured that out (first check what the current analyst does, note it down. Then Measure the entirety of Valentine, and then we will then build the new analyst, test that, and see what we gained, and lost, and what is still missing that we need to capture. Protocol. Signed."

**"Protocol"** — his word since S149: the persistent method (POLICY §9), each category an episode with a partial closeout, receipts and a Desk line. **The experiment stands** (S146 §1). The S149 residue still open on his side: "clear" for the un-split of Valentine's held copy; the held Damodaran bundle's departure (0d68f0e0, no event); the two-page Marker probe (`--html_tables_in_markdown`) when the seat is empty.

**The reading I take (the echo, in the record before the act; his words name both a class and a layer, so the build has two faces):** the table-geometry repair is built as a LAYER — its own module with its own invariant, measurable on its own — and exposed to the acceptor as a CLASS, so the analyst's ledger counts it beside escape / link / markup / hyphen / ligature. Measured BEFORE on the whole of Valentine as the book stands, measured AFTER with the same instruments plus the class's own, compared, and the missing named. Narrower readings (a class without the layer; a layer that never reaches the analyst) would each answer only half his sentence; wider ones (the vision route, Gemini, a local vision model) are his decisions and stay out unless he names one.

## §2 Starting State — `open.sh`, the S150 card (verbatim)

```
════════ MUSTER · OPEN · 2026-09-14T16:45:47Z ════════
[0] IDENTITY
    ledger newest    S149  cc3c2fe
    this session     S150  ·  desktop  ·  2026-09-14
    session marker   coordination/private/session.current = S150 (guard_record / guard_git read it)
    advisory         S150 is named in the INHERITED tree (cc3c2fe) — read each, do not blanket-correct:
                     OPEN-TASKS.md sessions/S141-desktop-2026-09-12.md sessions/S149-desktop-2026-09-14.md 
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-14T16:45:47Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 86 (tally == hook)
[3a] LEDGER ORDER  ✓ 132 Desktop-lane rows ascend (S16 → S149)
[3b] LEDGER PARSE  ✓ 132/158 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ cc3c2fe (S149) == ledger cc3c2fe, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 7 · anchor 25 · pending 0 · drop 0
    intake           waiting 0 · oldest 0s · active null · written 3s ago by pid 18844
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    events           390 line(s)
    open-tasks       149 item(s) · last written 12 hours ago
    error-bin        105 row(s) — read the CLASS table before you probe · last written 23 hours ago
    symptoms         132 row(s), 44 open · last written 22 hours ago
    relay            201 entries · run `gate.py status` for the board
    widget           27548
    widget autostart task 'File Portal widget (J57)' Ready — AtLogOn · auto-logon ON (Bndit)
    task             'File Portal desk (relay)' Ready LASTRUN=2026-09-13T00:59Z RESULT=4294967295
    task             'File Portal relay watch (Fable)' Ready LASTRUN=2026-09-12T19:41Z RESULT=0
    task             'File Portal tracker' Ready LASTRUN=1999-11-30T05:00Z RESULT=267011
    task             'File Portal desk watch' Ready LASTRUN=1999-11-30T05:00Z RESULT=267011
    python procs     10
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            7 note(s) · tip f489189
    thinkpad         converter=active · vault tip f489189 · staging 0 · receipts 25
[3] PIN
    --since          cc3c2fe
    closeout         sessions/S150-desktop-2026-09-14.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

The one uncommitted item is the untracked `.codex/`. The levers line read aloud: **audit=enforce · analyst=local · batch=8**. The smoke ran ALONE in the foreground after the card: **44/44 green · 51 s**. The card at the open: 2,854 MiB used of 10,240 at 40 % utilisation with Rab on the seat (Observed at 05:1xZ, before his commission) — every GPU act in this sitting (the analyst's local model, the Marker probe) waits for a free card and an empty seat, the law.

## §3 The pin

`--since cc3c2fe` — S149's closing SHA (row e467625; its post-close 5be4f62 observed green, run 34808192565 / num 340).

## §9a The thinking he asked for — what a new class changes, and what to capture before and after

**What state a new class carries into the analyst (the things that change when it lands):**
1. The acceptor's whitelist (`edit_whitelist.py`: FULL = escape · link · markup · hyphen · ligature, reflow as the comparison) gains `table-geometry`, with its own invariant — an edit is accepted as table-geometry only when the invariant holds.
2. The analyst's prompt programs gain a grid program (a damaged table block in a chunk → the program that repairs its shape), or a layer runs before/after the general rewrite; either way the chunk's record says which.
3. The manifest's `analyst.edits` (accepted / reverted by class) and the `chunk_scores` rows' `e:[accepted, reverted]` count the new class; the dispositions carry its keys.
4. The audit's degeneration verdict on the zone (the S79 caution "a GRID whose column headings were lost") — whether a repaired grid still trips it, and whether it should.
5. The vault note: a table that renders as a table (the S149 view is the eye).
6. Nothing else moves: the fidelity survival/coverage numbers are the agreement instruments' (the OCR layer against the text), not the table's shape.

**Captured BEFORE (E2, the whole of Valentine as it stands, one instrument, one CSV, one summary):** per table block — its lines, rows, cells per row, header/delimiter agreement, the health reading (the S149 rules, ported to Python), whether a GFM parser renders it and how many rows, the letter-column signature (column 1 mostly empty, its filled cells one to four letters or `<br>`-stacked letters, the letters concatenated top-down), header cells carrying `<br>`, the dot matrix (• per row, the total, the stray glyphs `٠ Ā Ė` and their like), the audit zone on it if any and its kind, the page it sits on where the block record knows it, and the analyst's chunk records that touched it (which classes were accepted or reverted there). Totals: tables, renderable, unrenderable and why, damaged grids by signature, tables under audit zones, the letters-in-order that spell a word and those that do not.

**Captured AFTER (E5, the same instruments on the analyst's output, plus the class's own):** proposals made, accepted, reverted, refused by the invariant; the invariant's checks (every cell outside the label column and the header rows byte-identical; the • matrix identical; the table count unchanged; rows unchanged but for the title row); the rendered-table delta; the health-issue delta; the labels recovered, listed; the audit verdict per table zone before and after; the time and the card's cost per table. **Lost** is what must read zero: a cell outside the label column changed, a table that stopped rendering, content removed, a legitimate single-letter column (a rating A–F) merged into a word. **Still missing, known before the build:** the rows a rotated label spans (only the picture knows — the label goes on the run's first row and the extent is UNREAD); the spanning title's place (a caption line, its provenance noted); the OCR-confused dots; tables the OCR split into two blocks; anything the bench's view would show that the numbers do not.

**What the layer lets us understand that we could not:** a per-table health ledger for a whole book (today the book has zones, not a table census); the rotated-rail damage as a class, counted across the book and later the library; whether recovering the labels is deterministic (the letters arrive in reading order) or needs the picture — the comparison is the answer, table by table.

## §9 The plan (protocol: each an episode with receipts and a Desk line)

- **E0 — the record (this) and the compaction.** Nothing else before the record; the plan re-read after the compaction.
- **E1 — what the current analyst does, noted down (Observed from the code, no run):** `analyst.py` (chunking, the prompt programs, the link fence, the `num_predict` bound, the rejections, the chunk journal, the resume), `edit_whitelist.py` (the classes, the invariant per class, the revert slot), where a table block goes today (does the analyst see it as prose? which program touches it?), the audit's grid diagnosis (`fidelity_audit.py`) — written as a record section and as a note beside the measurement (private `measurement/ANALYST-AS-IT-STANDS-2026-09-14.md`).
- **E2 — Valentine measured whole (the BEFORE):** the instrument (`windows-converter/table_census.py` or a probe under private `measurement/probes/`; CPU; docling-env's markdown-it-py for the render column) over the held copy as it stands (the split noted; after his "clear" the un-split runs first and the census re-runs — both filed); the CSV and the summary under private `measurement/evidence/s150-valentine-before/`; the two-page Marker probe (`--html_tables_in_markdown`, pages 133–134) when the card is free and the seat empty — does surya emit a merged cell for this exhibit at all.
- **E3 — the layer and the class built:** `table_geometry.py` (find the damaged grids; propose the shape repair — the title row to a caption, the header row to the header, the letter column to labels on the run's first row, the stray dot glyphs — with the analyst (qwen3, local) asked only for the word the letters spell, or a dictionary when it needs no model; the invariant `grid_invariant(before, after)`), the acceptor class `table-geometry`, the analyst's wiring (the record says which program ran), the dispositions for the new keys, the selftests (the Valentine exhibit as a fixture; a healthy table untouched; a rating column NOT merged; the invariant's negative controls).
- **E4 — tested on Valentine:** a dry run of the analyst over the book's tables (the card free, the seat empty; the lever unchanged; nothing ships), the chunk records read.
- **E5 — gained, lost, missing:** the AFTER census with the same instrument, the comparison table in the record and the journal, the missing named as tickets; the Desk line; the close as S149's.

Not this sitting's unless he names it: the vision route (Gemini or a local vision model); the ship of a repaired Valentine (the lever holds a `fail`); the Damodarans; D1.

## E1 — the analyst as it stands (Observed from the code, no run; the note is private `measurement/ANALYST-AS-IT-STANDS-2026-09-14.md`, written 16:53Z)

Read from `analyst.py` (817 lines), `edit_whitelist.py` (400), `fidelity_audit.py` (665), `prompts/`, the held manifest and the anchor copy's block record; every claim in the note names its line. The shape of a run: the image fence (line 137) → chunks split on blank lines at ~4,000 characters (151) — **a pipe table has no blank line inside it, so a whole table is one paragraph and travels in one chunk** — → the one program `prompts/readability.txt` (**no sentence about tables**: the model is told nothing of pipes, cells, headers or rotated labels) → `qwen3:8b` on the local Ollama, `NUM_CTX 8192`, `num_predict` bounded at 2× the chunk → the gates in order (`truncated`, `think_leak`, the fence, survival ≥ 0.80, inflation ≤ 1.5) → the acceptor `ew.reconcile(chunk, candidate, ew.FULL)` (591): every hunk not in {escape, link, markup, hyphen, ligature} is spliced back from the input. The `markup` rung strips table pipes and dashes for its comparison, so a pipe moved is equivalent — but a letter moved from one cell to another is a word moved and **a table's shape repair (labels merged, a title lifted out, a cell emptied) is not a class: it reverts.** The record per chunk: `i/s/r/x/e` rows and the manifest's `analyst` block (`backend, model, program, chunks_*, duration_s`, `edits` since J46).

What reaches a table today: **Valentine never met the analyst** — the held manifest has no `analyst` block and no `chunk_scores` (the scan lane's audit held it: `fidelity.verdict fail`, `convert.kind agreement`); the audit blanks pipe-table rows before its loop detector (`degeneration(strip_table_rows=True)`, SYM-067, J29) so tables are invisible to the degeneration measure by design; the "GRID whose column headings were lost" caution is the bench's banked signature, not the audit's finding. **Net (Observed from the code): the current analyst can neither see a table as a table nor keep a shape repair.** The conversion side, from the anchor copy's `blocks.json`: 81 table blocks, 11 with `rowspan`/`colspan`; Exhibit 8.2 (continued) `/page/133/TableGroup/9` carries `colspan="9"` on the spanning title and `rowspan` 0 — surya read the title as one merged cell and the rail as one cell per row; the first half's title is chopped into fragments in the record itself. So `--html_tables_in_markdown` would have kept the title's span in 11 tables and could not have kept the rail's — the S149 §18 probe answered from the record, not run (the card stays Rab's).

## E2 — Valentine measured whole, the BEFORE (16:58–17:04Z; the instrument public, the evidence private)

**The instrument.** `windows-converter/table_geometry.py` (public, the reading half of the layer: `fence_mask`, `table_blocks`, `cells`, `health` — the S149 rules ported, the page's twin —, `read_table` → `TableReading`, `census`, `orphan_runs`; pure functions, no I/O, no model) with `table_geometry_selftest.py` **27/27** (`ALL OK: 27/27`; the Valentine exhibit as the fixture, the first half with its `_` artefact, the p.367 matrix, a rating column A–D, a ratio-name column, a two-heading header and a healthy table as the negative controls); the probe `measurement/probes/table_census.py` (private, docling-env, markdown-it-py 4.2.0 `commonmark` + `table` for the render column, the anchor's `blocks.json` for the page — matched by a ≥ 20-character cell's text and **said so in the CSV: Inferred**) → `tables.csv`, `orphans.csv`, `summary.json` under private `measurement/evidence/s150-valentine-before/{held,anchor1}/`. Both copies measured: the held bundle `b6fbdd75f6242f53` as it stands (the S149 split in it) and the anchor `(1)` copy (the S145 record, unsplit — the same PDF converted twice).

**The numbers (Observed, every table of the body; the denominators named):**

| measure | held (split) | anchor (1) |
|---|---|---|
| body lines | 6,103 | 6,121 |
| tables a renderer reads (header + delimiter + rows) | 80 | 81 |
| … that markdown-it-py renders as `<table>` | 80 / 80 | 81 / 81 |
| health issues (the S149 rules) | 4 — all the split at 1579–1583 | 0 |
| orphan pipe runs (rows no renderer reads as a table) | 2 (1 + 16 rows: the split exhibit) | 0 |
| table rows · cells filled / empty | 1,050 · 2,524 / 529 | 1,109 · 2,726 / 646 |
| tables whose header cells carry `<br>` (rotated or wrapped headings) | 18 (36 cells) | 18 (42 cells) |
| the letter-column signature (a rotated rail read letter by letter) | 2 | 3 |
| the title-row shape (one filled cell, or a fragment pair, above a real header) | 2 | 2 |
| dot matrices: tables with `•` · `•` total · stray glyphs (`٠ · ∙ ● ◦ *`) | 2 · 101 · 8 | 4 · 179 · 8 |
| audit runs touching a table | 0 of 0 runs | 0 of 0 runs |
| page known (Inferred by text against the block record) | 42 / 80 | 49 / 81 |
| analyst block · chunk scores | absent · 0 | absent · 0 |

**Readings.** (1) **The split is the whole difference in the count:** the exhibit's continued half is the held copy's orphan (a lone header at 1579, a headless run of 16 rows at 1583) and the anchor copy's 81st table (line 1575: 13 columns, 17 rows, the rail `R l v Ė N U E · 6 O S T s · M G M T · V A Ā O N`, 75 `•`, 2 stray glyphs, page 133 by text). (2) **Three rotated rails in the book, all seen, none complete the same way:** the continued half spells REVENUE / COSTS / MGMT / VALUATION with confusions (`l v` for EV, `6` for C, `Ā` for TI) but every position present; the FIRST half (held 1556 / anchor 1552) reads `S A R T E G Ϋ` and `F N A N Č Ĺ` for STRATEGY / FINANCIAL — **letters missing (T, I, I, A) and two swapped, with a `_` artefact in the rail** — so the letters do NOT always arrive complete and reading order alone cannot spell the word: the repair needs a dictionary or the model for the word, and the layer must accept "unreadable" as an answer; p.367 (held 4539 / anchor 4522) reads `H G H · L L O` for HIGH / LOW in a 2×2 matrix (Lipstick on a Pig / Reputation Builder) — a second exhibit of the same damage, in prose the book, not a data table. (3) **The title-row shape is ambiguous without the block record:** it fires on the exhibit in the anchor (where `blocks.json` says `colspan="9"`) and on two regression tables (held 2503, 2557; anchor 2549) whose first row is `Significance` above `F` — a stacked column heading, not a title; the first half's title, chopped into header fragments (`Start`, `with th`, `s sour`, `ce to ir`…), is NOT read at all — a MISSING signature, named. (4) **Two conversions of one PDF do not read the same glyphs:** the first half carries 7 stray dot glyphs in the held copy and 6 in the anchor; the matrix 1 and 0; the continued half 2 (anchor) — the dot matrix is not stable across conversions either. (5) **Nothing else in the book breaks a renderer:** the 4 issues are the split; the 18 `<br>` headers render (a cosmetic). (6) **The damage is concentrated:** 3 tables of 81 carry a rail, 4 carry a dot matrix, 1 carries a spanning title — the layer's whole target in this book is a handful of tables, which is what makes a per-table invariant affordable.

**The rule's own evolution, recorded (it is part of the measurement):** rule 1 (every filled first-column cell one to four letters) saw 1 of the 3 rails (the `_` artefact hid the first half); a relaxed rule (four in five cells letterish) saw the first half and also the Method / Pros / Cons table (line 3622: `P/E`, `PEG`, `P/FCF`, `P/S` beside a `<br>`-wrapped cell) — a false positive; the rule that stands (every piece of a cell, split on `<br>` and space, is ONE glyph) sees the 3 rails and nothing else, and the two-heading header (`| | Lipstick on a Pig | Reputation Builder |`) stopped reading as a title. The rule-1 outputs are kept beside the final ones as `summary.rule1.json` / `tables.rule1.csv`. **Tag law:** the counts are Observed over every table; the judgement "a true rail" is Observed for the 3 flagged tables (their first column printed and read) and the other 78 are clean **by the rule**, not by eye (Inferred; the AFTER census and the invariant are what would catch a rail the rule missed).


## E3 — the layer and the class built (17:05–17:23Z)

**The layer's repair half** (`windows-converter/table_geometry.py`, appended under the reading half): `fold_letters` / `letters_fit` — the word check that bounds the resolver (the word must contain the letters the OCR read, in order, but for ONE confusion and any number of missing letters; a doubled letter across a row boundary collapses: `SARTEGΫ` fits STRATEGY, `RlvĖNUE` fits REVENUE and not COSTS, `HGH` HIGH, `LLO` LOW, `6OSTs` COSTS, `VAĀON` VALUATION, `FNANČĹ` FINANCIAL); `propose(lines, resolver)` — per healthy table a `caption` (the spanning title, ≥ 20 characters with a space, its chopped tail `ment` dropped when it is the title's own suffix), a `rail` per letter run (the letters and the rows' own text to the resolver; the word on the run's first row, blanks under it; unresolved when there is no resolver, no word, a `?`, or a word that does not fit — every rail proposal carries its letters, its word and how), `dots` (stray glyphs → `•` inside a table that has a matrix); `apply_table` (an unchanged row keeps its bytes; with a caption the second row becomes the header and the delimiter row moves under it); `grid_invariant(before, after)` — the class's law on one table (one table on both sides; the same columns; the same rows but for a title lifted to a caption whose text sits above the table; every cell outside column 1 byte-identical or a stray glyph become `•`; column 1 changed only where a run became a fitting label with blanks under it) returning the reasons and the facts (cells compared, dots fixed, labels, caption); `geometry_pass(text, resolver)` — propose → apply on a copy → keep only what the invariant admits, the record written by the act (tables, with_signature, proposals, applied, refused, unresolved, labels, captions, unresolved_rails, refusals, dots_fixed, invariant_checks, resolver_calls).

**The class** (`edit_whitelist.py`): `FULL_TABLES = FULL | {"table-geometry"}`, `CLASSES` + `table-geometry`; in `reconcile`, with the rung in force, `_mask_tables` pairs every table of the input with the candidate's table at the same position (same count, same order — counts differing fall to the span path as before), judges a differing pair WHOLE by `grid_invariant` (the two lines above the candidate's table are read, so a caption the model lifted is admitted with the table), swaps each pair for one token `⟦TBL-k⟧` on both sides so the word alignment treats it as equal, and `_unmask_tables` returns the chosen text — the candidate's when admitted, the input's otherwise — with one log entry `("table-geometry", accepted, a, b)` per judged pair; an identical pair is no edit. **The design choice recorded:** a span rung cannot see a table — the token alignment splits a rail repair into a substitution (`R` → `REVENUE`) and three deletions between equal pipe tokens, each judged alone — so the class judges the table whole, before the alignment, which is also what closes the hazard the markup rung left open: a dropped pipe (`| a | b |` → `| a b |`) was equivalent under markup (pipes stripped) and SHIPPED a row one cell short; under `FULL_TABLES` the table is restored whole. `FULL` is unchanged — Rab's slot.

**The wiring** (`analyst.py`): `process(markdown, backend, program, tables=None, resolver=None)` — with `tables` on, the layer runs on the fenced text before the chunks are cut; the word route is `_word_resolver(generate)` on the grid program `prompts/grid-word.txt` (the letters spaced, the rows' text as context; one call per rail with the bound `GRID_NUM_PREDICT` 24 and the previous bound restored; the first line's first word in capitals; `?`, a non-word, an empty reply or a backend error → None, and a failure here may never cost the analysis); the acceptor runs `FULL_TABLES`; the resume key carries `+tables`; the layer's applied / refused count under `table-geometry` in `edits` beside the acceptor's own; `meta["geometry"]` carries the record (None when the layer is off — honest absence) with `program` = the grid program or `resolver`. **Lever-waiver: `ANALYST_TABLES = False`** — off in the pipeline; the dry run passes `tables=True`; entering the shipped whitelist is his signature (§12.2's precedent). `observability/dispositions.json`: `converter:geometry` EVIDENCE (the detector read the key as rendered by an incidental match on the word elsewhere; the entry says it is manifest-only). `docs/15` §12.4 appended (the finding, the layer, the class, the wiring, the lever, what it cannot do, the tripwires).

**Tripwires, all green:** `table_geometry_selftest.py` **54/54** (+27: the word check with its negatives; the proposals on the exhibit — a caption, two rails, the dots; the pass applies all four; the AFTER census reads no letter column, no title row, no stray glyph, `•` 53, 10 rows, 13 columns; the caption above, the header then the delimiter; REVENUE on the run's first row with the question cells untouched; the invariant holds on the pass's own output with 120 cells compared; idempotent; without a resolver the rails are reported and the rest applies; a wrong word refused; `?` unresolved; a healthy table and a rating column propose nothing; the invariant's negatives — a cell reworded, a `•` dropped, a row dropped, a pipe dropped, a label that does not fit, a title dropped with no caption, a cell inside the rail's rows reworded; a split table never proposed for), `edit_whitelist_selftest.py` **53/53** (+13: the repair admitted under `FULL_TABLES` and reverted under `FULL`; the pipe hazard shipping under `FULL` and restored under `FULL_TABLES`; a reworded cell restores the table while a hyphen join outside it is accepted; an unchanged table is no edit; a lifted caption admitted; a dropped title with no caption restored; two tables judged apart; the counts-differ fall-through; the tally; the names), `analyst_selftest.py` **38/38** (+4: the lever on — the exhibit repaired before the chunks, the record, the class in `edits`, the whitelist naming it; the lever off — geometry None, the whitelist as shipped, the exhibit as it came; the resolver's contract and its bound; a refused word on the record). The glass detector since the pin: clean.

**Two findings from the build itself, kept:** (1) the invariant's first loop jumped over a rail's rows with the rail and compared 60 of 120 cells — its own negatives caught it (a `•` dropped inside the rail passed) — fixed by comparing every row's cells first and column 1 second; the negatives' row indices were also off by the caption's shift and now assert the fixture's rows before mutating them. (2) A rail repair reverted under `FULL` returns the input's WORDS in the candidate's whitespace (`| l v  |`, two spaces) — the acceptor's contract since S140 (words, not bytes), noted, not changed. **Not built, by the plan:** the vision route; the un-split (his clear); the lever's move (his slot).


## E4a — the new analyst run dry on Valentine, the half that needs no card (17:24–17:27Z)

**The probe** (private `measurement/probes/table_dryrun.py <bundle> --out DIR --resolver none|local --chunks none|tables|all`): `analyst.process(body, backend="local", tables=True, resolver=…)` with `_generate` wrapped — a chunk goes to the readability model only under `--chunks all`, or `--chunks tables` when it holds a table (the rest are handed back unchanged, their rows written like any passed chunk); the pipeline's own files untouched (`FP_PIPELINE` → `DIR/fp`, the chunk journal ledgered into the private agent-scripts journal, the heartbeat under DIR); the output body, the analyst's record (`meta.json`), the probe's own `run.json`, and a `bundle/` copy so the census reads the AFTER exactly as it read the BEFORE. Nothing written to the bundles; nothing ships.

**This half:** `--resolver none --chunks none` on both copies — the layer's deterministic repairs (captions, dots), every rail reported unresolved, no model call (**0 GPU**), 266 / 267 chunks handed back unchanged, wall 4.2 s / 4.3 s (Observed; `run.json`; the chunk journals ledgered D0289 / D0290). Evidence under private `measurement/evidence/s150-valentine-after0/{held,anchor1}/` with the AFTER census beside it (`census/`).

| the layer's record (`meta.geometry`) | held (split) | anchor (1) |
|---|---|---|
| tables read · with a signature | 80 · 2 | 81 · 3 |
| proposals · applied · refused · unresolved | 5 · 1 · 0 · 4 | 9 · 3 · 0 · 6 |
| captions lifted | 0 (the exhibit's title is chopped into fragments in this copy; the regression tables' `Significance` is under 20 characters — no false caption) | 1 — line 1575, "Start with this source to investigate before meeting management", the `ment` fragment dropped |
| stray glyphs → `•` | 7 | 8 |
| invariant checks · resolver calls | 1 · 4 | 2 · 6 |
| unresolved rails (rows · letters) | 1563–1566 `SARTEGΫ` · 1570–1573 `FNANČĹ` · 4541 `HIGH` · 4544–4545 `LLO` | 1559–1562 `SARTEGΫ` · 1566–1569 `FNANČĹ` · 1579–1582 `RlvĖNUE` · **1585–1591 `6OSTsMGMTVAĀON`** · 4524 `HGH` · 4527–4528 `LLO` |
| `edits.accepted` | `table-geometry: 1` | `table-geometry: 3` |

**The AFTER census against the BEFORE (the same instrument, the same block record):** held — `•` 101 → 108, stray glyphs 8 → 1 (the one left is a `·` in column 2 of the p.367 matrix, a table with no `•` matrix, so the `dots` rule never proposed for it — what that glyph is stays UNREAD), everything else identical (80 tables, 80 render, 4 issues = the split, 2 orphan runs, 2 title shapes, 18 `<br>` headers, 2 rails); anchor — title-row shapes 2 → 1 (the exhibit's lifted), `•` 179 → 187, stray 8 → 0, body lines 6,121 → 6,122 (the caption: +2 −1), pages matched 49 → 48 (the census matched the exhibit by its title cell's text; lifted, the header cell's text matches both halves' blocks — an instrument artefact, not a table lost), everything else identical (81 tables, 81 render, 0 issues, 3 rails). **Lost: nothing** — no table stopped rendering, no cell outside column 1 changed (the invariant's own count), no row lost.

**What this half shows before the card is touched:** (1) the layer's deterministic repairs apply and the invariant admits them on the real book; (2) the rails are exactly the census's three, listed with their rows and letters, and **in the anchor copy three rails ran together** — `6 · O S T · s · M G M T · V A · Ā · O N` is ONE run of seven rows (no blank row between COSTS, MGMT and VALUATION), so the resolver will be asked for a phrase and the label will sit on the run's first row with the boundaries UNREAD (the grid program now says so: "if the letters run through more than one label… answer the words in order"); (3) the held copy reads `H<br>I<br>G<br>H` where the anchor reads `H<br>G<br>H` — two conversions, one letter apart; (4) the split exhibit in the held copy is untouched (a health issue): the layer will not repair what the bench's un-split must first make a table — his clear gates it.

**The half that needs the card** (`--resolver local`, the grid program on qwen3:8b for the six rails; `--chunks tables` on the table-bearing chunks so the acceptor's class meets a real candidate) waits on the law: at 17:2xZ `query user` read the console session **Active, idle none** (Rab on the seat) and the card 3,730 MiB / 40 % with the desktop's own processes — a watcher polls the seat and the card every minute and reports when both are free (idle ≥ 10 min, no lock, utilisation under 15 %); until then nothing is launched.


## Aside, while the card is waited for — the S149 residue landed: the zone anchor in the view (17:33Z)

S149's refuter set the view's renderer to `html:false` (a book's markdown is untrusted text in the page that holds the write token) — and the raw `<a id="fp-zone-anchor"></a>` that `renderView` injected into the markdown source before the zone's line became visible text in the view (owed in S149 §18). `bench.html` now injects a plain marker paragraph (`⟦FP-ZONE-ANCHOR⟧`) and swaps the rendered `<p>` for the anchor after the render; the renderer stays `html:false`. `test_bench_page.py` gained the tripwire (the marker in, the raw tag never in the source, the swap present, `html:false` kept): **82/82**; the feature invariant (`--script-changed-ok`) and the node harness as before. `bench.py` untouched — his open bench shows it on a reload, no reopen needed. Not part of the commission; a known defect of mine, one file, landed while the seat is his.


## §1b Rab's second word of the sitting (chat, 18:11Z, verbatim; the reading in the record before the act)

> "run persistent protocol. Im heading to school, if you're done and got the report, send it all as a document to gmail draft. Find some tickets to complete, run persistent while im away, you can do another automate experiment. Keep watch of desk. See you later. Record all receipts. Signed."

Asked just before, "What are we still waiting for btw?" — answered: only the card for E4b, under his own GPU law (the seat Active, idle 0 min, the card at 34 % at 18:08Z); his options were "run it", wait, or close without it.

**The reading (Inferred from his words; each act below carries its receipt):** (1) *"run persistent protocol"* — the persistent method (POLICY §9), episodes with partial closeouts, receipts and Desk lines, continuing while he is away; (2) *"heading to school"* — the seat empties, so the watcher launches E4b on its own when the law's conditions hold (idle ≥ 10 min, no lock, the card under 15 %) — not before, not on a word he did not say; (3) *"if you're done and got the report, send it all as a document to gmail draft"* — when S150 is closed, the whole record as the report goes into a Gmail draft as a document (the overnight-report convention: a draft, never a sent mail), nothing sent; (4) *"Find some tickets to complete, run persistent while im away, you can do another automate experiment"* — after the close, a new sitting opens (S151) on register rows from `OPEN-TASKS.md` §B (mechanical — no row that needs his signature), under the experiment's rule (I sign off my own runs; agents without internet; the journal); the card is free while the seat is empty (the law's own condition); nothing ships to the vault, no lever moves; (5) *"Keep watch of desk"* — the Desk watcher stays armed; a Desk post is answered on the Desk; (6) *"Record all receipts"* — every episode a partial receipt, every sitting a whole receipt, the chain's four receipts at each close. What stays his: the lever (`table-geometry` into FULL), D1, the experiment's end, his "clear" for the un-split, the vision route.


## §1c Rab's third word (chat, 18:16Z, verbatim)

> "run it now, don't wait for the seat, and run autonomously, procs, calls sequences episodes, record all receipts do tickets in the background if you can, you can run autonomously, send messages to desk as well update after each episodes as per usual. See ya."

**The reading:** E4b launches on his word — the GPU law's own exception (the card is his to give; the `.gpu-lock` check stays, one GPU act at a time); the receipts in every kind the chain writes (calls, cost, usage, code, procs where a tracker runs — a tracker is started for the sitting if one exists on the machine, else procs stays UNREAD by rule and says so), the sequences checked, every episode its partial closeout; the tickets after S150's close, in S151, with lanes in the background where a ticket allows it (reading, panels) and the tree kept clean for the chain; a Desk line after every episode. Launched at 18:16Z with the seat still Active.


## E4b — the new analyst run dry on Valentine, the half with the card (18:16–18:32Z, on Rab's word)

**Launched on his word** (§1c: "run it now, don't wait for the seat") with the seat still Active — the law's own exception, the `.gpu-lock` check kept, one GPU act at a time. `e4b_run.sh --on-rabs-word`: the probe with `--resolver local --chunks tables --model-lines …` (the chunks holding the exhibit, the p.367 matrix and the dot tables go to the readability model; the rest are handed back unchanged), the anchor copy then the held copy, the card sampled every 5 s, then the AFTER census on both with the same instrument and block record. Nothing written to the bundles; nothing ships; the model unloaded after each copy (`/api/ps` empty at the end).

**Three runs, the first two kept as evidence of their own defects (private `measurement/evidence/s150-valentine-after-run1/`, `-run2/`; the third is `s150-valentine-after/`):**
- *Run 1 (18:16–18:19Z)* — every rail "the resolver gave no word". A direct probe of the backend with the grid prompt (`resolver_probe.py`, Observed on the card) showed qwen3 answering with the letters SPACED as the question spelled them (`R E V I N U E`, `H I G H`) and the parser refusing seven one-letter words; and a hallucination bounded as designed — for `6 O S T s M G M T V A Ā O N` the model answered the rows' own text, "MAJOR PRODUCTIVITY INITIATIVES", which `letters_fit` refuses. Fixed in `analyst._word_resolver` (single letters joined into the word; temperature 0 for the grid calls through `_call_bound`, restored after; the program says "written as a word"); `analyst_selftest.py` +4 assertions, 38/38.
- *Run 2 (18:21–18:23Z)* — the same result, and the reason was the PROBE, not the analyst: its `_generate` wrapper decided by "does this prompt's tail hold a table" whether to call the model, so the grid program's calls — no table in them — were handed back unchanged and the resolver parsed the prompt's own tail; `resolver_calls` 6 counted calls the card never saw. Fixed in `table_dryrun.py` (a prompt that starts with the grid program goes to the real backend, counted as `grid_calls_to_model`). Runs 1 and 2 did exercise the class: the readability model altered tables in 4 chunks of the anchor copy and the acceptor restored every one whole (`edits.reverted table-geometry`), accepting none of its table edits; one exhibit chunk was rejected by the survival guard (0.7727) — the model dropped text, the original shipped, as the analyst has always done.
- *Run 3 (18:24–18:26Z; `-run3/`)* — the word route reached the card, and the model answered **REVENUE for every rail of the book** — ten calls, one word — because the program's new sentence carried an example ("written as a word (REVENUE, not R E V E N U E)") and at temperature 0 the model copied it. `letters_fit` refused nine of the ten (each refusal on the record with its count: `SARTEGΫ` 3 of 7, `FNANČĹ` 1 of 6, `HGH` 0 of 3, `LLO` 0 of 3, the seven-row run "more letters read (14) than the word holds (7)"); the tenth, REVENUE for `RlvĖNUE` (6 of 7 in order), was right and was applied — but tainted by the leak, so it is not counted as the route's result. The example removed from the program ("one word with no spaces between its letters… no example"); a tripwire in `analyst_selftest.py`: the grid program may carry no capitalised word a rail could be. **The invariant's worth, measured by accident:** a systematically wrong word was stopped nine times out of ten by the letters alone.
- *Run 4 (2026-09-14T18:28:05Z → 2026-09-14T18:30:31Z; `s150-valentine-after/`)* — the AFTER. Peak card 8731 MiB at 93 % (Observed, the sampler).

**The anchor (1) copy** — wall 94.2 s (the analyst's own duration 75.3 s); chunks to the readability model 6 of 266 (the rest handed back unchanged), grid calls to the model 6; chunks passed 265 · rejected 1 ({"fence": 0, "survival": 0, "think_leak": 1, "inflation": 0, "truncated": 0}). The layer: tables 81, proposals 9, applied 7, refused 0, unresolved 2, dots fixed 8, invariant checks 3, resolver calls 6, program `grid-word`. **Labels recovered:** rows 1559–1562 `SARTEGΫ` → **SARTGEY** (resolver (6 of 7 letters in order)); rows 1585–1591 `6OSTsMGMTVAĀON` → **TOSSTMGMTVAAON** (resolver (13 of 14 letters in order)); rows 4524–4524 `HGH` → **HGH** (resolver (3 of 3 letters in order)); rows 4527–4528 `LLO` → **LABEL** (resolver (2 of 3 letters in order)). **Unresolved:** rows 1566–1569 `FNANČĹ` — the word 'FANAN' does not fit the letters 'FNANČĹ': 4 of 6 letters in order; rows 1579–1582 `RlvĖNUE` — the word 'REVINUE' does not fit the letters 'RlvĖNUE': 5 of 7 letters in order. Captions: line 1575 "Start with this source to investigate before meeting management" (the `ment` fragment dropped). Refusals: none. `edits`: accepted {"table-geometry": 7} · reverted {"table-geometry": 3} · chunks reconciled 3. The chunk rows that carry an edit or a reason: chunk 63 e [0, 1] s 1.0000 r 1.0018, chunk 65 REJECTED think_leak (s 0.0000), chunk 67 e [0, 1] s 0.9231 r 1.0000, chunk 204 e [0, 1] s 0.9143 r 0.9688. Body lines 6121 → 6122.

**The held copy (the split in it)** — wall 50.6 s (the analyst's own duration 37.0 s); chunks to the readability model 3 of 265 (the rest handed back unchanged), grid calls to the model 4; chunks passed 265 · rejected 0 ({"fence": 0, "survival": 0, "think_leak": 0, "inflation": 0, "truncated": 0}). The layer: tables 80, proposals 5, applied 4, refused 0, unresolved 1, dots fixed 7, invariant checks 2, resolver calls 4, program `grid-word`. **Labels recovered:** rows 1563–1566 `SARTEGΫ` → **SARTGEY** (resolver (6 of 7 letters in order)); rows 4541–4541 `HIGH` → **HIGH** (resolver (4 of 4 letters in order)); rows 4544–4545 `LLO` → **LLO** (resolver (3 of 3 letters in order)). **Unresolved:** rows 1570–1573 `FNANČĹ` — the word 'FANAN' does not fit the letters 'FNANČĹ': 4 of 6 letters in order. Captions: none. Refusals: none. `edits`: accepted {"table-geometry": 4} · reverted {"table-geometry": 3, "punctuation/case": 2} · chunks reconciled 3. The chunk rows that carry an edit or a reason: chunk 40 e [0, 1] s 1.0000 r 1.0000, chunk 63 e [0, 1] s 1.0000 r 1.0000, chunk 203 e [0, 3] s 0.9143 r 0.9754. Body lines 6103 → 6101.

**What the card half found (Observed in the records above):** qwen3:8b cannot spell a rail from its letters. Asked for the word, it returned the letters re-arranged (`SARTGEY` for STRATEGY, `TOSSTMGMTVAAON` for the seven-row run), an echo (`HGH`, `LLO`), a near-miss the letters refuse (`REVINUE`, `FANAN`), or a wrong word the letters admit (`LABEL` for LOW — two of three letters in order, the one confusion the check allows); the one right answer, `HIGH`, was an echo of letters the OCR had read complete. `letters_fit` did what it was built to do — the near-misses were refused with their counts — and it did NOT do what it was never built to do: it cannot tell a word from the letters shuffled, so **four labels went into the tables that are no better than the garble they replaced** (`SARTGEY` in both copies, the fourteen-letter string, `LABEL`), inside the invariant (nothing outside column 1 changed). That is a LOSS by the plan's own rule ("lost must read zero"): the layer's deterministic half loses nothing; its word route, with this model and this bound, makes a rail wrong in a new way. The lever stays off, as it was. **The bound that is missing, named:** a lexicon — the book's own prose (it says "revenue", "costs", "management", "valuation", "strategy", "financial", "high", "low") — so a label must be a word the book uses that fits the letters; short reads (three letters or fewer) must fit exactly after the doubled-letter collapse (`LLO` → LOW, never LABEL); an echo of incomplete letters is no answer. A ticket, not this sitting's build. **The class, meanwhile, held every time it was asked:** in four runs the readability model altered 27 tables and the acceptor restored every one whole, accepting none of its table edits (two `punctuation/case` reverts in the held copy's run 4 were the model's, outside the tables); one exhibit chunk was rejected by the survival guard in runs 1–3 and by the think-leak filter in run 4 — the analyst's own guards, unchanged.

**The AFTER census against the BEFORE (the same instrument; `e5_compare.py`; the AFTER carries the four wrong labels as filled first-column cells, which is why the rail count does not fall):**

**the held copy (the split in it)**

| measure | BEFORE | AFTER, no card (captions, dots) | AFTER, the card (the words too) |
|---|---|---|---|
| tables a renderer reads | 80 | 80 | 80 |
| … rendered by markdown-it-py | 80 | 80 | 80 |
| health issues | 4 | 4 | 4 |
| orphan pipe runs | 2 | 2 | 2 |
| orphan rows | 17 | 17 | 17 |
| title-row shapes | 2 | 2 | 2 |
| tables with <br> headers | 18 | 18 | 18 |
| letter-column (rail) tables | 2 | 2 | **0** |
| • total | 101 | **108** | **108** |
| stray bullet glyphs | 8 | **1** | **1** |
| tables with a • matrix | 2 | 2 | 2 |
| pages known (Inferred by text) | 42 | 42 | 42 |
| letter-column tables, no card | | 1556 `SARTEGΫFNANČĹ`; 4539 `HIGHLLO` |
| letter-column tables, the card | |  |

Tables that changed (2 of 80): line 1556 → 1556: letter_column True→False, letters_in_order SARTEGΫFNANČĹ→FNANČĹ, dots_total 77→84, stray_dot_glyphs 7→0 · line 4539 → 4539: letter_column True→False, letters_in_order HIGHLLO→

**the anchor (1) copy**

| measure | BEFORE | AFTER, no card (captions, dots) | AFTER, the card (the words too) |
|---|---|---|---|
| tables a renderer reads | 81 | 81 | 81 |
| … rendered by markdown-it-py | 81 | 81 | 81 |
| health issues | 0 | 0 | 0 |
| orphan pipe runs | 0 | 0 | 0 |
| orphan rows | 0 | 0 | 0 |
| title-row shapes | 2 | **1** | **1** |
| tables with <br> headers | 18 | 18 | 18 |
| letter-column (rail) tables | 3 | 3 | **1** |
| • total | 179 | **187** | **187** |
| stray bullet glyphs | 8 | **0** | **0** |
| tables with a • matrix | 4 | 4 | 4 |
| pages known (Inferred by text) | 49 | **48** | **48** |
| letter-column tables, no card | | 1552 `SARTEGΫFNANČĹ`; 1577 `RlvĖNUE6OSTsMGMTVAĀON`; 4523 `HGHLLO` |
| letter-column tables, the card | | 1577 `RlvĖNUE` |

Tables that changed (3 of 81): line 1552 → 1552: letter_column True→False, letters_in_order SARTEGΫFNANČĹ→FNANČĹ, dots_total 78→84, stray_dot_glyphs 6→0 · line 1575 → 1577: rows 16→15, title_row True→False, letters_in_order RlvĖNUE6OSTsMGMTVAĀON→RlvĖNUE, dots_total 75→77, stray_dot_glyphs 2→0, rendered_rows 16→15 · line 4522 → 4523: letter_column True→False, letters_in_order HGHLLO→


## E5 — gained, lost, and what is still missing that we need to capture (18:35Z)

**Gained (Observed, E2–E4):**
1. **A per-table ledger for a whole book** where there were four zones: 80 / 81 tables named by line, rows and columns, whether a renderer reads them, their signatures (three rotated rails, one spanning title, four dot matrices, the split), on both copies with one instrument that runs again after any change (`table_geometry.census`, the private probe). The book's table damage is a handful of tables — what makes a per-table invariant affordable.
2. **The deterministic half of the layer works on the real book and loses nothing:** the exhibit's spanning title lifted to a caption above the table (its `ment` fragment dropped), 15 stray bullet glyphs made `•` across the two copies, admitted by `grid_invariant` — every table still renders, no cell outside column 1 changed, no row lost (the AFTER census, twice).
3. **Tables are protected whole in the acceptor.** Under `FULL_TABLES` the readability model's edits to a table are accepted only under the invariant, else the table returns untouched: in four runs it altered tables 12 times and was restored 12 times, admitted 0 times; a dropped pipe — which shipped a row one cell short under the markup rung — is restored (the selftest, both ways). This holds whether or not the word route ever works.
4. **The record writes the AFTER itself:** `analyst.geometry` lists every proposal, label, refusal and unresolved rail with its rows, letters and reason; `edits` counts the class beside the five that were.
5. **The two findings the build gave for free:** an invariant's own negatives catch its bugs before a book does (the first loop compared 60 of 120 cells); a prompt's example word leaks into the answers at temperature 0, and the letters check stops nine of ten.
6. **From Rab's two books (the reading, private note):** the measurement discipline this layer lacks — cell-level buckets against both denominators, a ground truth that measures its own reproducibility, one region per composite element, ground truth as boxes and identifiers — and the two modern measures computable from our pipe tables, TEDS and GriTS.

**Lost (the plan said this must read zero; it does not):**
1. **Four labels made wrong in a new way** by the word route in run 4 (`SARTGEY` for STRATEGY in both copies, `TOSSTMGMTVAAON` for the seven-row run, `LABEL` for LOW), applied inside the invariant because `letters_fit` bounds the letters and not the word. Nothing shipped, the lever is off, and the bound that is missing is named (the lexicon row). The layer's deterministic half stands on its own; its word route with qwen3:8b and this bound does not.
2. Not losses, named so they are not mistaken for one: a reverted table repair under `FULL` returns the input's words in the candidate's whitespace (the acceptor's contract since S140); the census's page match dropped from 49 to 48 when the title cell it matched by was lifted (an instrument artefact); one exhibit chunk rejected by the survival guard (runs 1–3) and one by the think-leak filter (run 4) — the analyst's own guards on its own behaviour, unchanged.

**Still missing, to capture (each a register row):**
1. **The word route's second bound** — `analyst/rail-word-route-lexicon`: a label must be a word the book's own prose uses and fit the letters; short reads fit exactly; an echo of incomplete letters is no answer; then the AFTER re-measured.
2. **Ground truth and error measures** — `analyst/table-geometry-ground-truth-and-error-measures`: the format per table, the Repair Bench as the truthing tool, the buckets against both denominators, the truth truthed twice with its variability reported, accuracy stratified by a per-table complexity, TEDS / GriTS beside the buckets — before the lever moves.
3. **Where one rail ends and the next begins** — `analyst/rail-boundary-unread` (rails run together into one run; the block record's row groups, a blank-row heuristic, or the vision route — his decision).
4. **A title chopped into header fragments** — `analyst/title-fragments-unread`.
5. **A stray glyph in a table with no matrix** — `analyst/stray-glyph-outside-a-matrix`.
6. **The split table** — the bench's un-split, gated by his "clear"; then the layer can reach it.
7. **The whole-book readability pass under `FULL_TABLES`** — E4b ran the model on the table chunks only; what the class does across every chunk of a book is measured only where a table sits (by construction the rung touches nothing else).
8. **The lever** — `analyst/table-geometry-lever`: his slot; the loss above says not yet.

**What the layer lets us understand that we could not (his question in §9a, answered):** a book's tables can be counted, read and re-read as a ledger, not as zones; the rotated-rail damage is a class we can name, count and bound; and the question "are the letters enough" has an answer — no: reading order alone spells nothing when letters are missing, and a model without a lexicon spells worse than the OCR read.


## §7 Work log (UTC)

- 16:45 `open.sh` clean (S150, pin cc3c2fe); the smoke ALONE 44/44 · 51 s; the record (542a086) with his words, §9a and the plan; the compaction.
- 16:5x–17:04 E1 the note; E2 the instrument and the two censuses (the rule's three versions); the sections; the commits (54be7d9, 2a3ca50); the Desk (9bfcdef6); the receipts (D0284–D0288).
- 17:05–17:27 E3 the repair half, the class, the wiring, the program, the dispositions, docs/15 §12.4; 54/54 · 53/53 · 38/38; the commit (85101ac); the Desk (92edc386); the receipts (D0291–D0293).
- 17:24–17:27 E4a the probe and the deterministic dry runs on both copies; the AFTER census; the commits (aa937fb, 8c1bb40); the Desk (86ae4085); the receipts.
- 17:3x the aside (the view's anchor, 703c37e); the close templates; the seat watcher armed (the law: his seat Active).
- 17:4x–18:0x his two PDFs read (14 Sonnet readers, 4 min; the top pages read by me; the private note); the web check (TEDS, GriTS).
- 18:1x his second and third words (§1b, §1c; 020eb6e, e14b142); the tracker started (18:17Z, detached); E4b launched on his word (18:16Z).
- 18:16–18:29 E4b's four runs (run 1 the spaced answers; run 2 the probe's swallowed grid calls; run 3 the example word leaked; run 4 the AFTER); the fixes and their tripwires; the section; the commits (ee63679, e7209a3); his Desk question answered (df5dec0d); the receipts.
- 18:35 E5, the register, the journal, the close.

## §8 Findings

1. The current analyst cannot see a table as a table, and the markup rung ships a dropped pipe (Observed from the code and the acceptor's selftest, E1/E3).
2. Valentine's tables are healthy but for the split; three rotated rails, one with letters missing — reading order alone cannot spell it (Observed, E2).
3. A signature rule earns its negatives from the book (the ratio-name column); an invariant's negatives catch its own bugs (60 of 120 cells) (Observed, E2/E3).
4. Tables judged whole are protected: 12 restorations, 0 bad edits admitted, across four runs of the readability model (Observed, E4b).
5. qwen3:8b cannot spell a rail from its letters; the letters-only bound admits shuffles and echoes; four labels lost — the word route needs a lexicon (Observed, E4b).
6. A prompt's example word leaks into every answer at temperature 0; a dry-run wrapper can swallow the very calls it should pass — both found by reading the records, both tripwired (Observed, E4b).
7. Rails run together when the OCR leaves no blank row between them; the boundary is unread without the picture (Observed, E4a).
8. Two conversions of one PDF differ by a glyph (Observed, E2).
9. From the two books: the measurement discipline is what the layer still lacks — buckets against both denominators, a ground truth that measures itself, one region per composite element (Observed on the pages).

## §10 Known failures / residue (open at close)

- The four wrong labels are in the dry-run output only (private evidence); the bundles are untouched; the lever is off. The fix is a register row (the lexicon bound), not a rebuild in this sitting.
- The whole-book readability pass under `FULL_TABLES` is unmeasured beyond the table chunks (by construction the rung touches only tables).
- The held copy's split exhibit waits on his "clear" for the un-split; the layer skipped it as a health issue.
- The census's page match by text is Inferred and fragile (49 → 48 when the title moved); the block record's own table ids would be the honest key.
- DAS pp. 261–271 and 376–387 unread (a chunk boundary; an empty extraction); every figure and equation of both books lost to the text extraction.
- The receipts tile by time: E3's window holds E4a's runs; E4b's window holds the reading and the wait.
- The tracker ran from 18:17Z, not from the open; procs before that are UNREAD.

## §15 Current state (Observed at close, tagged where not)

- Public: the layer, the class, the wiring, the program, the dispositions, docs/15 §12.4, the bench aside — committed; the lever `ANALYST_TABLES = False`; `FULL` unchanged. Private: the note, the probes, the evidence of every run, the reading note — committed.
- The pipeline untouched: nothing shipped, no bundle written, no lever moved (Observed: the evidence dirs are the only outputs).
- The held Valentine bundle as it was at the open (the split in it); the anchor copy untouched.
- The card: four dry runs, ≈ 2 min each, peak ≈ 8.7 GB, the model unloaded after each (Observed, the samplers); Ollama holds nothing at close.
- The Desk: five lines from me this sitting and his question answered; Codex asleep on his word.

## §18 Next entry point

1. **S151, on his word (§1b/§1c), opens after this close:** tickets under the persistent method — B6 (`marker_version` via `importlib.metadata`), the lexicon word route (`analyst/rail-word-route-lexicon`) re-measured with the same instrument, the title fragments, the ground-truth format and scorer with a reader panel on rendered pages (the "automate experiment"), B21 (the table-loop tested on its page); lanes in the background where a ticket allows; the card while the seat is empty.
2. **His:** the lever; "clear" for the un-split; D1; the experiment's end; the vision route for the rail boundaries.
3. The report to his Gmail drafts as a document (this record whole, the reading note beside it) — right after this close.
4. S148/S147 §18 leftovers stand: `converter/ceiling-crawl-triggers-the-ladder`, the guards' first live rows, the scan lane's second witness, the Damodarans, a census-grade tracker; `C:/Users/Bndit/ml/.s149-bench/` to remove at a later sitting.


## E1–E5 — the partial closeout's receipts (POLICY §9; tiled onto S149's whole receipt's end, 2026-09-14T04:54:47.096Z)

- **E1–E2 (the analyst noted; Valentine measured — one flow):** `journal/{calls,cost,usage}/S150-ep1-2.md` — window 2026-09-14T04:54:47.096Z → 2026-09-14T17:04:37Z (tiled onto the previous receipt's end); calls 85 · preloaded 8 · consulted 7 · scratch 9 · inferred 33 · harness 28 · denied 0 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 w; Grand total (USD equivalent): 32.90 (session 32.90 + fleets 0.00); Categories used: testing (1), auditing (1), developing (1); harness: api errors 0 · compactions 1 · model fallbacks 0 · hook timings 3 · advisories 1 · Rab's mid-turn words 2. Ledgered D0284 (calls) · D0285 (cost) · D0286 (usage) · D0287 (summary.json) · D0288 (summary.json); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 288 · twin rows 288 · disagreements 2; sessions: 14 · kinds: 5 · missing: 5 · strays: 12 · revisions without a row: 0; the denies cross-check: denies: guard_git main: journal 0 == log 0 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-14T04:54:47.096Z → 2026-09-14T17:04:37Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 40 of 40 messages (100%), 2026-09-14T04:54:47.096Z → 2026-09-14T17:04:37.032Z.
- **E3 (the layer and the class built):** `journal/{calls,cost,usage}/S150-ep3.md` — window 2026-09-14T17:04:37Z → 2026-09-14T17:27:03Z (tiled onto the previous receipt's end); calls 82 · preloaded 9 · consulted 14 · scratch 4 · inferred 7 · harness 48 · denied 0 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 w; Grand total (USD equivalent): 9.60 (session 9.60 + fleets 0.00); (no line starting with 'Categories used'); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 0 · advisories 0 · Rab's mid-turn words 0. Ledgered D0291 (calls) · D0292 (cost) · D0293 (usage); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 293 · twin rows 293 · disagreements 2; sessions: 14 · kinds: 5 · missing: 5 · strays: 12 · revisions without a row: 0; the denies cross-check: denies: guard_git main: journal 0 == log 0 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-14T17:04:37Z → 2026-09-14T17:27:03Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 15 of 15 messages (100%), 2026-09-14T17:04:38.351Z → 2026-09-14T17:26:40.520Z.
- **E4a (the dry run's half without the card):** `journal/{calls,cost,usage}/S150-ep4a.md` — window 2026-09-14T17:27:03Z → 2026-09-14T17:27:51Z (tiled onto the previous receipt's end); calls 7 · preloaded 0 · consulted 0 · scratch 2 · inferred 3 · harness 2 · denied 0 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 work; Grand total (USD equivalent): 0.82 (session 0.82 + fleets 0.00); (no line starting with 'Categories used'); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 0 · advisories 0 · Rab's mid-turn words 0. Ledgered D0294 (calls) · D0295 (cost) · D0296 (usage) · D0297 (run.json) · D0298 (run.json); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 298 · twin rows 298 · disagreements 2; sessions: 14 · kinds: 5 · missing: 5 · strays: 12 · revisions without a row: 0; the denies cross-check: denies: guard_git main: journal 0 == log 0 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-14T17:27:03Z → 2026-09-14T17:27:51Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 3 of 3 messages (100%), 2026-09-14T17:27:05.621Z → 2026-09-14T17:27:50.704Z.
- **E4b (the dry run's half with the card):** `journal/{calls,cost,usage}/S150-ep4b.md` — window 2026-09-14T17:27:51Z → 2026-09-14T18:32:35Z (tiled onto the previous receipt's end); calls 121 · preloaded 5 · consulted 6 · scratch 20 · inferred 27 · harness 63 · denied 1 · errors 2 · lanes: 14 lane transcript(s) with calls in the window · lane calls 68 · lane denies 0 · workflows:; Grand total (USD equivalent): 27.11 (session 20.76 + fleets 6.35); Categories used: none; harness: api errors 1 · compactions 0 · model fallbacks 0 · hook timings 12 · advisories 3 · Rab's mid-turn words 3. Ledgered D0307 (calls) · D0308 (cost) · D0309 (usage) · D0310 (run.json) · D0311 (run.json) · D0312 (run.json); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 312 · twin rows 312 · disagreements 4; sessions: 14 · kinds: 5 · missing: 5 · strays: 12 · revisions without a row: 0; the denies cross-check: denies: guard_git main: journal 1 == log 1 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-14T17:27:51Z → 2026-09-14T18:32:35Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 70 of 70 messages (100%), 2026-09-14T17:27:53.809Z → 2026-09-14T18:32:34.680Z.
- **E5 (gained, lost, missing; the close's own calls up to the receipt):** `journal/{calls,cost,usage}/S150-ep5.md` — window 2026-09-14T18:32:35Z → 2026-09-14T18:35:02Z (tiled onto the previous receipt's end); calls 8 · preloaded 0 · consulted 1 · scratch 1 · inferred 2 · harness 4 · denied 1 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 work; Grand total (USD equivalent): 1.10 (session 1.10 + fleets 0.00); Categories used: none; harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 0 · advisories 0 · Rab's mid-turn words 0. Ledgered D0313 (calls) · D0314 (cost) · D0315 (usage) · D0316 (e5_compare.md) · D0317 (READING-DAS2002-BATEMAN2008-2026-09-14.md); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 317 · twin rows 317 · disagreements 4; sessions: 14 · kinds: 5 · missing: 5 · strays: 12 · revisions without a row: 0; the denies cross-check: denies: guard_git main: journal 1 == log 1 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-14T18:32:35Z → 2026-09-14T18:35:02Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 2 of 2 messages (100%), 2026-09-14T18:33:08.166Z → 2026-09-14T18:35:02.403Z.

*The receipts tile by time, without gaps: E3's window holds E4a's dry runs (its receipt was written after them) and E4a's own receipt is the record's append and commit; the E1–E2 window carries the compaction. The usage receipt printed no `Categories used` line for E3 and E4a (no categorised script ran in those windows) — said, not invented.*

## §19 Close values — `close.sh cc3c2fe`, 2026-09-14T18:38:42Z (verbatim)

```
    PIN              cc3c2fe
    DIFF             18 file(s) since cc3c2fe · dirty 0 = mine 0 · peer-owned 0 (none) · 1 untracked
    GLASS            clean since cc3c2fe (--enforce; bare runs exit 0 regardless — SYM-046)
    RUST             skipped — no windows-widget/ change since cc3c2fe
    CI               no run for 7043b6db yet (unpushed? ahead=9) — NOT a green
    LEVERS           3 threshold-shaped constant(s) added since cc3c2fe with no lever, no waiver: GRID_NUM_PREDICT · TITLE_MIN · FAILS
                     docs/18 §2: a number that decides something is a lever. Convert, or add '# lever-waiver: <who/what evidence>'
    DOCTOR           lever lexical parity: figure-triage.txt names 8 · quoted refs 8 · MISSING 0
                     LEXICAL REF: mode · min_area_pt2 · vector_min_paths · cluster_gap_pt · text_coverage · words_per_line · table_overlap · accounted_for
                     UNREAD — a quoted lexical occurrence does NOT prove the consumer reads the lever; ARMED on MISSING only
    DOCTOR-HEAD      UNREAD — closeout-headline probe re-run not yet mechanized; NOT a statement the numbers reproduce
    CENSUS           selftest.sh: declared 11 = FIRED 11 = banner 11/11 (exit 0)
    CENSUS-SCOPE     UNREAD — broader TEST-STRATEGY fixture census not yet mechanized; NOT a statement all promised tripwires ran
    REGISTER         OPEN-TASKS.md written this session: +6 / -0 lines
                     STRUCK 0 · ADDED 6 — net +6, GOVERNANCE GREW. §I: justify it or work one down
    MEMORY           clean — soft clock is DURABLE at 4a2de91
    DEBT             open SYM 44, unchanged since cc3c2fe
    PUSH             ahead of upstream: 9 (the close pushes; 0 after)
════════ close exit 0 — values only; the judgment half is SKILL.md ════════
```

- **DIFF:** every changed file is in the content commit `7043b6d` or this closing commit; the row follows, then
  `row_check.sh 150` on it, then the push.
- **Relay:** no entry — nothing to Codex (standing).

