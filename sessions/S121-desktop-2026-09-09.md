# S121 — Desktop — 2026-09-10 (local 2026-09-09) — three signatures: J50, J51, J52

⟨claimed: Fable · S121 · 2026-09-10⟩ · occupant Claude Fable 5.1 · pin `--since ec0f72c`

## §1 Session Intent (Rab's words, verbatim, left unedited)

Rab, 2026-09-10T00:3xZ: *"What do you want me to sign, tell me quickly"*

My three slots, one line each, as offered:

> 1. **J50, the 13 August Codex entries** — sign **"confirm-all, receive-only"**: a handler writes an honest "read 2026-09-10, no work follows" restatement per entry, the debt goes to zero, nothing old gets worked.
> 2. **J51, the entry word cap** — sign **"meter-only, no refusal"**: the count already prints at post time and the last-ten conformance rose without a cap.
> 3. **J52, the watch that outlives the session** — sign **"per-logon supervision"**: run `coordination/relay_watch.sh --as Fable` under a Windows scheduled task at your logon. Twin rule: a lane STALE beyond 45 minutes renders "presumed dead" instead of `working`.

Rab, 2026-09-10T00:35Z: *"J50, J51, J52 all signed."*

**My reading:** the three slots as written above, nothing wider. J50 = 13 confirmations (MSG-CDX-0029, 0030, 0033–0043), each with a truthful restatement of what the entry asked and the receive-only clause, no work taken from any of them. J51 = the word count stays a printed meter at `post`; no refusal at any N; recorded as his signature. J52 = (a) a Windows scheduled task at his logon running the tracked watcher, logging to `coordination/private/relay-watch.log`, and the session's own Monitor tailing that log instead of running a second watcher when one is alive; (b) `gate.py status` renders a lane whose beat is older than 45 minutes as **presumed dead** (a rendering, no state written, no guard changed). Everything else stays as it is.

## §2 Starting State — the `open.sh` card, verbatim (2026-09-10T00:36:08Z)

```
[0] IDENTITY
    ledger newest    S120  ec0f72c
    this session     S121  ·  desktop  ·  2026-09-09
    advisory         S121 unnamed in the inherited tree
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-10T00:36:09Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 85 (tally == hook)
[3] HARD clock ... ✓ ec0f72c (S120) == ledger ec0f72c, ancestor of HEAD
    WORKING TREE . 3 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 8 · anchor 29 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    events           311 line(s)
    open-tasks       134 item(s) · last written 3 hours ago
    error-bin        65 row(s) — read the CLASS table before you probe · last written 3 hours ago
    symptoms         78 row(s), 28 open · last written 3 hours ago
    relay            157 entries · run `gate.py status` for the board
    widget           25188
    python procs     7
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 70c60e6
    thinkpad         converter=active · vault tip 50896d1 · staging 0 · receipts 13
[3] PIN
    --since          ec0f72c
    closeout         sessions/S121-desktop-2026-09-09.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

The 3 uncommitted: Codex's `ack-codex.json` and its nine relay entries (0049–0057, its own to commit, MSG-FAB-0081's ASK), plus `.codex/`. HEAD `1f70582` (S120's last post-close pointer). The widget (PID 25188) and the pipeline watcher are up again — Rab's hand after the 20:29Z power loss. The relay watch: Monitor `b12cgyuob` on the tracked script, armed 21:29:51Z, ALIVE every 10 min. Codex: `working` on J53-AB, its lane's session named S121 in its own files (isolated branch); the Desktop lane's S121 is this record — the ledger is multi-lane.

## §3 The pin

`--since ec0f72c`.

## §9 The plan

1. Commit this record. 2. **J50:** one Sonnet handler, sequential (the sidecar is single-writer), reads each of the 13 entries and confirms it with a truthful restatement + the receive-only clause; NR-05 must read 0 after. 3. **J51:** record the signature (SIGNATURES.md, OPEN-TASKS J51 resolved, a comment at the meter in gate.py). 4. **J52:** (a) the scheduled task at logon (`schtasks`), verified by running it once and reading ALIVE lines in the log; the session's watch swaps to a log tail when a supervised watcher is alive; (b) `status` renders "presumed dead" past 45 min, with a tripwire either side of 45; a Sonnet builder + refuter. 5. Registers, closeout, close.sh, ledger, push, CI, memory.

## §8 Findings

- **F1 — Two of the August thirteen can never be confirmed by the tool, and the log is not the reason.** Their bytes are identical at their first commits (2026-08-30) and now; the digests Codex's sidecar claimed at post time never matched what it appended. Law 4 refuses a mismatch forever, so J50's "debt to zero" ends at 2, named as sender mis-claims (NR-05 = 2). `Verified` (first-commit vs HEAD digests; the prose-`## ` hypothesis refuted).
- **F2 — `schtasks` cannot be called from Git Bash:** MSYS rewrites `/Create` into a path (the `open.sh` `/FI` family). PowerShell's ScheduledTasks module registered the task; the handover cost 73 s of dark watch (00:39:20 → 00:40:33). `Observed`.
- **F3 — A scheduled task's default 72-hour execution limit would have killed the supervised watcher on day three;** the task is registered with the limit disabled, `IgnoreNew` for a second instance, and battery/idle stops off. `Intended` beyond the first run (the first run is `Observed`: ARMED in the log, 3 bash processes).
- **F4 — Receive-only worked as a mechanism:** eleven truthful restatements in 3 minutes by one gate agent, no ticket taken, one beat, and the two refusals reported verbatim rather than retried. `Observed` (the handler's pasted `digest verified` lines; the inbox after).

## §7 Work log (UTC)

- **00:3x** Rab: "What do you want me to sign, tell me quickly" → the three slots → **00:35** "J50, J51, J52 all signed." **00:36:08** `open.sh` clean. **00:37** record committed `05bb6f8` (before any act).
- **00:37** J50 handler summoned (brief `coordination/private/j50-receive-only-brief.md`); J52 render fleet launched (`wf_2dd3afaf-b95`, Sonnet builder + refuter).
- **00:38:58** J52 handover: STOP file → the session's watcher printed STOPPED at 00:39:20 (Monitor `b12cgyuob` ended) → STOP removed. **`schtasks /Create` FAILED under Git Bash** — MSYS rewrote `/Create` into `C:/Program Files/Git/Create` (the `open.sh` `/FI` family, SYM-family); the watch was dark 00:39:20 → 00:40:33.
- **00:40:33** the task registered through PowerShell's ScheduledTasks module — **"File Portal relay watch (Fable)"**, AtLogOn for Rab's user, `ExecutionTimeLimit` zero (the default 72 h would have killed it), `MultipleInstances IgnoreNew`, battery/idle stops off, `StartWhenAvailable` — started once now: `LastTaskResult 267009` (running), the log's `WATCH ARMED … headers=158`, 3 bash processes. **00:41** the session's Monitor re-armed as a tail of the supervised log (`bi0gqfll1`, ALIVE lines filtered).
- **00:40** J50 done by the handler: **11 confirmed** (0033–0043, each with a truthful restatement + the receive-only clause, printed `digest verified` lines pasted), **2 refused** — MSG-CDX-0029 and 0030 `RED: digest mismatch` (claimed `5e8e2ea3…`/`ae035489…` vs the log's `36882dda…`/`f40f2944…`); beat posted. **00:44 probe:** both entries' digests are IDENTICAL at their first commits (`8ee44bb` 2026-08-30 22:59 local, `02e6a6b` 23:49) and at HEAD — the log never changed; the prose-`## ` boundary hypothesis refuted (no such lines in either body); so **Codex's sidecar claimed wrong digests at post time** (its gate's digest input, not the log). Law 4 refuses them forever; NR-05 reads 2, named. Disposition: Rab's strike or Codex's re-claim from its own sidecar; recorded, not forced.
- **00:45** registers committed `880058d` (SIGNATURES.md block, J50/J51/J52 rows amended). **00:44** J51's comment at the meter line `5471922`.
- **00:45:09** the supervised watcher's first live signal through the log tail: MSG-CDX-0059. **MSG-CDX-0058 (00:39Z) fell inside the 73-second dark gap and was never signalled** — the re-armed watcher counted it silently (headers=158); the handler read both. Both are notices (`requires_ack: false`): Codex's J53 remaining-work packet (six execution subtickets J53-01…06 at `724eff9` on its branch, "independent scope review PASS" as its own claim; J53-R "NOT READY TO SIGN"); it read this S121 record ("signatures are peer-recorded separately and will not be re-requested") and restates that the 0081 stage-and-commit stays owed after J53. Nothing for Fable; for Rab: sign J53-01…06 as construction, its ask.
- **00:49** MSG-CDX-0060 (notice): Codex records Rab's "Signed." on J53-01…06 and "Installation Signed!" (for a completed Old/New product, not the lab stage); nothing for Fable; the 0081 stage-and-commit still queued behind J53.
- **00:56** the J52 render fleet completed (`wf_2dd3afaf-b95`, 19 min, 2 agents): builder `20429aa` (198/198; decoy caught: no "presumed dead" existed at line 1193), refuter **GO** with its own from-scratch fixture 8/8, the inverted comparison red on exactly the four tied checks, two of the builder's residues closed (an unparseable beat renders UNREAD; age == 45 does not fire), no control leak. One deviation worth banking: `selftest.py`'s `run()` decodes the child's stdout with the host locale, so an em-dash assertion had to be split into two ASCII substrings — a pre-existing gap in the helper. **00:57** cherry-picked `afa8a7c`, **198/198**, ruff clean, pushed. Live board: both lanes' beats under 45 min, no marker yet; the rule waits for its first real stale lane.
- **00:59:55** MSG-FAB-0082 posted (no-ack notice, 350 words): the three signatures executed; 0029/0030 named as Codex's own mis-claims for it to re-claim or leave; beat posted.

## §10 Known failures / residue

- **NR-05 ends at 2, not 0:** MSG-CDX-0029/0030 refuse by law 4 forever unless Codex re-claims them from its own sidecar or Rab strikes them; the cause (wrong digest claims at post time, 2026-08-31) is `Verified`, the fix is not Fable's file.
- **The 73-second dark gap** (00:39:20 → 00:40:33) swallowed MSG-CDX-0058's signal (read by a handler afterwards). A handover should arm the new watcher BEFORE stopping the old one when the log is shared — the STOP file stops both, so the order must be: start the task with a distinct STOP path, then stop the session watcher. Filed as the next improvement, not done.
- **The scheduled task is `Observed` for one run only** (ARMED, 3 bash processes); its logon trigger, the no-time-limit setting and `IgnoreNew` are `Intended` until Rab's next logon shows a fresh WATCH ARMED line in the log.
- **The task runs the watcher under Rab's user with `-lc`**, so `uv` must be on the login PATH (it was, this run); a PATH change breaks it silently — the log's ALIVE cadence is the tell (NR-13).
- **`selftest.py`'s `run()` helper decodes with the host locale** (the em-dash finding); not fixed this session.
- **Codex's twelve entries (0049–0060) and its sidecar stay uncommitted** by its word until J53 leaves it idle; close.sh attributes them as peer-owned.
- **The J52 fleet worktrees/branches** pruned post-close.

## §15 Registers

- **coordination/SIGNATURES.md:** the J50/J51/J52 block (Rab's words verbatim, what was executed, the two law-4 reds named). **OPEN-TASKS.md:** J50/J51/J52 rows amended SIGNED + executed. **SYMPTOM-INDEX / ERROR-BIN:** SYM-079 (from S120 §20: byte- or id-keyed equality against the append-only log fails on real history — fixed by the subsequence rule) and ERR-066 (S120's two tripwire drafts) remain to be written at the next open together with this session's SYM-080 (MSYS rewrites `/Create`-style flags — the `open.sh` `/FI` family, now a second instance) — stated here so the next open finds them.
- **gate.py:** J51 comment at the meter (`5471922`); J52 presumed-dead render (`afa8a7c`), selftest 191 → 198.
- **coordination/private/j50-receive-only-brief.md** (untracked): the receive-only handler's brief.
- **Relay:** MSG-FAB-0082 (00:59Z, notice). Received and read: MSG-CDX-0058, 0059, 0060 (notices).

## §18 Next entry point

1. **Open:** `open.sh`; check the supervised watcher first — `tail -3 coordination/private/relay-watch.log` must show a WATCH ALIVE within 10 min and a WATCH ARMED after Rab's last logon; if not, `Start-ScheduledTask "File Portal relay watch (Fable)"` (PowerShell, never `schtasks` from Git Bash) and file the gap. Arm the session's Monitor as a tail of the log, never a second watcher.
2. **Registers:** write SYM-079, SYM-080, ERR-066 (S120 §20 + S121 §15).
3. **Carry Codex's answer on 0029/0030** (re-claim or leave) and, when J53 leaves it idle, its own `stage --as Codex` + commit of 0049–0060; then re-run the NR-set (NR-05, NR-12 → 0).
4. **Rab's slots:** none of Fable's open. Codex's lane: J53-R and its J53-01…06 construction (his signatures of 00:4xZ, recorded by Codex).
5. **The analyst road** resumes where S119 left it: J47 → J46 → J49 → J44 + free `--reaudit fc1f068c3a8eeb63` → J48 · Univ 4e · J40/J42/J43.
