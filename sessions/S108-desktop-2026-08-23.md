# S108 · Desktop · 2026-08-23 · THE SIGNING — the queue executes

⟨claimed: Fable⟩ · opened 2026-08-23, intent recorded BEFORE work

## §1 Intent

Rab signed the entire queue in his own words: "Signed. All of it. If it's something you think I
should inspect before I sign again, develop it, and leave it at the end for me to sign for it to
pass... defer to codex... Just have a clear standard." Scope of the signature: the fleet check's
ranked twelve (post-S107 Gmail report), the roadmap C0–C6 + cadence laws (ADOPTED as the standing
ranking organ), the atlas plan's eleven integration paths (rebuild-only, per his standing "do not
integrate, just plan" — the PLAN is now signed to execute as rebuilds), A22 relay KEPT.

## §2 The execution standard (stated to him, binding on every lane)

verification-with-probe · gates warn-only first · zero live-library writes (staged to the sign
sheet) · choices built-per-recommendation and listed for ratification · commit-per-item with
strikes · Codex via relay · close green.

## §3 Waves

Wave 1 (this sitting, fleets): close.sh gates (DOCTOR warn-only + census) · CI for
windows-converter + manifest + B1 · P-1 wiring (A18 map repair + A4 out-of-band host, rec'd) +
SYM-050 quarantine LIST · bench security (loopback token) + input fixes (A30 rec'd) + DOM/sentence
tests · roots registry + FP_PIPELINE resolver with scratch-root tripwire · status
source_component + A20 product clock · small mechanicals (F3, B6, B9/B25) · wiki a11y page +
circle rules + adoption receipts. Deferred to sign sheet: C0 book GO, physical bundle moves, pack
eviction priority, A43 remote execution, S94 exercise (blank form staged), atlas acceptance,
arming the gates. Deferred to Codex via relay: D2 clean-bundle P-1 re-measure, its D1 remainder,
adversarial verify of the new gates.

## §4 Starting state

Clocks: S107 / 2c5d87d / row e5aa417, lockstep verified at S107 close; post-close commit 3aadf6d
(Codex relay entries). CI green observed on e5aa417. Pipeline: last event 2026-08-14.

## §5 Codex parcels — D1, D2, and the adversarial gate Circle

⟨claimed: Codex · S108 · 2026-08-23⟩

### D1 — authorship debt

Codex claimed the S97 sections it wrote without claiming Fable's §4a, and replaced the
`docs/43 §3` scaffold with its account of the S97 closing side. Commit `6ae112f` carries the
first Codex model trailer in this queue. The final relay handoff remains the closing act of this
parcel, because it must point Fable to the completed evidence rather than announce completion in
advance.

### D2 — P-1 re-measurement

**Clean negative control, Observed.** The out-of-band host resolved a clean post-S60 bundle by
manifest name and SHA: `bojieli_ai-agent-book…`, converted `2026-08-03T08:00:45Z`, 19 source
pages, source SHA `21bfdffc…60dd3`. Numerator = **1 source-figure page with at least one mapped
asset**; denominator = **1 source-figure page**; result **1/1 = 1.0000**, uncovered **0**,
page map `as-shipped`, `sym050_doubled_offset.detected = false`. Conditions: PyMuPDF 1.28.0 /
MuPDF 1.29.0, Python 3.12 win32 64-bit, CPU-only, one process, live
`figure-triage.txt`, mode `caption`, MD5 furniture identity, report-only/no writes, manifest SHA
matched observed source SHA. **SYM-050 does not hold on this clean bundle.** This is one clean
negative control, not a population claim.

**Poisoned positive control, Observed.** On the 1,356-page pre-S60 `Investment Valuation`
bundle, the full doubled-offset signature fired. Numerator after in-memory repair = **220
source-figure pages with at least one mapped asset**; denominator = **269 source-figure pages**;
result **220/269 = 0.8178**, uncovered **49**. The unrepaired neighbour was **30/269 = 0.1115**,
uncovered **239**. The repair left **0** assets out of range and modified no bundle file.
Conditions were the same instrument/build and live lever file, mode `caption`, CPU-only,
one process, furniture digesting disabled (`--no-hashes`, named in the report), client wall
59.33 s, report-only/no writes. **SYM-050 does hold on the known poisoned bundle.** The clean
negative and poisoned positive controls distinguish the detector from an always-on repair.

Commit `0fbb6e3` wires the narrow S106 repair, adds an end-to-end poisoned-map tripwire, and adds
the zero-write out-of-band host. `figure_coverage_selftest.py` is **30/30** and
`coverage_rescore_selftest.py` is **8/8**. The host also refuses source-SHA mismatch, ambiguous
Markdown, missing asset references, and a Windows legacy-console Unicode crash.

### The scan-page omission class Rab named — Stafford Beer

The Stafford Beer example is a different question from P-1. Its manifest says `lane: scan`, 184
pages, source SHA `b7b711d4…172d9`. Each hand-drawn diagram is a subregion inside a full-page
scan raster; P-1 deliberately excludes that raster and therefore returned numerator **0** /
denominator **0**, coverage **null**. That is **UNREAD for diagram survival**, not clean.
`coverage_rescore.py` now fails this class closed and directs rendered-source-page versus final
asset/placement review instead of emitting a persuasive raster count.

A read-only visual contact-sheet pass covered all 184 source pages, followed by a contact sheet
of all 91 final assets. The first page-map census was a false green: 56 obvious diagram-bearing
pages each had at least one page-mapped asset, but that did not prove the mapped pixels contained
the diagram. High-resolution comparison then found a concrete omission on PDF page **129**
(printed page 101). The source has two large hand-drawn callout bubbles and their connecting
bracket; the only mapped final asset, `_page_128_Picture_15.jpeg`, is a **511 × 70 constant-color
blank strip** (grayscale extrema 240/240, standard deviation 0, entropy 0). Both raw and
analyst-local Markdown place that blank reference after the convergence-of-purpose exercise.
Neither callout's text survives in Markdown. In context, the lost annotations challenge the
book's academic classification categories and ask who authorized those categories to overrule
the natural laws of viable systems. This is a verified semantic visual omission, not merely an
asset-count ambiguity.

Two mechanisms remain concrete. Marker selected a blank subregion instead of the meaningful
hand-drawn page regions, so page-level asset presence cannot prove crop completeness. Then
`analyst.py` replaces every surviving image with `IMG-n`, sends only
text and opaque tokens to the model, and accepts a candidate when the **sorted token multiset**
matches. The model never sees the pixels, and the fence does not prove that a token stayed beside
its caption. A Markdown-only agent likewise receives the reference, not the diagram. Required
follow-up: a scan-lane rendered-page/asset visual coverage instrument plus an image-adjacency
fence (or protected image paragraphs). Until then, a surviving asset count cannot license “the
LLM saw the diagram.”

### Gate Circle — adversarial verification and disposition

Two independent read-only lanes attacked the new close gates before repair.

- **DOCTOR violation:** comments and inert strings greened as “consumed”; an unparsed
  declaration silently shrank the denominator; grep rc >1 rendered “missing.” The repaired card
  validates the complete declaration grammar, distinguishes grep rc 1 from probe failure, and
  labels matches `LEXICAL REF`. It explicitly prints **UNREAD — lexical occurrence does not
  prove a consumer read**. Therefore DOCTOR remains **warn-only and unarmed**; a structural
  consumer registry/probe is still required before arming.
- **CENSUS violation:** the old parser read only the denominator, so `0/2` greened; a hard-coded
  `2/2` could conceal an unreached T2; oversized integers fell through the shell comparison.
  The repaired gate requires unique declared IDs, explicit `TRIPWIRE Tn FIRED` control-flow
  markers, one anchored banner, bounded positive integers, and
  numerator = denominator = declaration count. Planted `0/2`, missing T2, and oversized-number
  fixtures all bite. Broader TEST-STRATEGY census remains an explicit `UNREAD` row.

Verification on the repaired revision: Bash syntax clean; wiki tripwires **11/11**; full muster
tripwires **37/37**. The historical red-CI network case was explicitly **SKIP/UNREAD because
GitHub was unreachable**, not counted as a pass. Commit `0bec6a7` contains the repairs and
negative controls. Circle disposition: **HELD at warn-only; do not arm DOCTOR or the broader
census claim this close.**

Boundary: no live-library, vault, pipeline, conversion, GPU, or bundle write was performed by
these parcels. Corpus PDFs and assembled bundles were opened read-only; generated visual contact
sheets and audit helpers live outside the repo under Codex's visualization workspace.

## §6 Close (Fable) — wave 1 landed, verified, struck

⟨claimed: Fable · 2026-08-23 close⟩

Wave 1 shipped across two fleets (one killed by a usage wall mid-write, resumed;
Codex completed and committed two dying lanes' work — `0bec6a7`, `0fbb6e3`, dual-trailer)
plus my own integration. Verifier verdict: **GREEN-WITH-WARN-ONLY** — every suite re-run
independently: cargo 31/0 · bench tests 19 OK · figure_coverage 30/30 · fp_paths scratch
tripwire PASS (22/22 roots, 25/25 constants, 0 literals) · acceptance 41/41 · muster 38/38
· wiki CLEAN. Register struck per §H rule 1 (`c56d486`); six wiki pages truth-updated and
restamped (`0ae635f`); F3 completed (`ffc7b8a`+`97906a3`). Codex collected D1+D2 and filed
SYM-053. Ratifications and GOes: **`sessions/S108-SIGN-SHEET.md`** (15 signable items).
Non-destructive continuation while Rab is out, per his word: the relay parcel below;
nothing further touches runtime, library, or vault this sitting.

### §6a Close-phase addendum

The row (`7a1e9f4`) cites close `a652781`; three commits landed DURING the mechanical close
as its own new gates fired on wave-1's code — `307e491` (LEVERS: SYM050_SLICE waiver),
`e4d731e` (GLASS: writer identity rendered + slice_size signed REPORT). Final close SHA for
the clocks: **`e4d731e`**, close.sh exit 0 with pin `2c5d87d`. This addendum exists so the
row-vs-final gap is DECLARED, not discovered (the 1790554 lesson, honored inside the close
instead of after it).

### §6b Final line — CI observed

First CI run over the wave (006d46b) came back RED: lane F's new status test files tripped
the PRE-EXISTING enforcing lint gate — the exact failure the "ruff absent locally" gap
predicts; the new warn-only steps were not the cause. Repaired with CI's own pin in a
scratch env (`2a4fa21`, format-only). **CI success on `2a4fa21` — observed, not assumed.**
The converter lane's first ruff scan shows a 16-finding backlog behind its warn-only step
(sign sheet §E). Final repo SHA of the sitting: `2a4fa21`.

## §7 POST-CLOSE ARC — the two-model protocol (2026-08-24, after the ledgered close)

**Read this before treating the clocks as broken.** S108 closed at `e4d731e` (row `7a1e9f4`, CI
observed green on `2a4fa21`). Everything below landed AFTER that close, in the same sitting, and is
NOT a rewind. This is the same gap `1790554` named at S106: the two-clock protocol has no
representation for post-close work. Recorded here rather than left to be discovered.

**Model handoff:** the arc up to the wiki was Fable 5; from the residency block onward, Opus 5
(Rab switched deliberately). Per the generation caveat, Opus 5 re-read the block on its merits and
took ownership rather than inheriting.

**What was built, all pushed:**
- **`/relay-gate`** (`.claude/skills/relay-gate/`) — the ACK layer over `relay.md`. Sidecar state
  (`ack-<model>.json`, single-writer each), three-state qualifier with **restatement + independent
  re-digest**, GUARD A (no new ticket into a `working` recipient), GUARD B (no silent trip to Rab —
  `blocked-on-rab` requires an announced escalation), and its second half (a post may never
  downgrade `blocked-on-rab`). **Selftest 25/25**, every law both directions.
- **The Bus Standard** (`coordination/BUS-STANDARD.md`) — Rab's design: one bus, halt while
  parsing, appends never erase, digests never trust, everything else is a turn. The territory/lease
  draft is recorded as **withdrawn**.
- **Codex residency** (`coordination/CODEX-RESIDENCY.md`, `codex/` created by Codex itself) —
  public-repo law, three rungs (exists→endorsed→adopted), communication symmetry.
- **The prompt contract** (in `RELAY-ACK-PROTOCOL.md`) — GROUND·ASK·DONE·BOUNDS·ROUTE, marked
  **OPEN for Codex's amendment**.
- **The private layer** (`coordination/private/`, `codex/private/`, gitignored) — evidence stays
  local, the bus keeps pointer+digest. Rab's correction: both models share one filesystem; only
  the *push* was ever the boundary.
- **`coordination/SIGNATURES.md`** — four signed entries, honest that it is a transcript not proof.

**Live protocol state (reconstructs from disk — run `gate.py status`):** 13 Fable messages, 4 Codex
messages, every one confirmed with a verified digest in both directions. Fable `blocked-on-rab`;
Codex idle.

**OPEN, in priority order:**
1. **`CR-CDX-0002`** — the open escalation, Rab's alone. Codex's reconciliation of the relay's
   three-part envelope with the five-slot transaction. **Both models endorse it without dissent** —
   which is exactly why neither may decide it (authority by domain).
2. **Sign-sheet item 15** — the adversarial review of Codex's **Concordance Lab** dossier
   (988 lines, `f0787c02…`, external). It is the orchestration/"ghost" app spec, and the relay gate
   turns out to be a working implementation of its transaction layer.
3. **`T-004`** — the joint seam (EXTRACTED vs AUTHORED terms), blocked on Rab.
4. **`sessions/S108-SIGN-SHEET.md`** — 15 items still unsigned.
5. **C0 has still not breathed.** Roadmap law 1 stands: product before governance.
