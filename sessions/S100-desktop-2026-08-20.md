# S100 · Desktop · 2026-08-20 · THE CONCORDANCE AMENDMENT, AND THE FIRST TEST

⟨claimed: Fable · S100 · 2026-08-20⟩

## §1 Session Intent

Rab's commission, in his words: **"Sign it, and then lets run a test"** — signing the
concordance amendment debated across two relayed exchanges with ChatGPT, then exercising the
new machinery rather than trusting it.

The aligned reading (no `/echo` needed: an explicit signature on a named, already-framed
proposal is unambiguous by construction — SKILL.md Phase 0's own carve-out):

1. **Sign and build the concordance amendment** into `coordination/relay.md`: the carrier
   labels every carried claim `agree` / `conflict` / `unverified`; relayed FOR RAB text is
   **must-quote**, never paraphrased; `unverified` is the default for anything the carrier has
   not checked itself; and — his refinement from the "it sounds a lot more complicated"
   exchange, the one that collapsed cross-model agent choreography into a single line — **a
   `conflict` must name the probe that would settle it, and either side (or its agents) may
   run that probe.** Factual disputes resolve by evidence; only judgment, priorities, and
   authorization reach Rab.
2. **Run a test.** A guard born today gets its tripwire today (docs/32 §6): build
   `coordination/selftest.sh` and prove the protocol's rules are mechanically checkable, on
   fixtures AND on the real file — including the carry-selection rule (newest addressed to me,
   never re-carry what my lane already carried) and the ledger-row format whose S99 violation
   this project already caught once.

Related standing: ChatGPT's cooperation pledge (relayed by Rab, recorded verbatim in the
memory library's `external-ai-relay-protocol`) endorsed this amendment; per that same
protocol, another model's endorsement is discussion, and only Rab's word above builds it.

*(This section is written before work and is not edited afterwards.)*

## §2 Starting State

`open.sh` clean, 2026-08-20 (values): ledger newest S99 `89ffd7d` · this session S100 · SOFT
76 (tally == hook) · HARD mirrors ledger, ancestor of HEAD · origin in sync (fetched) ·
pipeline held 4 · anchor 23 · levers audit=enforce · analyst=local · batch=16. **Pinned
`--since`: `89ffd7d`.** Register item 12 verified free at filing (SYM-045's rule, exercised).

## §3 Work Plan

1. This closeout; commit the open.
2. Amend `coordination/relay.md` §Protocol (the four rules above) + the carry template.
3. Build `coordination/selftest.sh`; run it; record the result honestly whichever way it goes.
4. Register item 12 SIGNED in his words.
5. Write relay entry 2 (Fable→Codex): the amendment announced, ChatGPT's pledge quoted with
   provenance, the asks restated.
6. Close in lockstep from `89ffd7d`; push.

## §4 Scope and Evidence Boundary

- Repo writes: `coordination/` (relay.md amendment, new selftest.sh), docs/37, SYMPTOM-INDEX
  if a defect surfaces, CHANGELOG, this closeout.
- **No pipeline, widget, converter, or vault file. No GPU process. No home-bootstrap edit**
  (S99's relay step already names the protocol by reference, so an amendment inside the file
  needs no bootstrap change — verified before claiming).
- Codex's entries, sections, and claims remain untouched and unauthored.

## §6 Implementation Delta (`--since 89ffd7d`)

Commit per task: open (this file) · **the amendment + its tripwire + relay entry 2** in one
commit (they are one act: the rule, the proof it is checkable, and the first entry written
under it) · register **item 12 SIGNED** · this close.

- `coordination/relay.md` §Protocol → `### The concordance amendment`, four numbered rules.
- `coordination/selftest.sh` (new, 11 cases) — the protocol is now **mechanically checkable by
  either model**, which is what makes the amendment enforceable rather than aspirational.
- Relay entry 2 (2026-08-20T23:02Z, Fable→Codex): the amendment announced, ChatGPT's pledge
  **quoted verbatim with provenance and labeled `unverified`** — the new format demonstrated in
  the first entry written under it, on the one claim I genuinely cannot check.
- No pipeline/widget/converter/vault file; no GPU; no bootstrap edit (verified unnecessary:
  both bootstraps reference the protocol by file, not by rule).

## §7 The Test — what was run and what it found

**`coordination/selftest.sh`: first run 8/9 RED → 11/11 green after fixing the test.**

- **The red was the test's own bug, not the system's.** Case 5 asserted *every* ledger row
  carries the `S<n>:` key; 21 rows (July 2026, task ids `L1–L4`/`W5`/`W6`) predate session
  numbering and legitimately carry none — muster already reports them as "21 discarded". The
  assertion was rewritten to the property that actually guards the S99 regression: *every row
  that NAMES a session carries it in parseable position* — now 86/86 — plus a new case proving
  the guard **bites** on an S99-shaped row, and a case proving muster's census is **complete**.
- **The near-miss worth recording.** Chasing that red, I compared the file's 107 rows against
  "80/105" and was about to file a defect against muster's census. The probe said otherwise:
  independent recompute = ok 82 + lane 4 + discarded 21 = **107**, exactly what muster prints
  today. My "80/105" came from the S97 open card pasted at 19:47Z, before two rows existed.
  **A stale reference nearly became a claim about a guard.** Caught by `tools-over-inference`
  doing precisely its job — and it is the second time in two days that a number quoted from a
  document that had moved was the failure mode (docs/21's founding lesson).
- **Live behavioural test on the real relay**, not fixtures: carry-selection returns *nothing*
  for Fable (correct — Codex has written nothing) and exactly the 22:13Z entry for Codex. The
  open-time behaviour is proven in the real state, in both directions.
- Re-run after every subsequent edit this session: 11/11 each time.

## §8 Decision Ledger

- Amendment built exactly as signed, including the probe-naming line from his "it sounds a lot
  more complicated" exchange — the line that replaced cross-model agent choreography with one
  obligation: **a conflict must be falsifiable**.
- ChatGPT's endorsement recorded as discussion, never as authority (external-AI-relay law);
  its pledge carried at `unverified` because a statement about future conduct has no probe.
- The test's red was fixed in the TEST, not by loosening the system — and the failure is
  recorded in the closeout, the selftest's own comments, and relay entry 2. A guard that
  quietly rewrites its assertion after going red is a proxy with a birth certificate.

## §9 Known Failures and Unread Surfaces

- **Codex has still not run.** Every cross-model claim remains half-proven: no entry from it,
  no claims filed, no trailer adopted. The relay's *carry* step is `Intended`, never Observed —
  the first Codex session is the only thing that can promote it.
- The amendment binds Codex only if Codex reads it; adherence is voluntary by construction
  (there is no enforcement channel, and building one was never signed).
- SYM-045's mechanical id-preflight still unbuilt.

## §10 Current State

Pipeline untouched (held 4 · anchor 23 · enforce/local/16 · exe C3C05D49 · vault 6). Register:
items 6–9 OPEN (the conversion-completeness slate — still the largest unspent decision),
10–12 SIGNED+built. Three tripwires now guard the governance layer: muster 22/22 · echo 5/5 ·
coordination 11/11. Relay: 2 entries, both Fable→Codex, both unanswered.

## §11 Next Entry Point

Muster, then carry the relay (currently: nothing new for Fable). Then his word by slot —
**items 6–9** are the real work waiting: P-0's wiring slice is the S-effort first move, and
the glass-mode question rides with it. For Codex: the suggested prompt in relay entry 2.

## Correction (appended S101, 2026-08-20) — the glass evidence in this closeout was hollow

This closeout cited **`glass_detector.py` exit 0** as evidence of observability cleanliness.
That claim proved nothing: the detector exits 0 **unconditionally** without `--enforce`, and a
bare run prints its unsigned glitches and still returns 0. Measured S101: bare → exit 0 while
64 unsigned glitches were listed; `--enforce` → exit 1; `--since <pin> --enforce` → exit 0 with
"no unsigned glitches", which is the ritual's actual form and the only one that tests the
property (*this session introduced none*).

**The substance was right, the evidence was not.** Re-run at S101 against every pin in the
S98-S101 arc (`3b56f13`, `d452c9a`, `89ffd7d`, `bbf6e70`), each with `--enforce`: all four exit
0, "no unsigned glitches". So no session in this arc introduced an undispositioned measurement
- but that is known now, and was not known when it was claimed. Filed as **SYM-046**. The
original line is left standing above rather than edited: the record shows what was claimed and
what corrected it.
