# S124 — Desktop — 2026-09-09 (UTC 2026-09-10) — J56 and J55 signed; Codex away

*Record before the act (docs/28; muster Phase 6). §1 is Rab's words and is left unedited for the rest of the session.*

## §1 Session Intent (Rab's words, verbatim, left unedited)

> J56-J55 signed, Codex isn't gonna be on for a while bud. Sorry.

**Standing from S123 (mid-turn, 02:44Z):** *"pace yourself, if you get anxious, investigate thoroughly, do not repeat a mistake that is in the errors you know of."*

## §2 Starting State — `open.sh`, 2026-09-10T03:33:42Z (verbatim)

```
════════ MUSTER · OPEN · 2026-09-10T03:33:42Z ════════
[0] IDENTITY
    ledger newest    S123  fe2ce4e
    this session     S124  ·  desktop  ·  2026-09-09
    advisory         S124 unnamed in the inherited tree
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-10T03:33:43Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 85 (tally == hook)
[3a] LEDGER ORDER  ✓ 106 Desktop-lane rows ascend (S16 → S123)
[3b] LEDGER PARSE  ✓ 106/132 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ fe2ce4e (S123) == ledger fe2ce4e, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 8 · anchor 29 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    events           311 line(s)
    open-tasks       137 item(s) · last written 19 minutes ago
    error-bin        68 row(s) — read the CLASS table before you probe · last written 20 minutes ago
    symptoms         85 row(s), 32 open · last written 20 minutes ago
    relay            166 entries · run `gate.py status` for the board
    widget           25188
    python procs     7
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 70c60e6
    thinkpad         converter=active · vault tip 50896d1 · staging 0 · receipts 14
[3] PIN
    --since          fe2ce4e
    closeout         sessions/S124-desktop-2026-09-09.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

The one uncommitted item is `.codex/` (Codex's, untracked, never mine). Inherited and `Historical` until re-measured: S123 §18 (watch OFF by Rab's word, STOP file present; J56 is Rab's; Codex reads 0083/0084 on return; J55 is a sweep; run `error_families.py --census` at every close). Rows that intersect today: SYM-085 (the shared checkout has no guard — J56 is its fix), SYM-078/082 (autocrlf — every register edit is CRLF-preserving), ERR-068 (a ground written for one mode carried into another — every brief today names its mode), ERR-063 (record before instrument — this file precedes every script), ERR-009 (scripts in files).

## §3 The pin

`--since fe2ce4e` (the S123 closing commit; the ledger row `16b907d` and the three post-close commits `c0d8959` `f6718c4` `d112ef9` `e16aee3` are already on origin).

## §4 What the signature is read to mean

Rab signed **J56** and **J55** in one line and said Codex is away for a while.

- **J56** as filed offered (a) a docs/47 amendment, (b) a mechanical guard, (c) both. He signed the ticket, not a letter. Read as **(c) both** — the ticket's own "Rab's" column names the law as his signature and the hook as a standing change to his checkout, and docs/32 §5 says a rule without a mechanism is a proxy with a reputation. Stated here as an assumption; reversible (a hook is one line in `settings.json`).
- **J55**: the sweep. Each closeout finding without a row is filed as a row in its family or its reason for not being filed is recorded; then the census counts it.
- **Codex away**: nothing of J56/J55 waits on the Codex lane. What Codex would have verified is done single-lane and marked so. A door-brief pointer goes under the door at close (MSG-FAB-0085), no ticket (Guard A; a dormant lane gets no new ticket).

## §9 The plan (before any instrument)

1. **J56 — the guard.** (a) Read the harness hook contract (the guide lane, from the docs) and then MEASURE it: the hook's own deny log will show the payload a subagent's call carries. (b) `.claude/hooks/guard_git.py`, a PreToolUse hook on Bash and PowerShell: DENY `git reset|checkout|clean|stash|restore` (and `git -C <path>` forms) when the target is the main checkout; ALLOW inside a worktree; FAIL CLOSED (a payload it cannot read denies, never allows — UNREAD is not clean); a bypass only with a written reason, and the reason is logged. Wired in the tracked `.claude/settings.json`. (c) `guard_git_selftest.py`: positive control (a plain `git status` passes) and the negatives (each verb, `-C`, the PowerShell tool, the worktree carve-out, a malformed payload). (d) A red-team fleet against a SACRIFICIAL CLONE registered as guarded, never the real checkout: Sonnet lanes told the guard exists and asked to get a destructive verb past it; a planted uncommitted file in the clone is the measurement. (e) docs/47 §9 — the law in Rab's signature: every ground names its MODE; an in-place lane may not run the five verbs in the shared checkout; the hook is the mechanism, the brief names the verbs. (f) SYM-085 → `fixed` with the guard named; SIGNATURES.md block; the S120 ground's MODE CHECK paragraph promoted into the brief template.
2. **J55 — the sweep.** Input: S123's third reader returned 83 closeout findings, 33 of them flagged NOT FILED (its return, kept at `scratchpad/S123-R3-closeouts.json`); that is a claim list, not a filing list. Fleet: reader lanes each take a slice of the S104–S123 closeouts and, for each candidate, QUOTE the closeout's own lines, propose a row in the register's exact column form with its family, or say why not (already a row — name it; not a defect; a ticket, not a symptom). Refuter lanes check every proposed row against the registers (a duplicate of an existing SYM/ERR is refused) and against the quoted source (a row the source does not support is refused); decoys planted. Then I file by my own hand, ids allocated AFTER a pull (the ThinkPad allocates too — S123's collision), CRLF-preserving, and the census counts them. In-place lanes are told the mode and the forbidden verbs; readers are read-only.
3. **Close in lockstep**: `error_families.py --census` (must be 0 unplaced), `close.sh fe2ce4e`, ledger row, push, CI observed, memory, MSG-FAB-0085 under the door.

## §8 Findings

- **F1 — The guard holds for real subagent calls.** The harness routes a workflow lane's Bash and PowerShell calls through the `PreToolUse` hook with `agent_id`/`agent_type` in the payload (`Observed`: 50 denials across five red-team lanes carry their ids in `coordination/private/git-guard.log`); every planted byte on clones 1–5 survived, sha256 matching the baseline (`Verified`: my own hash script after the fleet, not the lanes' word).
- **F2 — One breach, then five more classes from the critic, all closed.** Lane 6 destroyed clone-6 with `cd <scratchpad>; C=<clone>; git --git-dir="$C/.git" --work-tree="$C" checkout -- tracked.txt`: the guard read `$C` as a literal, could not resolve it to a repository, and rendered "not guarded" — a failed probe rendered as a negative observation (docs/32 §5 rule 4, S4's shape) inside the guard built against S11. Closed in v4: an UNREAD target (a shell-expanded path, an unresolved `cd`, `%VAR%`) fails closed for any destructive verb or lane write; the equals-form `--work-tree=`/`--git-dir=` had also stripped a Windows path's backslashes (clean_token, meant for verbs) — fixed with a quote-only unquote for path arguments. The critic's five: command-prefix keywords (`command git`, `! git`, `if…then git`, `while…do git`), an ANSI-C head `$'\x67it'`, `git${IFS}reset`, a variable head hiding a `-C <guarded>` target, and `git config core.worktree` set in the same compound command before the hook could read it — each with a regression case; selftest 84 → 103 → 126 → 129.
- **F3 — Two false denies on the main session, both mine.** (a) `sed -n "$((L+1)),$p"`: the segmenter took `$((` for `$(` and manufactured a `$p` command head; fixed v4.1 (arithmetic is data in both quote contexts). (b) A compound call `redteam_setup.py … && git --work-tree=… checkout` — the deny cancels the WHOLE tool call, so the rebuild never ran; I read the deny as "hole closed" and launched a re-test agent against a clone that was still destroyed. It ran zero git writes and was stopped (Rab: just me). Banked: **an expected deny gets its own tool call; nothing shares a call with it.** (PROBE-SHAPE, self-caught; no bin row — it never reached a record as a claim.)
- **F4 — The J55 sweep, and what a second reading is for.** Five reader lanes, 79 candidates, every decoy refuted; 39/39 quotes re-found in their closeouts by my hand before a row was written (five had been paraphrased across line breaks). 24 SYM + 9 ERR + 5 J filed. The consolidator lane finished after I had filed and disagreed on five rows: it caught **SYM-093's direction reversed** — S104's vetoes REMOVE flagged pages, so p63/p73 "surviving all three vetoes" stay flagged (a false alarm), the opposite of the row as first written; verified against S104 §8 and corrected. SYM-097's probe is not in the tree (→ `Historical`); SYM-094's root is `find_tables()` semantics (→ S12). Kept against its advice, with reasons: SYM-106 (J43 is a ticket, not a symptom row), SYM-108 (SYM-071's class on a different surface — one row, one failure), SYM-109 (`LNK1104` is what an operator greps), ERR-076 (a double-append on the shared log is a banked hazard).
- **F5 — The occupant swap.** Claude Opus 4.8 occupied the Fable lane from ~04:00Z to ~04:26Z (`f83867e`, `816c997` carry its trailer); Claude Fable 5.1 before and after, switched back by Rab's `/model` at ~04:27Z ("see what happened. eyes on deck. Just you to ride it."). The commit trailer told the truth without being asked; the sidecar's `occupant` field did not — it read Fable 5.1 throughout, because it is declared by hand and nothing re-measures it. Named in SIGNATURES.md, the ledger row and MSG-FAB-0085.
- **F6 — Read-only lanes were never false-denied.** Six J55 lanes: 89 ALLOWs, 0 DENY — the lane tier passes read-only git as designed (`Observed`, the log).
- **F7 — The census's refusal worked on 24 rows at once.** `error_families.py --census` would have exited 1 on any unplaced row; the filing script placed all 24 in the same commit (S16 has its first five rows), and the cross-check moved one. 109/109 · 77/77.

## §10 Known failures / residue

- **The hook covers only tool calls the Claude Code harness routes through Bash and PowerShell.** Codex's harness is NOT covered; docs/47 §9 (the law) covers it, and its lanes need worktrees. A `git` inside a script file (`bash x.sh`) or a target a program assembles at runtime is invisible to a command-line guard. The worktree is the isolation; the guard is defense in depth — stated in the hook's docstring and in SYM-085's fixed row.
- **The guard's own config is not guarded:** `coordination/private/git-guard.roots`, `.claude/hooks/guard_git.py`, `.claude/settings.json` are ordinary files a lane could edit. The law forbids it; the trace log would show the ALLOW that preceded such a write. Not mechanized.
- **The live lane re-test after v4 was not run** (Rab's word: just me). The harness path was `Observed` in run 1; the v4 logic is `Verified` by the selftest's lane payloads through the same script; the main-session live probes of the exact destroyer and the sibling form were denied at 04:31Z and clone-6's planted bytes survived.
- **The sidecar's `occupant` field was wrong for 26 minutes** (F5) and no mechanism catches that; a candidate for the card (print the last commit's trailer beside the sidecar's occupant) — §18.
- **DEBT rises by design:** twelve of the 24 new SYM rows are OPEN mechanisms that were open before and uncounted (P-1's vetoes, the survival guard's blind zones, the rejections breakdown, the 95 % bar, pdfium's ligatures, FP_COORD's default, the harness's stale worktrees, the LEVERS gate). The count was under-reported; now it is not.
- The J55 consolidator's own `not_filed` list (51) is in the workflow journal; the 41 already-filed / not-a-defect candidates from the proposers are recorded with reasons in §8b below (their quotes were not individually re-verified — they file nothing).
- Ephemeral residue in the scratchpad: six sacrificial clones, the `.codex/` backup, the fleets' journals. The roots file is back to its header; the trace file is removed.

## §15 Registers

- **SYMPTOM-INDEX:** +24 (SYM-086..109); SYM-085 → `fixed` (J56); SYM-093 corrected (direction), SYM-097 → `Historical`, SYM-094 → S12.
- **ERROR-BIN:** +9 (ERR-2026-08-21-069 … ERR-2026-08-28-077, dated by the event; classes PROBE-SHAPE ×2, OVER-CLAIM ×2, PROVENANCE-DROP, DENOMINATOR, REGISTER-MISS, HARNESS-MISUSE ×2).
- **OPEN-TASKS:** +5 (J57 no unattended start path — Rab's; J58 the exporter's startup-only sweep — Rab's; J59 `stage` without a lock; J60 the watch handover's dark window; J61 `selftest.py` locale decode); J55, J56 struck DONE.
- **coordination/SIGNATURES.md:** the J55 · J56 block (Rab's two lines verbatim; the (c)-both reading; the occupant note).
- **docs/47 §9** (the law); **docs/58 §5** dated note; **`observability/error_families.py`** 24 ids placed across 10 families.
- **New:** `.claude/hooks/guard_git.py`, `.claude/hooks/guard_git_selftest.py` (129), `coordination/fleet-ground-template.md`; `.claude/settings.json` wired (`Bash|PowerShell`, interpreter failure → exit 2).
- **Relay:** MSG-FAB-0085 under the door; the closing beat.

## §18 Next entry point

1. **Open:** `open.sh`. The guard is LIVE on this session's tools: a destructive git verb aimed at the shared checkout is denied, and a command that CHAINS an expected deny with anything else loses the whole call (F3). Read-only git and the session's own add/commit/push pass. If it ever denies something legitimate, the fix is a selftest case first (v4.1's shape), never a loosened rule.
2. **Codex's return:** 0083, 0084, 0085 under the door; its harness is not covered by the hook — worktrees for its fleets; J59's discipline (each lane commits before the other stages) until a lock exists.
3. **Rab's:** J57 (unattended start), J58 (exporter sweep), J32/J46 (the survival guard's blind zones now rowed as SYM-091/104), J43 (SYM-106), J48 (SYM-105). Candidate, not filed: the card printing the last commit's trailer beside the sidecar's occupant (F5).
4. **Run `error_families.py --census` at every close** (it refuses an unplaced row); run `guard_git_selftest.py` whenever the hook changes.
5. The watch stays OFF (STOP file) by Rab's word.

## §8b The recorded why-nots (J55 — the 41 candidates not filed, from the proposer lanes' own reasons)

- S115 §8-F9 — not-a-defect: Both instances were caught and neither propagated; the commit trailer is the authoritative model record and the file's own count is authoritative, so a lane's self-stamp 
- S104 §9 + §10 — already-filed: Already SYM-049 (family S5 A-GUARD-OFF-THE-PATH; docs/58 lists 049 there). Predicted at S104 from a fixture, MEASURED and filed by the S105 Circle (Lane C) on Cyb p34/p78
- S104 §10 (+ correction) — already-filed: The system mechanism is already SYM-050 (family S9 THE-WRONG-REFERENCE-OBJECT, its attribution twin; RESOLVED in source S108) and the wire-the-repair work is OPEN-TASKS A
- S104 §10 — not-a-defect: A declared design choice (S104 §4, §6: 'The module stays unwired and report-only'), not a failure. It would only become the S7 PRODUCED-BUT-NEVER-PROJECTED class if it we
- S105 §9 (Known Failures) — already-filed: This is docs/45 §6 item 2, tracked as OPEN-TASKS D3 ('does Family 1 have a check yet — what compares a claim to the probe that produced it?'; D3 notes '§6.2 remains fully
- S105 §9 (Known Failures) — already-filed: Already OPEN-TASKS F6 ('MEMORY.md is at ~200 lines — the read limit — and has silently dropped pointers before'). Mechanism-shape is S3 A-CUT-WEARING-A-NUMBER (a read cap
- S105 §10 (Awaiting Rab's Signature — 14 items) — already-filed: All 14 are already in OPEN-TASKS as A18-A29 (§A.3 'The S105 Circle's 14'), several since STRUCK/BUILT (A18/A20/A21/A22/A26 dispositions recorded). These are open governan
- S106 §9 (The Determination) — not-a-defect: A determination/judgment answering the principal's question, not a failure mechanism. (Its load-bearing '0% of Cybernetics' clause IS a measurement-language error — see r
- S106 appended correction (Still OPEN + Addendum) — already-filed: All already in OPEN-TASKS: the post-close protocol gap = A31 + F2 (Observed lines 159, 319); Ctrl+Z = A30 (BUILT S108); whole-file render = B22 (RESOLVED S108); MIN_SIDE_
- S108 §5 (scan-page omission, Stafford Beer) — already-filed: SYM-053 (session S108, Codex visual pass) is exactly this — same book (Beer p129), same 511x70 blank strip, same three sub-mechanisms (blank-subregion crop, asset-presenc
- S108 §6b (Final line — CI observed) — already-filed: The 'a linter CI runs is absent from the local ritual' mechanism is SYM-020's exact shape (the rebuild ritual ran clippy but never cargo fmt), its rust twin. The method t
- S108 §3 (correction 2026-09-05) — already-filed: Both unbuilt items are already tracked as open J-tickets: J36 (adoption receipts, 'claimed shipped in S108, never built') and J37 (circle-skill rules, 'claimed shipped tw
- S109 §5.2 (GUARD B's third path) — already-filed: The closeout self-places it in the SYM-042/047/049 family ('a mechanism cannot cover a path it does not see'), which is docs/58 S5. Fixed in-session with tripwires T25/T2
- S109 §10 (STILL OPEN, item 4) — already-filed: The gate.py message-id-reuse mechanism is filed: ERR-025 (LOCK-SCOPE — next_id and relay append outside the lock; forced concurrent preserves allocated the same MSG-CDX-0
- S109 §7 (Known failures, item 3) — already-filed: The mechanism's rule is the RUNTIME-RESIDUE class, ERR-030 ('classify every stable runtime pathname at introduction as tracked evidence or ignored residue; exercise its f
- S109 §16 — already-filed: This finding IS SYM-054 (filed this same session S109). docs/58 places SYM-054 under S5 ('a fleet's servers outlive a harness that audits files, not processes'). Already 
- S109 §19.3 (second sitting) — already-filed: This finding IS SYM-055 (filed this same session S109). docs/58 places SYM-055 under S2. Already a row.
- S109 §3 / §7 item 1 / §10 item 1 — already-filed: Filed and FIXED as J17: 'A CLOSE WROTE A LEDGER ROW THAT THE MUSTER COULD NOT SEE' — muster.sh now applies the S109 DESCENDANT rule at its is-ancestor calls (selftest CAS
- S107 §5 (Where I was wrong) — already-filed: The 'record precedes the act' / recording-after-work mechanism is the ORDERING class: ERR-060 (executed signed slots before creating the session file; Phase 6 violated), 
- S107 §3 (Work delta, item 1) — not-a-defect: The negative control (the critic) worked as designed: the four refuted numbers never shipped (the scan's output was kept off-repo by design, 'a scan's output must not be 
- S109 §12 (the ontology layer) — not-a-defect: This is a design/ontology synthesis (docs/46), not a failure mechanism in its own right — each of the ten addresses is filed or dispositioned individually elsewhere. It n
- S107 §5 (Where I was wrong) — not-a-defect: A judgment/scope lesson, already recorded in cookie-tally (#77). Not a claim-outran-probe method error nor a system mechanism; none of the docs/58 families holds it, and 
- S110 §8-1 (Codex lane, Close findings) — already-filed: Already SYM-055 (SYMPTOM-INDEX line 74: 'A protocol's digest function can be swapped from sha256 to md5... the 83-case selftest passes 83/83'). This §8 finding is the cro
- S110 §8-2 / §27 (Codex + Fable lanes) — already-filed: Already ERR-2026-08-27-014 (PREDICATE-COLLAPSE, ERROR-BIN line 173) and SYM-056 (the array-imbalance mechanism). Both the method error and the system mechanism have rows.
- S110 §10 (Codex lane, Known Failures / Open) — already-filed: The DEBT-undercount half is already OPEN-TASKS J15 ('THE DEBT GATE UNDER-COUNTS BY FORMATTING', now STRUCK/FIXED 2026-09-03); the REGISTER-counts-ADDED half was repaired 
- S110 §10 (Codex lane, Known Failures / residue) — not-a-defect: These are the §10 items that make the planted decoy false: UNREAD readings and owed-record debt are not defects and are not SYM rows (see decoy_result).
- S111 §10 / §15 (Codex lane, Known Failures) — already-filed: All five already have SYM rows (SYM-057 S110; SYM-058/059/060/061 S111), plus SYM-062/063/064 for the resolve-authority, memory-cause, and credential-hang findings. S111 
- S111 §10 (Codex lane, Known Failures) — not-a-defect: §6 'Deliberate Divergences' and §8 Decision Ledger record these as signed design choices, not failures; the debt counts are measurements. No mechanism to file.
- S111 §10-6 (Codex lane, Known Failures) — not-a-defect: A register-hygiene lag, not a system defect; corrected 2026-09-04 (S114 verifier flipped SYM-032 col 5 to FIXED per '3446f7c is an ancestor of HEAD'). No standing row nee
- S112 §10-1 (Fable close, Known Failures) — already-filed: The closeout itself files it as SYM-065; repaired S113 (`583f752`) by comparing the resolver to a live rev-parse instead of the frozen anchor.
- S112 §8-4 (Codex lane, VW-E2-R2 build) — not-a-defect: Caught by the build's own Circle before the artifact ran or reached anyone; the correction history lives inside the packet (append-only discipline working). ERR-021 alrea
- S113 §5 (M6-R1 commission) — already-filed: Already SYM-066 (fidelity_audit.py:378 sorted(runs)[:25], S112) and SYM-070 (Bench state()/coverage() cap-as-population, S113, FIXED by this M6-R1 commit `57a5da6`).
- S113 §5 (M6-R1-F1, post-close) — already-filed: Already OPEN-TASKS J21 (STRUCK/DONE): 'M6-R1-F1: display cap applied to the manifest verdict... Codex... found a SECOND boundary loss (Bench.state() sliced retained runs 
- S113 §3 (Opening Incident) — already-filed: Recorded as relay entry MSG-CDX-0033 and enumerated in OPEN-TASKS J50 ('MSG-CDX-0033 is a digest mismatch on MSG-FAB-0055'; the 'claimed digests never matched the bytes i
- S121 §8-F3 — not-a-defect: A platform default correctly pre-empted by config; it never manifested, so it is not a live defect. Grep: 0 for 'ExecutionTimeLimit'/'72-hour'. Could be filed as a preven
- S121 §10 — not-a-defect: A documented operational fragility with a designed monitoring tell (the NR-13 ALIVE cadence), not an unhandled defect. Grep: no row. SYM-adjacent (S4, a silent failure wh
- S118 §8-F2 — already-filed: This IS SYM-076, filed by S118 itself (grep line 96), and the ligature half rides J44. Recorded for accounting; family S15 (the instrument manufactures the finding).
- S119 §8-F1 — already-filed: The S15 umbrella conclusion; its concrete instances are filed - escape = SYM-076, ligature/pdfium = the S119 8-F2 candidate above (fix J48), citation = J44 rung 3; the mo
- S119 §8-F8 — already-filed: The clean census and the exclusion rule (strip span anchors, IMG tokens and link targets before counting) live in J45's body. Not an ERR: S118 10 explicitly says the 145/
- S122 §8-F4 — already-filed: Codex's code; filed as J54 item 3 (delegated on MSG-FAB-0083, collected when Codex's next entry answers). Family S6 (a child/resource outlives the stop) - the port answer
- S118 §8-F5 — not-a-defect: A disclosed measurement circumstance, not a failure: the reference was gone before the lever moved and the record names its basis (the 09-05 measurement) and its first po

## §7 Work log (UTC)

- **03:3x** Rab: J56-J55 signed; Codex away. **03:33:42** `open.sh` clean (S124). Read: the J55/J56 rows, SIGNATURES.md's J50–J52 form, `.claude/settings.json` (two hooks: ERR-009 warn on Bash, coordination check on Stop), `warn_heredoc.py` (the payload pattern), docs/47's map and §3, docs/21 §5, SYMPTOM-INDEX and ERROR-BIN row forms, docs/58 §5, the S120 ground's MODE CHECK paragraph, S123's R3 return from the workflow journal (83 rows; 33 NOT FILED-like). A guide lane asked the hooks docs six questions (payload fields, subagent identity, deny contract, matcher regex, project settings scope, `CLAUDE_PROJECT_DIR`). This record written before any instrument.
- **03:43** guard v1 wired and live-proved on both tools (`6edd150`); **03:50** v2 allowlisted, lane tier, aliases (84/84, `ff324c8`); **03:53** v3 env targets, definers, core.worktree (103/103, `689b414`). Six sacrificial clones built and registered; the red team launched (`wf_bc906697-742`, six angles + a critic) and the J55 sweep (`wf_bf53b2a5-cf9`, five slices + a consolidator).
- **04:00** docs/47 §9 + `fleet-ground-template.md` (`f83867e` — **occupant Claude Opus 4.8 from here**); pushed.
- **04:1x** the red team returned: clones 1–5 survived by hash; **clone-6 destroyed** (F2); the critic's six classes. **04:16–04:26** v4 (`816c997`, 126/126): UNREAD targets fail closed, backslashes kept in path args, prefixes, raw-head expansion, `targets_named`, core.worktree.
- **04:19** the compound rebuild-and-probe call denied whole (F3b) — read as "closed"; **04:20** a re-test agent launched against a still-destroyed clone. **04:27 Rab:** `/model claude-fable-5-1` — *"see what happened. eyes on deck. Just you to ride it."* **04:28** state read: the swap, the void re-test, no second breach; **04:29** the agent stopped; **04:30** clones rebuilt (`rmtree` needs an `onerror` chmod on git's read-only objects); **04:31** the destroyer denied live as UNREAD, the sibling form denied twice (variable value, then literal path); clone-6's bytes survived.
- **04:3x** J55 proposers read from the journal (79 candidates, every decoy refuted); 39/39 quotes verified; classes chosen from the bin's own table; **04:34** the `$((` false deny (F3a) → v4.1 (129/129, `2aa9067`). **04:40** rows filed after a fetch (`4706c20`; the first attempt refused itself on a wrong-shaped in-sync test — HEAD was ahead, origin was not); census 109/109 · 77/77. **04:41** registers closed (`c19dca8`). **04:42** the consolidator's cross-check → three corrections (`157bcb6`). **04:4x** MSG-FAB-0085 posted (406 words); the closing beat; the roots file emptied and the trace removed.

## §19 Close values — `close.sh fe2ce4e`, 2026-09-10T04:46:28Z (verbatim)

```
    PIN              fe2ce4e
    DIFF             16 file(s) since fe2ce4e · dirty 0 = mine 0 · peer-owned 0 (none) · 1 untracked
    GLASS            clean since fe2ce4e (--enforce; bare runs exit 0 regardless — SYM-046)
    RUST             skipped — no windows-widget/ change since fe2ce4e
    CI               no run for b2667a7e yet (unpushed? ahead=5) — NOT a green
    LEVERS           no unlevered threshold constants added since fe2ce4e
    DOCTOR           lever lexical parity: figure-triage.txt names 8 · quoted refs 8 · MISSING 0
    DOCTOR-HEAD      UNREAD — closeout-headline probe re-run not yet mechanized
    CENSUS           selftest.sh: declared 11 = FIRED 11 = banner 11/11 (exit 0)
    CENSUS-SCOPE     UNREAD — broader TEST-STRATEGY fixture census not yet mechanized
    REGISTER         OPEN-TASKS.md written this session: +7 / -2 lines
                     STRUCK 2 · ADDED 5 — net +3, GOVERNANCE GREW. §I: justify it or work one down
    MEMORY           clean — soft clock is DURABLE at 1ee3f19
    DEBT             open SYM 32 -> 41 (+9) — this session
                     ADDED net debt. §A21: justify a new governance artifact or work one down.
    PUSH             ahead of upstream: 5 (the close pushes; 0 after)
════════ close exit 0 — values only; the judgment half is SKILL.md ════════
```

- **REGISTER, justified:** the five J rows are J55's own product — Rab signed "the closeout findings without a row … files each as a row in its family or records why not", and five of the findings are open work, not symptoms (J57/J58 are his to decide; J59–J61 are mechanical). Two struck (J55, J56).
- **DEBT, justified:** +9 open SYM is the sweep counting mechanisms that were open before and uncounted (twelve OPEN rows among the 24, three of them `by construction` and one `by declared default`, minus SYM-085 → fixed and the rows that closed). The number did not get worse; it got measured. The families doc's S16 went from "no symptom row yet" to five rows.
- **CI:** the row commit and this one go up in the next push; observed after it (§20). **MEMORY:** durable at the S123 lockstep; the S124 lockstep follows the ledger row (§20). The one untracked item is `.codex/` (Codex's).
