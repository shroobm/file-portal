# S127 — Desktop — 2026-09-10 — auto-logon (J57's other half) is Rab's hand; Electron parked

*Record before the act (docs/28; muster Phase 6). §1 is Rab's words and is left unedited for the rest of the session.*

## §1 Session Intent (Rab's words, verbatim, left unedited)

> auto-logon to finish J57, keep electron on the backburner for now.

**Standing:** *"pace yourself … do not repeat a mistake that is in the errors you know of"* (S123); *"eyes on deck. Just you to ride it"* (S124); *"stop giving stuff to codex for now"* (S125).

## §2 Starting State — `open.sh`, 2026-09-10T06:2xZ (verbatim)

```
════════ MUSTER · OPEN · 2026-09-10T06:28:34Z ════════
[0] IDENTITY
    ledger newest    S126  ca91338
    this session     S127  ·  desktop  ·  2026-09-10
    advisory         S127 unnamed in the inherited tree
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-10T06:28:35Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 85 (tally == hook)
[3a] LEDGER ORDER  ✓ 109 Desktop-lane rows ascend (S16 → S126)
[3b] LEDGER PARSE  ✓ 109/135 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ ca91338 (S126) == ledger ca91338, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 8 · anchor 29 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    events           311 line(s)
    open-tasks       138 item(s) · last written 22 minutes ago
    error-bin        79 row(s) — read the CLASS table before you probe · last written 15 minutes ago
    symptoms         110 row(s), 41 open · last written 15 minutes ago
    relay            167 entries · run `gate.py status` for the board
    widget           25188
    widget autostart task 'File Portal widget (J57)' Ready — AtLogOn (J57; the unattended proof is the next logon)
    python procs     4
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 70c60e6
    thinkpad         converter=active · vault tip 50896d1 · staging 0 · receipts 14
[3] PIN
    --since          ca91338
    closeout         sessions/S127-desktop-2026-09-10.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

Inherited and `Historical` until re-measured: S126 §18 (J58 `Intended` until the next real bless; nothing to Codex). Rows that intersect today: SYM-110 (the guard's false denies — a fifth shape fired this session), ERR-063/078 (ORDERING — and it happened again, §7), docs/19 §0.3 (adoption and system configuration are Rab's hand), docs/19 §0.8 (the seat may be his brother's).

## §3 The pin

`--since ca91338` (the S126 closing commit; `6cffdd4` row and `52a1370` §20 are on origin).

## §4 What the two words are read to mean — and the one thing I will not do

1. **"auto-logon to finish J57."** J57's task launches the widget at logon; a power cut still ends at the logon screen. Auto-logon (Windows signing Bndit in at boot without a hand) closes that half. **It is a security setting and it stores the account password** (the netplwiz checkbox and Sysinternals Autologon both write the password as an LSA secret; the raw registry route writes it in cleartext). The safety rules I run under forbid me from modifying system or security settings and from entering a password anywhere, even when asked — and docs/19 §0.3(c) already puts system configuration in Rab's elevated shell. So the deliverable is: the machine's current state **read**, the procedure that fits it **verified against that state**, the caveats named, a card row that reads the result every open, and a J row with a done-when. Rab sets it.
2. **"keep electron on the backburner for now."** docs/59 §7 stands as the assessment; the decision is parked by his word — recorded in OPEN-TASKS §G ("what is NOT open, so nobody re-opens it") with the re-open conditions, and in SIGNATURES.

## §9 The plan

1. Read the machine (read-only, PowerShell): Winlogon's `AutoAdminLogon`/`DefaultUserName`/whether a cleartext `DefaultPassword` exists (never its value), the PasswordLess/Hello device setting (hides the netplwiz checkbox), the account type, the last sign-in provider (PIN vs password — auto-logon needs the password), the OS build, BitLocker (may be UNREAD without admin), whether Sysinternals Autologon is on the machine, the J57 task.
2. The card: the J57 probe also reads `AutoAdminLogon` — ON (user) / OFF (the hand that remains — J62) / UNREAD — with a muster selftest case for each reading.
3. Registers: **J62** (auto-logon — Rab's hand; done-when: the card reads ON and a reboot brings the widget up unattended, the boot log showing `watcher_start` with no click), the §G Electron entry, SIGNATURES, SYM-110's fifth shape.
4. **J63 — the record-first tripwire** (ERR-063's class fired for the third time this session, §7): a `PreToolUse` hook that denies an Edit/Write to a tracked repo file while the current session's `sessions/S<N>-*.md` does not exist. Built after this record, with its selftest.
5. Close in lockstep.

## §8 Findings

- **F1 — Auto-logon is Rab's hand, and the machine's state is read, not assumed.** `AutoAdminLogon` unset; no cleartext `DefaultPassword`; `DevicePasswordLessBuildVersion = 2` (the netplwiz checkbox is HIDDEN until it is 0 — the step most write-ups omit); the account is LOCAL; the last sign-in provider is the **password**, not a PIN (auto-logon needs the password — no trip there); Windows 10 Pro 19045; BitLocker `UNREAD` (access denied without admin); Sysinternals Autologon not present. Two routes fit that state (the tool, or PasswordLess=0 then netplwiz); both store an LSA secret, neither is mine to run. J62 carries the procedure and the done-when.
- **F2 — The card reads the logon half beside the click half.** The same probe now prints `auto-logon ON (user) / OFF (the logon is still a hand — J62) / UNREAD`; three cases (CASE 46). `Observed` today: OFF. When Rab sets it, the next open says so without anyone remembering to check — and if he ever changes the password, the row will still say ON while the logon silently fails: the row reads the registry flag, not a successful boot. The done-when needs the reboot.
- **F3 — The guard's fifth false-deny shape, and a real bypass found by fixing it.** A PowerShell `switch` whose case labels are quoted GUID strings: a quoted-string head is an expression there, but the `{` inside it tripped the bash brace-expansion rule. v4.5's negative control then showed that bash `{git,} reset --hard` had been splitting into `git,` and PASSING — a hole nobody had exploited, closed in the same version (braces split only at word boundaries). SYM-110 holds all five shapes; 144 → 149 cases.
- **F4 — J63 built, and it caught its author first.** The ordering lapse fired for the third session running (ERR-063 → 078 → 080), so the rule left memory and entered the harness: the card publishes `session.current`; `guard_record.py` refuses a tracked-file write and `guard_git.py` a `git commit` while the session's record is absent. The moment settings.json was written the hook was live — and my next edit to a tracked file was refused because no card had written the marker yet. Live-proved through the harness a second time on purpose: marker → S999, a Write to `docs/59-electron-assessment/PROBE-J63.txt` refused with the message naming the missing record, marker restored, no file written (`Observed`).
- **F5 — A latent hazard for the peer lane, fixed before it fired.** Both hook commands in settings.json named the Windows uv interpreter by absolute path with an exit-2 fallback: on the ThinkPad lane's Linux checkout every Bash call would have been DENIED by the interpreter-failure clause. Now `[ -x "$PY" ] || PY=python3`. Not observed on the ThinkPad (its lane has not opened a session since S123); `Intended` there.
- **F6 — An f-string ate a brace.** The record script's `{git,}` inside an f-string raised `NameError: git` — ERR-009's family (a script dying on its own delimiters), caught by the script itself; `{{git,}}`. No claim reached anyone; named here, not binned.
- **F7 — "Just you" held.** No fleet, no delegation, nothing to Codex.

## §10 Known failures / residue

- **J57 stays `Intended` until Rab sets auto-logon AND a reboot brings the widget up unattended** (the boot log shows `watcher_start` with no click). The card will read ON before the reboot proves anything.
- **BitLocker is UNREAD** from this session (non-admin). If C: is not encrypted, auto-logon means anyone who powers the machine gets the desktop — named in J62, Rab's to weigh (his brother's seat, docs/19 §0.8).
- **J63's gap:** a session that never runs `open.sh` and writes only through scripts (Bash `sed -i`, Python) is caught at its `git commit`, not at the write; a session that only reads trips nothing (correct). The marker is per machine and untracked; the ThinkPad lane's first open under this settings.json is the first exercise of the hooks on Linux (`Intended`).
- **SYM-110's class stays open by construction** (five shapes); a sixth gets a case first.
- Nothing was written to Codex. `.codex/` remains the one untracked item.

## §15 Registers

- **OPEN-TASKS:** J62 (auto-logon — Rab's hand, the procedure, the done-when); J63 filed and struck DONE (the record-first tripwire); §G: Electron PARKED by Rab's word (docs/59 §7 the re-open path).
- **SYMPTOM-INDEX:** SYM-110 fifth shape + the brace bypass. **ERROR-BIN:** ERR-2026-09-10-080 ORDERING (the third instance). Census 80/80 · 110/110.
- **SIGNATURES.md:** the block for both words (auto-logon's why-not-mine, the read state, the procedure; Electron parked). **docs/28:** dated note (J63).
- **Card:** `open.sh` writes `coordination/private/session.current` and reads the auto-logon half; `selftest.sh` CASES 46–47 (86/86).
- **Hooks:** `guard_git.py` v4.5 + the commit check (149/149); **`guard_record.py`** (new, 11/11); `settings.json` wires it and falls back to `python3`.

## §18 Next entry point

1. **Open:** `open.sh` — it now writes the session marker; the guards refuse tracked-file writes and commits until `sessions/S<N>-*.md` exists. **Write the record first; the harness will not let you forget.** The `widget autostart` row reads `auto-logon ON/OFF`.
2. **Rab's:** J62 (auto-logon — his elevated shell; then a reboot to prove J57); the Electron re-open conditions are docs/59 §7 and his word only.
3. **When the ThinkPad lane next opens:** its first Bash call exercises `settings.json`'s `python3` fallback; if the guard misfires there, a case first (SYM-110's rule).
4. **Standing:** nothing to Codex until lifted; `--census` at every close; run a gate as `ci.yml` spells it; an expected deny gets its own tool call.

## §7 Work log (UTC)

- **06:2x** Rab's line. `open.sh` clean (S127). The read-only probes: `AutoAdminLogon` empty, no `DefaultUserName`, no cleartext `DefaultPassword`; `DevicePasswordLessBuildVersion = 2` (the netplwiz checkbox is hidden); account `Bndit` is LOCAL; last sign-in provider **password** (not a PIN); Windows 10 Pro 19045; BitLocker `UNREAD` (access denied, non-admin); Sysinternals Autologon not on the machine; the J57 task Ready.
- **06:2x** The guard denied the sign-in-provider probe (a PowerShell `switch` whose case labels are quoted GUIDs — the fifth shape of SYM-110); v4.5 (`cb964a6`, 144/144). Its own negative control found a real bypass in the process: bash `{git,} reset --hard` had split into `git,` and passed — closed in the same fix (braces split only at word boundaries).
- **06:3x** The card's `widget autostart` row now reads the auto-logon half (`ca3768a`; CASE 46, 84/84): **OFF today.**
- **06:3x THE ORDERING LAPSE, third instance (ERR-063 → ERR-078 → now):** the guard fix and the card row preceded this record. Binned at close; the J63 hook is the answer that is not a resolution.
- **06:4x** The S127 record (`040fdef`, late — F4's cause). Registers: J62, §G Electron, SIGNATURES, SYM-110's fifth (`d6d460e`). **J63 built:** the marker in `open.sh`, the commit check in `guard_git.py`, `guard_record.py` + selftest, settings.json (portable), muster CASES 46–47 — the hook refused my next edit until `open.sh` had written the marker (06:42:49Z); suites 149/149 · 11/11 · 86/86; build `3f8dff6`, registers `f7a3801`. **06:55** live probe through the harness: marker → S999, the Write refused, marker restored by the card, no file written.
