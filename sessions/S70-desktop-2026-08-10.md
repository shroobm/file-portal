# S70 — Desktop lane — 2026-08-10

**Closeout under `docs/21-session-closeout-contract.md`.** Tier: **EXTENDED** — two shared docs
shipped, one carrying interactive JS. Omitted sections are marked with why.

---

## §1 Session Intent

`Observed` — pre-declared in `ae6ab36`, from Rab's MUSTER-keyed commission: commit the transcribe
showcase as docs/23, then investigate his chain — "these models can also run the processing of
the pdf's… a lever that changes the model chosen… the lever determines capability and level of
output" — and render the investigation as an interactive diagram: a real PDF node, the
"do you want to use x y z" card, routing on click, events and rates simulated from the timeline's
own errors. Left unedited; delivered as specified.

## §2 Starting State

`Observed` — MUSTER run out loud at open: memory loaded; HARD = S69/`40c68d5` ancestor-verified,
row `1bf6d9a`; SOFT = 57/3 mirrored — clocks equal. Repo clean + synced; vault clone content-clean
at `70c60e6` (stat-phantom only); ThinkPad reachable. `SYMPTOM-INDEX.md` read: SYM-003 the sole
OPEN row — and the commission's exact target.

## §3 Development Chain

`Observed` — `ae6ab36` open plan · `6696039` docs/23 lands (scratchpad → repo, status comment
updated) · `19a93e7` docs/24 (the investigation + simulator) · then CHANGELOG + this file in the
close, ledger row after.

## §4 Analogue + Boundary

`Observed` reasoning — the simulator's analogue is the widget's own pre-flight card (the "ask"
lever), generalized from analyst-choice to engine-choice. **Boundary:** docs/24 SIMULATES —
it replays recorded history and tagged predictions in compressed time; it drives no pipeline, and
its `[I]` columns are bands, not measurements. Do not read a simulated wall-time as a promise.

## §5 Observable Contract

`Intended` — Rab, each under a minute, failure first:

1. Both docs are open in your real browser now — **fails if** either tab is missing or undrawn.
2. docs/24: pick **Damodaran** — **fails if** no recommendation line renders under the three
   lever buttons.
3. Damodaran × **Marker** — **fails if** the run does NOT replay the S45 wedge and the power-cut
   → 16 s resume, or ends anywhere other than FAIL → held/.
4. Cybernetics × any lever — **fails if** any position ends better than flag-class (the 0.688
   ceiling is representation, and the doc's claim is that it is lever-immune).
5. The rates tables — **fails if** any granite-docling number appears without an `[I]` /
   uncalibrated marking.

## §6 Deliberate Divergences

- **The simulator is deterministic, not randomized.** Same patient + lever = same run, always.
- **`[I]` bands play their midpoints**, said on-screen in the log line that plays them.
- **No mermaid in docs/24** — the rail and chain are hand-rolled CSS; the simulator needed zero
  dependencies more than it needed prettier arrows.
- **No screenshots in evidence** — the Browser pane refused to composite (hidden-pane timeout);
  DOM probes carried the verification, per the S69 precedent.
- **Delivery = the real browser** (`Start-Process`, both docs) — the "inside the house" law,
  first session where it was standing policy from the start.

## §7 Implementation Delta

`Observed` — `git diff --stat 1bf6d9a..HEAD`:

| Path | What |
|---|---|
| `docs/23-transcribe-repair-showcase.html` | **new** — the S69 showcase, committed; header comment now records committed status; content unchanged (PROPOSAL, two unsigned rows) |
| `docs/24-engine-lever-simulator.html` | **new** — investigation (the chain, the rates, the conclusions table) + the simulator (5 real patients × 3 levers, scripted from timeline truth) |
| `CLAUDE_README.md` / `CHANGELOG.md` / `sessions/S70…` | plan→close blurb · S70 entry · this file |

## §8 Decision Ledger

1. **Deterministic simulation.** Rejected: randomized outcomes — rolling dice over real history
   would fictionalize the very record the doc exists to honor; uncertainty lives in labeled
   bands, not in RNG.
2. **granite-docling rendered as UNCALIBRATED bands everywhere.** Rejected: point estimates —
   the S69 research gives priors, not measurements; the doc's own conclusion is that calibration
   precedes trust (S28→S30 tradition).
3. **Analyst failures kept lever-orthogonal.** The claude-code script says out loud that engine
   choice could not have prevented its analyst loop. Rejected: implying the lever helps
   everything — one lever per authority, the projection law's cousin.
4. **The ceiling case included** (Cybernetics): a lever-immune failure, kept in to bound the
   idea honestly — the bless rail stays load-bearing under every position.
5. **docs/23 committed content-identical** (comment-only change). Rejected: editing the proposal
   while landing it — Rab blessed what he saw.

## §9 Evidence

`Observed` — HTTP-served probes (localhost:8321, the docs launch entry):

- Structure: 5 patient cards · ask-card appears on pick · 3 levers · recommendation renders.
- Damodaran × Marker: 11 log events; **wedge replay fired** ("froze 5 h 01 m") and **power-cut
  fired** with the 16 s resume line; rail lit through stations; outcome **FAIL**, landed held/.
- Cybernetics × docling: outcome **FLAG** with the lever-immune ceiling line present.
- Console: zero errors, both runs. Rates section: 3 tables present.
- Both docs launched via `Start-Process` (default browser) — delivery confirmed by tool result.

## §10 Known Failures

- **Browser-pane screenshots unavailable** (pane not displayed → no frame compositing; 5 s
  timeout). Also retroactively explains S69's black frames. Verification stands on DOM probes;
  recorded as tooling knowledge, no SYM row (no pipeline surface).
- Nothing shipped broken by the checks above.

## §11 Unproven by Machine

`Intended` — §5 on Rab's screen; and the two verdicts only he can issue: does the chain read as
he saw it, and does the engine lever earn a calibration run.

## §12 Uncertainty Register

`Unknown` — CI on the close push (docs-only; both jobs expected green; reported in-chat). The
real uncertainties are the `[I]` bands themselves — resolved only by the calibration run the doc
argues for.

## §13 Dependencies / Assumptions

`Inferred` — the simulator's `[M]` scripts trust the Change Ledger + docs/22 (stress-tested S69)
as accurate history; docs/22's claims were second-checked one session ago, which is as good as
documentary evidence gets here.

## §14 Regression Surface

`Inferred` — near zero: two new standalone docs, no shared file besides the brain/CHANGELOG.
The docs' JS runs only in a reader's browser.

## §15 Symptom Signatures

No new rows. SYM-003 remains the only OPEN row; docs/23 + docs/24 are its proposed answer,
awaiting Rab.

## §16 Recovery / Return Log

*Omitted — no defect, human-found or otherwise.*

## §17 Current State

`Historical` as of the close: branch pushed through the row; both docs live in Rab's browser;
vault `70c60e6`/6 notes; held/ = 4; widget `F28C58A8` untouched. Unsigned queue now: Decision 5 ·
the four standing signatures · repair audit-credit · transcribe-gate thresholds · **the engine
lever itself** · Valentine zones 2–4 · Cybernetics-stale · Textor.

## §18 Next Entry Point

1. MUSTER; newest row **S70**, ancestor-verified; TIME-STATE agrees (57/3).
2. If Rab signs the transcribe build → docs/23 Part 6 is the build shape (a Bench slice:
   `/api/transcribe` + model pull + gates + accept UI).
3. If Rab wants the lever → **calibration precedes everything**: granite-docling over the vaulted
   corpus, replace docs/24's `[I]` bands with measurements, then thresholds, then a lever file.
4. Otherwise: docs/19 §3 build candidates stand (events rotation + `verified_from`; cross-day
   event-row date stamp).
