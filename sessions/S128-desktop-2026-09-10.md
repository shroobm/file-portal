# S128 — Desktop — 2026-09-10 — "Tried it. Nice." — J57 and J62 observed

*Record before the act (docs/28; muster Phase 6; J63's guards). §1 is Rab's words and is left unedited for the rest of the session.*

## §1 Session Intent (Rab's words, verbatim, left unedited)

> Tried it. Nice.

(Preceded, this sitting, by: *"Tell me exactly what you want me to do, unless its something you can do yourself for me. Tell me what this done is simple english"*, *"but thats fine, as long as they cant rc into it no?"*, *"Like only my tailscale mesh network should be able to connect to it"* — answered from read-only measurement, no session opened for them.)

**Standing:** *"pace yourself … do not repeat a mistake that is in the errors you know of"* (S123); *"eyes on deck. Just you to ride it"* (S124); *"stop giving stuff to codex for now"* (S125).

## §2 Starting State — `open.sh`, 2026-09-10T16:4xZ (verbatim)

```
════════ MUSTER · OPEN · 2026-09-10T16:46:13Z ════════
[0] IDENTITY
    ledger newest    S127  49fc55f
    this session     S128  ·  desktop  ·  2026-09-10
    session marker   coordination/private/session.current = S128 (guard_record / guard_git read it)
    advisory         S128 unnamed in the inherited tree
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-10T16:46:14Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 85 (tally == hook)
[3a] LEDGER ORDER  ✓ 110 Desktop-lane rows ascend (S16 → S127)
[3b] LEDGER PARSE  ✓ 110/136 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ 49fc55f (S127) == ledger 49fc55f, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 8 · anchor 29 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    events           311 line(s)
    open-tasks       139 item(s) · last written 10 hours ago
    error-bin        80 row(s) — read the CLASS table before you probe · last written 10 hours ago
    symptoms         110 row(s), 41 open · last written 10 hours ago
    relay            167 entries · run `gate.py status` for the board
    widget           9096
    widget autostart task 'File Portal widget (J57)' Running — AtLogOn · auto-logon ON (Bndit)
    python procs     2
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 70c60e6
    thinkpad         converter=active · vault tip 50896d1 · staging 0 · receipts 14
[3] PIN
    --since          49fc55f
    closeout         sessions/S128-desktop-2026-09-10.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

The card's own reading, before any probe of mine: the widget is a NEW process (9096; it was 25188 across S124–S127), the J57 task is **Running** (its instance is that widget), and **`auto-logon ON (Bndit)`** — the row built in S127 read the change Rab made without anyone remembering to check. Inherited and `Historical` until re-measured: S127 §18 (J62 is his; a reboot proves J57).

## §3 The pin

`--since 49fc55f` (the S127 closing commit; `cb92ea2` row and `47be229` §20 are on origin).

## §4 What "Tried it. Nice." is read to mean

Rab set auto-logon (J62, his hand) and rebooted. The commission is the one J62's done-when named: **observe the chain and record it** — the card reads ON, and a reboot brought the widget up with no click. Nothing else is asked; nothing else is done.

## §9 The plan

1. Read-only: boot time, logon session, the J57 task's last run, the widget's start time and its parent chain to the watcher, the auto-logon registry (never the password), the widget's boot log if it can be found.
2. If the chain is `Observed`: J62 struck DONE with the times; J57's row annotated OBSERVED; SIGNATURES (his word + the measurement); docs/13's note gets its `Observed` line; the memory block; close in lockstep.

## §8 Findings

- **F1 — The chain fired unattended, and every link was read from its own source, not from the word "Nice".** Boot 16:36:07Z (`Win32_OperatingSystem`); the console session `bndit` logged on at 16:36 (`query user`); the widget started 16:36:29Z as pid 9096 (`Get-Process`), the J57 task's LastRunTime 16:36:36Z with state Running (its instance IS that widget); the widget's own boot log at 16:36:37Z: `boot: all loops launched` → `watcher_status stopped` → `watcher_start running pid 14120`; the watcher's parent is 9096 (`Win32_Process`). Thirty-one seconds from power to converting, no click. J57 → `Observed`; J62 → DONE.
- **F2 — The card read the change before I did.** `widget autostart … · auto-logon ON (Bndit)` at open — the row built in S127 read Rab's change unprompted, which is what a reading on the card is for. `AutoAdminLogon=1`, `DefaultUserName=Bndit`, no cleartext `DefaultPassword`: he used a route that stores an LSA secret.
- **F3 — J63's guard held on its author again, and taught the S124 rule a second time.** My first attempt put the record script and the commit in ONE Bash call; at hook time the record did not exist, so `guard_git` refused the whole call — the record script never ran either. A gated command gets its own call (S124 F3); the record's creation and its commit are two calls. Nothing was lost; the refusal is the mechanism working.
- **F4 — The remote posture, measured, not assumed (this sitting, before "Tried it"):** from the ThinkPad on the same LAN (192.168.2.237), the Desktop's LAN address (192.168.2.42) refuses 22, 47989, 47990; its Tailscale address (100.108.102.101) answers all three. Remote Desktop is off (`fDenyTSConnections=1`, TermService stopped); `sshd` is keys-only (`PasswordAuthentication no`); Sunshine's ports listen on all interfaces but the Ethernet sits on the Public firewall profile; Ollama listens on localhost only. The firewall rules themselves are UNREAD to this session — only their effect was measured. Auto-logon changes who is at the console after a boot, not who can reach the machine.
- **F5 — The boot log lives in the pipeline root**, not under `AppData\Local\File Portal` (`main.rs:427`: `gpu_pipeline_dir/widget-boot.log` = `~/ml/library/widget-boot.log`, 409 KB, epoch-stamped). My first probe looked in the wrong place and said UNREAD — correctly — rather than "no boot log".

## §10 Known failures / residue

- **A later password change breaks auto-logon silently** while the card's row still reads ON (it reads the registry flag, not a successful boot). Named in J62; the reboot is the only proof.
- **BitLocker is still UNREAD** from this session; the physical-access trade-off stands as Rab weighed it ("thats fine").
- **The J57 task shows `Running` while the widget lives** (its instance is the widget process); closing the widget by hand ends the instance — expected, but a reader of the card should not mistake `Ready` for "not working".
- **The firewall rules are UNREAD** (access denied); the LAN-closed reading is the effect from one LAN host. If the Ethernet profile ever flips to Private, re-measure from the ThinkPad the same way.
- Nothing was written to Codex. `.codex/` remains the one untracked item.

## §15 Registers

- **OPEN-TASKS:** J62 struck DONE (Rab's hand; the chain observed with times); J57's struck row annotated OBSERVED 2026-09-10 16:36Z.
- **SIGNATURES.md:** the block for "Tried it. Nice." (the measurement; the remote posture; what keeps it true). **docs/13:** the Observed line under the J57 note.
- No SYM/ERR rows this session (census 80/80 · 110/110 unchanged). Nothing to Codex.

## §18 Next entry point

1. **Open:** `open.sh` — the `widget autostart` row should read `Running … auto-logon ON (Bndit)` while the widget lives; a `Ready` there after a boot with no widget process is the signal to look at the task's LastTaskResult.
2. **The J57 arc is closed.** What remains from S127's list: the Electron re-open conditions (docs/59 §7, his word only); the analyst road (J47 → J46 → J49 → J44 …); J58's `Intended` becomes `Observed` at Rab's next real bless (`journalctl --user -u file-portal-converter | grep EXPORT-BLESS-SEEN` over ssh).
3. **Standing:** nothing to Codex until lifted; the record before any tracked write (J63 enforces it — and the record's creation and its commit are two calls); `--census` at every close; run a gate as `ci.yml` spells it.

## §7 Work log (UTC)

- **16:4x** Rab: *"Tried it. Nice."* `open.sh` clean (S128): widget 9096, task Running, auto-logon ON (Bndit). This record written before any register moves — under J63's guards, which would have refused otherwise.
- **16:4x** Probes (read-only): boot/logon/task/widget/watcher times and the registry (F1, F2); the boot log found in the pipeline root (F5). **The guard refused my compound record+commit call** (F3) → the record written alone, then committed (`c25434b`). Registers (`a10fa9e`). No fleet, no delegation.

## §19 Close values — `close.sh 49fc55f`, 2026-09-10T16:49:38Z (verbatim)

```
    PIN              49fc55f
    DIFF             6 file(s) since 49fc55f · dirty 0 = mine 0 · peer-owned 0 (none) · 1 untracked
    GLASS            clean since 49fc55f (--enforce; bare runs exit 0 regardless — SYM-046)
    RUST             skipped — no windows-widget/ change since 49fc55f
    CI               no run for ca210cf4 yet (unpushed? ahead=3) — NOT a green
    LEVERS           no unlevered threshold constants added since 49fc55f
    DOCTOR           lever lexical parity: figure-triage.txt names 8 · quoted refs 8 · MISSING 0
    DOCTOR-HEAD      UNREAD — closeout-headline probe re-run not yet mechanized
    CENSUS           selftest.sh: declared 11 = FIRED 11 = banner 11/11 (exit 0)
    CENSUS-SCOPE     UNREAD — broader TEST-STRATEGY fixture census not yet mechanized
    REGISTER         OPEN-TASKS.md written this session: +2 / -2 lines
                     STRUCK 1 · ADDED 0 — net -1, the register moved DOWN
    MEMORY           clean — soft clock is DURABLE at 8b7c52f
    DEBT             open SYM 41, unchanged since 49fc55f
    PUSH             ahead of upstream: 3 (the close pushes; 0 after)
════════ close exit 0 — values only; the judgment half is SKILL.md ════════
```

- **REGISTER moved down** (J62 struck; the J57 row annotated in place). **DEBT unchanged.** The one untracked item is `.codex/`. **CI** after the push (§20); **MEMORY** at the S127 lockstep, the S128 one follows the row (§20). `--census` unchanged (no rows this session).
