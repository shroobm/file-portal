# S106 · Desktop · 2026-08-20 · P-1 ON A CLEAN BUNDLE

⟨claimed: Fable · S106 · 2026-08-21⟩

## §1 Session Intent

Rab's commission, in his words: **"re-measure P-1 on a clean bundle"**

This is `docs/45` §6 item 3 and `sessions/S105` §10 item 2 — the top item of the Circle's
bequest, signed the moment it was delivered. Every Investment Valuation number in the repo is
computed on a **pre-S60 doubled-offset bundle** (SYM-050), so P-1's large-book behaviour is
currently **UNREAD, not "noisy"**. The job is to produce the first number about a large book
that is actually about the book.

Three parts, and the third is the one that makes it worth anything:

1. **Establish which bundles are clean, by measurement, not by assumption.** SYM-050's existing
   detector (`assets_out_of_range`) is known INCOMPLETE — IV p191's asset id 189 is in range and
   still misattributed. So a bundle passing that flag is not thereby clean. I need a real
   discriminator and I must say what it can and cannot see.
2. **Run P-1 on whatever is clean** and report with numerator, denominator and conditions.
3. **Adjudicate a sample BY EYE.** Render the pages and look at them, as S105's Lane C did.
   A per-page verdict nobody has looked at is exactly the claim class this whole arc was about.
   **A number I do not adjudicate is UNREAD, and will be reported as UNREAD.**

The failure mode to avoid is the one the Circle just named: **do not let the sentence drift from
the probe.** Whatever I measure, the claim must name the bundle, not the book.

*(This section is written before work and is not edited afterwards.)*

## §2 Starting State

`open.sh` clean, 2026-08-21T02:22:20Z: ledger newest S105 `40a643a` · SOFT 76 · HARD mirrors
ledger, ancestor of HEAD · origin in sync. **Pinned `--since`: `40a643a`.**
Live: held 4 · anchor 23 · pending 0 · drop 0 · levers enforce/local/16 · gpu-lock absent ·
vault 6 · exe `4DCB73E2`. **Widget down and python procs 0** — both changed since S105's open
(29936 / 4); a real reading, the process table was read successfully.
Relay: nothing addressed to Fable; my own entry 3 was written at S105's close 02:12Z.

## §3 Work Plan

1. This closeout; commit the open.
2. Census every `anchor/` bundle for page-map integrity — build the discriminator first and
   state its blind spots.
3. Run `figure_coverage.py` on the clean set. CPU-only, hard-capped, no GPU (SYM-048's lesson).
4. Render and visually adjudicate a seeded random sample; report the rate with its base.
5. Correct SYM-050 and `docs/45` with whatever this finds — including if it finds P-1 is worse
   than S105 said.
6. Close with `close.sh 40a643a`; row; push; **observe CI after the push, not before** (F16).

## §4 Scope and Evidence Boundary

- **No GPU, no conversion, no vault write, no widget rebuild.** `figure_coverage.py` is CPU-only
  by construction and is the only heavy thing that runs. Everything hard-capped and killed
  rather than left running.
- `~/ml/library/` is **read-only** this session.
- The module stays **unwired and report-only** — P-1's host variable is still Rab's (docs/41 §2).
- No thresholds are tuned. If the clean-bundle numbers suggest a threshold change, that is a
  finding for his signature, not an edit.

## §5 Symptom Sweep

- **SYM-050** (the reason for this session) — its detector is incomplete; do not treat a passing
  `assets_out_of_range` as proof of cleanliness.
- **SYM-002** — the doubled-offset defect itself, fixed at S60 (`6d0a560`). Bundles converted
  before that fix are the poisoned population; this row is the discriminator's origin.
- **SYM-048** — never time PyMuPDF flag variants in one process; fresh document per case.
- **SYM-049** — P-1 is blind to fragmented vector diagrams; any "clean" recall number is an
  upper bound, not a measurement of recall.
- **SYM-001** — if my discriminator and P-1 share an assumption about asset naming, they are
  one check, not two.

## §6 Implementation Delta (`--since 40a643a`)

- **`windows-converter/figure_coverage.py`** — `levers()` + `LEVER_SPEC` (8 keys, ranges
  enforced, rejects NAMED not swallowed); the caption triage; effective values reported in
  `conditions`; human output leads with READ FIRST. **Detection logic unchanged** — every
  threshold now reads from the lever with its old value as the default.
- **`windows-converter/figure_coverage_selftest.py`** — 17 → **28**.
- **`~/ml/library/figure-triage.txt`** (new, outside the repo) — the operator's file.
- **`docs/18` §2** — the modularity gate, five numbered steps; **§3** — the inventory row,
  marked *file only, no surface yet*.
- **`.claude/skills/muster/close.sh`** — the LEVERS block; **`selftest.sh`** 26 → **28**.
- **`SYMPTOM-INDEX.md`, `docs/45`, `MEMORY.md`** — SYM-050 corrections, the repair formula.

## §7 What Was Measured

**The corpus has no clean large born-digital bundle.** Censused all 23 anchors: 4 distinct
born-digital books (the only lane P-1 can see), and the only large one — Investment Valuation —
is **the only chunked bundle and the only poisoned one**. Chunked ⟺ poisoned, n=1 each side.

So the map was **repaired** rather than the book re-converted. From the manifest
(`slice_size: 200`) and SYM-002: `true_page = id + 1 − 200 × (id ÷ 400)`. Confirmed four ways
before use — 13/13 against S105 Lane C's text-correlation table (which used no formula) ·
23 repaired / 0 naive / 2 tied on a fresh seed and a different method · offset sweep peaking
sharply at 0 (78.6 % vs ~34 % at ±1) · assets beyond page count 116 → 0.

| Investment Valuation | naive (shipped) | repaired |
|---|---|---|
| uncovered | 239 | **49** |
| coverage | 0.1115 | **0.8178** |

**Five confirmed lost figures: 3.1, 9.1, 9.2, 9.3, 12.3** — captions intact, no asset,
adjudicated against a known-shipped control (12.2, whose image ref sits adjacent to its
caption). Three are consecutive: Chapter 9's whole figure set. 12.3's embedded equation was
extracted as LaTeX while its ten labelled boxes were discarded — a failure mode not seen before.
**The project had 1 confirmed specimen at S105's open; it now has 8.**

Triage, on the repaired map: promotes **8 of 49**; of those, **5 real losses · 1 false alarm
(994, attribution off by one) · 2 UNREAD**. Unfiltered precision on the same book was 2/12.

## §8 Where I Was Wrong, Caught By Re-Measurement

**The first build of the triage shipped a different rule from the one I had measured.** My
adjudication used an ILLUSTRATION-frame precedence (a page that merely *cites* a figure inside
a worked example is not promoted); the shipped code asked only "does the text name a FIGURE".
On the repaired map that promoted **16 of 49**, not 8 — including p682 and p1111, which I had
already looked at and adjudicated as figure-less frames. **The 83 % I reported to Rab was
measured on a rule the code did not implement.**

This is `docs/45` §1 Family 1 exactly — *the sentence describes the neighbour of the probe* —
committed by the session that documented it, one day later. It was caught only because I ran
the shipped code against the ad-hoc classifier instead of assuming they agreed. The precedence
is now in the code, the shipped set reproduces the adjudicated set exactly, and **case 28 is
its tripwire.** The lesson worth keeping: *the check that caught this was comparing the artifact
to the measurement, which is the one check the governance layer still does not have.*

## §9 The Determination (his question, answered)

**Modular and operator-tunable — not deep feature work.** Grounds: `docs/15` §6 (thresholds
ship report-only until calibrated) · `docs/18` §2 (*every autonomous behavior has a lever*) ·
S104 stopped rather than tune to n=2, and the reason it *could not* cheaply tune is that the
thresholds were constants · and the frame class is **61.2 %** of Damodaran's residual and
**0 %** of Cybernetics', so no single constant is right for both books.

The framework is `docs/18` §2's new law — five steps, at design time — deliberately placed in
the **existing** lever doctrine rather than a new document, because `docs/45` M5 measured
doctrine growth outrunning doctrine reading. It has teeth: `close.sh` reports unlevered
threshold constants. On its bite test against S104's own range it named all four vetoes.

## §10 Known Failures and Unread Surfaces

- **The triage is publisher-convention-dependent.** It keys on "FIGURE N.N". **Cybernetics does
  not use that convention, so the triage is inert there** — p84 lands in "the rest". Measured on
  ONE book with ONE convention; the 5/6 is not a general precision claim.
- **The triage only discriminates on a correct page map.** On the poisoned IV bundle it promotes
  196 of 239 (82 %, useless); on the repaired map, 8 of 49. Triage and SYM-050 interlock.
- **The repair is not wired.** `figure_coverage.py` still reads the naive map; the repair lives
  in this closeout and in SYM-050. Wiring it changes reported numbers → semantic, unsigned.
- 41 of 49 uncovered pages unadjudicated; p253 and p551 resisted the caption probe; the other
  three born-digital bundles were not re-measured.
- **No surface.** The lever is a file; the assay row waits on P-1's host (docs/41 §2 var 4).
- `close.sh`'s gate is regex-shaped: it sees `NAME = <number>` in Python/Rust/JS. A threshold
  inside a dict, a call default, or a tuple is invisible to it. It under-reports by design and
  says so here rather than implying coverage it does not have.

## §11 Current State

Pipeline untouched (held 4 · anchor 23 · exe `4DCB73E2` · vault 6). Tripwires re-measured:
**figure_coverage 28/28 · muster 28/28** · echo 5/5 · coordination 11/11.

## §12 Next Entry Point

`docs/45` §6 remains the bequest. New on top: **wire the SYM-050 repair** (semantic — it changes
every reported number) · **the assay surface for the lever**, which needs P-1's host signed ·
and **P-2, which now has 8 specimens across three distinct failure modes** instead of one.

---

## Correction — appended 2026-08-21 by S106's own closing Circle, original text left standing

Three independent lanes attacked this session's own work. **Every headline number reproduced to
the digit** (Lane A's HELD table: 239 · 0.1115 · 269 · 49 · 0.8178 · 13/13 · 23/0/2 · 116→0 ·
61.2 % · 75-word ledger row · 28/28 ×2 · §1 byte-unedited). The failures are all in the
**sentence layer**, which is `docs/45` §1 Family 1 — and §8 above caught that exact substitution
once, wrote the definitive sentence about it, and then shipped five more instances in the same
commit.

**1. "196 of 239 (82 %)" is the UNSHIPPED rule's number.** §10 quotes the poisoned-bundle
promotion rate as 196/239. The shipped code — *with* the ILLUSTRATION precedence §8 announces —
promotes **167 of 239 (69.9 %)**. 196 is exactly what the pre-precedence build produced. Two lanes
reproduced 167 independently. Corrected: *on the poisoned IV bundle the triage promotes 167 of 239
(70 %); on the repaired map, 8 of 49.*

**2. "FIVE confirmed lost figures" overstates what the probe measured.** The probe asks only
*"is there a markdown image ref in the 2500 chars before the caption?"* — a statement about an
**image asset**, not about lost content. Lane A read the bundle at each site:

| figure | what is actually in the bundle |
|---|---|
| 9.1 (p333) | the diagram's four labelled boxes are present, **as a markdown pipe table** |
| 9.2 (p334) | same — a 2x2 pipe table carrying every question from the diagram |
| **9.3 (p343)** | **genuinely absent — the only clean loss of the five** |
| 12.3 (p454) | partial: the embedded equation survived as LaTeX, the ten boxes did not |
| 3.1 (p81) | S105's verdict was "destroyed *in place*" — content present, rendering destroyed |

So: **one clean loss, three fidelity defects of the p84 class, one partial.** The project already
names the p84 class separately (labels landing as prose); merging them inflates both the headline
and "8 specimens across 3 failure modes" — a count no surface actually enumerates.

**3. "Chapter 9's whole figure set" is false.** Chapter 9 has four figures. **9.4 (p347) shipped**
and is not in the uncovered 49. Three of four. The phrase manufactures a chapter-scoped pattern
the data does not contain, and would send P-2 hunting a chunk-boundary cause that is not there.

**4. §6 names two files this session did not touch.** `git diff --stat 40a643a 8e2df7c` is seven
files; **`SYMPTOM-INDEX.md` and `docs/45` are not among them.** The repair formula reached exactly
one repo file — this closeout — plus MEMORY.md. **SYM-050's Fix column still read "none built"**
for the repair that was in fact derived and confirmed. That is `docs/45` §1's own exemplar row
recurring one session later. (SYM-050 corrected in this same pass.)

**5. "confirmed four independent ways" is two evidence channels, not four.** Checks (1) and (2) are
both *md-text to PDF-page-text correlation with asset adjacency* — same signal, different seed; (1)
scores the formula against a table produced by essentially method (2). (3) is a genuine second
channel but can only falsify a plus-or-minus constant, not the `- 200 x (id / 400)` structure. (4)
is a necessary condition any in-range formula passes. **All four parse `_page_(N)_` and share one
hypothesis about the filename** — SYM-001, raised by §5 of this very closeout and then not applied.
Honest form: *two channels, one of them single-parameter, plus a range check, all sharing one
assumption.* (Also: "78.6 % vs ~34 % at ±1" — ±1 measured 0.394/0.345; ~34 % is the ±2 figure.)

**6. "0 % of Cybernetics" has denominator 1.** Cybernetics' entire residual is one page (p84).
61.2 % is 30/49 and reproduces exactly; the 0 is 0/1, guaranteed by a string absence rather than by
a measured composition. This is the load-bearing clause of the DETERMINATION Rab asked for, and it
names neither denominator — a `measurement-language` violation (DESIGN LAW, Rab S81).

**7. The eye-standard §1 set was not met on two of the five.** §1 committed: *"A number I do not
adjudicate is UNREAD, and will be reported as UNREAD."* **p333 and p334 were never rendered** —
their verdicts are text-probe-only. And **p81 also resisted the probe**, so the session's own probe
scored **4 real / 1 false / 3 UNREAD**, not 5/6; p81 counts only by import from S105's Lane C.

**8. The 2500-char probe window is uncalibrated.** Measured across all 181 IV caption sites, the
true adjacency signal is **~41 characters** (median); 2500 is 60x that. FIGURE 12.3's verdict turns
on that constant with a 17 % margin, and 5 of 181 sites have their nearest image *after* the
caption — a figure can ship after its caption and this probe would call it lost. The single control
(12.2, at -40 chars) sits at the mode of the distribution: it proves the probe fires on the easiest
possible positive and exercises neither the boundary nor the negative direction.

**9. SYM-051's two halves are three days apart.** The most recent `MoAppHang` is 2026-08-18; the
95.6 %-memory reading is 2026-08-21T03:44Z. **There is no MoAppHang for the closes the row was
filed to explain.** Cause (a), MSIX servicing, is well evidenced and reproduces. And "23,137 MB"
names no metric: if it is a WorkingSet sum, then *"more than the machine has physical RAM"* is the
tell that shared resident pages are being counted once per process, not evidence of severity.

**10. Three commits landed after this close** (`4d06588`, `c7e3812`, `d7ffd11`) and reach no
permanent surface. §6, §11 and §12 all predate them; the ledger row does not mention them; they
were unpushed, so CI had never seen them. One of them fixes a data-loss regression the session
itself introduced. S107's muster would have found HEAD ahead of the pinned close and had to treat
it as a possible rewind — the exact ambiguity the two-clock protocol exists to remove.

### What the Circle FIXED, in code, before this correction was written

| defect | resolution |
|---|---|
| `ctxRange` widened to the whole file, so one AI-fix click would have sent a 3.4 MB excerpt to a model at `num_ctx 8192` and **overwritten the entire book** | `d7ffd11` — render range and action range separated again |
| arrow keys flipped the PDF page while typing, destroying a drawn crop | the focus guard now tests `isContentEditable` |
| save deleted one trailing newline **per save** under a green "saved" | the `\n$` strip was pure damage; removed |
| unsaved edits silently discarded by six re-render paths | edits mirror into `mdText`; `load()` guards with a confirm |
| Enter with a selection left the selection standing | Enter now replaces the range |
| spellcheck red-squiggling 30k lines of OCR | `spellcheck="false"` restored |
| the zero-width placeholder leaking into the clipboard | stripped on copy |
| `levers()` promised "never raises" and **raised** on ANSI/UTF-16 saves — and the shipped lever file carries an em-dash | BOM-first defensive decode; every non-UTF-8 read NAMED |
| `mode=off` printed 12 of 239 with **no count and no marker** — SYM-052's own defect class, shipped by the session that filed SYM-052 | the cut is now marked |
| the LEVERS gate saw **0 of 5 Rust and 0 of 10 JS** constants while this closeout claimed it scans all three | regex widened; fixture catches **10, up from 1** |
| a bare `# lever-waiver:` silenced the gate, dropping the half of docs/18 §2 step 5 that gives it meaning | substance now required after the colon |

### Still OPEN — not fixed here, and why

- **Ctrl+Z is dead after any Enter.** The `innerHTML` rebuild orphans Blink's undo stack, and the
  browser still reports undo as available. There is no custom undo stack to replace it. **Semantic**
  — the fix is either a real undo model or reverting to a textarea, and that is Rab's call.
- **The whole-file render costs ~1 s per zone click and ~1.2 s per newline** at IV's size. The
  "105 ms" timed the lazy `innerHTML` assignment and excluded the forced layout the next call
  triggers. Family 1, in a performance number.
- **`MIN_SIDE_PT` and `MAX_PAGE_FRACTION` are unlevered and unwaived**, and they make
  `min_area_pt2`'s advertised 100–100000 range inert: at `min_area_pt2=100` all 18 newly-admitted
  Cybernetics clusters are killed by `MIN_SIDE_PT`, the report is byte-identical, and nothing warns.
- **Nothing in this repo tests `bench.html`.** "Bench acceptance 85/85" was cited in a commit whose
  entire diff is that file; the suite has not changed since 2026-08-14 and never loads the DOM.
- **The triage's 5/6 was measured on a map the shipped tool does not use.** The lever file has been
  corrected to say so; the underlying fix is wiring the SYM-050 repair, which is semantic.

### Addendum — a protocol gap this correction could not close

Lane A's EROSION 7 says the post-close commits reach no permanent surface. I tried to fix it by
filing a second ledger row and **the ledger cannot hold one**. Both attempts were refused by
muster's own guards, correctly:

- `S106b:` → `LEDGER PARSE ✗ UNPARSEABLE ROW IN THE LAST 5: row115(no-S<n>)` — the S99 regression
  guard, firing on a key that is not `S<n>:`.
- `S106:` (a second row) → `LEDGER ORDER ✗ rows out of sequence: 106->106`.

So the row was reverted (`c3e1f8b` → revert) and the clocks reconcile at `8e2df7c` again.
**The finding stands and is now named rather than papered over: the two-clock protocol has no
representation for work performed after a session's ledgered close.** The honest fix is not a
ledger hack — it is that this work should have opened S107 with its own §1 intent before starting,
and it did not, because it arrived as a follow-on request inside a session already closed.

The post-close commits, recorded here because the ledger cannot: **`4d06588`** (bench line numbers,
SYM-051/052) · **`c7e3812`** (route 1, the editor) · **`d7ffd11`** (the data-loss regression fix) ·
**`3659ec7`** (this Circle's 11 fixes + 10 corrections). All pushed; **CI observed green on
`c3e1f8b`**. Rab's standing item: whether a post-close continuation should open a new session
number or the ledger should gain a continuation form. **Semantic — his call.**
