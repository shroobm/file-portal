# S89 · Desktop · 2026-08-17 · THE THINKING ATE THE ANSWER

## §1 Session Intent

Rab's commission: his standing S88 posture (*"just build buddy, I sign… if something trips…
search for your exact answer"*), applied to the trip he reported with **"Was this your
doing?"** — a third withhold on the engineer question, now WITH its evidence (the S88 fold's
first live firing).

Scope: fix the diagnosed cause — `ask()`'s `max_tokens=700` is a COMBINED thinking+answer
budget for qwen3 under `--jinja`, so a long-thinking draw truncates the answer mid-sentence
before its citations land, and the guard then rightly withholds the fragment. One measured
line; prove with a verdict-rate measurement through the product path.

**Written before the fix. Left unedited** (docs/21).

## §2 Starting State

`open.sh` exit 0: S89 · `--since` = **`e08c22a`** · clocks clean · origin in sync · widget up
with his loaded model (server vintage 01:36:49, S88 code — the fold and the withheld log both
fired live). Evidence, all `Observed` this hour: his withheld raw = **304 tokens, zero
`docs/` pointers, mid-sentence end** (log entry ts 01:40:01, `tokens=[]`); direct probe at
the llama API with ask()'s exact body: **`finish_reason: length`, `completion_tokens: 700`,
`reasoning_content` 2,163 chars vs `content` 1,021** — the thinking block and the answer share
the one budget. Prompt constant at 12,896 tok (12,895 cached), context 16,384 → completion
budget ≤ ~3,400 always fits (asks are stateless; no history growth).

## §4 In-scope symptom rows

- **SYM-034/024's family** (a request that behaves differently by invisible internal state) —
  the discriminating fields (`finish_reason`, `usage`, reasoning split) are exactly what
  `ask()` discards; the fix's comment names them for the next reader.
- **SYM-022/032** — measurement asks ride the resident model; nothing new touches the card.
