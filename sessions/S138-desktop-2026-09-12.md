# S138 — Desktop — 2026-09-12 — The Readability lane over the 21 AGENT SCRIPTS briefs (three Sonnet lanes with planted controls), then the fixes

*Record before the act (docs/28; J63's guards). Written at the open, before any tracked write.*

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

> Work on the next tickets and tell me all you've worked on since we started using desk

**Standing:** nothing to Codex until lifted; the record before any tracked write (its creation and its commit are two
calls); a probe in the shared checkout is a file run by its literal path; `row_check.sh <N>` and CI's own gates before
any push; `--census` at every close.

## §2 Starting State — `open.sh`, 2026-09-12T15:56:58Z (verbatim)

```
════════ MUSTER · OPEN · 2026-09-12T15:56:58Z ════════
[0] IDENTITY
    ledger newest    S137  1de0eb0
    this session     S138  ·  desktop  ·  2026-09-12
    session marker   coordination/private/session.current = S138 (guard_record / guard_git read it)
    advisory         S138 is named in the INHERITED tree (1de0eb0) — read each, do not blanket-correct:
                     sessions/S137-desktop-2026-09-12.md 
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-12T15:56:59Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 86 (tally == hook)
[3a] LEDGER ORDER  ✓ 120 Desktop-lane rows ascend (S16 → S137)
[3b] LEDGER PARSE  ✓ 120/146 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ 1de0eb0 (S137) == ledger 1de0eb0, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 9 · anchor 30 · pending 0 · drop 0
    levers           audit=report · analyst=local · batch=8
    gpu-lock         absent
    events           341 line(s)
    open-tasks       142 item(s) · last written 14 minutes ago
    error-bin        90 row(s) — read the CLASS table before you probe · last written 14 minutes ago
    symptoms         116 row(s), 43 open · last written 14 minutes ago
    relay            167 entries · run `gate.py status` for the board
    widget           13196
    widget autostart task 'File Portal widget (J57)' Ready — AtLogOn · auto-logon ON (Bndit)
    python procs     7
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 50896d1
    thinkpad         converter=active · vault tip 8d7cc85 · staging 0 · receipts 17
[3] PIN
    --since          1de0eb0
    closeout         sessions/S138-desktop-2026-09-12.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

## §3 The pin

`--since 1de0eb0` (the previous session's closing commit, from the card).

## §9 The plan

1. **The Readability lane over the AGENT SCRIPTS briefs (Rab's ranking after J64; S134's owed review):** 21 briefs
   measured now (`ls agent-scripts/*/*.brief.md | wc -l` — the "19" carried since S134 was stale). The question each
   brief must answer for a COLD agent: can it run the script from the brief alone — PURPOSE, WHEN, CALL by literal
   absolute path, INPUTS, OUTPUTS, EXIT CODES, NEVER, a real EXAMPLE with measured output, RELATED (POLICY law 2)?
2. **Shape (subagents-on-opus + docs/47):** three Sonnet lanes, read-only, seven briefs each, plus ONE planted control
   brief per lane (a scratch copy carrying a `$VAR` CALL, a typed count and no NEVER section) — a lane that does not
   flag its control has its findings downgraded to `Inferred`. Each lane returns per-brief findings with the LINE quoted,
   tagged, and its residue declared. I verify a sample of each lane's findings against the file before any edit.
3. **Fixes** through the brief files (private repo, `workshop`), each a one-line reason; typed numbers re-measured, never
   copied. No script changes unless a brief cannot be made true without one.
4. The record; the close with the three receipts; lockstep; §20; the Desk.

## §8 Findings

- **F1 — three lanes, one control, all three caught it** (`Observed`, the lanes' reports): each Sonnet lane received seven
  real briefs and the same planted control (`row_reader.brief.md`: a `$PY $LIB` CALL, no script, no NEVER, a typed
  "19 briefs … 9 cells", an exit code 3) at a different list position. A found four of the five planted defects, B four,
  C all five — and C measured the library (21) against the control's typed 19. No lane's findings needed downgrading.
- **F2 — nineteen findings on the real briefs, all accepted after my own read of the line and the code** (`Observed`,
  each anchor checked before the edit): 4 BLOCKS, 8 MISLEADS, 7 COSMETIC over 13 of 21 briefs. The one that matters for
  Rab's standing ask: `close_chain.brief.md`'s INPUTS named the six arguments and not `CLOSE_TRANSCRIPT` / `CLOSE_SINCE`
  — a cold agent following it would close WITHOUT the price receipt and the scripts-used record, and the chain would
  say `receipts: UNREAD` and push. Now in INPUTS with the consequence spelled out.
- **F3 — a lane's reading found what no checker can** (`Observed`): `Bench.__init__` runs `self.assets.mkdir(exist_ok=True)`,
  so two "read-only" readers (`bundle_holes`, `rescore_bundle`) can create a directory; both NEVER sections now say so.
  And `bundle_holes.py` exited 1 for two CONFIG failures — worse, the bench's own `SystemExit` fired first, so the
  reader's check was dead code. Both readers now catch the bench's refusal → `CONFIG: … exit 2` (tripwired on an empty
  dir: exit 2, `Observed`).
- **F4 — the mechanical half of law 2 exists now** (`Observed`): `auditing/brief_check.py` — nine headings in order, a
  script beside every brief and a brief beside every script, literal CALL paths, exit codes 0/1/2 (chains exempt),
  dated EXAMPLE; selftest 12/12 with three positive controls (bash, `/c/`, PowerShell `C:\`) and one case per planted
  defect. Its first run found: `lockstep.sh` really exited 3 (→ 1; POLICY §5), two false hits on the PowerShell briefs
  (its own bug — a doubled backslash in a raw-string regex; the selftest's positive control now carries that shape),
  and a script with no brief: `memory_timestate.py`. Over the planted control: five defects, exit 1.
- **F5 — the lockstep's date was a typed constant** (`Observed`, `memory_timestate.py` line `"· 2026-09-12 (S%s)."`): every
  lockstep from tomorrow on would have stamped the TIME-STATE with 2026-09-12. Today's UTC date now; usage → exit 2;
  its brief written. SYM-117 (fixed; S8). Also the "CI CI" doubling from S137 §20 is in its INPUTS.
- **F6 — my own typed number** (`Observed`): "19 briefs" was carried from S134 into S137 §18, the S137 memory block and
  the S137 Desk plan; the open's `ls … | wc -l` said 21. ERR-091 (STALE-GROUND).
- **F7 — the guard, twelfth shape** (`Observed`, 16:1xZ): `sed -n "$(grep -n … | cut -d: -f1),\$p" file` — the text after a
  `$(…)` substitution opens a new segment whose first token `,\$p"` reads as a `$p` head. Not retried; `tail -n +N`
  instead. Noted on SYM-110 (the `$p` family's first shape, back through a different door). A case is owed.
- **F8 — the cost of the lane** (`Observed`, the three notifications' usage lines): 97,948 + 116,367 + 116,920 subagent
  tokens; 23 + 23 + 30 tool uses; 5.7 / 6.8 / 8.2 minutes wall. The receipt prices them at Sonnet rates.

## §10 Known failures / residue

- **The lanes did not verify RELATED citations** (register ids, docs sections) — declared in all three residues; a
  pointer to a wrong row stays possible. Not a checker's job either.
- **`brief_check.py` reads shape, not truth**: INPUTS that disagree with argv, OUTPUTS naming a line the script never
  prints — today's 19 findings — are a lane's reading. The brief says so in WHEN.
- **`brief_check` in the chain is a reading, not a gate** (a private brief must not stop a public close); a red there
  is named in §19 and fixed in the same sitting or filed.
- **SYM-110's twelfth shape (F7) has no case yet** — J64's block is the template; owed to the next guard session.
- **The bench server (pid 15444/22604)** on the renamed held dir: Rab's process.
- The `.codex/` untracked dir on the card: not mine, left alone.

## §15 Registers

- **OPEN-TASKS:** no item struck, none added — said out loud: the Readability lane was S134's owed review, never a J
  item; the dated line is the register write. **SYMPTOM-INDEX:** SYM-117 filed (fixed; S8); SYM-110 annotated (F7).
  **ERROR-BIN:** ERR-091 (STALE-GROUND). **`error_families.py`:** SYM-117 → S8.
- **Private (`agent-scripts`, `workshop` 3496990 · b5afd68):** 13 briefs corrected; `bundle_holes.py`, `rescore_bundle.py`,
  `lockstep.sh`, `memory_timestate.py` (+ brief) fixed; NEW `auditing/brief_check.py` + brief + selftest; README rows;
  the chain's first line.

## §18 Next entry point

1. **Open:** `open.sh`; the card should read S139. The Desk inbox first. Record through `record_open.py`.
2. **Next by Rab's ranking:** J46 (the diff-whitelist acceptor — semantic; echo it back before building). Or Rab's own
   pick from the Desk. A cheap guard session is also on the table: SYM-110's twelfth shape as a case (F7).
3. **The library from now on:** `brief_check.py` before any commit that touches a brief or a script; a lane for
   meaning, the checker for shape; a number in a brief comes from a run named beside it.
4. **Standing:** nothing to Codex until lifted; the record before any tracked write (two calls); scripts in files by
   literal path; `run_gates.sh`, `row_check.sh` and the guard before any push; `--census` at every close; the three
   receipts at every close; a fleet is ≤5 on Sonnet unless Rab names another shape.

## §7 Work log (UTC)

- **15:57** `open.sh` clean (S138, pin 1de0eb0). This record. 21 briefs measured (the carried 19 corrected — F6).
- **15:58** the S138 tracker (pid 4008, `-Label S138-receipt`, covers the sitting). The control brief; `gen_lanes.py`
  (a `$CTRL` inline function denied first — the designed deny, mine); three Explore/Sonnet lanes launched 16:0xZ.
- **16:0x–16:1x** B, A, C returned; each finding's line and code read before the edit; `patch_briefs_s138.py` (every
  quoted output asserted present in the script's source first); the readers' `SystemExit` catch; `brief_check.py` +
  selftest + brief; `memory_timestate.py` (F5) + brief; README rows; private 3496990; the chain line b5afd68.
- **16:1x** the guard's twelfth shape (F7). The record; registers; census; close chain.
## §19 Close values — `close.sh 1de0eb0`, 2026-09-12T16:18:13Z (verbatim)

```
    PIN              1de0eb0
    DIFF             7 file(s) since 1de0eb0 · dirty 0 = mine 0 · peer-owned 0 (none) · 1 untracked
    GLASS            clean since 1de0eb0 (--enforce; bare runs exit 0 regardless — SYM-046)
    RUST             skipped — no windows-widget/ change since 1de0eb0
    CI               no run for 848268e9 yet (unpushed? ahead=2) — NOT a green
    LEVERS           no unlevered threshold constants added since 1de0eb0
    DOCTOR           lever lexical parity: figure-triage.txt names 8 · quoted refs 8 · MISSING 0
                     LEXICAL REF: mode · min_area_pt2 · vector_min_paths · cluster_gap_pt · text_coverage · words_per_line · table_overlap · accounted_for
                     UNREAD — a quoted lexical occurrence does NOT prove the consumer reads the lever; ARMED on MISSING only
    DOCTOR-HEAD      UNREAD — closeout-headline probe re-run not yet mechanized; NOT a statement the numbers reproduce
    CENSUS           selftest.sh: declared 11 = FIRED 11 = banner 11/11 (exit 0)
    CENSUS-SCOPE     UNREAD — broader TEST-STRATEGY fixture census not yet mechanized; NOT a statement all promised tripwires ran
    REGISTER         OPEN-TASKS.md written this session: +1 / -0 lines
                     STRUCK 0 · ADDED 0 — net 0 — nothing was worked down; say so in the closeout rather than imply it
    MEMORY           clean — soft clock is DURABLE at 09339b7
    DEBT             open SYM 43, unchanged since 1de0eb0
    PUSH             ahead of upstream: 2 (the close pushes; 0 after)
════════ close exit 0 — values only; the judgment half is SKILL.md ════════
```

- **DIFF:** every changed file is in the content commit `848268e` or this closing commit; the row follows, then
  `row_check.sh 138` on it, then the push.
- **Relay:** no entry — nothing to Codex (standing).


## §20 Post-close (UTC) — CI observed, memory in lockstep

- **CI on the row commit, `Observed`: RED.** e3021fb, run 34704816196 / num 263: rust success 16:22:44Z; python FAILURE
  16:19:18Z at step 15 *Governance acceptance (observability)*. The step's LOG (read through the stored credential by
  the new `auditing/ci_step_log.py`, never the API conclusion — SYM-075): `FAIL a stale signature is caught in the SIGNED
  (--since) mode` · `FAIL — 139/140 checks`. Cause, measured at the pushed HEAD: `_exercising_ref()` found no commit in
  the 40-deep window introducing an extractable key (`keyed = None`) — the close and row commits pushed the last one
  out — and the case returned False instead of skipping; the property holds against any diff (HEAD~1/2/5 all True).
  The chain's local gate had passed at the CONTENT commit two commits earlier. **SYM-118** (fixed, S8).
- **The chain said `chain exit 0` under that red** — the observe step's exit code was never propagated. **ERR-092.**
  `close_chain.sh` now exits 10 on a red row commit and names the log reader (private `dabc1be`).
- **The fix, `Observed`: GREEN.** `17161e2` (acceptance.py: keyed ref when there is one, else HEAD~1, SKIP only with no
  diff at all; 140/140 locally at the pushed HEAD) — run 34705375311 / num 264, conclusion success: python 16:30:24Z,
  rust 16:32:26Z.
- **Memory, in lockstep:** library commit `40ba056` — TIME-STATE → Desktop S138 / `6057a53` / row `e3021fb` / CI red 263
  → green 264; S133–S131 folded (−489 bytes); the first refusal (17,603, exit 1 — the size guard's new code) paid by a
  tighter block; **17,477 bytes — 33 under the hook; the next open folds before it writes.** The date on the TIME-STATE
  line is the first stamped from the clock (SYM-117); pushed to the private remote.
- **Both clocks:** `muster.sh` CLEAN after the lockstep (HARD 6057a53 == ledger, ancestor of HEAD; SOFT 86 == hook);
  the row passed `row_check.sh 138` (row 147 of 147) before the push.
- **The three receipts:**
  - process: `proc-feed/proc_events/receipt-20260912-115803.md` — `S138-receipt`, window 15:58:03Z → 16:34:09Z (the
    whole sitting this time); 1,024 processes seen; git.exe 284 · bash.exe 235 · python.exe 78 (the hooks on every call).
  - cost: `agent-scripts/receipts/cost-S138-20260912-161812.md` — **12.24 USD-equivalent** = session 9.87 (claude-opus-5,
    57 calls, 14.46 M cache-read, 107 k cache writes at 1 h, 62.5 k output) + fleets 2.37 (claude-sonnet-5, 37 calls,
    120 k input, 2.49 M cache-read, 270 k cache writes at 5 m). The window ends at the chain's own run (16:18Z); the red's
    repair, the lockstep and this §20 are outside it.
  - scripts-used: `agent-scripts/usage/S138.md` — 82 tool calls in the window; navigation 4 · reading 6 · testing 2 ·
    auditing 4 · developing 11.
- This §20 is a post-close commit and gets its own CI run, reported in the reply, not here.
