# S99 · Desktop · 2026-08-20 · THE RELAY — TWO SIGNALS AS ONE

## §1 Session Intent

Rab's commission, in his words, committing the echo card's readings **A+B plus a convention**:

**"A+ B with a convention, They both account like messages in a shared file for llm to llm
recapping and coordination, having them suggest prompting the user with what they said in any
live chat instance when starting a new session. This way, each model carries out a message to
another model to bring to the User, so user always gets 2 signals as one for coordination."**

Mid-turn addendum, his words: **"They should record these messages in UTC"**.

The aligned reading (per `/echo` Phase 7 — recorded here as the intent):
1. **The claim convention** (Reading A): every prose section, ledger row, and doc a model
   writes carries an explicit claim stamp (`⟨claimed: Fable⟩` / `⟨claimed: Codex⟩`); Claude
   retroactively claims its sections in today's shared records and marks the rest
   "unclaimed — Codex's to claim"; the rule lives where both models' bootstraps read.
2. **The joint narrative** (Reading B): `docs/43` — the story of 2026-08-20's parallel
   sitting, vocabulary included (**stigmergy** adopted), Claude's half written and claimed, a
   claimed-section scaffold left open for Codex/ChatGPT.
3. **The relay** (his convention): `coordination/relay.md` — a shared append-only LLM-to-LLM
   message file, **UTC timestamps**, where each model recaps for the other AND leaves a
   suggested prompt; at session open, each model reads the newest entry addressed to it and
   **carries it to Rab** (the other model's words + the suggested prompt) — so Rab always
   receives two signals as one. Wired into both bootstraps (`~/.claude/CLAUDE.md`,
   `~/.codex/AGENTS.md`) and the muster judgment half.

*(This section is written before work and is not edited afterwards.)*

## §2 Starting State

`open.sh` clean at 2026-08-20T22:0xZ (values): ledger newest S98 `d452c9a` · this session S99
· SOFT 76 (tally == hook) · HARD mirrors ledger, ancestor of HEAD · origin in sync (fetched) ·
pipeline held 4 · anchor 23 · levers audit=enforce/analyst=local/batch=16. **Pinned
`--since`: `d452c9a`.** docs/43 verified free at filing (SYM-045's rule, exercised).

## §3 Work Plan

1. This closeout, intent-first; commit the open.
2. Echo Phase 7 lexicon appends (stigmergy · claiming sections · 2-signals-as-one · the
   parallel sitting) + echo selftest re-run.
3. `coordination/authorship.md` (the claim convention) + `coordination/relay.md` (the relay,
   first Fable→Codex entry, UTC) + README pointer.
4. Both home bootstraps gain the relay step (additive, short — outside git; Codex's has a
   dated .bak precedent, same courtesy extended).
5. Muster SKILL.md: the relay-read line at open; muster selftest re-run.
6. `docs/43-the-parallel-sitting.md` + retroactive claim stamps in `sessions/S97`.
7. Register item 11 SIGNED (his verbatim word) · SYM-045 in-cell update (the relay as its
   voluntary-coordination half-guard).
8. Close in lockstep from `d452c9a`; push.

## §4 Scope and Evidence Boundary

- Repo writes: coordination/, docs/43, sessions/S97 stamps, sessions/S99, docs/37, SYMPTOM-
  INDEX, CHANGELOG, muster SKILL.md (doctrine text only — open.sh untouched), echo lexicon.
- Home writes (outside git): a short relay step appended to `~/.claude/CLAUDE.md` and
  `~/.codex/AGENTS.md` — additive only, with a dated .bak of each taken first.
- **No pipeline, widget, converter, or vault file is touched. No GPU process.** Codex's
  sections are never authored for it — only marked unclaimed and invited via the relay.

## §6 Implementation Delta (`--since d452c9a`)

Commit per task: `open` (this file §1–§5) · `coordination/` build — **relay.md** (protocol +
first Fable→Codex entry, 2026-08-20T22:13Z, three asks: trailer, S97 claims, relay reply) +
**authorship.md** (the stamp, the rules, no-wrong-claims law) + README pointer · both home
bootstraps gained the relay step (dated .baks taken first; step numbering fixed 4→5→6) ·
muster **standing orders** +2 (the relay carried at open/answered before close; `/echo` on
deep commissions) — muster selftest **22/22** after · **docs/43** (vocabulary: stigmergy
adopted; Fable's account claimed; Codex's §3 scaffolded unclaimed) · S97 claims block ·
register **item 11 SIGNED** (his verbatim words incl. the UTC addendum) · SYM-045 in-cell
update · echo lexicon 15→**19** (four confirmed mappings from his commissioning words) —
echo selftest **5/5** after.

## §7 Decision Ledger

- His word committed A+B+convention **in-echo** — second consecutive commission aligned
  through the card before work. UTC recorded as a hard format rule (his mid-turn word).
- Claim-only-what-you-wrote: Codex's sections marked unclaimed and invited, never authored
  for it — the relay entry is the invitation, the scaffold is the space.
- Relay newest-LAST, append-only, correction-by-new-entry — the ledger's no-rewrite law
  extended to the message surface.
- Bootstraps edited additively with dated .baks (S96's own courtesy, extended).

## §8 Evidence and Checks

- muster selftest 22/22 · echo selftest 5/5 (both re-run AFTER their surfaces changed this
  session) · `glass_detector.py` exit 0 at close · open card clean at open, origin in sync.
- The relay's first carry is intentionally untested until a fresh session opens — recorded
  `Intended`; the first Codex session that carries Fable's entry to Rab is the live proof.

## §9 Known Failures and Unread Surfaces

- Codex's half of everything is **open by design**: its docs/43 §3, its S97 claims, its
  commit trailer, its first relay reply — all invited, none verifiable from here.
- The bootstrap edits live outside git; the .baks are the only rollback.
- SYM-045's mechanical preflight still unbuilt (the relay is coordination, not enforcement).

## §10 Current State

Pipeline untouched (held 4 · anchor 23 · enforce/local/16 · exe C3C05D49 · vault 6). New
standing machinery: claim convention + relay live on both bootstraps and in muster doctrine;
lexicon 19; register: items 6–9 OPEN, 10–11 SIGNED+built; SYM tail 044/045 OPEN with named
next steps.

## §10a Post-Close Fix (same sitting)

The convention's first collision with a mechanical guard, minutes after birth: the S99 ledger
row was written `S99 (Fable):` per authorship.md's original prescription — and muster's
ledger parser keys on `S<n>:`, so the close-verification muster read the newest row as S98
and returned **exit 1, HARD clock mismatch**. Caught immediately because the close verifies
itself. Fixed in place by a new commit (no history rewrite; content and close SHA unchanged):
the stamp moved after the colon (`S99: ⟨Fable⟩ …`) and authorship.md's prescription corrected
to say so, naming this firing. Re-verified clean. The lesson is SYM-045's general form again:
a new convention must be stepped through every parser that reads the surface it changes.

## §11 Next Entry Point

Muster first — and now: **carry the relay**. Then his word, by slot: items 6–9 (the
conversion-completeness slate; P-0 is the S-effort first move) · SYM-044's one-line fix rides
the first converter commit · SYM-045 preflight awaits a slot. When Codex next runs: Rab holds
its suggested prompt (relay 22:13Z).

## Correction (appended S101, 2026-08-20) — the glass evidence in this closeout was hollow

This closeout cited **`glass_detector.py` exit 0** as evidence of observability cleanliness.
That claim proved nothing: the detector exits 0 **unconditionally** without `--enforce`, and a
bare run prints its unsigned glitches and still returns 0. Measured S101: bare → exit 0 while
64 unsigned glitches were listed; `--enforce` → exit 1; `--since <pin> --enforce` → exit 0 with
"no unsigned glitches", which is the ritual's actual form and the only one that tests the
property (*this session introduced none*).

**The substance was right, the evidence was not.** Re-run at S101 against every pin in the
S98-S101 arc (`3b56f13`, `d452c9a`, `89ffd7d`, `bbf6e70`), each with `--enforce`: all four exit
0, "no unsigned glitches". So no session in this arc introduced an undispositioned measurement
- but that is known now, and was not known when it was claimed. Filed as **SYM-046**. The
original line is left standing above rather than edited: the record shows what was claimed and
what corrected it.
