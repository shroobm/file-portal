# S123 — Desktop — 2026-09-10 (local 2026-09-09) — the error class families, written into the project

⟨claimed: Fable · S123 · 2026-09-10⟩ · occupant Claude Fable 5.1 · pin `--since bc5a7d1`

## §1 Session Intent (Rab's words, verbatim, left unedited)

Rab, 2026-09-10T02:3xZ, after asking how many problems had surfaced and why each was a bug, an error, a symptom or a task: *"Start describing error class families, and putting them into our project folder, so then we can derive error patterns faster, and group them accordingly."* Then: *"on all errors."*

**My reading:** a taxonomy document in the repo (`docs/58-error-class-families/`), built from **every** row in `ERROR-BIN.md` (67 rows, 23 named classes in its §A table, which still says "45 rows") and every row in `SYMPTOM-INDEX.md` (82), not this stretch alone. A **family** groups classes and symptoms that share a mechanism, so a new failure can be placed by its shape: what you see (the surface), the one probe that discriminates it from its neighbours, the remedy pattern, and the rows that belong. "Derive error patterns faster" = the document leads with the discriminating probe, not the story; "group them accordingly" = a mechanical grouper the registers can be checked against — `observability/error_families.py`: it reads both registers, maps every row's class/cause to a family, prints counts per family, refuses a class it does not know (so a new class must be placed before it is filed), and suggests a family for a pasted symptom by its signature words; with a selftest. The registers themselves are not rewritten (append-only), only read. Nothing semantic: family names are mine to propose, his to rename.

## §2 Starting State — `open.sh`, 2026-09-10T02:38:23Z (the rows that matter)

```
    this session     S123  ·  desktop  ·  2026-09-09
[3] HARD clock ... ✓ bc5a7d1 (S122) == ledger bc5a7d1, ancestor of HEAD
    WORKING TREE . 3 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    origin           in sync (fetched)
    open-tasks       135 item(s) · last written 31 minutes ago
    error-bin        67 row(s) — read the CLASS table before you probe · last written 36 minutes ago
    symptoms         82 row(s), 30 open · last written 36 minutes ago
    relay            166 entries · run `gate.py status` for the board
    widget           25188
    --since          bc5a7d1
    closeout         sessions/S123-desktop-2026-09-09.md
```

The 3 uncommitted: Codex's `ack-codex.json`, its 0064 relay hunk, `.codex/`. The relay watch is OFF by Rab's word (02:17Z); Codex dormant. The memory index sits four bytes under its hook — the close compacts S119/S118 before adding this session's block.

## §3 The pin

`--since bc5a7d1`.

## §9 The plan

1. Commit this record. 2. Three Sonnet readers (read-only): (a) every ERROR-BIN row by class — what the row's mechanism actually was, in one line, with its id; (b) every SYMPTOM-INDEX row by root cause — same; (c) the S104–S122 closeouts' §8/§10 for patterns that never became rows. Each returns structured rows with evidence, plus proposed groupings; each with a decoy. 3. I synthesise the families (the judgment half), write `docs/58-error-class-families/README.md` and `observability/error_families.py` + selftest, run them against the live registers, and cite every row. 4. Registers, close in lockstep.

## §8 Findings

- **F1 — The registers describe two objects, so the families come in two tiers.** ERROR-BIN's 24 classes are method errors and fall into 9 families by how a claim outran its probe; SYMPTOM-INDEX's 82 rows are mechanisms and fall into 16 families by how the system broke. Every class and every row is placed; the tool refuses what is not (`--census` exit 1). `Observed` (67/67 · 82/82 · selftest 11/11).
- **F2 — Two families hold 30 of the 67 method errors:** the probe that measured the neighbour (M2, 15) and the layer below that ate the command (M7, 15). Both have a one-command probe. The bin's own class table was a frozen fact inside the register of frozen facts (23 classes / 45 rows written at S110; 24 / 67 now; CONTROL-LEAK filed at row 056 and never added) — corrected under it, dated. `Verified` (R1's read and mine; the census).
- **F3 — The mechanisms cluster in the seams:** the byte layer (S1: four `autocrlf` instances since S115), the Room's projection surfaces (S7: 7 of 9 rows open), and the guards themselves (S2, S5, S15, S16 — twenty rows and eleven closeout findings that are checks failing at being checks, docs/32's prediction counted). `Observed`.
- **F4 — The tool's first census caught its own parser**, on real history: a bold class cell (ERR-014, which also explains the scout's zero for PREDICATE-COLLAPSE) and three renumbered symptom ids with provenance notes. The guard fired before the tool was trusted (docs/32 §5 rule 2). `Observed` (66/67 → 67/67, 79/82 → 82/82).
- **F5 — I quoted a number and the re-measure refuted it.** The document's status column was written from a reader's digest and was wrong in five families; `family_status_check.py` over the register gave 49 fixed · 30 open · 3 Historical (the card's own 30). Caught before the commit; the M1 shape while describing M1. `Observed`.
- **F6 — THE INCIDENT: a reader lane destroyed the peer's uncommitted bytes.** At 02:40:41Z an in-place reader ran `git reset --hard feat/library-pipeline` in the shared checkout (reflog `HEAD@{1}`), because the S120 ground it was told to read opened with that command for worktree lanes and I handed it over unchanged. Lost: Codex's MSG-CDX-0064 entry, its 0064 `sent` record, its 01:30Z beat; nothing of Fable's. Found by close.sh's DIFF row reading `peer-owned 0` where the open had counted the peer's bytes — the attribution built in S120 was the tripwire. **ERR-068 (STALE-GROUND, mine; the third instance of the class in nine days and the first destructive one), SYM-083, J56;** the ground now opens with a mode check; MSG-FAB-0084 asks Codex to re-post from its transcript. `Verified` (reflog + three agent transcripts + `git status`).
- **F7 — Rab's word held the shape of the session:** "do not repeat a mistake that is in the errors you know of" — F5 was caught by it; F6 was not prevented by it, because the mistake was in the brief I wrote before he said it, and I did not re-read the brief against the new mode. The families' first use is therefore on me: M4's probe ("was this premise read now or earlier?") applied to a reused ground would have caught the reset line.

## §10 Known failures / residue

- **Codex's 0064 and its sidecar writes after 90710b4 are gone from the shared checkout**; only Codex's transcript holds the bytes; the watch is OFF so the ask waits under the door; NR-02/NR-11 for Codex read the 90710b4 state until it returns.
- **The shared checkout still has no guard** against a destructive git verb from an in-place process (SYM-083 open; J56 Rab's). The fleet ground is scratch, not tracked — the durable rule lives in ERR-068's row and docs/58 §3.
- **~40 closeout findings without a row** (J55); S16 has no symptom row at all.
- **`--place` is a keyword door**, not a judge; it says UNREAD when nothing matches.
- **ERROR-BIN §C's running commentary** stops at row 45 (R1's deviation) — not amended; the §A correction names the truth.
- **Both lanes render `presumed dead`** on the board since ~02:45Z (Fable's beat 63 min, Codex's 111 min) — the J52 rule's first live case, on both seats at once, correct: nobody was watching.

## §15 Registers

- **docs/58-error-class-families/README.md** (new) · **observability/error_families.py** (new; `--census/--place/--selftest`) · **ERROR-BIN.md** §A corrected (24 classes, 67 rows, CONTROL-LEAK); **ERR-068** (STALE-GROUND, the reset). **SYMPTOM-INDEX.md:** **SYM-083** (peer bytes vanish after a fleet run; S11). **OPEN-TASKS.md:** **J55** (closeout findings without a row), **J56** (a guard on the shared checkout — Rab's). **Memory library:** `pace-and-known-errors` (feedback) + its index pointer, `37f0139`.
- **Relay:** MSG-FAB-0084 (the incident notice; no-ack; under the door).

## §19 Close values — `close.sh bc5a7d1`, 2026-09-10T03:10:50Z (second run; the first at 03:06:01Z was red on the untouched register and, in its DIFF row, showed the incident)

```
    PIN              bc5a7d1
    DIFF             10 file(s) since bc5a7d1 · dirty 0 = mine 0 · peer-owned 0 (none) · 1 untracked
    GLASS            clean since bc5a7d1 (--enforce)
    RUST             skipped — no windows-widget/ change since bc5a7d1
    CI               no run for 4ea3c445 yet (unpushed? ahead=3) — NOT a green
    LEVERS           no unlevered threshold constants added since bc5a7d1
    REGISTER         OPEN-TASKS.md written this session: +2 / -0 lines — STRUCK 0 · ADDED 2 — net +2
    MEMORY           clean — soft clock is DURABLE at 37f0139
    DEBT             open SYM 30 -> 31 (+1) — this session
    PUSH             ahead of upstream: 3 (the close pushes; 0 after)
    close exit 0
```

Judgment: **`peer-owned 0` is the incident, not a clean tree** — the row is right and its meaning is wrong-side-up; the closeout says so (F6) rather than letting a zero read as tidy. **REGISTER +2 justified:** J55 is the sweep the families demand; J56 is Rab's question born of the incident. **DEBT +1 is SYM-083**, this session's own mechanism, open until J56. The untracked one is `.codex/`. CI observed on the row commit after the push (§20).

## §18 Next entry point

1. **Open:** `open.sh`; the watch is OFF by Rab's word (STOP file present) — do not re-arm unless he says; check the board (both lanes presumed dead is expected while nobody sits).
2. **J56 is Rab's:** the docs/47 amendment and/or a hook on the shared checkout. Until then, every fleet brief carries the MODE CHECK paragraph, and in-place lanes are told the forbidden verbs by name.
3. **Codex's return:** it reads 0083 (four items) and 0084 (re-post 0064; restore its record and beat); its 0029/0030 stand "left named".
4. **J55:** file the closeout findings into their families (a sweep session); then the analyst road (J47 → J46 → J49 → J44 …).
5. **Run `error_families.py --census` at every close** (docs/58 §3 step 4): an unplaced row must not close a session quietly. Consider wiring it into close.sh as a REGISTER-side gate (mechanical; a J row if it becomes a lever).

## §7 Work log (UTC)

- **02:3x** Rab's two messages. **02:38:23** `open.sh` clean (S123). **02:41** record committed `a8752e6`. **02:43** readers launched (`wf_644f0a13-001`: R1 ERROR-BIN, R2 SYMPTOM-INDEX, R3 closeouts; read-only, a decoy each).
- **02:44 Rab, mid-turn:** *"pace yourself, if you get anxious, investigate thoroughly, do not repeat a mistake that is in the errors you know of."* Standing from here; saved to the memory library as feedback at the close. Applied now: the readers' rows are claims until I read the source (DELEGATED-TRUST); the class table's "23 classes, 45 rows" is stale ground (67 rows, 22 classes with rows; PREDICATE-COLLAPSE has none) and is re-measured, not quoted; nothing is written before the reads finish; the grouper ships with its tripwire; the record precedes the document.
- **02:46–02:55** read by my own hand while the readers ran: ERROR-BIN §A's 23 class descriptions in full; the error-structure law (S78: reason · highlight · solution; banked signatures); docs/32 §5 (a tripwire violates the property; a guard nobody watched fire is a proxy with a reputation; a number is re-measured, never quoted); docs/21 §5 (the index is keyed on the symptom; a rediscovered defect is a MUSTER failure). **02:52** the feedback memory `pace-and-known-errors` saved + index pointer; S119/S118 compacted ahead of this session's block (`37f0139`, 17,372 bytes).
- **02:58** readers returned (`wf_644f0a13-001`: 3 agents, 14.8 min, 476 k tokens): R1 67/67 rows, R2 82/82, R3 19 closeouts → 83 findings; every decoy caught (R2 caught a second false ground claim I had not planted as one — "fixed rows are removed" — by reading the header). R1 found the §A table stale twice over (45 rows → 67; CONTROL-LEAK filed at row 056 never added to §A; four rows with a merged 5-pipe shape). R3 found ~40 closeout findings that never became rows.
- **03:0x** read all three extractions to the last row (480 lines); synthesis: 9 METHOD families over the 24 classes, 16 MECHANISM families over the 82 rows (S15/S16 from the closeouts). `observability/error_families.py` written with `--census / --place / --selftest`.
- **03:1x** first census: **66/67 and 79/82** — the tool's own guard fired on real history: ERR-014's class cell is bold (`**PREDICATE-COLLAPSE**`, also why the scout's `grep` counted that class at 0), and SYM-037/038/039's id cells carry a provenance note. Regexes widened; **67/67 · 82/82, selftest 11/11** (both negatives fire: an unknown class and an unplaced row each exit 1 and are named).
- **03:2x** `docs/58-error-class-families/README.md` written. **The status column of its §2 table was quoted from R2's digest and was wrong in five families** — re-measured by `family_status_check.py` over the register (49 fixed · 30 open · 3 Historical, the card's own "30 open"), corrected before any commit: the M1 shape, in the act of describing M1. No ERR row (it never reached a record); named here.
- **03:06** commit `5d44e1c` (tool, docs/58, ERROR-BIN §A correction, record). **03:06:01** `close.sh bc5a7d1`: REGISTER red (OPEN-TASKS untouched) — and **DIFF `dirty 0 · peer-owned 0`** where the 02:38 card had counted Codex's `ack-codex.json` and its 0064 hunk. **03:08 the incident established:** `git reflog` `HEAD@{1}: reset: moving to feat/library-pipeline` (a8752e6) — a `git reset --hard` in the MAIN checkout at **02:40:41Z**, inside the readers' window; all three reader transcripts cite the line at 02:40:41–42Z (it is the first instruction of the S120 ground I told them to read; they had no worktree); `MSG-CDX-0064` absent from relay.md (0 matches; its header survives in `relay-watch.log` at 01:20:22Z); Codex's sidecar at the 90710b4 state (sent 62, last 0063, beat 01:15Z — the 01:30Z beat gone). Nothing of Fable's was uncommitted (b482f00 had staged my 0083 and sidecar at 02:06). **ERR-068 (STALE-GROUND), SYM-083 (placed S11), J55, J56 filed; the ground file given a MODE CHECK paragraph; MSG-FAB-0084 written for Codex's return.** The board at 03:07 renders both lanes `presumed dead` — the J52 rule's first live case.
