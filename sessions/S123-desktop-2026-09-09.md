# S123 — Desktop — 2026-09-10 (local 2026-09-09) — the error class families, written into the project

⟨claimed: Fable · S123 · 2026-09-10⟩ · occupant Claude Fable 5.1 · pin `--since bc5a7d1`

## §1 Session Intent (Rab's words, verbatim, left unedited)

Rab, 2026-09-10T02:3xZ, after asking how many problems had surfaced and why each was a bug, an error, a symptom or a task: *"Start describing error class families, and putting them into our project folder, so then we can derive error patterns faster, and group them accordingly."* Then: *"on all errors."*

**My reading:** a taxonomy document in the repo (`docs/58-error-class-families/`), built from **every** row in `ERROR-BIN.md` (67 rows, 23 named classes in its §A table, which still says "45 rows") and every row in `SYMPTOM-INDEX.md` (82), not this stretch alone. A **family** groups classes and symptoms that share a mechanism, so a new failure can be placed by its shape: what you see (the surface), the one probe that discriminates it from its neighbours, the remedy pattern, and the rows that belong. "Derive error patterns faster" = the document leads with the discriminating probe, not the story; "group them accordingly" = a mechanical grouper the registers can be checked against — `observability/error_families.py`: it reads both registers, maps every row's class/cause to a family, prints counts per family, refuses a class it does not know (so a new class must be placed before it is filed), and suggests a family for a pasted symptom by its signature words; with a selftest. The registers themselves are not rewritten (append-only), only read. Nothing semantic: family names are mine to propose, his to rename.

## §2 Starting State — `open.sh`, 2026-09-10T02:38:23Z (the rows that matter)

```
    this session     S123  ·  desktop  ·  2026-09-09
[3] HARD clock ... ✓ bc5a7d1 (S122) == ledger bc5a7d1, ancestor of HEAD
    WORKING TREE . 3 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    origin           in sync (fetched)
    open-tasks       135 item(s) · last written 31 minutes ago
    error-bin        67 row(s) — read the CLASS table before you probe · last written 36 minutes ago
    symptoms         82 row(s), 30 open · last written 36 minutes ago
    relay            166 entries · run `gate.py status` for the board
    widget           25188
    --since          bc5a7d1
    closeout         sessions/S123-desktop-2026-09-09.md
```

The 3 uncommitted: Codex's `ack-codex.json`, its 0064 relay hunk, `.codex/`. The relay watch is OFF by Rab's word (02:17Z); Codex dormant. The memory index sits four bytes under its hook — the close compacts S119/S118 before adding this session's block.

## §3 The pin

`--since bc5a7d1`.

## §9 The plan

1. Commit this record. 2. Three Sonnet readers (read-only): (a) every ERROR-BIN row by class — what the row's mechanism actually was, in one line, with its id; (b) every SYMPTOM-INDEX row by root cause — same; (c) the S104–S122 closeouts' §8/§10 for patterns that never became rows. Each returns structured rows with evidence, plus proposed groupings; each with a decoy. 3. I synthesise the families (the judgment half), write `docs/58-error-class-families/README.md` and `observability/error_families.py` + selftest, run them against the live registers, and cite every row. 4. Registers, close in lockstep.

## §7 Work log (UTC)

- **02:3x** Rab's two messages. **02:38:23** `open.sh` clean (S123). **02:41** record committed `a8752e6`. **02:43** readers launched (`wf_644f0a13-001`: R1 ERROR-BIN, R2 SYMPTOM-INDEX, R3 closeouts; read-only, a decoy each).
- **02:44 Rab, mid-turn:** *"pace yourself, if you get anxious, investigate thoroughly, do not repeat a mistake that is in the errors you know of."* Standing from here; saved to the memory library as feedback at the close. Applied now: the readers' rows are claims until I read the source (DELEGATED-TRUST); the class table's "23 classes, 45 rows" is stale ground (67 rows, 22 classes with rows; PREDICATE-COLLAPSE has none) and is re-measured, not quoted; nothing is written before the reads finish; the grouper ships with its tripwire; the record precedes the document.
- **02:46–02:55** read by my own hand while the readers ran: ERROR-BIN §A's 23 class descriptions in full; the error-structure law (S78: reason · highlight · solution; banked signatures); docs/32 §5 (a tripwire violates the property; a guard nobody watched fire is a proxy with a reputation; a number is re-measured, never quoted); docs/21 §5 (the index is keyed on the symptom; a rediscovered defect is a MUSTER failure). **02:52** the feedback memory `pace-and-known-errors` saved + index pointer; S119/S118 compacted ahead of this session's block (`37f0139`, 17,372 bytes).
- **02:58** readers returned (`wf_644f0a13-001`: 3 agents, 14.8 min, 476 k tokens): R1 67/67 rows, R2 82/82, R3 19 closeouts → 83 findings; every decoy caught (R2 caught a second false ground claim I had not planted as one — "fixed rows are removed" — by reading the header). R1 found the §A table stale twice over (45 rows → 67; CONTROL-LEAK filed at row 056 never added to §A; four rows with a merged 5-pipe shape). R3 found ~40 closeout findings that never became rows.
- **03:0x** read all three extractions to the last row (480 lines); synthesis: 9 METHOD families over the 24 classes, 16 MECHANISM families over the 82 rows (S15/S16 from the closeouts). `observability/error_families.py` written with `--census / --place / --selftest`.
- **03:1x** first census: **66/67 and 79/82** — the tool's own guard fired on real history: ERR-014's class cell is bold (`**PREDICATE-COLLAPSE**`, also why the scout's `grep` counted that class at 0), and SYM-037/038/039's id cells carry a provenance note. Regexes widened; **67/67 · 82/82, selftest 11/11** (both negatives fire: an unknown class and an unplaced row each exit 1 and are named).
- **03:2x** `docs/58-error-class-families/README.md` written. **The status column of its §2 table was quoted from R2's digest and was wrong in five families** — re-measured by `family_status_check.py` over the register (49 fixed · 30 open · 3 Historical, the card's own "30 open"), corrected before any commit: the M1 shape, in the act of describing M1. No ERR row (it never reached a record); named here.
