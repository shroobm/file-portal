# S96 · Desktop · 2026-08-19 · THE MEMORY LIBRARY REPAIR

## §1 Session Intent

Rab's commission, in his words: **"Whatever it may be, plan it out, then use sub agents based on
your choice of method, to save my ass, and yours."** — extended the same sitting with **"Please
make sure you have what you need, use sub agents to argue with you, instead of solo thinking it,
Organize yourself, and then keep alignment which commiting to an action. You have my signature."**

The object of the repair is the nine findings recorded in `docs/39-memory-library-inventory.md`
(written earlier this sitting, before this session was opened): the memory library's hub file is
weeks stale, its index has absorbed the session closeouts, it has no version control, stale
duplicates sit outside it, and its link graph has orphans and dangling targets.

*(This section is written before work and is not edited afterwards. The gap between it and the
outcome is itself a finding.)*

## §2 Starting State

`open.sh` exit **1 — INCIDENT**. Card verbatim:

```text
════════ MUSTER · OPEN · 2026-08-19T05:44:18Z ════════
[0] IDENTITY
    ledger newest    S95  b93218e
    this session     S96  ·  desktop  ·  2026-08-19
    advisory         S96 unnamed in the inherited tree
[1] GROUND
    verifier         1E2BE296  (repo == ~/.claude)
──────── MUSTER · 2026-08-19T05:44:18Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 74 (tally == hook)
[3a] LEDGER ORDER  ✓ 78 Desktop-lane rows ascend (S16 → S95)
[3b] LEDGER PARSE  ✓ 78/103 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 4 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79
[3] HARD clock ... ✗ SHA mismatch: TIME-STATE 841497d vs ledger b93218e
    WORKING TREE . 2 uncommitted · feat/library-pipeline
VERDICT .......... ✗ INCIDENT — reconcile before work
    muster exit      1
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 4 · anchor 23 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=16
    gpu-lock         absent
    events           137 line(s)
    widget           14984
    python procs     5
    ollama           2
    installed exe    C3C05D49  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 70c60e6
    thinkpad         converter=active · vault tip 70c60e6 · staging 0 · receipts 7
[3] PIN
    --since          b93218e
    closeout         sessions/S96-desktop-2026-08-19.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: INCIDENT (exit 1) — reconcile before work ════════
```

**Pinned `--since`: `b93218e`.**

## §2a The Incident, and its reconciliation

**Symptom:** hard clock `b93218e` (Desktop S95) vs TIME-STATE `841497d` (Desktop S94).

**Diagnosis — `Observed`.** Not a fork, not a rewind, not a blind session:

| Probe | Command | Result |
|---|---|---|
| S95 closed for real | `git log --oneline -12` | `42b1509 docs: Change Ledger row - Desktop S95 (b93218e)` |
| no divergence | `git merge-base --is-ancestor b93218e HEAD` | ancestor (so is `841497d`) |
| ledger intact | card `[3a]` | 78 Desktop rows ascend S16 → S95 |
| origin clean | card | in sync, fetched, zero backlog |

**Cause:** S95 wrote its ledger row (hard clock) but never advanced the TIME-STATE line in
`MEMORY.md` (the soft clock's mirror). Its own §3 work plan step 8 committed to *"advance the
ledger and soft clock in lockstep"*; the ledger half landed, the mirror half did not. The cookie
half of the soft clock is consistent (tally 74 == hook 74).

**This incident is evidence for the repair it opens.** S95 ran the full muster, produced a 94 KB
whole-system document, and still skipped the mirror. That is not carelessness — it is the
structural cost of finding #2: the TIME-STATE block has grown into a ~77-line hand-maintained
narrative, and hand-maintained narrative is precisely what gets skipped at close. The two-clock
design worked exactly as intended: the disagreement was the detection.

**Reconciliation:** TIME-STATE is advanced to mirror the ledger (S95 / `b93218e` / row `42b1509`)
as part of this session's finding-#2 rewrite, and `open.sh` must exit 0 at close.

## §2b Inherited precondition, resolved before work

S95 §18 item 5 required: *"Reconcile the `.Codex` versus `.claude` canonical memory namespace
before migrating or duplicating memory data."* This session's work is exactly that migration, so
the precondition was discharged first.

`Observed` — three probes:

```
find ~ -maxdepth 6 -name "MEMORY.md"   → exactly 3 hits: the canonical library, and the two
                                          known stale copies under Documents\Claude Code Memory Backup\
find ~ -maxdepth 6 -type d -name memory → exactly 1 hit: the canonical library
ls -a ~/.codex                          → harness runtime dir (auth.json, config.toml, logs_2.sqlite,
                                          memories_1.sqlite, computer-use, ambient-suggestions)
```

`.Codex` and `.codex` are the same directory (case-insensitive filesystem). No
`Codex-Memory-Backup` markdown library exists. A dedicated read-only agent was dispatched to
characterise `.codex/memories_1.sqlite` and `.codex/AGENTS.md` rather than assume — its verdict is
recorded in §9. Nothing in `.codex` is edited by this session.

## §3 Work Plan

**Method: the shop — persistent counterparties, one writer.** Sub-agents are read-only by
capability (`Explore` type cannot Edit or Write), so a misaligned counterparty can be wrong but
never destructive. The orchestrator holds the pen and applies serially. The guardrails point at
the orchestrator, because in this shape the orchestrator is the risk.

1. **Record intent** (this file), before any mutation.
2. **Safety before edits** — snapshot the library to a dated sibling of the existing
   `memory-snapshot-2026-07-22/`, then `git init` the library and commit all 28 files as the
   baseline. Rab signed the git decision. This converts every later guardrail into `git diff`,
   permanently, rather than for this run only.
3. **Four counterparties, read-only, dispatched before drafting:**
   - **Surveyor** — re-measure everything `docs/39` claimed, plus the evidence the rewrites need
     (ledger S75–S95, docs map 00–39, exe lineage, held/vault state, symptom index, a SHA-256
     baseline manifest and the link graph). *A number is re-measured, never quoted.*
   - **Witness** — reads all 28 originals cold, **before any draft exists**, and pre-registers the
     load-bearing facts per file, plus the facts that appear in exactly one file. This is a
     Survival Audit run on the rewrite: the registered inventory is the reference window, fixed
     before the new text can anchor it.
   - **Adversary** — attacks the nine findings and the plan itself: which finding is false, where a
     "fix" destroys value, what the plan missed, whether `open.sh` would even catch a bad edit, and
     the strongest case for doing nothing.
   - **Namespace investigator** — discharges the S95 precondition above.
4. **Draft in quarantine.** Every proposed file is written to the scratchpad as
   `<name>.proposed.md`, never to the library. Each rewrite ships with a **removals ledger**: every
   fact dropped, with its justification (duplicated at X / superseded by evidence Y / folded to
   Historical). The author accounts for his own deletions in advance.
5. **Dispositions, not votes.** Every Witness omission and every Adversary objection receives a
   written disposition — *accepted-and-fixed*, or *refuted-with-evidence* (the evidence, not my
   say-so). An unanswered objection blocks the apply step. Silence from a dead agent is never a
   pass; it is re-spawned.
6. **Apply serially**, one writer, commit per repair in the library's new git.
7. **Acceptance:** `open.sh` exits 0 · index covers N/N with zero dangling · the SHA-256 manifest
   proves every file that should not have changed did not · TIME-STATE parses under whatever
   pattern `open.sh` actually uses (the Adversary is tasked with finding that parser).

## §4 Signature and scope boundary

Rab's **"You have my signature"** covers the three items previously gated: the open-queue order,
`git init` on the library, and the stale duplicates outside it.

Two deliberate restraints stand anyway, and are recorded so the record does not overstate the
authorisation:

- The stale duplicates under `Documents\` are **renamed**, not deleted. Renaming achieves the whole
  safety objective (no session can mistake them for current) and is reversible. A signature
  authorises an act; it does not make the destructive form wiser than the reversible one.
- `.codex/` and `.agents/` are **not touched**. Neither is in scope, and `.agents/` was explicitly
  left outside session commits by S94 and S95.

## §5 Sections deferred to close

§7 Implementation Delta · §8 Decision Ledger · §9 Verification Evidence · §10 Known Failures and
Unread Surfaces · §15 Symptom Signatures · §17 Current State · §18 Next Entry Point.

## §7 Implementation Delta

No runtime behaviour changed. Three surfaces edited: the memory library (its own new git repo),
one harness config file, and this repo's session record.

**A · The memory library** — `git init`'d at open (Rab signed), now 4 commits deep.
1. `0087a22` baseline · `bb31b31` + `5b5eab5` byte-exact storage fix (see §9).
2. `4b18ce3` — **TIME-STATE advanced S94→S95 / `841497d`→`b93218e` / row `6725cb6`→`42b1509` /
   date →2026-08-19**, clearing the open incident. And the **false PENDING adoption corrected**:
   the block instructed the next session to have Rab adopt `C3C05D49`, which is already the
   installed exe. Now recorded as ADOPTED with the guard-exercise left explicitly `Unknown`.
3. `7aba848` — five WRONG claims corrected, two missing maps added, history untouched:
   - `segment-analyst` gemini transport `curl`→**urllib** (S93; key off argv, F-13/CWE-214)
   - `segment-intake` — `gemini` listed as a live analyst-mode value with **no mention of Rab's
     standing bar**; the config value is kept (it is valid), the bar is now stated beside it
   - `segment-convert-marker` — Damodaran "parked in `pending/`" → **`held/`** (measured:
     held 4, pending 0)
   - `thinkpad-machine` — exporter supersede flow listed as **pending work** when it was BUILT
     at S43 (`bd02fc0`) and live-fired S50/S56
   - `session-bootstrap` — "numbering is ONE global cross-machine sequence" → **machine-scoped
     lanes since S86**; the old text contradicted the tool that parses it
   - `git-repo` docs map **00–14 → 00–39**, titles read from each file's own heading
   - `segment-control-room` exe lineage **`B7F720C4`→…→`C3C05D49`**, with S88–S92's missing
     hashes marked `Inferred` and the unexercised S94 guards marked `Unknown`
   - `file-portal-project` — a dated CURRENT-STATE block **prepended** above the S37-era header.
     The stale block is kept verbatim. **The open queue was NOT pruned.**

**B · `~/.codex/AGENTS.md`** (backup `AGENTS.md.bak-2026-08-19-S96`). Two corrupted literals
reverted: the memory path (`~/.Codex/projects/…Codex-Memory-Backup/memory/`, which does not
exist) and `Codex-rewind-hazards` (the real file is `claude-rewind-hazards.md`). A blanket
Claude→Codex find-and-replace had mangled the one string it had to leave alone. The file loads
in every Codex session and was **causing the exact blind-session failure it exists to prevent**.
Line 3's "Codex session" is a legitimate adaptation and was left.

**C · This repo** — `docs/39-memory-library-inventory.md` (the inventory this session acts on)
and this closeout.

## §8 Decision Ledger

| Decision | Evidence and reason |
|---|---|
| Sub-agents are read-only **by capability**, not by instruction | All four ran as `Explore`, which cannot Edit or Write. A misaligned counterparty can be wrong but not destructive. The orchestrator held the pen throughout. |
| `git init` the library **before** drafting, not after | A first commit taken after drafting is a snapshot of already-mutated state. It also converts every later guardrail into `git diff`, permanently rather than for one run. |
| Killed finding #2 (move session narrative out of `MEMORY.md`) | Three independent agents converged: nothing guarantees a non-index memory file is auto-loaded; the Witness proved ~14 load-bearing facts live **only** in that block; the Surveyor showed four files are reachable only via the index. |
| Killed findings #6, #7, #9 | #9's remedy is forbidden by docs/21 §1 (`Historical` **carries the date, always**) and its newest block already matches measured reality. #6/#7 are decorative — all 28 files are reachable via the index. |
| Narrowed #1 to a prepended block; queue **not** pruned | `resume auto-detect-analyzed` has zero hits anywhere else on this machine. Deleting the queue wholesale would have destroyed it. |
| Renamed the stale duplicates rather than deleting | Renaming achieves the whole safety objective and is reversible. A signature authorises an act; it does not make the destructive form wiser than the reversible one. |
| Literal paths for the renames, never a glob | `memory-snapshot-*` would have marked this session's own rollback artifact `.STALE`. |
| Fixed `.codex/AGENTS.md` despite §4 saying `.codex` was out of scope | The restraint was written before the file was known to be broken; it existed to avoid gratuitous scope creep, not to preserve a defect. Amended openly here rather than silently. |
| Did **not** touch the split ledger table, the four stale SYM status columns, or the vault | Repo-structural and vault changes are Rab's call; they are reported in §18, not acted on. |

## §9 Verification Evidence

| Check | Result |
|---|---|
| MUSTER at open | **Exit 1 — INCIDENT** (§2a). Reconciled during the session. |
| MUSTER at close | **Exit 0 — CLEAN.** `[1] MEMORY ✓ · [2] SOFT clock ✓ 74 (tally == hook) · [3] HARD clock ✓ b93218e (S95) mirrors ledger, ancestor of HEAD` |
| TIME-STATE parser, re-verified after the edit | `muster.sh` reads a **6-line awk window** (`TS_WINDOW=6`) armed on the first line containing "TIME-STATE"; `exp_sha` = first `[0-9a-f]{7,40}` match **case-insensitively**; `exp_sess` = first `S[0-9]+`; the cookie count must fall inside the window. Post-edit: window still 6 lines, `received 74 / given 3` still on printed line 4, `exp_sess=S95`, `exp_sha=b93218e`. **No reflow was performed** — the `-i` flag makes `C3C05D49` a valid SHA match, so any reflow lifting it above line 8 would silently break the hard clock. |
| Library rollback, round-trip | `git checkout-index` to a temp tree, `cmp` per file: **28/28 byte-identical**, 199,166 == 199,166. |
| Round-trip negative control | A 1-byte append is detected → "pass" is not vacuous. |
| Library rollback, second method | `sha256sum -c` against the baseline manifest: **28 OK / 0 FAILED** at baseline; **27 OK / 1 FAILED** after repair 1 (only `MEMORY.md`), as intended. |
| Blast radius, repair 1 | `git status` = exactly 1 file. |
| Blast radius, repair 2 | 8 files, all intended; 48 insertions, 15 deletions. |
| Removals ledger | Every one of the 15 deletions accounted: 8 are `modified:` frontmatter stamps paired with fresh ones by the memory harness; the other 7 are **in-place replacements whose new text retains the original content**. Zero facts lost. |
| Line-ending integrity per file | Style preserved on all 8: CRLF stayed CRLF (`file-portal-project.md`), LF stayed LF (7 files). The first version of this check wrongly assumed every file was CRLF and produced two false alarms; corrected before being trusted. |
| `.codex/AGENTS.md` fix | Post-fix diff vs `CLAUDE.md` = **one line** (the legitimate "Codex session" adaptation). Every path it now names resolves: the memory dir plus `claude-rewind-hazards.md`, `build-workflow-protocol.md`, `session-bootstrap.md`, `cookie-tally.md`. Negative control: the old broken path still does not resolve, so the check discriminates. |
| Installed exe, three shapes | `sha256sum` of the real path = `C3C05D49`; the muster card agrees; the **MSIX shadow check** (documented discriminator) shows no package mirror, so the read hit real AppData. |
| `glass_detector.py --since b93218e` | **0 keys, 0 sites, no unsigned glitches.** The detector's own "reads clean is not the same as is clean" disclaimer is retained. |
| §4 accounting | `git diff --name-only b93218e..HEAD` → `CLAUDE_README.md` (S95's own ledger row), `docs/39-…md`, `sessions/S96-…md`. `.agents/` remains untracked and outside session commits, as at S94/S95. |
| Counterparty output treated as evidence, not authority | Every consequential agent claim was re-probed by the orchestrator before acting. One was **refuted** — see §10. |

## §10 Known Failures and Unread Surfaces

- **My own safety net was broken on first build, and its tripwire caught it.** Git's global
  `core.autocrlf=true` stored 4 CRLF files (`MEMORY.md`, `cookie-tally.md`,
  `file-portal-project.md`, `measurement-language.md`) with their CRs stripped — a restore would
  have silently rewritten 375 bytes of line endings across the index and the soft clock. The
  `-text` commit did **not** fix it: git's stat-cache saw no working-tree change and kept the LF
  blobs. Only `git add --renormalize` did. Found by the round-trip test, not by inspection.
  This is docs/32's disease in its named position — the defect moving into the guard built to
  prevent it.
- **A counterparty finding was REFUTED, not accepted.** The Surveyor reported the vault clone
  carrying an uncommitted modification to a vaulted note and warned it "breaks the exporter's
  L12 push-and-verify assumption." Measured: worktree and `HEAD` blob are byte-identical
  (sha256 `42c91b20…`, 171,318 bytes both, `git diff` = 0 bytes). It is the **stat-only phantom**
  `git-repo.md` documents. Refinement worth keeping: memory records that phantom on
  `Library\.gitignore`; it now sits on a note file, so it is not path-specific.
- **My brief to the Adversary was lossy.** `docs/39` §7 disclosed that `[[wikilinks]]` and
  `[[…]]` are prose specimens, not links — my summary to the agent dropped that line, and the
  agent (correctly, on what it was given) raised it as a near-miss that would have "fixed" a
  shipped invariant. The orchestrator's brief is an attack surface too.
- **My own verification logic was proxy-shaped once** — the CRLF check that assumed every file
  was CRLF. Caught by reading its own two "failures" instead of trusting them.
- **`Unknown`, not closed:** nobody has recorded exercising the S94 guards after adoption —
  the minimize+relaunch double-launch test, Room styling under CSP, the Recent-audits panel,
  the chat page. Adoption is `Verified`; exercise is `Unknown`. Now stated as such in two files.
- **`Unknown`:** `c5afd9edcf620fc6` is simultaneously in `held/` with verdict `fail` and vaulted
  as the git tip. Whether that is the supersede rail's intended state or a leftover was not
  determined.
- **Not re-probed:** the observability acceptance suite (S95 recorded red 38/39) and the Linux
  unit suites (S95: UNREAD, offline `watchdog`). Both stand as S95 left them. **UNREAD is not a
  pass.**
- The library's git repo has **no remote**. It is local-only; the Drive mirror named in
  `desktop-machine.md` was not verified this session.

## §15 Symptom Signatures

- **SYM-001** (green harness, wrong behaviour): the close gate `open.sh` exit 0 is
  **self-certifying** — `exp_sha` is read out of `MEMORY.md` and compared to the ledger, so the
  agent rewriting that file can turn the gate green by construction. Treated as necessary and
  **not sufficient**; the load-bearing proofs are the SHA-256 manifest, the git blast radius, and
  the Witness's pre-registered fact inventory.
- **SYM-029** (CRLF): fired in a new place — not a shell edit, but git's `autocrlf` inside the
  safety net. Rule extended in practice: *a backup is not a backup until its restore is proven
  byte-identical.*
- **SYM-031** (a failed probe rendering as a clean reading): honoured — the glass detector was
  run with the correct interpreter rather than reported from the Store stub; the Linux suites
  and the acceptance suite are carried as **UNREAD**, not as passes.
- **SYM-039** (documentation contradicting code): this session's whole subject. Five instances
  corrected; four more (the SYM status columns) reported but left for Rab.
- **SYM-007** (a write that "succeeded" but did not land): the MSIX shadow check was run before
  asserting the installed exe hash, rather than trusting a single read.
- **NEW class, unnumbered — the corrupted namespace clone.** `~/.codex/AGENTS.md` was produced
  by a blanket Claude→Codex find-and-replace over `~/.claude/CLAUDE.md`, which mangled the
  literal path it was meant to preserve, sending every Codex session to a nonexistent memory
  directory. A bootstrap file causing the blind-session failure it was written to prevent.
  Candidate for a SYM row at Rab's word.

## §17 Current State

The memory library is 28 files, now under local git, with a proven byte-exact rollback and two
snapshots. Both clocks reconcile; `open.sh` exits 0. Five wrong claims are corrected, two missing
maps restored, one blind-session bootstrap repaired. **No session narrative was rewritten and no
dated historical state was folded** — the library still reads as the sessions that did the work
wrote it. The stale duplicates outside the library are renamed, not deleted. `docs/39`'s findings
2, 6, 7 and 9 were **withdrawn on evidence**, and 1 was narrowed to an additive block. Product
source and pipeline are untouched; the installed exe remains `C3C05D49`.

## §18 Next Entry Point

1. **Rab's call, repo-structural:** the Change Ledger table is physically split — rows S16–S78
   in one block, **S79–S95 at EOF with no header and 4 columns instead of 5**. The newest 17
   rows do not render as a table.
2. **Rab's call, cheap and high-value:** `SYM-032`, `SYM-033`, `SYM-041`, `SYM-042` all read
   `OPEN` in the status column while their bodies record S94 signing and building them. Four
   false OPENs for anyone reading the column.
3. Exercise the S94 guards and record it (§10) — the one open item this session created rather
   than closed.
4. Decide `c5afd9edcf620fc6`: held and vaulted simultaneously.
5. `S82`'s ledger row is dated 2026-08-16; its closeout file is `S82-desktop-2026-08-15.md`.
6. Still open from S95 and earlier, unchanged: Stage 2 GO (converter tests + CI) · asset posture
   docs/37 §3.3 · Stage 3 GO · the standing queue · observability acceptance red 38/39 ·
   Linux suites UNREAD.
7. If the library's git is to survive a disk loss, give it a remote or fold it into the Drive
   mirror. It is currently local-only.
