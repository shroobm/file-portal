# S74 — Desktop lane — 2026-08-13

**Closeout under `docs/21`.** Tier: EXTENDED — widget source changed; an exe is staged.

## §1 Session Intent
`Observed` — plan `a87c1bc`, Rab's bedtime words: "2 signals are just 1, remember, and it's
good. Sign it, and finish what was left to do… get slice 2 finished before you end working,
send a draft to gmail, and a morning message on desktop." Cookies #59–61 (session open ·
commission · goodnight). He adopted `48BCB4A6` and gave the big-screen verdict at the session's
start ("The glow is nice") — the slice-2 gate opened — then killed the widget before sleeping
to clear the lane.

## §5 Observable Contract
`Intended` — Rab, after YOUR adoption (one Copy-Item + hash + launch, law 3):
1. Copy `target\release\file-portal-widget.exe` (SHA-8 **0454114C**) over the installed exe,
   relaunch, open the Room — **fails if** the rail's stations don't read as keycaps (face
   light + elevation), or if the rail's density changed (same sizes, same spacing).
2. With Gate cards waiting or an Assay fail — **fails if** no underglow under that station;
   with everything viable — **fails if** ANY rail key glows.
3. Let a convert finish while the Room is open — **fails if** the Convert keycap doesn't play
   exactly one press-and-spring (and it must NOT repeat on later polls).
4. With a big held queue / an old unacknowledged alert — **fails if** the glow doesn't sit
   wider / hotter than a fresh solo summons (Wall and rail both); ack + clear them — **fails
   if** the glow doesn't shrink back to base while the summons itself persists.
5. Windows reduce-motion on — **fails if** anything still breathes or travels.

## §7 Implementation Delta
`Observed` — `styles.css`: wl-glow ::after parameterized (`--fp-glow-o`, `--fp-glow-in`) ·
`fp-ga2/ga3 fp-gv2/gv3` modulator classes · `fp-breathe` keyframe made var-relative
(base render byte-identical: .5 × .44 = .22) · `.rl-glyph` keycap block (key-face + elev-rest
+ `--rl-bg` tint layer, hover/active elevation, `rl-pressed` one-shot `fp-keypress` on the
thock spring, `rl-glow` ::after LED) · reduced-motion guards for both. `room.js`: `glowMods()`
+ `heldAgeMin()` helpers · `railPrev` press-on-complete state · renderWall + stationRail
derivations (classes only) · inline `animation:` now emitted only when the pulse is real (a
blanket `animation:none` would override the press keyframe). Plus CHANGELOG / CLAUDE_README /
this file / the audit rig `harness-s74-audit.html` (kept, reusable for slices 3–5).
**Nothing else touched.**

## §8 Decision Ledger
1. **Age steps in units of the signed M lever (cap 4×M)** — reuses Rab's own escalation unit
   instead of inventing thresholds; the amendment adds zero unsigned policy. Rejected: a
   continuous opacity ramp (unreadable as steps; uncappable without a new constant).
2. **Gate modulates by volume only** — cards carry no on-disk age; we modulate only where the
   ledger already measures. Honest by construction.
3. **Press class lives for ONE render** — detected on the poll transition, gone at the next
   innerHTML rebuild; no timers, no cleanup code, reduced-motion collapses it.
4. **The rail tint moved from inline `background:` to `--rl-bg`** — the only way the key-face
   gradient can layer over the JS-chosen tint without the JS knowing about compositing.
5. **The audit rig graduated to a committed tool** — docs/25 demands this audit per slice;
   slices 3–5 reuse it (pre-file regenerated per its header note).

## §9 Evidence
`Observed` — audit: Room innerText 2100 chars, ONE diff char = the header clock's seconds
digit (`0:11:55`→`0:11:56` @ i=87); Wall 187 chars, zero diffs; errs []. Live harness (HTTP
:7080, S69 file://-snapshot law respected): Assay `rl-glow fp-gv3 fp-ga3` → computed ::after
opacity 0.85 / inset 0px; shrink test → bare `rl-glow` opacity 0.5 / inset 14%; Gate 2 cards →
`rl-glow fp-gv2` volume-only; presslog exactly `["convert@10:13:05","vault@10:13:23"]`, class
absent on following polls; heroes mass-only ×3; verdict `wv-attn` on ATTENTION; console clean
every probe. Ritual: fmt silent · clippy clean 10.9s · 20/20 · node --check ×2 · build 2
bundles · staged `0454114C` (9.6 MB). Screenshots skipped: Browser pane not displayed (S70
compositing law) — computed-style assertions carried the proof instead.

## §10 Known Failures
None found. One tooling trip: PS 5.1's native-arg quoting split a commit message on embedded
double quotes — reworded, committed clean (`482d6a3`).

## §15 Symptom Signatures
No new rows. SYM-001 honored (audit rig = differently-shaped second check against the class
review); SYM-007 honored (adoption left to Rab); SYM-020 honored (fmt led).

## §17 Current State
`Historical` — widget NOT running (Rab killed it pre-sleep); installed exe `48BCB4A6`;
**`0454114C` staged, unadopted.** Pipeline idle, drop/ empty at close. Unsigned queue
unchanged: slices 3–5 (Dock · Bench chrome · sound lever) · transcribe thresholds · audit
credit · Decision 5 · the four · engine lever · Valentine zones 2–4 · Cybernetics-stale ·
Textor · parked scan-lane locate.

## §18 Next Entry Point
1. MUSTER; newest row S74, ancestor-verified.
2. **Rab adopts `0454114C`** (§5), judges the rail + the amendment live → his verdict gates
   slice 3 (the Dock).
3. Slice 3 is small (tile mass + press states); slice 4 needs the Bench acceptance suite green
   as its extra gate; slice 5 is his ears only.
