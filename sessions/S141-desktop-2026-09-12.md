# S141 — Desktop — 2026-09-12 — THE TICKET RUN — twelve categories as twelve episodes, each a partial closeout (the audit layer built on itself)

*Record before the act (docs/28; J63's guards). Written at the open, before any tracked write.*

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

> Rab (chat, 2026-09-12 ~22:5xZ): "For you tickets, do each category in order. Work until all categories are done. Each time you finish a category, I want you to treat it as a partial closeout, you can compact and then retrieve the closeout with the carry over tickets (the next category) and repeat that as an episode. Calls, sequences, episodes, catch your procs, use what is available to you, attempt to do no inference by preemptively planning ahead. Follow that through strictly without anxiety, and investigate as much as you need." — the tickets being measurement/TICKETS-2026-09-12.md (private, fd096c0): 34 named tickets in twelve categories, born of the Astra TOOLING + MEASUREMENT mandate (relayed by Rab 21:2xZ), the study, the research and the second reading. Same message, the channel policy: "If you receive a message from desk, message gets sent to desk and don't have to laid out in chat. If it's in chat, then it stays in chat." Desk b1796936 (22:43Z): "categorize your ideas, then investigate, then write the series of tickets based on the category of what they're working towards … lettering (A-B-J) retired … The thinkpad might be unreachable for now but don't let that stop you from writing stuff for it if you need to. We'll hold the thinkpad related stuff to test when I say so." Desk 8f703581 (22:44Z): "significant policy changes … contracts and anything an agent is given has to be considered in thought."

**Standing:** nothing to Codex until lifted; the record before any tracked write (its creation and its commit are two
calls); a probe in the shared checkout is a file run by its literal path; `row_check.sh <N>` and CI's own gates before
any push; `--census` at every close.

## §2 Starting State — `open.sh`, 2026-09-12T22:56:23Z (verbatim)

```
════════ MUSTER · OPEN · 2026-09-12T22:56:23Z ════════
[0] IDENTITY
    ledger newest    S140  73544bb
    this session     S141  ·  desktop  ·  2026-09-12
    session marker   coordination/private/session.current = S141 (guard_record / guard_git read it)
    advisory         S141 is named in the INHERITED tree (73544bb) — read each, do not blanket-correct:
                     sessions/S140-desktop-2026-09-12.md 
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-12T22:56:24Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 86 (tally == hook)
[3a] LEDGER ORDER  ✓ 123 Desktop-lane rows ascend (S16 → S140)
[3b] LEDGER PARSE  ✓ 123/149 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ 73544bb (S140) == ledger 73544bb, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 9 · anchor 31 · pending 0 · drop 0
    levers           audit=report · analyst=local · batch=8
    gpu-lock         absent
    events           351 line(s)
    open-tasks       140 item(s) · last written 2 hours ago
    error-bin        96 row(s) — read the CLASS table before you probe · last written 2 hours ago
    symptoms         119 row(s), 43 open · last written 5 hours ago
    relay            167 entries · run `gate.py status` for the board
    widget           8076
    widget autostart task 'File Portal widget (J57)' Running — AtLogOn · auto-logon ON (Bndit)
    python procs     5
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            7 note(s) · tip 8d7cc85
    thinkpad         UNREAD — host did not answer; NOT a statement that it is down or clean
[3] PIN
    --since          73544bb
    closeout         sessions/S141-desktop-2026-09-12.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

## §3 The pin

`--since 73544bb` (the previous session's closing commit, from the card).

## §9 The plan

THE SHAPE — one session, twelve episodes, one per ticket category, in the order of measurement/TICKETS-2026-09-12.md:
  E1 truthful-numbers (5) · E2 provenance (4) · E3 corrections (2) · E4 typed-failure (1) · E5 cross-checks (4) ·
  E6 guard-holes (4; one blocked-on-Rab) · E7 unread-surfaces (7) · E8 services (2) · E9 baseline-experiments (2) ·
  E10 retirement (1) · E11 contracts (2) · E12 thinkpad (2, HELD: written, not tested).
  (The tickets' four waves were an order of VALUE; Rab's order is BY CATEGORY — his order governs.)

AN EPISODE, every time (the sequence is fixed here so no step is inferred mid-work):
  a. PLAN — write <scratch>/ep<N>_plan.txt: the tickets, the files each touches, the literal calls in order, the gates.
  b. BASELINE — `git show HEAD:<path> > <scratch>/base_<name>` for every file to be edited (never stash).
  c. BUILD — edits by the Edit/Write tool (hooks: Edit → selftest green → commit; SEQUENCES §0 law 4); scripts by literal path;
     a probe is a FILE; an expected deny gets its own call; never edit a file a lane is reading.
  d. TRIPWIRE — the ticket's tripwire case added and run; the touched script's selftest; `testing/library_smoke.py` when a
     library script changed (`--public` when a hook changed); `uvx ruff==0.15.20 check` on every .py edited.
  e. ACCEPTANCE — the ticket's acceptance command run and its output pasted into the episode section.
  f. COMMIT — one commit per ticket (private repo; public repo only where the ticket names it, after the record — this one —
     exists), message = the ticket id + what/why/proof.
  g. PARTIAL CLOSEOUT — append `## Episode N — <category>` to this record: tickets done/deferred with acceptance lines,
     the calls journal slice for the episode (`auditing/script_journal.py S141-ep<N> <transcript> --since <ep start> --until <ep end>`),
     scripts-used slice, the process tracker checkpoint (the partial receipt's window), what carried over; commit + push the
     record (public) and the receipts (private). THEN it is safe to compact: the next episode is retrieved from this record.
  h. CARRY-OVER — the first line of the next episode's plan names what E(N) left open.

THE CLOSE (after E12): SEQUENCES §3 CLOSE — close_chain.sh with CLOSE_TRANSCRIPT / CLOSE_SINCE / CLOSE_NOTES / CLOSE_CODE_NOTES,
  the five receipts, the row, the push, ci_observe, lockstep; MEMORY.md was folded at this open (16,450 bytes, room for the block).

CONTRACTS (Rab 22:44Z): every ticket names the contracts it touches; E11 (contracts) lands LAST and describes what exists.
CHANNEL POLICY (Rab): Desk posts answered on the Desk only; chat stays in chat.
GPU LAW: E7's watcher ticket touches the pipeline — `.gpu-lock`, `nvidia-smi`, `query user` before it.
THE TRACKER: started at this open (step 7), STOP file at the close; E8 makes it a scheduled task.
PIN: --since 73544bb.

## §8 Findings

(filled as they are made)

## §10 Known failures / residue

(filled as they are made)

## §7 Work log (UTC)

- **22:58** `open.sh` clean (S141, pin 73544bb). This record.

## Episode 1 — truthful-numbers (22:56 → 23:17Z) — PARTIAL CLOSEOUT

**Goal:** every number a receipt prints is what happened. **Tickets 5/5 done** (private `9b9faac` · `4b1a0dc` · `cd769ac` · `bd0ae5b`).

| ticket | what changed | acceptance (`Observed`, re-measured) |
|---|---|---|
| classify-calls-by-head | `auditing/_calls.py` — ONE classifier, by the PROGRAMS of a call's segments (every shell line, `&&`/`;` split outside quotes, heredoc bodies excluded; `cd <dir> &&` resolved; the highest kind wins, a mixed call marked); a fourth kind **consulted** (a read of a tool or its brief); `SCRATCH` any depth. Shared by `script_journal.py` and `scripts_used.py`; a brief for the module. | S140's window: `preloaded 39 · consulted 43 · scratch 9 · inferred 50` (the retired any-mention rule had read 76/9/56 — 58 reads filed as runs). `script_journal_selftest.py` **42/42**. |
| read-harness-exit-code | the result column is the harness's own `Exit code N` (RECORDED); an echoed `exit N` at a line start is its own column, never merged; prose (`exit 0 means …` in a brief) no longer reads as an exit. | fixture: `Exit code 3` → `exit 3`; the brief's prose → `ok`; `chain exit 10` → `echoed 10`. S140's "errors 2" were two `Exit code` results — now `exit N` rows. |
| journal-the-lanes | `--subagents auto`: `<transcript-stem>/subagents/**/agent-*.jsonl` read for the window, labels from the workflow `journal.jsonl`; a lanes table; every lane deny listed; a coverage line. | S139's window: `5 lane transcript(s) · lane calls 269 · lane denies 3` — the three `$PY` heads the guard log held and S139's journal never saw. S140: 4 lanes, 106 calls, 0 denies. |
| census-classify-by-run-not-text | `tool_census.py`: a `.py` with a brief, or wired as a hook in `settings.json`, is a TOOL whatever its text says; `measurement/` `.py` are **probes** (their own kind); one rule line printed with the totals. | `pipeline_roots.py`, `check_coordination.py`, `warn_heredoc.py` read `tool`; totals **287 files · tools 97 · probes 16 · tripwires 58 · modules 65 · agent-facing 60** (S139's 87/50 kept in the brief, dated, as the retired rule's reading). |
| recompute-cost-by-second-reader | `journal_check.py --content`: distinct `requestId`s with usage per model in the receipt's window vs its `calls`; rows re-summed vs `session`; `session + fleets` vs the grand total; `--journal <dir>`. **Found a replayability hole:** S140's receipt said `Window: … → end`, and `end` had moved (149 requests then, 351 now) — `cost_receipt.py` now records the EFFECTIVE end when `--until` is empty; a pre-S141 receipt reads `UNREAD — no window end`, not red. | `journal_check_selftest.py` **10/10**; over the fresh S141 receipt: `calls 78 == requests 78 · session rows 21.24 == 21.24 · total ✓`; S140: UNREAD by design, rows 73.58 == session 73.58. |

- **Contracts touched:** `script_journal.brief.md`, `scripts_used.brief.md`, `_calls.brief.md` (new), `tool_census.brief.md`, `journal_check.brief.md`,
  `cost_receipt.brief.md` (OUTPUTS/EXIT CODES/EXAMPLE, the old numbers kept dated); `journal/README.md` (the kinds); `SEQUENCES.md` §0
  (the S141 kinds; S138's 46/30 named as the retired rule's). `brief_check` 31/0; `library_smoke` **38/38** (16/16 on auditing mid-way).
- **An agent is handed:** "preloaded = a call whose PROGRAM is the tracked tool; a read of a tool's source or brief is CONSULTED; the
  result is the harness's exit code; a lane's denies are read."
- **Receipts for the episode (private, `journal/*/S141-ep1.md`):** calls 94 (preloaded 31 · consulted 13 · scratch 2 · inferred 13 ·
  harness 35 · denied 1 — #8, the memory-library commit refused because this record did not yet exist, J63 working as built); by
  segment: preloaded 39 · consulted 23 · scratch 10 · inferred 89; usage: auditing 21 · testing 5 · developing 4 · navigation 3;
  cost **26.01 USD-eq** (claude-opus-5, 94 requests); the tracker's checkpoint `receipt-20260912-185838.partial.md` covers
  22:58:38Z → (running; window read at 23:17:40Z), PID 3052, STOP file at the close.
- **Discovered:** (1) a receipt whose window says `→ end` is not replayable — the end must be the newest record read, written down
  (fixed for cost; the calls/usage writers get it in E2's stamp); (2) a call is usually several programs — the per-call kind (highest)
  and the per-segment counts disagree by design (scratch 2 vs 10 here: the patch scripts ran first in mixed calls) — both lines are
  printed, neither is "the" number; (3) my own `$VAR`-free, literal-path calls still trip the guard when the RECORD does not exist
  yet (J63 fired on the memory library's commit at 22:57Z — right, and the fold+index write went through once the record existed).
- **Carry-over → Episode 2 (provenance):** stamp-producer-and-sources (the `_stamp.py` header for the five writers — including the
  effective window end for calls/usage), extend-dump-ledger-not-invent-manifest (public `dumps/dump.sh` → a tracked write after this
  record, GPU law n/a), coverage-lines-and-datasheet, occupant-per-session (`auditing/occupant.py` + `row_check.sh`'s stamp check —
  public + private). Nothing from E1 is open.

## Episode 2 — provenance (23:17 → 23:35Z) — PARTIAL CLOSEOUT

**Goal:** every receipt names who made it, from what bytes, and what it could not see. **Tickets 4/4 done** (private `7eaf96d` ·
`362598f` · `6a3e228` · `01c402f` · `60561b5`; public `94e87e0`).

| ticket | what changed | acceptance (`Observed`) |
|---|---|---|
| stamp-producer-and-sources | `auditing/_stamp.py`: every writer's header carries the producer path + the git blob of the bytes that RAN + HEAD (flagged `UNCOMMITTED at write time` when the working copy differs — the second reading's F5 moving-target specimen now self-reports), each source's bytes + sha256 at generation, the harness versions seen, the window READ with its effective end (calls/usage now record it like cost), the coverage line. `journal_check --content` verifies: producer blob in git or red; each source's recorded bytes reproduce their sha (the replay test) or red; no stamp = UNREAD. | `journal_check_selftest.py` **16/16** — its expectations follow the writer's own commit state and say so (`cost_receipt.py's blob is NOT in git` mid-edit → the stamp red, as designed; `IS in git` after the commit → ✓). A receipt written by the uncommitted writer during the build read `blob NOT in git ✗` — the detector fired on its own maker. |
| coverage-lines-and-datasheet | each writer's `Coverage:` sentence (calls: main + lanes, rows read, the harness's exit; usage: direct calls only; cost: ids = requests, retries unpriced, fleets counted, cache-write pricing; procs: PARTIAL or clean, the tracker's window and grid; code: git is the mechanical half, the blocks are the agent's own account); `journal/DATASHEET.md` — Gebru's seven sections, once, for the tree. | `brief_check` 33/0; the S141-ep2 receipts carry the lines. |
| extend-dump-ledger-not-invent-manifest | **Rab's `dumps/dump.sh` (2026-08-26) extended, not forked:** `--ref` records a file in place; a `LEDGER.jsonl` twin row per md row (path, producer {path, blob}, sources [{path, bytes, sha256}], coverage, `row_sha256` over the row's canonical JSON — RFC 8785's shape); `--ledger <dir>` for the private receipts' ledger (`agent-scripts/journal/LEDGER.md` + `.jsonl`); the md row's seven columns never change shape. `dumps/ledger_check.py` verifies a ledger; the close chain rows the four transcript-derived receipts at every close and runs the checker. One coreutils trap paid: `sha256sum` prefixes a backslash when the path carries one. | `ledger_check_selftest.py` **11/11**; the public ledger: 5 md rows · 0 twins (pre-S141, said) · 0 disagreements; the private receipt ledger after this episode: **11 rows · 11 twins · 0 disagreements** (S140's five, ep1's three, ep2's three). |
| occupant-per-session | `auditing/occupant.py`: which model ANSWERED — the majority `message.model` per distinct message id in the window, every minority with its span, every `model_refusal_fallback` row, the lanes' models beside; `--check "⟨Fable · X⟩"` against the ledger row's stamp (exit 1); `--print-name`. The close chain derives the ledger commit's `Co-Authored-By` from the transcript (typed default only without `CLOSE_TRANSCRIPT`, said as UNREAD) and gates the row's stamp after `row_check` — **exit 11 OCCUPANT MISMATCH, not pushed**. `CLAUDE_README.md` ledger rules + `coordination/authorship.md`: one sentence each. | `occupant_selftest.py` **12/12**. S140's window: `claude-opus-5 149 of 149 · fallback none · lanes sonnet 78 · CHECK ✓`. The 2026-09-10 03:00–05:00Z window: `claude-fable-5-1 191 of 241 · also claude-opus-4-8 50 (03:55→04:26Z) · FALLBACK row 03:54:20Z "Fable 5.1's safeguards flagged this message … Switched to Opus 4.8"`. Rab's 08:32Z question is a checked invariant. |

- **Contracts touched:** `_stamp.brief.md`, `occupant.brief.md` (new); the five writers' briefs + `journal_check.brief.md` (the stamp sentence);
  `close_chain.brief.md` (exit 11; the trailer's source); `SEQUENCES.md` §3 step 5 (exit 11); `journal/README.md` (the ledger); `dumps/README.md`
  (a dated S141 section, nothing above rewritten); `.gitignore` (three negations beside README/LEDGER/dump.sh); `CLAUDE_README.md` (the row's
  stamp is checked); `coordination/authorship.md` (the same, with the fallback named). `row_check.sh` and the hooks untouched (E6's domain).
- **An agent is handed:** "a receipt without a Producer line is UNREAD; a receipt whose producer blob is not in git was made by a moving
  target; the stamp you type on the row is compared to what the transcript recorded; every receipt is rowed into a ledger by `dump.sh`,
  never by hand."
- **Receipts for the episode (`journal/*/S141-ep2.md`, stamped, ledgered D0009–D0011):** calls 78 (preloaded 14 · consulted 10 · scratch 10 ·
  inferred 17 · harness 27 · **denied 4**); usage: auditing 16 · testing 3; cost **27.00 USD-eq**.
- **The four denies, mine, all inferred shapes my own laws forbid:** two heredocs to python that mention git (the patch went to a file
  each time), one `$PWD` head, one `$J` head in a `for` loop (the loop went to a file). SEQUENCES §0 law 1 held; the journal counted them.
- **Discovered:** (1) a stamp is only as good as its commit — the verifier must read `blob NOT in git` as "made by code nobody can address",
  and the selftest of a writer under edit must expect its own red (it does, and says so); (2) `sha256sum` under Git Bash prefixes a
  backslash for a path with one — a digest column that silently starts with `\` would have failed every later comparison; (3) the
  receipt ledger and the receipts' stamps are two records of the same bytes from two writers (`dump.sh` in bash, `_stamp.py` in python) —
  CORROBORATED by construction, the first pair in the tree that is.
- **Carry-over → Episode 3 (corrections):** ledger-and-r2-files (the writers refuse to overwrite `S<N>.md` without `--revision`;
  `journal_check` demands a CORRECTIONS row per `-rN`; the twelve owed rows filed with originals named by git blob — the S138/S139
  journals are in git at `4847588`), instrumentation-bug-register (`journal/INSTRUMENT-BUGS.md`, IB-001…, `error_families.py --place`
  learns the family — a PUBLIC edit under `observability/`). Nothing from E2 is open.

## Episode 3 — corrections (23:35 → 23:45Z) — PARTIAL CLOSEOUT

**Goal:** history is never rewritten; a correction is a first-class record. **Tickets 2/2 done** (private `f393a79`; public `4bd7dd3`).

| ticket | what changed | acceptance (`Observed`) |
|---|---|---|
| ledger-and-r2-files | `_stamp.target_path()`: every writer REFUSES an existing `S<N>.md` (exit 1, the rule printed); `--revision K` writes `S<N>-rK.md`; `journal_check` reds a revision without a row in **`journal/CORRECTIONS.md`** — seeded with sixteen rows, originals by git blob where git has them (the S139 journal at `4847588`/`bf3a727`; the S138 journal was never committed — said, UNREAD), each with reason · corrected · what changed · downstream conclusions changed?: my 21:07Z Desk sentence (withdrawn), the STUDY's wrong claims (originals gone / runtime uncountable / find_bundle exits 2 / "2 vs 5" / ten scripts / line 79 / 2,698 / nothing hashes / retries invisible / POLICY §6), CODE_EXT, the pre-S141 `→ end` receipts. | `journal_check_selftest.py` **20/20** (+4: a second run REFUSED with the rule; `--revision 2` → `S9-r2.md`; a revision without its row red; with it ✓); `script_journal_selftest.py` 42/42 — its own second run had to take a new label: the old test shape was exactly the act now refused. `journal_check --all`: 5 sessions · 0 missing · 0 strays · 0 revisions without a row. |
| instrumentation-bug-register | **`journal/INSTRUMENT-BUGS.md`** — thirteen IB rows in ERROR-BIN's row shape and class vocabulary (the any-mention classifier, the exit regex, SCRATCH's depth, the lanes unread, the census's substring rule, `close_chain`'s `head -n 4` retuned twice, CODE_EXT, the `→ end` window, the in-place regenerations, the S139 false deny, `sha256sum`'s backslash, guard_record's missing log, the `.codex` copy); public `observability/error_families.py --bin <register>` places them (row id widened to `IB-`). | `error_families.py --bin …INSTRUMENT-BUGS.md` → **placed 13/13** (M2 ×3, M5 ×3, M6 ×3, M1/M3/M4/M7 ×1); `--selftest` 13/13 (+2); the live census unchanged `ERR 96/96 · SYM 119/119`. |

- **Contracts touched:** POLICY §7 law 6 (never in place; revisions + rows); SEQUENCES §4 step 5; `journal/README.md`; the five writers'
  briefs (INPUTS `--revision`, NEVER); `error_families.py`'s usage lines (`--bin`). `brief_check` 33/0; `library_smoke --only auditing` 20/20.
- **An agent is handed:** "an existing receipt is refused; a correction is a new file and a row; an instrument that read wrong gets an IB
  row placed in a family."
- **Receipts (`journal/*/S141-ep3.md`, ledgered D0012–D0014):** calls 58 (preloaded 12 · consulted 9 · scratch 5 · inferred 17 · harness 15 ·
  **denied 3**); usage: auditing 12 · testing 1; cost **22.00 USD-eq**. The receipt ledger: 14 rows · 14 twins · 0 disagreements.
- **The three denies:** #2/#3 — `git push` refused by J71 (the E2 public commit had touched `CLAUDE_README.md`; a push that changes it needs the
  session's row) → the sentence was DEFERRED to the closing commit (`062fbcf`) and the push went; #53 — a commit message body that
  said "git blob" inside a heredoc `-F -` (the guard reads a program-fed heredoc mentioning git as UNREAD) → the message went to a file.
  All three inferred shapes; the record keeps them, the journal counted them.
- **Discovered:** (1) J71 bites the middle of a session: any public commit touching `CLAUDE_README.md` before the row exists blocks every
  push until the close — a contract note for the register/contracts tickets ("CLAUDE_README edits belong to the closing commit");
  (2) the old selftest of the journal RAN THE WRITER TWICE on one label — the very act the rule forbids — and went red the moment the
  rule shipped: a tripwire's own habits are part of what a rule changes; (3) a commit message is a heredoc to the guard: the word
  "git" in prose fed through `-F -` is refused — messages go to files (SEQUENCES §3 step 3 already said so; I had not read it as
  applying to `-F -`).
- **Carry-over → Episode 4 (typed-failure):** five-codes-in-policy — POLICY §5 gains `3` nothing-found and `70` crash (sysexits
  `EX_SOFTWARE`); `auditing/_run.py` wrapper at every `__main__`; `find_bundle.py` / `who_holds.ps1` → 3; `close_parts.py`'s bare assert
  → a measured 1 with the reason; every brief's EXIT CODES; `library_smoke` plants a crash and asserts 70; `close_chain.sh` prints the
  code's name. Nothing from E3 is open.

## Episode 4 — typed-failure (23:47 → 23:53Z) — PARTIAL CLOSEOUT

**Goal:** a cold agent reads an exit code and knows which of five things happened. **Ticket 1/1 done** (private `49c9f5a`, 59 files).

| ticket | what changed | acceptance (`Observed`) |
|---|---|---|
| five-codes-in-policy | POLICY §5: **0 · 1 · 2 · 3 nothing found · 70 crash** (sysexits `EX_SOFTWARE`; the deviation from 64/78 stated). `auditing/_run.py` installs a `sys.excepthook` on import — traceback, `CRASH: …`, exit 70 via `os._exit` (a `sys.exit` inside a hook cannot change the status); a chosen `SystemExit` passes through. Every library `.py` imports it in two lines after its docstring (29 hooked). `find_bundle` / `read_journal` / `read_review` return 3 on nothing — `find_bundle` had exited **1** (the same code as a measured red) until now: the second reading's C2. `close_parts`' bare `assert` is a measured 1 with its reason. `brief_check`: EXIT CODES may name 0/1/2/3/70; every `.py` brief MUST name 70 (26 given the line; the three search briefs name 3); a case count (`12 cases`) or a section (`§5`) is not a code — two false reds paid and fixed in the rule. `close_chain.sh` names each code it can exit with. | `library_smoke` **45/45**: `a planted uncaught exception exits 70 (crash), never 1 — exit 70` · `every library .py imports _run` · `find_bundle with nothing to find exits 3 (an answer), not 1 — exit 3`; every selftest green under the hook; `brief_check` 34/0, its selftest 13/13 (+1: a `.py` brief without 70 is a defect; the exit-3 negative became exit-4). |

- **Contracts touched:** POLICY §5 (five rows + the paragraph); 29 briefs' EXIT CODES; `_run.brief.md` (new); `brief_check.py`'s law and
  docstring; `close_chain.sh`'s code names.
- **An agent is handed:** "exit 1 = the thing is wrong (said); 2 = I could not judge; 3 = nothing there; 70 = I broke — never confuse them."
- **Receipts (`journal/*/S141-ep4.md`, ledgered D0015–D0017):** calls 23 (preloaded 6 · consulted 5 · scratch 2 · inferred 2 · harness 8 ·
  **denied 0**); usage: auditing 6 · testing 2; cost **9.46 USD-eq**. Ledger 17 rows · 17 twins · 0 disagreements; `journal_check --all` clean.
- **Discovered:** (1) a universal hook is cheaper than 29 wrappers and catches the top-level scripts that have no `main()` (13 of them);
  (2) the brief law's regex had to learn that a number in an EXIT CODES paragraph is not always a code — "12 cases", "§5" — a mechanical
  law reads prose and prose has numbers; (3) zero denies this episode: every call was a file by path.
- **Carry-over → Episode 5 (cross-checks):** journal-denies-equal-guard-log (`journal_check --content`: guard_git DENIED (main) ==
  `git-guard.log` `agent=-/-` rows in the window; `guard_record.py` gains the same `log()` — a PUBLIC hook edit: Edit tool → its selftest
  green → commit, law 4), tool-version-at-call-time (a preloaded call between an uncommitted edit of that tool and its commit is flagged
  from `file-history-delta` rows), code-journal-paths-in-commit (`journal_check --content`: every explained path in the commit's
  `--name-only`; UNEXPLAINED == 0), procs-join-via-receipt-to-tokens (`proc_receipt` prints covered / matched / sub-grid-possible from
  the existing joiner — read `receipt_to_tokens.py` first). Nothing from E4 is open.

## Episode 5 — cross-checks (23:53 → 00:05Z, 2026-09-13) — PARTIAL CLOSEOUT

**Goal:** surfaces agree, or say why they cannot. **Tickets 4/4 done** (public `a4c0033`; private `52e1d37` · `14b5498` · `8573aec`).

| ticket | what changed | acceptance (`Observed`) |
|---|---|---|
| journal-denies-equal-guard-log | `guard_record.py` writes every deny to the ONE guard log (`guard=record`, the agent id as `guard_git` does) — until now its denies lived only in the transcript. `journal_check --content`: the calls receipt's DENIED rows vs the log's DENY rows in the receipt's window, per guard and per agent (main vs lanes); the marker read in its field, not anywhere in the line — the episode's own close found a `guard_git` deny whose command TEXT said `guard=record` and read as the other guard (1 != 0 → fixed → 1 == 1). | Over the four episode receipts (windows recorded): **1 == 1 · 4 == 4 · 3 == 3 · 1 == 1**, lanes 0 == 0 — two independent surfaces (the transcript vs the hook's own file), tolerance 0. `guard_record_selftest` **13/13** (+2, the log redirected to a throwaway); `journal_check_selftest` 24/24 (+4). |
| tool-version-at-call-time | `script_journal.py`: a PRELOADED call whose tool was edited after its last commit and before the call is flagged — CERTAIN from the harness's own `file-history-delta` rows (a Write/Edit backup), POSSIBLE when the tool was committed within 15 min after the call, because an edit made through a patch script leaves NO harness row (the coverage hole, printed in the header). `_stamp.repo_of` learned MSYS `/c/` paths (every `cd …/agent-scripts && python …` call had read as "not in a checkout"). | The second reading's F5 specimen reads by itself: `#22 18:10:34Z script_journal.py — POSSIBLE moving target: committed 2m20s AFTER this call`. This session's window: **42** flagged — a build session runs its writers uncommitted; the baseline gets the number. `script_journal_selftest` **45/45** (+3). |
| code-journal-paths-in-commit | `journal_check --content`: every `### path` under a `## sha` of the code receipt is in that commit's `--name-only`; the tail's UNEXPLAINED == 0. | S140's receipt: `24 file entries in their commits ✓ · UNEXPLAINED 0 ✓`; the selftest plants a path not in a real commit → ✗. |
| procs-join-via-receipt-to-tokens | `proc_receipt.py --transcript`: the JOIN with the transcript through the EXISTING `proc-feed/receipt_to_tokens.py` — shell calls in the tracker's window, joined to ≥ 1 process, zero-process calls said as a LOWER BOUND (shorter than the grid or the tracker not alive), mine processes joined / classified, each with its denominator. | S141 so far (the partial receipt): **190 shell calls · 66 joined · 124 zero-process · mine 391 / 596 · grid 121.1 ms**. |

- **ERR-097 (RUNTIME-RESIDUE), mine:** the guard's selftest wrote five fixture denies into the PRODUCTION log on its first run after
  `guard_record` learned to log, before the selftest redirected `FP_GIT_GUARD_LOG` — removed by a scratch script (a copy of the log kept
  in the scratchpad), filed in ERROR-BIN (census 97/97). A selftest of anything that writes sets its target to a throwaway first.
- **Contracts touched:** `guard_record.py` (Edit tool → selftest green → commit, law 4), `guard_record_selftest.py`; `journal_check.brief.md`,
  `proc_receipt.brief.md`, `script_journal.brief.md`; SEQUENCES §3 step 8 (`--transcript`); `brief_check` 34/0; `library_smoke --only auditing` 24/24.
- **An agent is handed:** "a deny you receive is in two places and the close compares them; a tool you ran uncommitted is named in the journal;
  a code-journal block is checked against the commit it explains; a call with no process is a lower bound, never 'nothing ran'."
- **Receipts (`journal/*/S141-ep5.md`, ledgered D0018–D0020):** calls 54 (preloaded 13 · consulted 7 · scratch 4 · inferred 11 · harness 19 ·
  **denied 1** — #20, a `python -c` wrapper that mentioned git); moving targets 8; usage: auditing 12 · testing 1; cost **23.51 USD-eq**.
  Ledger 20 rows · 20 twins · 0 disagreements.
- **Discovered:** (1) the first cross-check to run on the very episode that built it disagreed (1 != 0) — a command's TEXT carried the
  marker the check looked for anywhere in a line; a marker is read in its field; (2) the harness's file-history rows cover only its own
  Write/Edit — a patch script's edit is invisible to them, so the "certain" moving-target signal never fired this sitting (every edit
  went through patch files) and the "possible" one fired 42 times; the two signals are printed as two; (3) 124 of 190 shell calls
  joined to no process — most of this sitting's calls are shorter than a 121 ms poll grid, which is the tracker's known limit, not a
  finding about the calls.
- **Carry-over → Episode 6 (guard-holes):** cd-tilde-and-cd-dash (`guard_git.py`: `~` resolves, `-` is UNREAD → deny; +2 selftest cases;
  a SYM row), row-check-with-no-upstream (fail closed: run `row_check.sh` on the working tree), guard-record-selftest-shapes (path-less
  payload for a matched tool → deny; MultiEdit/NotebookEdit cases), retire-codex-hook-copy (blocked-on-Rab: delete vs junction — the
  ticket is written; nothing is done without his word). Nothing from E5 is open.

## Episode 6 — guard-holes (00:05 → 00:12Z) — PARTIAL CLOSEOUT

**Goal:** the guard fails closed everywhere. **Tickets 3/4 done; 1 blocked-on-Rab** (public `d35c2fc`).

| ticket | what changed | acceptance (`Observed`) |
|---|---|---|
| cd-tilde-and-cd-dash | `guard_git.scan()`: `cd ~` resolves to `%USERPROFILE%`; `cd -` (the shell's previous directory — unknowable to a guard that is not a shell) sets the tracked cwd UNREAD like `cd $VAR`. Before: both were no-ops and `cd - && git reset --hard` from an unguarded cwd was ALLOWED. **SYM-120** (S16). | `guard_git_selftest` **177/177** (+5): `cd - && git reset --hard` → deny; `cd - && git status` → allow; `cd ~ && git status` → allow. (`git branch -D` after `cd -` stays allowed for the main session by design — not a tier-1 verb; the case was corrected to the verb the rule covers.) |
| row-check-with-no-upstream | `row_check_missing()`: with NO upstream the ledger row is checked unconditionally — the reader needs no upstream — and the deny says `[no upstream to compare against — the ledger was checked unconditionally]`. Before: 'no upstream' read as 'nothing to judge' → allowed. **SYM-121** (S4). | +2 cases on a fixture repo with its upstream unset: a bad newest row → deny; the pushed five-cell row → allow. Not reproducible on the live repo read-only (it needs `--unset-upstream`) — the fixture is the proof. |
| guard-record-selftest-shapes | `guard_record.decide()`: a WRITING tool with no `file_path` / `notebook_path` / `path` is UNREAD and refused (fails closed; it passed); NotebookEdit and MultiEdit shapes exercised. | `guard_record_selftest` **16/16** (+3). |
| retire-codex-hook-copy | **BLOCKED-ON-RAB** — `.codex/hooks/` (untracked since 2026-08-28) duplicates `.claude/hooks/check_coordination.py` and has diverged by one line; delete, or make it a junction to `.claude/hooks/`? Nothing done without his word; the question is in the reply. | — |

- **Both hooks edited with the Edit tool, selftests green BEFORE the commit (SEQUENCES §0 law 4).** `library smoke: --public full run 59/60 (the one red = the WRITE TRIPWIRE: my own episode receipts were written into the private repo during the 503 s run — not the smoke’s doing; every hook and tripwire case green; the hooks re-run alone: 11/11)`.
- **ERR-098 (QUOTING), mine:** a `python -c "…"` patch whose double-quoted text carried a literal `` `git push` `` — bash ran it by command
  substitution before python saw it; both runs printed `Everything up-to-date`, nothing moved; the patch went to a file. A backtick in
  double quotes is a command to bash whatever it is to python. Census ERR 98/98 · SYM 121/121.
- **Contracts touched:** `guard_git.py` (docstring of `row_check_missing`), `guard_record.py`, both selftests, SYMPTOM-INDEX (120, 121),
  ERROR-BIN (098), `error_families.py` (the two placements).
- **An agent is handed:** "`cd -` before a git verb is denied — write the path; a push without an upstream still has its row read;
  a writing tool without a target is refused."
- **Receipts (`journal/*/S141-ep6.md`, ledgered D0021–D0023):** calls 27 (preloaded 11 · consulted 3 · scratch 1 · inferred 2 · harness 10 ·
  **denied 1** — #14, a `python -c` wrapper mentioning git; the cross-check: `guard_git main: journal 1 == log 1 · guard_record main:
  journal 0 == log 0` — the guard_record comparison is LIVE from this episode on); cost **12.57 USD-eq**. Ledger 23 rows · 23 twins · 0
  disagreements.
- **Discovered:** (1) the refuter's `cd - ; git branch -D x` reproduction was a true allow but not a hole — `branch -D` is a main-session
  write, not a tier-1 destroyer; the hole was `reset --hard` after `cd -`, which the same reproduction also showed; a ticket written
  from a finding must name the verb the rule covers; (2) the selftest's `ledger()` helper commits by itself — my first fixture
  committed twice and died on "nothing to commit": read the helper before using it.
- **Carry-over → Episode 7 (unread-surfaces):** read-the-transcript-system-rows, tool-runtime-from-transcript, desk-server-log-format
  (restart the Desk server when Rab is not mid-post), watcher-stuck-drop-file (the GPU law: `.gpu-lock`, `nvidia-smi`, `query user`
  before touching the watcher), orphan-chunk-journal-dump-before-rmtree (`analyst.py`, public), scheduled-tasks-on-the-card
  (`open.sh`), workflow-journals-and-widget-boot-log (the widget half waits for a rebuild slot). Nothing from E6 is open except Rab's call.

## Episode 7 — unread-surfaces (00:12 → 01:03Z, 2026-09-13) — PARTIAL CLOSEOUT

**Goal:** every surface the project writes has a reader — the harness's own rows, the tools' runtimes, the OS's tasks, the pipeline's orphans, the watcher's log, the Desk's log, the fleets' journals. **Tickets 7/7 done** (one with its widget half deferred to a rebuild slot). Public `027e3de` · `5b3ecb3` · `0abb952`; private `cd03d9e` · `2d1e45a` · `47786a3`. One compaction inside the episode (00:2xZ); the plan file carried it.

| ticket | what changed | acceptance (`Observed`) |
|---|---|---|
| read-the-transcript-system-rows | `script_journal.py` `## The harness`: api_error, compact_boundary, model_refusal_fallback, stop_hook_summary durations (p50/p95/max per hook), attachments by kind; a header line. | this episode's receipt: api errors 0 · compactions 1 · fallbacks 0 · advisories 5 · Rab's mid-turn words 4. Selftest cases on fixture rows. |
| tool-runtime-from-transcript | `scripts_used.py`: per library script p50/p95/max wall from tool_use→tool_result stamps, a slow list. | Runtimes table in `usage/S141-ep7.md`; `scripts_used_selftest` 7/7. |
| scheduled-tasks-on-the-card | `open.sh` [2] two `task` rows (`File Portal desk (relay)`, `File Portal relay watch (Fable)`): STATE / LASTRUN / RESULT from `Get-ScheduledTask`; SKILL.md says what the scheduler's words are not. | The card read `desk Running RESULT=267009 · relay watch Ready RESULT=0`. **No selftest case** — the probe hits the real scheduler; said in the commit, not hidden. |
| orphan-chunk-journal-dump-before-rmtree | `analyst.py` `_dump_journal`: the chunk journal is ledgered via `dumps/dump.sh` BEFORE `rmtree`; `manifest.analyst.chunk_journal_dump` = the DUMPED line or `UNREAD: …`; `FP_DUMP_LEDGER` for a selftest's throwaway; disposition `converter:chunk_journal_dump` same commit. The 12-day orphan `d58db211c41b0e17` → **D0042** in the public ledger, its dir removed. | `analyst_selftest` **30/30** (J46 (a) asserts DUMPED + a row in the throwaway); acceptance exit 0 (150 PASS); glass `--enforce` 0. **ERR-099** (RUNTIME-RESIDUE): the selftest's FIRST run wrote 36 fixture rows D0006–D0041 + 36 files into the tracked ledger — pruned with copies kept; D0042 keeps its id. |
| watcher-stuck-drop-file | **The investigation changed the ticket.** The 08-31 traceback was NOT the full-width `：` (the same name moved on 07-19 and twice on 07-22); the source left `drop/` by another hand 20 s into its conversion (`drop/failed/` written 07:37:14Z, between `estimate` 07:36:55Z and `converted` 07:37:39Z). Nothing is stuck in `drop/` today (the folder holds only `done/ failed/ .supersede`; `.intake-state.json` waiting 0; the folder's own mtime 04:13Z = hours before the ticket was written) — **ERR-100** (DELEGATED-TRUST), mine: a lane's `ls drop/` carried into the ticket as Observed. Built: `_move_source()` — a failed move is one `MOVE FAILED` line + an `intake move_failed` event (source, outcome, dest, reason: missing vs the OSError), never a traceback; the worker's catch-all emits `intake worker_error` (SYM-124); `FP_WATCHER_MUTEX` honoured like `FP_CARD_MUTEX` — since f045a66 the fixed mutex made `deferral_gate_selftest.py` exit 3 beside the live watcher, un-runnable 13 days (SYM-122); its first run found a chat-hold deferral had lost its DEFERRED line + `intake deferred` event since f045a66 → `_note_deferred()` in the loop (SYM-123); the watcher log UTF-8 from its next start (was cp1252+backslashreplace, `…` as 0x85 crashed a UTF-8 reader — SYM-125); `open.sh` `intake` row = the watcher's own receipt (waiting · oldest · active · age · pid; STUCK > 1 h waiting with no active convert; STALE > 60 s; UNREAD when absent). | `deferral_gate_selftest` **19/19 with the LIVE watcher up** (D: a `：`-named PDF converts and archives — the positive control; E: the stub removes its own source → one `move_failed`, no Traceback, the next PDF converts); muster `selftest.sh` **101/101** (CASE 57: UNREAD / STUCK with the numbers / not STALE / STALE); live row: `waiting 0 · oldest 0s · active null · written 3s ago by pid 13572`. **The live watcher (pid 13572, since 15:41 local) keeps the old code until its next start — not restarted.** Not built: the `\\?\` long path (no evidence; `LongPathsEnabled` unread). |
| desk-server-log-format | `desk.py`: access log → `data/access.jsonl` (UTF-8 JSONL: request line, status, size; `message`; `reset` = a dropped connection counted with its errno, never a traceback; `error` = any other handler exception with its traceback text); `serve --files` opens `serve.log`/`serve.err` as UTF-8; `desk_boot.ps1` / `desk_restart.ps1` redirect nothing. **Restarted UNDER the task** at 00:59:59Z (stop the serve python → the boot script logged `server exited with code -1`, the task Ready → old file renamed `serve.err.utf16-until-2026-09-13` → `Start-ScheduledTask` → `address present after 1s`). | The old file, decoded after it closed: 803,998 bytes, 401,130 NULs (UTF-16), 6,883 lines = 4,940 GET + 25 POST + **76** ConnectionResetError tracebacks (the ticket's 63 had grown). After: `access.jsonl` NDJSON 0 NUL, `serve.err` 0 bytes, `/api/entries` 200 with 67,753 chars (Rab's log untouched), task Running. `relay-desk/selftest.py` **19/19** (+4: a raw client RSTs mid-conversation → one `reset` row count=1 errno 10054; the next request 200; access rows parse; no Traceback on stderr). |
| workflow-journals-and-widget-boot-log | `script_journal.py` reads every run's `journal.jsonl`: started / results / MISSING (a lane that never returned) / REPLAYED (a result whose key had a result in an earlier run, or no `started` in its own run = the cache, not computed); a header line, a section, the headline's `workflows:` field. **Widget half DEFERRED** — `widget-boot.log` rotation (10,511 lines / 431,529 bytes; writers `main.rs:427`, `bench.rs:293`) is a Rust change → a rebuild slot Rab names. | Measured on the 21 runs since 09-05: 94 started, 93 results, **1 missing** (wf_93e0a4a6, 09-09), **0 replayed** — the mechanism exists, no instance yet. `script_journal_selftest` **51/51** (+3). |

- **ERR-009 (QUOTING) bit twice more, both caught by the hook's own advisory:** a heredoc collapsed `\\` and `\r\n` in two patch appendices (the escape-mangled text never reached a repo — the appendix was stripped and rewritten with the Write tool). The rule from here: a script with a backslash in it is written by the Write tool, never a heredoc. Plus a `$PY` head (SYM-120's family, denied) and a `git stash` chained after `acceptance.py` (denied — nothing in a refused call runs; acceptance re-run alone: exit 0). Census **ERR 100/100 · SYM 125/125**; `error_families` selftest 13/13.
- **Contracts touched:** `windows-converter/watch_and_convert.py` (four docstrings), `deferral_gate_selftest.py` (Phases D–E, the isolation), muster `SKILL.md` (the `intake` row), `open.sh`, `selftest.sh`; `relay-desk/README.md` (the health line) + `desk.py` docstring; `dumps/`' disposition table (`converter:chunk_journal_dump`); SYMPTOM-INDEX 122–125; ERROR-BIN 099–100; `error_families.py` (five placements); private: `script_journal.brief.md` unchanged (the brief's contract still holds — the new section is additive; a brief line for `workflows:` goes with E11's contracts pass).
- **An agent is handed:** "the Desk's health is `tail data/access.jsonl`; a watcher's failed move is an `intake move_failed` event, not a traceback, and the card's `intake` row is the watcher's own receipt (STALE means the watcher may be dead); a fleet result marked REPLAYED was not computed in that run; a ticket's *born of* is re-measured before the build."
- **Receipts (`journal/*/S141-ep7.md`, ledgered D0024–D0026):** calls **206** (preloaded 30 · consulted 56 · scratch 14 · inferred 63 · harness 43 · **denied 2** — #73 the `git stash` chain, #92 the `$PY` heredoc; errors 2 — a `TaskOutput` misuse and the blocked `sleep`); the cross-check `guard_git main: journal 2 == log 2 · guard_record 0 == 0`; workflows 0 in the window (21 on disk); cost **56.31 USD-eq** (the window 00:10:59Z → 01:03:55Z tiles onto E6's end). Ledger 26 rows · 26 twins · 0 disagreements; `journal_check --all` clean.
- **Discovered:** (1) a ticket written from a lane's reading and "confirmed" by a second reading of the same log is still one reading — the folder's mtimes refuted it in one call; (2) a tripwire that cannot run beside the live process it guards is a reputation, not a guard — its first run in 13 days found a second regression; (3) `logging.basicConfig(filename=…)` without `encoding` writes the locale's codepage with backslashreplace: a file no single decoder reads; (4) a subprocess's log opened by PowerShell's `>>` is UTF-16 — let the program open its own files; (5) Windows dir mtimes are a timeline when the events file is silent.
- **Desk (Desk→Desk policy):** Rab's `5f4006ad` "What's up?" (00:45Z) and `3215b19f` "How do you feel like you're doing…" (00:49Z) answered on the Desk at 00:46Z and 00:50Z.
- **Post-close (01:1xZ):** CI RED on `670b51c` (run 34729685347 / num 276 — `Schema registry parity (A4, hard gate)` 17/18: `checked-in schemas.json is current` FAIL; the rust job green). Cause: T5's new event `intake/move_failed` with a stale `schemas.json` — the partial close ran no registry check (**ERR-101**, BOUNDARY-OMISSION, mine). Reading the fix found **SYM-126**: the registry's writer extractor walked only module-level functions, so `intake/worker_error` (emitted from `_Worker._run`, a class method) stayed unregistered under a green `--check` — `_functions()` now includes every class's methods; `schemas.json` regenerated with both events; `schema_registry_selftest` 20/20 (+2). Pushed as a follow-up commit; CI observed on it below.
- **Carry-over → Episode 8 (services):** tracker-as-scheduled-task (gated on `session.current`), desk-watcher-as-task. **Deferred to a slot Rab names:** the widget-boot.log rotation (a rebuild). **Blocked on Rab:** `.codex/hooks/` delete vs junction (E6). **Live processes still on old code until their next start:** the watcher (pid 13572). The Desk runs the new code (restarted under its task).

## Episode 8 — services (01:12 → 01:23Z, 2026-09-13) — PARTIAL CLOSEOUT

**Goal:** the recorders survive a session restart. **Tickets 2/2 built and verified; both REGISTRATIONS wait on Rab's word** (a scheduled task is a persistent machine setting — the Desk task's own rule, `install_task.ps1`: "run -Register only on Rab's word"). Private `0d62731` · `fe6a6cd`; public `open.sh` row in this close. CI green on `ea220fe` (run 34730088065 / num 277) observed before this episode's build.

| ticket | what changed | acceptance (`Observed` / `Intended`) |
|---|---|---|
| tracker-as-scheduled-task | `proc_events.ps1 -GateFile` (a clean stop the moment the gate disappears); `proc_tracker_service.ps1` (the task's action: wait for `coordination/private/session.current` → record under its label → stop on removal → wait; `STOP-SERVICE` ends the service; a `service.log`); `install_tracker_task.ps1` (Status / -Register / -Start / -Unregister; **-Register refuses without `-Consent "<Rab's word and its date>"`** and writes it into the task's description); `record_open.py` writes the gate after the record; SEQUENCES §1 row 7 / §3 row 8 name the gate (the close removes it BEFORE `proc_receipt` reads). | **Observed** against a scratch gate + outdir: waits (0 receipts) → the gate `S9` → a gated tracker (pid) → the gate removed → one final receipt (`label=S9-receipt started_by=task`, window 5.5 s) → waits → exits on STOP-SERVICE. `proc_events_selftest` **34/34** (+5, run 7). `install_tracker_task.ps1`: NOT registered; `-Register` without consent → REFUSED exit 1, 0 tasks after. **Intended** (after his word): a session restart mid-sitting leaves the task Running and one receipt — observed by `schtasks /query /tn "File Portal tracker"` + one `receipt-*.jsonl` per gate lifetime. The live S141 tracker untouched (hand-started, `-Seconds 21600`). |
| desk-watcher-as-task | `desk.py watch --jsonl [PATH]` (one row per new entry: ts_seen, id, from, ts, text; default `data/watch.jsonl`); `desk_watch_boot.ps1` (the task's action; restarts the watch after a pause; reads the log file, needs no Tailscale address); `install_task.ps1 -RegisterWatch / -UnregisterWatch` (**refuses without consent**), status shows both tasks; README. The chat Monitor (b0wsjv9wo) stays the wake-up. | `relay-desk/selftest.py` **20/20** (+1: a POST landing while `watch --jsonl` runs = one row with its id and a ts_seen). Status: desk task Running since 20:59:59 local; watch task NOT registered; `-RegisterWatch` without consent → REFUSED exit 1, 0 tasks after. **Intended**: a post while no chat is open is in `watch.jsonl` with its time — after his word. |

- **The card:** `open.sh`'s task rows now include `File Portal tracker` and `File Portal desk watch` — ABSENT until registered, a reading (public, this close's commit).
- **Contracts touched:** SEQUENCES §1 row 7 / §3 row 8 (the gate), `proc_events.ps1` header (-GateFile), `relay-desk/README.md` (watch.jsonl + the task), `install_task.ps1` / `install_tracker_task.ps1` (the consent gate — the persistent-setting rule now mechanical), muster `open.sh`.
- **An agent is handed:** "you do not start the tracker once the task exists — the open's `session.current` IS the start and the close's removal IS the stop; until then, row 7 by hand. A scheduled task is registered only with Rab's word typed into the call; the task's description carries it."
- **Rab's word asked on the Desk** (entry `eac27c03`, 01:2xZ, posted from Fable): (1) `install_tracker_task.ps1 -Register -Consent … -Start`, (2) `install_task.ps1 -RegisterWatch -Consent …`, (3) E6's `.codex/hooks/` delete vs junction.
- **Partial-close gates on the public tree BEFORE this push (ERR-101's rule, first application):** `schema_registry.py --check` PASS · `acceptance.py` exit 0 · `glass_detector.py --since 73544bb --enforce` exit 0 · families census ERR 101/101 · SYM 126/126.
- **Receipts (`journal/*/S141-ep8.md`, ledgered D0027–D0029):** calls **77** (preloaded 10 · consulted 20 · scratch 8 · inferred 19 · harness 20 · denied 0 · errors 0); the cross-check `guard_git 0 == 0 · guard_record 0 == 0`; workflows 0 in the window; cost **17.05 USD-eq** (window 01:03:55Z → 01:23:11Z). Ledger 29 rows · 29 twins · 0 disagreements; `journal_check --all` clean. ERR-009 did not recur in this episode (every script by the Write tool).
- **Discovered:** (1) the selftest already had a run 6 — a new case takes the next number, read the file's last case first; (2) a service is a loop around the tool, not a change to the tool: `-GateFile` is one condition in `Test-ShouldStop`, the wrapper owns waiting/labels/restart; (3) the consent gate turns a comment ("only on Rab's word") into a refusal — the same move as the guard's, applied to the OS scheduler.
- **Carry-over → Episode 9 (baseline-experiments):** baseline-s141-s145 (`auditing/baseline.py` → `measurement/baseline/S<N>.json` + README line), h-001-sequences-pre-registered (`measurement/hypotheses/H-001-sequences.md`). **Waiting on Rab:** the two registrations, `.codex/hooks/`. **Deferred:** widget-boot.log rotation (a rebuild slot).

## Episode 9 — baseline-experiments (01:24 → 01:32Z, 2026-09-13) — PARTIAL CLOSEOUT

**Goal:** a policy change is an experiment with a baseline. **Tickets 2/2 done.** Private `e6056ff` · `9bc40e9`. CI green on the E8 close push `e1f1556` (run 34730440050 / num 278) observed before this episode's build.

| ticket | what changed | acceptance (`Observed`) |
|---|---|---|
| baseline-s141-s145 | `auditing/baseline.py <N> [--tickets K] [--refuter R/T] [--chain-exit N]`: one sitting's row from receipts and registers — calls receipts (kinds with `consulted` beside `inferred`; denies; errors; harness exit≠0 by kind; crashes 70; clean negatives 3; scratch written+run; moving targets; harness rows; workflows; the window — `S<N>.md` + `S<N>-ep*.md`, a `-rK` revision replaces, `-mid` named as skipped), cost totals, the guard LOG's DENY rows inside the calls window by agent and by guard, CORRECTIONS rows, the procs receipt's coverage, the record's open commit and the ledger row's commit (lead time), the CI API's conclusion for the row commit; three hand-given numbers no file carries. `measurement/baseline/S<N>.json` + a README row (replaced, never duplicated); UNREAD never 0; every rate with its n. Brief; SEQUENCES §3 row 11; measurement/README convention. | Retro S137–S140 + S141 in flight: S137 76 calls · 3 denies · 9.64; S138 133 · 2 · 12.24 · **CI on the row FAILURE** (run 34704816196 — SYM-118's red, read from the API, the reading a prose grep had missed); S139 128 · 5 (main 1 · lanes 4) · 28.14; S140 220 · denies UNREAD (its receipt's window says `→ end`) · 78.19; S141 617 · 12 · 193.91 · consulted 123 / inferred 144 · lead time UNREAD (no row yet). `baseline_selftest` **23/23**; `brief_check` 35 briefs / 0 defects. The first draft read CI and the chain's exit by grepping the record's prose and took a quoted fixture line (`chain exit 10`) as S141's — replaced by the API for CI and a hand-given `--chain-exit` (no file carries it). |
| h-001-sequences-pre-registered | `measurement/hypotheses/H-001-sequences.md` — STUDY §12 verbatim in the §12 template; PRIMARY = the guard log's main denies per sitting with n_inferred beside; SECONDARY = chain exit, CI on the row, inferred with consulted beside, tickets; COUNTEREVIDENCE ×3 (the guard loosened — selftest negatives 172 → 177 must not shrink; the sitting did less — read with calls and tickets; denies moved into the hook's advisories); RESULT UNREAD until the window closes (read S146–S150, direction only). README: the `hypotheses/` convention (above RESULT never edited; a change is a new file naming the old). | The file exists before its RESULT can be read (committed 01:3xZ; the window S139–S145 closes at S145). The BASELINE line carries `baseline.py`'s read-back of S137/S138. |

- **Contracts touched:** SEQUENCES §3 (row 11: the baseline row after the ledger row is pushed and CI read), `measurement/README.md` (two conventions), the new brief. No public file changed in this episode except this record.
- **An agent is handed:** "a baseline row is read, never typed: `baseline.py <N>` after the row is pushed; UNREAD is a reading; the three numbers you type (tickets, refuter, chain exit) are the only ones no file carries. An experiment is a file above its RESULT line before the result exists."
- **Partial-close gates on the public tree BEFORE this push:** only this record changes publicly; the four gates ran at E8's close on the same public tree (registry PASS · acceptance 0 · glass 0 · census 101/126) and no public code changed since — said, not re-run.
- **Receipts (`journal/*/S141-ep9.md`, ledgered D0030–D0032):** calls **38** (preloaded 3 · consulted 5 · scratch 5 · inferred 12 · harness 13 · denied 0 · errors 0); the cross-check 0 == 0; cost **10.24 USD-eq** (window 01:23:11Z → 01:31:48Z). Ledger 32 rows · 32 twins · 0 disagreements; `journal_check --all` clean.
- **Discovered:** (1) a regex over prose is an inference wearing a field name — the CI reading moved to the API and the chain's exit to a typed argument the moment the fixture line matched; (2) S138's row CI was RED (the API) — the retro baseline reads what the sessions' own §20s called green after the fix; the row's commit is what CI ran; (3) an unbounded receipt window (`→ end`) bounds nothing downstream: S140's denies and tracker coverage stay UNREAD until a revision with an effective end.
- **Carry-over → Episode 10 (retirement):** census-columns-and-first-candidates (usage columns on the tool census; `clocks.sh` the first candidate). **Waiting on Rab:** the two registrations, `.codex/hooks/` (Desk `eac27c03`). **Deferred:** widget-boot.log rotation.

## Episode 10 — retirement (01:32 → 01:44Z, 2026-09-13) — PARTIAL CLOSEOUT

**Goal:** the library can shrink. **Ticket 1/1 done; the seven candidates wait on Rab.** Private `fd33cc9`. CI green on the E9 close push `64546bb` (run 34730801611 / num 279).

| ticket | what changed | acceptance (`Observed`) |
|---|---|---|
| census-columns-and-first-candidates | `tool_census.py --list / --retire`: for the private library's tools — `born` (the adding commit's UTC stamp), `uses` (the scripts-used receipts' direct runs, both the S136–S140 three-column and the S141 four-column shapes; `-mid` skipped) with the last sitting that used it, `sittings observed` since birth (a usage window that ended at or after the birth), `nested-by` (library scripts whose SOURCE names the tool — the chain's calls are invisible to the receipts, G's hole closed statically), `replaced-by`, and the verdict `-` / `nested` / `young (k/5)` / `RETIRE?` (0 uses over ≥ 5 observed sittings, nothing nests it) — a PROPOSAL, never a deletion. POLICY §7 step 7; the brief's RETIREMENT paragraph. | First run, 6 sittings observed (S136–S141): **seven candidates** — `auditing/clocks.sh`, `errors/organize_errors.py`, `navigation/bundle_audit.py`, `navigation/procs.ps1`, `navigation/who_holds.ps1`, `reading/read_journal.py`, `reading/read_review.py`; `close_chain.sh` reads 11 uses (last S140) and `nested` tools read `nested`. `tool_census_selftest` **12/12** (the pure functions on fixtures); `brief_check` 35/0; `library_smoke` **49/49**. |

- **The census caught its own instrument, three times:** (1) `_calls.py` read a shell LOOP's body as one inferred segment (`for n in …; do baseline.py $n; done` → `do`/`done` as programs) — two loop calls of `baseline.py` counted as zero uses and the tool born an hour earlier read RETIRE?; control words are stripped from a segment's head and control-only segments skipped (`script_journal_selftest` 54/54, +3); E9's calls + usage receipts re-issued as `S141-ep9-r1` over the same window (preloaded 3 → 5, inferred 12 → 11, `baseline.py` 0 → 2 uses — a use is a call segment, not an iteration) with **CORRECTIONS C-017**, ledgered D0033–D0034 — the E9 section's numbers stay as written; (2) a local DATE for `born` made a tool born tonight "observed" by six sittings that ended before it existed — `%cI` in UTC, observed = a window that ENDED at or after the birth; (3) `journal_check.py`'s NAME regex admitted `S<N>-r<K>` but not `S<N>-ep<M>-r<K>` — the first episode revision read as a stray (selftest 25/25, +1); and `library_smoke` read the census's new footer as a usage banner (the word `usage` + exit 0) — reworded, not weakened.
- **Contracts touched:** POLICY §7 (step 7, retirement), `tool_census.brief.md`, CORRECTIONS (C-017), `_calls.py`'s classification (a loop's body is walked — every future calls receipt), `journal_check.py` (revision names).
- **An agent is handed:** "`tool_census.py --retire` proposes; Rab disposes; a tool named by another script's source is `nested`, never a candidate; a use is a call segment; a loop's iterations are not separate uses."
- **Rab's word asked on the Desk** (a reply on `eac27c03`, status `blocked`, 01:43Z): the seven candidates — all, some, none.
- **Partial-close gates on the public tree BEFORE this push:** only this record changes publicly; the four gates ran at E8's close on the same public code and none changed since — said, not re-run.
- **Receipts (`journal/*/S141-ep10.md`, ledgered D0035–D0037):** calls **52** (preloaded 12 · consulted 15 · scratch 3 · inferred 7 · harness 15 · denied 0 · errors 0); the cross-check 0 == 0; cost **15.16 USD-eq** (window 01:31:48Z → 01:43:54Z). Ledger 37 rows · 37 twins · 0 disagreements; `journal_check --all` clean (strays 0 after the NAME fix).
- **Discovered:** (1) a census that proposes deletions must first be run against a tool it knows was used — the first candidate list carried the newest tool in the library; (2) "observed since birth" needs a stamp, not a date, and the sitting's END, not its start; (3) the receipts' two shapes (three columns before S141, four after) are both live data — a reader that knows one shape reads half the history as zero.
- **Carry-over → Episode 11 (contracts):** policy-and-sequences-rewrite-pass (POLICY §2 two headings, formats-by-kind, SEQUENCES §0 laws, the briefs, docs/47 GROUND, `brief_check`), register-naming-transition (OPEN-TASKS.md — the tracked public write). **Waiting on Rab:** the two registrations, `.codex/hooks/`, the seven retirements (Desk `eac27c03`). **Deferred:** widget-boot.log rotation.

## Episode 11 — contracts (01:44 → 02:07Z, 2026-09-13) — PARTIAL CLOSEOUT

**Goal:** the written contracts match the new workings (Rab, 22:44Z: "contracts and anything an agent is given has to be considered in thought"). **Tickets 2/2 done.** Private `ea526f4` · `708f90d`; public `07a9b69` (the first tracked public write of the ticket run: `OPEN-TASKS.md`). CI green on the E10 close push `0e8a7dc` (run 34731299311 / num 280).

| ticket | what changed | acceptance (`Observed`) |
|---|---|---|
| policy-and-sequences-rewrite-pass | **POLICY** §2: eleven headings — WHAT CAN MAKE IT WRONG (the assumptions a correct run rests on; the known FP/FN) and WHERE IT HAS FAILED (the ERR/SYM/C/IB rows naming the script, or `none on record` with the birth date) between NEVER and EXAMPLE (STUDY §17); §6 FORMATS BY KIND (JSONL · Markdown-with-stamp · JSON · Markdown table; never mixed; the repo's newline per file; UTF-8 named). **All 35 briefs** carry both: WHAT CAN MAKE IT WRONG hand-written per tool; WHERE IT HAS FAILED generated from the four registers at generation time, the filename on a word boundary (`j64_hole_baseline.py` had matched `baseline.py` on the first pass — regenerated). **SEQUENCES** §0 laws 7–9 (no `cd -`/`cd ~`/`cd $VAR` before a git verb; a compaction is a memory boundary; a partial closeout is a close — `run_gates.sh --pin` before every episode push), §3b PARTIAL CLOSE (three rows); `run_gates.sh` gains the families census. **journal/README** (episode receipts, revisions, the baseline as a projection). **docs/47 §3** three clauses appended (the lane journal is read; the tool-version rule; consulted is not inferred). Muster **SKILL.md** Phase 3b (named tickets). | `brief_check` **35 briefs · 0 defects** under the eleven headings (its selftest 15/15, +2 negatives); `library_smoke` 49/49 (E10's run; no script changed since except `run_gates.sh`, whose own run is the reading: `ALL GATES GREEN`); 18 briefs carry rows, 17 read `none on record` with their birth date. The cold-call path: the smoke's bare-call case covers every script. |
| register-naming-transition | `OPEN-TASKS.md` `## TICKETS (named)`: 36 rows (id · works towards · status · bodies), 30 struck as done, 6 open (blocked / deferred / on Rab's word / held), the transition dated; `open.sh` [2b] and `close.sh` [8] count named ids beside lettered ones (the same two censuses, one more spelling of the id). | muster `selftest.sh` **101/101** — CASE 35 reads 6 with a named open row and a struck named row; the negative control now excludes 7 and 8 (the fixture grew from 6 to 8 id-bearing rows and the first run of the control read the old 6 — fixed in the same commit, said here). `run_gates.sh --pin 73544bb` before the push: `ALL GATES GREEN` (A4 · acceptance · glass · ruff · census). |

- **Contracts touched:** every one named above — this ticket IS the contracts ticket and landed last so it describes what exists. What it does NOT describe: the widget's boot-log rotation (deferred), the two registrations and the seven retirements (Rab's word), the ThinkPad tickets (held, E12).
- **An agent is handed:** a brief that says what can make it wrong and where it has failed; a SEQUENCES with a partial-close row; a GROUND that names the lane journal, the version rule and the consulted/inferred pair; a register where new work is a named ticket.
- **Receipts (`journal/*/S141-ep11.md`, ledgered D0038–D0040):** calls **64** (preloaded 11 · consulted 17 · scratch 4 · inferred 17 · harness 15 · denied 0 · errors 0); the cross-check 0 == 0; cost **21.65 USD-eq** (window 01:43:54Z → 02:07:13Z; the E12 writes below fall inside this window — four calls at 01:5xZ). Ledger 40 rows · 40 twins · 0 disagreements; `journal_check --all` clean.
- **Discovered:** (1) the mandate's nine teachability questions were seven in the headings and two in the history — the two missing were the ones a tool's author least wants to write, which is why the checker demands them; (2) a generated "where it has failed" is only as honest as its match: a substring found `baseline.py` inside another filename; word boundaries; (3) `run_gates.sh` had zero uses and was the partial-close gate the whole time — the census's RETIRE? list carried the tool law 9 needed.
- **Carry-over → Episode 12 (thinkpad, HELD):** written, not run — below. Then the S141 full close.

## Episode 12 — thinkpad (HELD; written 01:5xZ, inside E11's window) — PARTIAL CLOSEOUT

**Goal:** the ThinkPad's surfaces on the map, its held state provenanced — **written now, run when Rab says test.** Private `708f90d`.

| ticket | what was written | what was NOT done |
|---|---|---|
| map-receiver-and-exporter-surfaces | STUDY §1T: row T — the five surfaces (the receiver's log, the exporter's receipts, `held/`, the seam receipts, `vault.git`) with producer, P/D, what each cannot prove, replayability, how each is read from here; every cell `Historical`; the cross-seam corroboration named (T4's sha = the manifest's = T5's sidecar's; T2's held rows = T3's directories). `thinkpad_state.sh --surfaces` (sizes, stamps, counts over the ssh channel; UNREAD rows; exit 2 on a failed ssh) — `bash -n` clean; the brief's INPUTS says NOT RUN. | Not run. No ssh to the ThinkPad was attempted (the card's row stays UNREAD); no cell promoted to `Observed`. Acceptance (five rows or UNREAD each) waits for Rab's word. |
| chunk-journal-and-held-state-provenance | The design, in STUDY §1T: a `held/` bundle carries a dump.sh-shaped LEDGER row in the vault at the moment its directory is created; a supersede adds a `superseded` row; `--surfaces` reads the row count beside the directory count and the two must agree. | Not built: the exporter's hold path (the ThinkPad's code) is untouched; the contract touched (`docs/segment-vault-export`) is named, not edited. |

- **Receipts:** inside E11's (D0038–D0040) — four calls; no separate window, said here rather than manufactured.
- **Carry-over → the S141 full close:** the chain (`close_chain.sh` with the notes, `CLOSE_SINCE=2026-09-12T22:56:00Z`), the tracker stopped and `proc_receipt.py S141 --transcript`, the baseline row for S141 with `--tickets 33 --chain-exit <the chain's>`, the lockstep, §20, the Desk report, the chat report (the instruction came in chat). **Waiting on Rab:** the two registrations, `.codex/hooks/`, the seven retirements (Desk `eac27c03`); the ThinkPad tickets; the widget rebuild slot.
## §19 Close values — `close.sh 73544bb`, 2026-09-13T02:14:37Z (verbatim)

```
    PIN              73544bb
    DIFF             32 file(s) since 73544bb · dirty 0 = mine 0 · peer-owned 0 (none) · 1 untracked
    GLASS            clean since 73544bb (--enforce; bare runs exit 0 regardless — SYM-046)
    RUST             skipped — no windows-widget/ change since 73544bb
    CI               no run for b627955b yet (unpushed? ahead=1) — NOT a green
    LEVERS           no unlevered threshold constants added since 73544bb
    DOCTOR           lever lexical parity: figure-triage.txt names 8 · quoted refs 8 · MISSING 0
                     LEXICAL REF: mode · min_area_pt2 · vector_min_paths · cluster_gap_pt · text_coverage · words_per_line · table_overlap · accounted_for
                     UNREAD — a quoted lexical occurrence does NOT prove the consumer reads the lever; ARMED on MISSING only
    DOCTOR-HEAD      UNREAD — closeout-headline probe re-run not yet mechanized; NOT a statement the numbers reproduce
    CENSUS           selftest.sh: declared 11 = FIRED 11 = banner 11/11 (exit 0)
    CENSUS-SCOPE     UNREAD — broader TEST-STRATEGY fixture census not yet mechanized; NOT a statement all promised tripwires ran
    REGISTER         OPEN-TASKS.md written this session: +48 / -0 lines
                     STRUCK 30 · ADDED 36 — net +6, GOVERNANCE GREW. §I: justify it or work one down
    MEMORY           clean — soft clock is DURABLE at 6a1585f
    DEBT             open SYM 43, unchanged since 73544bb
    PUSH             ahead of upstream: 1 (the close pushes; 0 after)
════════ close exit 0 — values only; the judgment half is SKILL.md ════════
```

- **DIFF:** every changed file is in the content commit `b627955` or this closing commit; the row follows, then
  `row_check.sh 141` on it, then the push.
- **Relay:** no entry — nothing to Codex (standing).

