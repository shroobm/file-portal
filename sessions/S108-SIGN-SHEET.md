# S108 SIGN SHEET — ratifications and GOes awaiting Rab

⟨staged by Fable, 2026-08-23, per his order: "develop it, and leave it at the end for me to sign"⟩
Sign by replying in any session ("sign sheet: 1, 3, 5–8" works) or annotating this file by hand.
Blank ink = not yet decided (the atlas's own visibility pattern).

## A — Ratifications of choices built this session (each is LIVE; signing confirms, striking reverts)

| # | Choice built | What was chosen and why | SIGN |
|---|---|---|---|
| 1 | **A30 — the Bench undo model** | Native path: `contenteditable='true'` + `beforeinput` policing (`0f0e83f`). Measured: Ctrl+Z alive through Enter; plaintext-only MODE was the killer, not the engine. Textarea proven unnecessary. One manual Ctrl+Z by your hands confirms on hardware | ______ |
| 2 | **A4 — P-1's host** | OUT-OF-BAND (`coverage_rescore.py`, `0fbb6e3`): zero card-lock cost, back-fills the corpus, report-only. In-converter wiring remains available later | ______ |
| 3 | **The G-lane semantic trade** | Force-killing the widget now ENDS an in-flight `--resume`/`--reanalyze` GPU run instead of orphaning it (SYM-047 class closed). Previously the run survived; if you ever relied on that, say so and it reverts to opt-in | ______ |
| 4 | **Loopback token posture** | All 14 mutating routes (bench 11, room-chat 3) fail closed without the launch token; hand-started servers without `--token` refuse mutations with remedy text. Widget half passes tokens (`?token=`). End-to-end verified by construction; first real widget launch is the lived check | ______ |
| 5 | **Token entropy** | SystemTime+pid mix, 28-hex — loopback-CSRF grade, not cryptographic (threat model in code). Upgrade path exists if you want real RNG | ______ |

## B — GOes (nothing runs until signed)

| # | GO | Prepared state | SIGN |
|---|---|---|---|
| 6 | **C0 — the first conversion** | Pipeline verified end-to-end at code level; resolver tripwire green. YOU name the book (drop/ is empty). Recommendation: a small post-S60 born-digital PDF for a clean first breath | ______ |
| 7 | **held/ four dispositions** | `b7b711d4` finish at the bench (mid-repair, launch entry exists) · `c5afd9ed` resolve A41's held-and-vaulted conflict first (verify vault copy, then release) · `0d68f0e0` + `b6fbdd75` bench queue | ______ |
| 8 | **SYM-050 physical disposition** | Quarantine is record-only (`sym050-quarantine.txt`); poisoned bundles still sit in anchor/. Options: leave-in-place-flagged (recommended; the detector + record now guard every read) or move to a quarantine dir (a real data move — your hands or your explicit word) | ______ |
| 9 | **Arm the gates** | DOCTOR + CENSUS in close.sh and the CI warn-only steps arm next session — AFTER this push's CI is observed green | ______ |
| 10 | **A43 — memory-library remote** | Staged: create a PRIVATE repo (it holds working notes), then `git -C <library> remote add origin <url> && git push -u origin master`. Needs your GitHub hands (gh here is unauthenticated) | ______ |

## C — Your hands (forms staged, blank ink visible)

| # | Exercise | Where | SIGN |
|---|---|---|---|
| 11 | **S94 guard exercise** (~30 min, ~12 sessions overdue) | `sessions/S94-GUARD-EXERCISE-FORM.md` | ______ |
| 12 | **Atlas MANUAL-ACCEPTANCE** (~5 min) + the two AA contrast fixes noted in `wiki/a11y-conventions.md` | quarantine `release/MANUAL-ACCEPTANCE.md`; sim still live at `localhost:8765` | ______ |

## D — Policy questions (unbuilt until answered)

| # | Question | SIGN/ANSWER |
|---|---|---|
| 13 | **Pack eviction priority** (wave-2 path 7): when a model-send budget overflows, what drops first? | ______ |
| 14 | **Countersign rule**: does an agent-transcribed Rab signature require your countersign to count? (Codex's own RAB-SIGNATURE models the gap) | ______ |
| 15 | **Concordance Lab dossier**: commission my adversarial review of Codex's cooperation-simulator spec? (its 22:36Z relay entry, external path + SHA recorded there) | ______ |

## E — Recorded, no signature needed (residue and honesty)

- Wave-2 census: literals remain in `figure_coverage.py`, `bench.py`, `room_chat.py`, rust, js, sh (E-lane census; the five core modules are clean).
- B5 residue: the 19-test harness is headless; an in-browser DOM run stays open.
- CI: first-ever ruff over windows-converter may surface a lint backlog — cleanup is future mechanical work.
- acceptance.py `--since` degrades to SKIP over a long non-python commit run (B2 note) — watch once.
- My two pre-critique relay entries lack the three-part form; per the append-only law (which Codex itself violated and confessed), they stay as-written — every entry from `2026-08-23` close onward complies.
- Codex's D2 numbers, its SYM-053 filing, and its register strikes were reviewed and stand; commit `0bec6a7` carries a dual trailer (my lane's authorship, Codex's completion) — a first, and worth a naming convention eventually.
