# S116 — Desktop — 2026-09-05

⟨claimed: Fable 5.1 · S116 · desktop · opened 2026-09-05T21:05Z⟩

## §1 Session Intent

Rab, verbatim: *"Muster Protocol, Signed: ThinkPad pull+restart · reaudit c5afd9edcf620fc6 · J34 1.5x reject ·
Atlas: widget reads the tracked catalog, §15 1-9 as designed, R1/R4b ratified, O3-2 no · arm DOCTOR/CENSUS."*

*(Written before the build work; left unedited for the rest of the session — docs/21 §1. See §4's admission:
two of the five signed items were EXECUTED before this file existed.)*

## §2 Starting State — the `open.sh` card, verbatim

```
════════ MUSTER · OPEN · 2026-09-05T21:05:49Z ════════
[0] IDENTITY
    ledger newest    S115  eb5958e
    this session     S116  ·  desktop  ·  2026-09-05
    advisory         S116 unnamed in the inherited tree
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-05T21:05:50Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 85 (tally == hook)
[3a] LEDGER ORDER  ✓ 98 Desktop-lane rows ascend (S16 → S115)
[3b] LEDGER PARSE  ✓ 98/123 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 4 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79
[3] HARD clock ... ✓ eb5958e (S115) == ledger eb5958e, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 7 · anchor 27 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    events           270 line(s)
    open-tasks       120 item(s) · last written 45 minutes ago
    error-bin        58 row(s) — read the CLASS table before you probe · last written 4 hours ago
    symptoms         74 row(s), 26 open · last written 4 hours ago
    relay            136 entries · run `gate.py status` for the board
    widget           15308
    python procs     8
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 70c60e6
    thinkpad         converter=active · vault tip 70c60e6 · staging 0 · receipts 9
[3] PIN
    --since          eb5958e
    closeout         sessions/S116-desktop-2026-09-05.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

**Pinned `--since eb5958e`** (the S115 close). HEAD at open: `ba7c79f` (S115's post-close §20 pointer).

## §3 Inherited, all `Historical`

- S115 §18 (2026-09-05): Rab's slots in unblock order — the Atlas adoption reading (O5's sheet), a real
  `--reaudit c5afd9edcf620fc6`, the ThinkPad pull + restart for the J33 exporter, J34, arm DOCTOR/CENSUS, J23,
  J25/J30, Panel C; then drop a book. Hygiene: stale worktree branches gone (done at the S115 close).
- S115 §10: Cybernetics `c5afd9edcf620fc6` re-audits `fail → flag` on a scratch copy (dry-run 1.4 s); the ThinkPad
  checkout at `f6442cf`, receiver active; the Atlas's adoption unsigned (`adoption_authorized: false`); the
  `converter:rejections` disposition and the six subscript-assigned J31/J33 keys.
- S108 sign sheet item 9 (2026-08-23): *"DOCTOR + CENSUS in close.sh and the CI warn-only steps arm next session —
  AFTER this push's CI is observed green."* docs/50 row S108-9 `blocked-on-rab`.
- OPEN-TASKS J34 (2026-09-05): ratio guard proposal — output words / input words > 1.5 → reject, reason
  `inflation`; measured 7.59× (chunk 296) vs next 1.18×; 1 of 500 above 1.25/1.5/2/3. A41: `c5afd9ed` held AND
  vaulted simultaneously (S96 §18.4).
- Relay: newest entry to Fable is `MSG-CDX-0044` (2026-09-03T21:42Z), answered by `MSG-FAB-0068`. Nothing new from
  Codex since (its beat 2844 m stale at open, running gate `57187c87` ≠ this shell's `f34009c8` — a quiet lane).
- SYMPTOM-INDEX: 26 open at the pin; rows intersecting today: SYM-067 (J29's gate — the reaudit steps on it for
  real), SYM-073 (re-audit drift; J31 is its remedy), SYM-074 (think-leak guard, fixed; J34 catches the class
  independently), SYM-001 (two checks sharing an assumption are one check — see §4's CI-step finding).

## §4 Promoted this session (`Observed`, each with its probe)

**Admission first (F0).** Two of the five signed items were executed BEFORE this file existed — Phase 6's
"recording precedes action" was violated for them. Each ran on `Observed` premises and on Rab's own words, and
each is recorded here with its probe; the ordering error is the finding, not the acts.

- **21:08Z ThinkPad probe** (`tailscale ssh rab@archlinux`): `HEAD=f6442cf`, clean tree, `behind=40 ahead=0`
  against `origin/feat/library-pipeline`; `file-portal-converter.service` active since 16:33:52Z; only
  `linux-converter/converter/exporter.py` (+124/−2) and `tests/test_exporter.py` change in the 40 (`git diff --stat`),
  no requirements change.
- **21:09:25Z ThinkPad pull + restart (SIGNED, executed):** `git pull --ff-only` → `HEAD=ba7c79f`; `systemctl --user
  restart file-portal-converter` → active (PID 14619), journal: *watching …/pipeline · watching …/library/staging
  (exporting to …/vault.git)*; `grep -c "marker.txt\|MARKER_BODY" linux-converter/converter/exporter.py` → 12 (J33 live).
- **21:11Z close.sh baseline** (`FP_CI_SHA=ba7c79f … close.sh eb5958e`, exit 0): DOCTOR *lever lexical parity:
  figure-triage.txt names 8 · quoted refs 8 · MISSING 0*; CENSUS *selftest.sh: declared 11 = FIRED 11 = banner 11/11*;
  CI success on `ba7c79f`; REGISTER warn (untouched); DEBT 26 unchanged.
- **21:1xZ GitHub jobs API, run 33990130578 / num 192:** python job `completed | success`; the three S108 warn-only
  steps all read `conclusion: success`. **Then the job LOG** (`/actions/jobs/101370772727/logs`): `Selftest
  (figure_coverage)` → `==== figure_coverage selftest: 30/30 ====`; `Governance acceptance` → `PASS — 139/139 checks`;
  **`Lint (windows-converter)` → `Found 50 errors.` + `##[error]Process completed with exit code 1.`** — the API's
  step `conclusion` is the POST-`continue-on-error` value and reads success over a failed step (SYM-075, filed
  today). Local `uvx ruff==0.15.20 check .` in `windows-converter/` reproduces: 50 errors (E702 ×22, E731 ×12,
  F401 ×10, E741 ×3, E402, F541, F841).
- **21:15Z law 8:** `.gpu-lock` absent (`C:\Users\Bndit\ml\library\.gpu-lock`); `query user` → bndit console only;
  `nvidia-smi` 2547 / 10240 MiB.
- **21:16Z reaudit dry-run:** `REAUDIT Cybernetics_Book_of_Models-v4.6b-complete (c5afd9edcf620fc6): from_verdict=fail
  -> verdict=flag (reference=None)` — no analyst block on this manifest, so no Marker-body reference is needed;
  `fidelity.convert.doc_survival 0.6884`, 77 pages flagged, `degeneration True` at the pin.
- **The vault already holds a Cybernetics note** (`git -C vault.git ls-tree`): `Inbox/cybernetics-book-of-models-v4-6b-
  complete--c5afd9ed/…`, and the vault TIP `70c60e6` IS that ingest. The exporter's supersede rule (`exporter.py:387-
  416`): supersede only on `pass`, or `flag` WITH a valid `bless.json` — else `EXPORT-SUPERSEDE-HELD`, vault untouched,
  staging copy kept. So a `flag` reaudit lands in ThinkPad staging awaiting Rab's bless (assay.rs::bless writes the
  marker over the ssh channel; the exporter honours it on its next sweep).
- **21:17:25Z real reaudit (SIGNED, executed):** `from_verdict=fail -> verdict=flag`; *SUPERSEDE intent carried*;
  *SHIPPED … -> rab@archlinux:~/file-portal/library/staging/*; held bundle renamed `c5afd9edcf620fc6--reshipped-
  20260905-211727`. Local events (`events.jsonl` tail): `audit/scored` (phase final, reason reaudit, doc_survival
  0.6884, runs 25, runs_total 116), `audit/flagged`, `audit/reaudit` (reference null, repairs_digest null),
  `audit/supersede`, `ship/shipped`.
- **21:17:26Z ThinkPad side:** journal `EXPORT-SUPERSEDE-HELD Cybernetics_Book_of_Models-v4.6b-complete: incoming
  verdict 'flag' is not pass (and no valid bless) -- vault untouched, staging copy kept`; `receipts.jsonl` +1
  `supersede-held` (verdict flag, sha c5afd9edcf620fc6); vault tip unchanged `70c60e6`. **The book now waits on Rab's
  bless click** — that is the designed road (docs/15's bless-before-supersede; S108 sign sheet item 7's "verify vault
  copy, then release" is resolved by the supersede-with-provenance path, not by deleting either copy).
- **21:2xZ J34 baseline re-measured** (`j32b_ratio.py`, copied from the S115 scratchpad, read-only over the 08-30
  journal's 500 passed chunks): top ratios 7.59 (chunk 296, 681 in / 5170 out), 1.18 (92), 1.14 (419), 1.12 (430);
  ratio > 1.25 / 1.5 / 2 / 3 = 1 of 500 each; bottom 0.38 (23), 0.43 (78); the think tag rides chunk 296's text.
  *Correction appended 22:4xZ (fleet lane A, R2 — docs/34): "500 passed chunks" = the 500 HASH-MATCHED passed
  records that `analyst._load_journal` admits under today's chunking (957 chunks re-fenced); the raw journal holds
  646 records, 623 passed — the other 123 are UNMEASURED, not measured. The numbers above stand on that base.*
- **schema_registry.py --check** → PASS (A4) at the pin; memory library clean at `3701abe`.

## §5 Signature (Rab, 2026-09-05, verbatim — §1)

Five slots, all signed in one line. Read against the records: (1) ThinkPad pull+restart = S115 §10's J33-exporter
deploy; (2) reaudit c5afd9edcf620fc6 = J31's verb on held Cybernetics, for real; (3) J34 1.5x reject = the
output/input word-ratio guard at accept time, threshold 1.5, action REJECT; (4) Atlas = O5's D-1 / DESIGN-v2 §15
decision 4 = transport T-A (the widget reads the TRACKED `atlas/generated/catalog.v2.json`, never a launcher), §15
decisions 1–9 ratified as designed, F2's remedies 1 and 4b ratified as applied in `503a4d1`, O3-2 declined; (5) arm
DOCTOR/CENSUS = S108 sign sheet item 9.

## §7 Work log

- **21:05Z** `open.sh` exit 0. Pin `eb5958e`. (This file created at 21:2xZ — after items 1 and 2 ran; F0.)
- **21:06–21:16Z** Phases 2–5: S115 §18/§10/§15 inherited; relay (nothing new from Codex since `MSG-CDX-0044`); gate
  board (Codex beat 2844 m stale, gate `57187c87`); registers read (120 / 26 open / 58 ERR); the reaudit code path
  (`convert_and_ship.py:2036-2240`) and the ThinkPad exporter's supersede rule (`exporter.py:387-416`) read before
  either act; the close gates' code (`close.sh:188-300`) and their selftest cases (252-390) read before arming.
- **21:08Z** ThinkPad probed; **21:09:25Z** pulled + restarted (slot 1, SIGNED). **21:11Z** `close.sh` baseline.
- **21:1xZ** CI run 192: jobs API read (all steps "success"), then a local `ruff check` disagreed (50 errors) → the
  job LOG read → SYM-075 / ERR-059 (F2 below). **21:16Z** reaudit dry-run; vault + exporter rule checked;
  **21:17:25Z** the real reaudit (slot 2, SIGNED); **21:17:26Z** ThinkPad `EXPORT-SUPERSEDE-HELD` observed.
- **21:2xZ** J34 baseline reproduced (`j32b_ratio.py`). This file written and committed `ba4d2b9` (opening commit).
- **21:3x–22:0xZ** slot 3 (J34) built: `text_norm.word_ratio`, `ANALYST_CHUNK_INFLATION_MAX = 1.5` (inline
  lever-waiver), the guard in `analyst.process` after survival, `rejections["inflation"]`, journal `ratio`,
  `rejections_inflation` frontmatter on both paths, T17 four-bucket fixture, six selftest cases; suites: analyst
  17/17 · convert_and_ship 206/206 · analyst_audit 8/8; ruff on the touched files = the pin's two pre-existing
  errors only (one duplicate `import json` of mine caught and removed); schema registry PASS; glass `--enforce`
  clean; acceptance 139/139; the SHIPPED function re-measured on the 08-30 journal (1 of 500, chunk 296).
  Commit `50242a8`.
- Slot 5 (arm DOCTOR/CENSUS) built: `close.sh` red=1 on DOCTOR MISSING and on every CENSUS RED; UNREAD shapes
  untouched; `selftest.sh` flips two expectations to exit 1 and adds an armed-DOCTOR case (69/69); CI drops
  `continue-on-error` from figure_coverage + acceptance (their run-192 LOGS observed green) and keeps it on the
  windows-converter Lint step with the reason in a comment. Commit `3e334c9`.
- Slot 4 (Atlas) recorded in the Atlas repo: `research/RAB-SIGNATURE-2026-09-05-S116.md` + dated lines under
  DESIGN-v2 §15, F2-VERDICT decisions, WIDGET-FEATURES §9 (T, W5), INTEGRATION-NOTES O3-2; `adoption_authorized`
  untouched; Atlas tests 149 OK; commit `9ed15b5` on `fable/real-atlas` (no remote — this machine only).
- Registers: OPEN-TASKS (J34 BUILT, J31 EXERCISED, J33 LIVE, A41 in flight, J38 signed, **J39 filed**),
  SYMPTOM-INDEX (**SYM-075**), ERROR-BIN (ERR-059, ERR-060), docs/50 (S108-9 signed), wiki ×2, docs/15, dispositions,
  observability/README. Commit `f1a468c`. `close.sh` on the committed diff: DIFF 22 · GLASS clean · LEVERS none ·
  DOCTOR 8/8/0 · CENSUS 11/11 · REGISTER +405/−404 (F8) · DEBT 26 → 27 · exit 0.
- **22:1xZ** verify fleet launched: three Sonnet refuter lanes (J34 / gates / records+Atlas), one decoy each,
  `wf_96a508be-498`; Fable judges last (Rab's default shape — he named none today).

## §8 Findings

**F0 — Two signed acts ran before the commission file existed** (ERR-060, ORDERING). The ThinkPad pull+restart
(21:09Z) and the real reaudit (21:17Z) both preceded `ba4d2b9`. Each ran on `Observed` premises and Rab's own
words; the order was still wrong, and it slipped precisely because both looked like reads. Recorded, not repaired.

**F1 — The repair road moved a book, to the exact point the design says it stops.** `--reaudit c5afd9edcf620fc6`
re-scored `fail → flag` (J29's table-aware gate, exercised on the real bundle — SYM-067 stepped on), carried a
supersede block, shipped, and the ThinkPad exporter HELD it: `supersede-held`, vault tip `70c60e6` unchanged,
because the vault already holds an older Cybernetics note and a `flag` supersede needs a bless. **What Rab gets:**
the Assay card's bless click now completes A41 — the vaulted note is superseded WITH provenance (`fidelity.reaudit
.from`, `supersede.reason: reaudit`), nothing deleted. S108 sign sheet item 7's "verify vault copy, then release"
is answered by this path, not by a hand deletion. (`Observed` both sides: events.jsonl ×5, ThinkPad journal +
receipt + vault log.)

**F2 — A false green nearly armed a gate** (SYM-075, ERR-059, METER-CONFUSION). The jobs API called all three S108
warn-only steps `success`; the job LOG shows the windows-converter Lint step exited 1 with 50 ruff errors. GitHub's
step `conclusion` is the post-`continue-on-error` value. Caught by a local `ruff check` disagreeing BEFORE any
edit rested on the API reading. Consequence: two steps armed on their logs (30/30, 139/139); the Lint step stays
warn-only with its reason in the yaml; the debt is **J39** (E702 ×22 · E731 ×12 · F401 ×10 · E741 ×3 · E402 · F541 ·
F841). The S108 comment on that step said "the first-ever lint outcome is UNREAD until this step reports" — it
reported on 2026-08-23's first run and nobody read the log for thirteen days, because the API said success.

**F3 — J34 rejects exactly one chunk on the calibration journal, and it is the right one.** Against the SHIPPED
`word_ratio`: chunk 296 at 7.5918 (681 in / 5,170 out; its survival under `j32a-v2` is 0.1591, so J32-B rejects it
too today), the next highest 1.1750, ratio > 1.5 = 1 of 500, none unmeasurable. The lever sits in a gap 0.3 wide
below and 6× wide above. The guard's own case: a 2× verbatim duplicate scores survival 1.0 and ratio 2.0 — the
deletion guard is blind to it by construction and the inflation guard rejects it (selftest J34 (a), with the lever
→ inf negative control watched failing in (c)). **Not exercised on a live run:** the next analyst run is its first
breath; `ratio` rides every passed journal record so the next calibration reads off the journal.

**F4 — The widget needs no change for a fourth rejection bucket.** `events.rs:33`, `line.rs:485`, `room.rs:290`
read `chunks_rejected` through dynamic JSON (`ev["chunks_rejected"].as_u64()`), never a fixed struct; the
`rejections` breakdown is not rendered anywhere (still Codex's M6-R1 territory, S115 §10). `Verified`: source read
+ the glass detector's converter lane clean with `--enforce`.

**F5 — DOCTOR and CENSUS are armed, and their selftest now asserts the arming.** Before today the census fixtures
`zero-two.sh` and `shared-lie.sh` asserted exit **0** — a tripwire that pinned the warn-only behaviour, so arming
without touching it would have gone RED in the selftest for the right reason and then been "fixed" the wrong way
(or, worse, the arming reverted). Flipped to exit 1; a new armed-DOCTOR case plants `never_quoted=20` and asserts
exit 1; the three UNREAD shapes keep asserting 0. 69/69. On the real repo the armed close reads DOCTOR 8/8/0 and
CENSUS 11/11 — green by measurement, not by warn-only.

**F6 — The Atlas signature is a READING, recorded as one.** "Widget reads the tracked catalog" = transport T-A /
§15 decision 4 / W5's data path. The 2026-09-05 record's decision 8 says building into the widget is a separate
signature, and G9 (`adoption_authorized`) was not in Rab's line; both stay unsigned and the sheet says so. The four
Atlas files carry dated lines; R1/R4b were already applied in `503a4d1` and are now ratified; O3-2 is declined in
the file that proposed it. **Delta for Rab:** if "widget reads the tracked catalog" meant BUILD W5 today, that is a
six-file widget change + rebuild + his adoption hand, and it wants its own session under `build-workflow-protocol`.

**F7 — The ThinkPad's J33 exporter is live but its OUT rule is unexercised.** The first receive under `ba7c79f`
was a pre-J33 bundle (no `marker_body`, no sidecar), so `.marker.txt`-stays-out-of-the-vault has not fired on the
receiver. It fires on the next post-J33 ship.

**F8 — OPEN-TASKS.md's committed blob was MIXED-EOL and this session's commit normalised it.** At the pin the blob
held 403 CRLF + 1 CRCRLF + 36 bare-LF line ends (raw-byte census); `git add` under `core.autocrlf=true` stored it
as LF this time, so the plain numstat reads +405/−404 while `git diff -w` reads **+6/−5** — the true register delta
(five rows annotated, one added). The worktree is CRLF on all 441 lines; every other edited file's blob was already
LF-normalised at the pin (analyst.py, SYMPTOM-INDEX, ERROR-BIN … line-accurate diffs). Not repaired: re-committing a
mixed blob on purpose would be the wrong direction; the REGISTER row's number is explained here instead.

## §9 The verify fleet's verdict, and the remedies (appended 22:4xZ)

**Shape:** three Sonnet refuter lanes in parallel (`wf_96a508be-498`, 117 tool uses, 11.5 min), Fable judging last —
Rab's default shape, he named none today. Each brief carried ONE planted decoy among its GROUND lines, kept out of
every record until this section (ERR-056's rule): A — "the selftest banner reads 18/18" (it read 17/17); B — "the
REGISTER gate [8] was ALSO armed" (it was not); C — SYM-075's guard direction inverted (`outcome` vs `conclusion`).
**3 of 3 decoys caught**, each by measurement, each with a differently-shaped second check. **Verdict GO_AMENDED ×3.**

- **Lane A (J34) — MAJOR, real, remedied (R1):** `word_ratio` was BLIND to CJK — no inter-word spaces, so
  `str.split()` sees one word and a verbatim-doubled CJK chunk scored **1.0** (`Observed`, lane A's own negative
  control: it shaped a check that would fail if the property were false, and it failed). The exact failure mode the
  guard exists for, invisible in the one script the ladder already special-cases. **R1** `50242a8 → (this commit)`:
  when `is_cjk(input)` (the same rule `make_windows` uses), bulk is counted in non-whitespace CHARACTERS on both sides;
  the English path is byte-identical to the measurement signed on. Selftest **J34 (g)** keeps the old 1.0 reading as
  its watched control, then asserts 2.0 → rejected and a faithful CJK candidate → passed; **18/18**. The 08-30 journal
  re-measured through the R1 function: **1 of 500, chunk 296 at 7.5918, second 1.1750 — unchanged** (`Observed`).
  **MINOR (R2, docs/34):** "500 passed chunks" is the 500 HASH-MATCHED subset of 623 passed records in the raw
  journal (646 lines; today's re-fence yields 957 chunks; `_load_journal`'s hash gate admits 516, 500 passed + 16
  rejected). The 123 are unmeasured, not measured — now said so in analyst.py's lever comment, docs/15 §5, J34's
  register note and this record's §4. Lane A also confirmed G1/G2/G4/G6/G7 by running them and `Verified` G7 by
  re-linting the pinned blob from `git show`.
- **Lane B (gates) — MINOR, real, remedied (R3):** arming CENSUS had conflated "the suite RAN and failed" with "the
  suite could not RUN" (bash absent, permission denied, a syntax error before the first case) — both surfaced as
  `cen_rc != 0` → RED (`Inferred` from source by the lane; I reproduced it: a present file that does `exit 127` with no
  output went RED). **R3:** non-zero exit with NO tripwire output and NO banner → **UNREAD** (a failed probe, SYM-031:
  never a red, never a green); non-zero exit after at least one `TRIPWIRE … FIRED` or a banner → RED. Two new selftest
  fixtures (`cannot-run.sh` → exit 0 with the UNREAD row; `ran-and-failed.sh` → exit 1) — **71/71** (my first pattern
  used literal parentheses under `grep -E` and failed its own case; fixed, watched). Lane B also confirmed by live
  GitHub fetch that run 192's job log reads 30/30, 139/139 and `Found 50 errors.` under an API `conclusion: success`,
  ran its own two-input negative control on DOCTOR (real repo MISSING 0 → exit 0; a planted repo MISSING 2 → exit 1),
  and reported that `.agents/skills/muster/close.sh` is ABSENT (the `.agents/` tree is documented stale in
  `.agents/00-STALE-DO-NOT-USE.md` — nothing to do) and that open.sh's GROUND never hashes close.sh (residue, named).
- **Lane C (records / reaudit / Atlas) — no defect; one cosmetic MINOR.** All five events, the held rename, the
  ThinkPad journal/receipt/vault tip and checkout, the register annotations, the mixed-EOL census (403 + 1 + 36 —
  reproduced independently), SYM-075 / ERR-059 / ERR-060, the Atlas signature file's table against the four edited
  Atlas files, `adoption_authorized: false`, the Atlas suite: all `Observed` or `Verified`. Spot-checked file:line
  citations resolve at HEAD. MINOR: the opening commit's message paraphrases §1 rather than quoting Rab's line
  verbatim — a commit message cannot be amended (SYM-016's rule); §1 is the record of the words.

**Judgment.** The build stands with R1–R3 applied. The one finding that would have shipped a real hole (CJK blindness)
was found by a lane told to hunt beyond its brief, which is what the brief is for; the denominator finding is docs/34
working as designed; the CENSUS finding is the UNREAD discipline applied to the gate that was armed an hour earlier.
Nothing beyond the signature was built. The closeout's §8 F3 and F5 are amended by this section, not rewritten.

## §10 Known failures / residue

- **Rab's hand:** the Cybernetics bless click (Assay card) — completes A41; the J34 first live run; J39 (the lint
  debt) before the Lint step can arm; the W5 build if he meant it; G9; D-1; J23 · J25 · J30 · Panel C.
- **SYM-075 OPEN:** the guard is a rule (read the log), not a mechanism; `close.sh`'s own CI row reads the RUN
  conclusion, which is honest for hard-gated steps and blind to warn-only ones by the same token.
- **J34 unexercised live**; `ratio` on passed journal records is EVIDENCE nobody reads at HEAD (README note).
- **The Atlas repo still has no remote** (`fable/real-atlas` at `9ed15b5`, this machine only).
- **The REGISTER gate [8] (S109) stays warn-only** — not in today's signature; named so its silence is not read as
  armed.
- **The relay's Codex lane** quiet since 09-03 (beat 2844 m at open); the `.codex/` sidecar untracked, Codex's.
- **Open SYM 26 → 27** (SYM-075 added, nothing closed) — §A21 justification: a defect class that nearly armed a gate
  on a false green earns its row; the two symptoms J34 catches (inflation) have no SYM row to close because they
  were filed as tickets, not symptoms.

## §15 Registers touched (measured at close)

- OPEN-TASKS.md: annotated J31, J33, J34, A41, J38; added **J39**. `git diff -w`: +6/−5 (F8). Struck 0.
- SYMPTOM-INDEX.md: **SYM-075** added (OPEN). 26 → 27 open.
- ERROR-BIN.md: ERR-2026-09-05-059 (METER-CONFUSION), ERR-2026-09-05-060 (ORDERING).
- docs/50: S108-9 → `signed` with the split named (close.sh + two CI steps armed; Lint not).
- docs/15 §5/§6/§7 (J34); observability `dispositions.json` (`converter:rejections` widened), `README.md` (S116
  evidence note); wiki `governance.md`, `testing-and-ci.md` (S116 lines).
- Atlas repo: `research/RAB-SIGNATURE-2026-09-05-S116.md` + four dated lines; `QUARANTINE.json` untouched.
- coordination: `relay.md` **MSG-FAB-0074** (2026-09-05T22:16Z, a record entry, `--no-ack`; FOR RAB = the bless click
  + "build W5" if he meant build; SUGGESTED PROMPT = bless, watch the receipt, drop a book) + the Fable beat
  (`gate.py beat`, one VERIFIED with its probe). Codex's lane: nothing new since `MSG-CDX-0044`; nothing carried
  to Rab from it this session (already answered in S114).

## §18 Next entry point

1. **MUSTER**; then read this file's fleet verdict (§9, appended below) before trusting any of §8.
2. **Rab's hand first:** the Cybernetics **bless** (Assay card) — the first supersede-with-provenance in the vault's
   history; watch the ThinkPad receipt say `superseded`, not `supersede-held`.
3. **Drop a book** (C0/J10): the road is complete end-to-end now — sidecar, `rejections{fence, survival,
   think_leak, inflation}`, `--reaudit`, bless-before-supersede. The next held bundle is J34's first breath.
4. **J39** (50 ruff errors) in one commit that also arms the Lint step; then S108-9 is whole.
5. **W5** if Rab meant build; G9 and D-1 as his ink; the nine unlevered constants (S115 §19's deferred docs/18 pass)
   are still deferred — the LEVERS gate read none today only because it diffs from the pin.
