# S183 — Desktop — 2026-09-17 (05:04Z) — TWO SYMPTOM ROWS READ AGAINST THE ACCEPTOR THAT CLOSED THEM; THE LADDER'S SUITE ONTO THE RUNNER (Claude Opus 5's thirtieth sitting in the Fable lane; the autonomy experiment's thirty-eighth)

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

No new word of his (the Desk inbox read at 05:03Z: nothing since 657233f5); his seven of the night stand — the restart window OPEN ("If any of tasks touch the converter and need live runs or anything do it." — Desk 8ca8279b), "if something isn't working the way it suppose to or could be fixed to run more logically and better… fix it" (cdaf6619), the proposal form (e1bd1e1e) — under his standing words ("Run autonomous until I say stop… Every episode rewards a cookie. Record all receipts." · AUTO MEANS NEVER IDLE).

S182's carry-over set the order: **(0)** the fold (the room 292 — a block does not fit); **(1)** §C's next mechanism in the lane's layer; **(2)** the `[3c]` gap (the ladder's suite is not on close.sh's runner list). The §C read at this open (the 28 open rows' heads; SYM-091, SYM-104, SYM-096, SYM-097 whole) found two rows whose REMEDY was built on 2026-09-12 (J46, S140 — the diff-whitelist acceptor: "everything else is REVERTED: word substitutions, deletions, insertions, numeral changes") and whose cells still read OPEN pointing AT that remedy: **SYM-091** (a partial deletion passes the per-chunk guard's blind zone and ships) and **SYM-104** (a paragraph missing or a numeral rewritten `100` → `10` ships past the guard). The acceptor's own suite already proves the reverts (`edit_whitelist_selftest`: numeral, a paragraph's deletion, a deletion at the chunk's start/end — 54/54 last run), and the analyst's own path proves it live (`analyst_selftest` J46 (a): "a numeral change and a deletion reverted" through `process()`). The rows were never re-read against the build. That is E1. SYM-096 (the LEVERS gate's regex cannot see a constant inside a dict literal, a call default or a tuple — OPEN by design) and SYM-097 (Historical — a one-off instrument) read; SYM-096 is the next mechanism after these, a later sitting or E3. His word, if it arrives, outranks this order.

## §2 Starting State — the `open.sh` card, verbatim (2026-09-17T05:03:5xZ)

```
════════ MUSTER · OPEN · 2026-09-17T05:04:48Z ════════
[0] IDENTITY
    ledger newest    S182  4637f69
    this session     S183  ·  desktop  ·  2026-09-17
    session marker   coordination/private/session.current = S183 (guard_record / guard_git read it)
    advisory         S183 unnamed in the inherited tree
[1] GROUND
    verifier         07BD3BA7  (repo == ~/.claude)
──────── MUSTER · 2026-09-17T05:04:49Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 234 (tally == hook)
[3a] LEDGER ORDER  ✓ 165 Desktop-lane rows ascend (S16 → S182)
[3b] LEDGER PARSE  ✓ 165/191 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · unchanged since the close 4637f69 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ 4637f69 (S182) == ledger 4637f69, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 8 · anchor 25 · pending 0 · drop 0
    intake           waiting 0 · oldest 0s · active null · written 5s ago by pid 23248
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    events           437 line(s)
    open-tasks       96 item(s) · last written 16 minutes ago
    error-bin        109 row(s) — read the CLASS table before you probe · last written 6 hours ago
    symptoms         133 row(s), 28 open · last written 18 minutes ago
    relay            208 entries · run `gate.py status` for the board
    widget           10056
    widget autostart task 'File Portal widget (J57)' Running — AtLogOn · auto-logon ON (Bndit)
    task             'File Portal desk (relay)' Running LASTRUN=2026-09-15T22:51Z RESULT=267009
    task             'File Portal relay watch (Fable)' Ready LASTRUN=2026-09-15T22:50Z RESULT=0
    task             'File Portal tracker' Running LASTRUN=2026-09-15T22:51Z RESULT=267009
    task             'File Portal desk watch' Running LASTRUN=2026-09-15T22:51Z RESULT=267009
    python procs     10
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    adoption         C4559805 (2026-09-04T02:3xZ, by rab) · installed C4559805 — MATCH
    vault            7 note(s) · tip f489189
    thinkpad         converter=active · vault tip f489189 · staging 0 · receipts 25
[3] PIN
    --since          4637f69
    closeout         sessions/S183-desktop-2026-09-17.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

**The pin:** `--since 4637f69` (S182's closing SHA; the row `a7919d1`; CI green run 35183752313 / num 518). The marker `coordination/private/session.current` = S183. The card's `1 uncommitted` is the untracked `.codex/` (not the lane's — S181 §10). No record exists under S183 before this file.

## §3 Inherited, stamped Historical

- **S182's post-close CI** (8e961e8): `ci_observe.py` launched 05:01Z in the background (`sittings/S182/ci_observe_post.txt`) — read at this sitting's §20. S182's Gmail draft r3500726243439789296 (never sent).
- **The index (Observed 05:03Z):** MEMORY.md `89a20ca` at 17,218 of 17,510 — 292 bytes; the fold FIRST: S182 + S181–S178 → one `S182–S178` line, the ten SHAs kept.
- **SYM-091 as the index holds it (Historical):** "A book fails the analyst-stage audit for real deletions that each PASSED the per-chunk guard — a chunk that loses one paragraph of five scores ~0.8 and passes … **OPEN** by construction — the lever is Rab's (J32); the remedy is J46's diff-whitelist acceptor". **SYM-104:** "A chunk passes the per-chunk survival guard (0.80–0.82) and the analyst audit, yet a paragraph is missing from its output or a numeral was silently rewritten (`100` → `10`) … **OPEN** — J45 (per-chunk digit multiset) + J46 (the diff-whitelist acceptor)". Both filed S117/S118; J46 BUILT S140 (`edit_whitelist.py`, policy FULL ships; `analyst.process` reconciles every ACCEPTED chunk against its input — `rungs = ew.FULL_TABLES if tables else ew.FULL`, no lever turns it off); the per-chunk guard's blind zone itself is unchanged (the lever his) — the DEFECT (a deletion or a rewrite SHIPPING) is what J46 closed, by a different mechanism than the rows imagined.
- **The `[3c]` list as close.sh holds it (Historical, line 156):** nine Python suites + the bench's node suite; `analyst_audit_selftest.py` (the ladder's — 20 cases, FP_PIPELINE quarantined since S182) and `edit_whitelist_selftest.py` (the acceptor's — 54, pure functions) are NOT on it; the row's tally grep already reads both shapes (`ALL TRIPWIRES FIRED — n/n`, `n/n green`). `selftest.sh` cases 61–63 plant a failing suite through `FP_CONV_SUITES` and do not depend on the list's members. The row is WARN-ONLY; arming (`FP_CONV_ARMED=1`) is his — a list entry is not an arming.
- **The converter, as S182 left it:** the watcher on `005382d` (writer 23248, detached); the child carries B27 and the ladder lever (OFF — `ladder.txt` absent); the intake idle (`held 8 · anchor 25 · pending 0 · drop 0`); `audit=enforce · analyst=local · batch=8`.
- **The lessons in force:** the record before any act; two calls at the open; a script by path; ERR-009 — the Edit tool or a file by path, never a heredoc with escapes; `close_ep` as its own call; a symptom row's FIRST marker is its status and `Was:` opens superseded text; FIXED needs the tripwire NAMED and RUN this sitting; no `sed -i` on a CRLF file (bytes in, bytes out; `od` the probe); a cookie's `until` READ from the receipt's Window line; the intake key `card_state`; `ci_observe.py` in `auditing/`; `git add` by NAME beside `.codex/`; the Desk watcher (bkz7v2rt3 armed 05:03Z, cap ~05:33Z — re-armed at expiry).
- **Rab's standing words in force (Historical, unchanged):** the persistent method; AUTO MEANS NEVER IDLE; scripts private; the GPU law (no engine, no model of the lane's without his word — the proofs here are suites with fake backends); a UI click on his screen is his hand; a Desk post answered on the Desk only; nothing to Codex; a cookie per episode; a gate change warn-first with his signature (a runner list entry under a WARN-ONLY row is not a gate change; the ARMING stays his).

## §4 The echo (what each act must be, before it is done)

**The fold (E0).** `compact_memory.py ">   **S182 (" ">   **S177–S174**"` → one `S182–S178` line ≤ ~450 bytes keeping `4637f69`/`a7919d1` · `32dd0df`/`afefd63` · `f2b36e8`/`ca58709` · `2d266a6`/`8115ec3` · `b9744b7`/`0c95ad9` (J44 built OFF, the audit's share; B27 applied; B31; docs/64; the alarm read; the HTML page's author); the MUST list first (S182's + the S182 line's words), the dry run, the real number freed said, the read-back check; a library commit, no cookie. **SYM-091 and SYM-104 read against J46 (E1).** `e1_acceptor_proof.py`: the two rows' defects as FIXTURES through the REAL live path — `analyst.process()` with a FAKE backend (no model, no network; the pattern of `analyst_selftest`'s J46 (a)) that (a) drops one paragraph of five from the chunk it is given (SYM-091's shape — the per-chunk guard reads ~0.8 and PASSES it; the acceptor must revert), (b) rewrites `100` → `10` (SYM-104's shape — no window moves; the acceptor must revert); the shipped text compared to the input; `meta["edits"]["reverted"]` read — `deletion` 1, `numeral` 1; and the NEGATIVE CONTROL that the guard alone would have passed both (`tn.chunk_survival` ≥ the lever on (a); 1.0 on (b)) — the blind zone is real and unchanged, the acceptor is what closes the defect. Both suites run this sitting (`edit_whitelist_selftest` 54/54; `analyst_selftest` 49/49). Then the two rows' FIRST markers → **FIXED S140 (J46) — read S183 E1** with `Was:` before the OPEN text, the cases NAMED (the suites' + this sitting's fixture, which joins `analyst_selftest` as a case if it is not already one — ONE `@case` per shape); open SYM 28 → 26; the guard's lever (J32) untouched and said. **The `[3c]` gap (E2).** `e2_runner_list.py`: `analyst_audit_selftest.py` and `edit_whitelist_selftest.py` appended to `CONV_SUITES` in `close.sh` (LF; the anchor once); `selftest.sh` run whole (its own count read, never typed); a close-card dry run of the `[3c]` row through `FP_CONV_SUITES` unset (the real list) — eleven Python rows clean; the register row `close/ladder-and-acceptor-suites-on-the-runner` ADDED and STRUCK in the same cell (the honest touch); the arming untouched. **Not this sitting:** SYM-096 unless the two above close early (E3: an AST census of Python numeric literals in dict values / call defaults / tuples over the added hunks, WARN-ONLY beneath the LEVERS row, with its selftest — a helper `observability/lever_census.py`, not a rewrite of the gate); a `pass` through the intake; any engine run; a lever flipped; the widget.

## §5 The method

Episodes with receipts (`close_ep.py 183 <K> <since> "<category>"` — its own call; the first `<since>` = S182's whole receipt END 2026-09-17T04:53:23.343Z), a cookie per episode (E0 none), a Desk line per episode, the carry-over updated; then the close through the tool, the driver's checker first; the block ≤ ~330 bytes.

## E0 — the fold (05:06Z; a library commit, no cookie)

`compact_memory.py ">   **S182 (" ">   **S177–S174**" s182_178_short.txt` — the dry run, then `--go`: S182 + S181–S178 → one `S182–S178` line (the ten SHAs; the audit's share; the lever; B27; B31; docs/64; the alarm; the HTML page's author). `e0_fold_check.py` (S182's MUST list + the S182 line's ten): **bytes 16,956 (was 17,218) · room 554 · missing 0 · below `## General` unchanged True**. Library `9bb8997`, pushed. The real number freed: 262. S182's post-close CI (8e961e8): GREEN — run 35184127597 / num 519 (read 05:07Z).

## Rab's word, 05:07Z, in chat (verbatim) — it outranks §4's order

> @"C:\Users\Bndit\Downloads\Document Layout Analysis Copy 2.pdf" @"C:\Users\Bndit\Downloads\Document Layout Analysis.pdf" @"C:\Users\Bndit\Downloads\Demystifying Graph Databases Analysis and Taxonomy.pdf"
> choose any of these to test the widget for a full run start to finish

A chat message is answered in chat (the channel policy). It is the `pass` through the intake the register held for his word — a real book of HIS, on his word, through the widget's own intake, allowed to reach the vault. §4's E1 (SYM-091/104) steps back: nothing of it was written (the case-adding script's dry run had stopped on a strict assertion — `analyst_selftest.py` carries one bare-LF line, 890, since S146's `ac58b82`); it becomes E2 if the run leaves time.

## E1 — the full run on his word: "Document Layout Analysis.pdf" through the widget's intake (05:08Z → )

**The three files read, not guessed (`e1_pdf_probe.py` → `.txt`, pymupdf, read-only):** (1) `Document Layout Analysis Copy 2.pdf` — 2,294,538 bytes, sha16 `fb31ba3ecfad8d05`, **82 pages**, producer `Qt 6.9.1`, no title, the first page "Book cover of Document Layout Analysis" (a re-render/export of the same book — a different page count; a dedup/supersede test for another day); (2) **`Document Layout Analysis.pdf` — 2,380,256 bytes, sha16 `45994b30f52baffe`, 92 pages, `Adobe PDF Library 10.0.1`, title "Document Layout Analysis", SpringerBriefs in Computer Science (Showmik Bhowmik) — a BOOK with a text layer (35–1,918 chars on the front pages), figures, the vault's kind**; (3) `Demystifying Graph Databases…` — 1,476,119 bytes, sha16 `5ee58f1d5162fa8f`, 46 pages, `pdfTeX`, an ACM survey (Besta et al.) — dense text, no images on the front pages, hundreds of citations (the ladder's cite class, which under the OFF lever the audit still counts as loss).

**The choice: (2), the Springer book** — the corpus's kind (a book, a text layer, figures and tables — the figure-coverage and table layers engage), long enough to be a real run (92 pages; Marker at the measured 37–72 ms/page ≈ a minute or two; the local analyst on ~100 chunks at the measured ~14 s mean ≈ 20–30 min), and the one whose fidelity the vault would want. The survey would end in `held/` on the cite class alone (the lever OFF — a reading, not a run); the Qt copy is a duplicate's test.

**Preconditions (Observed 05:08:34Z):** the intake `card_state idle · active null · waiting 0` (writer 23248 — the watcher on `005382d`, the child spawned per conversion carries B27 and the ladder lever, OFF: `ladder.txt` absent, `regex_id` will read `j32a-v2`); `.gpu-lock` absent; the levers `audit=enforce · analyst=local · batch=8` (a `fail` is PARKED in `held/`, a `flag`/`pass` SHIPS; the analyst auto-routes local — no card wait); the seat: `bndit console Active, idle none` — he is at the desk (he just typed); the card `1550 MiB of 10,240 at 45 %` before the drop (something of his on it — the run will share); the ThinkPad `converter=active · staging 0 · receipts 25 · vault tip f489189`; `events.jsonl` line count read before the drop by the script. **The act:** the PDF copied by name into `C:\Users\Bndit\ml\library\drop\` — the widget's own intake (the ⚡ tile's drag lands the file in the same folder; the tile drag itself is his hand) — then the events watched (a Monitor on `events.jsonl`, re-armed at each cap), the Dock's card state read from `.intake-state.json`, the manifest read at the end from `anchor/` or `held/`, the ThinkPad's staging and vault tip read after a ship. Where it stops is the report.

## Rab's second word, ~05:13Z, in chat (verbatim) — while the first run converts

> @"C:\Users\Bndit\Downloads\Data Science for Business What You Need to Know About Data Mining and Data-Analytic Thinking (Foster Provost, Tom Fawcett).pdf" @"C:\Users\Bndit\Downloads\Automate the Boring Stuff With Python, Third Edition Practical Programming for Total Beginners (Al Sweigart).pdf.pdf" @"C:\Users\Bndit\Downloads\Naked Statistics Stripping the Dread from the Data (Charles Wheelan).pdf" @"C:\Users\Bndit\Downloads\Information Dashboard Design The Effective Visual Communication of Data (Stephen Few).pdf" @"C:\Users\Bndit\Downloads\Data Science with R A Step By Step Guide With Visual Illustrations and Examples (Andrew Oleksy).pdf"
> choose any of these to test the widget as well. Whatever you think you want to also just have in md form to read from because it might be useful for you who knows.

His word: any of the five, for the widget's test and for the lane's own reading in markdown. Real books of his, on his word — the vault is where they go if they pass; `held/` if they fail (the lever `audit=enforce`), readable either way.

**The five read (`e3_pdf_probe.py` → `.txt`, read-only), each with a text layer:** *Data Science for Business* (Provost & Fawcett) — 409 pages, 19.6 MB, sha16 `5414cc78e6b8a6c6`, figure-heavy (it-ebooks); *Automate the Boring Stuff*, 3e (Sweigart) — 675 pages, 24.1 MB, `5d4b45acd9cfdef6`, code-heavy (Adobe/iText); *Naked Statistics* (Wheelan) — 285 pages, 2.2 MB, `7daeccc14ce57117`, a calibre conversion (prose, few images); *Information Dashboard Design* (Few, O'Reilly 2006) — 166 pages, 8.7 MB, `18604403df8087c5`, Distiller, images on most sampled pages, non-breaking spaces through the text (NFKC folds them — the ladder's `_common`); *Data Science with R* (Oleksy) — 201 pages, 8.2 MB, `d70aa28482fb732f`, calibre, code and plots.

**The order chosen, and why:** (1) **Information Dashboard Design** — the figure-coverage and vision layers' hardest class (charts on most pages), 166 pages (the analyst ≈ 40 min at the measured ~14 s a chunk), and the one most useful to the lane's own work (the Room is a dashboard; docs/16's laws cite Few's lineage); dropped NOW, while the first run converts — the watcher's queue (`waiting 1` on the Dock) is itself a widget behaviour to see; (2) *Naked Statistics* (prose; calibre's shape); (3) *Data Science with R* (code + plots); (4) *Data Science for Business* (409 pages ≈ 1.5 h of analyst); (5) *Automate the Boring Stuff* (675 pages ≈ 2.5 h) — the two giants last, each dropped only when the card is free of the previous, one book at a time after the queue test. **The cost, said:** the analyst is the card at full draw for the whole of each book; five books ≈ 6 h of his card. His word covers it ("test the widget as well"); the seat is his — if he takes the card back, the run yields (the deferral gate reads `chat-hold.json`; a kill is his hand) and the record says where it stopped.

## E2 — SYM-091 and SYM-104 read against the acceptor that closed them (05:11Z → 05:15Z; done beside the run, on the CPU, with a fake backend)

**The fixtures, through the REAL live path (`e1_acceptor_cases.py --go`: two `@case`s into `analyst_selftest.py` beside J46 (c); the file's one bare-LF line 890 — since S146's `ac58b82` — left as it is, the insertion CRLF; then `e1_case_104_fix.py`):** **S183 (SYM-091)** — five paragraphs of 24 words (ten windows); the fake backend drops the third; `tn.chunk_survival` reads **0.8** and the guard PASSES it (`0.8 ≥ ANALYST_CHUNK_SURVIVAL_MIN 0.80` — the blind zone, real and UNCHANGED; the lever his); `process()` ships the chunk with **all five paragraphs** — `reverted {deletion: 1}`. **S183 (SYM-104)** — four paragraphs + "the sample held 100 units in total" (nine windows); the backend writes `10 units`; **ONE window fails, the guard reads 0.8889 and PASSES**; `100 units` ships — `reverted {numeral: 1}`. The first draft asserted the guard reads 1.0 on the numeral (the row's own words: "one changed digit changes no window's membership") and the suite said 0.8 on a five-window chunk: a changed digit DOES fail the window that holds it; what the row meant is that the fraction barely moves in a many-window chunk — the case now says so with its denominator. **`analyst_selftest` 49 → 51/51** (the marker-env interpreter; J46 (c) the watched negative control beside them — reconcile replaced by identity, the edit ships); `edit_whitelist_selftest` 54/54 (05:04Z).

**The index (`e2_sym_fixed.py --go`; the first markers read OPEN before the write; CRLF 161/161):** SYM-091 and SYM-104 → **FIXED S140 (J46, signed Rab 2026-09-12) — read S183 E2**, the cases named, the OPEN text after `Was:`. What closed is the DEFECT — a deletion or a rewrite SHIPPING — by the acceptor at accept time; the guard's resolution is what it was, and its lever stays his. J45's per-chunk digit multiset was never needed for the numeral: the acceptor sees a digit as a substitution.

**Lessons:** a row that names its remedy is re-read the day the remedy is built — these two waited seventeen sittings; a fixture asserts what the instrument reads, not what the row said it reads (1.0 → 0.8889, the denominator with it); a symptom is closed by the mechanism that stops it shipping, and the record says which mechanism and which lever it is not.

## Rab's third word, ~05:24Z, in chat (verbatim)

> @"C:\Users\Bndit\Downloads\20250121-EB-Developers_Guide_to_RAG.pdf"
> oh and this too if it is relevant anymore

Read (`e3_pdf_probe.py`, appended to `.txt`): 22 pages, 16.2 MB, sha16 `002ed5ac1af8420d`, `Adobe PDF Library 17.0`, Confluent's *Developer's Guide to RAG* (2025) — a text layer, images on most pages, short. Relevant twice over: a shape the corpus lacks (a short, image-heavy e-book) and reading the lane would use. It joins the order after Few's book and before the giants — the analyst on 22 pages is minutes.

## E3 — the `[3c]` gap: the ladder's and the acceptor's suites onto the runner (05:15Z → 05:26Z; beside the run)

`e3_runner_list.py --go` (the dry run first; `close.sh` LF, both anchors 1×): `analyst_audit_selftest.py` (J32-A/J44 — FP_PIPELINE quarantined at its top since S182) and `edit_whitelist_selftest.py` (J46 — pure functions) appended to `CONV_SUITES`, the S183 note beside the S169 one; their tally shapes (`ALL TRIPWIRES FIRED — n/n`, `n/n green`) are read by the row's existing grep. `bash -n` parses; **`selftest.sh` 127/127** (Observed 05:24Z, `e3_muster_selftest.txt` — the count from its banner). The `[3c]` row stays WARN-ONLY; `FP_CONV_ARMED` untouched — a list entry is not an arming. `e3_register_row.py --go`: the TICKETS table gains `close/ladder-and-acceptor-suites-on-the-runner`, ADDED and STRUCK in one cell — the measure is this sitting's own close card carrying the two new `CONV` rows.

## E1 — the run so far (Observed from `events.jsonl`, the progress files, the card; 05:10Z → )

| when (UTC) | stage | what the record says |
|---|---|---|
| 05:10:10 | the drop | `cp` into `drop/`; `events.jsonl` at 437 lines |
| 05:10:11 | intake/detected | the watcher (23248) in **1 s**; `analyst_mode local`; `.gpu-lock` taken; the Dock `busy` |
| 05:10:12 | convert/probe · estimate | lane `clean` (`text_layer_present`, `ocr_invisible_ratio 0.0`), 2,654 chars/page, 92 pages; ETA **68 s at 0.749 s/page** (basis `similar`, 3 samples) |
| 05:14:37 | the second drop (Few) | `waiting 1` on the Dock in 10 s — the queue holds it |
| ~05:12–05:16 | Marker | `.convert-progress.json`: `Recognizing Text` 608/750 at 05:15 — the layout model recognising 750 regions (figures with text) on a book the probe called clean; the card 8,6xx MiB at 63–80 % |
| 05:16:55 | convert/converted | **wall 402.5 s, 4.38 s/page** (the estimate 68 s / 0.749 — off by 5.9×: the `similar` basis was three text-only books; this one's figures ran the recognizer), peak 8,643 MiB; `blocks` 605 on 92 pages, 1 slice, complete |
| 05:16:57 | audit/scored (convert) | `doc_survival 0.8633` (the witness vs Marker), runs 25 of 50, degeneration false → **verdict `flag`** (the convert phase's localisers) |
| 05:16:57 | analyst/start | local, 289,746 chars |
| 05:25:58 | the analyst | `.analyst-progress.json`: **41 of 75 chunks, 13.2 s/chunk, ETA 448 s**; chunk 41 `passed`, survival 0.9778; the card 7,512 MiB at 90 % |

The rest — the analyst's end, the analyst-phase audit (under `regex_id j32a-v2`, the lever OFF), the verdict, `_enforce_hold` or the ship, the ThinkPad's exporter, the vault — follows in E1's closing section when it lands.

## E1 — the run's landing: HELD on the analyst gate at 0.9921 — and every one of its 23 failed windows was the audit's own (05:32Z → 05:34Z)

| when (UTC) | stage | what the record says |
|---|---|---|
| 05:32:35 | analyst/done | **937.8 s** (15.6 min) for 75 chunks — 12.5 s a chunk; **63 passed · 12 rejected** (fence 6 · survival 4 · think_leak 1 · truncated 1 · inflation 0) · 0 failed; the acceptor reconciled 43 chunks — accepted reflow 33 · escape 28 · markup+escape 21 · link 6 · markup 3 · mixed 3; **reverted substitution 76 · punctuation/case 53 · insertion 27 · deletion 21 · numeral 1 · table-geometry 2** (E2's proof, live: 21 deletions and a numeral the model made never shipped) |
| 05:32:37 | audit/scored (analyst) | **doc_survival 0.9921 of 2,910 windows · runs_total 1** (`regex_id j32a-v2` — the lever OFF) → **verdict `fail`, phase `analyst`** |
| 05:32:37 | audit/held | `audit_mode enforce` → `_enforce_hold` parked it: `held/45994b30f52baffe/` (the note 292,212 bytes; the sidecar `Document Layout Analysis.marker.txt` 290,548 bytes, J33 — its sha256 matches the manifest; blocks.json 548,830; 31 assets); the anchor copy `anchor/Document Layout Analysis/`; the PDF → `drop/done/`; the ledger's row `wall_s 402.5 · s_per_page 4.375 · chunked false` |
| 05:32:37 | intake/detected | Few's book picked up from the queue in **the same second** — the Dock `busy` on it, `waiting 0`; probe 166 pages, `clean`, 1,776 chars/page; estimate 395 s at 2.38 s/page |
| — | the ThinkPad | nothing shipped, nothing to read there: a held book does not travel (the exporter's `EXPORT-INGEST-HELD` rail never engaged) |

**The one failed run, read (24 words):** `4page650 one group of these methods follow topdown object detection` — `4page650` is Marker's citation `[\[4\]](#page-65-0)` after the v2 ladder (the escaped brackets hide the link from the strip; `punct_free` leaves `4page650`), against the model's `[4](#page-65-0)` → `4`. **The cite-anchor class — J44's rung 3, built at S182 and OFF.**

**The held book re-read offline (`e1_held_v3_read.py` → `.txt`; the REAL `audit_analyst`, the sidecar the reference, the note's body the candidate — exactly `--reaudit`'s reading; the ladder BY NAME, the lever file never written; Observed 05:33Z):**

| ladder | doc_survival | failed windows / 2,910 | runs_total | the analyst gate |
|---|---|---|---|---|
| `j32a-v2` (the field's; reproduces the manifest to the digit) | 0.9921 | 23 | 1 | **fail** — HELD |
| `j32a-v3` (built S182, OFF) | **1.0** | **0** | **0** | **pass** — with the convert phase's `flag`, the book SHIPS |

**Reading.** 23 of 23 — 100 % of this book's residual — was the audit's own normalisation, not the book's. On DDIA the audit's share was 45 %; on a fresh book of his it is the whole of the difference between `held/` and the vault. Nothing about this run is the lane's to change: the lever is his (one line `j32a-v3` into `ladder.txt`, then `--reaudit 45994b30f52baffe` re-scores this book for free from its sidecar and ships it if the re-audit passes — the repair road J31 built for exactly this). Said to him in chat.

**What the run measured of the widget (Observed):** the intake in 1 s; the queue holding a second drop (`waiting 1`) and starting it in the same second the first landed; the estimator's basis `similar` off by 5.9× on a figure-heavy book (68 s promised, 402.5 s delivered — the three `similar` samples were text-only; the Dock's ETA bar lied by six minutes; the `converted` event carries the actual beside the promise, OK-17, so the record is honest even where the bar was not); the convert-phase verdict `flag` (survival vs the witness 0.8633, 50 runs — the localisers, warn-only by design); no stall, no retry, one slice, no OOM at 8,643 MiB peak; the analyst's per-chunk journal complete (`.analyst-work/a66593ee959a3010/chunks.jsonl`, 75 rows); the hold's rail correct under `enforce`. **Not measured:** the Dock's own rendering (his screen — the events file and the state file are what the Dock reads; a screenshot is his to take); the ship and the vault (nothing shipped).

**Lessons:** a book of his was held for nothing but the audit's own share — the lever's value is now a book, not a percentage; the estimator's `similar` basis needs a figure-density term (the probe already counts images per page — a row for the register); a full run's record is the events file + the state file + the manifest + the chunk journal, and the run reads honestly from them without a screen.
