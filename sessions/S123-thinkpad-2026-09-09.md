# S123 — ThinkPad / index-station lane — 2026-09-09 → 2026-09-10

⟨claimed: Fable lane · occupant: Claude Fable 5.1 · S123 · 2026-09-10 UTC⟩

Numbering: S123. Opened as S120 (docs/21 §176: global and sequential; the ledger's newest row was S119 at open). While this session ran, the Desktop ran and pushed S120, S121 and S122 — the SYM-040 case, found at push time — so this record and its two symptom rows were renumbered to follow the ledger before the push. The
ThinkPad lane's own series would say S80, but `sessions/S80-desktop-2026-08-15.md` exists, so S80
would collide (open.sh's COLLISION case). Rab may renumber at his close.

## §1 Session Intent

Rab's words, verbatim and in order:

1. *"give me a diagnostic on this machine, tell me how I can sufficiently extend it with chromadb
   and a minilm running on this machine, because it's all connected to file-portal. Go check the
   file-portal library-pipeline branch for more info"*
2. *"go ahead, pull and scaffold linux-indexer"*
3. *"if a solution can be created that is far more operator friendly, and gives more control and
   use case variety and capability, do it, make sure its free, open sourced, and integratable, if
   any of the current tools are lacking"*
4. *"deploy it and tell claude on desktop in a different session by using tailscale to file send
   back a report of what you did in the coordination relay"*
5. *"okay, commit, ac is on"*

This is the D7 go (OPEN-TASKS §D: "enrichment consumer", `blocked-on-rab` since S43) for its
embedding half. The tagging half (schema-constrained outputs) and the docs/14 Phase A window were
not asked for and are untouched.

## §2 Starting State

MUSTER at open (Observed 2026-09-09 ~23:30 UTC): branch `feat/library-pipeline` **behind 49**
(none under `linux-*`/`config` → pull safe, no converter restart), allocator + converter active,
vault tip `50896d1`, staging empty, receipts 13 lines ending `fixity-check pass`. Machine: i7-1265U,
15 GiB (a 6 GiB-heap Minecraft server resident at 1.9 GiB), 181 GB free, Iris Xe only, on battery.
The July Phase-3 bench env (`~/ml/chroma-env`, chromadb 1.5.9 + torch) still imported and
embedded — re-verified — but nothing was wired: `OPEN-TASKS` D7 awaited an explicit go.

## §3 Development Chain

1. Pull (`ba7c79f → 5940249`, fast-forward, 60 files, none Linux-side).
2. Six-reader conventions map of the repo (Workflow `wf_72d26344-00a`, 954k tokens): packaging/CI,
   runtime conventions, test doctrine, laws/registries, vault+bundle format, integration surfaces.
3. Empirical probes: chromadb + ONNX on Python 3.14 (yes); Chroma's ONNX MiniLM vs torch parity
   (cosine 1.0000); Chroma refuses `modify` with the distance key; telemetry on by default; a user
   service can reach the user bus (`systemd-run --user` probe) so a `post-update` hook can start
   the unit.
4. First scaffold on ChromaDB (31 tests). Rab's third word arrived mid-turn.
5. Assessment: the July stack could not search the vault's Chinese book (MiniLM is English-only),
   could not find an ISBN/exact phrase, was not operator-inspectable, phoned home. Probed
   sqlite-vec 0.1.9 (KNN, cosine, metadata `IN`, delete), FTS5 (bm25, snippet) in the system
   sqlite 3.53.2, fastembed 0.8.0 (catalogue, `query_embed`/`passage_embed`, rerankers).
6. Model bench on 1,244 real passages (July's two books + the CJK book from the bare vault by blob
   sha): the first pass at fastembed's default batch blew to 9.9 GiB (SYM-083); rerun at batch 32.
7. Rebuild onto SQLite + fastembed: store, reconcile, hybrid query, status, serve; 38 → 40 → 43
   tests. Live proof on a bare clone of the real vault, then three gaps found there (ISBN only in a
   filename; `--verdict flag` and `--bundle claude` starved by post-fetch filtering) closed by a
   title leg and filters pushed into the SQL of every leg.
8. Review: a five-lens adversarial fan-out died on the session limit (2.5M tokens, nothing back);
   a three-reader rerun returned docs + laws (16 findings, every one that held applied); the code
   reader restarted five times and was stopped — its lens closed by direct probes (transaction
   atomicity under fault injection, id/vec/FTS consistency, the flock, `fts_query` against FTS5
   syntax, endpoint bodies) which found and fixed two endpoint defects.
9. Deploy (Rab's fourth word): `install.sh`, first unit run, hook proof, real queries.
10. Delivery: Taildrop refused (SYM-084); `tailscale serve` of a loopback file server instead.

## §4 Analogue + Boundary

The station is the exporter's mirror on the read side: the exporter copies bytes into the vault and
never reads them (Decision #5); the indexer reads the vault and never writes it. Boundary: nothing
here gates an export, a verdict or a vault write (docs/11: "fully reversible, must not gate
anything"). The widget and `room.js` are Desktop-owned and untouched; the ask is a message.

## §7 Implementation Delta

`git diff --stat 5940249..<closing SHA>` is the record. In words: `linux-indexer/` (26 files:
`indexer/{config,receipts,vault,passages,embed,store,reconcile,main,query,status,serve}.py`,
`config/indexer.toml`, two units, `install.sh`, `post-update.hook`, five test files, README,
packaging); `.github/workflows/ci.yml` (own warn-only install + lint + test steps, header);
`.gitignore` block; `OPEN-TASKS.md` D7 bracket; `docs/18` lever row; `docs/50` D7 row;
`observability/README.md` (the receipt keys no lane harvests); `CHANGELOG.md` entry; two
`coordination/messages/` files; `SYMPTOM-INDEX.md` SYM-083/078; this closeout. Runtime (not in
the repo): `~/file-portal/index/{index.sqlite,models/}`, `~/file-portal/logs/indexer.log`, two
units in `~/.config/systemd/user/`, `~/file-portal/vault.git/hooks/post-update`, the `indexed`
receipt (line 14), `~/file-portal/outbox/index-station/` (the served copies), a transient unit
`index-station-share` + `tailscale serve --http=8080` (stop: see the deploy message §6).

## §8 Decision Ledger

| Decision | Choice | Why |
|---|---|---|
| Store | SQLite + sqlite-vec + FTS5, one file | inspectable with `sqlite3`, one-file backup, SQL filters, keyword search from the same file, transactions; Chroma phones home, is heavy, and hides its data |
| Embeddings | fastembed (ONNX), model a lever | no torch (211 MB venv vs 1.4 GB), 30-model catalogue incl. multilingual, rerankers; **Rab's word: free, open, integratable** |
| Default model | all-MiniLM-L6-v2 | measured 56/s · 420 MiB · 7/7 probes vs multilingual 5/s · 980 MiB · 6/7; docs/11's pre-answered choice; the multilingual one is a flip away |
| Reconcile | whole-tree, keyed on `source_sha256` + blob shas, one commit per run | idempotent; survives hand rewrites; the exporter's own identity key |
| Failure posture | refuse + exit 1 + receipt; `--rebuild` only on request | report-mode (fixity's doctrine); a derived index is never rebuilt behind anyone's back |
| Receipts | `indexed`/`index-failed`, one per changed run, `bundle` when one changed | the widget tails 60 lines; never the exporter's alarm vocabulary |
| Query | hybrid (vector + BM25 + title), RRF, filters inside SQL, opt-in reranker | an ISBN lives in a filename; a filtered common word starved under post-filtering (Observed) |
| Surfaces | CLI over `tailscale ssh`; opt-in loopback HTTP for `tailscale serve` | docs/06's own pattern; no new port on any interface; no unit for serve |
| Levers | nine in `indexer.toml`, ranges/menus, named fallbacks, build levers on the receipt | docs/18 gate; every bare number waived inline |
| Cross-lane code | `append_receipt` mirror-copied | the sdnotify one-file-port precedent; no cross-venv import |
| Dispositions | recorded in `observability/README.md`, not `dispositions.json` | no lane names a `linux-*` producer; a row would trip the stale-signature check (S115 precedent) |
| Session number | S123 | docs/21 global rule; S80 collides with a Desktop closeout |
| Push | not done | Rab said "commit"; the push is his word (public repo) |

## §9 Evidence

All Observed on this ThinkPad unless tagged. Bench (batch 32, 4 threads, 1,244 passages): MiniLM
56.3/s 420 MiB 7/7; multilingual-MiniLM-L12 5.2/s 978 MiB 6/7; bge-small 2.0/s 1,824 MiB 6/7;
arctic-xs 14.8/s 6/7; reranker ms-marco-L6 20 candidates in 1.14 s. Parity: fastembed MiniLM vs
Chroma-ONNX/torch cosine floor **0.5845** on 64 passages (Chroma-ONNX vs torch was 1.0000). Live
proof on a bare clone: 6 bundles → 3,970 passages 69.5 s 444 MiB; quiet run 0.6 s; supersede
70c60e6→50896d1 replaced only Cybernetics (583, 11.4 s); 7/7 hybrid probes; index 12 MB. Deploy:
unit run 75 s Result=success, 3,951 passages (the fence-aware splitter shipped between proof and
deploy), receipt at tip 50896d15, hook-fired quiet run at 01:15:47Z with no receipt. Gates:
`glass_detector --since 5940249 --enforce` no unsigned glitches; `acceptance.py` 141/141;
`schema_registry_selftest` 18/18; `pytest` 43 passed; ruff check + format clean (0.15.20).

## §5 Observable Contract

Receipts: `indexed` {result, tip, bundle?, note?, bundles, added, replaced, updated, removed,
refused, unchanged, passages, embed_s?, model, model_sha, passage_chars, passage_max_chars,
fts_tokenizer, threads, lever_fallbacks?}; `index-failed` {tip?, error}. Query JSON and HTTP
routes: `linux-indexer/README.md` and the contract message. Status JSON: `indexer.status`.

## §6 Deliberate Divergences

`Nice=10` + `IOSchedulingClass=idle` on the unit (first resource directives in the fleet; no
hardening directives, per convention). Its own warn-only CI install step (the shared install must
never fail the armed gates). `serve.py` binds loopback by a constant, not a lever. The
`FILE_PORTAL_ROOT` knob was removed on review: nothing else reads it.

## §10 Known Failures (mine, this session)

Three `pkill`/`pgrep -f` self-matches (one killed my own shell, one wait loop never ended) —
memory `pkill-self-match`. A heading's text was clipped to the 120-char metadata limit (test caught
it). A section heading was glued onto the previous passage's tail (test caught it). An unquoted
heredoc ate two backticked words from the deploy message (repaired). The first proof script called
`/usr/bin/time`, absent here. The first review fan-out spent 2.5M tokens and returned nothing
(session limit) — feedback drafted.

## §11 Unproven by Machine

The Desktop's own `tailscale ssh rab@archlinux '…python -m indexer.query'` leg (self-ssh is
refused here: no local sshd). CI on ubuntu/py3.11 (warn-only until observed in the job LOG,
SYM-075). The hook firing from a Desktop push over `tailscale ssh` (only an exporter-context
`systemctl --user` was proven, via the hook script run by hand and the user-bus probe).
Retrieval quality beyond the 7 probes; the multilingual model's CJK quality (6/7, one Chinese miss).

## §12 Uncertainty Register

Whether fastembed's MiniLM export (cosine floor 0.58 vs July) changes retrieval quality in
practice — 7/7 says not on these probes; UNREAD beyond them. Whether `unicode61` or `trigram`
should be the tokenizer for a mixed CJK/English vault (lever, default unicode61).

## §13 Dependencies / Assumptions

`sqlite-vec>=0.1.6,<0.2`, `fastembed>=0.7,<1` (range-bounded, not pinned); the system sqlite
must carry FTS5 and `enable_load_extension` (Arch: yes; CI: Unknown until observed). Model files
from Hugging Face on first run (~87 MB for the default) into `~/file-portal/index/models/`.

## §14 Regression Surface

`linux-indexer/tests/` (43): reconcile (14), query/status/serve (12), passages (8), config (5),
receipts (3). Planted negative controls: a lying projection (store deleted), a withdrawn bless
key, filter starvation, an FTS-syntax question, a non-object POST body, a two-body bundle, a sha
vaulted twice.

## §15 Symptom Signatures

SYM-083 (fastembed batch memory blow-up) and SYM-084 (Taildrop refused between a tagged node and
a user's device) filed. SYM-037's torn-line healing carried into the mirror copy with its test.

## §16 Recovery / Return Log

Rab's machine went dark mid-turn (2026-09-09); this ThinkPad did not reboot (uptime 2 d) and
every file survived. Battery fell to 30 % under bench + reviewers; flagged; AC restored at close.

## §17 Current State

`feat/library-pipeline` at the closing SHA (see ledger), **not pushed**. Services: allocator,
converter, indexer.timer (next 2026-09-11 00:10 UTC), vault-fixity.timer — all active. Vault tip
`50896d1`; index in sync (6 bundles, 3,951 passages). Receipts 14 lines. The served share is
still up (`http://archlinux.tailc44e8c.ts.net:8080/`) until Rab stops it. The July `~/ml` bench env
(7 GB) is superseded and still on disk; delete on his word.

## §18 Next Entry Point

1. **Rab:** push (or say why not); stop the share; decide whether the widget gets its first text
   input (a search box over the query contract — a signature item, docs/33 §3.2 class).
2. **Desktop lane:** read the two `coordination/messages/*index-station*` files; add the two
   `room.js` phrases; prove the `tailscale ssh` query leg; carry the relay entry in the deploy
   message §5 onto `relay.md`.
3. **CI:** read the job LOG for the three warn-only indexer steps; arm them (S108 form).
4. **ThinkPad MUSTER** now also checks `systemctl --user is-active file-portal-indexer.timer` and
   `linux-indexer/.venv/bin/python -m indexer.status` (`in_sync`).
5. **Later, unsigned:** the tagging half of D7; `docs/14` Phase A window; a dashboard search entry.
