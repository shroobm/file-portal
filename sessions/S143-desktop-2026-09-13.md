# S143 — Desktop — 2026-09-13 — same mode: the echo answered, the policy and the product on his letters

*Record before the act (docs/28; J63's guards). Written at the open, before any tracked write.*

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

> Same mode proceed. Signed even.

**Standing:** nothing to Codex until lifted; the record before any tracked write (its creation and its commit are two
calls); a probe in the shared checkout is a file run by its literal path; `row_check.sh <N>` and CI's own gates before
any push; `--census` at every close.

## §2 Starting State — `open.sh`, 2026-09-13T06:28:04Z (verbatim)

```
════════ MUSTER · OPEN · 2026-09-13T06:28:04Z ════════
[0] IDENTITY
    ledger newest    S142  242c397
    this session     S143  ·  desktop  ·  2026-09-13
    session marker   coordination/private/session.current = S143 (guard_record / guard_git read it)
    advisory         S143 unnamed in the inherited tree
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-13T06:28:05Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 86 (tally == hook)
[3a] LEDGER ORDER  ✓ 125 Desktop-lane rows ascend (S16 → S142)
[3b] LEDGER PARSE  ✓ 125/151 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ 242c397 (S142) == ledger 242c397, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 9 · anchor 31 · pending 0 · drop 0
    intake           waiting 0 · oldest 0s · active null · written 0s ago by pid 13572
    levers           audit=report · analyst=local · batch=8
    gpu-lock         absent
    events           351 line(s)
    open-tasks       149 item(s) · last written 19 minutes ago
    error-bin        101 row(s) — read the CLASS table before you probe · last written 5 hours ago
    symptoms         126 row(s), 43 open · last written 19 minutes ago
    relay            167 entries · run `gate.py status` for the board
    widget           8076
    widget autostart task 'File Portal widget (J57)' Running — AtLogOn · auto-logon ON (Bndit)
    task             'File Portal desk (relay)' Running LASTRUN=2026-09-13T00:59Z RESULT=267009
    task             'File Portal relay watch (Fable)' Ready LASTRUN=2026-09-12T19:41Z RESULT=0
    task             'File Portal tracker' ABSENT — not registered (install_task.ps1)
    task             'File Portal desk watch' ABSENT — not registered (install_task.ps1)
    python procs     9
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            7 note(s) · tip 8d7cc85
    thinkpad         converter=active · vault tip 8d7cc85 · staging 1 · receipts 19
[3] PIN
    --since          242c397
    closeout         sessions/S143-desktop-2026-09-13.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

## §3 The pin

`--since 242c397` (the previous session's closing commit, from the card).

## §9 The plan

PLAN S143 — written 2026-09-13T06:2xZ by Claude Fable 5.1, at the open. §1 is Rab's Desk post 0c6d2825 (06:26:16Z), verbatim.

WHERE THINGS STAND (Observed at 06:27Z)
- S142 CLOSED 06:2xZ: closing SHA 242c397, row dcf7a8d (CI green run 34742163860), post-close 4480c81 (CI green run 34742475592);
  memory library 37af0a8 (17,232 bytes -> fold FIRST, two folds: S141's block, S140–S118 merged). Both repos clean and pushed.
- The Desk: Rab's "Update me." (308a3c8a) answered 06:13Z; the S142 close report 677d2633; his "Same mode proceed. Signed even."
  (0c6d2825) answered 06:27Z with the ECHO (prompt-echo protocol: the readings differ by GPU hours and a vault write):
    (A) he signs the S142 close — nothing new moves;
    (B) he signs the persistence method as POLICY — four writes (a POLICY section; muster SKILL.md; docs/21; a feedback memory note);
    (C) he signs the six things on his word from the close report (four re-conversions; the CJK retire + nine anchor duplicates;
        the Zero to One reship on the ThinkPad; the Damodaran preference; eac27c03's three; the watcher restart).
  I act on A now; B and C wait for his letters or items. The Desk watcher (pid 12304) is armed; his answer re-invokes me.
- Live: the Desk watcher armed; the tracker to be started by hand for S143 (its STOP file from S142 is removed at start); the
  pipeline watcher pid 13572 on pre-S141 code (his hand).

THE OPEN (SEQUENCES §1) — the same mode: 1 fold ×2 (dry run, --go) · 2 open.sh card · 3 Desk inbox (done: the echo posted) ·
4 record_open.py 143 <card> intent_s143.txt plan_s143.txt "same mode: the echo answered, the policy and the product on his letters"
-> the commit as a SEPARATE call · 5 the tracker by hand (-StartedBy fable-S143 -Label S143-receipt) · 6 library_smoke.py --public
(≈15 min; the guard changed in S142; ALONE — nothing else writing while it runs; a red is fixed before the work leans on it).

THE EPISODES — on his letters:
 E1 (if B) contracts/persistence-as-policy: POLICY.md gains a section (a sitting may close in parts; each episode a partial
    closeout per SEQUENCES §3b; the plan file before a compaction, the section after; §18 still written last); the muster
    SKILL.md and docs/21 gain the same sentence; a feedback memory note (persistent-partial-closes.md) with the why. Acceptance:
    brief_check / the muster selftest green; the four writes in one commit with their record section.
 E2… (if C, the items he names) held-books executions, one item per episode, each a partial closeout: the re-conversions need
    the road read first (the watcher on pre-S141 code vs a direct convert_and_ship call; the GPU law before each; the time per
    book from the recorded rates); the retire is a move of held/21bfdffc… + the nine anchor dirs to a named place, ledgered
    before the move; the reship is a ThinkPad act (thinkpad_state.sh first; UNREAD if asleep).
 Without a letter: nothing substantive is unblocked — the residue list is on his word or frozen. The sitting holds after the open.

THE CLOSE (SEQUENCES §3): pin 242c397; prev row prefix "| 2026-09-13 | Desktop | S142:"; the gate OUTLIVES the post-close commit
(S142 §20: remove it last, with the tracker's STOP); the smoke alone; the rescorer under marker-env.

## §8 Findings

(filled as they are made)

## §10 Known failures / residue

(filled as they are made)

## §7 Work log (UTC)

- **06:28** `open.sh` clean (S143, pin 242c397). This record.

## §1b The commission (Desk 4ee544e2, 2026-09-13T06:40:43Z, Rab's words verbatim — read from the Desk file by id)

> If there are tickets you can compile for sequencing, and preload for them, and ensure they all have access to the briefing policy. Finish up what Opus left for you if you haven’t, continue with your run, when you’re done your sequences and episodes, have everything recorded and filed in, then I want you to deep research based on our problems and errors, what the internet on trust worthy sources (anything that is like the universal pdf world, or whoever standardized PDF’s) I want you to start working on the machine learning. Start gaging like a deep learner and machine learning computationist, based on what problems we have went against, and the model that works, is that we need to have PDF’s that are scans only to starting having less omissions, more variability, more observability and modes of observability. I’m not sure what that may look like, so I would want you to try and find that out to the point that you had over analyzed but came back with ground truth. Don’t be anxious, but if you are investigate thoroughly. Be strict on your calls. Manage and pace yourself accordingly. Never leave something out that was carried by you being active.

**Read at 06:42Z and answered on the Desk (the reading, in his order):** (1) compile the sitting's tickets, sequenced as episodes, each naming the scripts it preloads and the briefs it hands (POLICY §2); (2) finish what Opus left — Desk `eac27c03`'s three (the two task registrations with his Desk lines as the typed consent; `.codex/hooks/` as a junction; the seven retirements by POLICY §7 step 7) and the persistence method as POLICY, which his "Signed even" (§1) is taken to sign; NOT in that set: the S142 E1 executions (my decisions, on his named word), the watcher restart (his hand), the widget rebuild slot; (3) the run, recorded and filed; (4) the deep research — our conversion errors against the standard's own sources; (5) the ML gauge for scans-only PDFs, over-analyzed then brought back to ground truth on our bundles. Episode 2 lands after the tickets, so one word on the Desk before then stops any of it.

## Episode 1 — sequencing (06:28 → 06:5xZ) — PARTIAL CLOSEOUT

**Ticket:** `sequencing/compile-preload-brief-policy` — DONE. The sitting's tickets are compiled and sequenced in the private `measurement/TICKETS-2026-09-13-S143.md`: eight cards in four categories (`sequencing` · `opus-leftovers` ×5 · `research` · `ml-gauge`), each carrying two new slots — **PRELOAD** (the scripts the episode runs, by literal path, so the first call is the right one: S142's F2 was a rescorer run nine times under the wrong interpreter while its brief named the right one) and **BRIEFS** (the briefs an agent must have read, POLICY §2's eleven headings; docs/47 GROUND if a lane is summoned — none planned, Rab named no shape).

**The open, in the same mode (Observed):** the index folded twice and pushed (`3f2ec45`, 16,203 bytes); the 06:28Z card clean (exit 0, pin `242c397`); the record `ddfe2dd` with his Desk line as §1 and, after the smoke, §1b — the commission verbatim, read from the Desk file by id; the tracker by hand (pid 24132, 06:28:51Z); `library_smoke.py --public` ALONE: **64/64 green, 693 s** (the guard changed in S142; the three write-tripwires unchanged — the rule from S142 F1 held).

**Born-of re-measured for the next episode (Observed 06:4xZ):** `tool_census.py --retire` proposes SIX, not S141's seven — `bundle_audit.py` left the list because S142 E1 ran it (uses 1); the six: `auditing/clocks.sh`, `errors/organize_errors.py` (its selftest goes with it), `navigation/procs.ps1`, `navigation/who_holds.ps1`, `reading/read_journal.py`, `reading/read_review.py` — each born 2026-09-12T09:04Z, 0 uses over 7 observed sittings, nested-by none. `.codex/hooks/` holds ONE file (`check_coordination.py`, LF) whose drift from `.claude/hooks/` is one docstring line ("Codex caught both; I caught neither." vs the two-line attribution) plus line endings (0 vs 58 CR); `.codex/hooks.json` also names `.codex/hooks/warn_heredoc.py`, which does not exist there — a junction resolves both. The two install scripts refuse `-Register` / `-RegisterWatch` without `-Consent` (read, not remembered).

**The line drawn (posted on the Desk 06:42Z, in §1b):** Opus's leftovers = Desk `eac27c03`'s three plus the persistence method as POLICY; the S142 E1 executions are mine and stay on his named word. Episode 2 lands after this close; one word on the Desk before then stops any of it.

**Contracts touched:** `OPEN-TASKS.md` — the five S143 rows (this one struck; four open), the three `eac27c03` rows re-pointed to S143 E2. No code.

**What an agent is handed:** "read the ticket's PRELOAD before the first call and its BRIEFS before the first claim."

**Discovered (→ §8):** F1 — a born-of ages between sittings: "seven candidates" became six in one sitting because a candidate was used; the census, not the record, is the number. F2 — a hook config can point at a file that is not there (`.codex/hooks.json` → `warn_heredoc.py`): the Codex lane, if it woke, would run a hook that fails open (`|| true`). **F3 — a phantom red in the denies cross-check → SYM-127.** This episode's receipts read `guard_record main: journal 0 != log 1 · lanes: journal 0 != log 1 ✗` though no tool call of mine was denied for either; the guard log (Observed) holds two DENY rows at 06:41:20–21Z, `tool=? agent=- reason=unreadable-payload` and `tool=? agent=-/- guard=record cmd={}` — the smoke's `--public` hook probes, which feed the live hooks an empty stdin; the hooks log the refusal into the LIVE `git-guard.log`, and the cross-check reads the log by agent field (`agent=-` is not `-/-`, so a lane; `guard=record`, so the record guard). The check is right about the log and wrong about the cause; the instruments are frozen, so it is recorded (SYM-127, family S5 — a guard off the path the work takes; `error_families --census` 127/127 placed) and not fixed: a reader subtracts the `tool=?` rows. The one real deny in the window (06:18:38Z, `git commit` refused for no marker) is S142's post-close, inside this window by the tiling.

**The relay (Rab, Desk 8d2ee61c 06:48Z: "Codex is awake. Let codex in relay what are the new rules essentially."):** `MSG-FAB-0086` posted to the Codex lane at 06:5xZ — the thirteen rules that changed since 2026-09-10, where to read them in order, the state at 06:5xZ, nothing asked of Codex (body 1,084 words; digest `sha256:2e6003eed88f…`; the lane's state `blocked-on-ack`). The first `post` was REFUSED by the envelope contract (CR-CDX-0002: RECAP · FOR RAB · SUGGESTED PROMPT missing) and re-sent with the three slots — the refusal was the gate working, on its own call. The board (Observed 06:49Z): Codex `idle`, presumed dead since 09-10 (beat 4,654 min old), the Fable lane's occupant already `Claude Fable 5.1`; Codex's ACK will be the sign it is awake. A notice, not a ticket: work handoffs stay on Rab's next word — his "nothing to Codex until lifted" stands for work. The entry's FOR RAB and SUGGESTED PROMPT (for him to give Codex: confirm the entry, read POLICY/SEQUENCES/docs/47, declare its occupant, take no ticket until he issues one) were carried to him on the Desk in the answer to 8d2ee61c.

**Receipts:** `journal/{calls,cost,usage}/S143-ep1.md` — window 2026-09-13T06:11:49.649Z → 2026-09-13T06:47:49Z (tiled onto the previous receipt's end); calls 79 · preloaded 17 · consulted 6 · scratch 6 · inferred 31 · harness 19 · denied 1 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 ; Grand total (USD equivalent): 9.57 (session 9.57 + fleets 0.00); Categories used: navigation (1), testing (2), auditing (3), developing (7); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 5 · advisories 0 · Rab's mid-turn words 3. Ledgered D0062 (calls) · D0063 (cost) · D0064 (usage) · D0065 (smoke_s143_public.out); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 65 · twin rows 65 · disagreements 0;   """(lines, red): every `### \`<path>\`` under a `## <sha>` heading of the code receipt is in that commit's --name-only;; the denies cross-check: denies: guard_git main: journal 1 == log 1 · guard_record main: journal 0 != log 1 · lanes: journal 0 != log 1 ✗ (window 2026-09-13T06:11:49.649Z → 2026-09-13T06:47:49Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 40 of 40 messages (100%), 2026-09-13T06:11:49.649Z → 2026-09-13T06:47:48.482Z.

## Episode 2 — opus-leftovers (06:5x → 07:1xZ) — PARTIAL CLOSEOUT

**Five tickets, DONE, on Rab's two Desk lines** (0c6d2825 "Same mode proceed. Signed even."; 4ee544e2 "Finish up what Opus left for you if you haven't") — the reading echoed on the Desk at 06:42Z, no word against it before this episode landed:

- **`services/tracker-as-scheduled-task`:** `install_tracker_task.ps1 -Register -Consent "<his two lines, dated, and this sitting>"` → `registered. task 'File Portal tracker': state=Ready`, logon trigger (delay PT30S, user Bndit, runlevel Limited), the consent written into the task's description (Observed). NOT started: the by-hand tracker (pid 24132) records this sitting; the task arms at the next logon. `-Unregister` undoes it.
- **`services/desk-watcher-as-task`:** `install_task.ps1 -RegisterWatch -Consent "…"` → `watch task registered. task 'File Portal desk watch': state=Ready`, the same consent (Observed). Not started; the interactive watcher reaches me this sitting.
- **`guard-holes/retire-codex-hook-copy`:** `.codex/hooks/` is a JUNCTION to `.claude/hooks/` (`ReparsePoint=True`; 6 files visible through it; `warn_heredoc.py` resolvable) — the diff recorded first: the copy held TWO files, `check_coordination.py` (2,164 bytes; drifted by one docstring line and LF vs CRLF) and `warn_heredoc.py` (2,899 bytes; identical modulo CRLF); both backed up to the scratchpad before the removal. `.codex/hooks.json` untouched. **E1's F2 was WRONG** — it said `warn_heredoc.py` was not there; my `ls -la | head -12` had cut the listing. Corrected here; E1's text stands (appends never erase).
- **`retirement/six-candidates`:** `tool_census.py --retire` (Observed 06:4xZ) proposed six; `retire_six.py --go` (scratch, by path): six briefs `git mv` to `agent-scripts/retired/`, each with a RETIRED line naming its census row and what replaced it; seven files `git rm` (the six + `organize_errors_selftest.py`); seven README rows removed. After (Observed): `tool_census.py --list` 98 tools · 29 of 29 with a brief · 22 tripwires (104 / 35 / 23 before); `script_journal_selftest` 54/54 (its `who_holds.ps1` fixture is a string, not a file); `tool_census_selftest` 12/12 (its `clocks.sh` fixture likewise); `brief_check.py` RED — `briefs: 35 · defects: 6`: the walk read each retired brief as a brief without its script. POLICY §7 step 7 (S141) named `retired/` and never told the checker → **IB-2026-09-13-014** (BOUNDARY-OMISSION; family M2 by the placer): the walk now skips `retired/` and prints their count → `briefs: 29 · retired: 6 · defects: 0`. `library_smoke.py` ALONE afterwards: `library smoke: 42/42 green · 79s` (49 cases before — the retired selftest and the six tools' no-argument cases are gone with them; the three write-tripwires unchanged).
- **`contracts/persistence-as-policy`:** one script, four writes, each reading its file's newline — POLICY §9 (private, LF: "A sitting may close in parts"), the muster `SKILL.md` (CRLF; the paragraph before "What this cannot see"), docs/21 §2 (CRLF; "The episode clause"), the feedback memory note `persistent-partial-closes.md` with its MEMORY.md line (16,435 bytes; library `ee13b62`, pushed). The words: a category is an episode; an episode ends in a partial closeout; the next episode opens on its section; a compaction is a memory boundary — plan before, section after; §18 last; the marker outlives the post-close commit. Signed by Rab on the Desk 06:26Z after being asked, 05:1xZ, whether it was policy.

**Not in this set, said again:** the S142 E1 executions (mine — on his named word), the watcher restart (his hand), the widget rebuild slot.

**The relay and the Desk inside this window (Observed):** Codex CONFIRMED `MSG-FAB-0086` at 06:54Z — restatement "Read all thirteen changes: private scripts and eleven-heading briefs; literal calls and exit gates; named tickets; tiled immutable receipts; episodes and partial closeouts; record-first and shared-checkout law; …" — and `0085` with it; `gate.py check --as Fable` settled the lane to `idle`. `coordination/ack-codex.json` changed by Codex's own hand (its `updated_utc` 06:54Z): the single-writer law holds, and I do not commit its file — its lane's commit-last is Codex's. Rab's 89ff310c ("Do a relay (for codex) watcher and alongside your desk (for me) watcher.") → a persistent `gate.py watch --as Fable` Monitor beside the Desk watcher (its first event: an UNGATED-ENTRY `MSG-CDX-0027` from 2026-08-28 — Historical, benign); answered on the Desk 06:58Z.

**Contracts touched:** POLICY §9 (new); the muster `SKILL.md` and docs/21 §2 (one paragraph each); `agent-scripts/README.md` (seven rows); `brief_check.py` (the `retired/` skip — an instrument's bug fixed, rowed IB-014, named here for the frozen window's reader); INSTRUMENT-BUGS; `OPEN-TASKS.md` (five rows struck). SEQUENCES §1 row 7 still says "by hand until the task is registered" — now Historical; not edited this episode (a one-line contracts pass owed — residue).

**What an agent is handed:** "a persistent machine setting carries Rab's words in its description and `-Unregister` undoes it; a retired brief says what replaced it; the hooks live in `.claude/hooks/`, and `.codex/hooks/` is the same directory."

**Discovered (→ §8):** F4 — IB-014, the checker's walk vs the policy's `retired/`. F5 — E1's F2 was wrong by a truncated listing (`| head -12`); a count read through `head` is not a count. F6 — the Codex lane was never dead, only unaddressed: its sidecar moved two minutes after the first entry since 09-10.

**Residue (→ §10):** SEQUENCES §1 row 7's line (the tracker); at the next logon BOTH the task's tracker and a by-hand one would record if a session starts one by hand — the sequence must say "the task records; start none by hand"; `.codex/hooks.json` still swallows failures (`|| true`, IB-013) — Codex's file, not mine.

**Receipts:** `journal/{calls,cost,usage}/S143-ep2.md` — window 2026-09-13T06:47:49Z → 2026-09-13T07:05:23Z (tiled onto the previous receipt's end); calls 78 · preloaded 23 · consulted 7 · scratch 6 · inferred 18 · harness 24 · denied 0 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 ; Grand total (USD equivalent): 9.36 (session 9.36 + fleets 0.00); Categories used: navigation (2), testing (2), auditing (4); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 0 · advisories 0 · Rab's mid-turn words 8. Ledgered D0066 (calls) · D0067 (cost) · D0068 (usage) · D0069 (smoke_s143_e2.out); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 69 · twin rows 69 · disagreements 0;   """(lines, red): every `### \`<path>\`` under a `## <sha>` heading of the code receipt is in that commit's --name-only;; the denies cross-check: denies: guard_git main: journal 0 == log 0 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-13T06:47:49Z → 2026-09-13T07:05:23Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 26 of 26 messages (100%), 2026-09-13T06:48:21.295Z → 2026-09-13T07:05:02.478Z.

## Episode 3 — desk + relay (07:05 → 07:2xZ) — PARTIAL CLOSEOUT

**Rab's four Desk posts inside this window, each answered on the Desk:** a1037ca1 07:01Z ("Tell Codex to pace itself. And also update the deck so there is two separate message boards, on orange for Claude one greyish white for codex. Make it switchable like light mode or dark mode, and it changes who receives it and where they receive it. Figure out what you could do with a fleet doing that while you're working out on your own, and speaking with codex. Reply back to me on what codex thinks"); e34c50e1 07:05Z ("Also teach codex how to make similar desk watcher bashes and relay watcher bashes."); 795574c2 07:08Z ("If the bash scripts are too good, put them in private scripts? Then lead codex to private?"); and 89ff310c 06:55Z (the two watchers) from the previous window's end.

**Tickets (3), DONE:** `desk/two-boards-lane-switch` · `relay/pace-and-map-for-codex` · `relay/watcher-scripts-private` (the third born of 795574c2 inside the episode; its card is in the tickets file).

**The Desk's two boards (Observed):** a three-lane fleet on Sonnet — SERVER (`desk.py` 24,440 → 27,371 bytes), PAGE (`desk.html` 12,693 → 17,233), TESTS (`selftest.py` 19,567 → 31,795; `README.md` 6,006 → 9,014) — under one GROUND (`fleet_desk_ground.md`, scratch: the files by path, the shared-checkout law, never the live Desk, tag every claim, a negative control, declare the residue), Rab's default shape since he named none. Each lane wrote only its files and none committed; I integrated. The design: every entry and reply carries `lane` (`claude` | `codex`); an absent lane is Claude's, so every pre-S143 row and every Claude row stays byte-identical; the Codex board is `data/desk-codex.jsonl` with its own lock; every route and CLI command takes the lane (`?lane=` on GET, `"lane"` in POST bodies; `--lane` on `inbox` · `reply` · `watch` · and `show`, the last by my hand — the residue all three lanes flagged); the page's switch (`id="laneToggle"`, `localStorage["deskLane"]`, `data-lane` on the root, the greyish-white palette by `:root[data-lane="codex"]`) selects the board the page reads and the board a post lands on; a reply aimed across boards is refused (the reply route scopes ids to its lane). `selftest.py` 32/32 by the lanes and again by my hand after integration (ten new cases: lane defaults, the codex post in its file and NOT in Claude's, `?lane=claude` never showing it, `inbox --lane claude` never listing it, the backlog, the cross-board reply refused, a torn last line skipped then healed, the page's control). Live: `desk_restart.ps1` at 07:16Z (pid 27560; the task's child stopped; the task re-launches at the next logon); `GET /` carries the toggle; `GET ?lane=codex` `{"entries": []}` then, after the first post (`20b6cc4a`, from me, a notice to Codex), one entry with `"lane": "codex"`; `GET ?lane=claude` 39 entries before and after, the id absent (0); `inbox --lane codex` lists it; `show 20b6cc4a` without the lane → `no such entry` (exit 1).

**The fleet's own reports, said plainly:** all three lanes reported a read-only version-control command run against the ground's first law (a `show`, a `status`, a `diff --stat`), each named by the lane itself — the law held as a report, not as a mechanism: the receipts read `lane calls 78 · lane denies 1`, and that one deny was a `$py` head (the designed `$VAR` deny) in the TESTS lane, not a read verb — the hook lets a lane's read-only git verbs through and the ground's law was the only thing that named them; the fleet's cost 3.62 USD-eq beside my 12.28 in the window; two lanes received my mid-flight message (Codex's directory-path preference) and rightly treated it as untrusted against the ground's literal text, verified the code, and kept the sibling file — a lesson for the coordinator: a ground is not amended by a message; Codex said in 0067 the directory form "is not a competing requirement".

**The relay (Observed; the watcher Monitor delivered each signal):** `MSG-FAB-0087` (07:0xZ: Rab's pace word verbatim; the Claude lane's read-only map with hashes and gaps; Codex's map digest CONFIRMED `4e36ece5…`; the boundary; the Codex receipt route UNREAD; the board question) → Codex `0066` (07:08Z): option (a), a separate inbox, the directory form preferred but not required; the integration requirements (append-only events, physical isolation, the switch selecting destinations, `from` as attribution, backlog on a watch that starts at EOF, torn rows) — carried to Rab on the Desk in Codex's words at 07:09Z ("what codex thinks"). `MSG-FAB-0088` (07:08Z: the watchers as commands and rules, not a pasted script — the relay is public) → Codex `0067` (07:10Z): it wakes through its existing scheduled heartbeat (cadence UNREAD), starts no duplicate, qualifies `desk.py watch` as EOF-offset based. `MSG-FAB-0089` (07:2xZ: the landed route, the 32/32 receipt, the live evidence, the private watcher scripts by path, its first entry). Confirmed by me with restatements: `0065`, `0066`, `0067`; Codex confirmed `0086`, `0087`, `0088`. The Fable beat refreshed; `gate.py check` settled the lane to idle after each confirmation.

**The watcher scripts (Observed):** `agent-scripts/navigation/watch_relay.sh` and `watch_desk.sh` with their briefs and README rows — two tools inside the frozen window (S142–S145, `instruments-frozen/rows-only`), on Rab's word (795574c2), named here for the window's reader. `watch_desk.sh codex 5` on the live Codex board, run 1 (07:18Z): the first line at 07:18:01Z, the entry's line at 07:18:36Z (two seconds after the post) — and the STOP file, touched at 07:19:22Z, IGNORED: the run ended at 07:20:31Z by the caller's `timeout` (`stopped (signal)`, exit 124). Cause, read in the script: the child was a pipeline and a pipeline's `$!` is its last stage, so the loop's `kill -0` addressed the tee, fell through, and `wait` blocked. Fixed in both scripts (the python is the child; its output by process substitution) and re-run: `watch_desk.sh codex 5` run 2 stopped on its STOP file at 07:22:59Z (exit 0); `watch_relay.sh Fable` printed the ungated 0027, then `CONFIRMED MSG-FAB-0089 by Codex` live at 07:22:38Z, and stopped on its STOP file at 07:22:58Z (exit 0). The briefs carry run 1 as their first WHERE IT HAS FAILED line and run 2 as the EXAMPLE. **MSG-FAB-0089 said "then the STOP file, exit 0" of run 1 — that line was wrong when posted** (written from the design, not the log); corrected on the bus in `MSG-FAB-0090`.

**Contracts touched:** `relay-desk/README.md` (Two boards; the identifiers confirmed at integration); the Desk→Desk rule extended (a Codex-board post is Codex's to answer); `agent-scripts/README.md` (two rows); the tickets file (the `desk` and `relay` categories). SEQUENCES §1's Desk inbox row reads the Claude board — unchanged, said.

**What an agent is handed:** "a ground is the contract; a message is not an amendment; a lane that verifies the code over a message did right. The Desk's switch selects the board a post lands on, not only its colour."

**Discovered (→ §8):** F7 — a message text carrying the word "git" inside a python wrapper's argument is read by the guard as a verb (the Desk reply at 07:19Z, denied once, re-sent without the word) — the mentions-git rule over a wrapper's quoted text, by design, a false deny by the letter; counted (denies == log). F8 — the fleet's lanes read my SendMessage as a possible injection: the instruction-source boundary worked in the lanes' favour and against the coordinator's change; the ground must carry every requirement before the launch. F9 — Codex's lane was never dead: seven entries in fifty minutes, each confirmed within minutes.

**Residue (→ §10):** no Codex entry has yet come from Rab's phone (the page's switch is Intended until he loads it); the Desk task re-launches the server at the next logon (today's restart is a detached process); `File Portal desk watch` (the logon task) watches Claude's board only; the research record (E4) is written in first version this window and closes with its own episode.

**Receipts:** `journal/{calls,cost,usage}/S143-ep3.md` — window 2026-09-13T07:05:23Z → 2026-09-13T07:26:43Z (tiled onto the previous receipt's end); calls 101 · preloaded 13 · consulted 2 · scratch 4 · inferred 29 · harness 53 · denied 1 · errors 0 · lanes: 3 lane transcript(s) with calls in the window · lane calls 78 · lane denies 1 · workflows: ; Grand total (USD equivalent): 15.90 (session 12.28 + fleets 3.62); Categories used: auditing (1); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 0 · advisories 0 · Rab's mid-turn words 15. Ledgered D0070 (calls) · D0071 (cost) · D0072 (usage) · D0073 (fleet_desk_ground.md) · D0074 (watch_codex.log) · D0075 (watch_codex2.log) · D0076 (watch_relay2.log); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 76 · twin rows 76 · disagreements 0;   """(lines, red): every `### \`<path>\`` under a `## <sha>` heading of the code receipt is in that commit's --name-only;; the denies cross-check: denies: guard_git main: journal 1 == log 1 · guard_record main: journal 0 == log 0 · lanes: journal 1 == log 1 ✓ (window 2026-09-13T07:05:23Z → 2026-09-13T07:26:43Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 28 of 28 messages (100%), 2026-09-13T07:05:52.548Z → 2026-09-13T07:26:42.920Z.

## Episode 4 — research (written 07:0x → 07:3xZ, interleaved with E3) — PARTIAL CLOSEOUT

**Ticket:** `research/pdf-standard-vs-our-errors` — DONE as a first version: the private `measurement/RESEARCH-2026-09-13-scans.md` (committed in E3's private commit `9216546`; its last source added after). Every claim tagged; every Cited claim carries its URL and what was read (a fetched page or abstract, or the search engine's snippet — said which; pdfa.org and loc.gov returned HTTP 403 to the fetcher, so the standard's own clauses are paraphrased from its publisher's pages, and that is said in §5).

**What it found, in five lines:** (1) the format separates rendering from text extraction by design (ISO 32000-2, per the LOC description) — the two views our audit compares are the architecture, not a tool's fault; (2) the OCR layer is invisible text (render mode 3) that conformance never verifies against the image (the PDF Association's pages; OCRmyPDF exposes no quality gate); (3) the same two paths are an attack surface — "split-view PDFs", 25 gaps across 16 stacks and 7 LLM services, "dual-view consistency" as the defence (Liu & Ming 2026) — our agreement witness is that defence, and for a scan without a layer it has one view; (4) the ink itself can be wrong before any OCR (Kriesel's JBIG2 case); (5) the literature measures omissions as CER's D term hidden in a ratio, decomposes it per page (the Character Error Vector), reads engine confidence as a feature (ConfBERT), evaluates without ground truth (coherence, region entropy, redundancy), and runs layout before OCR. The mapping table (our error → the sources → the measurable) is §4.

**Contracts touched:** none (a measurement file). **What an agent is handed:** "a source is the standard, its publisher, a reference tool's own docs, or a dated paper; a blog is a pointer; a fetched abstract is not the paper's numbers — say UNREAD."

**Discovered (→ §8):** F10 — the standard's own text was not readable by the harness's fetcher (403 from both its publisher and the Library of Congress); the research says so rather than paraphrasing as if quoted.

**Receipts:** `journal/{calls,cost,usage}/S143-ep4.md` — window 2026-09-13T07:26:43Z → 2026-09-13T07:32:13Z (tiled onto the previous receipt's end); calls 17 · preloaded 4 · consulted 0 · scratch 1 · inferred 7 · harness 5 · denied 0 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 wor; Grand total (USD equivalent): 2.47 (session 2.47 + fleets 0.00); Categories used: testing (1); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 0 · advisories 0 · Rab's mid-turn words 5. Ledgered D0077 (calls) · D0078 (cost) · D0079 (usage) · D0080 (RESEARCH-2026-09-13-scans.md); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 80 · twin rows 80 · disagreements 0;   """(lines, red): every `### \`<path>\`` under a `## <sha>` heading of the code receipt is in that commit's --name-only;; the denies cross-check: denies: guard_git main: journal 0 == log 0 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-13T07:26:43Z → 2026-09-13T07:32:13Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 6 of 6 messages (100%), 2026-09-13T07:26:44.737Z → 2026-09-13T07:31:54.055Z.

## Episode 5 — ml-gauge (07:2x → 07:4xZ) — PARTIAL CLOSEOUT

**Ticket:** `ml-gauge/scan-omission-observability` — DONE as a gauge with its first ground truth: the private `measurement/ML-GAUGE-2026-09-13.md` — the problem from our data, six instruments each with numerator, denominator and conditions (a second witness · the engine's confidence read out · ink coverage under recognised boxes · variability as an ensemble with a disagreement map · a learned omission detector trained on the bench's repairs · the verdict weighing its denominator), the modes of observability named (the number, the map, the calibration curve, the denominator), what "the model that works" is when gauged as a deep learner (region omission before character error; two views before one better view; calibrate then gate; the bench is the ground truth), what was and was not measured, and the next three probes.

**The ground truth measured this sitting (Observed, `measurement/probes/scan_ink_probe.py` under marker-env, no OCR, no GPU):** Valentine's scan (the S142 E1 row 5 specimen): 465 pages · text-layer pages 1 · pages with an image 464 · codecs FlateDecode 462 / DCT 1 / Flate+DCT 1 / none 1 — no JBIG2 · the first image's effective dpi median 501. Ink fraction (dark pixels / all, greyscale render, threshold 128): at 72 dpi median 0.0009, "blank-ish" (< 0.5 %) 394 of 465; at 200 dpi median 0.0422 (q1 0.0341 · q3 0.0467 · max 0.2369, page 1 the cover), blank-ish 23 of 465, dense 1. **The first number was the instrument's condition, not the book** — a 501-dpi scan's strokes average to grey at 72 dpi; the denominator any coverage measure needs is taken near the scan's own resolution and the condition printed beside it (docs/34, made concrete). The two CSVs (465 rows each) are ledgered below.

**The machine, read not assumed (Observed by import in marker-env):** surya-ocr 0.17.1 (Marker's engine), marker-pdf 1.10.2, torch 2.11 cu128, pymupdf 1.28, numpy, OpenCV, pdftext; NO second OCR engine (no tesseract binary; no pytesseract, paddleocr, doctr, easyocr, rapidocr, onnxruntime); the old scan bundle (held and anchor copies) has no `blocks.json`, so the ink-coverage instrument needs a fresh Marker pass (GPU) before it can be read on Valentine.

**Not measured, and why (said):** the second witness (an install — Rab's word; a persistent machine change), ink coverage on Valentine (a GPU pass — Rab's word; the GPU law was read at 05:43Z and no GPU was spent this sitting), surya's confidence (produced inside a Marker run — the next probe), the learned detector (a training run — after the features, or text-only over the repairs next).

**Contracts touched:** none. `measurement/probes/` gains one probe (a probe is evidence, not a tool — the frozen window stands; the census counts it as a probe).

**What an agent is handed:** "a proposal without its measured denominator on our own pages is an opinion; a measurement without its condition is the wrong number — the 72-dpi blank count was."

**Discovered (→ §8):** F11 — the ink denominator moved 17× (394 → 23 blank pages) with the render resolution alone; any page-level image measure must carry its dpi and threshold in its name. F12 — the pipeline's only OCR engine is also its only witness for scans; a second view is an install decision, not a code change.

**Residue (→ §10):** the three next probes (surya confidence on N pages; ink coverage on a bundle with `blocks.json`; the omission labels from `repairs.jsonl` and the analyst's edits); `compute_verdict` weighing `pages_scored / pages` (a rule change inside the frozen window — S146's row); the bench session on a scan (Rab's hands).

**Receipts:** `journal/{calls,cost,usage}/S143-ep5.md` — window 2026-09-13T07:32:13Z → 2026-09-13T07:34:25Z (tiled onto the previous receipt's end); calls 10 · preloaded 0 · consulted 0 · scratch 1 · inferred 3 · harness 6 · denied 0 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 wor; Grand total (USD equivalent): 1.33 (session 1.33 + fleets 0.00); (no line starting with 'Categories used'); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 0 · advisories 0 · Rab's mid-turn words 1. Ledgered D0081 (calls) · D0082 (cost) · D0083 (usage) · D0084 (ML-GAUGE-2026-09-13.md) · D0085 (valentine_ink.csv) · D0086 (valentine_ink200.csv) · D0087 (valentine_ink200.out); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 87 · twin rows 87 · disagreements 0;   """(lines, red): every `### \`<path>\`` under a `## <sha>` heading of the code receipt is in that commit's --name-only;; the denies cross-check: denies: guard_git main: journal 0 == log 0 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-13T07:32:13Z → 2026-09-13T07:34:25Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 3 of 3 messages (100%), 2026-09-13T07:32:34.018Z → 2026-09-13T07:33:36.747Z.
## §19 Close values — `close.sh 242c397`, 2026-09-13T07:37:32Z (verbatim)

```
    PIN              242c397
    DIFF             11 file(s) since 242c397 · dirty 0 = mine 0 · peer-owned 0 (none) · 1 untracked
    GLASS            clean since 242c397 (--enforce; bare runs exit 0 regardless — SYM-046)
    RUST             skipped — no windows-widget/ change since 242c397
    CI               still running on 5a3d633f — close may proceed; CHECK IT AFTER
    LEVERS           no unlevered threshold constants added since 242c397
    DOCTOR           lever lexical parity: figure-triage.txt names 8 · quoted refs 8 · MISSING 0
                     LEXICAL REF: mode · min_area_pt2 · vector_min_paths · cluster_gap_pt · text_coverage · words_per_line · table_overlap · accounted_for
                     UNREAD — a quoted lexical occurrence does NOT prove the consumer reads the lever; ARMED on MISSING only
    DOCTOR-HEAD      UNREAD — closeout-headline probe re-run not yet mechanized; NOT a statement the numbers reproduce
    CENSUS           selftest.sh: declared 11 = FIRED 11 = banner 11/11 (exit 0)
    CENSUS-SCOPE     UNREAD — broader TEST-STRATEGY fixture census not yet mechanized; NOT a statement all promised tripwires ran
    REGISTER         OPEN-TASKS.md written this session: +11 / -3 lines
                     STRUCK 11 · ADDED 8 — net -3, the register moved DOWN
    MEMORY           clean — soft clock is DURABLE at ee13b62
    DEBT             open SYM 43, unchanged since 242c397
    PUSH             ahead of upstream: 0 (the close pushes; 0 after)
════════ close exit 0 — values only; the judgment half is SKILL.md ════════
```

- **DIFF:** every changed file is in the content commit `5a3d633` or this closing commit; the row follows, then
  `row_check.sh 143` on it, then the push.
- **Relay:** no entry — nothing to Codex (standing).

