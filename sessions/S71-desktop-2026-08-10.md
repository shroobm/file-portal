# S71 — Desktop lane — 2026-08-10

**Closeout under `docs/21`.** Tier: **EXTENDED** — code shipped, an env was created, and the
machine rebooted mid-session. The crash is part of the record, not a footnote.

## §1 Session Intent
`Observed` — plan `ac1939e`, Rab's words: calibration first, then the transcribe build, set up
for his hands in a fresh File Portal instance; UI design phase deferred until after deployment
(his mid-session instruction: document everything; do not change the UI yet).

## §2 Starting State
`Observed` — clocks equal at S70/`3e4bc0a`; pipeline idle; all six source PDFs in done/; GPU
free. SYM-003 the sole open row — the commission's target.

## §3 Development Chain
`Observed` — `ac1939e` plan · then (committed at close, this session's tree): the calibration
prototype + results + report · the transcribe build (worker, bench.py, bench.html, acceptance)
· docs 22/23/24 calibrated-truth amendments · SYM-021/022 · CHANGELOG · this file.

## §5 Observable Contract
`Intended` — Rab:
1. Open the widget fresh → Bench → the held Damodaran → drag a rectangle on a table → **⌨ read
   region** — **fails if** no editable side-by-side proposal appears with a gate receipt line.
2. Insert it → **fails if** ↩ undo does not restore byte-identically or the repairs rail shows a
   broken image instead of a ⌨ chip.
3. `prototypes/repair-bench/.sandbox/0d68f0e02293970c--20260810-053558` — **fails if** the G5
   sandbox does not hold a real transcribed finance table after zone 1's line.
4. `calibration-report.md` — **fails if** any [M] number there disagrees with `results.jsonl`.
5. Acceptance: **fails if** `acceptance.py` reports fewer than 35/35.

## §7 Implementation Delta
`Observed` — new: `prototypes/docling-calibration/` (4 files) · `transcribe_worker.py` ·
`sessions/S71`. Modified: `bench.py` (+2 methods, +2 routes, constants, kind-aware undo,
restart-safe arithmetic) · `bench.html` (⌨ button, proposal panel, repairs-rail chip) ·
`acceptance.py` (26→35) · docs/22 · docs/23 · docs/24 · SYMPTOM-INDEX (+2 rows) · CHANGELOG ·
CLAUDE_README. Env (outside repo): `C:\Users\Bndit\ml\docling-env` (recipe in the README).
**marker-env untouched. The widget exe untouched — the Bench is spawned fresh from these files;
nothing to adopt.**

## §8 Decision Ledger
1. **Calibrate before build** (Rab's order, the S28 tradition) — the calibration then rewrote
   the plan: crop timing bands, fp16 rejection, worker cap 240→360.
2. **Process-per-request worker in an isolated env.** Rejected: installing docling into
   marker-env (production lane ≠ lab bench) and a resident server (VRAM must return; SYM-022
   later proved the instinct right at machine level).
3. **Restart-safe line arithmetic + kind-aware undo** — a designed-then-caught double-count:
   transcribe records carry `lines`; undo pops the provenance record with the text. Rejected:
   drift-ledger-only (dies with the server) and manifest-blind undo (desyncs provenance).
4. **v1 gate posture:** auto-refuse only on parse failure/empty read; witness metrics REPORTED
   on the receipt, thresholds unsigned (Rab's), the human is the gate, ✂ embed is the floor.
5. **Calibration trimmed live** (p900 + scan pages dropped) once six page rows had decided the
   document-scope verdict — measured evidence over ritual completeness.

## §9 Evidence
`Observed` — hello: 258M on CUDA, 963 MiB, page 6 → clean markdown. Calibration 13 rows
(fsync'd): pages 3.5–149.4 s; crops 2.0–320.8 s (†contended); damodaran table crops **numeric
J = 1.0 twice**, p100 page J = 0.9688 + `<otsl>`; valentine scan crop md=0 ⇒ refusal path
census. fp16: no first record in >10 min at 100% GPU → tree-killed. Acceptance **35/35**
(including exact-increment shift check and provenance-pop check). UI probe 7/7 over HTTP.
**G5 live: real zone → p396 (text-evidence), read 90.6 s uncontended, table emitted, J 0.95,
applied 27 lines, undo byte-identical, re-applied for inspection.** py_compile both envs.

## §10 Known Failures
- **The machine BSOD'd (VIDEO_SCHEDULER_INTERNAL_ERROR) and rebooted** during calibration.
  My contribution: killing a `;`-chained background task's current python let its second command
  launch anyway (SYM-021) → two model processes contended (SYM-022) until the driver faulted.
  **Zero data lost** (fsync-per-row + files on disk). Both rows filed; laws written.
- The G5 driver first crashed on a cp1252 console glyph (the S48 print class) — fixed in the
  driver; bench.py already guarded.
- Dense-crop reads cost ~90 s — honest but not instant; the live clock carries it. Batching
  (all zones per wake) is the noted future economy.

## §12 Uncertainty Register
`Unknown` — CI on the close push (expected green: python jobs don't cover prototypes;
rust untouched). Whether 90 s/dense-crop feels acceptable on Rab's bench — his hands decide.
Gate thresholds + audit credit: unsigned, unchanged.

## §17 Current State
`Historical` — post-reboot, GPU healthy, pipeline idle, vault untouched (`70c60e6`), held/ = 4
untouched (G5 ran in sandbox). The fresh instance is READY: widget → Bench → any patient → ⌖/drag
→ ⌨. Design-plan phase (the big-screen UI brief) is NEXT, per Rab's sequencing — not started.

## §18 Next Entry Point
1. MUSTER; newest row S71, ancestor-verified.
2. **Rab's try-out** (§5 items 1–2), then his verdicts: gate thresholds · repair audit-credit ·
   whether dense-crop latency wants the batch-per-wake economy.
3. **The design-plan phase** (commissioned, sequenced after his look): the big-screen
   "mechanical-keyboard-reel" brief that keeps File Portal File Portal — a PLAN document first,
   no UI changes without it.
4. Standing queue unchanged: Decision 5 · the four · Valentine zones 2–4 · Cybernetics-stale ·
   Textor.
