# S174 — Desktop — 2026-09-16 (local; 00:22Z on the 17th) — THE HELD-FAIL PARK READ, AND THE REGISTER'S SIXTH CUT (THE LIBRARY'S BRIEFS) (Claude Opus 5's twenty-first sitting in the Fable lane; the autonomy experiment's twenty-ninth)

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

No new word of his opens this sitting; his two of S172 stand ("Thinkpad is on." · "Touch thinkpad"), under his standing words:

> "Run autonomous until I say stop. Keep doing tasks, be persistent." (Desk d7d03d67, S157)
> "…the ThinkPad over ssh: ATTEMPT it and report exactly where it stops." (the experiment's rule, 2026-09-13)

S173's carry-over set the order: **(0)** the index's ROOM (17,170 of 17,510): the S173/S172/S171 lines → one; **(1)** the register row `thinkpad/held-fail-park-unread` — READ on the box: the park's listing and its manifest's verdict; the code that parks (found here: `convert_and_ship.py:1927-1936` `ship()`'s move-aside `.held-<sha16>-<stamp>` (S146 E4, SYM-128), the S144 E4 hand repair it mechanised — the park on the box carries `held-fail`, S144's hand-named form); the desktop's `held/cb7e026f3fc02da8--reshipped-…` row; one line: the two sides describe the same event or two — then the row struck or turned; **(2)** the SIXTH cut — the private library's briefs: every `**WHAT CAN MAKE IT WRONG.**` paragraph against the script's selftest cases (a survey script over `agent-scripts/*/*.brief.md` and `*_selftest.py`), the shortlist read whole, a case built for one if it fits; **(3)** `library_smoke.py` at the close.

## §2 Starting State — the `open.sh` card, verbatim (2026-09-17T00:21:50Z)

```
════════ MUSTER · OPEN · 2026-09-17T00:21:50Z ════════
[0] IDENTITY
    ledger newest    S173  f2e3e69
    this session     S174  ·  desktop  ·  2026-09-16
    session marker   coordination/private/session.current = S174 (guard_record / guard_git read it)
    advisory         S174 unnamed in the inherited tree
[1] GROUND
    verifier         07BD3BA7  (repo == ~/.claude)
──────── MUSTER · 2026-09-17T00:21:51Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 207 (tally == hook)
[3a] LEDGER ORDER  ✓ 156 Desktop-lane rows ascend (S16 → S173)
[3b] LEDGER PARSE  ✓ 156/182 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · unchanged since the close f2e3e69 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ f2e3e69 (S173) == ledger f2e3e69, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 8 · anchor 25 · pending 0 · drop 0
    intake           waiting 0 · oldest 0s · active null · written 3s ago by pid 13620
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    events           391 line(s)
    open-tasks       99 item(s) · last written 10 minutes ago
    error-bin        109 row(s) — read the CLASS table before you probe · last written 69 minutes ago
    symptoms         133 row(s), 29 open · last written 89 minutes ago
    relay            208 entries · run `gate.py status` for the board
    widget           10056
    widget autostart task 'File Portal widget (J57)' Running — AtLogOn · auto-logon ON (Bndit)
    task             'File Portal desk (relay)' Running LASTRUN=2026-09-15T22:51Z RESULT=267009
    task             'File Portal relay watch (Fable)' Ready LASTRUN=2026-09-15T22:50Z RESULT=0
    task             'File Portal tracker' Running LASTRUN=2026-09-15T22:51Z RESULT=267009
    task             'File Portal desk watch' Running LASTRUN=2026-09-15T22:51Z RESULT=267009
    python procs     8
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    adoption         C4559805 (2026-09-04T02:3xZ, by rab) · installed C4559805 — MATCH
    vault            7 note(s) · tip f489189
    thinkpad         converter=active · vault tip f489189 · staging 0 · receipts 25
[3] PIN
    --since          f2e3e69
    closeout         sessions/S174-desktop-2026-09-16.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

**The pin:** `--since f2e3e69` (S173's closing SHA; the row `16fd889`; CI green run 35165750597 / num 500). The marker `coordination/private/session.current` = S174. `id_preflight.py --next S`: `next free S: S174`. No record exists under S174 before this one. **This record precedes every write of this sitting.**

## §3 Inherited, stamped Historical

- **S173's post-close CI** (bfd6472): `ci_observe.py` launched 00:20Z in the background (`sittings/S173/ci_observe_post.txt`) — not yet read at this write; reported in E1's receipt or §20 (docs/21 §9). S173's Gmail draft r-2097927832639517962 (never sent); the Desk close report 5ea5a2de.
- **The park (Observed S173 E2b; Historical S144 §10):** `~/file-portal/library/staging/.held-fail-cb7e026f3fc02da8-20260912-205007` (dir, Sep 13 09:08 mtime) — S144 §10: "the ThinkPad's staging holds one dot-dir (…the failing rerun's copy, kept)"; S144 E4's hand repair of a nested reship (SYM-128) → S146 E4 mechanised it in `ship()` as `.held-<sha16>-<stamp>` (no `fail` in the mechanical name). The desktop's `held/cb7e026f3fc02da8--reshipped-20260912-074321--reshipped-20260913-090357` (S144 E5's reship chain); the receipts' 09-13 09:08Z `supersede-noop` for Zero-to-One. Not yet read: the park's contents and its manifest's verdict.
- **The ThinkPad, as S173 left it:** the clone at `2eb9f4a`; the allocator (pid 3354) and the converter (pid 7685) on the current code; the card's row `converter=active · vault tip f489189 · staging 0 · receipts 25` (the card counts non-dot staging: 0 — right).
- **The lessons in force:** the record before any act; two calls at the open; a remote `git` over ssh in a script by path; `grep -c` at zero; the close's apparatus as S173 left it; the Desk watcher (bxon5w6gc armed 00:06Z, cap ~00:36Z — re-armed at its expiry).
- **Rab's standing words in force (Historical, unchanged):** the persistent method; AUTO MEANS NEVER IDLE; scripts private; the GPU law (nothing dropped on the ThinkPad; the park READ, never moved — a move of the park is the exporter's territory and his); no rebuild/adoption/drop; a Desk post answered on the Desk; nothing to Codex.

## §4 The echo (what each act must be, before it is done)

**The fold (E0).** `compact_memory.py ">   **S173 (" ">   **S170–S168**"` → one `S173–S171` line ≤ ~420 bytes keeping `f2e3e69`/`16fd889`, `7b2139a`/`ba1fc9a`, `adf955a`/`6dd85ec`, the ThinkPad deploy, the hook's live proof, docs/18's tripwire; the read-back check; ≥ 550 freed; a library commit, no cookie. **The park (E1), READ only:** `ls -la` the park, its `manifest.json` (`fidelity.convert.verdict`, `doc_survival`, `source_sha256`), its `.md` name and size; the desktop's held row's manifest the same (`held/cb7e026f…/manifest.json`); the code quoted (`ship()`'s move-aside; S144's hand form); one line in the row's cell: the same book, two copies — the ThinkPad's park is the failing rerun's staged copy the exporter never ingested (dot-prefixed), the desktop's held is the local one — agree/disagree said; the row STRUCK if the reading closes it, else turned into the row the disagreement needs. NOTHING moved or deleted on either side. **The sixth cut (E2).** A survey script: for each `agent-scripts/**/*.brief.md` with a `**WHAT CAN MAKE IT WRONG.**` paragraph, the failures it names (sentences) and whether a sibling `<name>_selftest.py` exists and mentions the failure's key words; the shortlist (a brief naming a failure with no selftest, or a selftest not naming it) read whole; one case built if hermetic and small. **The smoke (E3)** at the close.

## §5 The method

Episodes with receipts (`close_ep.py 174 <K> <since> "<category>"`; the first `<since>` = S173's whole receipt END 2026-09-17T00:12:39.441Z), a cookie per episode (E0 none), a Desk line per episode, the carry-over updated; then the close through the tool, the driver's checker first.

## E0 — the index given room (00:23Z → 00:24Z; `date -u` read at the script's run)

**What.** `compact_memory.py ">   **S173 (" ">   **S170–S168**"` — S173 · S172 · S171 (3 lines) → one `S173–S171` line (536 B; −511); `e0_fold_check.py` (S173's list + the S173 SHAs and this fold's phrases): **0 missing**; nothing below `## General` moved. **17,170 → 16,659; room 851.** Library commit `14b263a`, no cookie.

## E1 — the held-fail park read on both sides; the row struck (00:24:18Z; `e1_park_read.sh`, nothing moved)

**The ThinkPad's park** (`~/file-portal/library/staging/.held-fail-cb7e026f3fc02da8-20260912-205007`, Observed): a whole staged bundle — `Zero-to-One by Blake Masters and Peter Thiel.md` (297,756 B, mtime Sep 12 20:50), `.marker.txt` (295,098 B, Sep 12 03:39), `blocks.json` (704,480 B), `assets/`, `manifest.json` (12,033 B) — 1.3 MB; the manifest: `source_sha256 cb7e026f3fc02da8`, `engine marker`, `convert.verdict None` (no verdict key — a pre-verdict manifest shape), `doc_survival 0.9955`, `pages_flagged 0`, `analyst.doc_survival 0.9997`, **`supersede {reason: 'analyst-rerun', from_verdict: 'fail', requested_at_epoch_s: 1789246207}`** (= 2026-09-12 20:50:07Z); the park's own mtime **2026-09-13 09:08:30.41Z** — the second the receipts record `supersede-noop` for Zero-to-One (E2 of S173): the hand move-aside of S144 E4 and the exporter's noop are one event.

**The desktop's held row** (`held/cb7e026f3fc02da8--reshipped-20260912-074321--reshipped-20260913-090357`, Observed): the same `source_sha256`, `engine marker`, `convert.verdict None`, `doc_survival 0.9955`, `analyst.doc_survival 0.9947`, **`supersede {reason: 'reaudit', from_verdict: 'pass', requested_at_epoch_s: 1789290236}`** (= 2026-09-13 09:03:56Z — the S144 reaudit), three `.md`-shaped files (the reship chain's copies), `blessed None`.

**The code** (Historical S144 E4 → Observed at HEAD): `convert_and_ship.py:1927-1936` `ship()` — "a reship under a name still sitting in staging nested the new assembly inside the old copy (S144 E4, repaired by hand as `.held-<sha>-<stamp>`). The same repair, mechanical and BEFORE the move: an existing name is moved aside under a dot-prefixed name the exporter's sweep does not ingest" — the park on the box carries S144's hand-typed `held-fail` form; the mechanical form since S146 is `.held-<sha16>-<stamp>`.

**The reading, one line.** One book, two kept copies, nothing pending: the ThinkPad's park is the 09-12 20:50 reship's staged copy that S144's hand moved aside so the reaudit's reship would not nest into it; the desktop's held is the later copy; the vault has held the pass copy since `8d7cc85`. **The two sides agree.** The park is 1.3 MB of kept history the exporter ignores by its dot — its removal is the ThinkPad's housekeeping (a named act for a later sitting, or his word); this read moved nothing. **The row STRUCK** in the S157 form with the reading; the register 99 → 98.

**Not proved.** Whether the three `.md`-shaped files in the desktop's held dir are byte-identical copies or three reship states — their sizes differ (295,098 · 292,557 · 293,782); a later read if a row needs it.

## E2 — the register's sixth cut: the library's briefs, and the fold tool given its selftest (00:25Z → 00:28Z)

**The cut** (`e2_briefs_cut.py`, private, read-only; `e2_briefs_cut.txt`): 67 briefs under `agent-scripts/`; **12 scripts have a sibling selftest, 49 do not**; of the 12, eight `WHAT CAN MAKE IT WRONG` sentences name a thing their selftest never mentions (`id_preflight`'s `TEXT_EXT`, `occupant`'s majority model, `scripts_used`'s nested/segment rules, `lockstep.sh`'s "ci_observe verifies", `memory_timestate`'s "row_check is the verifier", `tool_census`'s text classification and `born`). The 49 without, read whole: **twenty are the handoff TEMPLATEs and their per-sitting instances** sharing one brief sentence ("a path or a sitting value left from the instance it was promoted from") — fixtures by nature, not tools to selftest; the read-only navigators (`find_bundle`, `pipeline_roots`, `bundle_audit`, `thinkpad_state.sh`, `watch_*.sh`) whose failures are the world's (an asleep ThinkPad reads UNREAD — the muster's own case 8); the receipts (`_calls`, `_stamp`, `cost_receipt`, `proc_receipt`, `fleet_proc_census`) whose failures are bounded by the journal's shape; `close_chain.sh` ("gates on the EXIT CODE — a gate that prints red and exits 0 passes: that gate's bug") — the muster selftest's cases 15–18 and 61–66 are its gates' cases; `code_journal.py` ("a file absent from the notes is UNEXPLAINED — a red, never a guess") — `code_notes_check.py`'s selftest covers the pre-step, the journal's own case is owed; **and the one the closes lean on nightly with no case at all: `compact_memory.py`** — "a block whose first line was edited by hand is not found (the dry run says so)" — used at S169, S171, S173 and this sitting's E0 by hand-typed prefixes.

**The build.** `compact_memory.py` gains `FP_MEMORY_MD` (two comment lines; the brief's INPUTS says so; unset, the library's index — the S166 shape). `compact_memory_selftest.py` (new, 13 cases; `library_smoke` walks it): a planted eight-line index; **(1)** a dry run prints `fold lines 3-5 (3 lines) -> 1 lines; bytes …` and `dry run`, exit 0, **(2)** writes nothing; **(3)** `--go` folds the span into the block and prints `written`; **(4)** the stop line and everything after byte-identical, the lines before untouched; **(5)** the delta printed equals the measured one; **(6) the brief's own failure** — a first prefix edited by hand (`>   **S140 EDITED (`) matches no line → `refused: first matches 0`, exit 1, **(7)** nothing written; **(8)** a prefix matching four lines refused; **(9)** first after stop refused (`order ok=False`); **(10)** usage exits 2; **(11)** a CRLF fixture keeps its CRLF; **(12)** the plant's anchor present; **(13) NEGATIVE CONTROL** — a copy with the uniqueness check weakened (`!= 1` → `< 1`) FOLDS on a two-line prefix — the check discriminates. `ALL OK: 13/13` on the first run; `ruff` clean on the selftest (the script's own E401/E702/E741 are its S13x style, untouched).

**Not proved.** The eight uncovered sentences of the 12 — read, not built; `code_journal.py`'s own case — owed, named in the carry-over.

## E3 — the private smoke, under a game's load (00:32Z → 00:42Z)

**The run** (`library_smoke.py`, nothing of the lane's writing during it; `sittings/S174/e3_smoke.txt`): **79/80 green, RED, 159 s** — the one red `testing/run_gates.sh TIMEOUT after 120s`; the three write tripwires `unchanged`; `compact_memory_selftest.py` walked by the smoke for the first time (green). Past smokes ran the same script in 44.8–58.8 s (S169–S171's outputs, grepped).

**The cause, read, not guessed.** `run_gates.sh` alone, timed per gate: `[1]` schema registry 25 s, `[2]` `acceptance.py` **99 s** (90 s on its own: `PASS — 187/187 checks`), `[4]`/`[4b]` instant — **`ALL GATES GREEN` in 124 s**. The machine: `Win32_Processor LoadPercentage 76`; `ReadyOrNotSteam-Win64-Shipping` pid 2896 at 3,896 CPU-seconds and 4.4 GB — **a game on the desktop's seat** (his, or a friend's over Moonlight; the GPU law's "the seat may be his brother's"). Nothing of the lane's touched it or the card. The gates are green; the smoke's 120 s bare-run cap read the load as a red.

**The build.** `library_smoke.py` gains `CAP` beside `SKIP` (`run_gates.sh: 400` — a bare run of that one tool RUNS its four gates; every other tool keeps 120); the brief's WHAT CAN MAKE IT WRONG says so and adds the reading rule: a TIMEOUT row is a cap, never a gate's verdict — run the tool alone before reading it red. Re-run `--only run_gates` under the same game: `exit 0 … (98.8s)`, 7/7 — under the old cap this time too (the load varies); the cap's edge itself is not exercised.

**Not proved.** The smoke's whole run again under the cap (the game holds the seat; 159 s of it would repeat the reading, not add to it); a smoke run on an idle machine — the next sitting's, if the seat is free.

## E4 — gained / lost / missing (00:45Z)

**Gained.** The index's room (S173…S171 → one line; 0 missing; 16,659). The held-fail park READ on both sides before anyone moves it — one book, two kept copies, the vault's pass copy the third; nothing pending; the row struck with the reading (99 → 98). The register's SIXTH cut: the library's 67 briefs against their selftests — 12 scripts with one, 49 without (named by kind), eight failure sentences a selftest never mentions — and the one that mattered built: `compact_memory.py` (the fold tool of four closes, no case) gains `FP_MEMORY_MD` and a 13-case selftest with the brief's own failure and a weakened-copy control. The private smoke walked it (79/80); the one red read for its cause — a game on the desktop's seat, the gates green in 124 s — and the smoke given `CAP` and its brief the reading rule.

**Lost.** Nothing shipped; nothing of his moved; no engine, no model, no PDF, no bundle written; the desktop's card and the seat under his game untouched; no book dropped on the ThinkPad; no fleet.

**Missing.** His hand: the widget's rebuild + adoption (B32's other half, SYM-023); the restart window's four; the ViGEm device node (the six taps posted); the runner's ARMING; the `UNFRAME_STUBS` lever; the §12.5 signature; `reanalyse Valentine`; D1; the experiment's end; his answer on the Desk. The lane's: the eight uncovered brief sentences (read, not built); `code_journal.py`'s own case (owed); the smoke whole on an idle seat; the ThinkPad park's removal (a named act, or his word).

## §7 What was built or changed

- Public (4 files since the pin f2e3e69 before this script's JOURNAL append — one block each in the private `sittings/S174/code_notes_S174.md`, checked by the driver before the chain): `OPEN-TASKS.md` (`thinkpad/held-fail-park-unread` STRUCK with the reading), `sessions/S174-…` (this record), the JOURNAL, `sessions/S173-…` and `CLAUDE_README.md` (S173's post-close §20 and row — S173's acts inside this pin's range). No public code.
- Private: `agent-scripts/developing/compact_memory.py` (+`FP_MEMORY_MD`, two comment lines), `compact_memory_selftest.py` (NEW, 13), its brief's INPUTS; `agent-scripts/testing/library_smoke.py` (+`CAP`), its brief's WHAT CAN MAKE IT WRONG; `sittings/S174/` — the open record's writer, the fold block and its read-back check, `e1_park_read.sh` and its output, `e2_briefs_cut.py` and its output, the selftest's first run, the three record appenders, the smoke's output, the Desk texts, the cookie script, the carry-over.

## §8 Findings

1. A cut of briefs against selftests finds the tool by USE, not by size: `compact_memory.py` is thirty lines and had folded the index four times by hand-typed prefixes with no case; its brief's own failure sentence ("edited by hand is not found") is now case 6.
2. A thirty-line script is selftested WHOLE through a two-line env override (the S166 shape from `memory_timestate.py`) — no refactor; the negative control is a mutated copy of the script in a scratch tree with `_run.py` beside it, and it folds where the original refuses.
3. A TIMEOUT row is a cap's reading, never a gate's verdict: the tool alone, timed per gate (`acceptance.py` 90 s), and the machine read (`LoadPercentage 76`, `ReadyOrNotSteam-Win64-Shipping` at 3,896 CPU-s) — the cause observed, not inferred from the red; SYM-075's family (read the log, never the conclusion).
4. A park is read on BOTH sides before it is moved: the ThinkPad's dot-parked staging copy and the desktop's `held/` copy are one book (the 09-12 reship, the vault's pass copy the third); the row is struck with the reading and the removal stays a named act.
5. A game on the seat is the world's condition, not the library's defect — the fix is a per-tool cap with the reason on the line, not a slower smoke for everyone.

## §10 Known failures / residue (open at close)

- His words, above (§E4 Missing) — B32's widget half; the restart window's four; the ViGEm node.
- The lane's own errors this sitting: ERR-009's hook fired on the brief's one-line update (a python heredoc on the same call as a `$'\r'` grep — the hook reads the whole call; the update ran once behind an assert; the remedy stands: a script by path); the smoke's first reading of the TIMEOUT needed the tool run alone to be read right.
- SYM-023 waits (Rust — his rebuild); the validator UNWIRED; T26 in waiting; the runner row warn-only until his arming; the ViGEm node until his taps; the eight uncovered sentences and `code_journal.py`'s case owed.
- SYM open 29 of 133 rows; register 98.

## §15 Current state (Observed at 00:45:29Z)

- The card: `1780 MiB, 10240 MiB, 56 %` (nvidia-smi); `.gpu-lock` absent; no model of the lane's loaded this sitting; no fleet.
- The seat: `>bndit                 console             1  Active      none   2026-09-15 6:50 PM` (query user); CPU load 31 % (Win32_Processor); ReadyOrNot pid `UNREAD (no output)` (Get-Process — his game, untouched).
- The registers (the muster's counter): open-tasks 98 · symptoms 133 rows, 29 open.
- The public tree: 0 tracked change(s) before the close chain. The private tree: 3 (the services' own appends).
- The monitors: the Desk watcher (bm9h6ia2v, re-armed 00:37Z, filtered `from=Rab`, cap ~01:07Z) — stopped by the close; the tracker is his boot service; the ThinkPad ON (untouched this sitting after E1's read).

## §18 Next entry point

1. The rule: the next sitting opens on the register at the next wake unless his word arrives first. What needs nothing now: `code_journal.py`'s own selftest (owed since E2); the eight uncovered brief sentences, cheapest first (`memory_timestate`'s "row_check is the verifier", `lockstep.sh`'s "ci_observe verifies"); the smoke whole on an idle seat; the index's room after this block (~500).
2. His words: the widget's rebuild + adoption (U02/U04/SYM-023); the restart window; the ViGEm device node; the runner's arming; the lever; the §12.5; the question on the Desk; the experiment's end.
3. The apparatus: a selftest of a script that reads one fixed path = an env override + the script run whole + a mutated copy as the control (`compact_memory_selftest.py`, `memory_timestate_selftest.py`); a smoke red under load = the tool alone, per-gate times, the machine read, then the cap with the reason on the line; a ThinkPad park = read on both sides, moved only as a named act.

## The receipts (tiled onto S173's whole receipt's end, 2026-09-17T00:12:39.441Z)

- E1: window 2026-09-17T00:12:39.441Z → 2026-09-17T00:25:13Z · calls 27 · cost 13.48 (fleets 0.00)
- E2: window 2026-09-17T00:25:13Z → 2026-09-17T00:28:12Z · calls 13 · cost 6.66 (fleets 0.00)
- E3: window 2026-09-17T00:28:12Z → 2026-09-17T00:42:51Z · calls 29 · cost 3.12 (fleets 0.00)
- Receipts: 3 over the episodes (the windows are contiguous from S173's end) · main-lane tool calls in the receipts: 69 · cost in the receipts: 23.26 USD-eq (fleets 0.00 — none) · the sitting's whole receipt is the close chain's.
## §19 Close values — `close.sh f2e3e69`, 2026-09-17T00:49:58Z (verbatim)

```
    PIN              f2e3e69
    DIFF             5 file(s) since f2e3e69 · dirty 0 = mine 0 · peer-owned 0 (none) · 1 untracked
    GLASS            clean since f2e3e69 (--enforce; bare runs exit 0 regardless — SYM-046)
    RUST             skipped — no windows-widget/ change since f2e3e69
    CONVERTER        skipped — no windows-converter/ or prototypes/repair-bench/ change since f2e3e69
    CI               no run for 20648128 yet (unpushed? ahead=5) — NOT a green
    LEVERS           no unlevered threshold constants added since f2e3e69
    DOCTOR           lever lexical parity: figure-triage.txt names 8 · quoted refs 8 · MISSING 0
                     LEXICAL REF: mode · min_area_pt2 · vector_min_paths · cluster_gap_pt · text_coverage · words_per_line · table_overlap · accounted_for
                     UNREAD — a quoted lexical occurrence does NOT prove the consumer reads the lever; ARMED on MISSING only
    DOCTOR-HEAD      UNREAD — closeout-headline probe re-run not yet mechanized; NOT a statement the numbers reproduce
    CENSUS           selftest.sh: declared 11 = FIRED 11 = banner 11/11 (exit 0)
    CENSUS-SCOPE     UNREAD — broader TEST-STRATEGY fixture census not yet mechanized; NOT a statement all promised tripwires ran
    REGISTER         OPEN-TASKS.md written this session: +1 / -1 lines
                     STRUCK 1 · ADDED 0 — net -1, the register moved DOWN
    MEMORY           clean — soft clock is DURABLE at fc172d4
    DEBT             open SYM 29, unchanged since f2e3e69
    PUSH             ahead of upstream: 5 (the close pushes; 0 after)
════════ close exit 0 — values only; the judgment half is SKILL.md ════════
```

- **DIFF:** every changed file is in the content commit `2064812` or this closing commit; the row follows, then
  `row_check.sh 174` on it, then the push.
- **Relay:** no entry — nothing to Codex (standing).

## §20 Post-close (UTC) — CI observed, memory in lockstep

- **CI, `Observed`:** green (run 35168154344, both jobs) (`ci_observe.py`, on the row commit `f0786c5`).
- **Memory, in lockstep:** library commit `97caef9` — TIME-STATE → Desktop S174 / row `f0786c5` / this CI; 16953 bytes under
  the 17,510-byte read-limit hook; pushed to the private remote.
- **Both clocks:** `muster.sh` clean after the lockstep; the row passed `row_check.sh 174` before the push.
- This §20 is a post-close commit and gets its own CI run, reported in the reply, not here.
- **The register's net (close.sh: `OPEN-TASKS.md written this session: +1 / -1 lines` — STRUCK 1 · ADDED 0, net -1, "the register moved DOWN"):** the row `thinkpad/held-fail-park-unread` struck at E1 with the reading; 99 → 98 (the muster's counter at §15 and at the chain).
- **The chain, clean at the first run — the driver's checker first:** `changed since f2e3e69: 5 · headed in the notes: 5 · UNEXPLAINED: 0`; the code receipt `explained 9 · unexplained 0` over 7 commits; `brief_check` exit 0 (61 briefs · 6 retired · 0 defects); the schema and acceptance gates exit 0 (acceptance ~90 s again — the seat's load, see below); GLASS clean since the pin; DEBT open SYM 29 unchanged; the row `f0786c5` (183 of 183) ✓; the occupant 86 of 86 messages `claude-opus-5`; push 5 → 0; CI run 35168154344 (num 502) — python 00:52:20Z, rust 00:54:32Z, both success.
- **The record's own readings, said:** the closing sections are headed `## E4` by the receipt count (three receipts, E1–E3, plus E0 in E1's window) — no double heading this sitting. §15's line `ReadyOrNot pid UNREAD (no output)` is a null from `Get-Process` — the game had EXITED between E3's read (00:38Z, pid 2896) and §15's (00:45:29Z); confirmed at 00:46:04Z (`Measure-Object` count 0, load 62 %). The seat was his game's for E3 and free by the close; nothing of the lane's touched it.
- **The lockstep THROUGH THE TOOL, the eighth close (Observed, `lockstep_run.txt`):** cookie #211 logged FIRST; `lockstep.sh 174 5d21817 f0786c5 "green (run 35168154344, both jobs)" block_s174.txt "…"` → `MEMORY.md would be 16953 bytes (limit 17,510)` · `written` · `fc172d4..97caef9 main -> main` · `MEMORY_SHA 97caef9 16953 bytes · staged: MEMORY.md cookie-tally.md` · SOFT ✓ 211 (tally == hook) · HARD ✓ 5d21817 == ledger, ancestor of HEAD · `VERDICT ✓ CLEAN`. The block 294 bytes; the room after it 557 — the next fold is due within two sittings (S170…S168's line and S167…S164's are the oldest still separate).
- **Receipts (whole sitting, window 2026-09-17T00:12:39.441Z → 2026-09-17T00:47:26.992Z, tiled onto the S173 whole):** `journal/{calls,code,cost,usage}/S174.md` — calls 86 · preloaded 11 · consulted 14 · scratch 0 · inferred 36 · harness 25 · denied 0 · errors 0 · lanes 0 · workflows 0 · compactions 1 · advisories 1 (ERR-009's hook on the brief's one-line update — the `$'\r'` grep on the same call, not the heredoc's body) · cost 25.31 USD-eq (fleets 0.00). The episodes' three receipts (13.48 + 6.66 + 3.12 = 23.26) sit inside it; the close chain's own window is the difference.
- **A label in the receipt that lies, found reading it:** `Rab's mid-turn words 2` counts `queued_command` attachments — this window's two are the Desk watcher's OWN task notifications (an expiry and an event line), none his (the Desk inbox read at every episode: nothing of his since 0e51eae9, 23:50Z). `script_journal.py`'s label names him for what the harness queued; a split (his words vs task notifications) with its case in `script_journal_selftest.py` is the next sitting's, named in the carry-over.
- **Baseline row S174 (`measurement/baseline/S174.json`, read never typed):** `baseline.py S174 --tickets 1 --chain-exit 0` — calls 86 · denies(log) 0 · cost 25.31 · UNREAD fields: tracker, refuter.
- **The relay at the close (Observed):** no entry — nothing to Codex; the Desk carried the E1, E2 and E3 lines; no post of his this sitting; the close report follows this commit.
- **CI:** GREEN on the row commit `f0786c5` — run 35168154344 / num 502; this §20's post-close commit gets its run, reported in the next sitting's §3 (docs/21 §9). S173's post-close CI (bfd6472): read at this sitting's open — green.
- **The card at the close:** 1,780 MiB of 10,240 at 56 % at §15's write (00:45Z) — NOT mine: his seat (the game's tail and his Moonlight); no engine, no model, no probe of the lane's on it all sitting; `.gpu-lock` absent.
- **The seat:** console session Active (his) throughout; Ready or Not on it during E3 (76 % CPU) and gone by the close; the ThinkPad ON, touched once (E1's read over ssh, a script by path).
- **The monitors after this commit:** the Desk watcher (bm9h6ia2v, re-armed 00:37Z, filtered `from=Rab`, cap ~01:07Z) is stopped by the close's last act with the marker; the tracker service is his.
- **Rab's words this sitting:** none new (his two of S172 standing; the gamepad's "done" not yet). The question on the Desk stands; the next sitting opens on the register at the next wake unless his word arrives first — the carry-over names the candidates.
