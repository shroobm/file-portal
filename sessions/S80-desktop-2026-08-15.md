# S80 · Desktop · 2026-08-15

## §1 Session Intent

Rab's commission, in his words:

> "the `--jinja` re-measure — and the gate there is token count, not speed. Ollama produces ~814
> per chunk; llama.cpp produced 1,380–2,048 and twice hit the cap. Until that matches, the +27%
> and the 1.72× are unbankable and the analyst must not be pointed at it."

Preceded by: *"Muster, and start working immediately, make sure everything is set before you
start, verify and double check in case."*

**Written before any work. Left unedited for the rest of the session** (docs/21; S79 §6 made this
requirement effective from S80, and this file is the first instance of it under that rule).

## §2 Starting State

`bash .claude/skills/muster/open.sh` — exit 0, verbatim:

```
════════ MUSTER · OPEN · 2026-08-16T02:11:03Z ════════
[0] IDENTITY
    ledger newest    S79  2729a63
    this session     S80  ·  desktop  ·  2026-08-15
    advisory         S80 is named in the INHERITED tree (2729a63) — read each, do not blanket-correct:
                     sessions/S79-desktop-2026-08-15.md
[1] GROUND
    verifier         344E1277  (repo == ~/.claude)
──────── MUSTER · 2026-08-16T02:11:04Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 72 (tally == hook)
[3a] LEDGER ORDER  ✓ 64 rows ascend (S16 → S79)
[3b] LEDGER PARSE  ✓ 64/85 rows parsed · 21 discarded, newest at row 21 · tail = last 5
[3] HARD clock ... ✓ 2729a63 (S79) mirrors ledger, ancestor of HEAD
    WORKING TREE . 0 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
[2] LIVE
    pipeline         held 4 · anchor 23 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=16
    gpu-lock         absent
    events           137 line(s)
    widget           18856
    python procs     4
    ollama           2
    installed exe    C3D277F7  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 70c60e6
    thinkpad         converter=active · vault tip 70c60e6 · staging 0 · receipts 5
[3] PIN
    --since          2729a63
    closeout         sessions/S80-desktop-2026-08-15.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

**Advisory resolved, not blanket-corrected.** Both `S80` hits in the inherited tree are forward
references written by S79 about its successor — §6 ("takes effect from **S80**") and §18 ("expect
exit 0, S80"). Neither is an artifact claiming to be S80's work. No collision.

**Pinned `--since` = `2729a63`.** Chosen here, before any result exists (S78 §10.1 chose it at
close by `len(diff) > 500` and shipped a green report over a red suite).

### §2.1 Correction to the card, at open — two widgets, not one

The card printed `widget 18856`, a single value. `Observed` immediately after, by
`Get-CimInstance Win32_Process`: **two** `file-portal-widget.exe` processes, **18856 and 19536**,
started 37 minutes apart, **both children of PID 5200 (`explorer.exe`)** — two hand-launches, not
a parent/helper pair. Each had spawned its own full watcher chain:

```
18856 → 6420 (marker-env python watch_and_convert.py) → 16148 (uv python watch_and_convert.py)
19536 → 12448 (marker-env python watch_and_convert.py) →  7300 (uv python watch_and_convert.py)
```

`Observed`: `windows-widget/src-tauri/Cargo.toml`'s `[dependencies]` block carries no
`tauri-plugin-single-instance`. Nothing prevents a second launch, and **SYM-032** says the one
file that looks like it would arbitrate — `.gpu-lock` — gates nothing. Two intake watchers on one
drop folder with no mutex is **SYM-022's exact precondition** (two concurrent GPU processes →
`VIDEO_SCHEDULER_INTERNAL_ERROR`).

Nothing was lost: `drop` was **0** throughout, so neither watcher had work.

Rab closed both mid-open ("Sorry I closed it again"). Re-measured rather than assumed, with two
differently-shaped probes that both ran and agreed — `Get-Process` counts and `tasklist` — widget
**0**, python **0**, ollama **1**. Filed below as a defect of the open, not of Rab.

## §3 Pinned closeout terms (stated at open)

1. Core sections §7, §8, §10, §15, §18 completed — including if this session aborts.
2. `git diff --name-only 2729a63..HEAD` — every changed file in CHANGELOG or the ledger row.
3. `glass_detector.py --since 2729a63` under the uv interpreter (bare `python` is the Store stub,
   exits 49).
4. Ledger row (≤80 words) as a **separate follow-up commit** — never `--amend` (SYM-016).
5. Both clocks advanced together; §1 unedited; no artifact carries another session's number.

## §4 In-scope symptom rows (swept at open)

- **SYM-032** — `.gpu-lock` gates nothing. Directly in scope: today's work is GPU work.
- **SYM-022** — one lab process on the card, ever. Ollama and llama-server must never hold the
  card together; the harness unloads one and proves VRAM back to baseline before starting the other.
- **SYM-021** — never chain GPU work with `;`.
- **SYM-031** — a failed probe may not render as a negative observation. Already exercised twice
  this session (the empty `Get-CimInstance` result, the empty `tauri-plugin` grep); both were
  re-probed with a method proven to have run before being called an absence.
- **SYM-029** — repo markdown CRLF. `sessions/*.md` verified LF (`git check-attr` empty, `file`
  reports no CRLF); `CLAUDE_README.md` still sliced with `head`/`tail` only.

## §5 What the commission turned out to be about

`Observed`, `windows-converter/analyst.py:158` — the analyst's production Ollama body carries:

```python
"think": False,
```

qwen3:8b is a hybrid thinking model. Ollama is told, on every chunk, not to think. `--jinja` makes
llama.cpp apply the model's **embedded** chat template instead of a legacy built-in guess — and
that template's own default for thinking is the open question, not a settled fact.

So the hypothesis under test, stated before the measurement and falsifiable by it: **the 814 vs
1,380–2,048 gap is a thinking-block gap, not a template-parsing gap** — in which case `--jinja` is
necessary but *not sufficient*, and parity additionally requires thinking to be turned off on the
llama.cpp side (`chat_template_kwargs.enable_thinking = false`).

Eliminated cheaply before spending GPU time: **not** a model mismatch. The GGUF room-chat points
at resolves to exactly one Ollama manifest, `qwen3:8b` — same weights on both sides (`Verified`,
`grep -rl <blob-sha> ~/.ollama/models/manifests/` → one file).

The harness measures three arms on the same real chunks of the held Valentine, reusing the
analyst's own `fence()` and `_chunks()` rather than a copy: Ollama `think:false` (production
today), llama.cpp `--jinja` default, llama.cpp `--jinja` + `enable_thinking:false`. It records
completion tokens, stop reason, and the analyst's image fence per chunk.

**Sampling never promotes** (docs/21 rule 2): N chunks measured is `Inferred` about the book,
never `Observed` about all of it.

## §6 The measurement

`Observed`, twice, by `windows-converter/backend_parity.py`. Valentine (266 chunks), 5 sampled,
qwen3:8b on both sides, the analyst's own `fence()`, `_chunks()` and `readability` program.

| arm | tokens | max | not-stop | fence bad | vs ollama |
|---|---|---|---|---|---|
| `ollama` `think:false` (production) | 2,307 · 2,298 | 633 | 0 | 0 | — |
| `llama.cpp --jinja` | 14,893 · 14,948 | 4,526 · 4,281 | 0 | 0 | **+546 % · +550 %** |
| `llama.cpp --jinja` + `enable_thinking:false` | 2,300 · 2,291 | 633 | 0 | 0 | **−0.3 % · −0.3 %** |

Per chunk, run 1, nothink vs ollama: −5, −7, −2, ±0, +7 tokens — inside ±2.1 % everywhere.

**`--jinja` alone fails the gate, and fails it upward.** It makes llama.cpp apply qwen3's
*embedded* chat template, whose default is thinking **ON**; think blocks ran 3,311–21,380 chars.
`analyst.py:158` has always sent Ollama `"think": False`. The llama.cpp counterpart is
`chat_template_kwargs.enable_thinking=false`, and with it the two backends are the same size.
**S79 §18's instruction — "restart llama-server with `--jinja` and re-measure" — would have failed
on its own terms.** `--jinja` is necessary and not sufficient.

Eliminated before spending GPU time: **not a weights mismatch.** The gguf room-chat points at
resolves to exactly one Ollama manifest, `qwen3:8b` (`Verified`). Cross-check that the two
backends receive the same input: prompt tokens 611 vs 603 on chunk 44, ~1.3 % apart.

## §7 Implementation Delta

| Commit | What |
|---|---|
| `0d20d71` | open plan — §1 intent before work, `--since` pinned at `2729a63` |
| `2d29b2b` | **the card reported the first widget, not every widget** — `open.sh` counts all instances; selftest 12 → **15**; SYM-033 filed |
| *(this close)* | **`windows-converter/backend_parity.py`** — the harness S79 did not keep; CHANGELOG; this closeout |

Inherited into the pinned range, not this session's work: `690a93d`, S79's own ledger-row commit,
which lands after `2729a63` by the never-amend rule (SYM-016).

## §8 Decision Ledger — what Rab signed

Nothing new was put to him. The session ran inside S79 §18's standing commission and one
mid-session correction of fact from him ("Sorry I closed it again"), which retired the two-widget
question as an incident and left it as a defect to fix.

**Wants his signature and did NOT get it (so: not done):**

1. Pointing the analyst at llama.cpp. The token gate is met, but on **5 of 266 chunks** —
   `Inferred` about the book — and the throughput half is unmeasured. Not proposed, not built.
2. A single-instance guard for the widget (SYM-033's prevention half).
3. Turning thinking off in `room-chat`. §10.2 shows why that is not the obvious win it looks like.

## §10 Known Failures — mine, first-class

**10.1 I read the card instead of the machine.** The card said `widget 18856`; two widgets were
alive. The card was not lying — `open.sh` was, structurally, and I would not have looked if a
process listing had not been the next thing I ran for an unrelated reason. The guard S79 wrote
against a **failed** probe rendering as absence had no twin for a **successful** probe rendering
as the whole truth. Fixed, with three tripwires, one of which asserts the new row stays quiet on
a single widget — a guard that fires on every input is the tautology case 0 exists to catch.

**10.2 I inferred that `room-chat` was broken, and it is not.** From the analyst's 3,311–21,380
char think blocks and room-chat's `max_tokens: 700`, I inferred it would exhaust its budget inside
the reasoning block and hand the user an empty answer. `Observed`: 292/700 tokens, 948 reasoning
chars, `finish_reason: stop`, a correct cited answer. **The workloads are different shapes** — a
short question over curated context thinks briefly; a 4,000-char rewrite does not. Recorded
because the inference was stated before it was tested and the test is the only reason it did not
reach Rab as a finding. It also inverted the recommendation: thinking-on cited **per claim**
(`[analyst.py:45]` on the chunk-size claim), thinking-off pooled both citations at the end and
attributed loosely. For a citation engine that is an argument against disabling it.

**10.3 My own harness crashed on its success message.** The scratchpad version wrote its JSON,
then died `UnicodeEncodeError` printing `raw → path` under cp1252 — exit 1 on a run whose science
was complete. Rendered as a **failed** background task, which is the same reading-vs-reality gap
as 10.1, pointing the other way: this time the confident readout said failure. ASCII in the repo
version.

**10.4 An overstated line-ending claim.** I wrote in §4 that `sessions/*.md` are LF. `file` said
so, but `core.autocrlf=true` means git converts on checkout — the commit warned exactly that. The
byte-level check I should have run first is the one I ran later on CHANGELOG (1,603 lines, 1,603
CRLF, 0 lone LF).

## §15 Symptom Signatures

- **SYM-033** (filed this session) — the widget looks normal but there are two of it; no
  single-instance guard, no mutex, so SYM-022's precondition is reachable by a double-click.
  `OPEN` — the card SHOWS it, nothing PREVENTS it.
- **SYM-031's mirror**, folded into SYM-033's guard column rather than given its own row: a probe
  that succeeded rendering a partial observation as a whole one.

## §17 Current State

`Observed at close` — widget **0**, watcher **0** (Rab closed both mid-session; nothing was in
`drop`, so nothing was lost). Ollama serving, model released, VRAM back to baseline. llama-server
**not running**; llama.cpp still wired to **nothing** — the analyst was not pointed at it and must
not be. `held/` 4, `anchor/` 23, drop 0, pending 0. `audit-mode` = `enforce`, `analyst-mode` =
`local`. Vault 6 notes, tip `70c60e6`. Installed exe `C3D277F7`. muster tripwires **15/15**;
`glass_detector --since 2729a63` clean, 0 keys.

*(`Historical` the moment it is written. The intake watcher is DOWN — a drop will sit unclaimed
until a widget is launched. Launch exactly one.)*

## §18 Next Entry Point

1. `bash .claude/skills/muster/open.sh` — expect exit 0, S81, `--since` = this session's SHA. The
   widget row now reports every instance; if it says `N INSTANCES`, stop and close all but one.
2. **The throughput half, which is now the only thing blocking the migration.** The token gate is
   met with `enable_thinking:false`. The **+27 % tok/s and 1.72× batching have no harness anywhere
   in the repo** — prose in S79 §18 and nothing else — so they cannot be re-derived, only quoted,
   and quoting is what rule 3 forbids. Build the throughput arm into `backend_parity.py` (same
   one-process-at-a-time discipline, warm-start both sides, repeat per chunk) before any migration
   is proposed.
3. **Raise `-n`.** 5 of 266 chunks is `Inferred` about the book. Cheap to fix: the nothink arm ran
   ~5 s/chunk.
4. **The analyst has no llama.cpp call path.** It speaks Ollama `/api/generate` with a raw prompt;
   the parity was measured against `/v1/chat/completions`. Whatever is built must carry
   `enable_thinking:false` or it silently reverts to the 6.5× arm — and a truncated rewrite drops
   trailing `⟦IMG-n⟧` and ships the un-analyzed original.
5. **SYM-033's prevention half** — `tauri-plugin-single-instance`, unsigned.
6. Standing, unchanged from S79 §18: `room-chat` graduation (Job Object + the signed watcher gate
   + its tripwire), Rab's ten real questions, the omission-class bank entry, the wrong-page embed
   in Valentine, docs/26 slice 3.
7. **`windows-converter/` is not in CI's lint set** (SYM-018's shape — the analyst writes the
   vault and no lane checks it). `backend_parity.py` ships unlinted; ruff is not installed on this
   machine's interpreters.
