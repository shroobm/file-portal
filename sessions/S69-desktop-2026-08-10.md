# S69 — Desktop lane — 2026-08-10

**Closeout under `docs/21-session-closeout-contract.md`.** Tier: **EXTENDED** — source files were
touched (comments only, zero behaviour) and the manual, a shared reference, changed. Sections with
nothing to say are marked *omitted, and why*.

---

## §1 Session Intent

`Observed` — pre-declared in the open-plan commit `18ed9f5`, from Rab's words: *"Re read the text
utilized for the Manual. Stress test it, and check each aspect of it for it's applicability in
reality vs whats its just saying, then remake it with that intention in mind."* Left unedited.
The outcome matched: the manual was re-verified claim-by-claim and remade where reality disagreed.

## §2 Starting State

`Observed` — S68 had closed hours earlier (`9810497`, row `dfbe8bc`); clocks agreed; tree clean;
origin in sync. The manual stood at rev "S67, amended S68" claiming "nothing here is folklore" —
a claim that had never received a second, independent check (S67 compiled it from one read).

## §3 Development Chain

`Observed` — `18ed9f5` open plan · `5562cdf` the manual's corrections + provenance ·
`ceb0f95` the two stale code comments · then CHANGELOG + launch.json + this file in the close.

## §4 Analogue + Boundary

`Observed` reasoning — this is the contract's own `Verified` discipline applied to a document:
S67's read is check one; S69's grep-the-source-directly is the differently-shaped check two
(it cannot fail the same way, because it never consults the manual's citations). **Boundary:**
claims tagged `Historical` in the manual (measured VRAM peaks, survival scores, session
narratives) were NOT re-observed — they describe past events and are checkable only against the
ledger that already sources them. The sweep covered every *current-reality* claim.

## §5 Observable Contract

`Intended` — Rab, each under a minute, failure condition first:

1. Open the manual (F5 if it's already open) — **fails if** the cover doesn't read
   "rev. S69 — stress-tested" with a stress-tested line in the meta block.
2. `ls windows-widget/src-tauri/src/*.rs | wc -l` → 14, and CH·10 says 14 — **fails if** they
   disagree.
3. `grep -c "#\[tauri::command\]" windows-widget/src-tauri/src/main.rs` → 38, and CH·10's command
   reference says 38 — **fails if** they disagree.
4. `grep force_ocr linux-converter/config/converter.toml` — **fails if** any line still asserts
   the scan lane *is* `force_ocr=True` (the comment now names it as the trap it is).
5. CH·17 — **fails if** the Stress-tested bullet is absent or claims more than 3 corrections.

## §6 Deliberate Divergences

- **Corrections in place, not a recompile.** The S67 structure and prose survived; only claims
  reality disputed changed. A full rewrite would have discarded verified content (and the S68
  scrollspy) for no accuracy gain.
- **One cross-lane comment fix** (`converter.toml`, a Linux-owned file) done from the desktop
  rather than filed as a message — precedent: S67 edited desktop-owned docs for the same class of
  rot; the edit is a comment, inert to the running service, and removes SYM-012's banned wording
  from the exact surface a user edits. The ThinkPad tree was verified clean + in sync first.
- **No widget rebuild, no service restart** — nothing behavioural changed anywhere.
- **`.claude/launch.json` committed with the new `docs` entry** rather than left untracked — it
  was already a tracked file (see §10).

## §7 Implementation Delta

`Observed` — `git diff --stat dfbe8bc..HEAD`:

| Path | What changed |
|---|---|
| `docs/22-engineering-manual.html` | 3 counts corrected (14 modules · 38 commands · 705/9 dashboard); repeated-line rule's &gt;20-chars condition; CH·17 Stress-tested provenance bullet; honest-note + foot + eyebrow + meta rev'd to S69 |
| `windows-converter/watch_and_convert.py` | docstring + `MODE_FILE` comment now name `ask` (comments only) |
| `linux-converter/config/converter.toml` | OCR comment rewritten — names FORCE_DROP_OLD and calls out the old `force_ocr=True` wording as SYM-012's trap (comment only) |
| `.claude/launch.json` | +`docs` entry (serve `docs/` on 8321) beside the two existing entries |
| `CHANGELOG.md` / `CLAUDE_README.md` / `sessions/S69…` | the S69 entry · plan→close blurb · this file |

## §8 Decision Ledger

1. **Fix wrong counts to exact numbers, not rounder ones** (3,792 lines; 705/9; 38). Rejected:
   keeping "~" approximations — the commission was precisely about prose that sounds measured;
   an exact number is falsifiable, a tilde is not.
2. **The manual's wrong counts corrected silently in their chapters, with the story told once in
   CH·17.** Rejected: inline strikethrough annotations at each site — a reference is read for
   truth, not archaeology; provenance has a chapter.
3. **Repo comments fixed rather than reported** (see §6's lane note). Rejected: a coordination
   message for two one-line comments — process weight exceeding the change.
4. **`Historical` claims left un-re-observed** (§4's boundary). Rejected: re-deriving measured
   history (VRAM peaks, survival scores) — impossible without re-running the events; the ledger
   is their source and the manual already tags them.
5. **Verification moved from the pane's `file://` view to a real HTTP server** once the snapshot
   staleness surfaced (§10). Rejected: trusting the DOM probes against a possibly-stale snapshot
   — a check against a cached copy is SYM-001 wearing a browser costume.

## §9 Evidence

`Observed` — the sweep, by the numbers:

- **Line counts:** all 21 files cited by the manual measured by `wc -l`; every one exact
  (transfer.rs 168 · allocator 218/74/66/31 · conveyor 126/1193/28 · analyst 438 · audit 491 ·
  linux-converter 320/134/145/77/67 · exporter 524 · bench 678/876 · main.js 1025 · room.js 871 ·
  styles.css 728). Rust total 3,792 across **14** files (`ls | wc -l`).
- **Commands:** `#[tauri::command]` count 38 = `generate_handler![…]` entry count 38 — none
  dangling; the 38 names enumerated and matched CH·10's table.
- **Constants verified in source** (selection): POLL_S 5 · stability 1/120 · outer 21600 ·
  STALL_FROZEN_S 900 · `max(3600, pages×20)` · RECOGNITION_BATCH 32 · SLICE_PAGES 200 ·
  thresholds {clean 600, scan 400} · batch (8,16,32)/16 · clamp 80 → worst 160 · slug 60 ·
  CHUNK_TARGET 4000 / NUM_CTX 8192 · Gemini 13 s / temp 0.2 / window 18 · throughput 138/186.7 ·
  audit: 12-word/24-CJK windows · FUZZY 90 · PAGE_MIN 15 · REVERSE 200 · zlib 0.20 ∧ trigram 40 ·
  line-repeat 20 (len&gt;20) · clean 0.97/0.85/50 · scan 0.70/0.20 · analyst 0.995/25 ·
  allocator 0.5/60 · 2048 MB · status 200 events · receipts tail 60 · algedonic 30/1–720/7-day/12 ·
  bench ports 7077–7096 · raster 140 / crop 220 · undo `del _undo[:-20]` · shingles (10,25,45) ·
  +3 lines/repair · dashboard THUMB_SIZE 160 / debounce 300 / LRU (path,mtime) · config.rs keys =
  CH·13's list · event stages = exactly {intake, convert, gate, analyst, audit, ship} · receipt
  outcomes = exactly the manual's nine.
- **Corrections found:** modules 15→14 (two places, incl. CH·17's own "all 15") · commands 37→38 ·
  dashboard ~600/8 → 705/9 (widgets/ subpackage uncounted) · repeated-line len-condition.
- **Code-comment rot found (manual right, code wrong):** `watch_and_convert.py:6,31` (no `ask`;
  "until the pre-flight card exists" — it has existed since S18) · `converter.toml` "(force_ocr=
  True)". Both fixed (`ceb0f95`); `py_compile` OK.
- **Render proof over HTTP** (localhost:8321): eyebrow/meta/CH·10/CH·12/38-commands/foot/CH·7
  probes all true; scrollspy tracked s1→s17→s12; console clean.

## §10 Known Failures

- **The Browser pane pins `file://` pages to static snapshots** — force-navigate, query-strings,
  and `location.reload()` all served the pre-edit page, and scripts behaved inertly there. Cost:
  three wasted verification attempts before the HTTP server settled it. Recorded as tooling
  knowledge (the `docs` launch entry is the fix), not a SYM row — no pipeline surface involved.
- **A `Write` clobbered the tracked `.claude/launch.json`** (I believed it new; it carried
  `repair-bench` + `room-harness` entries). Caught by `git status` in the same session, restored
  from the diff, my entry merged additively (+6 lines only). Near-miss, zero loss — but the
  lesson stands: **check `git status` before writing any file you didn't create this session.**
- **Screenshot capture returned black frames** on the localhost tab (paint-layer quirk); the DOM
  probes carried the visual verification instead.

## §11 Unproven by Machine

`Intended` — §5's five checks on Rab's own screen; and whether the manual now *reads* as
trustworthy — the stress test proves accuracy, only a reader can judge confidence.

## §12 Uncertainty Register

`Unknown`:

- **CI on this push** — comments/docs only; both jobs expected green (rust re-runs warm on
  unchanged Rust). Resolved by the run on the close push; reported in-chat.
- **Is a claim-sweep repeatable enough to be a habit?** This one took a session. If docs/22 drifts
  again, the cheap re-check is §5's items 2–4 plus the constants block against `git log docs/22`.

## §13 Dependencies / Assumptions

`Inferred` — the sweep trusted `wc`/`grep`/`ls` and file reads over the real tree at `ceb0f95`;
it assumes no source moved during the session (tree was clean before and after, verified).

## §14 Regression Surface

`Inferred` — near zero: comments and documentation. The only executable-adjacent file is
`watch_and_convert.py` (docstring; `py_compile` proven). `converter.toml` values untouched —
only its comment. launch.json is tooling config outside every runtime path.

## §15 Symptom Signatures

No new rows. The two comment-rot finds are instances of documentation lying about settled
symptoms (SYM-012's wording at a user surface), fixed at source; the symptom index already
carries the classes.

## §16 Recovery / Return Log

*Omitted — no human-found defect; the commission was preventive.*

## §17 Current State

`Historical` as of the close commit: branch pushed; manual at rev "S69 — stress-tested"; widget
exe `F28C58A8` untouched; no service restarted anywhere; vault untouched (last verified
`70c60e6`, 6 notes); held/ = 4 unchanged; TIME-STATE advances to S69 with this close.

## §18 Next Entry Point

1. MUSTER; newest row should be **S69**, ancestor-verified; TIME-STATE agrees.
2. The manual is now second-checked. If code moves under it, the code wins — update the chapter
   AND the CH·17 provenance line, or the "stress-tested" claim starts lying.
3. Awaiting Rab, unchanged: **Decision 5 signature** · the four standing signatures · Valentine
   zones 2–4 · Cybernetics-stale · Textor. Build candidates: docs/19 §3 onward; events rotation +
   `verified_from` stamps; cross-day event-row date stamp.
