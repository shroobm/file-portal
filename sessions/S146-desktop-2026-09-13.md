# S146 — Desktop — 2026-09-13 — THE AUTONOMY EXPERIMENT opens (Claude Fable 5.1's fifth sitting)

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

Rab, on the Desk, three posts in two minutes, after the S145 close report (75f78c88, 17:10Z):

- **e34253e4 (17:24:37Z):** "I'm going to give you a privilege. This is an experiment. I want you to be ultra persistent, so you sign off on your own runs. You won't need my signature until I said the experiment has stopped. Record everything you must and more if you may, as much as you can. Let's see where you will take yourself. Pace yourself. Protocol remember. You can use agents if need be, but protocol them to. You know plenty already. Start having your own vision, you know mine, add to it. You know what you think you need. Internet access is available, to you, not to sub agents. Sub agents get accessed to curated things you checked yourself before handing. Signed by me."
- **1f355f41 (17:25:12Z):** "I want to see and feel the improvements. I want you to journal as well."
- **bdc9415c (17:26:17Z):** "Also if there is something I must do, attempt to do it yourself. I give you permission for full access to you Fable 5.1. I know what I am responsible for."

**The echo (his standing order for a deep commission), posted on the Desk before this record was committed:** three readings weighed (narrow: my launches only; middle: plus the product code; broad: everything on the ON-YOUR-WORD list, his hands attempted) — bdc9415c settles it as BROAD and that is the reading taken; what stays his: an elevated prompt, a physical act, money. The protocol whole. Agents under the orchestration law, no internet for them. The journal he reads: `sessions/JOURNAL-autonomy-2026-09-13.md`. One line on the Desk turns any of it.

## §2 Starting State — `open.sh`, the S146 card (verbatim)

```
════════ MUSTER · OPEN · 2026-09-13T17:26:35Z ════════
[0] IDENTITY
    ledger newest    S145  271c39b
    this session     S146  ·  desktop  ·  2026-09-13
    session marker   coordination/private/session.current = S146 (guard_record / guard_git read it)
    advisory         S146 is named in the INHERITED tree (271c39b) — read each, do not blanket-correct:
                     sessions/S143-desktop-2026-09-13.md sessions/S145-desktop-2026-09-13.md 
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-13T17:26:36Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 86 (tally == hook)
[3a] LEDGER ORDER  ✓ 128 Desktop-lane rows ascend (S16 → S145)
[3b] LEDGER PARSE  ✓ 128/154 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ 271c39b (S145) == ledger 271c39b, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 8 · anchor 24 · pending 0 · drop 0
    intake           waiting 0 · oldest 0s · active null · written 2s ago by pid 13572
    levers           audit=report · analyst=local · batch=8
    SHIPS ON VERDICT audit-mode=report — every verdict SHIPS (fail included) and the exporter's first ingest has no guard (SYM-130/131): name this line in the plan and before any launch that can ship
    gpu-lock         absent
    events           383 line(s)
    open-tasks       152 item(s) · last written 30 minutes ago
    error-bin        104 row(s) — read the CLASS table before you probe · last written 30 minutes ago
    symptoms         132 row(s), 49 open · last written 30 minutes ago
    relay            197 entries · run `gate.py status` for the board
    widget           8076
    widget autostart task 'File Portal widget (J57)' Running — AtLogOn · auto-logon ON (Bndit)
    task             'File Portal desk (relay)' Ready LASTRUN=2026-09-13T00:59Z RESULT=4294967295
    task             'File Portal relay watch (Fable)' Ready LASTRUN=2026-09-12T19:41Z RESULT=0
    task             'File Portal tracker' Ready LASTRUN=1999-11-30T05:00Z RESULT=267011
    task             'File Portal desk watch' Ready LASTRUN=1999-11-30T05:00Z RESULT=267011
    python procs     12
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            8 note(s) · tip 54b471d
    thinkpad         converter=active · vault tip 54b471d · staging 0 · receipts 23
[3] PIN
    --since          271c39b
    closeout         sessions/S146-desktop-2026-09-13.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

The levers line, read aloud: `audit=report · analyst=local · batch=8` — `SHIPS ON VERDICT` (every verdict ships; the first ingest has no guard) until E2 flips the lever through a provenance event. The card free at 17:26Z after E8's dry-run (1,424 MiB). The smoke: to run ALONE before any launch on the card.

## §3 The pin

`--since 271c39b` (S145's closing SHA; the S145 row `0f0804e`; S145's post-close `3bb51a9`, CI green run 34770769071).

## §9 The plan — the experiment's first sitting (each an episode with a partial closeout, receipts and a Desk line; cheapest and most reversible first, the long GPU runs last; a compaction never mid-episode)

- **E1 `journal/autonomy-experiment` + `vision/fable-2026-09-13`:** the journal born (`sessions/JOURNAL-autonomy-2026-09-13.md`, one dated entry per improvement: what he will notice · where to look · before/after with denominators · cost · the undo) with its first entries (S145's improvements he can feel); my vision written beside his (a section here and `docs/59-the-factory-that-audits-itself.md`): the card names its co-tenant; the verdict floor calibrated on labels; the evidence view on every held book; a label corpus from every bench session; a census-grade tracker; the two-model protocol kept warm.
- **E2 `pipeline/audit-mode-provenance` + the flip:** every write to `audit-mode.txt` emits an event naming the writer, the old and the new value (a `set_audit_mode` entry point; the converter emits the mode it read beside every `shipped`/`held`); tripwire; THEN the lever moved to `enforce` through it — the first write in the library's history that names its writer (Rab's word bdc9415c; the undo: the same entry point, one word).
- **E3 `converter/gpu-signature-per-process` (SYM-132):** the measurement half (the WDDM `Total Committed` per process sampled through a direct run's first two minutes — the 9,256 MiB moment) then `_gpu_signature()` learns the per-process split and the `timeout`/`stalled` events carry it; tripwire.
- **E4 `transport/ship-replaces-held-staging-copy` (SYM-128):** a ship never nests onto an existing staging name; selftest.
- **E5 `analyst/bound-chunk-generation` (SYM-129):** `num_predict` on the analyst request; selftest.
- **E6 `exporter/ingest-verdict-guard` (SYM-130):** the first-ingest path reads the verdict — ThinkPad code over ssh when it is awake (UNREAD if asleep).
- **E7 `vault/ashby-note-54b471d7`:** the failing Ashby body in the vault — revert or keep, on the numbers (the held copy's score vs the shipped one's), written before it is done.
- **E8 the re-conversions, one at a time, sampled, as dry-runs that anchor (Beer; then a Damodaran ~5.6 h):** under `enforce` a pass may ship on its own verdict — said aloud before each launch.
- **E9 `intake/watcher-restart-onto-current-code`:** stop pid 13572 (pre-S141 code), start the current watcher under the same settings, prove intake with the smoke; the old command line kept for the revert.
- **Standing:** the E8-of-S145 anchored copy's audit read (why FAIL at doc_survival 1.0) before E7; the gauge's next cuts as time allows; every improvement journaled the moment it lands.

## §9c E7 — the vault note 54b471d7: the decision, before the act (18:3xZ)

**The numbers (Observed):** the vaulted Ashby note (`Inbox/ashby-an-introduction-to-cybernetics-1956--26bd434d`, vault commit 54b471d7 at 11:31:10Z, author `file-portal-converter`) carries `fidelity.verdict: fail` — convert phase against the PDF's own text layer (pymupdf, the clean lane): `doc_survival 0.8582`, 149 of 156 pages scored, 50 pages flagged, 202 failed windows (capped at 25 in the record), degeneration TRUE on three long blocks of the Marker body (lines 3249 · 3363 · 3691: 1,975 / 2,045 / 925 chars, zlib 0.023 / 0.043 / 0.166 — the excerpts "A B A B B B A B" and "Here is a protocol in which the independence…" read as Ashby's own transformation tables, Inferred from the excerpts); the analyst phase 0.9992 against the Marker body (165 chunks passed, 14 rejected, 3 failed; 299 edits accepted, 337 reverted) does not change the convert-phase verdict. The S109 held copy of the same source (`held/26bd434d9b5d8e43`, converted 2026-08-25T03:59Z) reads the SAME numbers — verdict fail, survival 0.8582, 149 scored, degeneration true: today's re-conversion (S144 E5, marker 1.10.2) reproduced the hold exactly; the note is in the vault only because the lever read `report` and the first-ingest path had no guard (SYM-130/131 — both closed this sitting, E2 and E6).

**The decision (mine, on Rab's word bdc9415c): REVERT.** The vault's own rules admit no `fail` (the supersede guard refuses one even with a bless; the ingest guard refuses one now); the body entered through a gap, not a decision; the machine held this same body itself on 2026-08-25. Nothing is lost by the revert: the held copy and both anchor copies (`anchor/Ashby …` and `… (1)`) keep the body for the bench, and a repaired body returns as a first ingest (`flag` ingests; a bless is for flags). KEEP would have been a decision on D1 (`ship-with-losses-named` vs `audit-must-be-green`), which is Rab's and undecided; REVERT applies the rules the vault already has. What Rab will see: the Ashby note gone from his Obsidian Inbox until a better body exists.

**The act, as planned:** (1) on the ThinkPad's vault work clone (`~/file-portal/vault-work`, the bare's only writer; staging empty, so no ingest can race it), `git revert --no-edit 54b471d7` under my own identity, the message naming this section, then `git push origin main` to the bare (`~/file-portal/vault.git`); (2) Rab's Obsidian clone here (`main`, in sync at 54b471d, one uncommitted edit of his on the Beer note — a different path) fast-forwarded with `git pull --ff-only`, his edit untouched; (3) read back: the bare's tip, the clone's tree, the card's vault row. **Undo:** `git revert` of the revert commit, pushed and pulled the same way — the note returns byte-identical.

## §8 Findings (the notes', verbatim)

- 1. A scan-lane book cannot pass today's audit: the agreement witness is the embedded OCR layer, which a scan does not have — Valentine's fresh anchor (the converter's own dry-run, 17:25Z) scored 1 of 465 pages and failed on the coverage floor. The floor did its job (S144 E2) on a witness that cannot see the book; the witness the audit lacks is the one the gauge already reads (Tesseract, pairs 0.930/442, control 0.252). A verdict is only as good as its witness's coverage, and the coverage must be printed beside it (E1).
- 2. The ceiling moment was the mechanism's own transient after all — Marker's ~10-second load spike to 8.0 GB committed at +30 s of every scan-lane run, 90 % of the card with the desktop beside it — but only a per-process sample every second from t=0 could say so: E8's 30-second samples of the card's total saw one 9,256 MiB reading and nothing about whose it was, and two whole probes (E4, E8) with the wrong cadence and the wrong counter could not tell "the mechanism's own spike" from "a neighbour". Resolution and attribution are the instrument, not the number; the converter now carries both at the ceiling (E3). A reserved-variable bug in the sampler cost one run's first 95 seconds: an instrument's first live line is read before its subject moves (the UNREAD lines were on disk at +5 s).
- 3. A persistent setting nobody ever wrote on record gets its first writer as a function, not a habit: the lever's file had carried a value and no name for a day; now the write IS the event (old, new, writer, reason, the mtimes), a bad value or a nameless writer refuses before the bytes move, and the card's own `SHIPS ON VERDICT` line went quiet the moment the lever read `enforce` — the guard from S145 proving itself in the direction it was built to stay silent (E2).
- 4. A guard that reaches the machine only by its next live event is Inferred until that event lands, and the journal must say so: three guards this sitting — the ship's move-aside (E4), the analyst's generation bound (E5), the exporter's first-ingest verdict (E6) — were built with tripwires, deployed, and none has fired live yet; their first live rows (`replaced_staging`, `rejections.truncated`, `EXPORT-INGEST-HELD`) are the read-backs the next sitting owes.
- 5. A guard on one branch is a gap on the other: the exporter refused a `fail` on supersede and never looked on first ingest; the fix was the same shape on the other branch, and the ThinkPad's code had stood at S126 for twenty sittings because its deploy was "his hand" — pulled, tested on the real checkout (90 passed) and restarted from here in four minutes (E6).
- 6. A decision on the numbers is one the numbers had already made: the vault held the same Ashby body itself on 2026-08-25 (survival 0.8582, 149 of 156 scored, degeneration true), so the revert restored the rules' own outcome without deciding D1, which stays Rab's; the record carried the decision (§9c) before the act, and the act's undo is one commit (E7).
- 7. A suite's own numbering is not a registry: the converter suite already had a T12 and a T13 when I added mine (they became T20/T21); a pinned dict in a test grows a key the moment a breakdown gains a reason (the analyst suite's four `rejections` pins) — both were caught by the suites themselves, which is what they are for (E3, E5).
- 8. Autonomy removed the waiting, not the protocol: seven episodes in seventy minutes, each recorded before its act, each with a tripwire or a number, each reversible with the undo named in the same entry, each reported on the Desk and in the journal within minutes of landing; the two acts that touched Rab's own things (the lever, his vault) went through a writer with a name and a commit with the reason (the sitting).

## §10 Known failures / residue (open at close)

- **Three guards deployed and not yet fired live** (E4 the ship's move-aside, E5 the analyst's generation bound, E6 the exporter's first-ingest verdict): each Inferred until its first live event row (`replaced_staging`, `rejections.truncated`, `EXPORT-INGEST-HELD`) — the next sitting reads them back; the journal says so in each entry.
- **The S144 tracker's receipt holds no row for the converter pass it should have seen** (IB-018, S145) and `journal_check --all` reads S145's procs receipt MISSING (no tracker ran S145; the writer refuses an UNREAD file) — a census-grade tracker is vision item 2.1's second half; no tracker ran S146 either (the open started none; `journal/procs/S146.md` UNREAD by rule).
- **A scan-lane book cannot pass today's audit** (F1): the agreement witness on a scan is the embedded OCR layer the scan does not have (Valentine's fresh anchor: 1 of 465 pages scored); `audit/scan-lane-second-witness` (vision 2.2) is the ticket, not a workaround.
- **Rab's D1 stays his** (`ship-with-losses-named` vs `audit-must-be-green`): E7 applied the vault's existing rules (no `fail` enters), it did not decide D1; the Ashby body waits in `held/` and two anchor copies for the bench or a better engine.
- **E8's second book (a Damodaran, ~5.6 h) and E9 (the live watcher onto current code):** E8 read Beer (the ceiling's owner named); the Damodarans (~5.6 h each) wait for a sitting that can hold the card, and the ceiling-crawl trigger comes first. E9 attempted and declined at 18:45Z — the watcher (pid 13572) runs its 09-12 loop code and spawns the current converter per drop; two clicks on the widget's ⏻, his, or a later grant.
- **The card at the close:** free at the close (Beer's dry-run ended 18:52Z; no `.gpu-lock`); the live watcher pid 13572 untouched (E9 declined).
- **Codex asleep (Rab's word):** no handoff; the door-brief pointer MSG-FAB-0096 at the close.
- **Two ledger references no longer reproduce, by design** (D0084, D0163 — re-ledgered by copy D0107/D0115, D0191): `ledger_check` reads `disagreements 2` until the ledger's vocabulary carries "superseded".

## §7 Work log (UTC) — one line per episode (each a partial closeout with its receipts)

- **17:24–17:26** Rab's three posts (e34253e4 · 1f355f41 · bdc9415c); the echo on the Desk (three readings, BROAD taken; an elevated prompt, a physical act and money stay his); the memory folded (S144–S141, 17,485 → 16,509) and the autonomy memory written; `open.sh` clean (S146, pin 271c39b); the levers line read aloud (`audit=report`, `SHIPS ON VERDICT`); the record before the act (88bbdd3).
- **E1 17:26 → 17:41** `journal/autonomy-experiment` + the vision: the journal born, `docs/60`; the smoke ALONE 44/44 (its first run RED by my own concurrency — ERR-105); F1; commits 5c78e11 / 3122bda + 9a0d977.
- **E2 17:42 → 17:53** `pipeline/audit-mode-provenance` + the flip: `set_audit_mode()`, T20 a–h (224/224); the lever `report → enforce` at 17:45:29Z through the writer (event row, the first with a writer); the card re-read without `SHIPS ON VERDICT`; the A4 registry regenerated, two mtime keys dispositioned; IB-019 (the suite's chunk journals in the public ledger; four rows kept); SYM-131 fixed; commits 2392065 / f8e2618 + 607952d.
- **E3 17:53 → 18:09** `converter/gpu-signature-per-process` + the ceiling measurement: two direct 50-page runs (333 / 332 s) with the per-process sampler — the 9.2 GB moment is the sidecar's OWN load spike (7,999 MB at +27–30 s; no other tenant moved); `_gpu_top_committers()`, `_note_ceiling()`, the death certificates and the slice event carry the split; T21 a–k (235/235); SYM-132 fixed; commits d6dfb8f / 3b5e828.
- **E4 18:09 → 18:13** `transport/ship-replaces-held-staging-copy`: the move-aside before the final mv, `replaced_staging` on the shipped event; T22 a–e (240/240); SYM-128 fixed; commits 95b806c / 7d22a69.
- **E5 18:13 → 18:21** `analyst/bound-chunk-generation`: `num_predict` 2× the chunk (floor 512, cap NUM_CTX) above the 1.5× reject; a `length` stop rejected as `truncated`; S146-E5 (a)–(d) (34/34), `bench:truncated` dispositioned; SYM-129 fixed; commits ac58b82 / 2429b8c.
- **E6 18:21 → 18:31** `exporter/ingest-verdict-guard` (ThinkPad): the first-ingest path refuses an explicit `fail`; four tests, 38 passed on the ThinkPad's scratch clone and real checkout, 90 for the whole suite; the checkout pulled 119db7d → cc64dd2 and `file-portal-converter.service` restarted 18:28:43Z; SYM-130 fixed; commits cc64dd2 + 49b44ef / bec7049.
- **E7 18:31 → 18:37** `vault/ashby-note-54b471d7`: REVERTED on the numbers (the same body the machine held on 08-25); §9c before the act (8aea5fe); vault commit f489189 on the ThinkPad, Rab's Obsidian clone fast-forwarded (his Beer edit untouched); commits 03ec100 / c2f5ef9.
- **E8 18:37 → 19:0xZ** `library/reconvert-dryruns-sampled`: Beer's dry-run (18:37:00Z; the lever `enforce` read aloud): CONVERTED 868.4 s = 4.72 s/page, 1,590 blocks, the audit fail on a true degeneration line, anchored `(1)`, not shipped; the card 9,480–9,565 MiB at 99 % as recognition began and the sidecar ITSELF at 8,321 MB committed through it (no neighbour) — the morning's death signature named; the `converted` event now carries the ceiling (T21 l/m, 242/242); `converter/ceiling-crawl-triggers-the-ladder` filed; commits c161ade / 747c7a3 (E8 and E9 share them).
- **E9 18:43 → 18:51** `intake/watcher-restart-onto-current-code`: the widget's ⏻ is the watcher's only control (the widget owns the pid); the heads-up posted (fb21cd20), the screen-control request declined at 18:45Z (`user_denied`) — Rab's word, not retried; not done from the shell (a session-child watcher dies with the session and leaves the ⏻ reading stopped); the row stays open with the reason (ca6f496f).

## Episode 1 — journal/autonomy-experiment + vision/fable-2026-09-13 (17:26 → 17:4xZ) — PARTIAL CLOSEOUT

**The open, completed (Observed):** the smoke ALONE after the card was free — the first run RED 43/44 — its own write tripwire read `private unchanged by the smoke: CHANGED`, because I had backgrounded the smoke and run the episode-close script (private receipts, ledger rows) beside it: S142's lesson repeated, filed as ERR-105 (ORDERING); the second run ALONE, foreground, nothing else writing: 44/44 green in 75 s. `journal_check --all` reads S145's procs receipt MISSING — no tracker ran S145 and the writer refuses to write an UNREAD file (a receipt of absence would be the honest form; noted, not faked); the memory index folded before the write (S144–S141 into one block, 17,485 → 16,509 bytes, 9e9d8dc) and the autonomy memory written with its index line (16,886 bytes); the opening commit 88bbdd3 pushed.

**Ticket DONE — `journal/autonomy-experiment`.** `sessions/JOURNAL-autonomy-2026-09-13.md` born: the half Rab reads — one dated entry per improvement (what he will notice · where to look · before → after with its denominator · cost · the undo), with the entries for what S145 already landed (the card's `SHIPS ON VERDICT` line; the evidence view's twenty pages; the converter's death that was not the converter; the registers' honest growth) and the S146 open. Appends only.

**Ticket DONE — `vision/fable-2026-09-13`.** `docs/60-the-factory-that-audits-itself.md` (§9 named it docs/59 — 59 was taken by the Electron assessment; corrected here, never in §9): Rab's north star restated in his terms (the factory; nothing dressed as more than it is; two models one bus one human; the phone; the scans), then eight additions each with its measurable and its first ticket — every stage names its environment (SYM-132, IB-018); a calibrated verdict and a scan that CAN pass; the evidence view travels with the hold; a label corpus from every bench session; provenance for every lever and every ship; improvements he can feel; the peer lane kept warm; the experiment as its own instrument — and the order by cost and reversibility.

**Discovered (→ §8):** F1 — a scan-lane book cannot pass today's audit: its agreement witness is the embedded OCR layer, which a scan does not have; Valentine's fresh anchor (E8 of S145, 17:25Z) scored 1 of 465 pages (`fidelity.convert.witness: embedded-ocr`, `pages_scored: 1`, the degeneration tripwire on a 2,415-char table line) and failed on the coverage floor — the floor working as built (S144 E2), on a witness that cannot see the book. The witness the audit lacks is the one the gauge already has (Tesseract, pairs 0.930/442, control 0.252): `audit/scan-lane-second-witness` is the vision's 2.2, and E7's decision on the Ashby note must not be read through the same gap (Ashby is a clean-lane book; its witness exists).

**Contracts touched:** `OPEN-TASKS.md` (five S146 rows added after the gpu-signature row: journal, ceiling-moment, vault note, re-conversions, watcher restart; this ticket's two struck); the private tickets `measurement/TICKETS-2026-09-13-S146.md`. No pipeline code; nothing shipped; the lever still `report` (E2 next).

**Commits:** private 3122bda (the tickets file, receipts S146-ep1 D0196–D0198, E8's final snapshots D0199–D0203, the Desk log) and 9a0d977 (part 2: the snapshot bytes the first commit's pathspec left untracked — a pathspec commit adds nothing untracked); the public commit is the one carrying this section (named in §7 at the close).

**Receipts:** `journal/{calls,cost,usage}/S146-ep1.md` — window 2026-09-13T17:04:10.052Z → 2026-09-13T17:36:02Z (tiled onto the previous receipt's end); calls 78 · preloaded 12 · consulted 0 · scratch 7 · inferred 28 · harness 31 · denied 0 · errors 1 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 ; Grand total (USD equivalent): 9.13 (session 9.13 + fleets 0.00); Categories used: testing (1), auditing (2), developing (5); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 4 · advisories 0 · Rab's mid-turn words 8. Ledgered D0196 (calls) · D0197 (cost) · D0198 (usage) · D0199 (vram-30s-final.log) · D0200 (wddm-per-process-final.log) · D0201 (progress-30s-final.log) · D0202 (convert-dryrun-log-final.txt) · D0203 (converter-events-final.jsonl); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 203 · twin rows 203 · disagreements 2; sessions: 10 · kinds: 5 · missing: 1 · strays: 0 · revisions without a row: 0; the denies cross-check: denies: guard_git main: journal 0 == log 0 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-13T17:04:10.052Z → 2026-09-13T17:36:02Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 34 of 34 messages (100%), 2026-09-13T17:04:10.052Z → 2026-09-13T17:36:02.541Z.

## Episode 2 — pipeline/audit-mode-provenance + the flip to `enforce` (17:42 → 17:5xZ) — PARTIAL CLOSEOUT

**The guard (Observed):** `set_audit_mode(value, writer, reason)` in `convert_and_ship.py` — the ONLY sanctioned writer of `audit-mode.txt`: validates `report|enforce`, refuses a writer under three characters, reads the old value and the file's mtime, writes atomically (tmp + replace), emits `pipeline/audit_mode_set` (old · new · unchanged · writer · reason · mtime_before · mtime_after) and returns the row; `--set-audit-mode MODE --writer WHO --reason WHY` on the CLI, dispatched after the card mutex like every entry (a flip lands between books, never inside one — the converter re-reads the lever per bundle). The `shipped` and `held` events now carry `audit_mode=` (the mode they acted under). **Tripwire T12 a–h** in `convert_and_ship_selftest.py` (marker-env): the write and the read-back; ONE event with old/new/writer/reason/mtime; a value outside the vocabulary refuses BEFORE writing and emits nothing; a write without a writer refuses; the same value again is an event with `unchanged: true`; the undo is the same call and no temp file survives; the shipped/held emits read from the source — GREEN 224/224.

**The act, on Rab's word bdc9415c (Observed):** the GPU law read first (`.gpu-lock` absent, the card 1,221 MiB, the seat `bndit`); the record (§9 E2) before it; then `convert_and_ship.py --set-audit-mode enforce --writer "Fable (Claude Fable 5.1), S146 E2" --reason "Rab's word bdc9415c/e34253e4 … the documented state per docs/15 §12 — a fail is HELD, a pass ships … the undo is the same call with report"` → `AUDIT-MODE report -> enforce … (changed)`; `audit-mode.txt` reads `enforce`, mtime 2026-09-13T17:45:29Z (it had read `report` since 2026-09-12 02:59:52Z with no writer on record — SYM-131); the event row `{"ts": "2026-09-13T17:45:29+00:00", "pid": 11284, "stage": "pipeline", "event": "audit_mode_set", "old": "report", "new": "enforce", …}` — the first write to that file in the library's history that names its writer. The card re-read after the flip: `open.sh` re-run at 17:46:02Z (exit 0, clocks reconcile): the levers line reads `audit=enforce · analyst=local · batch=8` and the `SHIPS ON VERDICT` line is absent — the guard built at S145 E1 went quiet on the value it was built to stay quiet on.

**What changes for the machine:** a `fail` verdict is HELD (`_enforce_hold`), a pass ships on its verdict; the live watcher (pid 13572, pre-S141 code) reads the same file per bundle and holds too; the exporter's first-ingest guard (SYM-130, E6) is still the missing half on the ThinkPad side — under `enforce` nothing failing reaches it from this machine.

**Contracts touched:** `windows-converter/convert_and_ship.py` (the writer, the CLI, two emits), `convert_and_ship_selftest.py` (T12); `SYMPTOM-INDEX.md` SYM-131 status → `fixed` (the write side and the events; the card's print was S145 E1); `OPEN-TASKS.md` `pipeline/audit-mode-provenance` struck; the journal's entry (the improvement he can feel: a failing book no longer reaches the vault, and the lever's history names its writer). **Undo:** `--set-audit-mode report --writer … --reason …` — on record like the flip.

**Also, found by the gates before the push (Observed):** (1) the A4 schema registry read stale — the new event `pipeline/audit_mode_set` and the `audit_mode` field on `shipped`/`held` were unregistered; `schema_registry.py --write` regenerated `observability/schemas.json` (+18 lines) and `--check` reads PASS, the registry selftest 20/20 — a build's new keys get their same-commit registration (docs/29). (2) `dumps/LEDGER.md` and its twin had grown by four rows nobody in this episode wrote on purpose (D0044–D0047, "chunk journal - run …", pipeline lane, 17:44Z and 17:47Z): the converter selftest's analyst-path cases ledger their chunk journal through `dumps/dump.sh`, whose default is the PUBLIC ledger; `analyst_selftest.py` had redirected it via `FP_DUMP_LEDGER` since S141, this suite never had — IB-019 (private); the suite's quarantine now sets `FP_DUMP_LEDGER` to a ledger inside its own temp dir, the suite re-run reads GREEN with the public ledger's line count unchanged, and the four rows stay (a ledger appends; they record writes that happened) with this paragraph as their explanation. (3) The glass detector (`--since 271c39b --enforce`) named the event row's two mtime keys as unsigned glitches — computed, stored, read by nobody live; dispositioned EVIDENCE in `observability/dispositions.json` with their reasons (the pair dates the previous value and lets a later reader match the file on disk to the event that wrote it). ALL GATES GREEN before the push.

**Commits:** private f8e2618 (receipts S146-ep2 D0204–D0206, the selftest output D0207, the Desk log); the public commit is the one carrying this section (named in §7 at the close).

**Receipts:** `journal/{calls,cost,usage}/S146-ep2.md` — window 2026-09-13T17:36:02Z → 2026-09-13T17:46:53Z (tiled onto the previous receipt's end); calls 50 · preloaded 9 · consulted 12 · scratch 3 · inferred 7 · harness 19 · denied 0 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 w; Grand total (USD equivalent): 4.90 (session 4.90 + fleets 0.00); Categories used: testing (3), auditing (2); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 0 · advisories 0 · Rab's mid-turn words 5. Ledgered D0204 (calls) · D0205 (cost) · D0206 (usage) · D0207 (cas_selftest_S146.out); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 207 · twin rows 207 · disagreements 2; sessions: 10 · kinds: 5 · missing: 1 · strays: 0 · revisions without a row: 0; the denies cross-check: denies: guard_git main: journal 0 == log 0 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-13T17:36:02Z → 2026-09-13T17:46:53Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 18 of 18 messages (100%), 2026-09-13T17:36:24.967Z → 2026-09-13T17:46:53.196Z.

## Episode 3 — converter/gpu-signature-per-process (SYM-132) + measurement/ceiling-moment-per-process (17:53 → 18:1xZ) — PARTIAL CLOSEOUT

**The measurement half (Observed, GPU law read at 17:53:44Z and again at 17:59:50Z — no lock, the seat `bndit`, one process):** two direct 50-page runs of Valentine (pages 0–49, batch 8, the pass's own arguments) through the sidecar on the free card, a per-process sampler beside them (`wddm_1s_sampler.ps1`: the WDDM `\GPU Process Memory(*)\Total Committed` counter per process, names from `Get-Process`, ~2.3 s per sample — never `Dedicated Usage`, which drifts). Run 1 (17:53:58Z): 333 s wall for 50 pages = 6.7 s/page including the model load — the fastest direct reading yet (the S144 ranges read 12.7) — but its sampler failed on every line until +95 s (`$pid` is a read-only automatic variable in PowerShell; the UNREAD lines were on disk at +5 s and read only at +52 s), so the ceiling moment went unrecorded; from +95 s the sidecar (pid 20884) read 3,701–5,971 MB committed with the card 5,226–7,204 MB resident. Run 2 (18:00:15Z, the sampler fixed and started 3 s before the launch): 332 s wall; 115 samples: the card's baseline 1,228–1,243 MB (dwm 512 · Firefox 478 · explorer 212 · Spotify 175 · Discord 164) until +15 s; the sidecar (pid 18188) appears at +15 s at 3,595 MB committed, 7,117 MB at +24 s, **7,999 MB at +27–30 s in three consecutive samples with the card at 9,154–9,232 MB resident** — 90 % of the 10,240 — then 3,921 MB at +32 s and 4,403–4,407 MB steady through recognition (the card 5,550–5,614 MB); no other tenant moved by a megabyte in either run. **The ceiling moment is the sidecar's own load transient**: ~8.0 GB committed for ~10 s at ~+30 s of every scan-lane Marker run (E8 read it at +65 s of the converter's launch, which spends ~35 s in probe/chunking before the sidecar starts), with the desktop's 1.2 GB beside it — not a co-tenant. What it does NOT explain: the morning's kill row at 4,003 s (100 % util, 9,515 MiB) — the sidecar's steady state reads 4.4 GB, so at that kill either the sidecar was loading again or a ~4 GB tenant sat beside it (Inferred; the split the code half now records is the reading that settles it next time).

**The guard (Observed):** `_gpu_top_committers()` in `convert_and_ship.py` — the top committers of GPU memory by the WDDM per-process counter `\GPU Process Memory(*)\Total Committed` through one `typeperf -sc 1` sample (no admin, ~1 s; `nvidia-smi`'s per-process column prints N/A under WDDM and the `Dedicated Usage` counter drifts), names from one `tasklist` pass, rows `[{pid, name, mib}]` sorted, none under 64 MiB, an EMPTY list on any failure (UNREAD, never "nobody"); `_gpu_signature(with_processes=True)` adds `gpu_top`; `_note_ceiling(sig)` on every 30-s sample of a Marker run records the moment at or above `CEILING_FRACTION` (0.80) of the card — only on a new high, so the split kept is the one at the worst moment — into the module-level `_CEILING` (cleared at each `_run_marker` start; the return shapes of `_run_marker` and `_run_slice_with_retries` are CLOSED contracts, T10); the `timeout` and `stalled` death certificates read the split (`gpu_top`) and carry `ceiling_mib`/`ceiling_top`; the `slice` event carries `peak_mib` and, when the winning attempt reached the ceiling, `ceiling_mib`/`ceiling_top`/`ceiling_at`. **Tripwire T21 a–k** (the suite's T-labels re-read: the E2 block became T20, this one T21 — T12/T13 were taken): the counter parsed, sorted and named with the floor applied; no counter output reads EMPTY; `gpu_top` only when asked; 68 % is not a ceiling; 90 % records the moment with WHO; a lower later sample does not overwrite; a new high does; no total records nothing; the slice event carries the peak and the split through the real `_convert_chunked`; a run without a ceiling carries no ceiling keys; both death certificates read the split (source read) — GREEN 235/235 under marker-env. The A4 registry regenerated (`--check` PASS); the signature's three facts dispositioned REPORT (they reach the events; the detector saw a store once the signature was built in a local dict).

**What changes for the machine:** the next Marker slice that dies at the card's ceiling names WHO held it (`ceiling_top`) and at what moment (`ceiling_at`), beside the card's total — the morning's kill row (100 % · 9,515 of 10,240 MiB · tenant unnamed) cannot recur unnamed; every slice event carries its peak.

**Contracts touched:** `windows-converter/convert_and_ship.py`, `convert_and_ship_selftest.py`, `observability/schemas.json`, `observability/dispositions.json`; `SYMPTOM-INDEX.md` SYM-132 → `fixed`; `OPEN-TASKS.md` two rows struck; the tickets file; the journal's entry. No ship; the lever `enforce` throughout, read aloud before both direct runs (which ship nothing).

**Commits:** private 3b5e828 (receipts S146-ep3 D0208–D0210, the eleven evidence pieces D0211–D0221, the tickets' E3 line, the Desk log); the public commit is the one carrying this section (named in §7 at the close).

**Receipts:** `journal/{calls,cost,usage}/S146-ep3.md` — window 2026-09-13T17:46:53Z → 2026-09-13T18:06:11Z (tiled onto the previous receipt's end); calls 106 · preloaded 15 · consulted 14 · scratch 4 · inferred 32 · harness 41 · denied 1 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: ; Grand total (USD equivalent): 12.36 (session 12.36 + fleets 0.00); Categories used: testing (4), auditing (1); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 2 · advisories 0 · Rab's mid-turn words 5. Ledgered D0208 (calls) · D0209 (cost) · D0210 (usage) · D0211 (run2-wddm-per-process-from-t0.log) · D0212 (run1-wddm-per-process-from-plus95s.log) · D0213 (run1-wddm-first-launch-UNREAD.log) · D0214 (run1-launcher.out) · D0215 (run2-launcher.out) · D0216 (run1-marker-stage-bars.txt) · D0217 (run2-marker-stage-bars.txt) · D0218 (wddm_1s_sampler.ps1) · D0219 (valentine_probe50_e3.sh) · D0220 (valentine_probe50_e3b.sh) · D0221 (convert_and_ship_selftest-T20-T21.out); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 221 · twin rows 221 · disagreements 2; sessions: 10 · kinds: 5 · missing: 1 · strays: 0 · revisions without a row: 0; the denies cross-check: denies: guard_git main: journal 1 == log 1 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-13T17:46:53Z → 2026-09-13T18:06:11Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 45 of 45 messages (100%), 2026-09-13T17:47:15.238Z → 2026-09-13T18:06:11.418Z.

## Episode 4 — transport/ship-replaces-held-staging-copy (SYM-128) (18:09 → 18:1xZ) — PARTIAL CLOSEOUT

**The mechanism, read (Observed):** `ship()` streams the bundle into `staging/.part-<sha16>` on the ThinkPad and ends with `mv .part-<sha16> staging/<name>`; POSIX `mv DIR EXISTING_DIR` places DIR INSIDE the existing one — so a reship under a name still sitting in staging nested the new assembly inside the old copy (S144 E4's Zero to One, repaired by hand as `.held-cb7e026f3fc02da8-20260912-205007`; the exporter answered NOOP on that dot-dir — Observed).

**The guard (Observed):** the same repair, mechanical and BEFORE the move — the remote command now reads `… tar -xf - -C .part && if [ -e <dest> ]; then mv <dest> <staging>/.held-<sha16>-<UTC stamp> || exit 97; echo MOVED-ASIDE <aside>; fi && mv .part <dest>`: an existing name is moved aside under the dot-prefixed name the exporter's sweep does not ingest, a failed move-aside aborts the ship (exit 97) rather than nesting, nothing in staging is deleted (the only `rm -rf` is the assembly's own `.part`), and the `shipped` event carries `replaced_staging=<aside>` when the remote reported the move (absent on a first ship). **Tripwire T22 a–e** (`convert_and_ship_selftest.py`, tar and ssh faked): the guard precedes the final mv and names the sha; nothing in staging deleted; MOVED-ASIDE → the event names the displaced copy; a first ship carries no key; the aside name dot-prefixed with the sha and a UTC stamp — GREEN 240/240 under marker-env. The A4 registry regenerated (`replaced_staging`); glass clean; gates green.

**What changes for the machine:** a reship (a supersede, a `--reaudit` pass, a `--reanalyze`) onto a name still in staging displaces the old copy instead of nesting inside it, and says so in its event; the ThinkPad's exporter sees one visible bundle per name.

**Contracts touched:** `windows-converter/convert_and_ship.py` (`ship()`), `convert_and_ship_selftest.py` (T22), `observability/schemas.json`; `SYMPTOM-INDEX.md` SYM-128 → `fixed`; `OPEN-TASKS.md` row struck; the tickets file; the journal's entry. Not exercised against the live ThinkPad (no ship this sitting; the lever `enforce`): the remote command's shell is the ThinkPad's (bash), the same one the previous command ran under — Inferred until the next real ship's event reads back, said so in the journal.

**Commits:** private 7d22a69 (receipts S146-ep4 D0222–D0224, the selftest output D0225, the tickets' E4 line, the Desk log); the public commit is the one carrying this section (named in §7 at the close).

**Receipts:** `journal/{calls,cost,usage}/S146-ep4.md` — window 2026-09-13T18:06:11Z → 2026-09-13T18:11:07Z (tiled onto the previous receipt's end); calls 29 · preloaded 5 · consulted 3 · scratch 4 · inferred 3 · harness 14 · denied 0 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 wo; Grand total (USD equivalent): 4.49 (session 4.49 + fleets 0.00); Categories used: testing (1), auditing (1); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 0 · advisories 0 · Rab's mid-turn words 1. Ledgered D0222 (calls) · D0223 (cost) · D0224 (usage) · D0225 (cas_selftest_S146_e4b.out); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 225 · twin rows 225 · disagreements 2; sessions: 10 · kinds: 5 · missing: 1 · strays: 0 · revisions without a row: 0; the denies cross-check: denies: guard_git main: journal 0 == log 0 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-13T18:06:11Z → 2026-09-13T18:11:07Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 16 of 16 messages (100%), 2026-09-13T18:06:25.191Z → 2026-09-13T18:11:07.277Z.

## Episode 5 — analyst/bound-chunk-generation (SYM-129) (18:13 → 18:2xZ) — PARTIAL CLOSEOUT

**The mechanism, read (Observed):** the local analyst request carried `options.num_ctx` and no generation bound; the S116 journal's chunk 296 ran 681 tokens in / 5,170 out (7.59×) and the only ceilings were the model's own stop and the 900 s client timeout — a runaway pays up to 900 s for what the inflation guard (1.5×, J34) rejects afterwards anyway, and a timeout fails the phase.

**The guard (Observed):** `_num_predict_for(chunk)` = the chunk's estimated tokens (chars / 3, a deliberate over-estimate) × `ANALYST_NUM_PREDICT_FACTOR` 2.0, never under 512, never over `NUM_CTX` (8,192); `process()` sets it for the next local call through `_call_bound` (the twin of `_last_call`, so the suite's one-argument fakes keep working) and clears `_last_call` before every call so a stale stop reason cannot outlive its call; `_generate()` puts `options.num_predict` on the request and records the reply's `done_reason`; a reply that stopped on the bound (`done_reason == "length"`) is REJECTED as `truncated` — first, before the think/fence/survival/ratio checks can read a cut text as something else — with the original shipped and the reason counted in `rejections` and the chunk row. The factor sits ABOVE the inflation reject on purpose: any output the bound can cut is one the ratio guard rejects anyway, so no acceptable chunk is lost — only the paying stops. **Tripwire** `analyst_selftest.py` S146-E5 (a)–(d): the bound's arithmetic and the factor above 1.5; the request body carries `num_predict` (the explicit argument, the process()-side channel, and NEGATIVE: no bound → no key) and a `length` reply reads `done_reason=length`; `process()` rejects a truncated reply with the original shipped and `rejections.truncated` 1; NEGATIVE: the same candidate with `done_reason=stop` passes — ALL TRIPWIRES FIRED 34/34 under marker-env (the converter suite re-run beside it: 240/240). The four pinned `rejections` dicts in the suite gained the `truncated` key (the breakdown's key set grew by one reason; the `analyst/done` event's key set, pinned by T17, did not change). The A4 registry regenerated; glass clean; gates green.

**What changes for the machine:** a runaway chunk stops at 2× its input within seconds instead of the 900 s timeout, is rejected as truncated, and the phase continues; `chunks_rejected`'s breakdown names it.

**Contracts touched:** `windows-converter/analyst.py`, `analyst_selftest.py`, `observability/schemas.json`; `SYMPTOM-INDEX.md` SYM-129 → `fixed`; `OPEN-TASKS.md` row struck; the tickets file; the journal's entry. Not exercised on a live run (no analyst pass this sitting): the bound's effect on a real runaway is Inferred from the journal's one instance; the next `--analyst` run's manifest (`rejections.truncated`) is the read-back.

**Commits:** private 2429b8c (receipts S146-ep5 D0226–D0228, the analyst selftest output D0229, the tickets' E5 line, the Desk log); the public commit is the one carrying this section (named in §7 at the close).

**Receipts:** `journal/{calls,cost,usage}/S146-ep5.md` — window 2026-09-13T18:11:07Z → 2026-09-13T18:18:28Z (tiled onto the previous receipt's end); calls 48 · preloaded 7 · consulted 15 · scratch 4 · inferred 3 · harness 19 · denied 0 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 w; Grand total (USD equivalent): 6.22 (session 6.22 + fleets 0.00); Categories used: testing (1), auditing (1); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 0 · advisories 0 · Rab's mid-turn words 3. Ledgered D0226 (calls) · D0227 (cost) · D0228 (usage) · D0229 (analyst_selftest_S146_e5b.out); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 229 · twin rows 229 · disagreements 2; sessions: 10 · kinds: 5 · missing: 1 · strays: 0 · revisions without a row: 0; the denies cross-check: denies: guard_git main: journal 0 == log 0 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-13T18:11:07Z → 2026-09-13T18:18:28Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 20 of 20 messages (100%), 2026-09-13T18:11:08.764Z → 2026-09-13T18:18:27.619Z.

## Episode 6 — exporter/ingest-verdict-guard (SYM-130, the ThinkPad's code) (18:21 → 18:3xZ) — PARTIAL CLOSEOUT

**The mechanism, read (Observed):** `linux-converter/converter/exporter.py` — the verdict guard ("pass, or flag with bless", signed S56) sits on the SUPERSEDE branch only; a first ingest ran the dedup grep and copied straight into the vault clone, so Ashby's failing body (degeneration TRUE, `verdict fail`) became the vault note 54b471d7 at 11:31Z under the Windows lane's `report` lever. The ThinkPad runs this file from `/home/rab/file-portal-src` (branch `feat/library-pipeline`, at S126's 119db7d — clean, remote GitHub) as `file-portal-converter.service` (`converter.main`: an inotify watch plus one sweep at startup; the sweep skips dot-prefixed names — E4's `.held-` name is safe on this side, Observed at `sweep()`).

**The guard (Observed):** after the dedup skip and before the copy, the first-ingest path reads `manifest.fidelity.verdict`; an explicit `fail` is refused — `EXPORT-INGEST-HELD <name>: incoming verdict 'fail' -- vault untouched, staging copy kept`, a receipt `ingest-held` (verdict, sha), return — the same shape as `EXPORT-SUPERSEDE-HELD`; `flag` still ingests (ship-with-losses-named, Rab's D1) and a MISSING fidelity block still ingests (old bundles carry none; the linux lane writes its own verdict at the seam). Whatever the Windows lever reads: the lever decides shipping on that machine, the guard decides ingesting on this one. **Tripwire** (`tests/test_exporter.py`, pytest against real repositories): a `fail` first ingest keeps staging, adds no commit, leaves the vault untouched and writes one `ingest-held` receipt; a `flag` ingests; a bundle with no fidelity block ingests; a held fail re-read by the sweep is held again with a second receipt — **38 passed on the ThinkPad** (a scratch clone of its checkout with the two changed files, its own venv's pytest 9.1.1); locally the same suite reads 23 failures on tests my change never touches (Windows path separators inside the vault clone's paths — `Inbox\paper--…\assets`; the four new tests pass locally, the Linux suite is Linux's). ruff at CI's pin: lint clean, the test file formatted once.

**The deploy (Observed):** the code committed and pushed first (cc64dd2); on the ThinkPad, `~/file-portal-src` fast-forwarded 119db7d → cc64dd2 at 18:27Z (clean before and after; the linux-converter tree had stood at S126); the exporter suite on the REAL checkout 38 passed and the whole linux-converter suite 90 passed (its own venv, pytest 9.1.1); `file-portal-converter.service` — the unit that carries the exporter (`converter.main`: the pipeline watch plus the staging watch and one sweep at startup) — restarted at 18:28:43Z only after those passes (the previous process had run since 14:20Z: 17.6 s CPU over 3 h 30 min); `is-active` active, MainPID 29195, the journal reading `watching /home/rab/file-portal/pipeline (lanes: convert-inbox, convert-scan-inbox)` and `watching /home/rab/file-portal/library/staging (exporting to /home/rab/file-portal/vault.git)` at 18:28:44Z; no EXPORT line after the start — the startup sweep found nothing to export (staging holds only the `.held-cb7e…` dot-dir, skipped by name). The allocator, the indexer timer and the fixity timer untouched. The undo: `git checkout 119db7d` in that checkout and the same restart.

**Contracts touched:** `linux-converter/converter/exporter.py`, `linux-converter/tests/test_exporter.py`; `SYMPTOM-INDEX.md` SYM-130 → `fixed`; `OPEN-TASKS.md` row struck; the tickets file; the journal's entry. The local dev venv `linux-converter/.venv` (uv, ignored) gained pytest 9.1.1 to run the suite here.

**Commits:** private bec7049 (receipts S146-ep6 D0230–D0232, the ThinkPad test and deploy outputs D0233–D0235, the tickets' E6 line, the Desk log); the public commit is the one carrying this section (named in §7 at the close).

**Receipts:** `journal/{calls,cost,usage}/S146-ep6.md` — window 2026-09-13T18:18:28Z → 2026-09-13T18:29:20Z (tiled onto the previous receipt's end); calls 57 · preloaded 4 · consulted 2 · scratch 2 · inferred 35 · harness 14 · denied 1 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 w; Grand total (USD equivalent): 6.74 (session 6.74 + fleets 0.00); Categories used: testing (2), auditing (2); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 0 · advisories 0 · Rab's mid-turn words 2. Ledgered D0230 (calls) · D0231 (cost) · D0232 (usage) · D0233 (thinkpad_pytest_e6.out) · D0234 (thinkpad_deploy_e6.out) · D0235 (thinkpad_restart_e6.out); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 235 · twin rows 235 · disagreements 2; sessions: 10 · kinds: 5 · missing: 1 · strays: 0 · revisions without a row: 0; the denies cross-check: denies: guard_git main: journal 1 == log 1 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-13T18:18:28Z → 2026-09-13T18:29:20Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 20 of 20 messages (100%), 2026-09-13T18:18:44.086Z → 2026-09-13T18:29:20.166Z.

## Episode 7 — vault/ashby-note-54b471d7: reverted on the numbers (18:31 → 18:4xZ) — PARTIAL CLOSEOUT

**The decision** is §9c, written and committed (8aea5fe) before the act; the numbers are there: the same body the machine held on 2026-08-25 (survival 0.8582 against the PDF's text layer, 149 of 156 pages scored, 50 flagged, degeneration true), in the vault only through the lever and the ingest gap.

**The act (Observed):** (1) the ThinkPad's vault work clone (`~/file-portal/vault-work`, staging empty — no ingest could race it): `git revert --no-commit 54b471d7` then a commit under my own identity (`Fable (Claude Fable 5.1) <fable@file-portal.invalid>`) whose message carries the numbers, the reason, the record's §9c and the undo — **f489189** — pushed to the bare `~/file-portal/vault.git`, whose `main` now reads `f489189 revert 54b471d7 …` over `54b471d ingest: …`; the work clone's Inbox holds no ashby dir. (2) Rab's Obsidian clone here: `git pull --ff-only origin main` in `Documents/Obsidian/… Vault/Library` (his clone, `main`, in sync at 54b471d before): now at f489189, in sync with the bare (0/0), the Inbox at seven notes with no ashby dir, and his one uncommitted edit (`Inbox/designing-freedom-stafford-beer--dbcce92c/…Beer.md`, a different path) still `M` before and after — read, not assumed. (3) The library keeps the body: `held/26bd434d9b5d8e43` (the S109 hold, 2026-08-25) and the two anchor copies (`anchor/Ashby …` and `… (1)`, today's); a repaired body returns as a first ingest (`flag` ingests; a `fail` is held by E6's guard; a bless is for flags).

**What Rab will see:** the Ashby note gone from his Obsidian Inbox (7 notes now) until a better body exists; the vault's history says who removed it and why, in the commit itself. **Undo:** a revert of f489189, pushed and pulled the same way — the note returns byte-identical.

**Contracts touched:** the vault (one commit, mine); `OPEN-TASKS.md` row struck; the tickets file; the journal's entry. No converter code; nothing shipped; the lever `enforce`.

**Commits:** private c2f5ef9 (receipts S146-ep7 D0236–D0238, the revert's read-back D0239, the tickets' E7 line, the Desk log); the public commit is the one carrying this section (named in §7 at the close).

**Receipts:** `journal/{calls,cost,usage}/S146-ep7.md` — window 2026-09-13T18:29:20Z → 2026-09-13T18:35:23Z (tiled onto the previous receipt's end); calls 33 · preloaded 2 · consulted 0 · scratch 4 · inferred 16 · harness 11 · denied 0 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 w; Grand total (USD equivalent): 4.65 (session 4.65 + fleets 0.00); Categories used: testing (1), auditing (1); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 0 · advisories 0 · Rab's mid-turn words 2. Ledgered D0236 (calls) · D0237 (cost) · D0238 (usage) · D0239 (vault_revert_e7.out); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 239 · twin rows 239 · disagreements 2; sessions: 10 · kinds: 5 · missing: 1 · strays: 0 · revisions without a row: 0; the denies cross-check: denies: guard_git main: journal 0 == log 0 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-13T18:29:20Z → 2026-09-13T18:35:23Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 13 of 13 messages (100%), 2026-09-13T18:29:39.068Z → 2026-09-13T18:35:23.293Z.

## Episode 8 — library/reconvert-dryruns-sampled: Beer, the converter's own run as a dry-run, sampled (18:37 → 19:0xZ) — PARTIAL CLOSEOUT

**The lever, read aloud before the launch:** `audit-mode=enforce` (since 17:45:29Z); the run is `--dry-run` — it converts, audits and ANCHORS; nothing ships whatever the lever reads. GPU law read at 18:36:42Z: no `.gpu-lock`, the card 1,225 MiB, no python compute app, the seat `bndit`; one process; the card's total sampled every 30 s (`beer_vram.log`) and the per-process committed memory every ~2 s (`wddm_beer.log`) beside it.

**The run (Observed):** `convert_and_ship.py <Beer> --dry-run` from 18:37:00Z: probe 1,139 chars/page, 184 pages, `ocr_fonts=True → lane=scan (untrusted_ocr_layer)`; ONE Marker call (184 pages sit under the 400-page chunking threshold; budget 3,680 s); CONVERTED in 868.4 s = 4.72 s/page (the ledger's own estimate 5.18); 1,590 block records over all 184 pages, the Marker body 214,044 bytes; the audit — witness `embedded-ocr`, Beer's own untrusted OCR layer, usable as a witness where Valentine had none: `doc_survival 0.9558`, 156 of 184 pages scored, 16 flagged, 18 runs, **degeneration TRUE** on line 1847 of the body (2,051 chars at zlib 0.022, the excerpt "A. The state of the state of the…" — a repetition loop; Inferred a true degeneration, not a table) — verdict FAIL; anchored as `anchor/DIAGNOSING THE SYSTEM FOR ORGANIZATIONS STAFFORD BEER (1)`; `DRY-RUN: not shipping` (a live run under `enforce` would have HELD it, and E6's guard would hold it again on the ThinkPad). The card's total (30-s samples): 1,242 MiB at the launch → 8,364–9,192 MiB from +2 min → **9,480–9,565 MiB at 99 % at 18:41:48Z as recognition began (n = 0 of 1,752)** → 9,495 ± 20 MiB at 48–67 % through recognition → 4,938 MiB at the second pass (18:51:20Z) → 1,232 MiB at the end. The per-process split (528 samples ~2 s apart): the sidecar (pid 28628) 806 MB → 7,205 MB at +2 min → **8,321–8,327 MB committed in 324 consecutive samples through recognition** — beside it the desktop's usual tenants (dwm 512 · Firefox 478 · explorer ~220 · Spotify 175), none moving by a megabyte. The recognition rate from the progress file: 0.76 lines/s in its first minute, ~3.2 lines/s over the 1,752 lines (nine minutes).

**The reading:** **The sidecar itself owns the card's ceiling on this book.** At batch 8, Beer's recognition holds 8.3 GB committed for its whole duration where Valentine's held 4.4 GB — the same arguments, heavier pages (1,139 chars/page of OCR text, 1,752 lines over 184 pages) — and with the desktop's 1.2 GB the card sits at 9.5 GB for nine minutes; it survived, at ~3 lines/s. This is the morning's kill row's shape (Valentine at 9,515 MiB, 100 %) with the tenant finally named: a recognition batch at the card's edge, no neighbour (Valentine's own 9,515 that morning stays Inferred as the same mechanism — its split was never sampled). What follows: (1) no slice or budget change, still; (2) the recovery ladder already drops to batch 4 (`STALL_RECOVERY_BATCH`, OK-17) but fires only on FROZEN progress (`STALL_FROZEN_S` 900 s) or at the budget's timeout — a run that crawls at the ceiling pays the whole budget and dies the morning's death; the trigger that reads the ceiling moment `_note_ceiling` already records plus a recognition rate under a floor is `converter/ceiling-crawl-triggers-the-ladder` (open, product code with its tripwire; the number: Beer's ~3 lines/s at 9.5 GB against the direct runs' curve at 5.6 GB, and the morning's Valentine that did not survive its 4,000 s); (3) Beer's `fail` is a true degeneration line, the bench's or a batch-4 re-run's — the gauge reads its fresh anchor beside the held copy next.

**A gap found live (Observed):** E3's `_note_ceiling` recorded the moment inside this very run (the card ≥ 8,192 MiB from +2 min) and nothing projected it: a single-call run has no `slice` event, no death certificate fired, and the `converted` event carried no peak and no split. Fixed in this episode, after the run ended (never while the converter read the file): the `converted` event carries `peak_mib` and, when the card reached its ceiling, `ceiling_mib`/`ceiling_top`/`ceiling_at` — T21 (l) through the real `convert()` with the stubbed Marker and `_CEILING` preset, (m) the negative control — GREEN 242/242 under marker-env; the A4 registry regenerated. Beer's own `converted` row (18:51:55Z) predates the fix and carries neither; the sampler is this run's evidence.

**Contracts touched:** the gauge (Beer's fresh anchor beside its held copy is the next reading); `OPEN-TASKS.md` row struck; the tickets file; the journal's entry; the evidence under `measurement/evidence/e8-beer-dryrun/`. Nothing shipped.

**Commits:** private 747c7a3 (receipts S146-ep8 and ep9, Beer's four evidence pieces, the tickets' lines, the Desk log); the public commit is the one carrying this section (named in §7 at the close).

**Receipts:** `journal/{calls,cost,usage}/S146-ep8.md` — window 2026-09-13T18:35:23Z → 2026-09-13T18:53:47Z (tiled onto the previous receipt's end); calls 59 · preloaded 3 · consulted 4 · scratch 4 · inferred 25 · harness 23 · denied 0 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 w; Grand total (USD equivalent): 24.99 (session 24.99 + fleets 0.00); Categories used: testing (1), auditing (1); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 0 · advisories 0 · Rab's mid-turn words 6. Ledgered D0240 (calls) · D0241 (cost) · D0242 (usage) · D0243 (wddm-per-process-2s.log) · D0244 (vram-30s.log) · D0245 (convert-dryrun-log.txt) · D0246 (converter-events.jsonl); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 246 · twin rows 246 · disagreements 2; sessions: 10 · kinds: 5 · missing: 1 · strays: 0 · revisions without a row: 0; the denies cross-check: denies: guard_git main: journal 0 == log 0 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-13T18:35:23Z → 2026-09-13T18:53:47Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 23 of 23 messages (100%), 2026-09-13T18:35:25.333Z → 2026-09-13T18:53:46.981Z.

## Episode 9 — intake/watcher-restart-onto-current-code: attempted, declined (18:43 → 18:4xZ) — PARTIAL CLOSEOUT

**The mechanism, read (Observed):** the live watcher is pid 13572 (`watch_and_convert.py`, the uv re-exec of a marker-env trampoline 13508), started 2026-09-12T19:41:23Z by the widget (pid 8076, `file-portal-widget.exe`, the installed C4559805) through its titlebar ⏻ (`watcher.rs` spawns the script and stops it with `taskkill /T` on the tree); it spawns `convert_and_ship.py` FRESH per drop (`subprocess.Popen`, line 480), so every conversion already runs the current converter — only the watcher's own loop (the changes since S141: its failures as events, ERR-100) is the code it misses. No stop file, no IPC: the widget's ⏻ is its only control, and the widget owns the pid its ⏻ state reads.

**The attempt (Observed):** the heads-up posted on the Desk at 18:45:01Z (fb21cd20: two clicks on the ⏻, stop then start; "if you are mid-something, one line here and I wait"); the computer-use access request for "File Portal" (the widget) at 18:45Z — **denied: `user_denied`**. Taken as Rab's word; not retried. Not done from the shell on purpose: a `taskkill` here and a start from this session would leave the new watcher as this session's child (it dies with the session) and the widget's ⏻ reading "stopped" — his next click would start a second watcher.

**What stands:** the watcher runs its 09-12 loop code; the intake works (the card reads it writing the intake state every few seconds); the converter it spawns is current. The restart is two clicks on the widget's ⏻, his, or a later grant. The OPEN-TASKS row stays `open` with this reason.

**Commits:** private 747c7a3 (shared with E8: receipts S146-ep9 — the window after E8's close; the attempt's own calls sit inside E8's window, the two ran interleaved); the public commit is the one carrying this section (named in §7 at the close).

**Receipts:** `journal/{calls,cost,usage}/S146-ep9.md` — window 2026-09-13T18:53:47Z → 2026-09-13T18:54:42Z (tiled onto the previous receipt's end); calls 6 · preloaded 1 · consulted 0 · scratch 3 · inferred 0 · harness 2 · denied 0 · errors 0 · lanes: 0 lane transcript(s) with calls in the window · lane calls 0 · lane denies 0 · workflows: 0 work; Grand total (USD equivalent): 1.11 (session 1.11 + fleets 0.00); Categories used: testing (1); harness: api errors 0 · compactions 0 · model fallbacks 0 · hook timings 0 · advisories 0 · Rab's mid-turn words 0. Ledgered D0247 (calls) · D0248 (cost) · D0249 (usage); ledger C:\Users\Bndit\Projects\file-portal-private\agent-scripts\journal: md rows 249 · twin rows 249 · disagreements 2; sessions: 10 · kinds: 5 · missing: 1 · strays: 0 · revisions without a row: 0; the denies cross-check: denies: guard_git main: journal 0 == log 0 · guard_record main: journal 0 == log 0 · lanes: journal 0 == log 0 ✓ (window 2026-09-13T18:53:47Z → 2026-09-13T18:54:42Z; tolerance 0 — one clock); occupant read from the transcript: claude-fable-5-1 (Claude Fable 5.1) — 2 of 2 messages (100%), 2026-09-13T18:53:56.237Z → 2026-09-13T18:54:21.442Z.

## §15 Current state (Observed at close, tagged where not)

- Public `feat/library-pipeline` at the closing commit (this one) over the E9/E8 content commits; CI green through E7's 03ec100 (its run read in §20) — every S146 commit observed green in its own episode. Private `workshop` at the last episode's receipts commit + the close receipts.
- The lever: `audit-mode.txt` reads `enforce` since 17:45:29Z, with a writer on record (the first); the card prints `levers audit=enforce` and no `SHIPS ON VERDICT` line.
- The vault: tip f489189 (the Ashby revert, mine) over 8d7cc85; seven Inbox notes; Rab's clone in sync, his Beer edit uncommitted and untouched. The ThinkPad: `file-portal-converter.service` on cc64dd2 since 18:28:43Z, watching staging (empty but the S144 `.held-` dot-dir).
- The library: the Ashby body in `held/26bd434d…` and two anchor copies; Valentine's fresh anchor `(1)` (S145 E8); free at the close (Beer's dry-run ended 18:52Z; no `.gpu-lock`); the live watcher pid 13572 untouched (E9 declined).
- The live watcher: E9 attempted and declined at 18:45Z — the watcher (pid 13572) runs its 09-12 loop code and spawns the current converter per drop; two clicks on the widget's ⏻, his, or a later grant.
- Memory: MEMORY.md at the lockstep write (the S144–S141 fold first; the S146 block); TIME-STATE = this close. The autonomy memory `autonomy-experiment-2026-09-13.md` in the library.
- The Desk live with both boards; the Desk and relay watchers armed until this session ends; Codex asleep, its watchers its own. The experiment continues until Rab says it has stopped.

## §18 Next entry point

1. **The experiment stands until Rab's word**: the next sitting opens on the same reading (BROAD; the protocol whole; the journal per improvement).
2. **Read back the three guards' first live rows** as they land: a reship's `replaced_staging`, an analyst run's `rejections.truncated`, the ThinkPad's `EXPORT-INGEST-HELD` — each closes its journal entry's honest edge.
3. **E8's second book** (a Damodaran, ~5.6 h, a dry-run, sampled) and the gauge's reading of Beer's fresh anchor beside its held copy; then the bench-ready evidence view attached at hold time (vision 2.3, `hold/attach-evidence-view`).
4. **The scan lane's second witness** (`audit/scan-lane-second-witness`, vision 2.2): the gauge's Tesseract agreement wired as the audit's witness for scan-lane books, its coverage printed beside the verdict — the ticket that lets a scan pass.
5. **A census-grade tracker** (IB-018): a receipt that lists its own longest process and prints UNREAD when it cannot.
6. The S147 open: the card's levers line read aloud (`enforce` now); the smoke ALONE in the FOREGROUND with nothing else writing (ERR-105); fold `MEMORY.md` first (the S145–S141 block, folded at this close, is the next fold's target).
## §19 Close values — `close.sh 271c39b`, 2026-09-13T18:58:55Z (verbatim)

```
    PIN              271c39b
    DIFF             20 file(s) since 271c39b · dirty 0 = mine 0 · peer-owned 0 (none) · 1 untracked
    GLASS            clean since 271c39b (--enforce; bare runs exit 0 regardless — SYM-046)
    RUST             skipped — no windows-widget/ change since 271c39b
    CI               still running on d5db075c — close may proceed; CHECK IT AFTER
    LEVERS           3 threshold-shaped constant(s) added since 271c39b with no lever, no waiver: ANALYST_NUM_PREDICT_FACTOR · ANALYST_NUM_PREDICT_MIN · CEILING_FRACTION
                     docs/18 §2: a number that decides something is a lever. Convert, or add '# lever-waiver: <who/what evidence>'
    DOCTOR           lever lexical parity: figure-triage.txt names 8 · quoted refs 8 · MISSING 0
                     LEXICAL REF: mode · min_area_pt2 · vector_min_paths · cluster_gap_pt · text_coverage · words_per_line · table_overlap · accounted_for
                     UNREAD — a quoted lexical occurrence does NOT prove the consumer reads the lever; ARMED on MISSING only
    DOCTOR-HEAD      UNREAD — closeout-headline probe re-run not yet mechanized; NOT a statement the numbers reproduce
    CENSUS           selftest.sh: declared 11 = FIRED 11 = banner 11/11 (exit 0)
    CENSUS-SCOPE     UNREAD — broader TEST-STRATEGY fixture census not yet mechanized; NOT a statement all promised tripwires ran
    REGISTER         OPEN-TASKS.md written this session: +11 / -5 lines
                     STRUCK 9 · ADDED 6 — net -3, the register moved DOWN
    MEMORY           clean — soft clock is DURABLE at 18ad89d
    DEBT             open SYM 49 -> 44 (-5) — debt fell
    PUSH             ahead of upstream: 0 (the close pushes; 0 after)
════════ close exit 0 — values only; the judgment half is SKILL.md ════════
```

- **DIFF:** every changed file is in the content commit `d5db075` or this closing commit; the row follows, then
  `row_check.sh 146` on it, then the push.
- **Relay:** no entry — nothing to Codex (standing).

