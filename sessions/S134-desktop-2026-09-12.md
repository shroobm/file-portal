# S134 — Desktop — 2026-09-12 — AGENT SCRIPTS: the private library of the scripts a session runs, each with its brief

*Record before the act (docs/28; J63's guards). Written at the open, before any tracked write. This public record names
the library's location and the public guard; the library itself, its README, its policy and every brief are PRIVATE.*

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

> If there are certain scripts that you must repeatedly use to navigate around the project and what not, start recording them down and keeping them in an extremely laid out and organized format in a AGENT SCRIPT folder, it should contain a read me, policy, a sub folder for scripts for navigation purposes, each subfolder for scripts testing, auditing, developing, reading, a sub folder with a python organizer that organizes error scripts into error sub folder categories (just the sub categories for scripts but their error log bins essentially) I think this would be advantageous for you, if you have any idea what else it should be organized, or propped up, just make sure that it's automate proof, and nothing is disorganized. The information you put down along side the script, should itself contain context that itself leads an agent in understanding, equivalent to a prompt. (In the future you would just have to tell sub agents what scripts they'll be using instead of them manually trying to make them)
>
> Also scripts should NEVER BE PUBLIC

**The evidence the ask rests on** (`Observed`, this session's scratchpad): `memory_s119.py` … `memory_s129.py`,
`record_s124_open.py` … `record_s133_open.py`, `registers_s121.py` … `registers_s129.py`, `s19_s124.py` … `s19_s129.py`,
`s20_s124.py` …, `ledger_row_s120.py` … `ledger_row_s129.py` — the same five scripts rewritten once per session for ten
sessions, plus the bundle inspectors written for one book by name (`zero_to_one_*.py`, ten files).

**Standing:** nothing to Codex until lifted; the record before any tracked write (two calls); a probe in the shared
checkout is a file run by its literal path; `row_check.sh <N>` and CI's own gates before any push (ERR-088);
`--census` at every close.

## §2 Starting State — `open.sh`, 2026-09-12T08:5xZ (verbatim)

```
════════ MUSTER · OPEN · 2026-09-12T08:53:03Z ════════
[0] IDENTITY
    ledger newest    S133  3973e62
    this session     S134  ·  desktop  ·  2026-09-12
    session marker   coordination/private/session.current = S134 (guard_record / guard_git read it)
    advisory         S134 is named in the INHERITED tree (3973e62) — read each, do not blanket-correct:
                     sessions/S133-desktop-2026-09-12.md 
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-12T08:53:03Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 86 (tally == hook)
[3a] LEDGER ORDER  ✓ 116 Desktop-lane rows ascend (S16 → S133)
[3b] LEDGER PARSE  ✓ 116/142 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ 3973e62 (S133) == ledger 3973e62, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 9 · anchor 30 · pending 0 · drop 0
    levers           audit=report · analyst=local · batch=8
    gpu-lock         absent
    events           341 line(s)
    open-tasks       144 item(s) · last written 7 minutes ago
    error-bin        88 row(s) — read the CLASS table before you probe · last written 7 minutes ago
    symptoms         115 row(s), 44 open · last written 25 minutes ago
    relay            167 entries · run `gate.py status` for the board
    widget           13196
    widget autostart task 'File Portal widget (J57)' Ready — AtLogOn · auto-logon ON (Bndit)
    python procs     7
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 50896d1
    thinkpad         converter=active · vault tip 8d7cc85 · staging 0 · receipts 17
[3] PIN
    --since          3973e62
    closeout         sessions/S134-desktop-2026-09-12.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

## §3 The pin

`--since 3973e62` (the S133 closing commit; `f8770cc` row, `a985479` fix, `ce48107` §20 are on origin; CI 253 green).

## §9 The plan

1. **Private** (`file-portal-private/agent-scripts/`, branch `workshop`): `README.md` (what it is; the index — one row
   per script: folder · name · one line · when; how to brief a sub-agent with it), `POLICY.md` (the laws), five folders
   Rab named — `navigation/`, `testing/`, `auditing/`, `developing/`, `reading/` — plus `errors/` (the organizer, its
   bins, its selftest, an `inbox/`). Every script has a sibling `<name>.brief.md`: PURPOSE · WHEN · CALL (the literal
   form) · INPUTS · OUTPUTS · EXIT CODES · NEVER · EXAMPLE · RELATED — the prompt a sub-agent gets.
2. **Generalised from the scratchpad, by argument, not by book name:** the session scripts (record open, the close
   chain with §19 / the row / `row_check` / CI's gates / the push / CI observed, the memory lockstep), the bundle
   inspectors (find, audit, holes, rescore — by held id, roots from `roots.json`), the receiver reader, the process
   readers, the fleet-journal readers, the CI observer.
3. **The organizer:** `errors/organize_errors.py` — files error logs from `errors/inbox/` into category bins by
   pattern (guard-deny · quoting · encoding · traceback · powershell · ssh-network · ci-red · selftest-red · unsorted);
   dry-run by default, `--go` to move, never deletes, one JSONL manifest line per move, idempotent; its selftest plants
   one of each and a decoy.
4. **Public:** `.gitignore` gains `agent-scripts/`; a junction `file-portal/agent-scripts` into the private folder (the
   prototypes' mechanism); `git check-ignore -v` the tripwire. Nothing else public.
5. Selftests; a review lane or two if the organizer warrants it; the close with `row_check.sh 134` and CI's gates
   before the push; the private commit pushed; memory in lockstep.

## §8 Findings

- **F1 — the library exists, privately** (`Observed`): private commit `c17ba74` on `workshop`, 57 files — `README.md`
  (the index, how to brief a sub-agent), `POLICY.md` (eight laws), 19 scripts in the five folders Rab named plus
  `errors/`, each with its `.brief.md` in the nine-heading form (PURPOSE · WHEN · CALL · INPUTS · OUTPUTS · EXIT CODES ·
  NEVER · EXAMPLE · RELATED). Every script ran once from its new home before the commit; the EXAMPLE in each brief is
  that run, dated.
- **F2 — the public guard** (`Observed`): `.gitignore` line 113 `agent-scripts/`; the junction
  `file-portal/agent-scripts` → the private folder; `git check-ignore -v` names the rule for `README.md` and for
  `errors/organize_errors.py`; the public `git status` shows only `.gitignore` modified.
- **F3 — the organizer and its tripwires** (`Observed`): nine bins; rules ordered specific-before-generic; dry run by
  default; a manifest line per move with the sha256; collisions suffix; a second `--go` is a no-op; the selftest plants
  one log per bin plus a two-rule specimen and a decoy, checks the byte census before == after — **20/20**. The real
  inbox is empty and says so.
- **F4 — the numbers I typed** (`Observed`, twice this session): a brief said `roots: 22` where the run printed 23; another
  said `17/17` where the banner read 20/20. Both corrected at the generator, and the organizer's brief now says "read
  the banner, never this line". Rule 3 of the tag law, in the very files whose policy states it.
- **F5 — the DEGENERATE flag uses the audit's own detector** (`Observed`): my first regex for "a loop" required one
  repeated word and missed `AND ROUTE AND ROUTE`; `bundle_holes.py` now asks `fidelity_audit.degeneration()`, and the
  only remaining "hole" on the shipped book is Marker's loop, flagged.
- **F6 — `close_chain.sh` without arguments died on `set -u`** (`Observed`): a usage line and exit 2 added at the
  generator; the brief's EXIT CODES were already promising it.


## §10 Known failures / residue

- **`close_parts.py` uses today's date for the record path** — a close after UTC midnight of the open's day needs the
  date passed; named in its brief, not fixed tonight.
- **The library's own line endings are LF** in a repo whose `core.autocrlf` is on: git warns it will check them out
  CRLF; the bash scripts must stay LF on disk (a CRLF `#!/usr/bin/env bash` fails). A `.gitattributes` (`*.sh text
  eol=lf`, `*.py text eol=lf`) is the fix; owed.
- **No review lane over the library** — the session's fleet budget went to S132's review and this build; the briefs
  were read once by their author only. A Readability lane over the briefs is the first thing S135 should spend on.
- **The scratchpad's one-off scripts stay where they are**; nothing was deleted (POLICY §3). What was promoted is named
  in the README's Lineage.

## §15 Registers

- No row struck, none filed — the ticket was Rab's word this sitting, not a register row; said out loud. The dated
  line the REGISTER gate reads is written for this session (S133's lesson).
- **Public:** `.gitignore` (the guard) and this record. **Private:** `c17ba74` (the library).

## §18 Next entry point

1. **Open:** `open.sh`; the card should read S135. The Desk inbox first (`desk.py inbox`).
2. **Use the library from the first call:** `agent-scripts/developing/record_open.py` for the record;
   `agent-scripts/developing/close_chain.sh` for the close; brief a lane by pasting its `.brief.md`.
3. **Owed:** `.gitattributes` for the library; the date argument in `close_parts.py`; a Readability lane over the
   briefs; J71 (the push hook); SYM-115's `control_token`; Rab's calls (`audit=report`?, the widget outside its task,
   J46, J65/J67/J68).
4. **Standing:** nothing to Codex until lifted; the record before any tracked write (two calls); a probe in the
   shared checkout is a file by its literal path; `run_gates.sh --pin` and `row_check.sh <N>` before any push;
   `--census` at every close.


## §7 Work log (UTC)

- **08:5x** `open.sh` clean (S134, pin 3973e62). This record. The receipt (tracker pid 6672) still running.
- **08:5x–09:1x** POLICY, README; the three generators (navigation + reading, testing + auditing + developing,
  errors); every script run once; the organizer's selftest 20/20; the private commit `c17ba74` pushed; the public guard
  line and the junction; the guard stepped on. Rab's Desk posts answered at 08:27, 08:34, 08:51.
- **09:05** the register's dated line; `run_gates.sh --pin 3973e62`; the content commit; `close_chain.sh 134 …` from
  the library; lockstep; §20; the receipt closed; the sign-off.
## §19 Close values — `close.sh 3973e62`, 2026-09-12T09:06:23Z (verbatim)

```
    PIN              3973e62
    DIFF             7 file(s) since 3973e62 · dirty 0 = mine 0 · peer-owned 0 (none) · 1 untracked
    GLASS            clean since 3973e62 (--enforce; bare runs exit 0 regardless — SYM-046)
    RUST             skipped — no windows-widget/ change since 3973e62
    CI               no run for d06efd15 yet (unpushed? ahead=2) — NOT a green
    LEVERS           no unlevered threshold constants added since 3973e62
    DOCTOR           lever lexical parity: figure-triage.txt names 8 · quoted refs 8 · MISSING 0
                     LEXICAL REF: mode · min_area_pt2 · vector_min_paths · cluster_gap_pt · text_coverage · words_per_line · table_overlap · accounted_for
                     UNREAD — a quoted lexical occurrence does NOT prove the consumer reads the lever; ARMED on MISSING only
    DOCTOR-HEAD      UNREAD — closeout-headline probe re-run not yet mechanized; NOT a statement the numbers reproduce
    CENSUS           selftest.sh: declared 11 = FIRED 11 = banner 11/11 (exit 0)
    CENSUS-SCOPE     UNREAD — broader TEST-STRATEGY fixture census not yet mechanized; NOT a statement all promised tripwires ran
    REGISTER         OPEN-TASKS.md written this session: +2 / -0 lines
                     STRUCK 0 · ADDED 0 — net 0 — nothing was worked down; say so in the closeout rather than imply it
    MEMORY           clean — soft clock is DURABLE at a85139a
    DEBT             open SYM 44, unchanged since 3973e62
    PUSH             ahead of upstream: 2 (the close pushes; 0 after)
════════ close exit 0 — values only; the judgment half is SKILL.md ════════
```

- **DIFF:** every changed file is in the content commit `d06efd1` or this closing commit; the row follows, then
  `row_check.sh 134` on it, then the push.
- **Relay:** no entry — nothing to Codex (standing).

