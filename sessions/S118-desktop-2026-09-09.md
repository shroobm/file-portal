# S118 — Desktop — 2026-09-09

⟨claimed: Fable 5.1 · S118 · desktop · opened 2026-09-09T14:2xZ⟩

## §1 Session Intent

Rab, verbatim: *"B"* — his pick on the walkthrough he asked for first (*"Before you do this, walk me through you're
doing if you did the task for this signature. Muster. Decide DDIA: (a) re-run / (b) lever 0.80 / (c) bench+reaudit.
J41 = manifest chunk_scores."*). (b) as walked through: **J41 first** (per-chunk `survival`/`ratio`/`reason` rows
kept in the manifest as `analyst.chunk_scores`), **then the per-chunk survival lever 0.50 → 0.80** on his word with
the 08-30 journal re-measured first, **then the DDIA re-run**, so the first journal that survives success is the
one that measures the new lever.

*(Written before any build; left unedited for the rest of the session — docs/21 §1.)*

## §2 Starting State — the `open.sh` card (2026-09-09T14:2xZ, exit 0; unchanged rows elided)

```
    ledger newest    S117  93c5dc6
    this session     S118  ·  desktop  ·  2026-09-09
[2] SOFT clock ... ✓ 85 (tally == hook)
[3] HARD clock ... ✓ 93c5dc6 (S117) == ledger 93c5dc6, ancestor of HEAD
VERDICT .......... ✓ CLEAN — clocks reconcile
    origin           in sync (fetched)
    pipeline         held 8 · anchor 28 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    open-tasks       123 item(s) · last written 3 days ago  *** not written in 3d — is it still true? ***
    symptoms         75 row(s), 27 open · last written 4 days ago
    relay            138 entries
    widget           15308
    thinkpad         converter=active · vault tip 50896d1 · staging 0 · receipts 13
    --since          93c5dc6
    closeout         sessions/S118-desktop-2026-09-09.md
```

**Pinned `--since 93c5dc6`** (the S117 close). HEAD at open: `a205375`. Three idle days: no commits, no relay entry
(Codex silent since 09-03), the ThinkPad's weekly fixity check passed 09-07 on tip `50896d15`.

## §3 Inherited, all `Historical`

- S117 §8 F2/F3 + §18: DDIA held at `fc1f068c3a8eeb63`, analyst-phase 0.9683 / 61 runs (a 264-word dropped paragraph
  + index pages prosified); J34's first live run 0/492 with its distribution lost (J41). Three built dispositions.
- S115 §7 / OPEN-TASKS J32: on the 08-30 Univ 4e journal (500 hash-matched passed records of 623) the 0.50 guard
  catches chunks 23/78; below 0.80 = 5 (v1) / 6 (v2, after R5) of 500 — "threshold and reject-vs-flag are Rab's".
- The 0.50 lever's waiver (`analyst.py`): *"Rab's word only … moves on a re-measured journal, never by taste"*.
- `--reanalyze` (docs/19 §3.1, `convert_and_ship.py:1951`): refuses a bundle whose only anchored copy carries an
  `analyst` block — "use re-convert instead"; it also never writes the watcher's `.gpu-lock`.

## §4 Promoted before the build (`Observed`, each with its probe)

- **DDIA's anchor** (`fp_paths.root('anchor')`, 28 entries): exactly one copy of the DDIA source, `analyst block:
  True`, verdict `fail` — so **`--reanalyze` would refuse**; the (b) re-run is a **re-drop of the PDF** through the
  watcher (Marker ≈ 504 s again + the analyst), which is also the path that writes `.gpu-lock` and makes the room
  chat yield. The J33 sidecar in `held/fc1f068c3a8eeb63` IS the Marker body a sidecar-aware `--reanalyze` could start
  from — not in this signature; filed at close (J42).
- Card at open: held 8, drop 0, gpu-lock absent, widget 15308 + watcher 26200 up since 09-03, room chat idle.
- **The pre-move re-measure cannot run — the 08-30 journal's reference is gone from this machine** (F1 below). The
  S116 script that scored 500 records on 09-05 now scores **0 of 0** (`Observed`, both my new script and the S116
  one as a control); `.chunk-work/` holds only `fc1f068c3a8eeb63` (DDIA) — **A1 (signed 2026-08-31, `convert_and_
  ship.py:1333-1340`) sweeps the previous book's slices when the next convert starts**, and the DDIA convert did
  exactly that at 00:31Z on 09-06 (*"SLICE CACHE: swept previous book 14c66834bdfeaa2e"*). Both anchored Univ 4e
  copies are analysed (`analyst: True`), the held one predates the sidecar (J33) — no Marker body survives. The
  Univ 4e PDF is still in `drop/done` (30,654,951 bytes, Aug 30): a Marker-only re-run (≈17 min at 0.75 s/page)
  could rebuild it, hash-match permitting — not in this signature. **The lever therefore moves on the 09-05
  measurement** (recorded S115 §7, S116 §9 R2, OPEN-TASKS J34: below 0.50 / 0.80 / 0.90 = 3 / 6 / 14 of 500
  hash-matched passed records under `j32a-v2`), which satisfies the waiver's rule (*"moves on a re-measured
  journal"*) as a measured journal four days old; and **the DDIA re-run's `chunk_scores` is the first post-move
  measurement** — J41 is the reason it will exist.

## §7 Work log

- **14:2xZ** `open.sh` exit 0, pin `93c5dc6`. This file committed BEFORE the J41 fleet was launched (`c131720`).
- **14:3x–14:5xZ J41 built** by a Sonnet builder in a worktree + a Sonnet refuter (`wf_d3f7b9b4-41f`, 16 min, 106 tool
  uses; the worktree spawned at `7c006f2` again — reset first): `_score_row` + `meta["chunk_scores"]` rows `{i, s?,
  r?, x?}` from the live loop, the failed branch and the resume branch; manifest only (both `analyst/done` emits
  untouched — T17 206/206); `converter:chunk_scores` EVIDENCE disposition; docs/15 §7; four selftest cases. Refuter
  **GO_AMENDED**, decoy caught (it read the code for the key names). Cherry-picked `07daaf5`. **Amended here on its
  two findings:** (c)'s negative control deleted a dict key, not the collector — rewritten to the SYM-074 (d)
  technique (blank the one collector line, exec the patched source, watch `chunk_scores` vanish, then the real
  module still carries it); a dead `status == "failed"` arm in the resume branch removed with its reason recorded.
- **The lever moved: `ANALYST_CHUNK_SURVIVAL_MIN` 0.50 → 0.80** on Rab's "B", the waiver header rewritten with the
  basis (the 09-05 measurement) and the reason the pre-move re-measure could not run (§4). Battery after both:
  analyst selftest **22/22** (the J32-B/J34 fixtures all sit outside 0.50–0.80, so no case moved), convert_and_ship
  206/206, analyst_audit 8/8, ruff clean, glass `--since 93c5dc6 --enforce` clean, acceptance 141/141, schema
  registry PASS, CRLF intact. docs/15 §5/§6 and J32's row carry the move.
- **The re-drop (slot 3)** follows the commits below: the PDF copied into `drop/` again; the watcher runs Marker
  (≈504 s) and the analyst under 0.80 with J41 collecting; the held slot parks the incoming beside the existing
  `fc1f068c3a8eeb63` if it fails again (S65's rule), or ships it if the doc gate passes.
- **14:54:27Z the re-drop** (`c5709d7` pushed first). PID 15944 took the lock at +5 s; **Marker did not run** — all
  four slices `resumed: True` from DDIA's own cache (A1 keeps the latest book), `convert/converted wall_s 0.0`,
  `cost_s 504.4` carried; convert-phase audit identical (flag 0.8559 / 299); `analyst/start` 14:54:43Z, 1,989,461
  chars. Monitor armed. Mid-run reads for Rab: 124/492 at 15:28Z, 236/492 at 15:57Z (16 s/chunk), 388/492 at
  16:4xZ — and the "what is it receiving" peek (`peek_analyst.py`): the program text + chunk 389 (612 words, stream
  processing / idempotence), chunk 388 back near-verbatim at survival 0.9556 / ratio 0.9944.
- **17:08:37Z `analyst/done`:** 472 passed · **20 rejected** (`fence 8, survival 12, think_leak 0, inflation 0`) · 0
  failed · 8,033.7 s · goodput 56.24 tok/s. **17:08:44Z analyst-phase audit: `doc_survival 0.9718`, `runs_total 49`
  (25 listed), verdict `fail` → `audit/held`.** The lever moved the book from 0.9683 / 61 to 0.9718 / 49 and caught
  twelve deletions instead of three — including the 264-word one — and the gate is 0.995. **Held again**, and the
  incoming REPLACED the first attempt in `held/fc1f068c3a8eeb63` (no repairs on the occupant, so S65's park-beside
  rule did not apply; the first attempt's analysed copy survives in `anchor/`, two DDIA copies there now).
- **J41's first live read** (`read_chunk_scores.py`): `chunk_scores` present, **492 rows, 17,507 bytes** (36 B/row);
  survival scored on 484, ratio on 472; passed-row survival buckets: [0.80,0.90) 36 · [0.90,0.95) 55 · [0.95,0.99)
  173 · ≥0.99 208; survival rejections at 0.033 / 0.29 / 0.33 / 0.39 / 0.44 / 0.52 / 0.65 / 0.69 / 0.72 / 0.73 / 0.77
  / 0.78 (chunks 488, 470, 401, 440, 284, 461, 290, 466, 82, 473, 1, 76); top ratio 1.15 (chunk 4). **Sum of (1−s)
  over the 472 passed rows = 14.33 chunk-equivalents = 3.04 % of windows**; the doc gate allows 0.5 %. By range
  (`loss_where.py`): front 1–40 → 0.90, **body 41–440 → 12.43**, back 441–492 → 1.00 — the loss is the body, not the
  index.
- **What the loss IS** (`failing_windows.py`, the actual lost 12-word windows of the four lowest passed chunks): (a)
  `within\_recursive` · `LIVES\_IN` · `in\_usa` · `doi…\_13` — Marker's backslash-escaped underscores that the model
  unescapes; **unit probe: `within\_recursive` vs `within_recursive` scores 0.0 under j32a-v2** because
  `prepare_output` strips the `_` as markdown BEFORE `unescape` can see the `\_` pair, leaving `within\recursive` vs
  `withinrecursive` (**SYM-076**); (b) `we rst follow` · `user proles` · `21page1750` — Marker's dropped fi/fl
  ligatures and broken link syntax that the model REPAIRS, counted as loss; (c) real deletions of 1–4 windows in
  chunks that passed at 0.80–0.82 (chunk 100's B-tree paragraph, chunk 127's evolvability paragraph); (d) a number
  rewritten: `person(100,` → `person(10,` in chunk 81 (a corruption no guard sees).
- **Sized offline, the real document audit on sidecar vs shipped body** (`ladder_v3_probe.py`, `ladder_v3b_probe.py`;
  the as-shipped run reproduces the manifest exactly, 0.9718 / 49; `fidelity_audit` binds `prepare_output` by name,
  so both bindings were patched): **v3 (unescape incl. `_` BEFORE the markdown strip) → 0.9756 / 46 (+0.0038)**, with
  `\rm` vs `rm` still 0.0 (R5's protection kept); **v3b (+ a ligature-blind rung, `ffi|ffl|fi|fl|ff` deleted on both
  sides) → 0.9808 / 33 (+0.0090 total)**. The remaining 0.014 to the gate is class (c)+(d): content.

## §8 Findings

**F1 — (b) did what it was for and it is not enough.** Twelve deletions rejected instead of three, the 264-word one
among them; 0.9683 → 0.9718; 61 → 49 runs. Still `fail` at the 0.995 gate. The lever governs the concentrated
deletions; the residual is diffuse — 36 chunks between 0.80 and 0.90, 55 between 0.90 and 0.95 — and a lever that
reaches it (≈0.95) would reject a fifth of the book, at which point the analyst is mostly shipping originals.

**F2 — Half of the residual is the audit's own blindness, now sized.** SYM-076: the ladder strips markdown before it
unescapes, so every window through a Marker-escaped underscore dies (0.0 on the unit probe). Fixed in memory and
measured: +0.0038. A ligature-blind rung: +0.0052 more. **J44** carries both with their numbers; a `--reaudit` of
this held bundle after J44 re-scores it with no GPU (the sidecar is the reference). Neither clears the gate.

**F3 — The other half is content, and it is the model's.** Paragraphs dropped inside chunks that pass at 0.80–0.82;
a numeral rewritten (`100` → `10`) that nothing catches. The program says *"Do NOT summarize, reword, add, or
remove"*; qwen3:8b under `readability` does anyway, at ≈2 % of windows on a technical book. **J45** proposes the
cheapest guard for the numeral class (a per-chunk multiset check of digit-bearing tokens — the model may reflow, it
may not change a number). The paragraph class has three answers, none built here: a stricter program, a stronger
model, or D1 signed (*ship-with-losses-named* vs *audit-must-be-green*, open since S110).

**F4 — J41 paid for itself on its first run.** Every number in F1–F3 came off the manifest's 17.5 KB `chunk_scores`
or was re-derived from the sidecar + shipped body it made possible; the same questions about the first attempt could
only be asked of its counts. The refuter's two amendments (a real negative control; a dead branch removed) landed
before the merge.

**F5 — The lever moved without its pre-move re-measure, and the record says why.** A1's sweep (signed 08-31) took
the 08-30 journal's reference when DDIA converted; the basis is the 09-05 measurement; this run's `chunk_scores` is
the first post-move measurement. Under 0.80 the fixtures did not move (none sit in 0.50–0.80).

**F6 — The re-drop replaced the first attempt in held.** Same sha, no repairs on the occupant → in-place replace, not
park-beside (S65's rule is for repaired occupants). Nothing lost: the first attempt's analysed copy is in `anchor/`.
Named so nobody looks for two held DDIA bundles.

**F7 — The Room's convert station showed 95 % for two hours** (Rab's screenshot): the bar is elapsed ÷ Marker's
promise, capped at 95 until `converted`; with a 0 s promise it sat at the cap while the analyst ran. The honest
counts (slices, chunks n/total, resumed, true cost) are in the numerations panel below it. **J43**, a widget change
on his sign.

**F8 — `--reanalyze` cannot use the sidecar it was built beside.** It refuses any bundle whose anchored copy carries
an `analyst` block, so the re-run cost a re-drop (free this time only because A1 kept the slices). **J42**: a
sidecar-aware `--reanalyze` starting from `marker_body` — the J33 sidecar's designed purpose.

## §10 Known failures / residue

- **DDIA held** at `fc1f068c3a8eeb63`, 0.9718 / 49; Univ 4e held for the same class. **Rab's next decision:** J44
  first (mechanical, then `--reaudit` this bundle for free), then the content half — program / model / D1 / Bench.
- **The numeric-token census** in `rescore_ladder.py` is NOISY (my output-span trimming cut tail numbers) and is not
  quoted; J45's first act is a clean census over `chunk_scores`-aligned pairs.
- **The ligature rung's false-match risk** is named, not measured (a candidate that wrongly turns "fine" into "ne"
  would also match).
- The 08-30 journal's reference is gone (F5); a Marker-only re-run of the Univ 4e PDF could rebuild it (≈17 min).
- The Atlas repo has no remote; Codex quiet since 09-03.

## §15 Registers touched (measured at close)

- OPEN-TASKS.md: annotated J10 (third breath), J32 (lever moved + the ladder blind spot), J34 (second live run,
  0/492), J41 (built + first live read); added **J42, J43, J44, J45**. Struck 0 (the sweep strikes).
- SYMPTOM-INDEX.md: **SYM-076** (the ladder strips markdown before it unescapes; `\_` windows always die). 27 → 28.
- docs/15 §5/§6 (the lever); observability `dispositions.json` (`converter:chunk_scores`, J41's lane).
- `windows-converter/`: analyst.py (J41 + amendments + the lever), analyst_selftest.py (22/22), text_norm untouched.
- coordination: MSG-FAB-0076 + beat.

## §19 close.sh values (2026-09-09T17:4xZ, pin `93c5dc6`, exit 0) — and the judgment on them

- **DIFF** 11 files since the pin · 0 uncommitted · 1 untracked (`.codex/`). **GLASS** clean (`--enforce`). **RUST**
  skipped (no widget change — J43 waits on Rab). **CI** success on `c5709d7` (the lever push; the close commit's own
  run is observed after the push, §20). **LEVERS** none: the moved constant carries its waiver inline; the nine
  deferred constants stay deferred.
- **DOCTOR** 8/8/0 and **CENSUS** 11/11, armed, green. **REGISTER** (armed): +8/−4 lines, STRUCK 0 · **ADDED 4**
  (J42–J45). Justified one by one: J44 and J45 are the two halves of a measured residual (0.009 the audit's, the rest
  the model's) that this session's whole purpose was to size; J42 is the reason the re-run cost a re-drop; J43 is
  Rab's own question from his screenshot. None is governance about governance; each names a defect with its number.
- **DEBT** open SYM 27 → 28 (+1, SYM-076). §A21: a normalisation blind spot that scores a correct edit as total loss
  (0.0 on a 15-word probe) and costs a technical book 0.4 % of its gate is a symptom, and its fix is filed with its
  measurement (J44). Nothing was closed because nothing today's build fixed had a row.
- **MEMORY** durable at `706aab6` (the lockstep write follows the ledger row). **PUSH** ahead 2 → the close pushes.

## §18 Next entry point

1. **MUSTER**; held 8, DDIA at `fc1f068c3a8eeb63` with `chunk_scores`.
2. **J44** (ladder v3: escape-first with `_`, ligature-blind rung; the two probes in this session's scratchpad are
   the measurement, +0.0090 on DDIA) — build, selftest with `\rm` protected, then **`--reaudit fc1f068c3a8eeb63`**:
   no GPU, the sidecar is the reference. Expect ≈0.981 / 33, still `fail`.
3. **Then Rab's content decision:** J45 (numeral guard), a stricter program, or D1. The Bench can repair 33 runs by
   hand if he prefers the book to the policy.
4. J42 (sidecar-aware `--reanalyze`), J43 (the Room bar), J40, the Univ 4e, the nine unlevered constants.

## §20 Post-close: CI observed on the row, the clocks advanced together

**Observed 2026-09-09T17:25:23Z.** GitHub Actions run **34382247980 / num 201** on the ledger-row commit `b9e7c0b`
→ `completed | success` (both jobs; watched from NO-RUN at 17:19Z through `in_progress` to `completed`). Push
`c5709d7..b9e7c0b`, ahead 0. **Lockstep:** the memory library's TIME-STATE reads close `e19e0e0` / row `b9e7c0b`
with this green named, the S118 block written, committed and pushed to the private remote; the cookie phrase stays on
one line (SYM-071); tally unchanged at 85. `close.sh` exit 0 at §19; the ledger row is its own commit; §1 unedited
since `c131720`; no artifact carries a session number other than S118. Rab was pushed the landing at 17:09Z.
