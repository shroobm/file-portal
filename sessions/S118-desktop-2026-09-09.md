# S118 — Desktop — 2026-09-09

⟨claimed: Fable 5.1 · S118 · desktop · opened 2026-09-09T14:2xZ⟩

## §1 Session Intent

Rab, verbatim: *"B"* — his pick on the walkthrough he asked for first (*"Before you do this, walk me through you're
doing if you did the task for this signature. Muster. Decide DDIA: (a) re-run / (b) lever 0.80 / (c) bench+reaudit.
J41 = manifest chunk_scores."*). (b) as walked through: **J41 first** (per-chunk `survival`/`ratio`/`reason` rows
kept in the manifest as `analyst.chunk_scores`), **then the per-chunk survival lever 0.50 → 0.80** on his word with
the 08-30 journal re-measured first, **then the DDIA re-run**, so the first journal that survives success is the
one that measures the new lever.

*(Written before any build; left unedited for the rest of the session — docs/21 §1.)*

## §2 Starting State — the `open.sh` card (2026-09-09T14:2xZ, exit 0; unchanged rows elided)

```
    ledger newest    S117  93c5dc6
    this session     S118  ·  desktop  ·  2026-09-09
[2] SOFT clock ... ✓ 85 (tally == hook)
[3] HARD clock ... ✓ 93c5dc6 (S117) == ledger 93c5dc6, ancestor of HEAD
VERDICT .......... ✓ CLEAN — clocks reconcile
    origin           in sync (fetched)
    pipeline         held 8 · anchor 28 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    open-tasks       123 item(s) · last written 3 days ago  *** not written in 3d — is it still true? ***
    symptoms         75 row(s), 27 open · last written 4 days ago
    relay            138 entries
    widget           15308
    thinkpad         converter=active · vault tip 50896d1 · staging 0 · receipts 13
    --since          93c5dc6
    closeout         sessions/S118-desktop-2026-09-09.md
```

**Pinned `--since 93c5dc6`** (the S117 close). HEAD at open: `a205375`. Three idle days: no commits, no relay entry
(Codex silent since 09-03), the ThinkPad's weekly fixity check passed 09-07 on tip `50896d15`.

## §3 Inherited, all `Historical`

- S117 §8 F2/F3 + §18: DDIA held at `fc1f068c3a8eeb63`, analyst-phase 0.9683 / 61 runs (a 264-word dropped paragraph
  + index pages prosified); J34's first live run 0/492 with its distribution lost (J41). Three built dispositions.
- S115 §7 / OPEN-TASKS J32: on the 08-30 Univ 4e journal (500 hash-matched passed records of 623) the 0.50 guard
  catches chunks 23/78; below 0.80 = 5 (v1) / 6 (v2, after R5) of 500 — "threshold and reject-vs-flag are Rab's".
- The 0.50 lever's waiver (`analyst.py`): *"Rab's word only … moves on a re-measured journal, never by taste"*.
- `--reanalyze` (docs/19 §3.1, `convert_and_ship.py:1951`): refuses a bundle whose only anchored copy carries an
  `analyst` block — "use re-convert instead"; it also never writes the watcher's `.gpu-lock`.

## §4 Promoted before the build (`Observed`, each with its probe)

- **DDIA's anchor** (`fp_paths.root('anchor')`, 28 entries): exactly one copy of the DDIA source, `analyst block:
  True`, verdict `fail` — so **`--reanalyze` would refuse**; the (b) re-run is a **re-drop of the PDF** through the
  watcher (Marker ≈ 504 s again + the analyst), which is also the path that writes `.gpu-lock` and makes the room
  chat yield. The J33 sidecar in `held/fc1f068c3a8eeb63` IS the Marker body a sidecar-aware `--reanalyze` could start
  from — not in this signature; filed at close (J42).
- Card at open: held 8, drop 0, gpu-lock absent, widget 15308 + watcher 26200 up since 09-03, room chat idle.
- **The pre-move re-measure cannot run — the 08-30 journal's reference is gone from this machine** (F1 below). The
  S116 script that scored 500 records on 09-05 now scores **0 of 0** (`Observed`, both my new script and the S116
  one as a control); `.chunk-work/` holds only `fc1f068c3a8eeb63` (DDIA) — **A1 (signed 2026-08-31, `convert_and_
  ship.py:1333-1340`) sweeps the previous book's slices when the next convert starts**, and the DDIA convert did
  exactly that at 00:31Z on 09-06 (*"SLICE CACHE: swept previous book 14c66834bdfeaa2e"*). Both anchored Univ 4e
  copies are analysed (`analyst: True`), the held one predates the sidecar (J33) — no Marker body survives. The
  Univ 4e PDF is still in `drop/done` (30,654,951 bytes, Aug 30): a Marker-only re-run (≈17 min at 0.75 s/page)
  could rebuild it, hash-match permitting — not in this signature. **The lever therefore moves on the 09-05
  measurement** (recorded S115 §7, S116 §9 R2, OPEN-TASKS J34: below 0.50 / 0.80 / 0.90 = 3 / 6 / 14 of 500
  hash-matched passed records under `j32a-v2`), which satisfies the waiver's rule (*"moves on a re-measured
  journal"*) as a measured journal four days old; and **the DDIA re-run's `chunk_scores` is the first post-move
  measurement** — J41 is the reason it will exist.

## §7 Work log

- **14:2xZ** `open.sh` exit 0, pin `93c5dc6`. This file committed BEFORE the J41 fleet was launched (`c131720`).
- **14:3x–14:5xZ J41 built** by a Sonnet builder in a worktree + a Sonnet refuter (`wf_d3f7b9b4-41f`, 16 min, 106 tool
  uses; the worktree spawned at `7c006f2` again — reset first): `_score_row` + `meta["chunk_scores"]` rows `{i, s?,
  r?, x?}` from the live loop, the failed branch and the resume branch; manifest only (both `analyst/done` emits
  untouched — T17 206/206); `converter:chunk_scores` EVIDENCE disposition; docs/15 §7; four selftest cases. Refuter
  **GO_AMENDED**, decoy caught (it read the code for the key names). Cherry-picked `07daaf5`. **Amended here on its
  two findings:** (c)'s negative control deleted a dict key, not the collector — rewritten to the SYM-074 (d)
  technique (blank the one collector line, exec the patched source, watch `chunk_scores` vanish, then the real
  module still carries it); a dead `status == "failed"` arm in the resume branch removed with its reason recorded.
- **The lever moved: `ANALYST_CHUNK_SURVIVAL_MIN` 0.50 → 0.80** on Rab's "B", the waiver header rewritten with the
  basis (the 09-05 measurement) and the reason the pre-move re-measure could not run (§4). Battery after both:
  analyst selftest **22/22** (the J32-B/J34 fixtures all sit outside 0.50–0.80, so no case moved), convert_and_ship
  206/206, analyst_audit 8/8, ruff clean, glass `--since 93c5dc6 --enforce` clean, acceptance 141/141, schema
  registry PASS, CRLF intact. docs/15 §5/§6 and J32's row carry the move.
- **The re-drop (slot 3)** follows the commits below: the PDF copied into `drop/` again; the watcher runs Marker
  (≈504 s) and the analyst under 0.80 with J41 collecting; the held slot parks the incoming beside the existing
  `fc1f068c3a8eeb63` if it fails again (S65's rule), or ships it if the doc gate passes.
