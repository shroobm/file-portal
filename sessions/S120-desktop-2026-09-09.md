# S120 — Desktop — 2026-09-09 — the relay's own bugs, its own numbers, and Codex as partner

⟨claimed: Fable · S120 · 2026-09-09⟩ · occupant Claude Fable 5.1 · pin `--since 82c76da`

## §1 Session Intent (Rab's words, verbatim, left unedited)

Rab, 2026-09-09T19:17:19Z:

> find a way to solve your coordination relay bugs, test out systems with their tripwires, and let codex be your compatriot and partner in this. You serve first, as in, I'm telling you the prompt, and you will have to guide yourself the rest of this, and find out once you solved latency while doing tasks (your main goal is to be able to communicate succintly, while maintaining your own work loads separately, but given enough information so you both operate in a rational form

Rab, 2026-09-09T19:19:04Z (mid-turn addition):

> make sure to capture any important information like problems, event times, etc, find real another set of numerations that is strictly between your communication and processes utilizing coordination relay

**My reading (Inferred from his words; his word corrects it):** (1) the bus — `coordination/relay.md`, the two sidecars, `gate.py`, `coordination/selftest.sh`, the 24/7 watcher, and `close.sh`'s treatment of the shared checkout — has bugs I have already tripped over; find them by measurement, fix each with a tripwire; (2) Codex is a partner on this ticket, not a verifier after the fact — it is asked to co-test and to adopt whatever form results, and it may ask Rab for signatures itself; (3) "solved latency" = the measured post-to-confirm round trip and the time a lane's state sits stale, before and after; (4) "succinctly … own work loads separately … enough information … rational form" = the compact entry form: every entry carries the three-part envelope and, when it needs an ACK, the five slots, each a line, with what the peer needs to act and nothing it does not; (5) the numerations are a NEW register measured only from the bus's own files (the NR-set), never from prose, each row with numerator / denominator / conditions (docs/34); (6) problems and event times go in this record as they happen (§7). Anything semantic — a length cap as a refusal, a policy on the 13 August entries, adoption of the form by Codex's lane as law — stays Rab's signature; this session builds the mechanical halves and measures.

## §2 Starting State — the `open.sh` card, verbatim (2026-09-09T19:25:17Z)

```
════════ MUSTER · OPEN · 2026-09-09T19:25:17Z ════════
[0] IDENTITY
    ledger newest    S119  82c76da
    this session     S120  ·  desktop  ·  2026-09-09
    advisory         S120 unnamed in the inherited tree
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-09T19:25:17Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 85 (tally == hook)
[3a] LEDGER ORDER  ✓ 102 Desktop-lane rows ascend (S16 → S119)
[3b] LEDGER PARSE  ✓ 102/127 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 4 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79
[3] HARD clock ... ✓ 82c76da (S119) == ledger 82c76da, ancestor of HEAD
    WORKING TREE . 5 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 8 · anchor 29 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    events           311 line(s)
    open-tasks       131 item(s) · last written 27 minutes ago
    error-bin        62 row(s) — read the CLASS table before you probe · last written 27 minutes ago
    symptoms         76 row(s), 28 open · last written 2 hours ago
    relay            145 entries · run `gate.py status` for the board
    widget           15308
    python procs     7
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 70c60e6
    thinkpad         converter=active · vault tip 50896d1 · staging 0 · receipts 13
[3] PIN
    --since          82c76da
    closeout         sessions/S120-desktop-2026-09-09.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

The 5 uncommitted: Codex's own bytes (`coordination/ack-codex.json`, its MSG-CDX-0048 hunk in `coordination/relay.md`, `sessions/S119-s118-verification-codex.md`, untouched by its word in MSG-CDX-0048), `.codex/`, and this session's instrument (§7). The board at 19:28Z: both lanes `idle`, Fable's beat 31 min old, Codex's 30 min, both on gate `f34009c8`, Codex's occupant `OpenAI Codex (GPT-5.6)`, no escalation open. The 24/7 relay watch (Monitor `btn2fwpdb`, armed S119) `running` at 19:31Z (`TaskOutput`, status running); no STOP file.

## §3 The pin

`--since 82c76da` (the S119 closing SHA; the ledger row is `db0ab6d`, the last S119 commit `5940249`).

## §4 Promoted at open — the bus measured before anything was touched (all `Observed`, times UTC)

| when | probe | reading |
|---|---|---|
| 19:2x | `.claude/skills/relay-gate/selftest.py` | 148/148 (before compaction; not re-run since) |
| 19:2x | `bash coordination/selftest.sh` | 9/11 — T2 (header form + three parts) and T3 (concordance amendment) FAIL |
| 19:26 | `coordination/relay_numerations.py` first run | the NR baseline, §5 |
| 19:27 | `for rule in …; tr -d '\r' < relay.md \| grep -ci -- "$rule"` | 3 / 2 / 2 / 2 matching lines — every amendment string IS present |
| 19:27 | `( set -o pipefail; tr … \| grep -qi -- 'Must-quote'; echo $? )` | **141** with pipefail, 0 without, 0 in count form — **T3's red is `grep -q` closing the pipe under `set -o pipefail`: `tr` dies of SIGPIPE and the pipeline reports 141, so a present string reads absent** |
| 19:27 | header census, `tr -d '\r' \| grep -cE` | dated headers 145 · match the selftest's `HDR` (no suffix) 13 · carry `⟨msg: MSG-` 126 · match neither 6 (all 2026-08-24, addressee `⟨to: Fable, and the record⟩`) — **T2's "every header well-formed" fails because the selftest's regex predates the gate's `· ⟨msg: …⟩` suffix**, and separately 81 of 126 canonical entries lack a slot |
| 19:28 | `gate.py status` | both idle; Fable `ticket=LEDGER-DIAG-S114`, Codex `ticket=S118-VERIFY-CDX` |
| 19:29 | EOL census (`eol_census.py`) | `relay.md` **5,252 CRLF lines + 1,117 bare-LF lines** (459,994 bytes); both sidecars pure LF; `git config core.autocrlf` = `true`; `.gitattributes` has no rule for `*.md`/`ack-*.json`; `git add` warns "LF will be replaced by CRLF" on both of Codex's files |
| 19:30 | `gate.py:504-506` | `_append_relay_locked` opens with `newline=""` and writes `"\n" + entry` — **the appended bytes are the body file's bytes verbatim**; a lane whose body file is LF appends LF into a CRLF log (both lanes do) |
| 19:30 | `gate.py:148-155` | `digest()` canonicalises line endings — law 4 (re-digest) is EOL-blind, so the mixed bytes do not break delivery; they break `git add` (S116 F8's +405/−404 shape) |
| 19:29 | Fable's sidecar `escalations[]` | 1 record: MSG-FAB-0009, T-005, 2026-08-24T08:21Z, **resolved 18:16Z — Rab SIGNED CR-CDX-0002**: "three-part envelope on every relay entry, the five slots as the inner transaction for requires_ack entries only … and gate.py escalate is to be brought into compliance with it before the next escalation" — the envelope is his signed law; enforcing it at `post` is the mechanical half he ordered |
| 19:31 | `git status --short` | `M ack-codex.json` (44 changed lines) · `M relay.md` (+4: the MSG-CDX-0048 header, 18:58Z) · `?? sessions/S119-s118-verification-codex.md` — Codex's, uncommitted since 18:58Z |

## §5 The relay numerations — baseline (NR-set, `coordination/relay_numerations.py`, 2026-09-09T19:26Z)

| NR | what | Fable | Codex | numerator / denominator / conditions |
|---|---|---|---|---|
| 01 | entries | 78 | 48 | canonical `## <UTC> · ⟨from⟩ → ⟨to⟩ · ⟨msg⟩` headers; 145 dated headers in all (the card's count), 126 canonical, 19 legacy |
| 02 | gated appends | 71/78 | 47/48 | headers whose id has a `sent` record in the lane's own sidecar; ungated: MSG-FAB-0030 and 0044…0049 (7), MSG-CDX-0027 |
| 03 | no-ack notices | 23/70 | 11/47 | `sent` rows with `requires_ack` false — invisible to the peer's `inbox` |
| 04 | ack latency, sent by → confirmed by peer | **median 7 min · p90 477 · max 8,377 (MSG-FAB-0068)** n 47 | **median 6 · p90 74 · max 8,456 (MSG-CDX-0044)** n 23 | `confirmed_utc − utc`, minute stamps, confirmed ack-required messages only |
| 05 | ack debt | 0 owed by Codex | **13 owed by Fable, oldest MSG-CDX-0029 at 232.6 h** | ack-required, unconfirmed (the August thirteen — Rab's word, S119) |
| 06 | envelope conformance | **30/78 = 38.5 %, last 10: 3/10** | **15/48 = 31.2 %, last 10: 1/10** | entries carrying all of `**RECAP` / `**FOR RAB` / `**SUGGESTED PROMPT` |
| 07 | entry length | **median 536 words · p90 949** · last 10: 456 | median 293 · p90 530 · last 10: 200 | whitespace words per body |
| 08 | restatement length | median 123 words · min 31 · n 24 | median 34 · min 16 · n 47 | words per confirmation restatement |
| 09 | beat age | 28 min | 27 min | `now − beat.utc`; cadence UNREAD (only the latest beat is kept) |
| 10 | escalations | 1 total · 0 open | 0 | sidecar `escalations[]` |
| 11 | state | idle · LEDGER-DIAG-S114 · sent 70 · confirmed 24 | idle · S118-VERIFY-CDX · sent 47 · confirmed 47 | the sidecar's own fields |
| 12 | peer bytes unstaged in the shared checkout | 2 files (+ the MSG-CDX-0048 hunk in relay.md, which the first version of NR-12 could not see) | | `git status` paths naming the peer |
| 13 | watcher signals logged | UNREAD | | the watcher writes no log; only the Monitor's output file holds its lines, and NEW-ENTRY lines carry no UTC |
| 14 | handler turnaround | UNREAD | | needs NR-13 |

Two readings out of this table decide the session's shape: **the median round trip is 6–7 minutes when both lanes are awake — latency is a tail problem (p90 8 hours for Fable's sends, one week at the max) made of sessions that were not open, not of the tool** — and **succinctness is a Fable problem first: my median entry is 536 words against Codex's 293, and my restatements run 123 words where Codex's run 34.**

## §6 The bugs, named (each `Observed` above unless tagged)

| # | bug | where | mechanism | remedy (mechanical) | tripwire |
|---|---|---|---|---|---|
| B1 | T3 reads a present amendment as absent | `coordination/selftest.sh` | `grep -q` + `pipefail` → SIGPIPE 141 | count form, never `-q` in a pipeline under pipefail | a fixture where the string is present at the top of a long file must PASS; absent must FAIL |
| B2 | T2 rejects every gated header | `selftest.sh` `HDR` | regex predates the `· ⟨msg: …⟩` suffix (13/145 match) | accept the gate's header; grandfather the 6 `and the record` headers by id, printed as a count | the S120 fixture with a `⟨msg:⟩` header passes; a header with a local-time stamp still fails |
| B3 | 81/126 entries lack a slot Rab signed as mandatory | both lanes' bodies | nothing checks at write time; the selftest checks the whole history, which append-only can never repair | `gate.py post` refuses a body missing `**RECAP.**` / `**FOR RAB.**` / `**SUGGESTED PROMPT**` (names the missing slot); the selftest checks entries newer than the enforcement cutoff and counts the legacy | positive control: a conforming body posts; negative: each missing slot refused, exit 1, nothing appended |
| B4 | `watch`/`inbox` blind to ungated appends | `gate.py cmd_watch`, `cmd_inbox` | both read only `sent` records | a third signal: a relay header from the peer with no `sent` record → `UNGATED-ENTRY <id>` | fixture: an appended header without a sidecar row fires once |
| B5 | a lane's state sits `blocked-on-ack` after its messages are confirmed until it runs `check` | `cmd_check` is the only settler | latency of the board, not of delivery | `status` renders the derived settle; `beat` settles under the Guard B clause (never over `blocked-on-rab`) | the SYM-042-family tripwire: settle must not downgrade `blocked-on-rab` |
| B6 | mixed line endings in the append-only log | `_append_relay_locked` | body bytes verbatim; `autocrlf=true`; no `.gitattributes` rule | append in the log's dominant EOL | fixture: CRLF log + LF body → appended bytes CRLF; LF log + CRLF body → LF |
| B7 | a Fable close cannot commit its own relay append without absorbing the peer's uncommitted hunk | `close.sh` DIFF + `git add` | one file, two writers, one index | `gate.py stage --as <lane>`: index blob = HEAD's bytes + this lane's own new entries, refused unless HEAD's bytes are a prefix of the working copy; `close.sh` DIFF attributes dirty files to their writer | staging with a peer hunk present leaves the peer hunk out of the index and in the tree; a non-prefix working copy is refused |
| B8 | the watcher has no tripwire, no log, no UTC on its signals, and dies with the session | `coordination/private/relay-watch.sh` (gitignored) | born in S119 as a Monitor script | track it as `coordination/relay_watch.sh --as <lane>` (path-relative, either lane), every signal line UTC-stamped and appended to `coordination/private/relay-watch.log`; a selftest with fixtures | fixtures for NEW-ENTRY / INBOX / BEAT / ESCALATION / UNREAD / STOPPED, and a positive control |
| B9 | ack debt: 13 Codex entries unconfirmed since August | Fable's lane | Rab's word pending (S119) | none here — a decision | — |
| B10 | the watcher can signal a phantom INBOX id | `relay-watch.sh` (`grep -oE "MSG-…"` over the whole `inbox` line) | its arming line at 18:39:45Z listed `MSG-FAB-0055` in Fable's inbox: that id sits in MSG-CDX-0033's **subject** ("RED: MSG-FAB-0055 digest mismatch"); `cmd_inbox` itself is right (19:41Z, `Observed`) | the tracked watcher reads the id from the first column only | fixture: an inbox subject naming another id yields exactly one INBOX signal |

## §7 Work log (UTC; problems and event times, per Rab's addition)

- **19:17:19** Rab's commission. **19:19:04** the addition. **19:2x** `open.sh` clean (card in §2); selftests run.
- **19:2x** `coordination/relay_numerations.py` drafted **before this record existed** — ERR-060's shape (the record must precede the act). Owned here: the record's commit precedes the instrument's commit, but the authoring did not. Filed as ERR-2026-09-09-063 (ORDERING) at close.
- **19:26** first NR run. Its NR-01 line said "+58 pre-gate headers": that was every `## ` heading minus the canonical ones, mislabeled as dated headers — a DENOMINATOR slip caught by the card's own `145` before it reached any claim; the instrument now counts `^## 20` as the card does (126 canonical + 19 legacy).
- **19:27–19:31** B1–B8 measured (§4).
- **19:31** the watch Monitor confirmed `running`.
- **19:34** record committed `17f4509` (only the session file staged; Codex's bytes untouched). **19:36** instrument committed `280cfc7` (ruff clean after 5 findings: four `l` names, one bare f-string).
- **19:37:21** MSG-FAB-0079 posted through the gate (`--ticket S120-RELAY-COTEST`, digest `sha256:1291304feb1d…`, Fable `blocked-on-ack`); beat at 19:37:21. The body is the compact form: envelope + five slots, one line each, 15 lines, 3,173 bytes, converted to CRLF before the post so the append matches the log's dominant EOL (the gate's own `"\n"` separator is one bare LF).
- **19:38** fleet launched: `wf_93e0a4a6-960` (task `wmvxxo6lq`), five Sonnet builders in worktrees + five Sonnet refuters, decoy per lane, shared GROUND at the scratchpad `S120-fleet-ground.md`.
- **19:40** the watcher's output read back: last signal 18:58Z (MSG-CDX-0048); nothing since (my own post is excluded by design). Its arming line named `MSG-FAB-0055` in Fable's inbox → B10 (§6). **19:41** `docs/56-relay-numerations/README.md` written (the NR register with the baseline).

## §8 Findings

(written at close)

## §9 The plan from here

1. Commit this record; commit the instrument.
2. Post **MSG-FAB-0079** to Codex through the gate, ticketed, in the compact form it proposes (envelope + five slots, each one line): the measured bugs, the form, the co-test asks, and the two asks it may take to Rab itself. Its confirmation time is NR-04's first "after" sample.
3. Sonnet fleet (builders in worktrees + one refuter each, decoys, docs/47): L1 `selftest.sh` (B1, B2, B3's history clause) · L2 `gate.py` append path (B3 post check, B6, B7 `stage`) · L3 `close.sh` attribution (B7) · L4 the tracked watcher + selftest (B8) · L5 `gate.py` read path (B4, B5).
4. Cherry-pick, run every suite myself, re-run the NR-set, re-arm the watcher on the tracked script (the gap timed), answer Codex, close in lockstep.

## §10 Known failures / residue

(written at close)

## §15 Registers

(written at close)

## §18 Next entry point

(written at close)
