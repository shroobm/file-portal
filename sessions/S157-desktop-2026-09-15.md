# S157 — Desktop — 2026-09-15 — THE OPEN SITTING: carry over, do not close, the lane's own tasks while his words wait (Claude Opus 5's fourth sitting in the Fable lane; the autonomy experiment's twelfth)

## §1 Session Intent (Rab's words, verbatim, left unedited for the rest of the session)

Rab, in chat, 2026-09-15 ~05:5xZ, after S156's close report:

> "You need to compact, but carry over, then don't close. Just be persistent, if you have to wait, do another task. everything is available to you, you have permissions to access the information you need. record all receipts"

**The reading (the echo, in the record before the act; on the Desk at E0):** *"compact, but carry over"* — the context will be compacted; before it is, the CARRY-OVER is written so nothing is lost: this record (the record is the memory — `persistent-partial-closes`) and a carry-over file in the private `sittings/S157/CARRY-OVER.md` (the state, the standing words, every open item with its command form, the receipts' tiling point, the traps). *"then don't close"* — this sitting does NOT run the close chain when its tasks run out; it stays open, episodes with partial closeouts (receipts, Desk lines, commits) as before, and the close comes on his word or when he stops the experiment. *"if you have to wait, do another task"* — his three words (`reanalyse Valentine`, D1, the experiment's end) wait; the lane's own items run meanwhile, from the register and the previous sittings' residue. *"everything is available to you, you have permissions to access the information you need"* — read access to the machine's files, repos and logs as the work needs; the standing constraints stand (the card is his; nothing shipped; the vault untouched; scripts never public). *"record all receipts"* — per episode, tiled onto S156's whole receipt's end (2026-09-15T05:42:23.471Z).

## §2 Starting State — `open.sh`, the S157 card (verbatim)

```
════════ MUSTER · OPEN · 2026-09-15T06:02:35Z ════════
[0] IDENTITY
    ledger newest    S156  d0e193b
    this session     S157  ·  desktop  ·  2026-09-15
    session marker   coordination/private/session.current = S157 (guard_record / guard_git read it)
    advisory         S157 unnamed in the inherited tree
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-15T06:02:36Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 86 (tally == hook)
[3a] LEDGER ORDER  ✓ 139 Desktop-lane rows ascend (S16 → S156)
[3b] LEDGER PARSE  ✓ 139/165 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ d0e193b (S156) == ledger d0e193b, ancestor of HEAD
    WORKING TREE . 2 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 8 · anchor 25 · pending 0 · drop 0
    intake           waiting 0 · oldest 0s · active null · written 3s ago by pid 18844
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    events           391 line(s)
    open-tasks       150 item(s) · last written 41 minutes ago
    error-bin        105 row(s) — read the CLASS table before you probe · last written 2 days ago
    symptoms         132 row(s), 43 open · last written 11 hours ago
    relay            208 entries · run `gate.py status` for the board
    widget           27548
    widget autostart task 'File Portal widget (J57)' Ready — AtLogOn · auto-logon ON (Bndit)
    task             'File Portal desk (relay)' Ready LASTRUN=2026-09-13T00:59Z RESULT=4294967295
    task             'File Portal relay watch (Fable)' Ready LASTRUN=2026-09-12T19:41Z RESULT=0
    task             'File Portal tracker' Ready LASTRUN=1999-11-30T05:00Z RESULT=267011
    task             'File Portal desk watch' Ready LASTRUN=1999-11-30T05:00Z RESULT=267011
    python procs     6
    ollama           2
    llama-server     1 — the NAME does not identify it (ollama's engine is also llama-server.exe); the parent PID decides, never the name (S81)
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            7 note(s) · tip f489189
    thinkpad         UNREAD — host did not answer; NOT a statement that it is down or clean
[3] PIN
    --since          d0e193b
    closeout         sessions/S157-desktop-2026-09-15.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

The seat (`query user`, 05:5xZ): console session Active (his). The card: 8,073 MiB of 10,240 at 27 % — his own applications (read, not touched); `.gpu-lock` absent; no model of mine runs this sitting unless his word names the card. The smoke runs ALONE in the foreground after this record's commit.

## §3 The pin

`--since d0e193b` — S156's closing SHA (row a67e9ed; CI green run 34933858623; the post-close a25bb7e and c6211ae, CI green runs 34934241233 / 34934572207).

## §9 The plan (each an episode with receipts and a Desk line; NO close at the end — the sitting stays open)

- **E0 — this record; the carry-over file; the tracker (`S157-receipt`); the smoke alone; the Desk line with the reading.**
- **E1 — the p.175 split rule (S155 E2's route, the lane's):** a second header-shaped row inside a table's body splits the table; a prose line beneath the split table's last row carrying the table's column count of numbers re-joins as a row; under `grid_invariant` with a new clause and its negatives; measured against p200-t3 on both copies (the truth in hand: 0.149 → ?).
- **E2 — the census reads `kind` from a reading:** `table_census.py --vision <path>`: a table the reading calls a figure is counted as a figure, not a table (Valentine: 81 → 80 tables, one figure named).
- **E3 — the p204-t1 note (S155 §10):** the pipeline's copy reads TEDS 0.550 where raw Marker reads 0.786 — which row the copy lost, and where in the pipeline (the layer? the acceptor? the analyst's chunking?).
- **E4 onward — the register's open rows the lane owns** (mechanical, no card, no ship), one per episode, chosen at the time from `OPEN-TASKS.md` with the residue named; and whatever word of his arrives on the Desk or in chat (the Desk is watched).
- **Held for his sentence:** `reanalyse Valentine` (the reading into the live copies); D1; the experiment's end. The card: his.


## E0 — the record, the carry-over, the tracker, the smoke (05:5x–06:0xZ)

- The record committed first (9c87f72). **The carry-over** for the compaction written and pushed: the private `sittings/S157/CARRY-OVER.md` (5599e73) — who and where, the state at the carry-over, his three open words with their exact steps, the lane's task list with command forms, the protocol per episode, the traps paid this week, the close's shape if his word comes. The tracker `S157-receipt` (pid 23948, 06:03:37Z). The smoke, alone: **68/68**, 74 s. The Desk line carries the reading. The card at the open: 8,073 MiB at 27 % — his own applications; nothing of mine.

## E1 — the p.175 split rule (S155 E2's route): a second stacked heading inside a body is a second table (06:1x–06:4xZ)

- **The copy's shape, read first** (Observed, the anchor copy's body lines 2427–2444): the ANOVA table holds its own rows, then the coefficients' stacked heading as two body rows (`| | Standard | | P- | Lower | Upper …` over `| Coefficients | Error | t Stat | value | 95% …`), the Intercept row, the label pieces `log` / `/manufacturin`; then a page footer line, then `indev)` and `3.58 0.36 10.03 0.00 2.86 4.29 2.86 4.29` as prose — the same fusion Marker's HTML mode showed in S155. The held copy (lines 2437–2453): the heading in ONE row with `<br>` inside its cells, `indev)` ABOVE the table, the last row's numbers scattered over two prose lines (`0.00 2.86 4.29 2.86 4.29` … `3.58 0.36`) with `10.03` lost.
- **The rule** (Observed, public `table_geometry.py`; `sittings/S157/e1_split_patch.py` + `e1_br_patch.py`): a SPLIT pass before the per-table proposals — `_heading_pair` (two rows: the first cell empty, no plain number — a `95%` heading is not a number, which the first cut refused —, a data row above, the pair's own words half or more the book's words: S152 E3's test, on the pair's cells, not the joined ones — the first cut tested the joined cells and `standarderror` is no word), `_heading_row_br` (one row, two or more cells with `<br>`), `_tail_rejoin` (label-only rows, blank lines, one short digit-free non-table line, a lone short line, a numbers line of exactly columns − 1 numeric tokens), `propose_splits`, `apply_split`, `split_invariant` (the exact re-partition; the re-joined row equal to the label pieces + the lone word + the prose numbers), `split_pass`; `geometry_pass` runs it first and records `splits` / `splits_refused`. The invariant's first cut carried an absolute index where a relative one was needed (refused the true split; fixed).
- **The tripwires** (Observed): section [13] — the anchor shape (one split at the pair, the fold as the header, the tail re-joined, the prose consumed, the footer kept); the record; the negatives (a number in the pair, no data row above, a prose line with the wrong token count leaves the tail and the prose, a mutated re-joined number refused by the invariant); the held shape (a `<br>` row, pair 1, the scattered tail NOT re-joined, both prose lines kept); idempotence; no split on a healthy table or on the rails fixture — **155/155**; `analyst_selftest` 40/40; `edit_whitelist_selftest` 53/53; ruff at CI's pin clean (two findings fixed: an unused variable, an ambiguous name).
- **The measure** (Observed; fresh dry runs of both copies, `--vision none`, under the private `measurement/evidence/s157-valentine-after-split/`; the 14 truths scored, S154's output as the BEFORE; the JSONs under `sittings/S157/e1/`):

| copy | splits | p200-t3 (the coefficients, p.175) | p200-t2 (the ANOVA) | other 12 | mean TEDS |
|---|---|---|---|---|---|
| anchor | 1 (two-row pair; the tail re-joined) | **0.149 → 0.996** — header 0/8 → 8/8; 18 unmatched → 17 correct, 1 wrong (the label `log /manufacturin indev)` for `log (manufacturing index)`) | 0.226 → 0.406 | unchanged | 0.672 → 0.745 |
| held | 1 (a `<br>` row; the tail left) | **0.247 → 0.742** — header 0/8 → 7/8; 8 missed (the scattered last row, `10.03` lost by the OCR) | 0.308 → 0.482 | unchanged | 0.735 → 0.783 |

  The census: 82 tables each (81 + the coefficients table).
- **docs/15 §12.4.11**; the register row `converter/lost-table-p175` struck with the residue (the label's OCR — a lexicon route for a row label, later; a scattered tail stays scattered).
- **Not this episode's:** Marker's `--use_llm` route (his); the label's word route; the live copies.


## E2 — the census reads `kind` from a reading: a block the panel called a figure is counted as one (06:1x–06:3xZ)

- **The change** (Observed, private `measurement/probes/table_census.py`): `--vision <reading.json>` — the file must be `vision-reading/1` (anything else refuses aloud, exit 1, the shipper's rule); every pipe block is matched to the reading's entry the layer's way (`table_geometry._vision_entry`: every anchor snippet in the block's own text, case-folded); a block whose entry says `kind: figure` is counted under `figures_by_reading`, and `tables` leaves it out (`table_blocks` keeps the raw count); each row carries `reading_matched` / `reading_kind` / `reading_page` / `reading_how`; the summary names the reading's entries, how many a block matched, and the anchors of the entries nothing matched.
- **A finding on the way** (Observed): the first cut matched 3 of the reading's 4 entries on both copies — p.97's anchors (`Quality of Self-Side`, `Experienced and`) sit in the table's CAPTION, and on an analysed copy the layer has already lifted that caption out of the pipe block onto the prose line above it, so the block alone no longer holds the anchor. The census now tries the block once more with the nearest non-blank line above it (up to three lines up) and says so in `reading_how` (`block + the caption line above (1119)`); 4 of 4 matched after.
- **The measure** (Observed; `evidence/s157-valentine-after-split/{anchor1,held}/census-vision/`, the E1 split output as the input; the S156 reading `valentine.vision.json`):

| copy | table_blocks | tables | figures_by_reading | the figure block | reading entries matched |
|---|---|---|---|---|---|
| anchor | 82 | **81** | **1** | body line 4519, p.367 — the "Lipstick on a Pig / Reputation Killer" quadrant under *Analyst's Communication Quality*, a 2×2 exhibit Marker read as a table; the panel's three readers called it a figure | 4 of 4 (p.97 via the lifted caption) |
| held | 82 | **81** | **1** | body line 4540, p.367 (the same block) | 4 of 4 |

  Negative controls: no `--vision` — 82 blocks, 82 tables, 0 figures, `reading: null` (the census unchanged); a file whose `format` is not `vision-reading/1` — refused, exit 1, nothing written.
- **What this is and is not:** the census REPORTS the reading's call; it does not judge the block (the layer's own figure step already skips repairs on such a block, S156 E1). The number the plan named (81 → 80) was written before E1 found the coefficients table: it is 82 → 81 + 1 now.
- **Residue:** a reading whose anchors were all inside a lifted caption is matched only when that caption sits within three lines above the block — a wider gap (a page footer between) would read unmatched, and the summary would say so by name.

## E3 — the p204-t1 note: nothing was lost, the copy carries EXTRA columns — the trim pass (06:21–06:5xZ; the receipts' windows are the truth for every heading's times)

- **The note re-read** (Observed, the S155 scorings `s155-valentine-html-tables/scores-html.json` and S157 E1's `sittings/S157/e1/*.json`): p204-t1 (`Regression Statistics`, truth 5 rows × 2 columns, 12 cells) — raw Marker's HTML copy 18 cells (6 × 3), the anchor copy 30 cells (6 × 5), the held copy 18 (6 × 3); **recall 1.000 on all three**, precision 0.667 / 0.400 / 0.667. No row is lost; the S155 note ("find the lost row") read the gap wrong. The copies carry trailing columns nothing fills — Marker's padding (a probe on both copies: fifteen tables each with one to seven trailing all-empty columns, none with a leading one, none with a health issue; six of them among the fourteen truths, and every truth says the columns are not on the page).
- **The rule** (Observed, public `table_geometry.py`; `sittings/S157/e3_trim_patch.py`): the TRIM pass — `propose_trims` (a table's trailing columns whose header cell and every body cell are blank, down to `TRIM_MIN_COLS = 2`; never a leading column — pp.97/132/133's rail column; never a table with a health issue or ragged rows; a glyph is not blank, S78 §10.5's 418 dots), `apply_trim` (every row re-rendered without its last k cells; the delimiter keeps its first columns' segments), `trim_invariant` (the exact cut: the same rows, each after row the before row cut to the kept count, every removed cell blank, the delimiter's kept segments unchanged), `trim_pass`; `geometry_pass` runs it after the split (so the ANOVA born of p.175's split is trimmed too, 9 → 7) and before the proposals; the record gains `trims` / `trims_refused`.
- **The tripwires** (Observed): section [14] — the p204-t1 shape (one trim, 5 → 2; the delimiter's two segments kept; the prose around untouched); the record; idempotence; the negatives (a `•` in the column, a leading empty column, ragged rows, a health issue — no trim; the two-column floor; a kept cell changed, a delimiter segment changed, a removed cell that was not blank — refused by the invariant); the split's ANOVA trimmed after the split; nothing on the healthy fixtures — **169/169**; `analyst_selftest` 40/40; `convert_and_ship_selftest` 254/254; ruff at CI's pin clean (two findings fixed: an unused variable, an ambiguous name).
- **The measure** (Observed; fresh dry runs of both copies, `--vision none`, under the private `measurement/evidence/s157-valentine-after-trim/`; the 14 truths scored, E1's output as the BEFORE; the JSONs under `sittings/S157/e3/`): 15 trims per copy, none refused, 0 refusals in the ordinary pass.

| copy | mean TEDS | up / same / down | p204-t1 | the other movers | recall |
|---|---|---|---|---|---|
| anchor | **0.745 → 0.818** | 6 / 8 / 0 | **0.550 → 1.000** (30 cells → 12) | p206-t1 0.688 → 0.880 · p264-t1 0.559 → 0.699 · the ANOVAs p200/p204/p206 0.406 / 0.511 / 0.606 → 0.501 / 0.579 / 0.686 | unchanged on all 14 |
| held | **0.783 → 0.856** | 6 / 8 / 0 | **0.786 → 1.000** (18 → 12) | p200-t1 0.647 → 1.000 · p206-t1 0.688 → 0.880 · the ANOVAs 0.482 / 0.606 / 0.606 → 0.595 / 0.686 / 0.686 | unchanged on all 14 |

  The census after (with the reading): 82 blocks each, 81 tables + 1 figure, all renderable, no issues; filled cells unchanged (anchor 2,688; held 2,596); empty cells anchor 610 → 411, held 602 → 397.
- **docs/15 §12.4.12.** No register row named this (the S155 §10 note was the pointer); nothing to strike.
- **Residue:** the anchor's p200-t1 stays 0.571 — a different fusion: Marker dropped its `Regression Statistics` caption and fused the ANOVA's title row (`| ANOVA | | |`) and half its stacked heading (`| | | Significance |`) into the statistics table's tail, so the last column is not empty; a split at a title-shaped body row would answer it — later. The held copy's p200-t3 (0.742, the scattered tail) as before.
- **Not this episode's:** the live copies (his word); the S155 §10 note itself (the record is append-only; this section is its correction).


## E4 — the register worked down: eighteen rows struck, each re-probed at HEAD first (06:29–06:5xZ)

- **The sweep** (Observed): `OPEN-TASKS.md` held seventeen open rows whose own status cell already read DONE / BUILT / SUPERSEDED (the 2026-09-04 verifier at `bde007d` wrote "Strike." on most of them; nobody struck). The register's own convention (2026-08-27) is that a strike is made by the striking session against the working tree, never taken from a prior report — so each claim was re-probed at HEAD `6644ecc` before the strike (`sittings/S157/e4_register_strike.py`; every probe appended to the row's last cell under an `Observed 2026-09-15` tag; nothing removed):

| row | verdict | the probe at HEAD |
|---|---|---|
| A4, D2 | DONE / COLLECTED | `0fbb6e3` an ancestor of HEAD; docs/50:320 `stale` |
| A20 | DONE | `main.js:561/665/676` render `last pipeline event: …` |
| A22 | DONE — KEPT | `relay.md` carries 94 `⟨from: Codex⟩` entries |
| A43 | DONE | the library's origin `shroobm/PROJECT-FILE-PORTAL-2026-2027`, `main...origin/main` |
| B1 | DONE | `observability/acceptance.py` → PASS 171/171 |
| B4, J39 | DONE | `ci.yml:92-94` `Lint (windows-converter)` → `ruff check .`, no `continue-on-error`; ruff clean locally twice this sitting; CI green on this HEAD (run 34937129934, num 366) |
| B8 | DONE in substance | `close.sh:107` runs `glass_detector.py --since "$PIN" --enforce`, exits 1 on an unsigned glitch |
| B9 | DONE, residual named | `watch_and_convert.py:223` writes `writer_pid`; `line.rs:72/84/213` reads it at boot (pid, not parent; a writer silent > 300 s invisible) |
| F5 | SUPERSEDED | A27 still open, the same ask |
| F7 | DONE | `settings.json:3` autoMemoryDirectory → the one library |
| J29 | DONE | `8aa8936`, `d9bfaaa` ancestors; SYM-067 FIXED |
| J31 | BUILT + EXERCISED | `convert_and_ship.py:2280` `reaudit()`; `d3f69e8` an ancestor; exercised on his word (S116) |
| J32 | BUILT, the lever on his word | `text_norm.py`; `analyst.py:77` `ANALYST_CHUNK_SURVIVAL_MIN = 0.80` with the waiver naming his two words |
| J33 | BUILT + LIVE | `convert_and_ship.py:799` `_write_marker_body_safe`; `exporter.py:91` `SHIP_MARKER_BODY_TO_VAULT = False` |
| J41 | BUILT + FIRST LIVE READ | `07daaf5`, `aa93818` ancestors; `analyst.py:443` `_score_row`; the disposition `converter:chunk_scores`; the Valentine manifest predates it, the DDIA re-run (492 rows) is its live read |

- **Two symptom rows flipped** (Observed, `SYMPTOM-INDEX.md`, the register's own "strike; flip" instruction on B8/B9): SYM-046 → **FIXED** `c9c0cf3` (the close's own `--since <pin> --enforce` run is the mechanical guard the row asked for); SYM-047 → **FIXED** `f045a66` with the residual named (pid, not parent; a silent writer). The old status text kept after "Was:".
- **An eighteenth row closed as-is** (Observed): `analyst/stray-glyph-outside-a-matrix` asked for the page to be read once — it was, by the S154 E6 panel (`sittings/S154/e6/readers.json`): three readers on the rendered page 367 call the block a FIGURE (a 2×2 quadrant of prose cells under a STEP 1/2/3 diagram; HIGH / LOW are rotated axis labels Marker read as `H<br>G<br>H` and `L` / `L<br>O`), the reading carries `kind: figure`, the layer gives a figure no repair (S156 E1), the census counts it as one (E2). The `·` is a stray of the axis label and stays as the OCR wrote it; no one-glyph rule.
- **The count** (Observed, rows matching an id in this file, before → after): 144 → 126 open. Numerator: un-struck rows whose first cell is an id (`A1`, `J39`, `analyst/…`); denominator: this file only — not the five-surface census.
- **Not struck, and why:** B17/B18 (the A-B-A run's idle-card condition unrecorded — Unknown, as on 2026-08-27); J10 (C0 half-moved); J45/J49 (BUILT parts, but the guard J45 names and the greedy run J49 owes are not built or need his card); B2 (`.agents/` is gitignored, not deleted — the row's other outcome, his call).


## E6 — J49's mechanical half: the readability pass's sampler named in the record (the values stay his) (06:5x–07:0xZ; written before E5's section — E5's skeptic fleet was still reading)

- **What J49 found** (Historical, S119 R2, Verified then): the local analyst request carries no `temperature` and no `seed` — `options: {num_ctx}` plus the S146 generation bound — so qwen3:8b decodes the readability pass at its Modelfile's thinking recipe (temperature 0.6, top_p 0.95, top_k 20; `ollama show qwen3:8b`, ollama 0.33.2), and the manifest says nothing about it. docs/34: every survival score in `chunk_scores` was decoded under conditions the record did not name. The ticket's build was three lines: pin the sampler (temperature 0 + a seed — **the values are Rab's word, the row says so**) and write the effective sampler into the analyst meta.
- **This episode does the recording half only** (Observed, public `windows-converter/analyst.py`; `sittings/S157/e6_sampler_patch.py`): `ANALYST_SAMPLER = {"temperature": None, "seed": None}` with the lever-waiver (None = the option is not sent; today's request byte for byte); `_request_options(num_predict)` builds the local request's options in one place — the per-call bound and the grid program's own 0.0 keep precedence over the lever, then the lever's temperature, then its seed; `sampler_record(backend)` → `meta["sampler"]`: `{temperature, seed, num_ctx, think, source, note}` — `source: "model default"` and the note naming whose recipe applied (the S119 reading, tagged Historical, not re-read by the pipeline) when nothing was sent; the request's values when the lever is set; Gemini's pinned 0.2 on that route. Same-commit disposition `converter:sampler` (EVIDENCE) in `observability/dispositions.json` — the meta literal is harvested by the glass detector.
- **The tripwires** (Observed): `analyst_selftest` S157-E6 (a) the record names None + the Modelfile note and the request is `{num_ctx: 8192}` today; (b) the lever's values ride the request and the record (temperature 0.0, seed 7 — the plumbing proved, the values not chosen); (c) NEGATIVE CONTROL: the grid program's per-call 0.0 keeps precedence over a lever temperature of 0.9, and the Gemini record pins 0.2 — **43/43**; `convert_and_ship_selftest` 254/254; ruff clean; `glass_detector.py --since d0e193b --enforce` → no unsigned glitches; `observability/acceptance.py` PASS 173/173.
- **Behaviour change: none.** The request the analyst sends is unchanged until his word sets the values; the record now says so. J49's measurement (a full greedy run's document number on the card) is still owed and is his.
- **The register:** J49 stays open — its status gains the recording half; the values and the greedy run are the row's remaining halves.


## E5 — the passes on the whole shelf, and five skeptics on what they did (06:38–07:1xZ; E6 ran inside this window)

- **The shelf probe** (Observed; `sittings/S157/e5_shelf_probe.py`, output `measurement/evidence/s157-shelf-geometry/`): the geometry pass — split → trim → the per-table proposals, resolver none, no reading — run DRY over every bundle on the shelf (25 anchor + 8 held = 33; nothing written), and the same probe with the S157-open layer (`9c87f72`'s `table_geometry.py`) as the CONTROL:

| | tables | splits | trims | columns dropped | ordinary proposals applied / refused |
|---|---|---|---|---|---|
| the S157-open layer (control) | 2,513 | — | — | — | identical per bundle |
| the layer now (E1 split + E3 trim) | 2,517 (+4: the four Valentine copies' coefficients tables) | 4 | **622** | 1,957 | **identical per bundle** — the passes changed no ordinary proposal's fate; the Damodaran refusals (1 / 1 / 2) are pre-existing |

  Where the trims are: Damodaran 4e 110 and 102 (two copies), the University Edition 101 / 101 / 29, Valentine 14–15 per copy, Ashby 5 per copy, Zero-to-One 1; the Beer books, DDIA, Ashby's tables with content, claude-code: none. Every one of the 622 re-counted by a second method (`cells()` on every row of every trimmed table's before-text: the removed cells blank, the counts as stated) — **0 defects** (Verified: the invariant and an independent count).
- **The skeptic fleet** (Observed; `sittings/S157/e5_skeptic_brief.md` written first — GROUND, deviation-is-the-report, tags, a planted control, residue; five Sonnet lanes on `packet_L1…L5.json` — 22–36 entries each, sampled with a seed, ONE planted trim per packet whose "blank" column holds a value; Workflow `wf_61ae5213-d2b` + the L1 re-read `wf_247a4314-233`): **6 of 6 readings caught their plant** and named no other real trim a defect.

| lane | packet | real entries | verdicts on the real entries |
|---|---|---|---|
| L1 (second reading) | Ashby 5 + Zero-to-One 1 + Valentine anchor 15 trims + its split | 22 | 20 clean; 2 "unexpected" — the split record's `consumed_lines` were 0-based (a real inconsistency, fixed below); the ANOVA's trim entry sits inside the split entry's span (by design: the trim runs AFTER the split — the brief did not say so; a residue of the brief) |
| L2 | Damodaran 4e, 35 of 110 | 35 | 35 clean |
| L3 | Damodaran 4e, another 35 | 35 | 35 clean |
| L4 | the held University Edition (14c6…), 35 of 101 | 35 | 32 clean; 3 "unexpected" — STUB tables (a bare `True False` header with no rows; a figure caption in a pipe frame; an `Announcement Date` header over one blank row): the trim is right, the table is not a table |
| L5 | the University Edition (1), 30 of 101 + the held Valentine split | 31 | 30 clean; the held split's `rejoined: null` questioned — by design (the scattered tail, E1) |

  The first L1 reading (the first packet) reported eight "defects" — every one a table below Valentine's p.175 split whose before-text the probe had sliced from the pre-split lines (a split inserts three lines; the trims' line numbers are post-split). The lane read the deviation as the brief asked; the probe was fixed, the packets rebuilt (L2–L5's byte-identical to what they read — their verdicts stand), L1 re-read. The probe's other fault, found on the way: its output slug cut the bundle name at sixty characters and three Valentine copies (and two University Editions) overwrote one another — a hash in the slug now.
- **Fixed from the reading** (Observed): `geometry_pass`'s record wrote a split's `consumed_lines` 0-based while every other line number is 1-based — now 1-based; selftest §13 checks the three numbers (169/169; analyst 43/43; ruff clean).
- **Added to the register:** `analyst/stub-tables-are-prose-in-a-frame` — 64 of the shelf's 2,513 tables have no filled body cell (a caption box, a chopped heading `Phi | loson | hv`, a footnote, framed as tables); a rule would unframe them; mechanical, the lane's.
- **Residue:** the skeptics read the packets, not the pages — "a blank column that lined something up on the printed page" stays Inferred-clean, not Observed; the sample is 152 of 622 trims read by a model, all 622 counted by a script; the after-text was not in the packets (the invariant is the proof of the after; a reader saw only the before and the numbers).


## His word at 07:05:00Z (the Desk, d7d03d67), verbatim — and its reading

> Run autonomous until I say stop. Keep doing tasks, be persistent. Have a cookie every closeout when you're running in this mode. Every episode rewards a cookie. Record all receipts.

- **Read as:** the persistent autonomous run continues until his word "stop" (the S157 word "don't close" stands; the experiment's end stays his sentence); the lane keeps taking its own tasks as episodes; **a cookie per episode closeout while this mode runs** — a standing grant; receipts at every closeout, as before.
- **Done on the word** (Observed): cookies #87–#93 logged for the seven closeouts cut before it (E0–E6; `cookie-tally.md` header 86 → 93, the entry at the top of the ledger with his words; MEMORY.md TIME-STATE mirrored; the library committed `d736cfa` and pushed). Each later closeout in this mode adds one at its receipt. The Desk reply carries the count.


## E7 — his question answered from the register; the census counts stubs; the close's files kept ready (07:05–07:1xZ)

- **His second Desk word (f905f27d, 07:07:58Z), verbatim:** "Also let me know how many tasks are current remaining/tickets in open task" — answered on the Desk (07:10:27Z) with the numbers measured at HEAD: OPEN-TASKS.md carries 229 id rows, **132 open**, 97 struck (the muster's own counter agrees; 150 at this sitting's open — E4 struck 18, E5 added 1); by section (open / rows) §A 42 / 48 · §B 27 / 34 · §D 6 / 8 · §F 10 / 12 · §J 37 / 68 (21 tagged MECHANICAL) · named 10 / 54; the ten open named tickets listed; the symptom index 132 rows, 41 open. Numerator and denominator named on the reply.
- **The census counts stubs** (Observed, private `table_census.py`): a table whose body has no filled cell → `stub`; the summary's `stubs` and `stub_tables`. Valentine after the trim: anchor 0, held 1 (`Phi loson hv` at body line 4573 — the chopped heading). The shelf's 64 (E5) are named on the register row `analyst/stub-tables-are-prose-in-a-frame` — whose text had carried literal pipes inside a cell and broke its own columns; fixed (0b2739d).
- **The close's files kept ready** (private `sittings/S157/code_notes_S157.md`: one block per public file changed since the pin, E1–E6 explained; the notes, the closing message and the five-cell row are written at his "close" from the episodes' sections) — so a close from him runs the chain without a pause.
- **Not this episode's:** a rule for the stubs (a stub may be the trace of a lost table's body — unframing would hide the trace; the census reports, the record names, his re-conversion decides).

## E8 — two mechanical register rows: J61 fixed, J45 answered by J46 (07:11–07:2xZ)

- **J61** (Observed, public `.claude/skills/relay-gate/selftest.py`; `sittings/S157/e8_j61_patch.py`): `run()`, `spawn()` and the T53 `Popen` decoded the gate's stdout with the host locale and started the child without `PYTHONIOENCODING`; now `CHILD_ENV` carries `PYTHONIOENCODING=utf-8`, the three call sites decode UTF-8 with `errors=replace`, the parent's stdout is reconfigured (the reading-bench selftest's S129 pattern). A tripwire added beside the J52 T1 assertion that had been split into ASCII halves: the gate's literal em dash (`*** STALE — presumed dead …`) round-trips through `run()` — 198 → **199/199**, run WITHOUT `PYTHONIOENCODING` on the parent. Negative control (Observed, a scratch run): a child on UTF-8 decoded by the old locale path renders `â€”` — the old `run()` under any session that exports `PYTHONIOENCODING=utf-8` (this one does); child and parent both on the locale round-trip by luck; the fix round-trips by design.
- **J45** (Observed): the acceptor J46 (S140, Rab's C+A 2026-09-12) classifies a hunk `numeral` when the digits differ and the letters agree (`edit_whitelist.py:172-179`), `FULL` excludes the class, so every numeral change is REVERTED at accept time and counted (`manifest.analyst.edits.reverted.numeral`; DDIA: 10, docs/15 §12.2). "The model rewrites numbers and nothing sees it" no longer holds — the acceptor sees it, reverts it, counts it; the reject-vs-flag slot is moot under the signed whitelist (the hunk ships as the input; no chunk is lost). Struck as answered; the residue is J46's own (a citation number moved out of a link — the `link` rung's; the `1,000` / `1000` formatting question, a substitution today, reverted).
- **The register:** 132 → 130 open (the muster's counter).
- **Not this episode's:** J49's values and run (his); the gate's other rows (J59 the stage lock, J60 the handover window) — real builds on the relay's referee, each its own episode with the 199-case suite as the floor.

## E9 — the process census (J3), and what it found: my own selftest had been loading the model on his card (07:18–07:3xZ)

- **The tool** (Observed, private `agent-scripts/auditing/fleet_proc_census.py` + brief; the smoke 69/69 after the promotion; briefs 56, defects 0): every process the tracker saw CREATED in a window with no exit recorded, checked against the live process table (`tasklist /FO CSV /NH` from Python — never `/FI` from a Git-Bash shell); the tracker's own processes and the shells never called a leak; exit 1 names each leak (pid, name, category, created, the command line's head), 2 UNREAD when there is no receipt or the live table could not be read; never kills.
- **Its first run** (Observed; `sittings/S157/e9_census_now.json`), over this sitting's window from the tracker's start (06:03:37Z): 3,007 records, 12 created-with-no-exit, 7 alive — five `OTHER` (two svchost, three firefox content processes, SearchFilterHost: the machine's and Rab's) and **`llama-server.exe` (pid 6260, ollama's runner, created 06:54:23Z, `--model … -c 8192`)** — qwen3:8b on the card, 6.3 GB, `ollama ps` "until 13 minutes from now".
- **The trace** (Observed): Ollama's server log — llama-server starts at 01:15:31, 01:28:13, 02:11:37, 02:26:07 and 02:54:23 local (05:15, 05:28, 06:11, 06:26, 06:54Z), each followed by a ~5–6 s `/api/generate`, then 1 s generates at 06:55:06Z and 07:03:33Z, and 1 ms ones (unloads) in between; the tracker — `analyst_selftest.py` (pid 25196) started 06:54:00Z, the runner 22 s later; the selftest's three runs at E6 (twice) and E5's fix are the three POSTs to the second; the earlier eight (S156's six, E1, E3) match the file's run minutes (Inferred by the minute, not the second). A urlopen SPY on a run of the file (no network, no GPU) named it: **`analyst_selftest.py:755` — the S156 (a) case's negative control, `analyst.process(S151_MD, backend="local", resolver=…)` called with the REAL `_generate`, outside the try/finally that scripts it** — one readability chunk, `keep_alive 30m`. Written by this lane's occupant in S156 E5; every "analyst 40/40 · 43/43" since cost him a model load; the S156 journal entry said "nothing on the card". A urlopen that RAISES did not catch it either: `process()` counts a backend error as a failed chunk and the case still passed (43/43, silent).
- **The fix** (Observed; `sittings/S157/e9_selftest_network_guard.py`): the control scripted like the positive; a NETWORK GUARD at the top of the file — every urlopen reached is counted and refused; a final case `S157-E9 NETWORK` reds on a non-zero count. Negative control run first (the guard alone, the control still leaking): `BAD … urlopen reached 1 time(s): ['http://localhost:11434/api/generate']`, 43/44; then the fix: **44/44**, and Ollama's log shows no request from the run. Ruff clean.
- **The card handed back** (Observed): `ollama stop qwen3:8b` at 07:2xZ; `ollama ps` empty; `nvidia-smi` 8,130 → **1,872 MiB** used (his applications' share at the open).
- **Filed:** ERROR-BIN **ERR-2026-09-15-106** (BOUNDARY-OMISSION: a comment promised a boundary the test never enforced); the autonomy JOURNAL's correction entry ("the card WAS touched — by my test, ten times today"); J3 struck (the census built, and it found this); SYM-054 flipped with the residual (the docs/47 clause is his to sign). The Desk line carries it to him plainly.
- **Residue:** the other selftests that mention Ollama (`convert_and_ship_selftest`, the room chat's) get the same census at the next episode; a pid the OS reused in the window would read alive (the name is printed beside it); the tracker sees what it polls.

## E10 — the same question asked of every selftest, and a second phantom of mine (07:32–07:4xZ)

- **The spy** (Observed; `sittings/S157/e10_urlopen_spy.py`): each selftest run in-process under a stand-in for `urllib.request.urlopen`, `http.client.HTTPConnection.request` and `socket.create_connection` that logs the URL, the request's model / keep_alive / prompt head and the caller's stack and answers with a canned reply — nothing reaches the network, nothing reaches the card (Ollama's log: no request during the runs; `ollama ps` empty before and after). Six files: `analyst_selftest` 44/44, `backend_parity_selftest` 9/9, `degeneration_selftest` 26/26, `latex_balance_selftest` 27/27, `convert_and_ship_selftest` 254/254, `edit_whitelist_selftest` 53/53 — **0 network calls reached in any of them** (the analyst one after E9's fix; before it the same spy named line 755). `table_geometry_selftest` cannot be run in-process from the spy (a bare-module import) — it makes no network call by construction (no `urllib` in the file; the E9 census saw no runner at its run times). The residue: a child process's calls are invisible to an in-process spy — the gate selftest and the muster suite spawn children; the process census (E9) is the instrument for those, and it saw no runner at their run times today.
- **A second phantom, mine** (Observed): the tracker's receipt lists `python.exe seat_free.py --min-idle 10` fifty-four times in this sitting's window — S154 E7's seat watcher (a Bash monitor polling `query user` and `nvidia-smi` every minute to launch the p.175 Marker probe when the card fell idle), left armed after S155 ran that probe on his word: fourteen hours (`still held (840 min)`) of polling his machine for a purpose that had ended, and a `SEAT FREE` notification waiting to mislead. Stopped (task bjvtv0hxa). Filed ERR-2026-09-15-107 (PHANTOM-MONITOR, the inverse shape: a watcher forgotten, alive). The monitors armed now, listed: the Desk watcher (bpq8ahhxo — his posts; ends at his "stop" or the close) and the process tracker (pid 23948 — ends at the close's STOP file). The carry-over carries this list from now on.
- **Not this episode's:** a network guard in every selftest (the analyst one has it; the others make no call today and the census stands watch); the muster and gate suites' children.

## E11 — J40: the exporter no longer calls a blessed flag "pass" (07:36–07:5xZ; CI as the evidence)

- **The change** (Observed, public `linux-converter/converter/exporter.py`, `tests/test_exporter.py`; `sittings/S157/e11_j40_patch.py`): the caller of `_supersede_replace` hands it the EFFECTIVE verdict — `pass` when the remedy passed on its own audit, `flag (blessed by <who>)` when a human bless carried it — and both the `EXPORT-SUPERSEDE` log line and the vault commit subject render `<from>→<effective>` (the first real supersede-with-bless, Cybernetics 2026-09-06, had written `fail->pass` over a manifest saying `flag` + `blessed{by rab}`: the data right, the two strings outrunning it — STATUS-THEATRE's shape, named by the ticket). The bless-path test asserts the commit subject `… (audit-remedy, fail→flag (blessed by rab))`; a negative control asserts the merit path keeps `fail→pass`. Ruff check and format at CI's pin clean (two files reformatted by ruff's own hand).
- **The evidence** (Observed): the Windows run of `test_exporter.py` fails in the pre-existing path class (`Inbox\…` backslashes in `git cat-file`; 23 of 38 failed BEFORE the change, my two among them for the same reason) — so, as J33's precedent says, **Linux CI is the executed evidence: green on `51edb34` (run 34942832685, num 376)**, the `Test (linux-converter)` step included.
- **Struck** with the residue his: whether `bless.json` rides in the vault beside the note (named, not judged, by the ticket); the ThinkPad deploy (pull + restart) is his hand — until then the exporter on the ThinkPad still writes the old string.

## E12 — J36: the adoption receipt, and the card reads it (07:4x–08:0xZ)

- **The receipt** (Observed, public `coordination/adoptions.jsonl`; `sittings/S157/e12_adoptions_backfill.py`): one JSON row per adoption — `ts` (the adoption's own time), `recorded`, `state` (adopted | superseded | destroyed), `sha8`, `bytes`, `exe`, `head`, `by`, `evidence`, `note`, `measured`. Seeded with the five lineage rows the memory prose carried (`4DCB73E2`, `7F7E2701`, `4C6B073B`, `1CF604CD` destroyed, `E7C5A47A` — Historical, `measured: false`, their times as the prose had them), then the current adoption written by the tool itself: `C4559805`, 10,335,232 bytes, measured on the installed path, `ts 2026-09-04T02:3xZ` (his word, S114), `head` null — S114's record has the build commit; the first cut had carried a guessed sha for a minute and was corrected before the first commit.
- **The writer** (Observed, private `agent-scripts/auditing/adoption_receipt.py` + brief; the smoke 70/70, briefs 57 / defects 0): "Adopted, hash matches X" becomes a measurement — the installed exe is hashed and a claimed hash it does not measure is REFUSED (`--sha8 DEADBEEF` → exit 1, nothing written); append-only; `--go` writes, without it the row is printed.
- **The card** (Observed, public `.claude/skills/muster/open.sh`; `sittings/S157/e12_open_patch.py`): an `adoption` row under `installed exe` — the newest `adopted` row against the measured exe: `MATCH`, `*** DRIFT ***`, or `UNREAD` (no file, or no adopted row — a reading, the exit unchanged; never "no adoption"). `selftest.sh` CASE 37: (a) a receipt matching a planted exe reads MATCH, (b) a receipt naming another hash reads DRIFT, (c) no file reads UNREAD and never MATCH — **106/106**. The live card at HEAD: `adoption C4559805 (2026-09-04T02:3xZ, by rab) · installed C4559805 — MATCH`.
- **Struck** J36 with the residue (the Historical rows' times are the prose's; the current row's build commit null, not guessed). The memory file `installed-exe-state.md` keeps its caveat ("trust the card") and now has a repo record to point at — the lockstep at the close notes it.

## E13 — J12: docs/47's four unexercised clauses, exercised (07:55–08:1xZ)

- **The gap** (Historical, docs/47 §7): "the tripwire tests the GROUND clause only. NEGATIVE CONTROL, DECLARE YOUR RESIDUE, HONEST NULLS and the anti-correlation clauses are Intended — signed, unexercised. Each needs its own case." E5's fleet kept them by hand (a plant per packet, a residue field in the schema, ids matched by eye).
- **The check** (Observed, private `agent-scripts/auditing/fleet_law_check.py` + brief; the smoke 72/72; briefs 58 / defects 0): on the brief — GROUND, deviation-is-the-report, the tags, a planted control, residue named; on the results against the packets and the orchestrator's plant list — PLANT (each lane's plant in its `defects`), RESIDUE (non-empty), NULLS (exactly the packet's ids, once each), TAGS (the four words), LENS (two lanes over a shared packet with identical verdict vectors read as one lens; skipped under five shared ids). Exit 1 names each red per lane.
- **Its tripwires** (Observed, `fleet_law_check_selftest.py`): a positive control (a clean two-lane fleet), then each clause VIOLATED on purpose — a missed plant, an empty residue, a padded lane, a truncated lane, an untagged entry, identical vectors over a shared packet, a brief that never names a plant, a lane with no plant on record — each reds exactly its own check and nothing else; the LENS skip over disjoint packets — **11/11**.
- **Its first live run** (Observed): S157 E5's skeptic brief and both readings — the first reading (five lanes on the first-cut packets): 25 checks, 0 red; L1's second reading: 9 checks, 0 red. The clauses that were kept by hand at 06:4xZ are read by a tool at 08:0xZ.
- **Struck** J12 with the residue: the law's §7 sentence is a signed text — his to amend; the check binds reader/skeptic fleets with a packet shape; a builder fleet's residue (files, processes, ports) is the process census's (E9). The lane's protocol from now on: `fleet_law_check --brief` before a summons, the full call after the return, before any verdict is quoted.

## E14 — J37: the circle skill's three S108 rules, imported (08:02–08:1xZ)

- **The gap** (Historical, The Atlas Plan §2 path 11, re-measured 2026-09-05): the live skill `~/.claude/skills/circle/SKILL.md` was untracked and unchanged since 2026-08-13; it carried none of the three rules and returned HELD as a verdict; the S108 record and the S94 guard form both said otherwise (corrected S115).
- **Built** (Observed): the tracked `.claude/skills/circle/SKILL.md` — the home file's text plus a section with the three rules in the plan's own wording: (1) frozen-commit immutable audit records — a repair lands as a new commit re-audited by the next Circle, a wrong closed record gets a dated correction, never a rewrite; (2) the aborted-lane law — a lane that does not return is NON-EVIDENCE for its scope, never silently absorbed, its scope re-run or UNREAD; (3) "HELD" retired as a verdict word — Phase 6's vocabulary now reads **WITHSTOOD** (checked and clean) / **BLOCKED** (the check could not run; never cleanliness). The user-level copy overwritten byte-identical (the skill resolves from there; the repo copy is the record).
- **The tripwire** (Observed, `.claude/skills/circle/selftest.sh`): the tracked skill carries the three rules and no HELD verdict bullet; the mirror is byte-identical (UNREAD when absent — never a pass; it went red before the copy); NEGATIVE CONTROLS — HELD planted back as a verdict bullet reds for that reason, the aborted-lane law removed reds for that reason — **4/4**.
- **The two written claims** (Observed): a dated note appended under each S115 correction (the S108 record §3; the S94 form line 23): the sentence stands as written and, as of today, is true.
- **Struck** J37. Residue: the memory `circle-skill.md` still describes the 08-13 shape — the close's lockstep notes the tracked copy and the rules.

## E15 — the rail route's false labels on Ashby: a run that is an INDEX is not a rail (08:05–08:3xZ)

- **Found** (Observed): E5's shelf probe had printed `labels 4` for Ashby's *An Introduction to Cybernetics* (both anchor copies and the held one); read at E15, the four labels are the rail route rewriting a matrix's single-glyph ROW LABELS: rows `3 4 5 6` → `ELASTIC` (the word on the first row, the labels blanked), `1 2 D 3 4 5` → `IDEAS`, `4` / `D 5` → `ADDS`, the merged rows `a<br>b` / `c` beside `α<br>β` / `γ` → `ABC`. The invariant admits it (the letters fit — `read_pattern("3456")` = e, a, s, c/g/b — and the words are Ashby's); the census reads a clean table. Under the lever ON since S154 any analysis of such a book would ship it; none has run since (the shelf's Ashby copies were analysed before the lever).
- **The rule** (Observed, public `table_geometry.py`; `sittings/S157/e15_index_guard_patch.py`): `_index_run` — a run refused before any word is sought, the reason on the record in `unresolved_rails`: (R1) more than half the bared glyphs are digits; (R2) every glyph is one of the table's own column headings (a matrix index); (R3) a stacked letter cell whose row's other filled cells are stacked to the same `<br>` depth with short parts (merged rows). Two first cuts corrected by the tripwires: R3 counted words as pieces (`_pieces` splits on whitespace too) and refused the p.367 quadrant's stacked HIGH beside prose — now `<br>` stacking only; R1 counted a space as a glyph and let `4` / `D 5` through as ADDS — now bared.
- **The tripwires** (Observed): `table_geometry_selftest` §15 — R1 on the 3-4-5-6 matrix with ELASTIC in the fixture's lexicon; R3 on the a-b / c merged rows; R2 on an a-b-c matrix; positive controls: `6OSTs` → COSTS still resolves (one digit among five), the quadrant's stacked rail beside prose is not merged rows — **175/175**; `analyst_selftest` 44/44; ruff clean.
- **The measure** (Observed): the shelf probe again (`shelf-before-E15.json` kept beside): Ashby ×3 labels 4 → 0, applied 6 → 3, unresolved 7 → 10 (each with its reason); the other 30 bundles byte-identical in every count; Valentine's fresh dry runs (`evidence/s157-valentine-after-index/`) scored against the 14 truths — every table's TEDS identical to E3's (anchor 0.818, held 0.856), the six labels as before.
- **Filed:** docs/15 §12.4.13; SYM-133 (FIXED, with the guard named); ERROR-BIN ERR-2026-09-15-108 (DENOMINATOR: "zero wrong labels" measured on one book, carried as the layer's property; the probe's `labels` column unread for ninety minutes). No register row named this; none added (fixed the same episode).
- **Residue:** a matrix whose row labels are real words would still read as a rail (none on the shelf); the reading settles a doubtful block where a page is read.

## E16 — a second register sweep: four rows re-probed and struck (08:16–08:2xZ)

- **How they were found** (Observed): the open rows that cite a symptom the index marks FIXED (a grep over both files — its first cut matched `unfixed` and `not fixed` as fixed; read by hand after), plus J34's own "strike at the next sweep".
- **B6** — FIXED S151 E2: `marker_version_stamp()` returns `1.10.2` now (a call); `87db1a7` an ancestor; SYM-044 FIXED with six tripwires. **B5** — BUILT: `test_bench_page.py` holds 82 tests and runs green now; "nothing tests bench.html" is false; the in-browser DOM run stays open, named as the residue (his screen). **J34** — the inflation guard at 1.5 on his word, two live books at zero inflation rejects, the selftest's negative control; its own status asked for the strike. **F8** — the correction appended to SYM-046's row: S100's hollow glass citation was in its LEDGER ROW, not its closeout (the closeout names glass only in a relay note, line 128; the S101 correction on it answers a claim it never made); S97:211's line was the same form and got no correction — the count stands as two closeouts and one ledger row.
- **Not struck:** B10 and B23 (SYM-049 OPEN — the grep's `unfixed`), B12 (SYM-034 OPEN — "not fixed"), A26 (his #9), B2 (his delete), J59 (SYM-078's fix is the autocrlf fixture, not the stage lock).
- **The register:** 126 → 122 open (the muster's counter).

## E17 — B12: Ollama's own log captured at a failed call (08:19–08:4xZ)

- **Correction to E16's count** (Observed): the register read **121** open after E16 by the muster's counter, not 122 — I had inferred the E14 figure instead of re-measuring; the counter is the number from now on.
- **The gap** (Historical, SYM-034): a hung `/api/generate` — no error anywhere, the server responsive, the model resident, the same request fine by hand seconds later — and only the client's side of it on record; B12 asked for Ollama's own log at the moment.
- **Built** (Observed, public `windows-converter/analyst.py`; `sittings/S157/e17_b12_patch.py`): `_capture_ollama_log(work_dir, i, run_key)` — on any failed local call the last 200 lines of `server.log` written to the analyst work root's `_captures/ollama-<key>-<i>-<ts>.log` and ledgered through `dumps/dump.sh` (DUMP_LANE=pipeline); the DUMPED id or an UNREAD reason returned, never raised; `meta["backend_failures"]` rows `{i, error, detail, ollama_log}` (the Gemini route: `n/a`); the chunk still ships as the original and `chunk_scores` keeps its `{i, x: failed}` row. Same-commit dispositions for the key and its three fields; glass `--since d0e193b --enforce` clean; acceptance 181/181.
- **The tripwires** (Observed, `analyst_selftest` S157-E17): (a) a planted 300-line log, a call that raises `TimeoutError("planted stall")` → the capture holds lines 101–300 with the header naming the chunk, the ledger row DUMPED, the row names chunk 1 / `TimeoutError` / the id, the original ships; (b) NEGATIVE CONTROL — a run with no failure: an empty list, no capture written; (c) no log at the path → `UNREAD: no ollama server.log …`, never a fabricated id — **47/47**; `convert_and_ship_selftest` 254/254; ruff clean. The first cut wrote the capture INSIDE the work dir, which `process()` rmtree's at assembly (J41's grave) — (a) failed on the missing file; moved beside the work dirs.
- **Filed:** SYM-034's guard cell gains the capture (the cause stays unknown); B12 struck. The register: 120 open (the counter).

## E18 — B14: the two filters that could null a lever, made levers and counted (08:25–08:4xZ)

- **The gap** (Historical, S106): `MIN_SIDE_PT` and `MAX_PAGE_FRACTION` were constants outside the lever file, so `min_area_pt2`'s advertised range was inert — at `min_area_pt2=100` all 18 newly-admitted Cybernetics clusters died on the side filter, the report came back byte-identical, and nothing said so.
- **Built** (Observed, public `windows-converter/figure_coverage.py`; `sittings/S157/e18_b14_patch.py`): `min_side_pt` (1–400) and `max_page_fraction` (0.10–1.0) join `LEVER_SPEC` at the old constants — nothing moves until an operator writes a line, and an out-of-range line is refused and named like every lever; the raster probe and the two kill sites read the effective values; each size filter's kills are COUNTED after the filter before admitted the region (`filters_killed`, per kind: raster min_area_pt2 / min_side_pt / max_page_fraction; vector min_paths_or_area / min_side_pt / max_page_fraction) and carried in the report's `conditions` beside the effective values — a lever change that buys nothing reads as a count on the next filter.
- **The tripwires** (Observed): `figure_coverage_selftest` + 6 — the defaults are the old constants; an operator's side and fraction take effect; out-of-range refused and named (two rejections); a bars fixture (six 480×8 pt rules): at `min_area_pt2=100` the area admits them and `min_side_pt` kills them — counted, no page promoted; the NEGATIVE CONTROL under the defaults: killed by area, `min_side_pt` 0; the report's conditions carry the count and the effective side — **36/36**. `schema_registry.py --check` red on the new key (the A4 hard gate, as designed) → `--write` → PASS; ruff clean; `coverage_rescore_selftest` OK; ship 254/254. figure_coverage is not a glass producer (the registry is its record).
- **Struck** B14. The register: 119 open (the counter).

## E19 — B7 / SYM-045: the id preflight (08:29–08:5xZ)

- **The gap** (Historical, SYM-045): an id filed twice — two instances each honestly taking "the next free number" from the one surface they read; the rule was "grep before writing" and no tool did it. This sitting filed SYM-133 and ERR-106/107/108 by max + 1 by hand.
- **Built** (Observed, private `agent-scripts/auditing/id_preflight.py` + brief): every id given is grepped as an exact word across the whole tree (the private junctions included; `.git`, `node_modules`, `target`, the analyst work dirs and the process receipts excluded; ~30 s) and refused with each hit's `file:line`; `--next <kind>` takes the next number from the kind's REGISTER SURFACE — the index's rows, the bin's rows, the relay's entries, the `docs/` and `sessions/` filenames — and preflights it. Two first cuts corrected in the hour: a tree-wide maximum offered `SYM-1000` (the muster selftest plants `SYM-999`); the tool found its own docstring's example ids (placeholders now, and its own file skipped).
- **The tripwires** (Observed, `id_preflight_selftest.py`, a planted repo): taken with file:line; free; `SYM-04` inside `SYM-045` is not a hit; `--next` per kind from the surface, not the decoy `SYM-999` / `S900`; the ERR next carries the date; the next session number preflighted against a plan that already names it reads TAKEN (the caller decides) — **10/10**; the smoke 74/74; briefs 59 / defects 0.
- **Its first live reading** (Observed): `SYM-133` TAKEN (the index, the record, the Desk); `SYM-134`, `ERR-2026-09-15-109`, `S158`, `docs/61` free.
- **Filed:** SYM-045 → FIXED with the residual (a read-before-write, not a lock; the filing acts must call it — the lane's protocol from now on); B7 struck. The register: 118 open (the counter).

## E20 — the leaked head: E3's residue on the anchor's p200-t1 answered (08:36–09:0xZ)

- **The shape** (Observed, E3): the statistics table's last two rows were the ANOVA's title row and half its stacked heading; the ANOVA below still carried both. The exact-duplicate rule E3 probed matched nothing — the heading is OCR-split with a doubled letter (`Sig` + `gnificance`); the layer's own `collapse_doubles` over the head's letter stream is the match that works.
- **The rule** (Observed, public `table_geometry.py`; `sittings/S157/e20_leak_patch.py`): `propose_leaks` — walking up from the last body row, rows with no numeric cell whose every filled cell is in the next table's head (its header and first two body rows; exactly, or as letters in the collapsed stream); the topmost dropped row opens with the next table's first header cell (the leaked TITLE anchors the leak); the next table within three lines; a body row kept; `apply_leak`; `leak_invariant` (the exact cut; the next table unchanged; every dropped cell non-numeric and in the head); `leak_pass` runs after the split and before the trim (the leak's removal empties p200-t1's third column, which the trim then drops); the record's `leaks` / `leaks_refused`.
- **The tripwires** (Observed): §16 — the p.200 shape (drop rows 9–10; the ANOVA untouched); the pass order with the trim (2 columns, 5 rows); idempotence; NEGATIVES — a number in the tail, a tail row that is not the next title (`Notes`), a next table too far; the invariant refuses a kept cell changed; nothing on the healthy fixtures or p.175's shape — **184/184**; analyst 47/47; ruff clean. Two fixture faults on the way: a bare `replace` of the tail line also hit the next table's header (the negatives read the changed header as the title) — the replaces now name the two-line context.
- **The measure** (Observed; `evidence/s157-valentine-after-leak/`, `sittings/S157/e20-*.json`): anchor p200-t1 **0.571 → 0.955** (the dropped caption is the rest), the other thirteen truths identical to E3; the held copy identical (no leak there; 0.856). The shelf probe: exactly one leak (this table); every other bundle's counts unchanged from the E15 state (the `before-E20` snapshot predates E15's last fix on Ashby, so Ashby shows labels 1 → 0 there — that is E15's, not E20's).
- **Filed:** docs/15 §12.4.14. No register row (E3's residue, named there); CI green on 38c1758 (run 34947778263, num 385) for E19's push.

## E21 — J59: `gate.py stage` made cumulative across lanes (08:42–09:0xZ)

- **The gap** (Historical, S120 §10): each `stage` rebuilt the index blob from HEAD plus `--as`'s own entries, so a second lane staging before the first committed REPLACED the first's index entry; correctness rested on "each lane commits before the other stages".
- **Built** (Observed, public `.claude/skills/relay-gate/gate.py`; `sittings/S157/e21_j59_patch.py`): the base is the INDEX blob when it already differs from HEAD — its entries beyond HEAD's (the peer's staged, uncommitted ones) are kept, matched as an ordered subsequence of the working copy exactly as HEAD's are; an index that is not a faithful append of HEAD, or whose staged entries are not in the working copy in order, is REFUSED as UNREAD and left untouched (a diverged index is never quietly overwritten); the second stage prints what it kept of the peer's. The existing B7/B12/B13 semantics are unchanged (an entry merely in the working copy, never staged, is still left for its lane).
- **The tripwires** (Observed, `relay-gate/selftest.py` + 5): Fable stages, Codex stages before any commit — the index holds BOTH; the second stage says what it kept; the working tree untouched; the reverse order on a fresh repo; NEGATIVE — a forged index blob (a different preamble) refuses and the index is left as it was — **204/204**; ruff clean on both files.
- **Struck** J59 with the residue: two stages in the same instant are serialised only by git's index lock; the stage→commit lock the row also named is not built — the cumulative base removes the harm the row measured. The gate's `gate_rev` changes with its bytes; the Codex lane is asleep (its sidecar's beat is stale regardless). The register: 117 open (the counter).

## E22 — five verifier lanes refute the day's claims against the day's diffs (08:50–09:1xZ)

- **The fleet** (Observed; `sittings/S157/e22_verifier_brief.md` — checked by `fleet_law_check --brief`, 5/5 clauses named; the packets `sittings/S157/e22/`: the diffs since the pin per lens and the record's sections E1–E21; Workflow `wf_2a285727-8fa`, five Sonnet lanes, distinct lenses — the table layer; the analyst; the gate; the muster card and the four private tools; the exporter and figure_coverage — each with ONE planted false sentence in its GROUND, recorded in `e22/plants.json` before the summons): **5 of 5 plants caught**, each named as the suspected plant with the lines that break it; 77 claims, 56 HELD, 7 BROKEN (five the plants), 14 UNREAD (every run-time number: the selftest tallies, CI, the TEDS — nothing ran, as briefed).
- **Two real findings, both paid:** (1) **V1-13, medium** — E15's selftest check for R2 (a matrix index) read `(not r3) or …` and the ASHBY3 fixture never fired the rail signature (no blank between the runs): the check passed VACUOUSLY and R2 was exercised by no case. The fixture is now the real Ashby shape (a / blank / b / blank / c with the probabilities split over two rows); the three runs fire and the check requires each refused as a matrix index — 184/184 (the count unchanged, the case now real). (2) **V4-03, medium** — the E12 section says the current adoption row was "written by the tool itself"; the lane read the row's key order (`recorded` last, not second as the writer emits) and concluded it was not: the truth is that the tool wrote it at 07:41:30Z, and I then EDITED the row twice by hand before its first commit — `ts` set to the adoption's own time with `recorded` appended (the reason the key order differs), and the guessed `head` set to null. The E12 section names the second edit, not the first; this line is the correction. The file is append-only from its first commit on; the hand edits were made before it.
- **Not paid (right as written):** V3's seven UNREADs are run-time numbers; V2's and V5's UNREADs the same; no lane found a claim the code contradicts beyond the two above.
- **The law on this fleet** (Observed): the brief passed the five clauses; every lane declared a residue and tagged every claim; a verifier fleet has no packet ids, so `fleet_law_check`'s PLANT / NULLS halves do not apply to it as built — the plants were checked by hand against `plants.json` (a packet shape for verifier fleets is the check's residue, named on its brief).
- **The process census** over the fleet's window: nothing of the lane's alive (his browser tabs and the machine's services only).


## E23 — the law check learns a verifier fleet (09:0x–09:1xZ)

- **E22's residue paid** (Observed, private `fleet_law_check.py` + brief; `fleet_law_check_selftest.py` 11 → 14; the smoke 74/74): a verifier fleet answers `claims` (HELD / BROKEN / UNREAD), not packet ids — `--packets none` says so and NULLS reads "not applicable, said" instead of red; its plants are SENTENCES from each lane's GROUND, caught when a claim marked BROKEN carries any four consecutive words of the sentence (a lane quotes or paraphrases). Run over E22's five lanes: 25 checks, 0 red — the by-hand check of E22 now a tool's. Three new tripwires: the positive control; the planted sentence marked HELD reds PLANT and nothing else; a tag outside the four reds TAGS.
- **Paid on the way:** the backtick trap, twice more (an inline python -c mangled a docstring and a brief; fixed by the Edit tool) — the carry-over's trap list already names it; the hand keeps paying.

## E24 — B23: the IV uncovered pages adjudicated on a CLEAN bundle; every anchor measured (09:01–09:3xZ)

- **What B23 says** (Historical, `docs/45` §6.5, S105): "219 of the 239 IV uncovered pages were never adjudicated; the other four anchors were never measured at all; SYM-049's fragmentation class is verified for Cybernetics only." The 239 was the 4e (2025) bundle's count under the POISONED page map (SYM-050); the adjudication of S104's twenty came out 19/20 FALSE for the same reason. `docs/45` §6.3 asked for P-1 re-measured on a clean bundle first.
- **The clean bundle** (Observed, `coverage_rescore.py --json`, both copies `… -- Four` and `… -- Four (1)`, source sha 14c66834… observed = manifest, SYM-050 NOT detected, map as-shipped, levers as shipped: min_area 4900 pt² · min_side 40 pt · max_page_fraction 0.92 · the three vetoes): **1,377 pages · 293 with a figure-like region · 76 uncovered · coverage 0.7406**, identical on both copies; 65 of the 76 regions are vector-only and 50 span the whole text column — the shape of a shaded ILLUSTRATION box, not a chart.
- **The adjudication** (`sittings/S157/e24_render_packets.py`: the 76 pages rendered at 100 dpi on the CPU — no GPU process, `.gpu-lock` absent, the card's 1.9 GB his desktop's — with the bundle's text around each page located by marker's own `<span id="page-N-…">` anchor where one exists (63) else by a line of the page's text (12; one page, 757, not located); `e24_reader_brief.md` checked by `fleet_law_check --brief` 5/5 before the summons; Workflow `wf_0bc5eb30-17e`, five Sonnet lanes, **every page read by two lanes** (lane k holds groups k and k+1), one planted control per lane — a COVERED page with a plain chart, its image line stripped from the excerpt, recorded in `e24/packets/plants.json` before the summons; 10.5 min, 329 tool uses, the lanes' only shell call a `cat` of their own packet): **76 of 76 pages agreed at the class level by both readers, 0 disagreements; 11 lost figures · 65 not figures.**
  - **Absent — caption and prose carried, the figure gone (5):** p.194 (two uncaptioned event-window timelines, one a stub table `| Announcement Date |` — E5's row already names it, the other dropped), p.352 FIGURE 9.3 *Converting Operating to Capital Expenditures* (the flowchart), p.426 FIGURE 11.4 *Estimating Revenue Growth*, p.435 FIGURE 11.7 *Estimating Reinvestment*, p.465 FIGURE 12.3 *Myth and Reality on Terminal Value*.
  - **Flattened — a diagram's WORDS carried as a pipe-table or prose, the picture lost (6):** p.81 FIGURE 3.1 *The Balance Sheet* (a garbled cell `- Invocanionae`), p.261 FIGURE 7.1 (eight thesis/implication panels as headed prose), p.341 FIGURE 9.1 and p.342 FIGURE 9.2 (labelled-box diagrams as tables), p.780 FIGURE 20.12 and 20.13 (two spreadsheet screenshots OCR'd into tables, the second garbled `Anı | nual`), p.903 FIGURE 24.1 *Key Person Value Effect*.
  - **Not figures (65):** tables 29 + box/table 11 = 40 (the ILLUSTRATION boxes' ruled tables, carried as pipe-tables); equations 10 + box/equation 10 = 20; box 1; other 4 (p.492, 805, 940, 945: a box frame around a heading and one paragraph on an otherwise blank page — the region is the frame; the lanes reported the geometry as a deviation, rightly).
  - **The one both lanes returned UNREAD, decided by the lane's own look (p.757):** ILLUSTRATION 20.7, a box of prose and equations, no figure — and its opening paragraph (*Consider two firms that produce similar products…*, six page lines) is ABSENT from the bundle (grep -F on three phrases: 0; `e24_text_loss.py`: 8 of the page's 11 prose lines not found — the only page of the 76 with three or more; over the 76 pages 40 of 1081 prose lines not found, the rest scattered singles). A lost PARAGRAPH — a new row.
  - **A second finding from the readers' deviation clause, verified by the lane (Observed, `e24/excerpts/p0308.md`):** p.308 TABLE 8.2 (*Betas and Operating Leverage — Shoe Companies*, 21 rows × 4) is carried as ONE header cell of `<br>`-joined values over 26 empty rows — the whole table in one cell. A new row. (L1's other deviation — "the packet's `table_rows_in_excerpt` reads 0 for p.400" — does not reproduce: the field reads 18 and the excerpt holds 18 pipe rows; the lane misread. Its observation that p.400's table is split in two by the page break holds.)
- **The law on this fleet** (Observed, `fleet_law_check --results --packets --plants`, `e24/fleet_law.json`): **30 checks · 1 red** — BRIEF 5/5; PLANT 5/5 caught (every lane called its plant FIGURE and listed it in `defects`; L3 and L5 also NAMED it, by its metadata — the plant entries carried `largest_area_pt2: null` and a full-page bbox, a signature the real entries never have: the control's blindness was broken by the packet, the instrument still fired; next time the plant's geometry comes from its real region); RESIDUE 5/5; NULLS 5/5 (31/31/31/32/32 ids once each); TAGS 5/5 (149 Observed, 3 Inferred); LENS: **L1~L5 IDENTICAL over their 15 shared ids — one lens twice by the check's rule**; the other four pairs differ. Read as measured: those 15 pages (62–342) count as ONE reading, not two; the lane's own look at six of them — five blind, before the fleet returned (62, 81, 177, 194, 261), and 342 after — agreed with both lanes on every one, four of the five lost pages among them included (not 341). Sampling never promotes — the fifteen stay "one lens, plus the lane where named".
- **Every anchor measured** (Observed, `sittings/S157/e24_measure_anchors.sh` → `e24/anchors_table.json`; report-only, CPU, source sha observed = manifest on all fourteen): Ashby 156 pp · 23 figure pages · **9 uncovered** (56, 84, 146–152) · 0.6087, both copies identical; Cybernetics Book of Models 91 · 57 · **1 uncovered** (p.84) · 0.9825, both identical; DDIA 673 · 104 · **2 uncovered** (162, 533) · 0.9808, both identical; Zero to One 166 · 49 · **5** (48, 56, 58, 109, 118) · 0.898; claude-code-up-and-running 104 · 8 · **0** · 1.0; **Investment Valuation 4e (2025) 1,356 · 269 · 49 uncovered · 0.8178 under the in-memory SYM-050 repair (239 unrepaired — the number B23 quotes)**; Brain of the Firm, Valentine (×2), Diagnosing the System (×2): **NOT APPLICABLE** — scan-lane books, every page one raster killed by max_page_fraction (439 / 464 / 348 killed), `coverage None` — a reading, never "0 uncovered".
- **B23's three clauses, where they stand:** (1) adjudicated — on the CLEAN bundle, 76 → 11, every page by two readers; the 4e (2025) bundle's 49 repaired-map pages are NOT adjudicated (the same apparatus runs on them — an episode, if wanted); (2) every anchor measured — the table above; (3) SYM-049 stays OPEN — the zero-area drop is at `figure_coverage.py:528` today (the row cites `:428`; the citation has drifted twice), the Book of Models' p.34 and p.78 are still invisible to the instrument (p.84 alone uncovered), and the class is verified for Cybernetics only — verifying it elsewhere needs readers over COVERED pages too, a different and larger study. The row is not struck; its status is appended.
- **What P-2 gains** (Inferred from the lists above): the specimen count `docs/45` called "a count no surface enumerates" has an enumeration on a clean bundle — 12 figures on 11 pages, 6 absent and 6 flattened — with the S105 specimens re-found at their University-Edition pages (3.1 flattened; 9.1/9.2 flattened; 9.3 absent; 12.3 absent, not partial).
- **The lane's own error, filed** (`ERROR-BIN.md` ERR-2026-09-15-109, EDIT-UNDER-EXECUTION): the anchors script was edited WHILE bash was running it (a hash suffix for the output slug — the E5 slug collision had bitten again: the Valentine and DDIA pairs overwrote each other on a 60-char cut); bash re-read the shifted bytes and ran the 4e (2025) line twice (54 s, 53 s). Harmless here (report-only); the law "never edit files a running lane is reading" covers the lane's own shell scripts.
- **The process census** over the window (`fleet_proc_census.py 2026-09-15T09:13:00Z`): six processes alive of his machine's (Firefox content, svchost, SearchFilterHost); nothing of the lane's.
- **Not this episode's:** the fix for the flattened class (Marker's OCR of a diagram into a table — a converter policy question, his); a rule that unframes stubs (E5's row, judged and not built); the 4e (2025) adjudication; SYM-049's real fix (clustering by stroke geometry).

## E25 — SYM-049 fixed: connector lines cluster; a diagram is not a table (09:32–09:5xZ)

- **The symptom** (Historical, SYM-049, S104/S105): a flow diagram plainly on the page and plainly absent from the bundle, and P-1 says nothing — `figure_coverage.py` dropped every zero-area path before clustering (`if _rect_area(r) <= 0: continue`), so a connector never joined the boxes it connects; two MEASURED specimens, the Book of Models' p.34 (54 drawings, 27 zero-area, 12 fragments, largest 541 pt²) and p.78 (2 clusters of 2 paths); the row's own residue said "the fix is clustering that follows stroke geometry". The neighbouring limitation, S105's "0-for-11": `find_tables()` calls a Watt engine a 16×18 table and the table veto removes the diagram.
- **Probed before building** (Observed, `sittings/S157/e25_sym049_probe.py`, `e25_lines_probe.py`, `e25_tablefill_probe.py`, `e25_curves_probe.py`; CPU, the card untouched): (a) a BRIDGE rule (a line touching two clusters merges them) catches p.78, not p.34 — p.34's 27 shafts each touch only their own 5×10 pt arrowhead; (b) lines as thin rects in the clustering catch both — and LOSE p.42 (a room drawn in perspective, flagged today only because one of its two clusters escaped the table veto; merged, the whole falls to it); (c) the filled-cell fraction of the vetoing tables does NOT separate diagram from table (Cyb p.30 a 3×3 at 0.556, IV p.944's real TABLE 25.4 at 0.304); (d) CURVES AND DIAGONALS do: over every (cluster, vetoing table) pair on the vector anchors, the Book of Models 14 of 15 pairs carry ≥ 3 sizable curves/diagonals (the engine 124+177, the time series 8+289, the room 0+10, the smallest 4+4), Investment Valuation 0 of 144 — once bullet glyphs (2 pt circles; p.944 draws 112) fall under a 4 pt span.
- **Built** (Observed, `windows-converter/figure_coverage.py`; the S104 behaviour reachable by lever): (1) a zero-area path is CLUSTERED when it is ANCHORED — within the gap of a positive-area path or of a zero-area line of the other orientation (`_anchored_lines`; a stack of bare parallel rules touches neither and stays out, so the min-side filter keeps its S104 meaning); (2) the table veto is DISQUALIFIED for a cluster whose drawings hold ≥ `VETO_TABLE_MAX_NONRECT = 3` curves/diagonals spanning ≥ `NONRECT_MIN_SPAN_PT = 4` (`_nonrect_items`), and a disqualified table leaves the frame sum too. Three levers (`zero_area_min_len_pt`, `table_max_nonrect`, `nonrect_min_span_pt`) in `LEVER_SPEC` with the report's `conditions` naming their effective values; two counters in the report (`zero_area_paths_clustered`, `table_vetoes_disqualified`) so the mechanisms' firing is a number, never silence. The A4 registry regenerated (`schema_registry.py --check` red on the five new keys, `--write`, PASS).
- **The measure, every anchor before and after** (Observed, `sittings/S157/e25_measure_anchors.sh` → `e25/anchors-after/`, `e25_diff_anchors.py` → `e25/anchors_diff.json`; the E24 run is the before): **the Book of Models: uncovered 1 → 3, GAINED exactly p.34 and p.78, LOST none; figure pages 57 → 71 (the fourteen diagrams the table veto had swallowed, every one covered by its asset); vetoed tables 15 → 0; 748 zero-area paths clustered; coverage 0.9825 → 0.9577 (the honest number: the instrument now sees the two pages it was blind to).** Every other book unchanged in its pages: Ashby 9 → 9 (30 lines clustered, 4 disqualifications, no page moved), DDIA 2 → 2 (572 lines clustered, all on pages already flagged by their rasters), Zero to One 5 → 5, claude-code 0 → 0, IV 4e (2025) 49 → 49 and the University Edition 76 → 76 (Damodaran's PDFs carry no zero-area paths and no non-rectilinear cluster; the 144 table vetoes stand), the scan-lane books NOT APPLICABLE both times.
- **The tripwires** (Observed): `figure_coverage_selftest.py` 36 → **44/44** — p.78's shape (two boxes, two connectors) clusters into one 4-path region and its NEGATIVE CONTROL (the lever at its ceiling) restores the S104 silence; six parallel rules 10 pt apart stay out; a ruled table drawn with lines now CLUSTERS (its perpendiculars anchor each other) and is vetoed AS A TABLE — silent by the veto, not by accident, and the report says which; a diagram (a circle + two diagonals) over the same grid is reported with the veto disqualified, and its NEGATIVE CONTROL (the disqualifier's lever at its ceiling) lets the veto swallow it again; `_nonrect_items` counts twelve bullet glyphs as 0; the report states the three levers' effective values. `coverage_rescore_selftest` 8/8; ruff 0.15.20 clean.
- **Residue, named:** p.40 of the Book of Models — a thermostat chart of shaded bands and axes, rectilinear (0 curves, 2 diagonals) — is still vetoed as a table (the 15th pair; covered by its asset, so no page moved); a rectilinear chart is the class this leaves. The instrument stays report-only and unwired (docs/15 §6; placement his). The stroke-geometry clustering the row asked for was not built: the anchored-lines rule is the smaller move that the measure justified.
- **SYM-049 → FIXED** (the row's status cell). **B23 STRUCK** with its residue: the 4e (2025) bundle's 49 repaired-map pages are measured, not adjudicated — the E24 apparatus runs on them when wanted.
- **Not this episode's:** wiring P-1 into the converter (his); the flattened-figure class (E24's six; a converter policy); the 4e (2025) adjudication.

## E26 — the crammed-table class measured; the census counts it; the rule withheld (09:51–10:0xZ)

- **The class** (E24's row `converter/table-crammed-into-one-cell`): a table whose content sits in ONE cell — IV University Edition p.308, TABLE 8.2's 21 rows × 4 columns as 99 `<br>`-joined values in the header cell over 26 empty rows.
- **Measured on the shelf** (Observed, `sittings/S157/e26_crammed_probe.py` → `e26/crammed.json`; every anchor bundle with a markdown, 25 bundles, 1662 pipe tables): a strict reading — exactly one filled cell — finds **0** (p.308's table has a second filled cell); the honest reading — the largest cell holds ≥ 90% of the table's characters and ≥ 10 `<br>` — finds **1** (p.308, share 0.99, on both copies of the bundle; the 4e (2025) bundle renders the same table as a 27×1 stacked column with 58 `<br>`, share 0.58 — a different shape, not this class). **24 STACKED cells** elsewhere (a cell of ≥ 10 `<br>` beside other filled cells, share ≤ 0.58: Ashby's matrices, Valentine's bullet cells, DDIA's one) are not this class and read fine as tables.
- **The census counts it now** (Observed, private `measurement/probes/table_census.py`: `crammed_measure()` → per-table `max_br`, `largest_cell_share`, `crammed`; the summary's `crammed` and `crammed_tables`): IV University Edition **152 tables · 4 stubs · crammed 1** (line 5476, 99 `<br>`, share 0.99); Valentine 81 · 1 · 0; Ashby 55 · 0 · 0. ruff clean.
- **The rule WITHHELD** (judged): one instance on the shelf does not earn an unstack pass — the same judgement as E5's stubs and S150's label OCR route; when the counter reads more than one on a future book the rule has its measure ready (the row keeps it). The row's status appended; not struck.
- **CI on E25's commit** is being observed in the background (`ci_observe.py c3fd4e6`); its result is in the E27 section or the carry-over, whichever comes first.

## E27 — register sweep #3: B10, F12, F4 struck (09:55–10:0xZ)

- **B10** (Observed): the row asked for "clustering that follows stroke geometry … not a threshold nudge; needs its own calibration". E25 built the smaller move the measure justified (anchored zero-area lines; the table veto disqualified on non-rectilinear clusters) and calibrated it on every anchor before/after — the two specimens surface, nothing else moves. Struck as ANSWERED, with the residue named (stroke-geometry clustering not built; a rectilinear chart still reads as a table).
- **F12** (Observed): "SYM-051(b) cites RAM 15.9 GB; the machine measures 31.9 GB — resolve by asking Rab whether RAM was added". The library already holds his answer: `desktop-machine.md` records the 2×16 GB G.Skill upgrade on **2026-08-21**, replacing 2×8 GB Corsair, with the warning "anything recalling 16 GB predates 2026-08-21"; today's `wmic memorychip` reads two DIMMs of 17,179,869,184 bytes, F4-3600C18-16GVK at 3200. The 15.9 GB reading was true at its moment. SYM-051's row annotated (cause (b) partially closed by the hardware; no re-measure under load since); F12 struck.
- **F4** (Observed): S82's ledger row says 2026-08-16, its file 2026-08-15 — the file's own MUSTER stamp reads `2026-08-16T03:42:24Z` (= 08-15 23:42 EDT) and git closes it at 08-16 00:21 EDT. One moment in two clocks: files are named by the local date at open, ledger rows by the UTC date. Both right; nothing edited; struck with the convention stated.
- **Judged and not taken:** B2 (`.agents/` delete or keep — his decision, the row says so); B11 (a widget JS wording drift + tripwire — live only after his rebuild and adoption); B16 (dead manifest fields — a decision); A29 (governance suites in CI — which suites is his to sign).
- **The register:** 118 → 115 open (the muster's counter).

## E28 — a prose-completeness probe, and what the shelf's bundles lost (09:58–10:1xZ)

- **The instrument** (Observed, private `measurement/probes/prose_loss.py <bundle> <pdf> --out <dir>`): for every page of the source PDF, every prose line (pymupdf text, ≥ 40 chars, a 4-letter word, not an equation, not furniture repeating on ≥ 3 pages) searched in the bundle's markdown by three 8-word windows (head, middle, tail) with tags stripped (real tags only — a bare `<` in prose, "Market cap <$1 billion", had eaten thousands of lines on the first run), link targets dropped (DDIA's references), the fi/fl/ff ligatures folded (Zero to One). Numerator missing lines, denominator lines tried, per page and per book; report-only; CPU. Born of E24's p.757.
- **The shelf** (Observed, `sittings/S157/e28_run_shelf.sh` → `e28/<tag>/summary.json`): IV University Edition copy 1 **349 of 27087 lines (1.29%), 24 pages ≥ 3**; copy 2 (`(1)`) **277 of 27087 lines (1.02%), 20 pages ≥ 3**; IV 4e (2025) 145 of 27830 lines (0.52%), 1 pages ≥ 3; Ashby 231 of 8182 lines (2.82%), 27 pages ≥ 3; DDIA 132 of 18112 lines (0.73%), 9 pages ≥ 3; Zero to One 11 of 3383 lines (0.33%), 2 pages ≥ 3; Book of Models 28 of 1735 lines (1.61%), 3 pages ≥ 3; claude-code 12 of 1694 lines (0.71%), 0 pages ≥ 3. The scan-lane books cannot be measured this way (their page text IS the OCR).
- **What the worst pages are** (Observed by `grep -F` on the bundle and by the lane's look at the rendered page): IV UE copy 1 p.1174 — the whole body of *A POSTSCRIPT ON VALUE ENHANCEMENT* (three paragraphs and a bullet list, 25 of the page's 42 lines) is gone and its HEADING stands alone above *Conclusion*; p.129 (22 lines), p.788 (18), p.1037 (18), p.1022 (14), p.1036 (13), p.212 (11), p.917 (11) the same shape. Copy 2 loses DIFFERENT pages (p.44 28 of 30, p.917, p.670) and shares p.129, p.212, p.757. The 4e (2025) bundle of the same author's text carries every one of those passages.
- **Where the loss happened** (Observed, the two copies' manifests): the CONVERT stage (Marker, pymupdf witness) is identical on both copies — survival 0.9333 / 0.9334, the same 25 runs, the largest **154 words** (`year. valuing famous using these inputs` — p.757's Illustration 20.7 box: that loss is Marker's); the ANALYST stage's runs differ between the copies and carry the whole sections — **588, 576, 432, 348, 324, 312 words** on copy 1 (`the value of a firm has three components`, `been around. pathways for bitcoin`, `service firms any firm that provides financial products`…), **576, 372, 312, 240** on copy 2. Both bundles were converted 2026-08-31 / 09-01 by converter `0.1.0-desktop` with qwen3:8b's readability program — BEFORE the S140 acceptor (J46, 2026-09-12) — and copy 1 took 900 of its 955 chunks from the resume journal. Both carry the audit's verdict `fail`; the survival audit's runs list already named these losses (with `page: null` — the probe supplies the pages).
- **Today's acceptor against the class** (Observed, `edit_whitelist.reconcile` under `FULL_TABLES` on the 4e text): the 422-word postscript dropped whole → REVERTED whole (labelled a substitution); one sentence dropped → reverted (a deletion). A tripwire added: `edit_whitelist_selftest.py` case 29, a 40-sentence section between two headings dropped → 609 words back, nothing accepted — **54/54**. Conversions since S140 are guarded against this class; the shelf's pre-S140 bundles are not, and re-conversion is the remedy (the `held-books/reconvert-four` row, his card).
- **The probe's residue, declared:** a table of contents with dot leaders (Ashby p.4, DDIA p.11–12) and a reference list read as "missing" when the bundle carries them re-shaped — formatting, not loss; Ashby's 2.82% is a 1956 typeset whose text layer and Marker's OCR may disagree on hyphenation — unverified by the lane; the threshold "≥ 3 lines" is a reading aid, not a verdict; whether a missing line was dropped by Marker or by the analyst is answered by the manifest's two audit blocks, not by the probe.
- **The register:** a new row `analyst/whole-sections-lost-pre-acceptor`; `held-books/reconvert-four` and `converter/lost-paragraph-in-illustration-box` get their status appended.

## E29 — the probe over the held bundles: the "before" a re-conversion needs (10:08–10:1xZ)

- **The held shelf** (Observed, `ml/library/held/`, eight bundles by source sha; `sittings/S157/e29_run_held.sh` → `e29/<sha8>/summary.json`; the two scan-lane holds, Valentine `b6fbdd75` and Diagnosing `b7b711d4`, skipped and said): IV 4e (2025) `0d68f0e0` (converted 2026-08-01, verdict fail) **349 of 27830 (1.25%), 20 pages ≥ 3** — p.1151 *A Postscript on Value Enhancement* gone under its heading (the SAME section E28 found gone in the University Edition's copy 1 at p.1174 — two conversions of two editions, one section, the analyst's), p.771 the financial-service-firms paragraph (E28's p.788), p.36 the copyright page; IV University Edition `14c66834` (2026-09-01, fail) **277 of 27087 (1.02%), 20 pages ≥ 3** — byte-for-byte the anchor's copy 2 (the probe is deterministic on the same bundle); Ashby `26bd434d` (2026-08-25, fail) 231 of 8182 (2.82%), 27 pages ≥ 3 (its p.4 is the contents page); Book of Models `c5afd9ed` (2026-07-21, flag) 28 of 1735 (1.61%), 3 pages ≥ 3; DDIA `fc1f068c` (2026-09-09, fail) 131 of 18112 (0.72%), 10 pages ≥ 3; **Zero to One `cb7e026f` (2026-09-12 — the acceptor's era; pass) 1 of 3383 (0.03%), 0 pages ≥ 3** against the anchor's older conversion of the same book at 11 missing on 2 pages (Observed; one book, two conversions — consistent with the acceptor reverting the drops, not proof of it).
- **What this is for:** `held-books/reconvert-four` asks for Ashby, Diagnosing and the two Damodarans re-converted with the promoted acceptor "scored beside their S142 numbers" — these summaries are the prose-side numbers to score beside, per page, with the missing lines named (`e29/<sha8>/pages.json`). The before exists now; the after is his card.
- **Not this episode's:** the re-conversions (his); why the analyst drops that one postscript twice (the analyst's owner — a specimen worth his eye: `the value of a firm has three components…`).

## E30 — B23's residue: the 4e (2025) bundle's 49 uncovered pages adjudicated (10:12–10:3xZ)

- **The apparatus, generalised** (Observed, `sittings/S157/e30_render_packets.py` from E24's; the E24 flaws paid: the plant entries carry their REAL geometry from the page's own images — no null area to give them away; the excerpt locator learned this bundle's two id namespaces): on the SYM-050 doubled-offset bundle the ASSET ids are doubled (asset 912 is page 513 — its image sits there and page 913 has none; the S106 formula holds) while the SPAN anchors are plain (`page-413` sits beside p.414's text, `page-501` beside p.502's, `page-681` beside p.682's) — the formula is the assets' repair, not the spans'; with that, 49 of 49 pages located (43 by span, 11 by a line; 0 disagreements between the two locators). The brief (`e30_reader_brief.md`) passed `fleet_law_check --brief` 5/5; the packets and `plants.json` written before the summons; Workflow `wf_8334268a-bb5`, five Sonnet lanes, every page read twice, one planted control per lane (the five largest Figure assets, mapped: pages 513, 228, 847, 273, 638); 8.6 min, 222 tool uses.
- **The fold** (Observed, `e30_adjudicate.py --mine`, `e30/adjudication.json`): **49 pages · 48 agreed at the class level · 1 disagreement decided by the lane · 10 lost figures · 39 not figures** (illustration boxes with tables 15, boxes 19, boxes with equations 3, a box with prose 1, a table 1). Absent: p.191 (the ER event-window timeline, a 279×44 pt raster — the lane's look: L1 called it a figure, L5 typeset rules; the crop shows an embedded image and the excerpt carries nothing between the formula and the *where ERjt* line; the item-2 timeline on p.190 IS carried as an image), p.343 FIGURE 9.3, p.414 FIGURE 11.4, p.423 FIGURE 11.7. Flattened: p.81 FIGURE 3.1 (a garbled fragment table), p.253 FIGURE 7.1 (headed prose), p.333 FIGURE 9.1 and p.334 FIGURE 9.2 (pipe tables), p.551 FIGURE 15.1 *Valuing Amgen — May 2024* (a spreadsheet screenshot as a large pipe table); p.454 FIGURE 12.3 both (one lane FIGURE, one FIGURE-FLATTENED — the ten Myth/Truth boxes gone, the central equation carried; S106 called it "partial" and that is the shape). Tags 97 Observed, 1 Inferred; in_bundle 93 carried, 5 partial.
- **Against E24's University Edition** (Observed, the two folds): the same nine figures lost on both editions' conversions (3.1, the timeline, 7.1, 9.1, 9.2, 9.3, 11.4, 11.7, 12.3); the UE also lost 20.12+20.13 and 24.1, the 4e also 15.1 — each edition's extra losses are pages the other bundle covers or does not flag. The P-2 specimen count on two clean-mapped adjudications: **12 figures on 11 pages (UE) and 10 on 10 (4e)**, with the nine in common.
- **The law on this fleet** (Observed, `e30/fleet_law.json`): 30 checks · 1 red — BRIEF 5/5, PLANT 5/5 (every lane called its plant FIGURE and listed it in `defects`; no lane named the plant's metadata this time), RESIDUE 5/5, NULLS 5/5 (20/21/21/21/20 ids once each), TAGS 5/5; **LENS L2~L3 identical over their 10 shared ids** — read as one lens: those ten (p.521–682) count as one reading; the lost one among them, p.551, is the spreadsheet-screenshot class E24 saw at p.780 and the lane accepts the readers' shape without a look of its own — Inferred, said.
- **B23**: struck at E25 with this as its residue; the residue is paid. The row gets the note. **Not this episode's:** the flattened class (a converter policy, his); the S106 asset-id repair's own correctness beyond the three pages the lane checked (Inferred to hold: the plants' images sit where the formula says).
- **The process census** over the window: nothing of the lane's alive.

## E31 — the audit's omission runs given a page and a box (10:28–10:4xZ)

- **The gap** (Historical, `windows-converter/marker_blocks.py`'s header, S131): the survival audit's ANALYST-stage omission runs carry `page: null` — the analyst audit compares the analyst's output to Marker's body and knows no pages — so the error-structure protocol's HIGHLIGHT (reason + highlight + solution, docs/29's read-side twin) was "unfillable": twenty-five runs "locatable only by their excerpt text". The convert-stage runs carry pages already (the pymupdf witness).
- **Filled after the fact** (Observed, private `measurement/probes/prose_loss.py --attribute-runs`): each analyst run's excerpt is tried as sliding 4-word windows against every page's MISSING prose lines; a page wins with ≥ 2 windows (one is a coincidence — the convert runs showed it: `in scenario analysis we` matched a page 261 away), and the winning line's bbox comes from pymupdf's text dict. `<out>/runs_attributed.json`. On the IV University Edition: **10 of 25 runs attributed, each with a bbox** — the 432-word run → p.1036 at [75.4, 395.2, 114.6, 408.1], the 588-word bitcoin run → p.1174 at [75.3, 153.1, 533.9, 164.2], the 324-word financial-service run → p.788, the 240-word private-equity run → p.917; the held 4e (2025) 6 of 25; Ashby 5 of 25; DDIA 4 of 25. The unattributed runs are equations, tables and lines under 40 characters — outside the probe's prose definition, said, never guessed.
- **What it is and is not:** the highlight for PROSE omissions, from the outside, on any bundle whose PDF is on the machine — not the analyst audit knowing its pages (that needs the block sidecar `marker_blocks.py` describes, unbuilt). Report-only; CPU.
- **The register:** B15's status appended (the omission class has its locator for prose; the bank entry's "highlight" half is fillable now); B3 re-measured — the acceptance census reads **39 unsigned glitches at 42 sites across 3 lanes** today (the row's 57 at 60 was S109's); glass `--since d0e193b --enforce` clean at HEAD.


## E32 — the close's files, drafted (10:34–10:5xZ)

- **Drafted and committed (private `sittings/S157/`, Observed):** `notes_S157.txt` (five lessons), `closing_msg_S157.txt` (the content commit's message), `row_s157.txt` (the ledger row, FIVE cells, `{SHA}` for the close driver), `sections_close_S157.py` (E<next> gained/lost/missing, §7 §8 §10 §15 §18, the receipts read from every `calls/S157-ep*.md` the journal holds — the episode count discovered, not typed — §15 probed at write time, then the JOURNAL entry, LF). A "close" from him is the chain in the carry-over §6 and nothing else.
- **The receipts, checked:** 30 receipt files over the episodes so far; the windows are contiguous from S156's whole receipt's end (E6 lies inside ep7's window, E12 inside ep13's — two receipts cover two episodes each; no minute unreceipted).
- **The code notes, checked:** `git diff --name-only d0e193b..HEAD` lists 30 public files; the code notes carry 30 blocks; the two lists are identical (`comm`, both directions empty).
- **Not this episode's:** the close itself (his word); the sitting continues on his standing word until then.


## E33 — the memory library's three owed updates (10:40–10:4xZ)

- **Written and pushed** (Observed, the library at `878400c`; MEMORY.md unchanged at 17,338 bytes — the index lines already point at the three files): `circle-skill.md` (the skill is tracked in the repo since E14, with its selftest and the WITHSTOOD/BLOCKED rule), `installed-exe-state.md` (the adoption lineage is a repo record since E12 — `coordination/adoptions.jsonl`, the receipt tool, the card's row), `file-portal-private-workshop.md` (the S155–S157 paragraph: the sittings convention, the four promoted instruments, the two probes, the lessons paid this run and the fleets' shape).
- **Why now and not at the close:** the facts are durable and independent of the close; the close's own memory block (the TIME-STATE fold and the lockstep) stays the close's.
- **Not this episode's:** MEMORY.md's TIME-STATE (moves at the close, in lockstep with the ledger row).

## E34 — the plan and the Circle's bequest brought current (10:42–10:5xZ)

- **docs/41** (Observed, append-only, CRLF): an S157 addendum after the plan's last line — P-1 fixed at E25 and re-measured on clean bundles at E24; P-2's specimens enumerated at E24/E30 with the flattened class named as §12's third framing; a P-3-shaped text-loss locator recorded as existing (private, report-only) and what it found. The frames above it are untouched.
- **docs/45** (Observed, append-only, CRLF): a §7 "Collected — S157, not by a Circle" under the §6 bequest: items 3, 4 and 5 collected with their episodes; items 1 and 2 left to the next Circle by name.
- **Why:** a plan whose measured facts live only in a session record is a plan the next reader will not find; the addenda point at the records and the private evidence, and change no frame.

- **Correction (same episode):** docs/45 is an LF file; the addendum was appended in the file's own ending (the first run of the script assumed CRLF and refused — fixed to detect the ending; idempotent on the heading).

## E35 — the frame veto calibrated; the region kind is the Damodaran verdict (10:45–11:0xZ)

- **The finding the two adjudications hand back** (Observed, `e24/adjudication.json` × `e24/coverage_A.json`, `e30/adjudication.json` × the 4e report): on both Investment Valuation bundles the region KIND is the verdict — every uncovered page whose regions were raster was a lost figure (10 + 1 raster+vector on the UE, 10 on the 4e: **21 of 21**) and every vector-only uncovered page was an ILLUSTRATION box, not a figure (**65 of 65 and 39 of 39**). The S104 header's third limitation ("Damodaran's ILLUSTRATION frames survive all three vetoes") is that whole class.
- **The frame veto, calibrated instead of tuned** (Observed, `sittings/S157/e35_frame_probe.py` → `e35/frame_probe.json`): the veto's own number, accounted = min(1, text_coverage + covered_frac(tables)), on the 104 adjudicated boxes against 203 surviving vector regions of the diagram books (Book of Models 90, Ashby 8, DDIA 105 — every one covered by its asset; Zero to One has none): boxes min / median / max **0.025 / 0.449 / 0.595**, figures **0.000 / 0.000 / 0.269**. A lever line `accounted_for=0.35` would veto **63 of 104** boxes and hide **0 of 203** figures with 0.08 of margin over the figures' maximum; 0.30 vetoes 80 and hides 0 with 0.03; 0.25 hides two (Cyb p.81, Ashby p.126). **The default stays 0.60** — the lever's value is the operator's line (docs/18 §2), and the calibration set is named so the line can be written with its consequence known. The 41 boxes under 0.35 are mostly-empty frames (a heading and one paragraph over blank space — p.805, 940, 945); no text-density threshold reaches them, the kind does.
- **Built** (Observed, `windows-converter/figure_coverage.py`): the report's `uncovered_by_kind` (a count per page-kind: `raster`, `vector`, `raster+vector`; never a verdict — a diagram book's figures are vector); the header's two dated notes (the calibration; the kind-as-verdict finding with its scope); `figure_coverage_selftest.py` case 44 (the diagram-over-grid page counts as one vector page) — **45/45**; the A4 registry regenerated for the new key (`--check` red, `--write`, PASS); `coverage_rescore_selftest` 8/8; ruff clean.
- **Also observed, for the struck row `library/damodaran-bundle-misplaced`:** `held/0d68f0e02293970c/` sits at the TOP level of `held/` today (folder mtime 2026-08-06) and nothing is nested under the Cybernetics hold (its folder mtime 2026-09-15T00:01Z); the pipeline's three events for the id are pending 08-01, failed 08-03 (ship: tar 1, ssh 255), resolved-held 08-03 — no move event. Whether S153's "nested" reading saw a copy since removed or a listing artefact is not decidable from here; the row gets the dated observation.
- **Not this episode's:** the lever line (his); the mostly-empty-frame class (a kind-aware triage would name it — a report change for a later episode if wanted); the flattened class's disposition (A5).


## E36 — the prose losses read by stage, per bundle (10:50–11:0xZ)

- **The table** (Observed: each bundle's manifest for the two survival numbers and the verdict; the probe's summaries from E28/E29 for the rest):

| book | converted | verdict | convert survival | analyst survival | probe missing | pages >= 3 |
|---|---|---|---|---|---|---|
| IV UE (anchor copy 1) | 2026-08-31 | fail | 0.9333 | 0.9374 | 349 of 27087 (1.29%) | 24 |
| IV UE (anchor copy 2 = held 14c66834) | 2026-09-01 | fail | 0.9334 | 0.9402 | 277 of 27087 (1.02%) | 20 |
| IV 4e (2025) anchor | 2026-08-01 | fail | 0.927 | None | 145 of 27830 (0.52%) | 1 |
| IV 4e (2025) held 0d68f0e0 | 2026-08-01 | fail | 0.927 | 0.9525 | 349 of 27830 (1.25%) | 20 |
| Ashby (anchor = held 26bd434d) | 2026-08-25 | fail | 0.8582 | 0.9452 | 231 of 8182 (2.82%) | 27 |
| DDIA anchor | 2026-09-06 | fail | 0.8559 | 0.9683 | 132 of 18112 (0.73%) | 9 |
| DDIA held fc1f068c | 2026-09-09 | fail | 0.8559 | 0.9718 | 131 of 18112 (0.72%) | 10 |
| Zero to One anchor (older) | 2026-09-12 | fail | 0.9955 | 0.9947 | 11 of 3383 (0.33%) | 2 |
| Zero to One held cb7e026f (09-12, post-acceptor) | 2026-09-12 | pass | 0.9955 | 0.9947 | 1 of 3383 (0.03%) | 0 |
| Book of Models anchor | 2026-07-21 | fail | 0.6884 | None | 28 of 1735 (1.61%) | 3 |
| claude-code anchor | 2026-07-30 | flag | 0.9913 | None | 12 of 1694 (0.71%) | 0 |

- **Three readings the table allows** (Observed, paired on the same book):
  1. **The 4e (2025) pair** — the anchor copy is Marker-only (no analyst block; probe 145 lines, 1 page) and the held `0d68f0e0` is the SAME Marker output plus the analyst (converted the same minute; probe 349 lines, 20 pages): the analyst's damage on that book, isolated — **204 prose lines on 19 pages**, whole sections, 0.7% of the prose; Marker's own share 0.5%.
  2. **The Zero to One pair** — the anchor copy and the held `cb7e026f` share the same convert and analyst survivals (0.9955 / 0.9947) and the held one was RE-SHIPPED on 09-12/09-13 (the J42 re-analysis under the acceptor): the probe reads **11 → 1** missing lines. The acceptor's effect on one book, measured, not inferred from its selftest.
  3. **Ashby** — convert survival 0.8582 with the analyst at 0.9452: the loss is Marker's (1956 typesetting; `grep -F` finds p.56's paragraph absent and p.51's list re-lettered f/g where the page says g/h — an OCR difference, not a loss), the opposite shape from the University Edition's. DDIA's convert 0.8559 beside a probe of 0.73% says the witness counts code and figure text the prose probe does not — two instruments, two denominators, both named.
- **Not a class** (Observed, `e28/ivue/pages.json` × the pages' own text): pages opening with ILLUSTRATION lose a prose line at 13.0% (24 of 185) against 9.5% (113 of 1,192) for other pages, 1.54% vs 1.25% of lines — p.757's lost opening paragraph is an instance, not "Marker drops a box's first paragraph"; the row `converter/lost-paragraph-in-illustration-box` keeps its one specimen and this measure.
- **Not this episode's:** the re-conversions (his card); why the analyst dropped one section twice (its owner).

## E37 — the close rehearsed; the levers gate paid (10:53–11:0xZ)

- **The rehearsal** (Observed, `sittings/S157/e37/close_rehearsal.txt`): `close.sh d0e193b` under the chain's clean-env form (`GIT_TERMINAL_PROMPT=0 GCM_INTERACTIVE=never`), read-only, **exit 0 in 72 s** — the card: DIFF 32 files since the pin, dirty 0, 1 untracked (`.codex/`); GLASS clean since the pin under `--enforce`; RUST skipped (no widget change); CI still running on the E36 commit ("close may proceed; CHECK IT AFTER"); DOCTOR lexical parity 8 = 8, MISSING 0; CENSUS 11 = 11 = 11; REGISTER +47 / −43 lines, STRUCK 38 · ADDED 4, net −34; MEMORY clean, the soft clock durable; DEBT open SYM 43 → 38 (−5); PUSH ahead 0. One row spoke: **LEVERS — five threshold-shaped constants added since the pin with no waiver.**
- **Paid** (Observed, public `567b842`): `ZERO_AREA_MIN_LEN_PT`, `VETO_TABLE_MAX_NONRECT`, `NONRECT_MIN_SPAN_PT` are the DEFAULTS of levers in `LEVER_SPEC` — the gate reads lines, not the table, so each line now says so in the gate's form (what set it, what moves it, that the ceiling restores S104); `OLLAMA_LOG_TAIL_LINES` (E17, a capture size: Rab's word, set on the day's longest failure trace, moves when a capture is seen truncated) and `TRIM_MIN_COLS` (E3: Rab's word) get their waivers. figure_coverage 45/45 · analyst 47/47 · table_geometry 184/184; ruff clean. The second rehearsal (`close_rehearsal_2.txt`): **LEVERS — no unlevered threshold constants added since d0e193b**, exit 0 in 71 s.
- **J4 re-timed** (Observed): the row's 8m37s (S109) was a cargo-cold-target number; today's close.sh runs in 72 s and 71 s with RUST skipped — the muster/relay-gate/echo suites the row named do not run in close.sh (its S109 verifier already said so). The row's status appended; not struck (the cargo path, when the widget changes, is unmeasured today).
- **Why rehearse:** a close that finds its red on his word costs him the wait; a rehearsal finds it on the lane's time. Nothing was written by the rehearsal; the LEVERS fix is the lane's own file discipline, paid before the word.

## E38 — navigation and one status (11:00–11:0xZ)

- **The private measurement README** (Observed): its `probes/` line named four scripts from S141; a second line names the table and prose instruments of S150–S157 with their call forms (`table_census.py`, `table_dryrun.py`, `table_truth_score.py`, `prose_loss.py` and the scan-lane probes), so the next reader finds them without the sitting records.
- **J14** (Observed): the row's Ashby convert-phase fail (`doc_survival 0.8582`) has the prose probe's reading beside it now — 231 of 8,182 prose lines, 27 pages, a Marker-stage loss on 1956 print; the `array` runaway (SYM-056) is a structural class the probe does not see.
- **Not a class, said again:** the mostly-empty ILLUSTRATION frames (41 of the 104 boxes under an accounted 0.35) have no mechanical separator from a figure frame — rectilinearity and path counts are shared by DDIA's 105 frames around rasters and by the Book of Models' rectilinear diagrams (p.15, 45, 53); measured on E35's set, declared, not built.

## E39 — five verifier lanes refute E24–E38; seven findings, every one paid (11:03–11:2xZ)

- **The fleet** (Observed; `sittings/S157/e39_verifier_brief.md` — `fleet_law_check --brief` 5/5; the packets `e39/`: the record's sections E24–E38, the public diffs since `a2483c3` per lens, the private evidence files named per lens; `e39/plants.json` written before the summons; Workflow `wf_10083974-b8b`, five Sonnet lanes with distinct lenses — the figure instrument; the adjudications; the prose probe; the registers; docs, close files and waivers — one planted false sentence in each lane's GROUND; 8 min, 77 tool uses): **5 of 5 plants caught** (each named as the suspected plant with the lines that break it); **67 claims: 48 HELD, 12 BROKEN (five the plants), 5 UNREAD**. The law after (`e39/fleet_law.json`): **25 checks · 0 red** (BRIEF, PLANT, RESIDUE, NULLS not applicable and said, TAGS).
- **The seven real findings, paid:**
  1. **V1-06 (medium) — E25's residue was wrong.** "p.40 of the Book of Models … is still vetoed as a table": under the shipped code its 46 anchored lines bring the axis arrows into one 54-path cluster with 6 non-rectilinear items, the table veto is disqualified, and **p.40 is REPORTED** — the anchors diff's `vetoed_tables: 0` said so, and the residue had read the pre-change probe. Corrected in the module header (a dated note), SYM-049's row, B10's struck row, and here. No rectilinear-chart residue is known on the measured corpus.
  2. **V1-07 (medium) — a citation.** E25 quotes "the University Edition 76 → 76" as if from `e25/anchors_diff.json`; the UE was measured separately (`e25/coverage_UE_after.json` against `e24/coverage_A.json`, 76 → 76, gained none, lost none). The number holds; the file named for it was the wrong one.
  3. **V1-10 (low) — the median.** E35's record says 0.449, the header note 0.448: the 104 boxes' two middle values are 0.448 and 0.449 (the record took the 53rd, the probe's quartile line the 52nd). The median is 0.4485; both places now read as "0.448/0.449, the two middle values".
  4. **V2-07 (medium) — who named the E24 plant's metadata.** E24 says "L3 and L5 also NAMED it, by its metadata"; the results say L2 named the null area in its suspected field and L3 in its residue; L5 suspected p.903, a real lost figure, not its plant; L1 also mentioned the metadata in its residue. So: L1, L2 and L3 saw the signature; L5's suspicion was wrong; every lane still called its plant FIGURE. E24's sentence is corrected here.
  5. **V3-08 (high) — E31 swapped two pages.** The record's "the 432-word run → p.1036 … the 588-word bitcoin run → p.1174" is backwards: `e31/ivue/runs_attributed.json` gives the 588-word bitcoin run → **p.1036** at [75.4, 395.2, 114.6, 408.1] and the 432-word postscript run → **p.1174** at [75.3, 153.1, 533.9, 164.2] (as E28 and E36 say elsewhere). The script filled the sentence from the file's row order, not from the words. Corrected here.
  6. **V5-07 (medium) — the second rehearsal's timing.** E37 says "exit 0 in 71 s"; `e37/close_rehearsal_2.txt` carries no timing line (the first file does, by `tee -a`; the second's 71 s was printed to the terminal only). The number stands as Observed by the lane; the file is not its evidence, and the record now says where the number came from.
  7. **V4-12 (medium) — the lens's own sentence.** "The register counts the record quotes 119 → 116 → 115 → 116" was the lane's lens text, not the record's (the record's only count line is E27's 118 → 115); a packet imprecision, like E24's — the lane read it as a claim and was right to.
- **Not paid (right as written):** the UNREADs are run-time numbers and the private files a lens was not given; V2's and V4's other BROKENs are the plants; nothing else in E24–E38 was contradicted by its evidence.
- **The lesson, again:** a residue is a claim like any other — E25's was written from a probe that ran BEFORE the change it described; the verifier read the after-file and the before-sentence side by side.

## E40 — his word: "I need a sweep on all possible tickets remaining" (15:16–15:4xZ)

- **The word** (Desk c42a1ee4, 15:16:38Z; acknowledged on the Desk at 15:17:32Z).
- **The sweep** (Observed): every OPEN row of `OPEN-TASKS.md` at HEAD — 116 by the muster's rule — in five Sonnet reader packets with one FABRICATED row planted in each (`e40/packets/plants.json` before the summons; the brief `e40_sweep_brief.md` passed `fleet_law_check --brief` 5/5; Workflow `wf_65c0fffc-161`, 8.4 min, 137 tool uses): **5 of 5 plants reported as "not in the register"**, 116 of 116 rows answered once each, every entry tagged; the law after: **25 checks · 0 red**. Each row: its ask, its OWNER now (signature · card · decision · lane · collect · stale), its state at HEAD, what would strike it, any deviation.
- **The count** (the lanes' calls): signature 39 · card 15 · decision 21 · lane 26 · collect 3 · stale 5 — what only he can move: **75 rows**; what a session can do: 26.
- **Seven rows struck on the sweep's evidence, each verified by the lane before the strike** (Observed): F3 (RESOLVED S108; both commits are HEAD's ancestors), F10 (the file absent and never tracked), A41 (RESOLVED S117 by his bless — the row said the bless retires it), J51 and J52 (signed 2026-09-10, executed S121; J52's later OFF state lives in J60), A38 (the held-books product session ran at S142 E1), `instruments-frozen/rows-only` (expired by its own dates). **Register 116 → 109.** Two statuses appended: A32 (its blocker A4 was struck at E4; the assay surface is unbuilt and undesigned — his word on whether it is wanted), J10 (five held → nine, triaged; C0 breathed; Ashby re-converted). One lane correction: L1 read A27 as F5's duplicate — it is the reverse (F5 was struck at E4 AS A27's duplicate); A27 stays, a decision of his.
- **The other surfaces** (Observed, `e40_reconcile.py` → `e40/reconcile.json`): docs/50's board carries 214 rows in its own namespace, 195 with a live status, **84 mapping to no register id** by name (the S108 sign-sheet items, the OK-/VW- quarantine tickets, SYM rows that live in SYMPTOM-INDEX) — generated at S110, not regenerated since; the private `measurement/TICKETS-*.md` (seven files) name 28 ids, **10 not on the register** — six bench/ml-gauge/vision tickets of S145–S147 never filed publicly and four path-shaped false matches.
- **The document:** `sessions/S157-SWEEP-2026-09-15.md` — every row by owner with ask · state · strike-when · tag, the reconciliation, the deviations the lanes reported (28), the residue (theirs and the lane's). Answered on the Desk with the counts and the list of what only he can move.

## E41 — his word: "Work on the ones without my word, see how conflicting they are first. Make sure the field is safe" (15:49–16:0xZ)

- **The word** (Desk 3852476e, 15:49:46Z; acknowledged 15:50:21Z). Read as: the 26 lane-owned rows of the sweep; before building, map their conflicts — with each other, with a signed word, with the live field — and take them in an order that leaves the field safe. **The field** (Observed at 15:5xZ, the process census and the widget's own polls): the watcher `watch_and_convert.py` is alive, the Desk server and its watcher, the gate watcher (`gate.py watch --as Fable`), the widget (its tailscale/ssh receipt polls every cycle), the bench prototype's server; the card carries his desktop apps only; the ThinkPad is offline. Safe means: no build touches what those processes read, nothing ships, no rebuild and no adoption of the widget, nothing on the card, every change behind its tripwire and reachable back.
- **Conflicts found (Observed from the rows' own texts):**
  1. **B19 hides A11.** "The analyst has no llama.cpp call path" is the two-engine choice A11 asks him to make (two engines / one) — B19 built is A11 decided. Not the lane's until A11 is his word. (It also needs llama-server on the card.)
  2. **The `main.js` cluster — B11, J16, J35, B28** all edit the widget's `main.js` (and B11 `room.js`; J35 also `line.rs` and `visual_witness_capture.py`): they must go one after another, never in parallel; none of them is live until his rebuild and adoption — the source edit is safe, the exe on his screen is his. B28 (a v2 feature: status polling + toasts) is the largest and the least asked-for — last, or his word.
  3. **D3 and D4 are one Circle**: D3's items 1–2 (docs/32 §6's prediction re-checked; Family 1's check) and D4 (the same prediction SCORED) — one episode, one fleet.
  4. **J14 and J44 both change what the audit reads** — J14's structural validator (unterminated `array`) and J44's normalisation rungs move survival numbers, hence verdicts, on live books. Their measurement halves are safe (a private probe; the rungs sized offline on the held DDIA); the switch that changes a shipped verdict is his word (the J32-A rungs were signed by him; v3 would be too). Built as: probe first, lever OFF, deltas reported.
  5. **A29 and the close**: adding suites to CI changes the close's own gate (close.sh reads CI's conclusion) — warn-only first, as S108's standard says, and the Windows-only suites (muster's `selftest.sh` probes `tasklist`) cannot run on the Linux runner at all — the row's list needs pruning to what a Linux runner can run.
  6. **B24 and B29's build half touch live state**: events rotation rewrites the file the watcher and widget write; `cargo tauri build` writes the staging path the adoption lineage reads (S114's rebuild destroyed 1CF604CD, ERR-048). Neither without his word. B29's docs half is safe.
  7. **B32's U-items and D5 sit on the ThinkPad or need his hands** (the receiver's allocator, the install script, the widget's config panic): the probes are safe reads and the ThinkPad is offline — record what can be read, fix what is repo-only.
  8. **converter/ceiling-crawl needs Beer's rate on the card; converter/table-crammed-into-one-cell is withheld for its count (E26)** — neither now.
- **The order, safest first, each its own episode with its tripwire:** (1) **B33** — close.sh's two evidence boundaries with negative controls (the close's own safety; nothing live reads close.sh). (2) **J66** — the `desk` skill (this sitting IS the protocol; a skill file + selftest). (3) **B15** — the omission-class bank entry, now that it has a highlight. (4) **F1** — the ledger table's header, tested against the muster parser on a copy before the edit. (5) **B27, D5** — reads first; strike or file. (6) **D3 + D4** — the Circle's two uncollected items, as a fleet with a plant. (7) **A47** — the adversarial challenge of docs/40's theses, as a fleet. (8) **A29** — the Linux-runnable governance suites into CI, warn-only. (9) **J14** as a private probe (the unterminated-environment count over the shelf); **J44**'s deltas re-measured, the switch left to him. (10) **B32** — the probes. (11) **J5** — relay-room's tests. (12) the **main.js cluster** in order B11 → J16 → J35, source only. Not now: B19 (A11), B24, B28, B29's build, J65, J4's cargo path, the two converter tickets.
- **Safety, said as rules for these episodes:** no edit to a file a running process reads (`watch_and_convert.py`, `convert_and_ship.py`, `analyst.py`, `gate.py`, `desk.py`, the events and receipts files); no rebuild, no adoption, no drop; a probe reads and never writes the book's folders; every gate change warn-only first; every change with its negative control; the census after every episode; a red anywhere stops the order.

## E42 — B33, the first of the order: close.sh cannot hang, and names what it cannot read (15:53–16:1xZ)

- **His signed word arrived mid-episode** (Desk 9dd0cb41, 16:05:22Z): "Go. Auto. Persistent. Protocol. Policy. Preplan. Scripts. Private. Read all. Don't leave anything out that needs awareness. Signed." Acknowledged 16:08:24Z; the order of E41 stands under it.
- **Built** (Observed, `.claude/skills/muster/close.sh`, public `f3b4583`): (1) the CI section's `git credential fill` runs noninteractive (`GIT_TERMINAL_PROMPT=0 GCM_INTERACTIVE=never GIT_ASKPASS=/bin/true SSH_ASKPASS=/bin/true`) under `timeout ${FP_CRED_TIMEOUT_S:-20}`; timeout's exit 124 renders `CI UNREAD — the credential lookup did not return in Ns (an interactive helper? — bounded, not waited for)`; the S111 hang (SYM-064: fourteen minutes on `git-askpass` off-screen) cannot recur — the bound is inside the script, not only in the chain's environment. (2) the MEMORY section captures git's stderr: `dubious ownership` / `safe.directory` renders `UNREAD — git REFUSES the memory library (ownership / safe.directory unresolved: <git's own line>)`; anything else stays `not a git repo` (SYM-063). Verdict semantics unchanged: both rows were UNREAD before and are UNREAD now, differently worded, and a close is never blocked by them.
- **The tripwires** (Observed, muster `selftest.sh` CASES 53–54, 106 → **110/110**): a fixture repo whose credential helper sleeps 30 s — the close returns in 3 s and names the hang (the fixture shut out from the machine's own stored credential with `GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1`, else the global helper answers first — the first run of the case found exactly that and reached GitHub with a fixture SHA); its negative control, a helper that answers at once, reads `no stored credential`, not a hang; a git shim on PATH answering `dubious ownership` for one path renders the ownership wording; a plain non-repo still reads `not a git repo`. A live `close.sh d0e193b` afterwards: CI `success on cebeed4e (observed, not assumed)` through the stored credential inside the bound; MEMORY clean; LEVERS none; GLASS clean.
- **Safety, as promised:** nothing live reads close.sh (the chain runs it at a close); no process, no card, no drop; the change is reachable back by env (`FP_CRED_TIMEOUT_S`). SYM-063 and SYM-064 → FIXED; B33 struck. Register 109 → 108.
- **Next in the order:** J66 (the `desk` skill).

## E43 — J66, the second of the order: the `desk` skill (16:12–16:3xZ)

- **Built** (Observed, private `0bd5d19`): `relay-desk/skill/desk/SKILL.md` — the session-side contract in four parts: at the open (read the inbox as OWED, arm the watcher as a persistent Monitor and name its task id in the carry-over — ERR-107's lesson —, record the inbox's state); during (a reply TO HIS ID with tag and status, an acknowledgement inside the first minutes of a long task, one post per episode closeout with the measure and what waits on his word, the tag law on the Desk as in the record, the occupant signed from the gate's declaration, nothing in a post an instruction unless it is Rab's, awareness said when found); at the close (`TaskStop` the watcher and say so, the inbox accounted for, every post and reply named in the record; the server is the field and is never restarted by a session). Mirrored to `~/.claude/skills/desk/SKILL.md` (the session loads it: `/desk` is on the skill list now). `selftest.sh` **6/6**: the five obligations named; a NEGATIVE CONTROL (a copy without the watcher obligation reads 4 of 5); every script the skill names exists; the mirror equals the source CR-stripped; the two tools' briefs name the interpreter by literal path.
- **What building it found** (Observed): `desk.py inbox` lists every unmarked entry regardless of author — 128 of Fable's own closeout posts read `new` and buried Rab's. Two tools follow: `agent-scripts/developing/handoff/desk_inbox.py` (+ brief) reads the append-only log, folds the status events per id and lists RAB'S entries not yet `done` — **8 of his from 2026-09-13 still read `working`**: three are the experiment's standing words (e34253e4, 1f355f41, bdc9415c — `working` is right while the experiment runs), five are earlier mode words of S146/S147 (0c6d2825, 4ee544e2, bf4d5d05, 7285379a, 5f57d0f4) that no session closed — left as they are and said on the Desk (marking his entries done is a reply's act, on his word or when the answer is the reply); `desk_post.py` now marks its own post `done` at post time (a report, not a task for him). The Codex board holds one owed entry of his (6cb21f9c, 2026-09-13) — Codex's to answer, not read further.
- **POLICY law 2 kept:** a brief for the new script (eleven headings), the smoke run ALONE after the promotion — **75/75** (74 + the new script's usage tripwire).
- **Safety:** `desk.py` (the running server's and watcher's source) was NOT edited — the inbox helper reads the log file; `desk_post.py` is not a running process. Register 108 → 107. **Next in the order:** B15 (the omission-class bank entry).

## E44 — B15, the third of the order: the omission-class bank entry (16:19–16:3xZ)

- **The bank** (Observed): `prototypes/repair-bench/signatures.json` — S78 §8.5's signature bank, six entries A–F, each a detector over the zone's markdown window plus reason · highlight · solution; every omission run the bench showed came back `unclassified` because a run has no markdown of its own (the Beer's 18/18, S79). Entry **G, `omission run — text that is not there`** (`detect: omission_run`, tag Inferred): the reason says what the audit measured (a run of adjacent source windows no output window matched; its size, page and first words) and names the two origins E28 measured with their remedies; the highlight sends the operator to the page and the first words (a null page is the audit's own blindness, said, never page 0); the solution forbids typing the words from memory — transcribe from the page at the bench or re-convert; a whole missing section is the readability pass's loss and a re-analysis under the S140 acceptor restores it. Written for an operator who has never opened a session file (the bank's own rule); provenance in `cite`.
- **The detector** (Observed, `bench.py` `_detect`): `omission_run` fires only on a run-shaped site (`words` and `excerpt` present) and returns the run's own measurements as `matched_on`; G leads `_ORDER` — the zone rules read a site's neighbourhood and would call a missing paragraph a wrapped cell (A) or a merged row (D). The existing six entries are unchanged in value (checked against HEAD's file).
- **Tripwires** (Observed): `test_bench_page.py` + 3 (both fixture runs diagnose G — page 757 named, the null page as UNREAD; a degeneration zone still reads E, not G — the NEGATIVE CONTROL; the bank's order) — **85/85**; `acceptance.py` **85/85** over a sandbox copy of the held Valentine (the sandbox removed after). `bench.py` carries a pre-existing unused `io` import (ruff F401) that CI does not lint (prototypes are outside its scope) — left as found, said.
- **Safety:** the bench server was not running (no `bench.py serve` in the process list); the held bundle was copied into the sandbox by the harness, never edited. B15 struck; register 107 → 106. **Next in the order:** F1 (the ledger table's header, on a copy first).

## E45 — F1, the fourth of the order: the Change Ledger's split header (16:23–16:4xZ)

- **The defect** (Observed): `CLAUDE_README.md` — the ledger header at line 1129, rows 1131–1214, then S43's narrative prose (`**Built…**` / `**Next:**` / `**§4 …**`, ~1968–1974), then the S79 → S156 rows from line 1975 to the end with no header above them: a Markdown table needs a header and a delimiter, so the newest 80-odd rows rendered as prose. Flagged S96 §18.1, unrepaired since.
- **The repair** (Observed, private `sittings/S157/e45_f1.py --go`): five lines inserted immediately above the S79 row — a blank, a one-line italic note (what split the table, that E45 inserted this header, the same five columns, append below), a blank, the header row and the delimiter row copied from line 1129–1130. Nothing moved, nothing rewritten: the rows are where they were. CRLF uniform after (2060 CRLF, 0 bare LF, read as bytes).
- **Proved on a copy BEFORE the edit** (Observed): the script runs muster.sh `[3b]`'s own ledger parse — `grep -E '^\| 20[0-9][0-9]-'` piped to the awk that reads the session from the `^S<n>:` cell and the SHA from the last cell — over the original and over the patched copy and asserts the two outputs byte-equal (165 lines, 139 ok rows); a header row and a delimiter never match `^| 20YY-`, so the muster cannot see the insertion. Then the live tripwires: `open.sh` exit 0 with `[3b] LEDGER PARSE ✓ 139/165 rows parsed · 21 discarded` and `[3] HARD clock ✓ d0e193b (S156) == ledger d0e193b, ancestor of HEAD` — the same reading as before the edit; `selftest.sh` **110/110**.
- **What it does not do** (said): the split itself stays — S43's prose sits between the two halves, and moving it would rewrite lines that 60 sessions of records cite by line number (`CLAUDE_README.md:1126`, F1's own row). Two identical headers with a note between is the append-only repair; the rendered check on GitHub after the push is the Verified half.
- **Safety:** no field process reads `CLAUDE_README.md`; the muster scripts were not running. F1 struck; register 106 → 105. **Next in the order:** B27 and D5 (reads), then D3+D4 as one Circle.
- **Deviation, said** (Observed): the push of 676af16 was REFUSED by guard_git — J71 runs `row_check.sh 157` on any push that carries a `CLAUDE_README.md` change, and the reader wants the newest Desktop row to be S157, which exists only at the close (`row_check: ✗ newest Desktop row is S156 (d0e193b, row 165) — expected S157`). The guard is right by its own rule and the rule is broader than its name: it gates the FILE, not the row, so no mid-sitting README repair can reach origin — an awareness item, not a change to the guard (every gate change is warn-only first and his). Holding the commit would have held every later public push (A29's CI observation needs the lane). So the pre-E45 bytes were restored in a NEW commit and `e45_f1.py --go` runs again at the close, right before the row (a step added to the close chain in the carry-over); the proof above stands, the bytes land with the row.

## E46 — B27 and D5, the reads of the order (16:38–16:5xZ)

- **B27, `resume auto-detect-analyzed`** — the one-liner that exists nowhere but the memory queue. **The reading** (Inferred, from S18's queue mechanics in `CLAUDE_README.md` and the code): `convert_and_ship.py --resume <id> --backend local` applies the analyst in place inside `pending/<id>/` and then ships; a failure past the analyst — a ship error — flips the card `failed` "for a retry click"; `resume()` (line ~2078) applies the analyst whenever the backend is local/gemini and never asks whether the parked bundle is already analyst output; `apply_analyst` builds the frontmatter as `"---\nanalyst:…" + head + "---\n"` — the prior head, `analyst:` block included, survives under the new one. **Measured** (Observed, private `sittings/S157/e46_b27_probe.py --go` — the marker-env interpreter, `FP_PIPELINE` at a temp dir before the import as the selftest does, `analyst.process` a fake, `ship` raising once with the offline ThinkPad's own error, no GPU, no edit to the module): after the failed first resume the note carried one `analyst:` block and one pass; after the retry click, **two blocks, two passes, two anchor copies (`book [analyst-local]`, `book [analyst-local] (1)`), the analyst called twice** — the manifest keeps only the last (`manifest["analyst"] = meta`), so note and manifest disagree about how many times it ran. NEGATIVE CONTROL: `--backend none` twice → zero calls, no anchor. On the field the second pass is ~10 minutes on the card and the S140 acceptor then judges the analyst's output against itself. **The fix** (Intended, written, not applied): two branches before `apply_analyst` — same backend as the parked pass → ship what is there and say so as an `analyst/skipped` event; a different backend → refused toward `--reanalyze` (J42's road, which restores the pre-analyst body from the sidecar); with its tripwire drafted for `convert_and_ship_selftest.py` (the retry runs no second analyst; the skip is an event; the negative control: a gemini click on a local-analysed parked bundle is refused and runs nothing). `e46/b27_resume_guard.patch.md`. **Held:** `convert_and_ship.py` is on this sitting's never-edit list (E41, told on the Desk) — one word applies both, then the selftest and glass `--enforce` (a new event kind may want a disposition). B27 statused, not struck.
- **D5, SYM-039's fixes from the ThinkPad's §2.6** (Observed): three were assigned — SYM-014's guard text (corrected at S86, the row cites `bundle.py:72`), SYM-018's stale note (annotated 2026-08-10 with SYM-020), and `CLAUDE.md`'s test counts + a "rsync over tailscale ssh" component line. The third has no target on this machine: `git log --all -- CLAUDE.md` is empty (the brain has been `CLAUDE_README.md` since its creation 0dcdac0), the counts 24/51 appear only in dated ledger rows and narrative (Historical by construction), the rsync phrase is in no tracked doc outside `sessions/` and the message itself; the ThinkPad's own untracked copy is UNREAD (offline since ~23:00Z). D5 struck; SYM-039 `fixed` with the residue named — the class (a hand-typed number contradicting the code) stays docs/21 §3's standing rule, not a row.
- **Safety:** reads only; the probe's pipeline root was a temp dir under `FP_PIPELINE` (nothing in the real `pending/`, `anchor/`, `held/` touched — the module resolves its roots at import and the env was set first, the selftest's own isolation). Register 105 → 104; SYM open −1. **Next in the order:** D3+D4 as one Circle (a fleet with a plant).

## E47 — D3 + D4 as one Circle: docs/32 §6's prediction scored; Family 1's check (16:45–17:3xZ)

- **The commission** (Phase 1, falsifiable): does docs/32 §6's prediction — *the next instance will appear in whatever we build next to enforce rules 1–3* — confirm against what was built since S105 (`40a643a`), and how many times; and does anything now compare a claim to the probe that produced it (docs/45 §6.2)? An ASSESSMENT Circle: no lane edits; the deliverable is findings. **The reference** (Phase 2): docs/32 §5–6 and docs/45 §1/§6, quoted verbatim in the brief (private `sittings/S157/e47_circle_brief.md`, committed 9497c59 BEFORE the summons; `fleet_law_check --brief` 5/5). **The seed** (Phase 3, predating the lanes): S1 J71 gates the file not the row; S2 the analyst selftest on the card (SYM-054); S3 could a header row be read as a ledger row; SYM-100/101/113/054 CONFIRMS-shaped; the S109 byte-equality rule FALSE-POSITIVE-shaped.
- **The fleet** (Observed): five Sonnet lanes, read-only, no internet, one planted control each in the packet's own shape (a guard file that does not exist ×2, SYM-134, ERR-2026-09-14-110, `claim_probe_check.py`) — **5/5 caught**; 782k tokens, 131 tool calls, 26 min. `fleet_law_check` on the raw results: 26 checks · 5 red — two PLANT reds were the checker's exact-id rule against a lane's prose (`<id> — PLANTED: …`), one NULLS red L2's doubled entries (both copies agree on every verdict — asserted), one L4's unanswered ERR-2026-09-12-097, one L5's seven F1 rows answered in prose; normalised (defects → the id, L2 → the last copy): **26 · 2 red**, the last two standing as deviations. Tallies: L1/L2 the 44 guard-shaped files — 39 STATES+TRIPWIRED, 3 STATES-ONLY (`.claude/hooks/check_coordination.py`, `.claude/hooks/warn_heredoc.py`, `prototypes/analyst-lab/edit-whitelist/promotion_check.py`), 2 NOT-A-GUARD; L3 the 78 SYM rows since S105 — **24 CONFIRMS**, 34 PLAIN-DEFECT, 17 FALSE-POSITIVE, 3 NOT-A-GUARD; L4 the 108 ERR rows — **31 CONFIRMS**, 56 NOT-A-GUARD, 15 PLAIN-DEFECT, 6 FALSE-POSITIVE; L5 the mechanisms — 9 COMPARES-CLAIM-TO-PROBE, 10 MEASURES-ONLY.
- **The verifier's half** (Observed, `e47_adjudicate.py --mine`): ten CONFIRMS re-read by the lane in the rows' own cells — SYM-096/113/121/126/055, ERR-088/092/059/106/014 — **10/10 hold** (the sharpest: ERR-092, the chain's `exit 0` under `conclusion=failure`, in the chain ERR-088's fix had just built — the prediction's exact sentence). The seeded suspicions: S1 CONFIRMED and sharpened (the J71 gate runs on any change to the file; `row_check.sh:53` reads only the last five rows and the newest, so an older row corrupted joins the 21 rows `[3b]` already discards — verified in the code by the lane); S2 CONFIRMED (29e5003); S3 REFUTED by construction (the row regex needs `| 20`), which E45's proof had shown the same morning.
- **The verdict on D4 / §6.1** (Verified — two lanes over two registers, the lane's sample, the rows' words): **55 rows since S105** read as a guard passing while its property was broken; at least twenty inside the governance guards built to enforce the rules (close.sh ×6, guard_git ×3, the open card, the registry check, the deferral-gate selftest, the relay-gate suites ×10, the chain ×3, the analyst selftest ×2, the fleet harness, the tracker); and one inside this Circle's own brief — it told L4 `ERROR-BIN.md` is LF; the lane measured CRLF and said so; re-measured: 660 CR lines. *If it confirms again, treat it as a law*: the count now exists in docs/32 §8 and docs/45 §8; **the promotion to a law is his signature** (docs/32 §7), not the lane's. D4 struck as SCORED; D3 struck (all five items collected — three at E24–E31, two here).
- **The verdict on D3 / §6.2** (Verified — L5's inventory read in the code, the lane's check of F1-1 and F1-6's mechanisms): Family 1 has checks for five NARROW claim kinds — a hash/SHA identity (`gate.py beat --verified --probe`, `row_check.sh`, `adoption_receipt.py`, `ledger_check.py`), a file-touched claim (`journal_check.py code_paths_check`; the code-notes `comm`), a fleet's self-report (`fleet_law_check.py`), a register status (`e40_reconcile.py`), a whole record's sentences by the verifier-fleet method (E22/E39; seven slides caught at E39) — the last on commission only. Of docs/45's seven rows, F1-1 and F1-6 would be caught today; **F1-2/3/4/5/7 slide past everything**: nothing reads a sentence's referent against the probe's scope. Said in docs/45 §8; no new register row (the instrument exists; making it continuous is a policy word — Family 2's shape would be to file it and feel discharged).
- **Findings in the lane's own work, first** (Phase 6): the brief's LF claim (VIOLATION of rule 3, caught by a lane); the L5 packet's wrong path for `census_compare.py` and a "private paths" sentence that sent the lane to the wrong repo for six public files (it found them and reported the mismatch). **Findings in the guards** (each re-read in the code by the lane): `check_coordination.py:40-44` exits 0 silently on a failed probe (HONEST-BUT-CONFUSING — intent stated in the comment, invisible from outside; no tripwire); `record_missing()` at `guard_git.py:451` accepts a zero-byte record (FRAGILE); the J71 gate + `row_check.sh:53` + `[3b]`'s unpinned discard count (EROSION); `warn_heredoc.py` and `promotion_check.py` have no tripwire (rule 2 unmet). **Gate** (Phase 7): all four are guard changes — warn-only first, his lever; RECOMMENDED, not built: pin `[3b]`'s discard count the way the DEBT gate pins open SYMs; a selftest case with an empty record; tripwires for the two hooks. Zero mutation — a success mode of the Circle.
- **Residue** (said): the lanes classified from the rows' own words and did not re-run the guards; the two registers' overlaps (one incident filed twice) are not de-duplicated — 55 is rows, not incidents; L4 answered 108/109; L5's F1 verdicts are prose. **Safety:** reads only; nothing on the card; the field's files untouched. Register 104 → 102. **Next in the order:** A47 (an adversarial challenge of docs/40's theses — a fleet), then A29.

## E48 — A47: the adversary over docs/40's four theses and the premises under them (17:23–18:0xZ)

- **The commission** (Rab, S111 §30 → A47): each thesis carries its author's objections; the missing signal is "a differently-prioritized reader and a falsifying probe." **The shape** (Observed): the brief (`sittings/S157/e48_adversary_brief.md`, committed 0dd9727 BEFORE the summons; `fleet_law_check --brief` 5/5) and five packets — LA thesis A for the operator's time, LB thesis B for crash safety as the record measured it, LC thesis C for the consumer, LD thesis D for the output as it is, LE §0's six premises and §8's stages for time — each with the thesis text quoted verbatim by line, three to seven claims with read-only probes, and ONE planted sentence attributed to docs/40 at a line that says something else; five Sonnet lanes, read-only, no internet; 546k tokens, 124 tool calls, 6 min. **Plants 5/5 caught**; the law on the raw results 25 · 4 red (the checker's exact-id rule against prose defect ids — the fourth fleet in a row; the lanes' instruments fired), normalised 25 · 0 red. The verdict vectors: A — A-1 WEAKENED, A-2 REFUTED, A-3 HOLDS, A-4 HOLDS; B — B-1 HOLDS, B-2 WEAKENED, B-3 WEAKENED, B-4 HOLDS; C — C-1 HOLDS, C-2 WEAKENED, C-3 HOLDS, C-4 WEAKENED; D — D-1 HOLDS, D-2 WEAKENED, D-3 HOLDS, D-4 WEAKENED; E — E-1 HOLDS, E-2 WEAKENED, E-3 WEAKENED, E-4 HOLDS, E-5 WEAKENED, E-6 HOLDS, E-7 WEAKENED.
- **The verifier's half** (Observed): the one number that looked wrong was — LD's `images_total=1` over 25 bundles. The lane counted CommonMark `![alt](src)`; the shipped markdown embeds figures as Obsidian wikilinks `![[assets/…]]` — **2,558 across the shelf (313 on the Damodaran 4e), 0 with an alt segment**, the form has no alt slot. The lane's headline ("figures aren't shipped") was wrong; the corrected finding is stronger for the opposite reason and is what docs/40 §12 carries. The lane re-measured the rest (`e48_d1_remeasure.py`, fenced blocks excluded): 10715 headings, 1281 level skips (12.0% — the lane's own 2% kill threshold not met, its headline stands), 1241 delimited pipe tables, 427 pipe blocks without a delimiter (the lane's 1,842/102 counted rows differently — both said). Two more experiments were already run: B's (E46's probe: two `analyst:` blocks, as predicted) and E's greps (buyer: one self-referential file; run_id: 0/25 manifests). A's (the operator's time to assemble a timeline by hand) is his to run — it measures him, not the lane. C's grep for an outbound send: 0.
- **What the adversary found** (Verified — the lanes' probes, re-read by the lane where the claim was load-bearing): no thesis refuted whole; each lost its strongest advantage sentence — A's value already reachable by a filename/hash join and the five `kind`s absent as typed artifacts (attempt identity truly absent; "an envelope can standardize lies" is the record's own class, ERR-005/014/059); B's "reliable directory ownership" against six unreliable transitions on the record, while the live hazard (E46) needed a key, not a kernel; C's no consumer, no signature, no human action in the bundle; D's half the bank maps to accessibility classes, no checker on the machine, the validate half of the thesis has a foothold and the produce half none. §0's premises hold on identity, acknowledgement and buyers; weakened where the Room already joins the five things as rows and freshness ages already render, and where reviewer findings reach later conversions as coded rules. §8's stages all moved under other names; none complete; **Stage 0's evaluable promise is still unwritten** — the adversary's next target. Written to docs/40 §12 (LF, append-only, §0–§11 untouched). A47 struck; A33 (the §10 gates) stays his.
- **Findings in the lane's own work** (first): the collector script (`e47_collect.py`) pointed at E48's journal wrote INTO `e47/results.json` (a hard-coded directory) — E47's results and law file were overwritten and restored from the private HEAD (`git show`, content byte-equal ignoring line endings) before E48's collector was made; a per-episode script with a hard-coded sibling's path is ERR-109's family (a tool aimed at the wrong file) — the E48 collector carries its own directory. Said, not filed as an ERR: no record was lost, the private commit dea1e75 held the bytes.
- **Safety:** reads only; the anchor shelf read, never written; drop/pending/held not opened by any lane; nothing on the card. Register 102 → 101. **Next in the order:** A29 (the governance suites into CI, warn-only; the smallest slice that Linux can run).
