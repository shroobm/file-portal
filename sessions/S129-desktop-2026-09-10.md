# S129 — Desktop — 2026-09-10 — The Reading Bench, and the skill that can rebuild it

*Record before the act (docs/28; muster Phase 6; J63's guards). §1 is Rab's words and is left unedited for the rest of the session.*

## §1 Session Intent (Rab's words, verbatim, left unedited)

> You're going to have to make an artifact that shows what you're describing using what's available to you. Im sorry.

> remember to make this artifact named based on the fact this is a super important artifact, you get to name it according to your understanding of it, and it's purpose, make it into a literal skill, based on what you did and did not do, and what the importance and reason why this skill was made, with the context provided.

**The context these words sit in (his, this sitting):** after "Wanna just talk to me for a bit" he said his goals had gone unclear — the converter's machine-learning problem was not arriving, training a model per document type would narrow it to that type, scans would break it, and the thing built so far was "junk-rat" out of available models yet came out ready for an operator, a reader, an analyst. Then he described what the converter should feel like: a pop-up with two panels, the section the model reads and the section it writes, an assembly line, gauges for fidelity and omissions, slice by slice; two shards joined by a family of category ids; a human crop tool that sets what the model reads on a scan; a training panel where an operator labels page images with crop tools and trains a small net. And: "we should capture the polygon numbers, the geometry, explain that a bit more."

**Standing:** *"pace yourself … do not repeat a mistake that is in the errors you know of"* (S123); *"eyes on deck. Just you to ride it"* (S124); *"stop giving stuff to codex for now"* (S125).

## §2 Starting State — `open.sh`, 2026-09-10T17:2xZ (verbatim)

```
════════ MUSTER · OPEN · 2026-09-10T17:54:33Z ════════
[0] IDENTITY
    ledger newest    S128  0de9696
    this session     S129  ·  desktop  ·  2026-09-10
    session marker   coordination/private/session.current = S129 (guard_record / guard_git read it)
    advisory         S129 unnamed in the inherited tree
[1] GROUND
    verifier         8181631B  (repo == ~/.claude)
──────── MUSTER · 2026-09-10T17:54:34Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 85 (tally == hook)
[3a] LEDGER ORDER  ✓ 111 Desktop-lane rows ascend (S16 → S128)
[3b] LEDGER PARSE  ✓ 111/137 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 5 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79 ThinkPad-S123
[3] HARD clock ... ✓ 0de9696 (S128) == ledger 0de9696, ancestor of HEAD
    WORKING TREE . 1 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 8 · anchor 29 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=8
    gpu-lock         absent
    events           311 line(s)
    open-tasks       138 item(s) · last written 66 minutes ago
    error-bin        80 row(s) — read the CLASS table before you probe · last written 11 hours ago
    symptoms         110 row(s), 41 open · last written 11 hours ago
    relay            167 entries · run `gate.py status` for the board
    widget           9096
    widget autostart task 'File Portal widget (J57)' Running — AtLogOn · auto-logon ON (Bndit)
    python procs     4
    ollama           2
    installed exe    C4559805  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 50896d1
    thinkpad         converter=active · vault tip 50896d1 · staging 0 · receipts 14
[3] PIN
    --since          0de9696
    closeout         sessions/S129-desktop-2026-09-10.md
                     absent — SKILL.md Phase 6 creates it BEFORE work, with §1 Intent in Rab's words
════════ mechanical half: clean (exit 0) — judgment half is SKILL.md Phases 2-6 ════════
```

## §3 The pin

`--since 0de9696` (the S128 closing commit; `b8cbf31` row and `5e0ae73` §20 are on origin).

## §4 What was done before this record, and why that is allowed

The artifact was built and published from the scratchpad before this session opened: `extract_geometry.py` read the DDIA bundle's `blocks.json` and `manifest.json` (read-only), `build_geometry_page.py` inlined the data, the page was published as **The Reading Bench** (`https://claude.ai/code/artifact/c3499166-c443-47c5-b57c-3c6fa550b01c`) and sent to Rab as a file. None of that touched a tracked file, so J63's guards had nothing to say and the MUSTER had no session to open — a scratchpad build is not a repo act. The moment Rab said "make it into a literal skill," it became one: this record, then the skill.

## §9 The plan

1. `.claude/skills/reading-bench/` — `SKILL.md` (the judgment half: what the bench is, what it must show, what it must never fake, why it exists, in Rab's words), `extract_geometry.py` and `build_page.py` (the mechanical half, generalized to any bundle), `selftest.py` (a synthetic bundle builds; a blocks.json without polygons is refused; the page names the block count it drew).
2. The skill's own residue named: no page images (the bundle ships figure crops, not page renders — the page is drawn to scale from geometry alone); the analyst's per-chunk scores are not joined to block ids yet; the artifact preview could not be screenshotted (the pane is not signed in; a local file renders as a static snapshot) — the look is Rab's.
3. Close in lockstep.

## §8 Findings

- **F1 — The real page sits under the geometry, and the geometry lands on the print.** Rab, after the first publish: *"can you embed the real pages underneath each of those page section in what it reads, and treat it as an overlay?"* `extract_geometry.py --pdf` now renders each chosen page from the SOURCE PDF with PyMuPDF (the bundle ships figure crops, never page renders) and `build_page.py` lays it under the block rectangles as an `<image>` with an opacity slider. `Observed`: six DDIA pages (161, 31, 58, 10, 1, 5) rendered at 96 dpi = 672 × 882 px each; the PDF's page rect 504 × 661.5 pt against the block page box 504 × 662 — a match within a point on six of six (`box_matches_pdf: true` ×6); the look taken through the pane on the local build (`preview_start reading-bench`, launch.json): on p.161 the Table block outlines the printed `fact_sales_table`, the Caption block sits on "Figure 4-7. Storing relational data by column rather than by row", the Text blocks on their paragraphs. Republished to the same artifact (c3499166) and sent as a file (1.0 MB).
- **F2 — A number in my own skill file was wrong: 139 → 144.** SKILL.md's first draft and this record's §7 say the manifest flags 139 pages. Re-read at the second pass (`count_flagged.py`: `len(pages_flagged) = 144`, `pages_scored = 662`, `doc_survival = 0.8559`): **144**. The rebuilt page's dial said 144 while the prose beside it said 139 — the instrument disagreed with its documentation. Rule 3 of the tag law, in a file whose first law is "real bytes only". SKILL.md corrected with the correction named in place; §7's line stands and this finding points at it. ERR-081 (OVER-CLAIM).
- **F3 — The bench produced its first finding about the converter.** On p.161 three blocks the layout stage found carry NO text in their `html`: `/page/161/SectionHeader/0` (the render shows `fact_sales_table` there), `/page/161/SectionHeader/2` ("Columnar storage layout") and `/page/161/Text/3` (the `date_key:` line) — while the Table block beside them carries its cells. `Observed` on the raw `blocks.json` (`peek_161.py`): across the book 4 SectionHeaders, 21 PageHeaders and 1,289 PageFooters have empty html; the Text block's html is tag-only. `Inferred`, not yet probed: those three sit inside Figure 4-7's region and the figure pass blanked the text under it — the bundle markdown for p.161 would settle whether the analyst ever saw the words "fact_sales_table". This is exactly the gap the two panels exist to show: the left panel has the block, the right panel has nothing.
- **F4 — The pane refuses `file://`, so the look now has a road.** S128 named "the look was Rab's" as residue. A `reading-bench` entry in `.claude/launch.json` serves the skill's gitignored `out/` on 127.0.0.1:7141; the screenshot came through it. Two defects it surfaced before anyone else saw them: `http.server` sends no charset, so the page's `·` and `—` rendered as mojibake until `<meta charset="utf-8">` became the template's first line (harmless under the Artifact wrapper, which adds its own); and the empty-block message called every empty block "a picture or figure" — reworded to what is true (the html is empty; a Picture/Figure ships pixels as an asset; any other kind is what the layout stage wrote).
- **F5 — The guard's sixth shape is filed, not fixed.** An inline `python -c` with braces was denied during the first build (S128); J64 carries the order of work (a reproducing FALSE-DENY case first, then the grammar, run the negatives); SYM-110's row carries the shape. The mechanism is `Inferred` until the case reproduces the deny line.
- **F6 — A filed item was repeated the day after it was filed.** The new selftest crashed under the marker-env (`UnicodeEncodeError` printing a child's stdout tail): the child printed in the locale, the parent decoded UTF-8 with `errors=replace`, the resulting U+FFFD could not be printed on the cp1252 console. J61 (S121 §10, filed S124) is the same defect in the other selftest. Fixed with `PYTHONIOENCODING=utf-8` in the child's env and `sys.stdout.reconfigure` in the parent; J61 annotated with the second instance. S128's "15/15 green" was measured under a console where it happened not to fire — a green that depended on the terminal.

## §10 Known failures / residue

- **The artifact itself was not screenshotted** (the pane is not signed in to claude.ai); the local build is the same bytes and was. `Observed` for the build, `Inferred` for the published page.
- **F3's cause is unprobed**: the bundle's markdown for p.161 and Marker's figure handling would settle whether the empty html is the figure pass or a lost read. The bench shows the gap; it does not yet explain it.
- **No per-block fidelity**: the analyst's journal scores are not joined to block ids (law 3 stands; the note on the page stands).
- **The render is a raster at one dpi** under a vector page; the geometry is exact, the picture is 96 dpi. A crop tool that reads pixels would want a higher dpi or the PDF itself.
- **J64 is owed** (the guard's sixth shape). **J61 now has two instances** and is the cheaper fix.
- `out/` holds a 1 MB build and a 1 MB dataset (gitignored); the artifact is the copy that lasts. Nothing was written to Codex; `.codex/` remains the one untracked item.

## §15 Registers

- **OPEN-TASKS:** J64 filed (the guard's sixth false-deny shape — a case first); J61 annotated with its second instance (F6). Net +1: a session that filed what it found and struck nothing — said out loud.
- **ERROR-BIN:** ERR-2026-09-10-081 OVER-CLAIM (the 139 that was never re-measured; F2). **SYMPTOM-INDEX:** SYM-110's first cell carries the sixth shape. Census: ERR 81/81 · SYM 110/110 placed.
- **The skill:** `.claude/skills/reading-bench/` (SKILL.md, extract_geometry.py, build_page.py, selftest.py 21/21, .gitignore) and the `reading-bench` launch.json entry — `8f1cf51`. Nothing to Codex.

## §18 Next entry point

1. **Open:** `open.sh`; the card should read S130. The bench is rebuilt with `SKILL.md`'s four lines; `--pdf` wants the source from the library's `drop/done/`.
2. **F3 is the first thread the bench handed back:** read the bundle markdown for p.161 (docs/37: the bundle, not the PDF) and say whether `fact_sales_table` reached the analyst; if the figure pass blanked it, that is a converter finding with a page number, and the crop tool's first use case.
3. **Cheap, owed:** J61 (two instances now; one decode rule for every selftest's `run()`), then J64 (the guard's sixth shape — the case first). Then the analyst road (J47 → J46 → J49 → J44 …); J58 `Observed` at the next real bless.
4. **Standing:** nothing to Codex until lifted; the record before any tracked write (J63 enforces it; the record's creation and its commit are two calls); `--census` at every close; run a gate as `ci.yml` spells it; **a number that goes into prose gets its probe run in that sitting** (F2).

## §7 Work log (UTC)

- **17:0x–17:2x** The conversation (§1's context). The extraction: DDIA `blocks.json` — 5,716 blocks, 673 pages, page box 504 × 662 pt (7 × 9.19 in), one Table in the whole book; `manifest.json` — convert `doc_survival 0.8559` over 662 pages scored, 139 flagged (the index), analyst `qwen3:8b` 492 chunks / 481 passed / 11 rejected (fence 8, survival 3), 57.76 tok/s. The guard false-denied an inline `python -c` with braces (the sixth shape of SYM-110's family; a case next session, not fixed mid-conversation). The page built (53 KB, real data inlined) and published; the pane could not show it (sign-in; a local file is a static snapshot) — sent as a file instead. `open.sh` clean (S129, marker S129). This record.
- **17:5x–18:1x** The skill written under the record (SKILL.md, the two scripts generalized, selftest 15/15 then 21/21). Rab: *"can you embed the real pages underneath each of those page section in what it reads, and treat it as an overlay?"* → `--pdf`/`--render` (PyMuPDF 1.28 in the marker-env), the `<image>` layer and the opacity slider; the selftest crashed on the console encoding (F6) and was fixed; the DDIA pages rendered (six, 96 dpi, rect matched); the pane refused `file://` → the `reading-bench` launch entry over `out/` (F4); the screenshot (F1); the mojibake and the wording fixed; the 139 caught against the page's 144 (F2); the empty-html blocks on p.161 read from the raw `blocks.json` (F3); the artifact republished and the file sent. The skill committed (`8f1cf51`), the registers written (J64, J61, ERR-081, SYM-110; census 81/81 · 110/110), this closeout.

## §19 Close values — `close.sh 0de9696`, 2026-09-10T18:13:18Z (verbatim)

```
    PIN              0de9696
    DIFF             12 file(s) since 0de9696 · dirty 0 = mine 0 · peer-owned 0 (none) · 1 untracked
    GLASS            clean since 0de9696 (--enforce; bare runs exit 0 regardless — SYM-046)
    RUST             skipped — no windows-widget/ change since 0de9696
    CI               no run for 8fc68627 yet (unpushed? ahead=3) — NOT a green
    LEVERS           no unlevered threshold constants added since 0de9696
    DOCTOR           lever lexical parity: figure-triage.txt names 8 · quoted refs 8 · MISSING 0
    DOCTOR-HEAD      UNREAD — closeout-headline probe re-run not yet mechanized
    CENSUS           selftest.sh: declared 11 = FIRED 11 = banner 11/11 (exit 0)
    CENSUS-SCOPE     UNREAD — broader TEST-STRATEGY fixture census not yet mechanized
    REGISTER         OPEN-TASKS.md written this session: +2 / -1 lines
                     STRUCK 0 · ADDED 1 — net +1, GOVERNANCE GREW. §I: justify it or work one down
    MEMORY           clean — soft clock is DURABLE at a09b2b0
    DEBT             open SYM 41, unchanged since 0de9696
    PUSH             ahead of upstream: 3 (the close pushes; 0 after)
════════ close exit 0 — values only; the judgment half is SKILL.md ════════
```

- **REGISTER net +1, justified:** J64 is a defect found by this session's own work (the guard denied a legitimate command) with its order of work named; nothing on the register was ripe to strike in a session spent building the skill. J61 gained a second instance, which makes it the cheaper of the two to work down next (§18). **DEBT unchanged** (SYM-110 grew a shape in place, not a row). The 12 files: the skill's five, launch.json, the three registers, this record, plus the open's files. The one untracked item is `.codex/`. **CI** after the push (§20); **MEMORY** at the S128 lockstep, the S129 one follows the row (§20). `--census` 81/81 · 110/110.
