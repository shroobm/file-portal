# S93 · Desktop · 2026-08-17 · STAGE 0 — THE TRUTH REPAIRS

## §1 Session Intent

Rab's commission, in his words: **"Boot up, you're on ultracode, muster, check what last
session left for you to do, start preparing for that, and work until it's done."**

Read as: execute Stage 0 (docs/37 §1) per the S92 pre-mortem (docs/37 §4), commit per task,
close, push. T2 (F-09) carries no signature in his prompt → per the pre-mortem's own rule:
do the rest, leave F-09 flagged, never guess.

## §2 Starting State

`open.sh` exit 0, card verbatim:

```
════════ MUSTER · OPEN · 2026-08-17T16:47:25Z ════════
[0] IDENTITY
    ledger newest    S92  9bbe360
    this session     S93  ·  desktop  ·  2026-08-17
    advisory         S93 is named in the INHERITED tree (9bbe360) — read each, do not blanket-correct:
                     sessions/S92-desktop-2026-08-17.md
[1] GROUND
    verifier         1E2BE296  (repo == ~/.claude)
──────── MUSTER · 2026-08-17T16:47:26Z ────────
[1] MEMORY ....... ✓ /c/Users/Bndit/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory/MEMORY.md
[2] SOFT clock ... ✓ 73 (tally == hook)
[3a] LEDGER ORDER  ✓ 75 Desktop-lane rows ascend (S16 → S92)
[3b] LEDGER PARSE  ✓ 75/100 rows parsed · 21 discarded, newest at row 21 · tail = last 5 · 4 other-lane: ThinkPad-S43 ThinkPad-S67 ThinkPad-S78 ThinkPad-S79
[3] HARD clock ... ✓ 9bbe360 (S92) mirrors ledger, ancestor of HEAD
    WORKING TREE . 0 uncommitted · feat/library-pipeline
VERDICT .......... ✓ CLEAN — clocks reconcile
    muster exit      0
    origin           in sync (fetched)
[2] LIVE
    pipeline         held 4 · anchor 23 · pending 0 · drop 0
    levers           audit=enforce · analyst=local · batch=16
    gpu-lock         absent
    events           137 line(s)
    widget           14324
    python procs     4
    ollama           2
    llama-server     1 — the NAME does not identify it (ollama's engine is also llama-server.exe); the parent PID decides, never the name (S81)
    installed exe    6CA0DEF0  (adoption is Rab's hand — docs/19 §0.3)
    vault            6 note(s) · tip 70c60e6
    thinkpad         converter=active · vault tip 70c60e6 · staging 0 · receipts 7
[3] PIN
    --since          9bbe360
    closeout         sessions/S93-desktop-2026-08-17.md
════════ mechanical half: clean (exit 0) ════════
```

The S93-in-inherited-tree advisory is S92's forward reference to this session's commission
(S92 §18: "expect S93 == Stage 0") — read, not a collision.

**Pinned `--since`: `9bbe360`** — chosen now, before any result exists.

Phase 3 promotions at open (each `Observed` this session, command in the transcript):
- docs/35 four drift sites re-proven against source: `analyst-mode.txt` enum incl. `ask`
  (watch_and_convert.py:5-6,:37,:153) · `chunk-batch.txt` = Marker slice recognition batch
  8|16|32 default 16 re-read per slice (line.rs:184,:192; room.js:378-382; convert_and_ship
  `_with_batch` → `--recognition_batch_size`) · events.rs derived/read-side ("nothing
  stored", events.rs:9,:14) · verdict enum `pass|flag|fail` (fidelity_audit.py:11,:411).
- docs/08 transport-revisit entry ABSENT while transfer.rs:11-12 and docs/01:91 point at it
  (F-05's dangling pointer, re-proven by reading docs/08 end to end).
- `.done` skip branch takes only `is_file()` (convert_and_ship.py:731); `.done` records
  `{source_sha256, page_range, wall_s, batch}` and the resume path reads only `wall_s`.
- events.py `emit()` appends with no last-byte check (events.py:28-29); exporter.py
  `append_receipt` carries the SYM-037 healing to port (exporter.py:88-95).
- analyst.py `_generate_gemini` passes the key in curl argv (analyst.py:126-131, F-13);
  room_chat.py `ask()` is the urllib idiom to mirror (room_chat.py:371-388).
- Corpus comment now lives at room_chat.py:318 (the pre-mortem's ":293" drifted — same
  comment, same rule): "MEASURED 12,909 tok … re-measure at any corpus change".

## §7 Implementation Delta

| Commit | What |
|---|---|
| `b926c07` | session open — commission + card + pin, before work |
| `4eb4f1d` | T1: docs/35 truth repairs ×4 (F-20) + corpus re-measured **13,076/16,384 tok, headroom 3,308** (llama-tokenize, qwen3 vocab, CPU-only; fixed point verified) + 4-site cascade + acceptance 24/24 |
| `df93b44` | T3: identity-gated slice resume (F-02) — `.done` gains `engine_args` + `marker_version`; mismatch/legacy/unparseable → loud re-convert; `batch` never gates (T2c); predicate 6/6 |
| `6d618f8` | T4: `events.emit()` torn-line healing (SYM-037 port) + `events_selftest.py` same commit, 7/7 |
| `843ec7f` | T5: analyst.py curl→urllib (F-13/CWE-214) — key off argv; TLS probe `Observed` (HTTP 403 JSON); local path proven live, zero GPU |
| `a397980` | T6: SYM-032 dated refresh · SYM-041/042 filed · docs/08 transport entry (F-05 pointer closed) — CRLF verified after each edit |
| `2b5d025` | T6b: glass dispositions (3 signatures walked through docs/29 §5.3, signed) + SYM-027 stale-`{}` clause dated; detector re-run clean |
| *(close)* | CHANGELOG + this closeout |

In-range but not mine: `bfa94d5`/`b3dc2da` are S92's late CHANGELOG entry and ledger row —
they postdate the pinned SHA and were already accounted by S92 §10/S93's inheritance.

## §8 Decision Ledger

1. **T2/F-09 NOT built** — Rab's prompt carried no F-09 signature; per docs/37 §4 T2 the
   per-slice re-read stays unimplemented and is now findable as **SYM-041**. Nothing was
   guessed. Awaiting his word on per-slice vs immutable-per-job (docs/37 §3.1).
2. **T5 timeout semantics, one recorded broadening**: the curl form let a 300 s request
   timeout ABORT the chunk (TimeoutExpired propagated, no retry); the urllib form classes a
   timeout as a transport failure and retries it inside the same 3-attempt/backoff budget.
   Bounded (≤3 attempts), and a timeout IS a transport failure — but the pre-mortem said
   "exactly", so the delta is recorded here rather than shipped silently. Overridable.
3. **A 4th cascade site**: docs/37 §5's own live-state note carried the corpus number; the
   pre-mortem's cascade list named three sites. Updated with the same measurement, deviation
   recorded (the fix-list for stale numbers was itself one site short).
4. **Dispositions without Rab's hand**: the 3 glass signatures were signed by me following
   the S79–S81 precedent entries (same lane, same reasoning shape). If signed silence needs
   HIS signature specifically, the three entries are the review queue — each names its sites
   and reasoning.

## §10 Known Failures — mine

**10.1** The ultracode verification workflow died 5/5 agents on a session limit ("resets
3 pm America/Toronto") and returned an honest zero — which I did NOT render as a clean
review (SYM-031's shape: a failed probe is UNREAD, never green). Recourse taken: a full
end-to-end read of the diff as the differently-shaped second check, plus the in-session
probes (6/6 predicate, 7/7 selftest, 24/24 acceptance, TLS 403, live ollama error-path).
The multi-agent layer can be re-run after the limit resets if Rab wants it.

**10.2** The pre-mortem's cascade list was one site short (§8.3) — trivial, but the
pattern (a stale-number fix-list that is itself stale-number-incomplete) is SYM-039's
shape and worth the line.

## §15 Symptom Signatures

Filed this session: **SYM-041** (mid-book batch-lever silence, F-09) · **SYM-042**
(no-lock `--resume`/manual runs, F-01 novel). Both OPEN by design — they are signature
slots, not fixes. SYM-032 and SYM-027 carry dated status updates (append-only in spirit).
No new failure class discovered in S93's own work.

## §17 Current State

`Observed at close` — Stage 0 exit criteria met: suites green (24/24, 7/7, 6/6 probes),
corpus re-measured (13,076/16,384), the index no longer lies about anything we know
(SYM-032/027 dated, SYM-041/042 filed, docs/08 entry written, docs/35 ×4 fixed). Glass
detector `--since 9bbe360`: no unsigned glitches. Widget up on Rab's morning launch;
assistant still on the PRE-S93 corpus and the 700-token budget until his ONE restart.
Working tree clean at close; origin synced by the close's push. *(`Historical` the moment
it is written.)*

## §18 Next Entry Point

1. `open.sh` — expect S94, `--since` = this close's SHA.
2. **Rab's hands, in order**: ONE widget restart (picks up S87's corpus + S88 guard + S89
   budget + S93's re-measured docs/35/36 together) → the engineer question's FOURTH ask —
   expect a cited answer at last. Then his word on: F-09 semantics (§3.1/SYM-041) ·
   Stage 1 GO (mutex signature §3.2/SYM-042) · the standing queue.
3. Stage 1 is the next build session (ONE widget rebuild + adoption): CSP + devtools out of
   release, bless-via-config, spot_check/degeneration_flagged/fixity renders,
   `recent_audits`, the named mutex — docs/37 §1 Stage 1.
4. Optional: re-run the S93 multi-agent review after the session-limit reset (§10.1) —
   the workflow script is preserved in the session's workflow dir.
