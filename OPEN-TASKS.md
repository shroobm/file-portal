# OPEN TASKS — the register of what File Portal has NOT done

**Retrieve by what is undone, not by which session left it undone.** Companion to
`SYMPTOM-INDEX.md` (which answers *"what is going wrong"*); this file answers *"what was
promised and never delivered"*.

Built 2026-08-22 (S107 open) by sweeping **all 42 closeouts in `sessions/`** (S67→S106, both
machine lanes), the `CLAUDE_README.md` Change Ledger (114 rows, 89 Desktop-lane, S16→S106),
`docs/08`, `docs/26`, `docs/32`, `docs/37` §3, `docs/40`, `docs/41`, `docs/45`,
`SYMPTOM-INDEX.md`, `coordination/relay.md`, `coordination/messages/`, and the memory library.

## How to read this file

| tag | meaning |
|---|---|
| `Observed 2026-08-22` | **re-measured by the session that wrote this file.** A command was run; the result is quoted. |
| `Historical` | faithfully transcribed from the closeout that recorded it, **not** re-observed. It may have been done since. |
| `SEMANTIC` | needs Rab's signature. A session must not build it on its own judgment. |
| `MECHANICAL` | no signature needed — a session may just do it. |

**Two standing hazards this file inherits, per `docs/45` §1 Family 1:** a sentence here may
describe the *neighbour* of the thing it names, and a status column may be stale while the
row body is correct. **Before acting on any line, open the cited source.** Every line carries
one.

⚠ **This register is itself a governance artifact**, and `docs/45` M5 + the S105 §10 #4 debt
gate proposal both argue the project is producing governance faster than output. It was asked
for explicitly; it is listed in §F as something that should either earn its keep by being
*worked down* or be deleted.

---

## §0 The one-paragraph answer

**Nothing in the product has moved for ten sessions.** The muster card at
2026-08-22T03:45:02Z reads `held 4 · anchor 23 · pending 0 · drop 0 · events 137 · vault 6 ·
exe 4DCB73E2` — **byte-identical to S97's open card** (`Observed 2026-08-22`). No conversion
has run since S96. S97–S106 built: a think tank, a skill, a relay, a claim convention, an
amendment, a selftest, a wiring slice, an adoption, a figure-coverage module, a close script,
a Circle, a second Circle, a lever file, a modularity gate. **`docs/45` names this as the
finding, and this register is the enumeration of the work those sessions did not do.**

Counts: **43 open semantic decisions** (§A) · **31 mechanical items** (§B) · **14 genuinely
open symptom rows + 4 falsely-open ones** (§C) · **8 delegations never collected** (§D) ·
**7 of 7 slate items unfinished or unwired** (§E) · **11 record-integrity repairs** (§F).

> **RE-MEASURED 2026-08-27 — the line above is `Historical` (2026-08-22) and is left standing
> per this file's own no-rewrite habit. Measured today, by counting rows rather than recalling
> them:**
>
> | section | rows | struck | open |
> |---|---:|---:|---:|
> | §A semantic | 44 | 2 | **42** |
> | §B mechanical | 31 | 2 | **29** |
> | §D delegated | 8 | 1 | **7** |
> | §F record repairs | 12 | 0 | **12** |
> | §J opened S109 | 18 | 4 | **14** |
> | | | | **104 open** |
>
> **THE DENOMINATOR, because this number has a twin that counts something else.** `104` counts
> **rows in THIS FILE only**, after the day's 8 strikes. It is *not* comparable to the **147**
> reported to Rab on the same day, which counts distinct open items across **five surfaces** —
> this file *plus* `SYMPTOM-INDEX.md`, `sessions/S108-SIGN-SHEET.md`, `coordination/BRIEF-S109.md`
> §6, and `sessions/S110` §23. Both numbers are correct about different populations. They were
> published hours apart with neither naming its population, a sweep agent read them as a live
> contradiction in the tree, and it was right to — that is `docs/34`'s rule (**every measured
> number names its numerator, denominator and conditions**) broken by the session that was
> re-measuring this file to make it honest. Filed as `ERR-2026-08-27-012`.
>
> **Two things the old line got wrong, both structural, neither anyone's carelessness:**
> (1) **§A has 44 rows, not 43.** The count was written when §A ran A1..A43 with none struck;
> S109 then struck A21 *and* added A44, and the two changes cancelled. The number survived by
> coincidence, not by maintenance — which is precisely the rot §H predicts.
> (2) **§J's 18 rows were never in the headline at all**, and `open.sh` could not see them
> either: it counts ids in `[A-F]`, and J is outside that class. The muster card has been
> printing `94 item(s)` against a file holding **112**. That is J15's shape — a counter that
> reads one spelling of a marker — applied to the register instead of the symptom index.

> **Struck 2026-08-27** (each re-probed against the working tree by the striking session, never
> taken from a prior report): `A18` `B22` `B25` `D1` `J2` `J8` `J9` `J11`. **`J10` was NOT
> struck** — C0 half-moved and a strike would have claimed it closed. **`B17`/`B18` were NOT
> struck** — a finished n=30 A-B-A run exists on disk, gitignored and unnoticed, but its one
> named condition (idle card) is unrecorded, so it stays `Unknown`.

> **RE-MEASURED 2026-08-28 — current row population after the S111 filings below:** §A has
> 48 rows / 2 struck / **46 open**; §B 33 / 2 / **31**; §D 8 / 1 / **7**; §F 12 / 0 / **12**;
> §J 18 / 4 / **14**. Numerator: open rows in this file. Denominator: those five sections only.
> **110 open.** This does not revise the historical 147-item, five-surface census.

**Current signable surface outside that historical five-surface denominator:**
[`sessions/S110-SIGN-SHEET.md`](sessions/S110-SIGN-SHEET.md). Blocks A and B are signed; C1,
C2 and D1 remain open. Do not fold this sheet retroactively into the 147 census. A MUSTER card
field was considered and deliberately not added in S111: it would change the open/close gate for
a reachability repair that this primary register already supplies.

---

## §A AWAITING RAB'S SIGNATURE — semantic, a session may not decide these

> **S211 (2026-09-23), on his word at 05:37:34Z — *"Tell me all the things you need me to sign or not sign
> and reasons, on the next open."* — the nine live items are written out in full, each with its reason,
> what signing costs, what refusing costs, and a recommendation where one is held:
> `sittings/S211/SIGN-BRIEF.md` in the private half. Read it at the open, before this register.**
>
> Its first item carries the only strong recommendation: **do not sign the degeneration exemption** —
> eight blocks read at their pages, eight loops, real false-alarm rate 0 of 14 rather than 21 %.

### A.1 The signature register (`docs/37` §3) — items still OPEN at HEAD

| # | The decision | Options on the table | Fable's standing recommendation | Source |
|---|---|---|---|---|
| A1 | **Asset posture** on out-of-range assets | warn (today) ~or~ fail ~or~ quarantine | encode in Stage 2 | `docs/37` §3.3 |
| A2 | **Stage 2 GO** — converter tests + CI | go / hold | go; negative cases enumerated | `docs/37` §1, §3.4 |
| A3 | **Stage 3 GO** — the verified seam (per-file SHA-256 `inventory.json`, exporter verifies BEFORE commit, receipt binds the digest) | go / hold | go — closes the transport integrity half | `docs/37` §3.4, `docs/08` |
| ~~A4~~ **STRUCK 2026-09-15 (S157 E4) — DONE** | **P-1 variable 1: HOST** ⟨status 2026-09-04: DONE — 2026-09-04 verifier (HEAD bde007d): DONE — SIGNED S108, BUILT `0fbb6e3` (ancestor of HEAD); docs/50:320 already 'stale'. Strike.⟩ | in-converter at `:998` (route to region-level coverage, extends card-lock) ~or~ out-of-band desktop re-score (zero lock cost, back-fills the whole corpus) | **SIGNED S108; BUILT out-of-band at `0fbb6e3`** — manifest/SHA-bound, report-only, zero GPU/card-lock cost; scan-lane subpage diagrams fail closed as UNREAD | `docs/37` §3.6, `docs/41` §2, S108 §5 · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — git merge-base --is-ancestor 0fbb6e3 HEAD → ancestor; docs/50-ticket-board.md:320 reads `stale` |
| A5 | **P-2 / the #598 tripwire** disposition | join `docs/15` §12's fail list ~or~ flag-only localizer ~or~ receipt-note-only | flag-only until §9 calibration gives a false-alarm rate | `docs/37` §3.7 |
| A6 | **DPI operating point** (P-5) | one-book measured probe ~or~ per-page render-to-budget ~or~ leave it + record why | the one-book probe first; `ocr_dpi` stamp reworked in the same commit | `docs/37` §3.8 |
| A7 | **Generative-SR standing ban** (P-6) | record the ban ~or~ defer-with-criteria (ground-truth set + no-SR diff lane) | zero code either way — just record it | `docs/37` §3.9 |
| A8 | **SPOT_CHECK_EVERY** 10 → 3–5 | his number | 3–5 (ThinkPad S78 §12 argues it) | `docs/37` §3.5 |
| A9 | **Stale-hold reap countersign** | countersign / revise | — | `docs/37` §3.5, S85 §8.2 |
| A10 | **`docs/34` rule 8 countersign** | countersign / revise | — | `docs/37` §3.5, S83 §18.4 |
| A11 | **The wrapper decision** — two-engine steady state vs Ollama off the machine | two engines / one | §6.1 is the measurement; nothing further to measure | S83 §18.3 |
| A12 | **GLM bench second-reader** — zone-keyed accept/reject proposals carrying reason·highlight·solution | build / don't | build; the S84 probe is the existence proof (3/3 omission runs recovered, ~3 s/page) | S84 §18.2 |

### A.2 The `docs/26` signature sheet — five decisions, open since 2026-08-13 (S75)

Deferred through **S76, S77, S78, S83, S84, S85** by name. Gates slice 3 (the Dock) and the
operator-simplicity discovery mission behind it.

| # | Finding | The decision | Recommendation |
|---|---|---|---|
| A13 | **F8** — ambient terracotta radial washes violate "nothing else may glow terracotta" | carve-out or desaturate | keep + sign the ambient-chrome carve-out (≤7–13 % clay, never animated, outside the summons grammar) |
| A14 | **F7 / F13 / F15** — `docs/25` bans motion > 400 ms with no carve-out while its own Part 3 requests a 2.6 s breathe | amend `docs/25` | "no *travel* motion > 400 ms; ambient opacity idles exempt; all motion collapses under reduced-motion" + fix the Part 3 active-wording and the compositor claim in the same pass |
| A15 | **F6** — "agreement lane": metric *kind* rendered in the lane slot; both rendered lanes are fictional | relabel | "scan lane · agreement witness" / "clean lane · fidelity witness" |
| A16 | **F4 + F5** — `doc_survival` **fabricated as 1.0 when nothing was measurable**; the Wall hero mixes metrics and inherits those fake 1.0s | fix + relabel | `null` not 1.0; `pages_scored` on the card; Wall avg windowed or relabelled "lifetime". **Own mini-session — touches Python + manifests** |
| A17 | **F3** — terracotta the *colour* now carries ≥5 meanings while glow stayed disciplined | colour policy | busy moves off clay; clay becomes summons-only in colour as well as glow. **Decide WITH slice 3 so Dock + rail change once** |

Also inside `docs/26` and unresolved: **F14** — slice 1's "60 fps trace" proof gate was never
machine-run, deliberately deferred to Rab's adoption. Big-screen + perf remain human gates.

### A.3 The S105 Circle's 14 — `sessions/S105` §10, "semantic, NOT applied"

`Observed 2026-08-22`: of the 14, **#2 was attempted at S106** (P-1 re-measured on a repaired
map — but *the repair is not wired*, so it moves to A18 below rather than closing); **#10 and
#11 are mechanical** and appear in §B; the remaining **11 are untouched**.

| # | Item | Verified state today |
|---|---|---|
| ~~A18~~ **STRUCK 2026-08-27 — BUILT** | **Wire the SYM-050 repair.** `true_page = id + 1 − 200×(id÷400)`, confirmed four independent ways at S106 | **BUILT S108 (`0fbb6e3`)** — narrow full-signature repair in memory; JSON names repaired/as-shipped map; clean control detector false at 1/1, poisoned IV detector true at 220/269 versus naive 30/269; SYM-050 resolved in source · `Observed 2026-08-27` — `git cat-file -t 0fbb6e3` = commit; `windows-converter/coverage_rescore.py` and `figure_coverage.py` both present in the tree |
| A19 | **#1 Stop opening a session per work item** — 8.7 min of committed work per session, each carrying a full closeout + ledger row + CHANGELOG + memory digest + two clock advances. *The arc's single largest cost* | `Historical` (S105) |
| ~~A20~~ **STRUCK 2026-09-15 (S157 E4) — DONE** | **#3 A product clock on the open card** — last-conversion timestamp, amber when idle N sessions. Today the card **cannot tell a healthy idle pipeline from a dead one** ⟨status 2026-09-04: DONE — 2026-09-04 verifier (HEAD bde007d): DONE — main.js:561/665 render 'last pipeline event: <age>' (RESOLVED S108 lane F); docs/50:318 'stale'. Strike.⟩ | `Observed 2026-08-22` — the card still shows no timestamp **RESOLVED S108 (lane F)** — the Dock renders 'last pipeline event: <age>' with fresh/idle-N-days/UNREAD states, tail-read only. · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — windows-widget/src/main.js:561/665/676 render `last pipeline event: …` (UNREAD until the first measurement) |
| ~~A21~~ **STRUCK S109 — BUILT** | **#4 A debt gate in `close.sh`** — refuse a new governance artifact while the OPEN SYM count exceeds its value at the last close. ~3 lines | `Observed 2026-08-22` — grep for debt / OPEN SYM in `close.sh` → no hits |
| ~~A22~~ **STRUCK 2026-09-15 (S157 E4) — DONE — KEPT** | **#5 Suspend the relay, delete the concordance amendment**; reinstate on Codex's first entry ⟨status 2026-09-04: DONE — 2026-09-04 verifier (HEAD bde007d): DONE — SIGNED S108 KEPT; relay.md carries 55 ⟨from: Codex⟩ entries at HEAD. Strike.⟩ | `Observed 2026-08-22` — `relay.md` has **3 entries, all `⟨from: Fable⟩`, zero from Codex** **SIGNED S108: KEPT.** The founding premise (one participant) died 2026-08-23 — Codex returned four entries in one day. · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — coordination/relay.md carries 94 `⟨from: Codex⟩` entries at HEAD; the relay was never suspended (S108 KEPT) |
| A23 | **#6 Delete `/echo` the skill, keep the lexicon** — 1 commission in 7; the triviality carve-out invoked 6/6 | `Observed 2026-08-22` — `.claude/skills/` = `echo`, `muster`. Still there |
| A24 | **#7 Cut the closeout contract to 6 core sections + Evidence**, and mechanize `docs/21`'s own 80-word check | `Historical` (S105) |
| A25 | **#8 Narrow the tag law** to §Known Failures, §Evidence and status sentences — it is at zero compliance | `Historical` (S105) |
| A26 | **#9 Invert glass's default** — `--enforce` on, explicit `--report` to print. Removes SYM-046's whole class instead of ruling against it | `Observed 2026-08-22` — `--enforce` is still `action="store_true"`, opt-in (`glass_detector.py:338`) |
| A27 | **#12 Correct the S102/S103 ledger rows and CHANGELOG**, or rule that newer rows supersede older ones | `Historical` (S105) |
| A28 | **#13 Sign `docs/32` §5's three rules** — obeyed as law in 8 places; the doc itself says otherwise | `Observed 2026-08-22` — `docs/32:134` reads **"Not signed."** |
| A29 | **#14 Put the governance suites in CI** — muster, echo, coordination, figure-coverage, glass acceptance. *Today nothing checks the layer that checks everything* | `Observed 2026-08-22` — `ci.yml` runs python lint/test for the **three linux dirs only** + the rust job. Zero governance suites, zero windows-converter **SMALLEST SLICE S108** — observability/acceptance.py runs in CI (warn-only); the remaining governance suites stay queued. · `Observed 2026-09-15 (S157 E49)` — THE LINUX-RUNNABLE SLICE IN, WARN-ONLY: nine governance suites as nine continue-on-error steps of the python job (relay-gate 204, guard_git 181, guard_record 16, ledger_check 11, echo 5, circle 4, wiki 11, coordination 20, muster 110 — the Desktop's counts), each run on the Desktop before the edit; the run's LOG read per step: run 35002913762 (num 419) on 778703c, job `python` conclusion=success — the API said `success` on all nine steps, which for a continue-on-error step is the post-override value and means nothing (SYM-075); the LOG (`e49_read_steps.py`, `e49_step_block.py`, `e49/steps_35002913762.json`, `e49/block_*.log`, `e49/muster_35002913762.log`) says: **green on ubuntu — 4 of 9**: relay-gate `ALL TRIPWIRES FIRED — 204/204, exit 0`; guard_record `16/16 green`; echo `5/5`; wiki `11/11, exit 0`. **Not green — 5 of 9, each read to its cause**: (1) guard_git `172/181 *** RED ***` — the eight "from elsewhere" ca. Excluded and said: relay_watch (needs uv), card_mutex (a Windows mutex), deferral_gate (the live watcher), the private desk skill. ARMING (continue-on-error removed) is his signature, as S116's was; the row stays open for that word and for the excluded four. **MEASURED S192 E2 (2026-09-17) — the LOG reading is the observer's own now:** `ci_observe.py` downloads the run's logs zip and prints every `warn-only` step's LOG verdict (`exit N` / `clean` / `UNREAD`) beside the API's post-override `success` (SYM-075's guard, mechanised; `ci_observe_selftest.py` 7/7). The first automatic reading, run 35228663880 (num 537, the S191 row): **7 of 10 green** — relay-gate, guard_git (the S157 red gone), guard_record, ledger_check, echo, wiki, widget projection; **3 of 10 red, each read to its cause** (`sittings/S192/e2_red_logs.txt`): circle 3/4 — the user-level mirror `~/.claude/skills/circle/SKILL.md` is absent on the runner (UNREAD counted as a fail by the suite's own rule); coordination 19/20 — case 13, the B1 negative control expects the `grep -q` SIGPIPE rc 141 and ubuntu's bash reads it otherwise (`yes: Broken pipe`); muster 124/136 — SYM-072's ⟨claimed:⟩ stamp cases (7), J57's task probe (2), J62's AUTOLOGON (3): Windows probes on ubuntu. Three platform-shaped suites; the fixes (a SKIPPED-on-this-platform reading that is neither pass nor fail, per case) are the lane's; arming stays his · `Observed 2026-09-17 (S194 E1)` — THE THREE PLATFORM-SHAPED REDS READ SKIPPED: the runner's logs (run 35255213033, `sittings/S194/e1_ci_logs.txt`) named them — circle's mirror check (no `~/.claude` on the runner), coordination's case 13 (ubuntu's bash did not deliver SIGPIPE's 141), muster's 12 (cases 40–44 plant `S43-desktop-*.md` and open.sh names the machine from uname; cases 45–46 probe the desktop-only widget-autostart row) — and each selftest now reads a case its platform cannot run as SKIP, its own tally (`fired / skipped / silent`), never a pass and never a fail; the exit reads the fired cases only. The controls: on Windows every case fires (muster 137/137 · circle 4/4 · coordination 20/20, 0 skipped); `MUSTER_SELFTEST_PLATFORM=linux` → 12 SKIPPED, exit 0; `CIRCLE_MIRROR=<absent>` → 1 SKIPPED; a DIFFERING mirror still FAILS; `COORD_SELFTEST_SIGPIPE_RC=0` → case 13 SKIPPED. The expected LOG verdict on CI's next run: `clean` for all three. **Arming stays his.** · `Correction 2026-09-17 (S203 E2)`: the S194 E1 clause above was generated by `sittings/S194/e1_register.py` BEFORE `e1_fix_span.py` corrected the wrapper's span and was never regenerated — the fixed `muster/selftest.sh` reads THIRTEEN platform-shaped assertions (cases 40–43 eight, cases 45–46 five) and `MUSTER_SELFTEST_PLATFORM=linux → fired 124 / skipped 13` (the S194 record E1 and its closing message 5a3cd5b carry the fixed figures); the `12 (cases 40–44)` and `12 SKIPPED` in the clause above are superseded. Found by the S203 day-read fleet, re-read by the coordinator |

### A.4 New from S106

| # | Item | Why it is his |
|---|---|---|
| A30 | **Ctrl+Z is dead in the Bench after any Enter.** The `innerHTML` rebuild orphans Blink's undo stack and the browser still reports undo as available | the fix is either a real custom undo model or reverting to a textarea — a product-shape choice **BUILT S108 (`0f0e83f`) — ratification on the sign sheet**: native-undo path (contenteditable='true' + beforeinput), plaintext-only MODE was the killer; Ctrl+Z measured alive through Enter. |
| A31 | **Post-close continuation: new session number, or a ledger continuation form?** The two-clock protocol has **no representation for work performed after a session's ledgered close.** S106 tried to file a second row; both muster guards correctly refused it (`S106b:` → parse fail; a second `S106:` → order fail), and the row was reverted | repo-structural; the guards are working as designed, the protocol has the hole |
| A32 | **The assay surface for the operator lever** — blocked behind A4 (P-1's host) | depends on a signature · `Observed 2026-09-15 (S157 E40)` — the blocker named here (A4, P-1's host) was struck at S157 E4 as DONE (signed S108, built `0fbb6e3`); the row's premise is stale. What an "assay surface for the operator lever" would show is not built and not designed anywhere the sweep could find (`grep assay` in the widget: 84 hits, the Assay card of S117; no lever surface). Owner now: his word on whether the surface is wanted |

### A.5 Product & platform direction

| # | Item | Source |
|---|---|---|
| A33 | **Pick one `docs/40` §10 decision gate before any platform build.** G1 trace view · **G2 exception layer (recommended first)** · G3 buyer/deliverable · G4 transport risk · G5 federation · G6 enterprise. *Do not build federation, PDF/UA, RBAC or a broker before that evidence* | `docs/40` §10, S97 §18.2 |
| A34 | **Rab's narrated legend** for the sketch's nodes `1`–`3`, the stars, and the final circle — can promote or falsify the S97 interpretations without touching code | S97 §18.3 |
| A35 | **The Repair Bench's operating doctrine is UNDISCOVERED** — no canonical example of a good repair exists. The operator-simplicity discovery mission is queued *after* the `docs/26` sheet resolves | `docs/26` "Standing context", S75/S76/S83 §18 |
| A36 | **Transcribe thresholds** + **repair audit-credit** — unsigned since S71/S72, deliberately deprioritized until the bench doctrine session | S71 §18.2, S72 §18.3 |
| A37 | **The scan-lane challenger** — bigger surgery, own design, Survival Audit A-B gate | S84 §18.4 |
| ~~A38~~ **STRUCK 2026-09-15 (S157 E40) — DONE S142 E1 — struck S157 E40** | **A product session for `held/`** — ⟨Historical S83–S85: "4 books are held"⟩ ⟨Observed S142 E1, 2026-09-13: NINE held, each triaged — score at hold vs today's audit vs a decision, in `sessions/S142-desktop-2026-09-13.md` Episode 1: four RE-CONVERT (Damodaran ×2, Ashby, Beer's *Diagnosing*), one RETIRE (the CJK front-matter PDF), one RESHIP (*Zero to One*'s held pass copy — `vault-moves`, on the ThinkPad hold), three HOLD (Valentine's scan-lane witness, the Book of Models = A41, DDIA's near-exact gate); the executions are Rab's word⟩ | S83/S84/S85 §18 · S142 E1 · `Observed 2026-09-15 (S157 E40)` — the product session for `held/` ran at S142 E1 (2026-09-13): nine held books triaged — four RE-CONVERT, one RETIRE, one RESHIP, three HOLD — with the dispositions in `sessions/S142-desktop-2026-09-13.md`; the executions are their own rows (`held-books/reconvert-four`, A41 now struck, the reship done S144); the ask of this row is met |
| A39 | **The guided Valentine session** — *deferred by name in S76, S77, S78-desktop, S78-thinkpad, and S79.* Remove the wrong-page embed, re-diagnose zones 1–2 as a matrix, zone 4's structure, zone 3's collapse last | S78 §18.4 |
| A40 | **Cybernetics-stale copy's fate** · **Textor re-download + Marker upgrade swap** — standing since S68, never resolved | S68–S73 §18 "standing queue" |
| ~~A41~~ **STRUCK 2026-09-15 (S157 E40) — RESOLVED S117 — struck S157 E40** | **Decide `c5afd9edcf620fc6`** — held and vaulted simultaneously ⟨status 2026-09-05 (S116): the designed resolution is IN FLIGHT — the real `--reaudit` re-scored the held copy `fail → flag` and shipped it with a supersede block; the exporter HELD it (`supersede-held`, vault untouched, tip `70c60e6`); Rab's bless click (Assay card) supersedes the vaulted note WITH provenance and retires this row. The local held copy is now `held/c5afd9edcf620fc6--reshipped-20260905-211727` (never deleted, S65).⟩ ⟨**RESOLVED 2026-09-06 (S117)**: Rab blessed from the Assay card (00:46Z, `bless.json` by rab, sha-bound); the ThinkPad exporter, restarted 00:47:27Z, logged `EXPORT-BLESSED` → `EXPORT-SUPERSEDE` → `EXPORTED-SUPERSEDE` (vault commit `50896d15`, blob-verified, staging removed); vault tip `70c60e6 → 50896d1`; the vaulted manifest carries `blessed{by rab, from_verdict flag}` + `supersede{reason reaudit, from_verdict fail}` + `fidelity.reaudit.from fail`. Held AND vaulted no longer: one vault note with provenance, one local `--reshipped-` copy. Strike at the next sweep.⟩ | S96 §18.4 · `Observed 2026-09-15 (S157 E40)` — the row's own text: Rab blessed from the Assay card 2026-09-06 00:46Z, the exporter superseded the vaulted note with provenance (vault `50896d1`), and "Rab's bless click … retires this row"; the local reshipped copy stays in `held/` by S65's rule (never deleted) — its fate is A38's dispositions, not this row |
| A42 | **The windows-converter test decision** (§8.3) — awaiting his word since S86 | S86 §18.4 |
| ~~A43~~ **STRUCK 2026-09-15 (S157 E4) — DONE** | **Give the memory library's git a remote, or fold it into the Drive mirror.** It is local-only; a disk loss takes it ⟨status 2026-09-05: DONE — Rab created the private repo `github.com/shroobm/PROJECT-FILE-PORTAL-2026-2027` and handed it over ("A43. Here is the private repo btw."); Fable added it as `origin` and pushed `main` (43 commits, 37 files, HEAD `b208369`, upstream set; a secret scan of every tracked file found nothing). The library is no longer local-only. New close step: push the library after its lockstep commit⟩ | S96 §18.7 · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — the library's `git remote -v` → origin github.com/shroobm/PROJECT-FILE-PORTAL-2026-2027.git; `git status -sb` → `main...origin/main`; pushed after every lockstep since S116 |
| A44 | **hOCR AS A SUPPLIED SIDE-INPUT** (Rab's design, 2026-08-25). An hOCR file is obtained externally and paired with the PDF at intake; the Analyst gains an INDEPENDENT expectation of what should be on the page, turning it from a reader into a comparator — which is the hole `docs/45` named (*nothing compares a claim to the probe that produced it*). Closes, if built: **SYM-053 becomes mechanical** (hOCR words inside a bbox that marker exported as a degenerate blank crop = a contradiction a machine can see, where it currently takes a human visual pass) · **per-REGION text survival** instead of a document-level score · **SYM-049** gets positive evidence for a fragmented diagram · and the 2-up gutter question becomes a reading-order check rather than 295 pages of human reading. **ACCEPTANCE CRITERION #1, and nothing is trusted before it: THE ALIGNMENT GATE.** hOCR bboxes live in the coordinate space of whatever produced them, often a DIFFERENT scan with its own crop, skew and offset. Align on high-confidence anchors per page, measure the residual, and **mark the pairing `UNREAD` rather than comparing when the residual is bad.** Silent misalignment producing confident garbage is **SYM-050** — the doubled-offset bundle that made 19 of 20 adjudicated verdicts FALSE. **#2: provenance is load-bearing** — `hocr_source` + `hocr_sha256` + the residual into the manifest. An hOCR from a Surya-family engine is CORRELATED and worth little (SYM-001). Unsourced hOCR is `Unknown`, not "reference". **#3: a disagreement is a FLAG WITH A LOCATION, never a verdict** — hOCR is a reference, not ground truth, and is often WORSE than Surya on hard text. Note the precedent: `docs/15` §5's "a large delta is a flag" was **WITHDRAWN at S101**; what makes this different is that the delta is bbox-anchored and `x_wconf`-weighted — a pointer to a place, not a score. That distinction decides whether this fixes that finding or repeats it. **UI (Rab's design):** hOCR is OPTIONAL in the drop box; dropping a PDF offers "confirm" or "supply hOCR", and an hOCR drop zone appears. ⚠ **It must not block intake** — today intake is fire-and-forget, and a modal turns a forgotten confirmation into a pipeline that looks alive and does nothing (`events.jsonl` frozen 11 days is what that looks like from outside). Land it in **`pending`**, a counter ALREADY on the muster card, so a forgotten pairing surfaces at every open; or give the dialog a countdown default. **Validate the pairing AT THE BOX** (page counts + a one-page text-overlap sample) and refuse with a reason, rather than failing deep in the run. **Blast radius:** changes the INTAKE CONTRACT, the most load-bearing surface in the pipeline, and touches both renderers where **SYM-043** is already open for copy lagging mechanism. Stage 2, signature required, not a tonight item | Rab in conversation 2026-08-25 (S109 second sitting); design notes in `sessions/S109` §19 and `coordination/BRIEF-S109.md` |

| A45 | **Finish `docs/30`'s still-open algedonic choices and decide whether to run its deferred research day:** silence/liveness, morning-note versus Gmail ownership, died versus stalled, and M/ack. Do not reopen §5.4, already signed; §5.2 remains deferred and the research day remains queued ahead of the guided Valentine | `docs/30:97-123,151-165,170-214`; commission preserved in S110 §28/§30 |
| A46 | **Choose the Room queue-order contract:** retain the current read-only filename order, or authorize operator reordering. If reordering is chosen, persistence and fairness are unresolved parts of its design, not requirements silently signed by this row | `windows-converter/watch_and_convert.py:196`; `windows-widget/src-tauri/src/line.rs:82-98`; `windows-widget/src/room.js:155-172`; commission preserved in S110 §28/§30 |
| ~~A47~~ **STRUCK 2026-09-15 (S157 E48) — the adversary ran; docs/40 §12** | **Rab's S111 commission: commission an independent adversarial challenge of all four `docs/40` theses A–D.** Each thesis already carries its author's own objections; the requested missing signal is a differently-prioritized reader and a falsifying probe. This is not A33, which chooses a §10 decision gate | authority: `sessions/S110-desktop-2026-08-26.md` §28/§30 and `coordination/relay.md:4310-4317`; thesis locations: `docs/40:270,300,312,324`; A33 above · `Observed 2026-09-15 (S157 E48)` — five read-only Sonnet lanes, one per thesis and one over §0's premises, each with a priority the author did not hold and a falsifying probe (three of five experiments run: B's at E46 — two `analyst:` blocks; D's — the shelf-wide heading-skip rate 12.0%; E's greps — buyer 1 self-referential file, run_id 0/25); plants 5/5; law 25·0 red normalised. No thesis refuted whole; each lost its strongest advantage sentence to a measurement (docs/40 §12). A lane's image count corrected by the verifier: 2,558 wikilink embeds, 0 with alt. The §10 decision gates (A33) stay his |
| ~~A50~~ **STRUCK 2026-09-24T20:34Z (S213) — DECIDED BY HIS WORD, Desk 926be434 (20:32:15Z): "Remove the tasks on portal, and re arm it".** The task is unregistered (Get-ScheduledTask reads it ABSENT) and its tree was stopped first so its loop could not restart it unarmed; PORTAL runs ARMED again (`/api/line` `armed: true`, since 20:34:07Z), started from the chat as it was before S212 — not durable across a reboot, by his choice; the /state pump restarted beside it. THE COST THE TASK HAD, MEASURED: both books he sent from his phone were refused `DISARMED` (jobs d1489040 19:40:54Z, 47d740e4 19:41:02Z) before he sent them in chat. | **PORTAL IS NOT A SERVICE — it is a chat session's child, and making it one is two decisions, not one.** `Observed 2026-09-24 (S212 E4)`, from the parent chain rather than inferred: `server.py serve` PID 10812 → bash → bash → bash → `claude.exe`, so the web app his phone uses goes down when a chat closes and always has — which is why its `since` reads a few hours old each morning and why no scheduled task names it (five exist; PORTAL is not among them). Nothing has been lost by this: the job store is on disk and all 74 jobs survive each restart. **DECISION 1 — register the task.** Persistent configuration on his machine; `portal_boot.ps1` and `portal_task_install.ps1` are written, committed and NOT RUN, in the shape of the five tasks that already exist, with an undo line. **DECISION 2, and the reason this is not one row — `PORTAL_ARMED` is a SAFETY PROPERTY, not a convenience.** `server.py`'s own docstring: the portal engine *"refuses unless PORTAL_ARMED=1 is in the environment (the human's hand); nothing is written under the pipeline root by this program until then."* A logon task carrying it arms the real line at every boot, forever, with no hand on it. It is a separate `-Armed` switch and **defaults to OFF**; unarmed, the app and the `/state` board come up and the engine refuses real work exactly as today. Until one of these is signed, `/state` reports the condition in amber on every affected row and the MUSTER card reads the task ABSENT **CORRECTED 2026-09-24 (S213), and it removes this row's premise:** "dying with that session every day. Measured, not inferred" was WRONG — the PARENT CHAIN was measured and the dying was INFERRED. PORTAL's own server.log shows it running 38.3 hours straight (banner 2026-09-22T02:47Z to the next at 2026-09-23T17:08Z, the latter a deliberate restart), with nothing that looks like dying when a chat ended; and at S213's open an S211 results watch (PID 24288, started 2026-09-23T00:55Z) was still running two days later as an ORPHAN after the Desktop app had been updated — background shells outlive the chat. Decision 1 was signed and installed on that premise (sign-sheet item 2), and it DISARMED the phone door: `armed: false` since 18:17:48Z. Its real value is surviving a REBOOT and restarting on a crash, not surviving a chat. Decision 2 is re-put to him with the risk stated as it actually is: arming lets anyone who can reach his tailnet send a document to convert — which his desktop drop folder already accepts with no switch (nothing on the intake path reads PORTAL_ARMED; checked) | S212 E4 · Desk 9a74442f · private `sittings/S212/portal_boot.ps1`, `portal_task_install.ps1` |
| A48 | **Resolve the two `docs/36` §8 questions that no other row mirrors:** where the briefing should live, and what the ThinkPad's untracked `CLAUDE.md` counts mean. Six of the eight questions are already represented by A8, A9, A10, A11, A12 and A42; do not duplicate them | `docs/36` §8; cross-check against A8–A12 and A42 |

**Negative control for this filing:** `docs/27` and `docs/28` were inspected and deliberately not
refiled; their work is already represented in the symptom/register record.

---

## §B MECHANICAL — no signature needed, a session may simply do these

| A49 | **Pick adoptions from the Okular digest** — `docs/49-okular-digest.md` §1 ranks 13 candidates (viewport sidecar, placeholder paint, prefetch, search suite, text layer, table tool, trim, zoom, unified undo, review panel, watch/swap, observability grammar, paged md reading surface) + ranked-out and pipeline extras, from 107 findings; 404 audit rows re-measured across the 7 code lanes, all MATCH (popplerweb lane unaudited, flagged); a critic pass filed 22 defects on the digest itself, folded in. Evidence `dumps/evidence/D0005`, sha256 `981f2959…`. Nothing built; each candidate names its bench pain and effort class. ⟨status 2026-09-04: STALE — 2026-09-04 verifier (HEAD bde007d): STALE — 'Nothing built' is false: the S112 sign sheet shows 13/13 signed; OK-0/1/2/7 built S112, OK-4 `0181636`, OK-5 `80df03b`, OK-6 `cd5b10e`, OK-8+12 `be191bf`, OK-14 `091ffd7`, OK-15 `77d0361`+`8da7005`; OK-3/9/10/11/13 signed and unbuilt (Codex's queue, MSG-FAB-0055). Rewrite; the row is also misfiled under §B's header.⟩ | SEMANTIC | `docs/49` §1, S112 Fable lane record, `Observed 2026-08-30` |
`Observed 2026-08-22` where a command is quoted.

### B.1 Standing reds and live hazards

| # | Item | Evidence |
|---|---|---|
| ~~B1~~ **STRUCK 2026-09-15 (S157 E4) — DONE** | **`observability/acceptance.py` is RED: 40/41.** The failing row expects `recent_audits: GLITCH` but S94 rendered it, so it is now `glass`. One line, turns a standing red green ⟨status 2026-09-04: DONE — 2026-09-04 verifier (HEAD bde007d): DONE — acceptance.py:46 expects recent_audits=glass; `57abcd9` (41/41). Strike.⟩ | ran it today: `FAIL — 40/41 checks · failed: recent_audits: GLITCH (glass)` **RESOLVED S108 (`57abcd9`)** — the answer key caught up to S94: 41/41. · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — `observability/acceptance.py` → `PASS — 171/171 checks` (the suite has grown from 41 rows; the RED row is gone) |
| ~~B2~~ **STRUCK 2026-09-16 (S161 E2) — BY RAB'S WORD: "Keep codex .agents, Thinkpad is offline for now." (Desk 796340cc, 18:25:34Z). The row asked delete OR gitignore: gitignored since S108 (`.gitignore:76-79`, 0 tracked files), the deletion refused by him today; the mirror stays on disk as Codex's, with its own `00-STALE-DO-NOT-USE.md` banner and `AGENTS.md:53,87` pointing the peer at `.claude/skills/`. Was:** | **`.agents/` — delete it or gitignore it.** It is a pre-S103 copy of the muster skill: **no `close.sh`**, zero mentions of `--enforce`/SYM-046/CI, points at a `.Codex/` path that does not exist, and its selftest prints `ALL TRIPWIRES FIRED — 22/22, exit 0`. **Any runtime that resolves skills from `.agents/` is running a blind close under a green banner**. **RE-MEASURED 2026-08-26 (S109 post-close), and the gitignore half of this item is DONE** (`.gitignore:71`): 16 files, and the drift is now specific -- `open.sh`, `muster/SKILL.md` and **`relay-gate/gate.py` (`a1246bd1` here vs `ab26a4f1` authoritative)** all differ; only `echo/lexicon.md` matches. **The `gate.py` drift is the one that matters: that file is the REFEREE of the two-model protocol and its own digest is the `gate_rev` the board prints, so two copies means two lanes can each be honest and still disagree about which protocol is running.** A `__pycache__/gate.cpython-312.pyc` sits beside it -- **this copy has been executed**, the drift is not hypothetical. **Remaining decision is Rab's: DELETE, or keep it marked.** A loud `.agents/00-STALE-DO-NOT-USE.md` was written on 2026-08-26 for the reader who skips `AGENTS.md` (untracked, like the directory). Deleting does not prevent recurrence if whatever generates the mirror runs again -- **fixing the generator is the only structural fix, and nobody has identified what creates it** | `ls .agents` → `skills/{echo,muster}`; `git check-ignore .agents` → **NOT ignored**; untracked in `git status` · `Observed 2026-09-16 (S160 E4)` — HALF DONE, the signed half: `.gitignore:76-79` carries `.agents/` (B2, signed S108); `git ls-files .agents` → 0 tracked; on disk 17 files, newest 2026-08-26 (`00-STALE-DO-NOT-USE.md`, the banner naming `.claude/skills/` as authoritative; `gate.py`/`selftest.py` copies of 08-24); `AGENTS.md:53,87` points Codex at `.claude/skills/` and away from it. The other half — deleting the peer runtime's mirror — is a hand on Codex's lane and stays his word (2026-09-10: nothing to Codex). |
| ~~B3~~ **STRUCK 2026-09-17 (S196 E2) — MET: the whole-repo `glass_detector.py` census today reads `✓ no unsigned glitches` (bench 189 keys / 371 sites · widget 155 / 303 · converter 151 / 200; `sittings/S196/b3_census.txt`) and SYM-027's FIRST marker reads FIXED S191 E3 — the thirty-six signed one judgment at a time (S191), the six bench keys to GLASS (S192); the row's own criterion (3) is met** | **The observability standing debt: 57 unsigned glitches at 60 sites across 3 lanes.** SYM-027's criterion (3) has never been met | the acceptance run today printed the census · `Observed 2026-09-15 (S157 E31)` — re-measured at HEAD: `observability/acceptance.py` census **39 unsigned glitches at 42 sites across 3 lanes** (was 57 at 60); `glass_detector.py --since d0e193b --enforce` clean. SYM-027's criterion (3) still unmet |
| ~~B4~~ **STRUCK 2026-09-15 (S157 E4) — DONE** | **`windows-converter/` is outside CI's lint set** — SYM-018's exact shape. `backend_parity.py`, `figure_coverage.py` and `convert_and_ship.py` ship unlinted ⟨status 2026-09-04: DONE — 2026-09-04 verifier (HEAD bde007d): DONE — ci.yml:69-72 lints windows-converter (RESOLVED S108 wave 1b). Strike.⟩ | `ci.yml` lints `linux-receiver`, `linux-dashboard`, `linux-converter` only **RESOLVED S108 (wave 1b)** — windows-converter joined the CI python job (ruff + hermetic selftests, warn-only until CI observed green; deferral/card-mutex selftests excluded by name). · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — .github/workflows/ci.yml:92-94 `Lint (windows-converter)` → `ruff check .`, no `continue-on-error`; run 34937129934 (num 366) green on this HEAD with that step in it |
| ~~B5~~ **STRUCK 2026-09-15 (S157 E16) — BUILT — the DOM run stays open, named** | **Nothing in this repo tests `bench.html`.** "Bench acceptance 85/85" was cited in a commit whose entire diff is that file; the suite has not changed since 2026-08-14 and never loads the DOM | S106, and SYM-052's own row says no tripwire guards the regression **PARTIAL S108 (`abe4830`)** — 19-test stdlib headless harness with positive+negative controls (both S106 regressions are named fixtures); in-browser DOM run remains open. · `Observed 2026-09-15 (S157 E16)` — `prototypes/repair-bench/test_bench_page.py`: 82 `def test_` (19 at S108's `abe4830`, grown since), `python -m unittest` → `Ran 82 tests … OK` now (Observed); SYM-052 FIXED. "Nothing in this repo tests bench.html" is no longer true. Residue, as the S115 re-measure named it: the in-browser DOM run (a real browser, not a parser) — a separate ask, his to schedule (the bench is public code and his screen) |

### B.2 Symptom fixes that are designed but unbuilt

| # | Item | Symptom |
|---|---|---|
| ~~B6~~ **STRUCK 2026-09-15 (S157 E16) — FIXED S151 E2** | One-line fix: read `importlib.metadata.version` | SYM-044 · `Observed 2026-09-15 (S157 E16)` — `convert_and_ship.marker_version_stamp()` (`:1412`): the installed distribution's version through `importlib.metadata` — `1.10.2` on this machine now (Observed, a call); commit `87db1a7` an ancestor of HEAD; SYM-044 reads FIXED with its six tripwires; bundles converted before S151 keep `unknown` (the Valentine anchor manifest does — an honest absence, not a regression) |
| ~~B7~~ **STRUCK 2026-09-15 (S157 E19) — BUILT** | A mechanical **id preflight** so concurrent same-machine instances stop colliding on every monotonic counter | SYM-045 · `Observed 2026-09-15 (S157 E19)` — private `agent-scripts/auditing/id_preflight.py` (+ brief; `id_preflight_selftest.py` 10/10; the smoke 74/74): `SYM-133` → TAKEN with three file:lines, `--next SYM` → `SYM-134` free, `--next ERR --date 2026-09-15` → `ERR-2026-09-15-109`, `--next S` → `S158`, `--next docs` → `docs/61` (each read from its register surface, then preflighted across the tree). Residue: a read-before-write, not a lock; the filing acts must call it (the lane's protocol from this episode on; the muster's own session-number collision check stays) |
| ~~B8~~ **STRUCK 2026-09-15 (S157 E4) — DONE in substance** | A close-ritual check that **refuses a closeout whose glass claim lacks `--since <pin> --enforce`** ⟨status 2026-09-04: DONE — 2026-09-04 verifier (HEAD bde007d): DONE in substance — close.sh [2] GLASS (`c9c0cf3`, 2026-08-20; header cites SYM-046) runs `--since <pin> --enforce` at every close and exits 1 on an unsigned glitch. The literal 'refuse a closeout whose CLAIM lacks the form' text-check is not built and is moot once the close measures the form itself. Strike; flip SYM-046 (docs/50:125 still 'open').⟩ | SYM-046 · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — `.claude/skills/muster/close.sh:107` runs `glass_detector.py --since "$PIN" --enforce` and exits 1 on an unsigned glitch (c9c0cf3, an ancestor); the literal claim-text check is moot |
| ~~B9~~ **STRUCK 2026-09-15 (S157 E4) — DONE (residual named)** | The watcher writes a **pid/parent file** the widget can compare at boot (a Job Object cannot reach backwards over a process it never spawned) ⟨status 2026-09-04: DONE — 2026-09-04 verifier (HEAD bde007d): DONE — `f045a66` (Conveyor State, 2026-08-31): the watcher's `.intake-state.json` carries writer_pid, refreshed every <=5 s (watch_and_convert.py:584); the widget reads it at boot (main.js:613, watcher.rs:182), refuses a second watcher beside a live writer (watcher.rs:212) and tree-kills it on stop. Residual: pid not parent; a writer silent >300 s (watcher.rs:128) is invisible — SYM-047's hand census stays for that case. Strike; flip SYM-047 (docs/50:126 still 'open').⟩ | SYM-047 · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — `windows-converter/watch_and_convert.py:223` writes `writer_pid`; `windows-widget/src-tauri/src/line.rs:72/84/213` reads `.intake-state.json` and its `writer_pid` (f045a66, an ancestor). Residual as the verifier said: pid, not parent; a writer silent > 300 s is invisible |
| ~~B10~~ **STRUCK 2026-09-15 (S157 E27) — ANSWERED S157 E25** | **Clustering that follows stroke geometry rather than bounding boxes** — zero-area connectors are dropped before clustering, so a spread-out diagram fragments and is missed. Two measured specimens (Cyb p34, p78). *Not a threshold nudge; needs its own calibration* | SYM-049 · `Observed 2026-09-15 (S157 E27)` — SYM-049 FIXED by the smaller move the measure justified: a zero-area path is clustered when ANCHORED (within the gap of a positive-area path or a perpendicular line — `_anchored_lines`), and the table veto is disqualified for a non-rectilinear cluster (`_nonrect_items` ≥ 3 sizable curves/diagonals). Its own calibration, as the row demanded: every anchor before/after (`sittings/S157/e25/anchors_diff.json`) — the Book of Models surfaces exactly the two specimens (p.34, p.78: 1 → 3 uncovered), no other book moves a page; selftest 44/44 with both negative controls. Stroke-geometry clustering itself was NOT built; the residue is a rectilinear chart (Cyb p.40) still read as a table · `Corrected S157 E39` — the residue named here (Cyb p.40 read as a table) does not hold under the shipped code: p.40 is REPORTED (verifier V1-06 against `e25/anchors_diff.json`); no rectilinear-chart residue is known on the measured corpus |
| ~~B11~~ **STRUCK 2026-09-15 (S157 E53) — the drift repaired in both files, with the tripwire the row asked for** | Fix the supersede/pending **projection drift** in `main.js` + `room.js`, and add a tripwire that fails if "pending/manual" wording returns while the handlers exist | SYM-043, S97 §18.1 · `Observed 2026-09-15 (S157 E53)` — `main.js:934` and `room.js:462` said `swap: manual — supersede (flow) pending` beside the ⟳ handler that AUTHORS the supersede intent (`assay.rs` reconvert; the exporter's guard replaces the vaulted note, live-fired S50/S56); both now say `swap: automatic — a passing ⟳ re-convert supersedes the vaulted note (exporter guard)`. The tripwire: `windows-widget/src/projection_selftest.py` (stdlib; reads the RENDERED `ac-swapnote` span, never a comment; reds if the drift wording returns while `.ac-remedy` exists; two negative controls plant it back) — 12/12, controls 4/4; a warn-only CI step. SOURCE ONLY: the installed exe (C4559805) shows the old note until his rebuild ritual (docs/19; `cargo fmt --check` leads) and adoption |
| ~~B12~~ **STRUCK 2026-09-15 (S157 E17) — BUILT** | Capture **Ollama's own server log** at the moment of the stall if SYM-034 recurs — today only the client side exists | SYM-034 · `Observed 2026-09-15 (S157 E17)` — `analyst._capture_ollama_log`: on a failed local generate call the last 200 lines of `%LOCALAPPDATA%\\Ollama\\server.log` are written to the analyst work root's `_captures/ollama-<key>-<i>-<ts>.log` and ledgered (dumps/dump.sh, DUMP_LANE=pipeline); `manifest.analyst.backend_failures` rows `{i, error, detail, ollama_log}` name each failure (UNREAD when the log or the ledger cannot be reached; `n/a` on the Gemini route); same-commit dispositions `converter:backend_failures / ollama_log / error / detail`; glass `--since d0e193b --enforce` clean, acceptance 181/181; `analyst_selftest` S157-E17 (a) a planted log's tail captured and DUMPED, the original ships, (b) no failure → no capture, (c) no log → UNREAD — 47/47; ship 254/254; ruff clean. The first cut wrote the capture inside the work dir — rmtree'd at assembly (J41's grave); the tripwire found it |

### B.3 Instruments built but never wired to a human

*This is the project's most-repeated failure class — "instrument not wired", named at S76 and
committed again five times since.*

| # | Item | Source |
|---|---|---|
| ~~B13~~ **STRUCK 2026-09-15 (S157 E57) — the three endpoints reach the glass, with their tripwire** | **The bench UI has no buttons for triage / report / ledger** although `/api/triage`, `/api/report` and `/api/ledger` exist and are proven. Only the collapse and omission chips reached the glass | S76 §18.4 · `Observed 2026-09-15 (S157 E56)` — scoped: `bench.py` exposes `/api/triage` (POST key/outcome/reason; the human-only outcomes, a dismissal needs a reason), `/api/report` (POST; `write: true` writes REPAIRS.md) and `/api/ledger` (GET) — all proven by `test_bench_page.py`; `bench.html` (2,932 lines) reaches none of them. The build: a triage control per site (zone/omission keys the page already renders), a report preview with an explicit write, a ledger panel — testable headless on a sandbox copy (the acceptance harness's shape). The next build episode · `Observed 2026-09-15 (S157 E57)` — `bench.html`: `≡ ledger` (GET `/api/ledger` → a modal: the chain audit line — intact or BROKEN —, the triage coverage, every event oldest first with gesture, chars, where, note), `▤ report` (POST `/api/report` in memory → the markdown; `✎ write REPAIRS.md` behind a confirm), and a TRIAGE row under the selected site's diagnosis — the current outcome, `∅ dismiss as noise` (the server's reason requirement asked on the page) and `↺ reopen`; only the human-only outcomes (`OUTCOMES_MANUAL`) are offered, the derived ones are shown as `derived`; nothing in reading mode. `test_bench_page.py` + 4 (`TestB13BenchButtons`: the controls and handlers in the source; the offered outcomes equal `bench.OUTCOMES_MANUAL`; a negative control on the source; the three routes through the REAL handler on a temp bundle — a reason-less dismissal refused, the manifest gains the triage, the ledger's `{events, audit, coverage}`, the report writes only when asked, reopen withdraws) — 89/89. Seen live on a SANDBOX copy of the held Valentine at :7099 (the ledger modal: chain intact · 7 writes · 1 of 4 sites addressed · 10 events; the triage row on an open zone; the report refused without `--token` and the page said so — the S108 gate the widget satisfies), the server stopped, the sandbox copy removed, the held bundle untouched |
| ~~B14~~ **STRUCK 2026-09-15 (S157 E18) — BUILT** | **`MIN_SIDE_PT` and `MAX_PAGE_FRACTION` are unlevered and unwaived**, which makes `min_area_pt2`'s advertised 100–100000 range **inert**: at `min_area_pt2=100` all 18 newly-admitted Cybernetics clusters are killed by `MIN_SIDE_PT`, the report is byte-identical, and nothing warns | S106 · `Observed 2026-09-15 (S157 E18)` — `figure_coverage.py`: `min_side_pt` (1–400) and `max_page_fraction` (0.10–1.0) in `LEVER_SPEC` at the old constants (nothing moves unwritten; an out-of-range line is refused and named like every lever); the three filter sites read the effective values; `filters_killed` counted per filter per kind (raster: min_area_pt2 / min_side_pt / max_page_fraction; vector: min_paths_or_area / min_side_pt / max_page_fraction) and carried in the report's `conditions` — the ticket's inert change reads as a count on the next filter, never a byte-identical report and silence. `figure_coverage_selftest` + 6 (the levers parse, range and take effect; a bars fixture where min_area_pt2=100 admits six regions min_side_pt then kills — counted; the negative control under the defaults: killed by area, min_side 0; the report carries the count and the effective side) 36/36; `schema_registry.py --write` then `--check` PASS (`p1_page_coverage.conditions.filters_killed` registered); ruff clean; coverage_rescore OK; ship 254/254 |
| ~~B15~~ **STRUCK 2026-09-15 (S157 E44) — the entry is in the bank** | **The omission-class bank entry** — 18/18 runs on the Beer are unclassified; the signature bank is blind to text that *isn't there* | S79 §18.5 · `Observed 2026-09-15 (S157 E31)` — the omission class has a LOCATOR for prose now: private `measurement/probes/prose_loss.py --attribute-runs` gives the audit's analyst-stage runs (page null) their page and line bbox from the pages' missing lines — IV UE 10 of 25 runs, the held 4e 6, Ashby 5, DDIA 4; the rest are equations/tables/short lines. The bank entry itself (reason · highlight · solution) can be written with a real highlight; the Beer's 18/18 unclassified runs are a scan-lane book the probe cannot read (its page text is the OCR) — that half stays · `Observed 2026-09-15 (S157 E44)` — `prototypes/repair-bench/signatures.json` entry G (`omission_run`, Inferred): reason · highlight · solution for text that is not there; `bench.py` fires it on run-shaped sites only (the run's words, page and first words as `matched_on`; a null page said UNREAD) and G leads the order so a run is never read as a wrapped cell; tests 82 → 85 (a zone still reads E — the negative control); the acceptance harness 85/85 (public `a8e53c2`). The Beer's 18/18 unclassified runs diagnose as G from now on; their pages are the audit's (the convert stage carries them) |
| ~~B16~~ **STRUCK 2026-09-16 (S161 E2) — DISPOSITIONED, both EVIDENCE (docs/29 §5.4's other branch): `converter:seams` is read by code (the slices count, `convert_and_ship.py:996/:1756`) and docs/18 §5.2 promised it findable, not rendered; `converter:reverse_sample` mirrors the census's own N-266 EVIDENCE (a 200-window sample whose n never travels with it — a headline render is the promotion docs/34 forbids). `acceptance.py`'s two answer-key rows corrected GLITCH → evidence with dated comments (the S115 precedent); the detector's unsigned glitches 39 → 37 at HEAD. Was:** | **`seams[]` and `reverse_sample`** are referenced by zero renderers; `pages_scored` only as a positioning denominator | SYM-027 |

### B.4 Measurement debt

| # | Item | Source |
|---|---|---|
| B17 | **Re-measure throughput with the A-B-A control on an idle card.** The instrument is built and validated; a clean run is what is missing. **Do not quote 0.77×, 0.62×, 0.95× or 0.81× — all four are withdrawn** | S82 §18.2–3 · **STILL OPEN, and a candidate nobody has checked. `Observed 2026-08-27`:** `windows-converter/backend_parity.json` (mtime 2026-08-16 01:33) holds **four arms at n=30 each** — `ollama_think_false`, `llamacpp_jinja_default`, `llamacpp_jinja_nothink`, `ollama_recheck`. The `ollama_recheck` arm IS the A-B-A return leg, and n=30 meets B18's floor. **`Unknown` whether the card was idle** — the file records `ollama`/`llamacpp` versions and `llamacpp_load_s`, but no card-occupancy field, so the one condition B17 names cannot be read from the artifact. Resolved by re-running with occupancy recorded, or by finding the run's log. ⚠ The file is **gitignored** (`.gitignore:51`), so it is invisible to anyone reading the repo — which is plausibly why two sessions left these rows open beside a finished run. |
| B18 | **Raise `n` to ≥ 20–30** before any ratio is quoted. 5–8 of 266 chunks is `Inferred` about the book; the no-think arm runs ~5 s/chunk, so n=30 is ~3 min of card time | S80 §18.3, S81 §18.2 · **STILL OPEN, and a candidate nobody has checked. `Observed 2026-08-27`:** `windows-converter/backend_parity.json` (mtime 2026-08-16 01:33) holds **four arms at n=30 each** — `ollama_think_false`, `llamacpp_jinja_default`, `llamacpp_jinja_nothink`, `ollama_recheck`. The `ollama_recheck` arm IS the A-B-A return leg, and n=30 meets B18's floor. **`Unknown` whether the card was idle** — the file records `ollama`/`llamacpp` versions and `llamacpp_load_s`, but no card-occupancy field, so the one condition B17 names cannot be read from the artifact. Resolved by re-running with occupancy recorded, or by finding the run's log. ⚠ The file is **gitignored** (`.gitignore:51`), so it is invisible to anyone reading the repo — which is plausibly why two sessions left these rows open beside a finished run. |
| B19 | **The analyst has no llama.cpp call path.** It speaks Ollama `/api/generate` with a raw prompt; parity was measured against `/v1/chat/completions`. **Whatever is built must carry `enable_thinking:false`** or it silently reverts to the 6.5× arm — and a truncated rewrite drops trailing `⟦IMG-n⟧` and ships the un-analyzed original | S80 §18.4, S81 §18.3, S82 §18.5 |
| B20 | **Vary `-ub` on the one engine** before concluding anything about two products — Ollama runs `llama-server` internally, so this is one engine under two invocations | S82 §18.4 |
| ~~B21~~ | **DONE S151 E5 (2026-09-14, Valentine, both copies):** the held copy's July verdict (`fail` on degeneration, pre-J29) rested on four zones — three pipe tables (Exhibit 8.2's two halves, Exhibit 3.3) and ONE real loop (`the purpose of` × 115 on the page's line "assess a company's likely success…", printed p.117); today's audit with the rows blanked keeps the loop and drops the tables on both conversions — the verdict stands for the right reason. The anchor's Exhibit 15.1 (line 3047, printed p.219) is SYM-067's specimen in this book: a real table padded to 29,107 chars for 43 cells, zlib 0.03, invisible once blanked. D5 (a loop of table rows only) not observed. Residue: the loop itself unrepaired (the bench or the vision route); Exhibit 15.1's run-together first cell is a conversion defect; the July `degeneration_detail` stays in the manifest until a re-audit. History kept: **Test the table-loop deliberately**: map a degeneration zone's lines to its page, render, probe. One page answers what the S84 GLM probe did not | S84 §18.3 |
| ~~B22~~ **STRUCK 2026-08-27 — RESOLVED** | **The whole-file Bench render costs ~1 s per zone click and ~1.2 s per newline** at IV's size. The "105 ms" timed the lazy `innerHTML` assignment and excluded the forced layout | S106 **RESOLVED S108 (`f9585b3`)** — zone click is highlight-only when text is unchanged; the perf log now INCLUDES forced layout so the S106 measurement substitution cannot recur. · `Observed 2026-08-27` — commit `f9585b3` *"bench: B22 - zone clicks move the highlight, not the whole file"* |
| ~~B23~~ **STRUCK 2026-09-15 (S157 E25) — all three clauses answered; residue: the 4e (2025) bundle's 49 pages** | **219 of the 239 IV uncovered pages were never adjudicated**; the other four anchors were never measured at all; SYM-049's fragmentation class is verified for Cybernetics only | `docs/45` §6.5 · `Observed 2026-09-15 (S157 E24)` — clause 1 done on the CLEAN University Edition bundle (SYM-050 not detected): 76 uncovered → **11 lost figures** (5 absent, 6 flattened into tables/prose), 65 not figures, every page read by two Sonnet lanes + the lane (`sittings/S157/e24/adjudication.json`); the 4e (2025) bundle reads 49 uncovered under the in-memory SYM-050 repair (239 unrepaired) and is NOT adjudicated; clause 2 done — every anchor measured (`e24/anchors_table.json`: Ashby 9 · Book of Models 1 · DDIA 2 · Zero to One 5 · claude-code 0; the three scan-lane books NOT APPLICABLE); clause 3 unchanged — SYM-049 open, the drop at `figure_coverage.py:528` today, Cybernetics only · `Observed 2026-09-15 (S157 E25)` — clause 3 answered: SYM-049 FIXED (anchored zero-area lines clustered; the table veto disqualified on non-rectilinear clusters) and MEASURED on every anchor — the Book of Models surfaces exactly p.34 and p.78 (1 → 3 uncovered), no other book moves (`sittings/S157/e25/anchors_diff.json`); figure_coverage_selftest 44/44. Struck. Residue: the 4e (2025) bundle's 49 repaired-map uncovered pages are measured, not adjudicated (the E24 reader apparatus runs on them when wanted); a rectilinear chart (Cyb p.40) still reads as a table · `Observed 2026-09-15 (S157 E30)` — the residue paid: the 4e (2025) bundle's 49 repaired-map pages adjudicated by the same two-reader apparatus (5/5 plants; 48/49 agreed, one decided by the lane): **10 lost figures** (4 absent, 5 flattened, 12.3 both), 39 not figures; nine of the ten are the University Edition's nine (`sittings/S157/e30/adjudication.json`) |

### B.5 Long-standing build candidates

| # | Item | Source |
|---|---|---|
| B24 | **Events rotation + `verified_from` stamps**; **cross-day event-row date stamp** — build candidates carried in §18 from S68 through S70, never picked up | S68/S69/S70 §18 · `Observed 2026-09-15 (S157 E56)` — measured: `events.jsonl` is 391 rows / 113 KB spanning 2026-07-19 → today, never rotated; `events.py` carries no rotation and no `verified_from`. The writers are the FIELD's processes (the watcher, the converter, the analyst emit through it) — on this sitting's never-edit list; the design (rotation by day vs size, where `verified_from` is stamped) is a word for the owner. **MEASURED again S180 E4 (2026-09-17 03:43Z):** `events.jsonl` 437 rows / 126,062 bytes, 2026-07-19 → 2026-09-17 (Jul 81 · Aug 170 · Sep 186 rows) — +46 rows since S157's measure, still never rotated. **What a rotation would break, read:** the widget's `algedonic.rs` reads the WHOLE file (`fs::read_to_string(base.join("events.jsonl"))`, :85) for every `stalled` / `failed` / `held` / `verdict_fail` fact and its own tests write it (:350–:437); `assay.rs` reads manifests, not events. A rotation by day would drop every alarm older than today from the Room unless `algedonic.rs` learns the rotated names — so the design is two-sided (Python's writer AND the Rust reader; the reader's change is his rebuild); the restart window (Desk 8ca8279b) now permits the writer's edit, the reader's does not. The lane's proposal, in his shape, when the field's growth demands it (126 KB does not): rotate by SIZE at a threshold, keep the current file whole for the readers, and have `emit` name the rotated file in its first row. Waits for his word |
| ~~B25~~ **STRUCK 2026-08-27 — ROW WAS STALE WHEN WRITTEN** | **`room-chat` graduation**: `llama_server_exe` config key + **adopt the llama-server spawn into the watcher's Job Object** (a hard kill currently orphans it — the S37 lesson, un-applied here) | S79 §18.3 **ROW WAS STALE — corrected S108**: the graduation shipped at S85 (config.rs llama_server_exe + adopted spawn, verified at HEAD by lane G); the orphan claim was false when this register was built. · `Observed 2026-08-27` — `llama_server_exe` live at `windows-widget/src-tauri/src/chat.rs:8,62,64` |
| B26 | **Remove the wrong-page embed in Valentine** | S79 §18.5 |
| ~~B27~~ **STRUCK 2026-09-17 (S181 E1) — APPLIED under the restart window his word opened (Desk 8ca8279b, 01:08Z): the two branches in `resume()` exactly as S157 E46 drafted them (same backend → `analyst/skipped` and the parked pass ships, no second anchor copy; a different backend → refused toward `--reanalyze`, J42; no prior → the analyst as before); the tripwire in `convert_and_ship_selftest.py` (five checks, 287/287 under the marker-env interpreter) with its POSITIVE CONTROL run beside it (`sittings/S181/e1_b27_control.py`: the same fixture against S157's text reads analyst calls 2 and no event; against the fix 1 and `analyst/skipped`); the event kind in the A4 registry by `schema_registry.py --write` (writer keys backend · bundle · reason + the envelope; consumer keys backend · bundle), spoken by `event-vocab.js` (source only — the phrase reaches the Dock and the Room at his rebuild) and named in docs/22; glass `--since f2b36e8 --enforce` clean, acceptance 212/212, the watcher's suite PASS. No restart needed — the child is spawned per conversion; the NEXT resume lands on the new text. The live proof is the field's: the next retry click after a ship failure past the analyst (a ThinkPad outage or his hand) — not the lane's to stage** | **`resume auto-detect-analyzed`** (Desktop one-liner) — ⚠ this line exists **nowhere else on this machine** than the memory open queue | memory `file-portal-project` · `Observed 2026-09-15 (S157 E46)` — READ: the line is S18's queue mechanic (`--resume <id> --backend local` applies the analyst IN PLACE in pending/<id>/, then ships; a failure past the analyst flips the card `failed` for a retry click) and `resume()` never asks whether the parked bundle is already analyst output. MEASURED on a fixture (the analyst a fake, ship raising once as the offline ThinkPad does today): the retry ran the analyst a SECOND time — the note's frontmatter two `analyst:` blocks, the body two passes, two anchor copies, the manifest only the last; `--backend none` twice ran it zero times (the control). The fix is two branches in `resume()` (same backend → ship the parked pass, an event said; a different backend → refused toward `--reanalyze`, J42) with its tripwire drafted — READY, NOT APPLIED: the file is on this sitting's never-edit list (the field's processes read it). Private `sittings/S157/e46/b27_resume_guard.patch.md`; one word applies it |
| B28 | **Windows half of the v2 feedback loop**: the widget polls `status.json` and shows delivered → sorted → failed per transfer; **toast/notification** on complete or fail | `docs/08` v2 · **MEASURED 2026-09-17 (S182 E2), the readable half read whole, no source written:** the poll half is `main.js` `pollStatuses` → `invoke("fetch_file_status")` (`status.rs`: `cat ~/file-portal/logs/status.json` over tailscale ssh) — **10 tries × 3 s = 30 s per sent batch**, then the status line reads `Sent -- allocator pending for N file(s).` and the watch ENDS: an allocation that lands after 30 s is never shown; `applyStatusEvent` writes the status line (`✓ file → dest · writer` / `✗ rejected: reason` / `⚠ skipped: reason`) and pulses the tile's class for 3 s (success) or 5 s (error) — a glance-timed cue, nothing persistent, no sound, no OS toast. The notification half: `Cargo.toml` carries no `tauri-plugin-notification` (only single-instance), no capability grants it, zero hits in `src/`. **Two shapes, his to choose:** (A) the OS toast — `tauri-plugin-notification` (a Cargo dep + a capability entry + ~20 lines in Rust behind one `notify(title, body)` command; `applyStatusEvent` calls it on `allocated`/`rejected` and on the 30-s give-up) — reaches him when the Dock is not on screen (the phone-dispatch north star's shape); (B) the tile alone — `pollStatuses` continued at 15 s until every sent file resolves, the tile's pulse made persistent until clicked — no new dependency, invisible when the widget is hidden. COST: either is one sitting of source + HIS rebuild and adoption (C4559805 → a new hash); (A) adds a plugin to the release. Nothing built until his word names the shape.** · `Observed 2026-09-15 (S157 E56)` — half exists: `main.js` `applyStatusEvent` polls `status.json` and renders allocated ✓ / rejected ✗ / skipped ⚠ per transfer on the tile (E54's terminal outcome rides the ✗ path); the OTHER half — a toast / OS notification on complete or fail — has zero hits in the widget source. A widget change → his rebuild + adoption; the notification's shape (Tauri's notification plugin vs the tile) is a choice worth his word |
| B29 | **Package the widget** (`cargo tauri build`) and document install steps for a second machine | `docs/08` v1 · `Observed 2026-09-15 (S157 E56)` — `windows-widget/README.md:22-42` documents `npm run tauri build` and the shortcut script for THIS machine; a second-machine install (the ThinkPad has no widget; a Mac was the S-early vision) is undocumented and unbuilt. The build itself is his rebuild ritual (docs/19); the doc could be written after one clean second-machine run, which nobody has done — UNREAD, not 'done' |
| B30 | **Screenshot/demo GIF for the README**; decide public vs private repo and finalize `CONTRIBUTING.md` | `docs/08` v3 |
| ~~B31~~ **STRUCK 2026-09-17 (S181 E2) — MET IN THE FIELD BY HIS HAND, unrecorded until read: the ThinkPad's own records (read-only over the keyed channel, 04:09–04:11Z; private `sittings/S181/e2_b31_read2.txt`). **v0** — `~/file-portal/logs/status.json` holds 41 allocator events since 2026-07-05: 34 `allocated` + 6 `rejected` in July (39), one `allocated` 2026-08-07, one 2026-09-16 (S172's sweep plant, the lane's); the sorted tree today: photos 15 (newest 08-07), misc 5 (07-12), quarantine 1 (a Wheeler PDF, 07-12 — the oversized/rejected path exercised); the categories are the tiles' (`convert-scan`, `convert`, `photos`) — the route was the widget's over Tailscale (Inferred from the categories; the July rows predate S108's writer identity). **v1.5** — 2026-08-07T04:36:35Z a widget screenshot (`file-portal-widget_dxDfv6ZTrU.png`) allocated into `sorted/photos/2026/08/`; 67 s later (04:37:42Z) the dashboard wrote `~/.config/file-portal/dashboard.toml` (its defaults — a first load or a close; either is the app ALIVE at that second, refresh 30 s); the user journal shows the desktop session opening at 04:34:44Z; the launcher installed 2026-06-25. Observed: the dashboard running within 67 s of an allocation, in his session, at S65's dawn (00:36 EDT). Inferred: the photo on its grid (the live-update watcher's job — nothing logs the grid). No closeout of S64/S65 names it; docs/08's boxes were never ticked — the field ran ahead of the record. docs/08 gains the dated append; the boxes stay as written. His to reopen with a word if he wants the test RE-RUN and watched** | **End-to-end manual test on real hardware** — open in `docs/08` v0 *and* v1.5 (dashboard with the allocator actively sorting) | `docs/08` |
| B32 | **Codex's completion audit exists, names seven source-cited product defects, and had reached NO register and NO relay entry before its S111 recovery.** `C:/Users/Bndit/Documents/Codex/2026-08-27/sca/outputs/file-portal-completion-audit-2026-08-27.md`, 37,957 bytes, 420 lines. Its three sibling reports were each announced on the bus; this one had not been. **U01** receiver startup backlog sweep — a restart with pre-existing inbox files must allocate exactly once (`linux-receiver/allocator/main.py:169-202`) · **U02** malformed widget config panics with no operator-visible remedy (`config.rs:112-120`, `main.rs:609-612`) · **U03** the vault-fixity service and timer exist but `linux-converter/scripts/install.sh:17-26` never installs them, so a fresh machine silently has no fixity checking · **U04** Room/Wall latest-event ordering is inverted — Rust returns newest-first while `room.js:952-953` takes the OLDEST of the bounded tail · **U05** receiver rule/config failure logs an exception and writes no terminal state, so the widget waits forever on a job that already died · **U06** the observability stale-signature control is history-dependent and fails when no exercising diff exists in 39 ancestors — a control that stops being able to fire, the same decay class as `close.sh`'s CI check. **U07 is filed separately as SYM-057.** | Filed as ONE row, not six: the detail lives in the audit and duplicating it here would grow the register by six for one document — exactly what B3 now measures. `Observed 2026-08-27` — the file was read by the Fable lane and U07 was independently verified at source; **U01–U06 are Reported, not Verified**, and each needs an independent probe before it is acted on · `Verified 2026-09-15 (S157 E51)` — each probed at HEAD c0d66be by the lane in the source: **U01 HOLDS** (`linux-receiver/allocator/main.py:169-215` arms the watch and never sweeps the inbox — a file that arrived while the service was down is never allocated); **U02 HOLDS** (`main.rs:622` `load_or_init().expect(…)` under `windows_subsystem = "windows"` — the parse error names the file and nobody sees it); **U03 FIXED here** (`install.sh` now templates and installs the fixity service and copies + enables the timer; `tests/test_install_script.py` 4/4 with a negative control; the ThinkPad's live timer state UNREAD, offline); **U04 HOLDS for the Wall** (`room.js:1007` `slice(-1)[0]` on the newest-first tail from `events.rs:58` = the OLDEST event; the numerations panel was fixed by review M1 at `:500-502`; the cited 952-953 moved) → the main.js cluster; **U05 HOLDS** (`main.py:62-68` logs the exception and writes no status record) → with U01, a receiver episode with its pytest; **U06 RESOLVED at S138** (SYM-118: `_stale_caught_under_since` falls back to HEAD~1, SKIPs only on a single-commit history — not the synthetic fixture Codex asked for; the file's own law is end-to-end against the real trees). U02 is a Rust source change CI can compile and test — no adoption. Four builds remain; the row stays open for them · `Observed 2026-09-15 (S157 E54)` — **U01 FIXED**: `sweep_inbox(handler, paths)` runs once at startup after the observer is armed (nothing arriving during the sweep is missed) and hands every pre-existing `inbox/<category>/` file to the handler — dot-prefixed in-progress files skipped, quarantine outside the inbox by design; exactly once: a file the observer allocates first vanishes under the sweep and is not recorded twice. **U05 FIXED**: a failed allocation records a terminal `rejected` outcome with `reason = allocation failed: <exc> (file left in inbox/<category>)` — the action the installed glass already renders as ✗ — and leaves the file (unlike a quarantine); a vanished file is the other path's, logged at INFO, no record. `tests/test_startup_and_failure.py` 6/6 (two negative controls: no sweep → the file sits; the vanished-file race records nothing) beside the allocator suite; ruff check + format clean; CI's hard Test (linux-receiver) step is the proof (ubuntu). The ThinkPad's running service is the OLD code until its restart — his hand, offline now. Three of six done (U03/U01/U05), U06 resolved at S138; U02 and U04 remain: U04's source fix landed at E53 (adoption his), U02 is the Rust episode · `Observed 2026-09-15 (S157 E55)` — **U02 FIXED at the source**: `main.rs` `main()` matches `config::load_or_init()` — on `Err` a native `MessageBoxW` (windows-sys `Win32_UI_WindowsAndMessaging`, no new crate; the lock unchanged) shows `config_failure_text(why)` — the parse error naming the file (already in `load_or_init`) plus the remedy (fix it, or move it aside for a fresh default) — then `exit(2)`; the text is pure and tested (`config_failure_tests`, a negative control on the bare error). docs/19's ritual: `cargo fmt --check` led, `clippy --all-targets -D warnings` clean, `cargo test` 37/37; CI's windows-latest rust job is the second proof. NO adoption: the installed exe (C4559805) panics silently until his rebuild and adoption. ALL SIX DISPOSED — U03/U01/U05 built and proven on CI, U04 and U02 fixed at the source, U06 resolved at S138 — the row stays OPEN for the two hands that are his: the ThinkPad's allocator restart (U01/U05) and the widget's rebuild + adoption (U02/U04) · `Observed 2026-09-16 (S172 E2, his word fda0dc27 "Thinkpad is on." + 0e51eae9 "Touch thinkpad")` — THE THINKPAD HALF DEPLOYED: the allocator's clone (`/home/rab/file-portal-src`, at `cc64dd2`, 300 commits behind, no `sweep_inbox`, five boots' journals without a `startup sweep:` line) fast-forwarded to `2eb9f4a` (clean; no requirements change), the service restarted 23:51:08Z (pid 887 → 3238) — the journal's first line under the new code `startup sweep: 0 pre-existing file(s) handed to the allocator`; the LIVE PROOF with his word: stopped, one empty `S172-sweep-plant.txt` in `inbox/documents`, started → `ALLOCATED … -> sorted/documents/S172-sweep-plant.txt` · `startup sweep: 1 pre-existing file(s)`, the status feed's `allocated` event, the plant removed (pid 3354 running). The converter service untouched (pid 889; its next restart carries J40's wording). The row stays OPEN for the widget's rebuild + adoption (U02/U04) alone. |
| B33 | **THE SOURCING LINE's first class finding (S209 E3, 2026-09-20): born-digital FINANCIAL TABLES fail at convert on the clean lane, and the document number hides it.** Public institutional PDFs found, verified and queued through PORTAL's door (private `file-portal-private/sourcing/`; Rab's word 13:58Z; his checklist word 90f75d88 — the errors predicted BEFORE the results, then scored). Banks: RBC (Royal Bank of Canada) — held 0.7857 (tables flagged 25/30; conv÷layer 2.222; 12.81 s/page); CIBC — Report to Shareholder done 0.7931 (tables flagged 21/22; conv÷layer 1.957; 1.92 s/page). Government: Office of the Parliamentary  done 0.9121 (tables flagged 1/6; conv÷layer 1.517; 1.51 s/page); Bank of Canada — Monetary Po done 0.873 (tables flagged 2/2; conv÷layer 1.044; 1.7 s/page). What the pages show (RBC p.39, the NSFR template): the converter's table exists and is destroyed — the header row fused into one cell, the same numbers repeated down the rows with `<br>`, every row label gone — and the analyst drops the wreck; the layer holds the table cleanly (pymupdf). Marker also re-OCR'd RBC (converter chars 2.2× the layer's; 12.8 s/page vs 7.0 promised; `Recognizing Text 10067/10067`) while the MPR converted at 1.7 s/page — the fonts, not the class. **To build (mechanical):** (1) a STRUCTURAL table-survival measure in the audit's convert block — row labels kept ÷ row labels in the layer's table region, header row present, column count == the layer's — reported per table and by page class beside `doc_survival` (a numeric-cell count reads high on p.39 while the table is unusable); (2) the OCR-vs-layer decision per page as a dial (`converted` event: pages Marker chose to OCR, the chars ratio). **His (a lever):** trust the text layer when the probe reads `text_layer True` and the fonts are embedded — warn-first, his signature. The instrument: `sourcing/postmortem.py`; the checklist: `sourcing/CHECKLIST-2026-09-20.md`; the notes: `sittings/S209/e3_notes.md`. **THE DIAL BUILT, record-only (S209 E8, 2026-09-20 19:1xZ):** every `Recognizing Text` bar Marker shows is noted (`_note_ocr_bar`; `ocr_dial(pages)` → `manifest.ocr = {lines, bars, lines_per_page}`); the `converted` event and the ledger row carry `ocr_lines` / `ocr_lines_per_page` (RBC's shape 132/page, C-31 64, the SEU 5, CIBC 0); `estimate_from_ledger` names `ocr_heavy_neighbours` (> OCR_HEAVY_LINES_PER_PAGE = 20) on the `estimate` event and prints ESTIMATE WARN — and EXCLUDES NONE: the SEU's promise was 6.29 s/page from RBC's and C-31's re-OCR'd pace against 1.52 measured (the third instance today); the exclusion is the threshold's use and needs Rab's signature. `ocr_dial_selftest.py` 6/6 (marker-env; not in CI — convert_and_ship's imports). Schema registry regenerated; the three keys signed EVIDENCE. **His:** the exclusion (say `exclude` on B33 and the estimator drops neighbours above the line), the line itself. **FOUR BANKS READ (S209 E9, 2026-09-20 20:5xZ): the failure is the TABLE RENDERER's, not the OCR's.** RBC (clean lane, re-OCR'd by Marker's choice, 25 of 30 table pages flagged), CIBC (clean, not re-OCR'd, 21/22), Scotia (its layer a GlyphLessFont OCR over images → the scan lane, where the dial shows Marker TOOK the layer and OCR'd 611 lines: 31/33 flagged, agreement 0.7298) — the same shape on either lane, with or without re-OCR; the inventions count on Scotia against its own layer reads 0.06 %. So the structural-table measure (header rows, spanning cells, row labels kept ÷ the layer's) is THE next build for this class; the dial and the inventions count have done their part. The estimator's promise on the scan lane: 8.588 s/page against 1.75 measured (the scan neighbours are true re-OCR'd books; a layer-taken 'scan' run is a third shape the dial now records — `ocr_lines` 611 on 92 pages). (This row's ID cell carried the dial's text from 19:1xZ to 21:0xZ — a blind `rsplit` in the lane's amendment script; correction 15.) | S209 E3 · `sourcing/results.jsonl`, `postmortem.jsonl` |
| B34 | **ANCHORS AT SHIP — BUILT S209 E6 (2026-09-20), behind a lever, OFF.** Rab signed the Desk proposal 7b1ef158 as (b) (58ba5591 "Signed."). `windows-converter/page_anchors.py` (ported from the tenant's `app/vaultlinks.py`, proved on 22 books: the nearest-hit rule against repeated running heads, the runaway-fence rule, no anchor on a table row or inside a fence, idempotent) — `anchor_markdown(md, blocks)`; the lever `anchors-at-ship.txt` under the pipeline root (`fp_paths.root("anchors_at_ship")`; reads `on`; absent = off; re-read per bundle); `anchors_at_ship()` and one `if` in `convert_and_ship.py`'s bundle assembly AFTER both audit gates score `body`, before the bundle's `.md` is written — so no survival number moves; `manifest.anchors = {lever, pages_anchored, pages_total}` (or a `note` when blocks.json is absent or the pass fails — the body ships unanchored, said); `page_anchors_selftest.py` 9/9 (hermetic; in CI). **His hand:** write `on` into `anchors-at-ship.txt` — from the next bundle on every shipped note carries ` ^p<N>` on each page's first paragraph and the exporter's re-ingest keeps them (the tenant's T-010 (8) closes then). The first measured run (pages_anchored ÷ pages_total on a real book) is what this row owes after his `on`. **S209 E5 (the zips read, 2026-09-20 17:0xZ): the rule's gaps closed behind the same lever, still OFF.** The five zips PORTAL shipped today re-anchored on the desk (`sittings/S209/anchor_measure.py`): the E6 rule left 34 of 309 pages unanchored — replayed page by page (`anchor_why.py`) the reasons were short keys 11 (the first six blocks all labels/footnote marks), a line taken by a contents list quoting the next pages' headings 9, a hit on a table row 5, text absent 3, table-only 4 — NOT "table-only pages" as the lane first told him (correction 8). Now: every text block of a page is a candidate; a hit whose line is a table row or already holds another page's id yields to the next-nearest; a page no text line can take gets the structured form Obsidian documents for tables (the id on its own line after the table, a blank line each side; found by a body ROW of the table's own html, since a header's cells render in another order); a key of 12–23 chars only when unique from the cursor on; Marker's `ListGroup` anchors on the list's last item; a table spanning pages carries one id (the first page's). `page_anchors_selftest.py` 17/17. After: RBC 76/76, Nilsson 73/73, CIBC 78/79 (p.15 = the spanning table's continuation), PBO 32/33 (the back cover), MPR 46/47 (the cover) — 3 of 309 left, each named. Owed: the same rule ported to PORTAL's `app/vaultlinks.py` (a tenant sitting), and his `on`. **RESTATED S209 E10 (2026-09-20, SYM-140):** the counts reported for the port (Scotia 92/92, TD 81/81) were counts of anchors PLACED, not of anchors on the right page — a truth measure (`anchor_truth.py`, private: right / near / wrong against the once-only keys whose positions increase with the pages) read NBC 11 wrong of 25 placed (59 pages unanchored), CIBC 2, TD 2, Scotia 1; the rule fixed (once-only keys lead from the top; the trusted run; the fence; 20/20; wrong 0 on all eight after, NBC 84/84); the tenant's copy ported (S14). **The lever stays OFF until his `on`.** The next reading of the measure is the batch's remaining documents as they land. | S209 E6, E5 · `page_anchors.py`, `page_anchors_selftest.py`, `convert_and_ship.py`, `roots.json` |
| B35 | **THE AUDIT'S BLIND SIDE (S209 E5, 2026-09-20; his Desk word 96db05cf "the structure and text of the converted material"): a re-OCR'd clean page INVENTS words its own layer never had, and survival cannot see them.** Measured page-exact against the source's text layer from the bundle's blocks.json (`file-portal-private/sittings/S209/layer_diff.py`, ten pages each): RBC (re-OCR'd by Marker's own decision, B33) invented 13 of 5,120 words (0.25 %) — `TLAC loverage satio` in the capital table, `last guarter`, a spanning header read as `for th ne three months s end … nir ne mo`, `#### 03 2026` for Q3 — and lost 123 of 5,390 (97 of them p.39's NSFR table); CIBC, not re-OCR'd, the control on the same instrument: 0 garble (its 13 are `&amp;`), 15 lost. The audit counts what is lost, never what is invented — a letter-perfect source shipped with reader-visible errors under 0.7857. **The measures:** (1) inventions per page against the layer, in the audit whenever the lane is clean (the layer is already the witness); a page over a threshold flagged like a survival miss; the number in the manifest beside survival. (2) The reader's file vs the engineer's: `<br>` inside cells (CIBC 4,885; RBC 1,962), `<span id="page-N-0">` inside 36 RBC headings, `<u>` ×89 on a typescript, `\$` before every dollar (RBC 1,753; CIBC 2,224), bold inside 87/113/21 headings — a ship-time reader pass or the analyst's markup program; the zip's README says what Obsidian renders. (3) Footnote digits glued to words (`earnings4`, `ratio1`, `chart8`) — a superscript that lost its baseline; the layer knows the font size. (4) The scan lane: the typescript's lines kept as paragraphs (Nilsson pp.16–17) and a paragraph's first line dropped under an underlined heading (p.22, the page rendered) — invisible to an agreement number. Clean on all five: duplicated lines 0, ragged rows 0 of 194 tables, ligatures/U+FFFD 0, hyphen breaks 0. **BUILT (S209 E8, 2026-09-20 18:0xZ), measure (1), report-only:** `fidelity_audit.audit_inventions(pages_raw, blocks, kind)` — per page from the bundle's `blocks.json` (attached before the audit runs), the words Marker's blocks carry that the witness page does not, with specimens and the losses beside them; `audit_convert(…, blocks=)` writes it as `fidelity.convert.inventions` (None = not measured, never 0; `meaning` says disagreement on the scan lane); `_audit_convert_safe` hands the blocks in; `inventions_selftest.py` (hermetic, in CI); `observability/dispositions.json` signs `converter:inventions` EVIDENCE (no renderer yet). The running watcher spawns the converter per job — the next conversion carries the number. **His:** the stages' order; the threshold for (1) — until then the number rides and no verdict reads it. | S209 E5, E8 · `fidelity_audit.py`, `convert_and_ship.py`, `inventions_selftest.py`, `zips_notes.md` (private), `layer_diff.py`, `md_text_probe.py` |
| B36 | **THE TABLES' SHAPE — the measure BUILT S209 E11 (2026-09-20), report-only.** The bank-tables class (17 of 35 NBC tables flagged; RBC 25/30, CIBC 21/22, Scotia 31/33, TD 11/29) had a survival number and no shape; `windows-converter/table_shape.py` reads the source's own table geometry (pymupdf `find_tables` inside each Marker table box, `blocks.json` bbox) against Marker's rows and width — the lines strategy a witness for rows and columns, the text strategy for a table's presence only, and three honesty rules (the cell floor, the row tolerance, the width) so a witness that disagrees is counted apart; `fidelity.convert.tables` beside survival (`columns_lost`, `rows_lost`, `tables_witnessed_lines` / `_text` / `tables_disagree` / `tables_unread`, the worst pages). Read on four banks whole: **NBC 77 columns lost over 19 pages** (p.26 `14×26 → 11×9`), RBC no witness (sparse rulings), CIBC/TD unruled (presence only). **The build that remains is his call:** Marker's table model on dense financial tables, or a second renderer for flagged table pages fed the layer's own cell grid (`extract()` gives the cells). `table_shape_selftest` 9/9 (the fixture drawn), on CI. **RESTATED S209 E13–E14 (2026-09-21) after the Codex lane's read-only counterexamples (MSG-CDX-0085):** the 77 was the instrument's — pymupdf's lines strategy splits a column at every ruling (NBC p.32 read 20 × 19 with nine columns empty in every row against a nine-column table whole on both sides); the witness now counts the columns and rows that carry text (SYM-145): **NBC 77 → 14 over 4 pages, CIBC AR 3 → 0, Desjardins 11 → 1**, and Codex's own replay calls the 14 a geometry diagnostic, not verified reader loss (p.26 `11x16->11x9` and p.74 unverified by eye). TableGroup blocks are read too (SYM-147: NBC p.57's moved row lived in one; NBC 15 over 5 pages, Desjardins 10 over 2, the captions lowering the cell agreement — disagree 123, honest). **The next cut, the lane's, report-only:** the per-page count of figures Marker carries MORE often than the layer (the duplicated-figure tell: NBC p.57 `1,040` × 3, `13,010`, `1,540`) beside `columns_lost` — a row-level loss no measure names yet. `table_shape_selftest` 10/10.** **RESTATED S211 E7-E8 (2026-09-22): the witness needed a THIRD rule and it is Rab's, and the measure now shows its work.** SYM-181 filed OPEN — the lines witness counts a DOUBLED HAIRLINE GUTTER as a column when a two-line stacked header drops text into it, which clears BOTH of the rules already there (SYM-145's carries-text and SYM-172's unit-gutter). Measured at the page from the witness's own cell boxes: the Spring Economic Update p.137 has a raw grid of 17 by 23 whose column widths are thirteen at exactly 5.40 pt, then 7.56, 11.52, 24.72, six at about 33 and one at 178.2 — thirteen gutters and eight real columns, which is what the page prints and what Marker rendered; the witness lands on 14 and 6 columns are booked lost on a complete rendering, 6 of that document's 7. THE CONTROL IS IN THE SAME DOCUMENT: p.118, identical doubled geometry, SINGLE-line header, reads 1x8 to 2x8 and nothing lost — so slivers alone do not cause it, slivers plus a stacked header do. Population over the shelf: 9 documents carry a non-zero columns_lost summing to 70, and 48 of 401 re-witnessed tables carry three or more columns under a third of their own median width — but NBC's annual report has 40 such tables and loses 2 columns, so the 48 is where to LOOK and never what is wrong. NOT FIXED, deliberately: every candidate rule needs a NUMBER (merge boundaries within N points, or a minimum cell width, or a fraction of the median) and columns_lost is one of the six fields in the registry's error sum, so that number ranks his whitelist. BUILT instead, report-only and landed: each worst page carries `column_geometry`, one line per shape, saying how many columns the witness found and WHAT WIDTHS they are — p.137 reads `23 columns, widths 5.4x13, 33.0x4, 33.1x2, 178.2, 7.6, 11.5, 24.7 pt` — so the sliver grid is readable without opening the PDF. columns_lost is IDENTICAL with and without it, asserted on SYM-145's and SYM-172's own fixtures; table_shape_selftest 10 to 16/16. What this row still owes is unchanged and is his: the number, or Marker's table model on dense financial tables. | S209 E10–E11 (`sittings/S209/e10_notes.md` "The bank tables have a shape", `e11_wiring.md`; the probes `table_shape.py`, `table_clip_probe.py`, `columns_lost_proto.py`, private); E13–E14 (`nbc_p32_probe.py`, `nbc_p57_probe.py`, `ts_whole` scratch; Codex MSG-CDX-0085/0087) — RESTATED 2026-09-21 S209 |
| B37 | **THE READER'S MARKUP — a stage, not built.** The reader's `.md` carries the engineer's markup: Scotia's Q3 report 6,422 inline HTML tags (`<sup>(1)</sup>`, `<br>` inside cells) and 3,525 backslash escapes (`\$`), TD's 1,094 and 1,816, RBC's 2,232, NBC's 509 — a phone reader shows the tags. The stage: `<sup>n</sup>` → a footnote reference, `<span id=page-N-0>` dropped from headings (RBC 36 inside headings; an outline pane shows them), `<u>` → plain, bold stripped inside headings, `\$` kept for Obsidian — a ship-time pass or the analyst's program; measure: the counts above per bundle (`md_text_probe.py`'s `html` / `escape` / `bold-heading` lines) before and after. His call which side does it. | S209 E5–E11 (`sittings/S209/zips_notes.md` ## The stages and ## 6–8; `md_text_probe.py`, private) — ADDED 2026-09-20 S209 |
| B38 | **FOOTNOTE DIGITS GLUED TO WORDS (converter) — a stage, not built.** A superscript that lost its baseline: RBC l.36 `earnings4`, `ratio1` ×3, `income2` ×2; MPR `chart8`; **TD's Q3 report 228** (`loans1`, `income2` — the report superscripts nearly every metric). The layer knows the font size and the baseline; the stage: a superscript run in the layer's spans → `<sup>` (or a footnote marker) at convert time, never a digit glued to the word; measure: `md_text_probe.py`'s `glued-footnote` count per bundle, TD the fixture (228 → 0). | S209 E5–E11 (`sittings/S209/zips_notes.md` ## The stages and ## 6–8; `md_text_probe.py`, private) — ADDED 2026-09-20 S209 |
| B39 | **THE OUTLINE AGAINST THE DOCUMENT'S OWN TOC — a stage, not built.** PBO's appendices at two levels; CIBC's 308 headings against a Contents of about 40; the SEU's outline 308 lines — Marker promotes bold run-ins and table captions to headings, and the reader's outline pane is the casualty. Measure: headings in the `.md` vs the entries of the source's TOC (when the PDF carries one: the outline or a Contents page) — a ratio per bundle, the excess named by level; the stage: a heading whose text is not in the TOC and whose block is a table caption or a run-in demoted at ship. Report-only first (docs/15 §12). | S209 E5–E11 (`sittings/S209/zips_notes.md` ## The stages and ## 6–8; `md_text_probe.py`, private) — ADDED 2026-09-20 S209 |
| B40 | **JOINED WORDS AND FRAGMENTS (converter; the inventions measure's next cut) — a stage, not built.** On the SEU's judged pages the `inventions` split three ways: true OCR garble (few), JOINED WORDS where Marker dropped a space (`electronicsand`), and FRAGMENTS where it broke one (NBC p.72 `cred wances aired paired uly`). The stage: `audit_inventions` classifies each invented word — joined (two layer words concatenated), fragment (a substring of a layer word), garble (neither) — and the manifest carries the three counts beside `invented_total`; then the converter's fix per class. Measure: the three counts per bundle; the SEU and NBC the fixtures. **S209 E13 (2026-09-21, Stanford CIFE, 113 pp): the fragments class read at its source — two mechanisms, both Marker's own and neither the extractor's (pdftext reads both pages whole): ROTATED TABLE PAGES (SYM-142; eight of sixteen Rotate-90 pages shipped as two-letter shards, p.80 1,213 tokens; pp. 96–100 rotated and whole) and A SYMBOL CELL RE-OCR'D INTO ONE WORD REPEATED (SYM-143; `second` × 291 for 48 check marks on p.32). The stage gains two predictors for the manifest, report-only and the lane's to build — a page's rotation and its non-letter glyph count from the layer — and one class more for `audit_inventions`: a repeated invented word counted as one (`repeated: word, count, page`). A pre-rotation pass and a symbol-cell rule at the converter are his lever.** **BUILT S209 E14 (2026-09-21 04:0xZ):** `audit_inventions` puts every invented word in one of four classes, counted per copy with specimens (page, word, count) — JOINED (two neighbouring layer words run together), FRAGMENT (a piece of a lost word), DROPPED LETTER (one letter off a lost word on the same page — SHEG's SYM-148) and GARBLE (none of these) — `fidelity.convert.inventions.classes` / `class_specimens`, report-only, six EVIDENCE dispositions; `inventions_selftest` 15/15 (the four on one page; `_one_edit` on five shapes). Live: SHEG 0 / 8 / 60 / 186 (the cover's OCR is the garble), CIFE 0 / 549 / 0 / 325 (the shards read as fragments), NBC 1 (`creditlosses`) / 7 / 0 / 16, MIT post-fold 0 / 2 / 1 / 7. **What remains is his lever:** the converter's fix per class — a space rule for joined, the layer's word for a dropped letter, the rotated-page pre-pass for CIFE's fragments.** | S209 E5–E11 (`sittings/S209/zips_notes.md` ## The stages and ## 6–8; `md_text_probe.py`, private); E13 (`sittings/S209/e13_notes.md` ## 3); E14 (`e14_notes.md` ## 7, `inv_classes_live.py`) — ADDED 2026-09-20 S209; RESTATED 2026-09-21 S209 E13; BUILT 2026-09-21 S209 E14 |
| B41 | **CHART TEXT AS TEXT — a stage, not built (the frontier's item 9; the AI Index its specimen).** The layer carries a chart's legend and axis labels as text; Marker's blocks drop them (the AI Index: 8,520 of 90,819 layer words lost; 316 of 424 pages flagged, figures 91 of 99; the MPR's 'dropped words' were chart legends). The stage: for a Figure block, the layer's words inside its box shipped as the figure's caption/alt text (the analyst could write the alt text from them); measure: the E3 P7 measure — layer words inside figure boxes vs words shipped for the figure, per page; and the stretched label (p.60 `ccccchhhhhiiiiinnnnnaaaaa`: five copies of each glyph the layer carries) named as its own invention class, folded to one. **THE MEASURE BUILT S209 E13 (2026-09-20), report-only:** `windows-converter/figure_text.py` — for every Figure or Picture block, the layer's words inside its box (pymupdf `get_text("words", clip=…)`) against the words Marker shipped for it; `fidelity.convert.figures` beside survival (`layer_words_in_figures`, `marker_words_for_figures`, `figures_silent`, the worst pages with a sample of the words the reader never gets); the scan lane unread. Read on three documents whole: the AI Index 10,551 layer words inside 479 figures, 680 shipped, 67 figures silent; the MPR 409 vs 8, 22 silent; NBC 8 vs 0. `figure_text_selftest` 6/6 (the fixture drawn), on CI. **The build that remains is his call:** the layer's words inside a figure shipped as the figure's caption or alt text (the analyst could write the sentence from them), or the words kept as a data block beside the image. | S209 E5–E11 (`sittings/S209/zips_notes.md` ## The stages and ## 6–8; `md_text_probe.py`, private) — ADDED 2026-09-20 S209 |
| B42 | **THE SCAN LANE'S PARAGRAPHS (converter, scan lane) — a stage, not built.** Nilsson (SRI 1969, the typescript): pp.16–17 the typescript's LINES kept as paragraphs (l.334–347), p.22 a paragraph's FIRST LINE dropped under the underlined heading — neither visible to an agreement number. The stage: lines joined into paragraphs when the OCR's line pitch says so; a paragraph's first line under an underlined heading kept; measure: `md_text_probe.py`'s `split-paragraph` and a line-pitch count per scan-lane bundle, Nilsson the fixture. | S209 E5–E11 (`sittings/S209/zips_notes.md` ## The stages and ## 6–8; `md_text_probe.py`, private) — ADDED 2026-09-20 S209 |
| B43 | **THE EQUATIONS — a class, named S209 E12 (2026-09-20) on Waterloo's PhD thesis (147 pp).** Marker writes every equation as LaTeX inside a math element; the source's layer has the glyphs. Two readings followed: (1) the inventions measure counted the command names as invented words — 1,810 of 32,843 (5.5 %), every worst page an equation page — FIXED at the measure (`1e60d0d`: the math elements taken out before the words are counted, the commands counted apart as `latex_commands`; 11 real readings remain, 0.036 %); (2) survival cannot judge an equation page either — 57 of 111 prose pages flagged on the thesis, the equations breaking the witness's twelve-word windows — and no measure of ours can yet. The stage: an equation-aware witness — the layer's glyph run inside an Equation block's box against the rendered LaTeX's symbols (a normalisation of both to a symbol sequence; pymupdf gives the glyphs, the block gives the LaTeX), report-only beside survival, with the Equation blocks' count per page so a flagged equation page reads as such; the build his call. Specimens: Waterloo p.53 (164 command tokens), p.141, p.136. **AND NOW ITS SHARE, on a SECOND document — `Observed 2026-09-24 (S212 E5)`, `sittings/S212/loss_anatomy.py`.** An Introduction to Statistical Learning (Python, 612 pp, survival `0.6729`) fails 7,275 of 22,239 windows (32.7%), and the failures read: **MATHEMATICS 4,146 (57.0%)**, prose 1,718 (23.6%), dot leaders 733 (10.1%), figure/table numerals 439 (6.0%), code listings 239 (3.3%). The windows are read AS THE SCORER SAW THEM — re-run through a harness whose baseline reproduces the shipped 0.6729 exactly — not sampled from the manifest's ten-token excerpts. THREE LIMITS STATED WITH IT: the residual 'code' is still equations by its own samples, so math is nearer 60% and is UNDERSTATED; the prose figure is an UPPER BOUND because contents-page lines leak into it; and the first classifier called equations 'code' and needed three dots for a leader, so math moved 49.8% -> 57.0% when those demonstrated errors were fixed — the movement is recorded rather than only the second number, because a classifier tuned until it agrees with its author is family S15 THE-INSTRUMENT-MANUFACTURES-THE-FINDING. THE CONSEQUENCE FOR PRIORITY: SYM-168's leader predicate, measured the same hour, touches 10.1% of these failures and yields +0.0138 of survival. B43 is where the survival on a mathematics textbook actually is, and it is a BUILD rather than a threshold — the fix a session's, the priority his | S209 E12 (`sittings/S209/e12_notes.md` THE EQUATIONS CLASS; `inv_rerun.py`, private) — ADDED 2026-09-20 S209 |
| B44 | **THE REAUDIT'S SLICE-CACHE FALLBACK IS UNCHECKED — named S209 E14 (2026-09-21) from the Codex lane's 102-question audit (MSG-CDX-0088).** `reaudit()` takes its Marker-body reference from the sidecar when the sidecar's sha256 and byte count match `manifest.marker_body` (R2, 2026-09-05), and otherwise from the slice cache under `CHUNK_WORK/<source_sha[:16]>/slice-*/slice.md` — bound to the source by PATH only, its bytes compared to nothing. Since SYM-150 the source PDF is hash-checked too; the slice cache is the one reference in the verb that is not. The stage: the manifest carries the slice cache's sha256 at write time (as `marker_body` does for the sidecar) and the fallback refuses a mismatch with its reason on the bus, exactly as the sidecar and the source do; `convert_and_ship_selftest`'s slice-cache cases carry real shas. Report first (how many held bundles would fall back to the cache today), then the build — the lane's, a refusal added to a verb. **REPORTED S210 E1 (2026-09-21 06:5xZ, `sittings/S210/b44_report.py`):** of the 21 held bundles, 15 carry a Marker sidecar matching `manifest.marker_body` (a reaudit takes it, hash-checked), 6 carry none — 4 of those with an analyst block, for which a reaudit REFUSES (`analyst reference unavailable`: the chunk-work dir `.chunk-work/` holds only the current run's slices; the cache is per run, not kept) and 2 without one (the convert audit alone); **0 would fall back to the slice cache today**; 21 of 21 sources under drop/done match their `source_sha256` (SYM-150's check passes on every held bundle). The build stays cheap and stays open: the cache's sha in the manifest at write time and a refusal on mismatch — no live case today, so it waits behind work that has one. | S209 E14 (Codex MSG-CDX-0088; `windows-converter/convert_and_ship.py` `reaudit` 2540–2560); S210 E1 (`b44_report.py`, private) — ADDED 2026-09-21 S209; REPORTED 2026-09-21 S210 |
| B45 | **THE TERRAIN'S DECISIONS — named S210 E1/E2 (2026-09-21) under Rab's commission on the line's turnout (Desk 3219a1ed): four mechanisms on the register (SYM-155–158), each with a fix proved or measured, each waiting on a signature or a re-run word.** (1) SYM-155, the annual report's half: Marker's `check_line_overlaps` compares an absolute area in pt² to a 0.1 "pct"; a LineBuilder whose test is the fraction its name says (0.5), injected through `marker.converters.pdf.LineBuilder` by `marker_blocks.py`, keeps RBC AR p.100 on the text layer with every table column right (PROVED on a copy, `sittings/S210/overlap_fix_proof.py`) and frees 121 of its 246 pages (every other bank PDF unchanged: 0 pages by fraction) — the engine policy, HIS signature. (2) SYM-155, the Q3 half: pdfium reads RBCTextTabular-Bold's ascent as zero (843 spans on 34 pages sit 6.8 pt low) and Marker's table pass re-reads the cells with pdftext's `table_output` on those raw coordinates — PROVED on p.20 (09:14Z): a `pdftext.pdf.pages.get_chars` wrapper that lifts a loose char box whose top sits below its ink (no font name; 0 chars touched on the AR and TD control pages) beside the fraction gate keeps the layer with every row right in 83 s against the OCR path's 740 s — the engine policy, HIS signature, with (1). (3) SYM-157: one clause in `_degenerate_blocks` — a space-free block with three or fewer distinct non-space glyphs is a rule, not a loop (Desjardins' 172 escaped underscores) — docs/15 §9, HIS signature, warn-first. (4) SYM-156: the lane's OCR-font trigger scoped to the share of spans or to the page (Scotia Q3: 160 of 25,152 spans sent 92 pages through the scan lane) — docs/18, HIS signature. (5) SYM-158: the tenant's `_station()` save deferring on OSError like `on_progress` — DONE IN CODE in the tenant's own S16 (2026-09-21 09:52Z–10:04Z: c343ddb, `tools/station_guard_selftest.py` 5/5 with its negative control, its SYM-016 FIXED, closing 9ab0259 in lockstep); ON THE WIRE OWED — the running server loaded the code before it, and the restart between jobs is the tenant's T-024 (a quiet window the line has not had, or his hand) — mine. (6) The re-run word: the held six (TD Q3, the AI Index, CIBC AR, Desjardins, CIFE, BMO Q3 — 144/152 fixed at the geometry pass) and, after their fixes, both RBC documents, C-31 and Scotia Q3 — HIS. (7) SYM-148 at scale (S210 E4, McGill's photonic thesis: 660 letters dropped, 93 of 192 pages flagged, survival 0.8169 — pdfium returns a font's fi / fl / ff ligature glyph as its first letter alone; MuPDF reads the letters; the loss on layer pages, not the gate's): SYM-148's converter rule — take the layer's word where Marker's is the layer's word minus a letter — or a provider that reads the clean lane's layer as MuPDF does, which would also carry decision (2)'s fix; the specimens McGill (660), MIT OCW (S209), SHEG (S209) — HIS signature. (8) SYM-160 (S210 E5, the bills: Antenna House pages carry a hidden off-page duplicate of both columns at x 1038–1284 on a 612-pt page; pdftext's `deduplicate_chars` drops 740 of the duplicate's newlines on C-30, `get_spans` glues 249 spans on 36 of 42 pages, and Marker's overlap gate — its off-page margin `expand(5, 5)` five page-widths a side — throws 39 of 42 pages to OCR, C-288 4 of 5; the visible layer whole): the provider drops chars outside the page box before dedup, as MuPDF's TEXT_MEDIABOX_CLIP does — a proof on a copy before it is proposed — HIS signature; the specimens C-30 and C-288, and every bill from this producer. Order by documents freed: (1) with (2) (both RBC documents and every future RBC), (6), (7) (every LaTeX-typeset thesis on the line), (8) (every bill on the line), (3), (4), (5). The object: `sittings/S210/TERRAIN.md` (private). | S210 E1/E2 (`sittings/S210/TERRAIN.md`, `terrain.py`, `marker_page_gate.py`, `overlap_census.py`, `font_metric_census.py`, `overlap_fix_proof.py`, private; SYM-155–158 on the register, `sessions/S210` §E1) — ADDED 2026-09-21 S210 |
| ~~thinkpad/held-fail-park-unread~~ **STRUCK 2026-09-17 (S174 E1) — READ, BOTH SIDES AGREE: the park is Zero-to-One's staged copy from the 2026-09-12 20:50Z reship (`manifest.json`: `source_sha256 cb7e026f…`, `supersede {reason: analyst-rerun, from_verdict: fail}`, convert `doc_survival 0.9955`, analyst `0.9997`; the `.md` 297,756 B, `blocks.json`, assets — 1.3 MB), moved aside BY HAND at S144 E4 (the park's mtime 2026-09-13 09:08:30Z = the receipts' `supersede-noop` second) so the S144 reaudit's reship would not nest into it (SYM-128; S146 E4 made the move-aside mechanical in `ship()` as `.held-<sha16>-<stamp>`); the desktop's `held/cb7e026f3fc02da8--reshipped-20260912-074321--reshipped-20260913-090357` is the SAME book's later copy (`supersede {reason: reaudit, from_verdict: pass}`, analyst `0.9947`); the vault has held the pass copy since `8d7cc85` (S144 §8). One book, two kept copies, nothing pending — the park is 1.3 MB of kept history the exporter ignores by its dot; its removal is the ThinkPad's housekeeping, a named act for a later sitting or his word, not this read's. Nothing moved. Was:** | S173 E2 (2026-09-17; the vault lane read on the ThinkPad after his "Thinkpad is on."): `~/file-portal/library/staging/` holds ONE entry, the dot-prefixed `.held-fail-cb7e026f3fc02da8-20260912-205007` (parked 2026-09-12 20:50Z; the exporter ignores dot entries by design — no pending export) — `cb7e026f3fc02da8` is Zero-to-One's sha16 (the desktop's `held/cb7e026f3fc02da8--reshipped-20260912-074321--reshipped-20260913-090357`; the receipts' 09-13 09:08Z `supersede-noop` names it). Unread: what the park holds (a body? a manifest? its size), which exporter rule parked it (a `held-fail` on a verdict-fail supersede?), and whether the desktop's `held/` row and the ThinkPad's park describe the same event or two. Measure: the park listed and its manifest's verdict read; the exporter's `held-fail` branch found in `linux-converter/converter/exporter.py` and quoted; one line in this cell saying the two sides agree or do not; then struck or turned into the row the disagreement needs | open — a read, the lane's (no hand of his; nothing moved) | sessions/S173-desktop-2026-09-16.md E2 · private `sittings/S173/e2b_staging_read.txt` |
| ~~observability/glass-subscript-blind~~ **STRUCK 2026-09-17 (S177 E4, his word cdaf6619 "fix it") — MEASURED S176 E3 (40 subscript writes, 158 census keys, 30 blind) and the MECHANISM landed WARN-ONLY: `glass_detector.branch_keys_from_python` (Assign/AugAssign on a string Subscript, `setdefault`) lists every subscript-hung key no renderer names and no disposition signs under its own heading and in `--json` as `subscript_warn` — never a glitch, `--enforce` unchanged (the full census names 10 after the renderer and disposition filters: `bench:unsplit`, `converter:blocks_path`, `ceiling_mib`, `ceiling_top`, `gpu_top`, `inflation`, `max_spec_cols`, `num_predict`, `table-geometry`, …); `acceptance.py`'s case plants a branch key and asserts it LISTED and NOT fatal (192/192). The gate — making them glitches once each carries a disposition — is his. Was:** **Glass's census cannot see a manifest key written by SUBSCRIPT.** `glass_detector.py`'s `_PyProducer` harvests dict LITERALS that leave a function (returned, assigned into a returned name, appended to one, or passed to `json.dump`); `added_keys_since` reads `"key":` in a diff's added lines. `manifest["x"] = value` matches neither — the key is computed, stored, and never asked about. Found S175 E4 wiring SYM-056's validator: `manifest["latex_structure"] = rec` passed `--enforce` clean, and a disposition entered BY HAND then read as STALE in `acceptance.py` (190/191) because the census had no such key; the wiring was rewritten to return the record as a literal (`out = {"latex_structure": rec}; manifest.update(out); return out`) and the check went 191/191. **Measure:** a census of the converter's producers for `manifest["…"] =` / `fid["…"] =` / `rec["…"] =` subscript writes (`grep -c` over `convert_and_ship.py`, `fidelity_audit.py`, `analyst.py`) against the keys the census knows — the difference is the blind set; then either the census learns `Assign(Subscript)` targets whose base is a returned or persisted name (with its negative control in `acceptance.py`'s planted-glitch case), or the limit is written into `glass_detector.py`'s docstring and docs/29 §5.3. **MEASURED S176 E3 (2026-09-17; `sittings/S176/e3_subscript_census.py`, read by AST):** the three producers write **40** string-keyed subscript assignments (`X["k"] = …`, `+=`, `setdefault`) — `convert_and_ship.py` 22, `fidelity_audit.py` 2, `analyst.py` 16 — against **158** keys the census harvests from their dict literals; **30 keys are written by subscript somewhere and known to the census nowhere** — among them the manifest's own BRANCHES: `fidelity` (`manifest["fidelity"] = fa.build_fidelity_block(…)`, :53), `analyst` (:2058; fidelity_audit :648), `final` (:2439), `supersede` (:738), `reaudit` (:2462), `marker_body` (:867), `chunking` (:1838), `blocks_path` (:1682), `gpu_top` (:532), `ceiling_mib`/`ceiling_top` (:1233), `state` (:2115), `verdict_phase` (:2441 — its dict-literal twin in fidelity_audit is what glass saw at S175), the flag's `max_spec_cols`/`mode` (:816); the rest are local records (`analyst.py`'s rejection fields `e`/`r`/`s`/`ratio`/`reason`/`survival`/`fence`/`inflation`/`think_leak`/`truncated`, the ollama option `num_predict`). The census sees the LEAVES (a block's literal keys) and never the BRANCH a subscript hangs them on. The measure is done; the mechanism half stays open: `_PyProducer` learning `Assign(Subscript)` whose base is a returned or persisted name (with its negative control in `acceptance.py`), or the limit written into `glass_detector.py`'s docstring and docs/29 §5.3. | S175 E4 (2026-09-17; `sittings/S175/e4_wire_visible.py`; the S161 rule "a build's new keys need same-commit dispositions" cannot be met for a key the census cannot see) |
| ~~audit/degeneration-calibration-on-this-corpus~~ **STRUCK 2026-09-17 (S179 E1) — MEASURED: every conversion on the desktop (33) re-run with the current rule (`e1_degeneration_read.py`; CPU only) and every flagged block READ (`e1_block_reader.py`): re-run vs manifest agree 24 · disagree 9 (six historical TRUEs were table rows, J29's class, FALSE now; Brain of the Firm's two unaudited manifests TRUE on re-run — the labeled specimen confirmed); the fourteen that flag today: **LOOP 11** (Diagnosing ×2 `the state of the state…` ×155; Zero-to-One pre-repair ×2 `AND ROUTE AND ROUTE…` ×199; Investment Valuation University ×3 — a 47 KB HTML document inside the markdown and a 21 KB equation block; Equity Research ×4 `of the purpose of the purpose…` ×112 in a list item's tail) · **STRUCTURE 3** (Ashby ×3 — the book's own Markov-chain protocols `A B A B B B…`, plus the S109 LaTeX runaway, SYM-056's). **The false-alarm rate 3 of 14 conversions (21 %), 1 of 6 books (17 %)** — one shape: blocks of one-letter tokens. docs/15 §9.5 appended with the table and the numbers. The exemption proposed (≥ 80 % of a block's words ≤ 2 chars → STRUCTURE, applied before the rule like §9.3's table blanking) MEASURED on the corpus (`e2_exemption_probe.py`): clears Ashby's 9 block-copies and none of the 33 loop blocks (the nearest, the equation loop, at 0.721) — posted on the Desk in his shape (S179 E2); a gate change, his. Was:** **The degeneration tripwire is TRUE on 20 of 29 conversions that carry it, and nobody has read whether they are loops.** docs/64 §5 (S178, 2026-09-17): by distinct book the best verdict is `pass` for 1 of 11; the tripwire (`DEGEN_ZLIB_MAX 0.20 · DEGEN_TRIGRAM_MAX 40 · DEGEN_BLOCK_MIN_CHARS 200 · DEGEN_LINE_REPEAT 20`; table rows blanked first, SYM-067) is TRUE on every conversion of both Damodaran editions (1,377 / 1,356 pp), Equity Research (465), Diagnosing (184) and Zero-to-One's three before its reaudit — the code's claim "calibrated zero-FP on the vaulted corpus" is S45's (Historical) and has not been re-measured on this corpus; the Brain-of-the-Firm true positive is the one labeled specimen. **Measure:** for each of the 20, `fidelity_audit.degeneration(md)` re-run on the bundle's markdown with its `worst` blocks READ (the excerpt, zlib, max_trigram per block): a loop (the decoder stuck) or legitimate structure (an index, a reference list, a table the blanking missed, a repeated running head)? — a per-conversion table (book · pages · blocks_total · the worst three read · verdict: LOOP / STRUCTURE / UNREAD); then the false-alarm rate with its denominator, docs/15 §9 amended with it, and — if the rate is not zero — the thresholds' calibration proposed to Rab in his shape (a gate change, his). A Sonnet reader fleet fits the orchestration law if the lane names the blocks; the lane alone otherwise. **RE-OPENED S211 E9 (2026-09-22) ON EVIDENCE, and the proposal it supports must not be signed until this is settled.** S179 classified Ashby's THREE flagged blocks as STRUCTURE — the book's own Markov-chain protocols — counted them among the false alarms behind the 3-of-14 (21 %) rate, and S179 E2's proposed exemption (a block whose words are 80 % or more at two characters or fewer reads STRUCTURE, applied before the rule) rests on that. I READ TWO OF THE THREE AT THEIR OWN PAGES and they are LOOPS. p.180: the source layer carries the protocol ONCE, 32 letters, then the sentence The transitions, on a direct count, are; Marker's block is 2,045 characters of A B repeated AND that following sentence is absent from every block on the page — the loop also swallowed. p.195: the layer prints the previous message would now start ... b b b c X b c X b b c X X X c ...., about twelve tokens ONCE; Marker's block is 941 characters of b b c x repeated roughly 230 times. Both are SYM-179's mechanism, filed the same night from the same book. THE THIRD IS UNREAD and is not guessed: p.294 is the ANSWERS TO THE EXERCISES section, 457 layer words against a 6,106-character block whose tail repeats LaTeX — neither STRUCTURE nor LOOP established. CONSEQUENCE: the exemption keys on blocks of one- and two-character words, which is exactly what b b c x and A B A B are, so as proposed it would EXEMPT these two real loops and Ashby would read pass with a printed line written two hundred times and a sentence missing — and degeneration is the convert phase's ONLY fail signal. So the 21 % rate is too high by at least two, and the rule needs those two blocks as NAMED FIXTURES IT MUST NOT CLEAR before it is worth signing. What this row now owes: the third Ashby block read at its page; the same re-read over the other conversions S179 counted as false alarms, since the same misreading may sit in them; and the negative control with its specimens. Posted to the Desk 23:33Z so it reaches him BEFORE the signature, not after. **S211 E10 (2026-09-22 23:5xZ) — THE THIRD BLOCK READ, AND IT IS A LOOP TOO, so ALL THREE are and Ashby leaves the false-alarm evidence entirely.** p.294, the ANSWERS TO THE EXERCISES section: the layer holds 2,512 characters and 457 words; Marker's ListGroup /page/293/ListGroup/484 holds 6,106 characters and 581 words, and its most repeated three-word sequence is a LaTeX downarrow-frac-five-over-alpha written 162 TIMES on a page whose layer contains ZERO alpha glyphs. The surrounding prose is faithful — illumination of the appears three times in both, the intensity of twice in both — so the block is honest text with an invented repetition inside it, which is a THIRD SHAPE of SYM-179's mechanism (letters on p.180, tokens on p.195, LaTeX here). PRECISELY WHAT THIS DOES AND DOES NOT DO to the pending exemption: the exemption keys on one- and two-character words, so it would clear p.180 and p.195 but NOT this LaTeX block. What all three together do is remove Ashby from the false-alarm evidence, so the 3-of-14 (21 percent) rate is too high by THREE on this book alone — and the rate is therefore not measured at all until the other conversions S179 counted as false alarms are re-read the same way, at their pages, against their layers. THE FIRST OWED ITEM OF THIS ROW IS NOW DISCHARGED; the second is the one that matters. **S211 E10 COMPLETED (2026-09-22 23:5xZ) — ALL FOUR DISTINCT PASSAGES READ, ALL FOUR LOOPS, AND THE RATE IS 0 OF 14.** The two Ashby conversions flag the same passages between them: the 156pp flags p.90, p.92 and p.99; the 306pp flags p.180 (= 156pp p.92), p.195 (= 156pp p.99) and p.294. Four distinct passages, each read at its page against its layer. THE FOURTH SETTLES IT IN THE BOOK'S OWN WORDS: p.90 prints Ex. 1: A system of two states gave the protocol (OF 50 TRANSITIONS): followed by fifty letters, ONCE. Marker's block is 2,044 characters, max trigram 654 — roughly a thousand letters, twenty times what the book printed. The other three: 32 letters once against a 2,045-character block with the following sentence GONE; about twelve tokens once against 941 characters of roughly 230 repeats; and a page with ZERO alpha glyphs against a LaTeX fraction written 162 times. NOT ONE is the book's own structure. AND THAT FINISHES THE RATE, because S179's own Desk entry says it in its words — of the 14 conversions it flags today, 11 are real loops and 3 are false alarms, ALL ONE BOOK: Ashby's own Markov-chain exercise protocols. Those three false alarms are Ashby, and Ashby's own protocol is fifty transitions printed once. So the measured false-alarm rate on that corpus is 0 OF 14 CONVERSIONS, 0 OF 6 BOOKS, the tripwire did not fire falsely once, and the exemption would clear the very blocks that prove it working. The code's old claim, calibrated zero-FP on the vaulted corpus, is the one that held. WHAT REMAINS AND IS NOT MINE: whether S179's record is formally corrected rather than annotated here — that is a record change on a CLOSED sitting and needs his word; offered to him on the Desk 23:56Z. **S211 E11 — THE SECOND OWED ITEM DISCHARGED: the LOOP side checked, 4 of 4 HOLD, and the error's shape is one-directional.** The eleven LOOP calls came from the same reading method as the three wrong STRUCTURE calls, so they were checked at their pages too — the side where an error would have cut AGAINST this row's finding. Zero-to-One p.2: layer ZERO characters, 2,046 characters of AND ROUTE, while the page's other blocks are correct (Notes On Startups, Peter Thiel, Founder of PayPal). Diagnosing p.118: layer ZERO characters, 2,051 characters of the state of the state of. Diagnosing p.2, the clearest specimen of the mechanism on the shelf: the layer prints it is about the organization of enterprises ... but in terms of UNDERLYING STRUCTURE, and Marker's block is exactly right as far as but in terms of, then DERAILS into an administrative to the control of the control of 124 times and never finishes the sentence in that block. Equity Research p.143: layer ZERO characters, the control of x115. SO: every LOOP call is right and every STRUCTURE call is wrong — a repeated PHRASE in prose reads obviously as a loop, a repeated LETTER looked like the book's own structure and was not. EIGHT BLOCKS READ AT THEIR PAGES, EIGHT LOOPS, no false alarm in either direction. STATED LIMIT: Investment Valuation cannot be checked at all — no conversion of either edition on the shelf carries both a blocks record and a marker body — so its 3 LOOP calls stand unverified and are named rather than counted. This row's two owed readings are now both done; what remains is his: the record correction, and the exemption's disposition. | S178 E1 (2026-09-17; `sittings/S178/e1_measures2.txt`; docs/64 §5) |
| ~~B33~~ **STRUCK 2026-09-15 (S157 E42) — BUILT with both negative controls** | **Repair the two S111 close-gate evidence boundaries without changing their verdict semantics:** (1) bound `git credential fill` itself or force noninteractive credential lookup, because the later `curl --max-time 25` cannot stop an earlier `git-askpass` hang; (2) make the memory durability probe distinguish “not a Git repository” from Git refusing the repository because ownership/safe-directory identity is unresolved. Add negative controls for an interactive helper and a dubious-ownership repo. | `Observed 2026-08-28` during S111 close; `SYM-063`, `SYM-064`; `.claude/skills/muster/close.sh` CI and MEMORY sections · `Observed 2026-09-15 (S157 E42)` — (1) the credential lookup bounded (`timeout ${FP_CRED_TIMEOUT_S:-20}`) and forced noninteractive; a hang reads UNREAD and names itself; (2) the MEMORY probe names an ownership/safe.directory refusal as such, a non-repo as such; muster selftest CASES 53–54, 110/110; the live close reads CI inside the bound (public `f3b4583`). SYM-063 and SYM-064 FIXED |

| B46 | **TWENTY-TWO THRESHOLD-SHAPED CONSTANTS ADDED ACROSS S211 CARRY NO LEVER AND NO WAIVER** (docs/18 §2, signed: a number that decides something is a lever). The close's census names them; `observability/lever_census.py --since <pin>` names 6 more as bare literals in the regex gate's blind classes. The batch, by where they live: **`table_witness.py`** — BAND_Y_FRACTION 0.6, ECHO_SIMILARITY 0.85, ECHO_MIN_FIGURES 3, ECHO_FIGURE_*_SHARE 0.5 ×3, COLLAPSE_MIN_BANDS 3, BOX_PAD_PT 6.0, BOX_SHORT_MIN_FIGURES 2, ROTATED_LINE_SHARE 0.5, CROSS_PAGE_MARGIN_PT 36.0, HEADER_CUT_MIN_LETTERS 3, HEADER_CUT_MIN_CELL_LETTERS 2, LABEL_MAX_WORDS 6; **`fixes.py`** — FRACTION 0.5, SHARE 0.05, _LOW_FREE_FRACTION 0.15, _DEFAULT_TOTAL_MIB 10240; **`ligature_repair.py`** — _MIN_REPAIR_LEN 4; **`table_shape.py`** — GEOMETRY_WIDTHS 7; **`fidelity_audit.py`** — LOST_JOINED_SPECIMENS 6. | **AND AN HONEST RE-BILLING, because the first one was wrong.** At S211's close I told Rab this was "mechanical, needs no signature, the cheapest real work the next session can take". Only the WAIVERS are mechanical. Most of the batch lives in `table_witness`, whose block rides BESIDE survival and is unseen by `compute_verdict` — so those thresholds shape a REPORT and do not rank his shelf, which makes them a session's to take. That is the good news. The bad news is that each still needs a judgement about what it decides and what evidence would move it, and a waiver that does not name those is the bare `# lever-waiver:` the S106 Circle already struck once. VALUE: compliance and visibility, not a defect fixed — rank it accordingly against real work rather than doing it because the list is short. `Observed 2026-09-23 (S212 E2)` — opened by the session that mis-billed it, not worked down by it |
---

## §C OPEN SYMPTOM ROWS — `SYMPTOM-INDEX.md`

**25 genuinely open after the S111 Dock, relay-authority, and close-gate filings.** Retrieval keys are in the
index; this is the roll call. The prior 14-row sentence omitted SYM-053–SYM-057; S111 adds
SYM-058–SYM-064.

`SYM-003` (Repair Bench is the response, not a fix) · `SYM-024` (resume stderr hardened, not
root-caused) · `SYM-027` (glass debt — criteria 2 and 3 NOT MET) · `SYM-034` (cause unknown,
exposure bounded) · `SYM-035` (bounded and measured, not eliminated) · `SYM-039` (fixes
assigned to Desktop by the ThinkPad handoff §2.6) · `SYM-043` · `SYM-044` · `SYM-045` ·
`SYM-046` · `SYM-047` · `SYM-049` · `SYM-051` (environmental) · `SYM-053` ·
`SYM-054` · `SYM-055` · `SYM-056` · `SYM-057` · `SYM-058` · `SYM-059` · `SYM-060` ·
`SYM-061` · `SYM-062` · `SYM-063` · `SYM-064`.

**⚠ 1 FALSELY open — `Observed 2026-08-28`.** `SYM-032` still reads `open` in the status
column while its own body records S94 signing and building it. `SYM-033`, `SYM-041`, and
`SYM-042` were repaired to `FIXED-S94` in S108; F3 below records that completed repair.

---

## §D DELEGATED AND NEVER COLLECTED

*This is the section the commission asked for by name.*

| # | Delegated to | The ask | State |
|---|---|---|---|
| ~~D1~~ **STRUCK 2026-08-27 — COLLECTED** | **Codex** | Four asks, standing since S99: adopt a **model trailer** on its commits · **claim its S97 sections** per `coordination/authorship.md` · write **`docs/43` §3** in its own words · **leave a relay entry back** | **COLLECTED S108.** `6ae112f` claims the Codex S97 sections and writes `docs/43` §3; all four parcel commits carry `Model: OpenAI Codex`; Codex returned the final evidence route to Fable at relay timestamp `2026-08-23T21:58Z`. Historical baseline preserved: the relay had zero Codex returns when this row was built S107 · `Observed 2026-08-27` — `6ae112f` exists; 8 commits carry the `Model: OpenAI Codex` trailer |
| ~~D2~~ **STRUCK 2026-09-15 (S157 E4) — COLLECTED** | **Codex** ⟨status 2026-09-04: DONE — 2026-09-04 verifier (HEAD bde007d): DONE — COLLECTED S108 at `0fbb6e3` (ancestor of HEAD). Strike.⟩ | The one probe Fable could not run: **re-measure P-1 on a CLEAN post-S60 bundle** independently and say whether SYM-050 holds | **COLLECTED S108 (`0fbb6e3`, S108 §5).** Clean post-S60 control: numerator 1 covered figure page / denominator 1 source-figure page = 1.0000, detector false, map as-shipped. Poisoned IV positive control: 220/269 = 0.8178 after repair versus 30/269 = 0.1115 naive, detector true. One clean specimen never promotes to a population claim. Stafford scan p129 then proved the separate blind spot: the mapped asset is blank while two hand-drawn callouts are missing (SYM-053) · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — 0fbb6e3 an ancestor of HEAD (the S108 collection) |
| ~~D3~~ **STRUCK 2026-09-15 (S157 E47) — the bequest collected, all five items** | **The next Circle** | **`docs/45` §6, the bequest — 5 items.** (1) re-check `docs/32` §6's prediction (2) *does Family 1 have a check yet — what compares a claim to the probe that produced it?* (3) re-measure P-1 clean — **COLLECTED CORPUS-WIDE 2026-08-31 (post-S112 night)**: all 24 anchor bundles via coverage_rescore.py, sessions/S112-p1-corpus-sweep.jsonl. IV book: 220/269 figure-pages covered (49 uncovered, map REPAIRED — the poisoned 239/0.1115 headline is formally replaced); Ashby 14/23 (9 uncovered, worst in library); Book of Models 56/57; born-digital small books full; scan lane fails closed UNREAD by A4 rule (Brain/Best-Practices/DIAGNOSING); Designing-with-Freedom UNREAD (source PDF absent). SYM-050 detector fired NOWHERE except the known IV specimen (4) P-2 now has multiple specimens across **different** failure modes (5) the explicit UNREAD list | S106's Circle collected part of it and **refuted item 4's own framing** (see §E). **§6.2 remains fully open: nothing compares a claim to its probe.** S106 proved it again by shipping a rule it had not measured · `Observed 2026-09-15 (S157 E47)` — items 3–5 at E24–E31 (docs/45 §7); items 1 and 2 by this Circle (docs/45 §8): §6.1 the prediction SCORED — 55 rows since S105 read as a guard passing while its property was broken (24 of 78 SYM rows, 31 of 108 ERR rows; five Sonnet lanes with a plant each, 5/5 caught; the lane's own sample of ten holds 10/10); §6.2 Family 1 has checks for five NARROW claim kinds (a hash/SHA identity, a file-touched claim, a fleet's self-report, a register status, and a whole record's sentences by the verifier-fleet method — on commission only) and NOTHING that reads a sentence's referent against the probe's scope: docs/45's rows F1-2/3/4/5/7 still slide past every mechanism. Not a new row — the instrument that catches the class is the verifier fleet (E22/E39), and whether it runs continuously is a policy word |
| ~~D4~~ **STRUCK 2026-09-15 (S157 E47) — SCORED: confirmed 55× since S105, in the guards built to enforce the rules** | **The next Circle (S79's)** | **`docs/32` §6's falsifiable prediction**, bequeathed via `docs/33` §4 | **Uncollected for 25 sessions.** Confirmed 5×, scored 0×. `docs/45` calls this its own most severe finding · `Observed 2026-09-15 (S157 E47)` — docs/32 §6's prediction checked against everything built since 40a643a: 55 confirmations across the two registers (SYM SYM-054, SYM-055, SYM-056, SYM-057, SYM-062, SYM-066, SYM-070, SYM-073, SYM-090, SYM-091, SYM-096, SYM-098, SYM-100, SYM-101, SYM-104, SYM-113, SYM-115, SYM-116, SYM-119, SYM-120, SYM-121, SYM-122, SYM-126, SYM-133; ERR …014, 017, 018, 019, 020, 022, 024, 025, 027, 031, 032, 034, 035, 036, 049, 050, 051, 058, 059, 065, 070, 075, 077, 079, 088, 092, 099, 101, 104, 106, 108), at least twenty of them INSIDE governance guards built after S105 (close.sh ×6, guard_git ×3, the open card, the schema registry check, the deferral-gate selftest, the relay-gate suites ×10, the close chain ×3, the analyst selftest's isolation ×2, the fleet harness, the tracker's receipt) — and one more inside this Circle's own brief (ERROR-BIN.md called LF; it is CRLF). docs/45 §6.1 said 'if it confirms again, treat it as a law' — the count exists now; the promotion is his signature (docs/32 §7: not signed). The guard inventory: 44 guard-shaped files changed since S105 — 39 state their property AND carry a tripwire, 3 state it with no tripwire (`.claude/hooks/check_coordination.py`, `.claude/hooks/warn_heredoc.py`, `prototypes/analyst-lab/edit-whitelist/promotion_check.py`), 2 are not guards |
| ~~D5~~ **STRUCK 2026-09-15 (S157 E46) — two of three done before, the third has no target** | **Desktop, from ThinkPad** | SYM-039's fixes, assigned via `coordination/messages/2026-08-16T21-51…wip-handoff` §2.6 | SYM-039 still `open` · `Observed 2026-09-15 (S157 E46)` — §2.6's three: SYM-014's guard text corrected at S86 (the row says so, against `bundle.py:72`); SYM-018 annotated 2026-08-10 (the strikethrough + SYM-020 note in the row); the `CLAUDE.md` test counts and the 'rsync over tailscale ssh' component line have NO target here — no `CLAUDE.md` exists in this repo's history in any ref (`git log --all -- CLAUDE.md` is empty; the brain has been `CLAUDE_README.md` since 0dcdac0) and the phrase is in no tracked doc outside sessions/ and the message itself; the ThinkPad's own untracked copy is UNREAD (offline). SYM-039 → `fixed` with that residue named |
| D6 | **Cross-machine** | Two coordination messages still carry `status: open`: `2026-07-05T0910Z--linux-to-desktop--windows-work-brief` and `2026-08-17T03-05--desktop-to-linux--familiarization-complete-and-the-fork-reconciled` | `Observed 2026-08-22` via a status grep over `coordination/messages/` · `Observed 2026-09-16 (S160 E4)` — FOUR carry `status: open`, not two: the row's two plus `2026-09-09T21-24…index-station-receipts-and-query-contract` and `2026-09-10T01-17…index-station-deployed` (linux-indexer → desktop). Dispositions: the 07-05 brief EXPIRED 2026-07-12 (README rule 2: history) and is NOT finished — its ask 2 (the end-to-end test) is B31 and its ask 3's toast is B28's missing half, so it is not flipped; the 08-17 one is addressed TO the ThinkPad and is the recipient's to flip (rule 4), expired 08-31; the 09-09 one FLIPPED `done` this episode with an `## Outcome` (the two `receiptMsg` phrases landed in `room.js`, rendering at his next rebuild; the query contract on record); the 09-10 one stays `open` — it also owes the query-leg proof (the ThinkPad UNREAD today) and a relay entry to Codex (held by his word). |
| D7 | **ThinkPad lane** | Phase-gated and awaiting an explicit go: **enrichment consumer** (schema-constrained outputs) · **`docs/14` Phase A phone window** · the **Desktop half of the supersede seam** (`assay.rs` ⟳ marker + `convert_and_ship` consume) so a real remedy produces a real swap | the S43 ledger row records the Desktop half as NOT done; the memory open queue carries the rest · **PARTIAL CORRECTION `Observed` 2026-08-27 — the third clause is FALSE and has been for ~4 weeks.** The **Desktop half of the supersede seam is BUILT, both halves.** The widget WRITES the marker (`assay.rs:22` `SUPERSEDE_DIR`, `:210`, `:329`) and the converter CONSUMES it (`convert_and_ship.py:405` `_take_supersede_marker`, `:430` `_stamp_supersede_safe`, `:384` the `--superseded-` destination). Landed `6a6fce0` **2026-07-25** (consume) and `e5bdd20` **2026-07-31** (write) — `git log -S` on each symbol. And it is TESTED across the seam: `assay.rs:646` exercises the converter's `_take_supersede_marker`, *"proving both halves agree on the actual bytes"* — a cross-side test, not two same-side ones (SYM-001). **The row's cited evidence is the S43 ledger row, which was true when written and has not been re-measured since.** The other two clauses — enrichment consumer, `docs/14` Phase A phone window — are NOT corrected here and remain as written; I did not probe them. Row NOT struck for that reason. Found by the S67–S110 closeout sweep; verified independently before this edit. ⟨status 2026-09-09 (ThinkPad, Fable): **Rab's go on the first clause** — *"go ahead, pull and scaffold linux-indexer"*, then *"if a solution can be created that is far more operator friendly … do it, make sure its free, open sourced, and integratable"* — the enrichment consumer's EMBEDDING half is scaffolded as `linux-indexer/`: ONE SQLite file at `~/file-portal/index/index.sqlite` (bundles, passages, FTS5 keyword index, sqlite-vec embeddings), whole-tree reconcile to the vault tip fired by a `post-update` hook + daily timer, fastembed all-MiniLM-L6-v2 by default with the model a lever (fastembed's ONNX export is NOT July's torch model: cosine floor 0.58 on 64 passages, retrieval 7/7 on the probes), hybrid query (vector + BM25 + title leg, filters inside SQL, opt-in reranker), status CLI, opt-in loopback HTTP endpoint, `indexed`/`index-failed` receipts, 43 hermetic tests, live-proved on a bare clone of the real vault, then DEPLOYED 2026-09-10 on Rab's word (units + timer + hook installed; first run 6 bundles → 3,951 passages, 75 s, `indexed` receipt at tip 50896d15; hook-fired quiet run proven). Remaining: the Desktop's `room.js` phrases for the two outcomes (coordination message filed), the tagging half (schema-constrained outputs) and the `docs/14` Phase A window — both untouched.⟩ |
| D8 | **Rab's hands** | The S94 guard exercise: minimize + relaunch (expect restore-and-front, no twin) · Room styling under CSP · Recent audits panel · chat page · boot log · one PID · **P-0's figures line on a real book** · the widget-restart "engineer question" **fourth ask** (pending since S89) | ⚠ **RECORD CONFLICT — do not restate either side without re-measuring.** `MEMORY.md` says the S94 guard exercise is still `Unknown` and unlogged; the **S102 ledger row says "S94 single-instance guard exercised and HELD (Unknown→Observed)"**. Both cannot be right · `Observed 2026-09-16 (S161 E3)` — THE CONFLICT RESOLVED by reading both sides: the S102 record (`sessions/S102` §3 items 6–7) says the single-instance guard was EXERCISED and HELD — a second launch produced no twin (PID 29936 unchanged, the watcher tree unchanged), the boot log healthy, one PID — and in the SAME section lists what stayed Unknown: Room styling under CSP · the Recent audits panel · the chat page · P-0's figures line. The memory library's `installed-exe-state.md` had lumped the whole S94 list as `Unknown` since before S102 — the stale side; corrected today (the library's commit names this row). Restore-and-front on minimize+relaunch was NOT stated observed at S102 (a second launch ≠ a minimize) and stays his eyes with the rest. The row stays for those: they are his hands, as its title says. |

---

## §E THE CONVERSION-COMPLETENESS SLATE — `docs/41`, register items 6–9

| item | state |
|---|---|
| **P-0** the wiring slice | **BUILT S101** and rendered on both surfaces. Its acceptance — *Rab looks at the figures line on a real book* — is D8, unlogged |
| **P-1** figure/vector completeness | **BUILT S102, vetoed S104, re-measured S106, WIRED S108 (`0fbb6e3`).** A18's narrow repair and A4's out-of-band host now have 30/30 + 8/8 tripwires. Remaining limit now has a real specimen: Stafford PDF p129 contains two large hand-drawn callouts, while its sole mapped final asset is a constant-color blank strip; page-level coverage falsely called the page covered. Scan-lane rendered-region versus final-crop coverage remains UNREAD/OPEN (SYM-053); B14's inert-constant disposition remains separate. **MEASURED S188 E7 / S189 E1–E2 (2026-09-17):** the crop side of SYM-053 is mechanical now — `blank_assets.py` reads every asset's pixels on every conversion (Stafford p129's blank strip reads blank, sd 0.00, referenced; three scanned blank versos and a text-only dedication page read near-blank; two referenced blanks and four near-blanks across ~2,300 assets), report-only, the consequence his; the rendered-region-versus-crop comparison stays UNREAD (A44's hOCR half, his design). The instrument's own convention assumption is declared (SYM-095 FIXED: `triage_convention` unmet on the Cybernetics book — READ FIRST empty by convention, not by coverage); the table veto's second-shaped check was S157 E25's disqualifier (SYM-094 stamped fixed, read S189: 0 vetoed tables on the specimen, 71 figure pages, p84 reported) |
| **P-2** the #598 tripwire | **NOT BUILT.** Disposition unsigned (A5). Specimen count is contested: S106 claimed "8 specimens across 3 failure modes"; **its own Circle found that is a count no surface enumerates** — of the five "confirmed lost figures", 9.3 is the *only* clean loss, 9.1/9.2 are present **as pipe tables** (the p84 fidelity class), 12.3 is partial, 3.1 was S105's "destroyed in place" |
| **P-3** RETAS / Flexible Character Accuracy — the text-loss locator | **NOT BUILT, never scheduled** |
| **P-4** LLM as adjudicator, never as searcher | **NOT BUILT, never scheduled** |
| **P-5** DPI as a measured experiment | **NOT BUILT** — decision A6 |
| **P-6** generative-SR standing ban | **NOT RECORDED** — decision A7. Zero code either way |

**⚠ Numbers not to quote without re-measuring**, per the S105 and S106 Circles: `uncovered 309`
(arithmetically impossible) · `5.6 ms/page` (an unshipped build; shipped is 37 ms/page IV,
72 Cybernetics) · `82 %` promotion (the pre-precedence build; shipped is **167 of 239 = 70 %**) ·
`FIVE confirmed lost figures` (the probe measures **image-asset absence**, not lost content) ·
every Investment Valuation figure computed before the map repair.

---

## §F RECORD-INTEGRITY REPAIRS

| # | Item | State |
|---|---|---|
| ~~F1~~ **STRUCK 2026-09-15 (S157 E45) — the header restored, proved on a copy first** | **The Change Ledger table is physically split.** Header at `CLAUDE_README.md:1126`; the S79+ rows sit at EOF (from ~line 1972) with **no table header**, after S43's narrative prose. The newest rows do not render as a table | `Observed 2026-08-22`, unrepaired since S96 §18.1 flagged it · `Observed 2026-09-15 (S157 E45)` — five lines inserted above the first EOF row (S79, line 1975): a blank, a one-line note naming the split and this repair, a blank, the same header row and delimiter as line 1129. Proved BEFORE the edit on a copy: muster.sh's own ledger parse (the `^| 20YY-` grep + awk) reads byte-identically before and after — 139/165 rows, a header and a delimiter never match the row pattern; then `open.sh` exit 0 (`[3b] LEDGER PARSE ✓ 139/165`), `selftest.sh` 110/110. CRLF uniform (2060 CRLF, 0 bare LF). The table now renders in two parts under two identical headers; the rows themselves were not moved (append-only) · `Observed 2026-09-15 (S157 E45, the hold)` — the README commit 676af16 could not be pushed: guard_git's J71 runs `row_check.sh 157` on any push carrying the ledger file and the S157 row exists only at the close; the pre-E45 bytes were restored in a new commit (history append-only) and the same script re-applies the proved repair at the close, immediately before the row. The strike stands on the proof; the bytes land with the row |
| ~~F2~~ **STRUCK 2026-09-16 (S161 E1) — WRITTEN DOWN: docs/21 §9 (appended, dated) names the post-close form the records already follow (Observed: 49 records carry `## §20` — the first S109, every Desktop record from S115 on, S111–S114 without; 143 `post-close` commits since S99) — §19 by the chain before the closing commit, the row never amended, §20 after the row with CI observed on the row commit and the lockstep, the post-close commit's own CI read by the next sitting's §3, the marker removed last (J63 the tripwire). Was:** | **The protocol has no form for post-close work** (A31). S106's four post-close commits — `4d06588`, `c7e3812`, `d7ffd11`, `3659ec7` — **reach no permanent surface**; they are recorded only in the S106 closeout's addendum because the ledger physically refused them | `Observed 2026-08-22`; CI observed green on `c3e1f8b` |
| ~~F3~~ **STRUCK 2026-09-15 (S157 E40) — RESOLVED S108 — struck S157 E40** | **Four false `OPEN`s** in the SYMPTOM-INDEX status column (§C) | `Observed 2026-08-22`, unrepaired since S96 §18.2 **RESOLVED S108 (`ffc7b8a` + `97906a3`)** — three verdicts folded, SYM-032 gained the verdict its cell never had; trails preserved. · `Observed 2026-09-15 (S157 E40)` — the row's own note: RESOLVED S108 (`ffc7b8a` + `97906a3`, three verdicts folded, SYM-032 gained its verdict); both commits are ancestors of HEAD (`git merge-base --is-ancestor`, 2026-09-15). Nothing left to repair |
| ~~F4~~ **STRUCK 2026-09-15 (S157 E27) — ANSWERED S157 E27** | **`S82`'s ledger row is dated 2026-08-16; its closeout file is `S82-desktop-2026-08-15.md`** | `Historical` (S96 §18.5) · `Observed 2026-09-15 (S157 E27)` — one moment in two clocks: S82 opened 2026-08-15 23:42 EDT = 2026-08-16T03:42Z (the file's own MUSTER stamp) and closed 08-16 00:21 EDT (git `8fb374e`); the closeout file is named by the LOCAL date at open, the ledger row by the UTC date — both right, neither to be edited. The convention worth stating: file names local, ledger rows UTC |
| ~~F5~~ **STRUCK 2026-09-15 (S157 E4) — SUPERSEDED** | **Correct the S102/S103 ledger rows and CHANGELOG**, or rule that newer rows supersede (A27) ⟨status 2026-09-04: SUPERSEDED — 2026-09-04 verifier (HEAD bde007d): SUPERSEDED by A27 (same ask, OPEN-TASKS:150). A27 is still OPEN — its `Historical (S105)` is the observation tag, not a disposition. Strike F5.⟩ | open · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — A27 is still an open row of this file (the same ask); F5 is its duplicate |
| F6 | **`MEMORY.md` is at ~200 lines — the read limit** — and has silently dropped pointers before. Compaction is Rab's call (S96 ruled the block must not be relocated). *That growth IS `docs/45`'s M5 finding, live* ⟨status 2026-09-04: STALE — 2026-09-04 verifier (HEAD bde007d): STALE — MEMORY.md re-measured 172 lines / 21,315 bytes (sha256 cf2ed96d…), under the ~200-line limit; compacted at the S112 and S113 closes. The 220/25,237 measurement no longer holds; docs/45 M5 growth remains a watch item.⟩ | `Historical` (S105). **Observed 2026-08-28:** canonical `MEMORY.md` is **220 physical lines / 25,237 bytes**, SHA-256 `958296A202D878D46577213A5B4DECCD3C58686FBF7CE71A93CCE9036FB9880F` — 20 lines beyond its ~200-line read limit. No compaction was performed; the disposition remains Rab's decision and the protected block was not moved. |
| ~~F7~~ **STRUCK 2026-09-15 (S157 E4) — DONE** | **Reconcile the `.Codex` vs `.claude` canonical memory namespace** before migrating or duplicating memory data ⟨status 2026-09-04: DONE — 2026-09-04 verifier (HEAD bde007d): DONE — one canonical namespace: `~/.claude/settings.json:3` autoMemoryDirectory → `~/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory`; the library's session-bootstrap.md:35 records it (baseline `0087a22`, S96); no `.Codex` path remains in the library (C:/Users/Bndit/.Codex is the Codex CLI's own home). Strike.⟩ | S95 §18.5 · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — `~/.claude/settings.json:3` autoMemoryDirectory → `~/.claude/projects/C--Users-Bndit-Documents-Claude-Code-Memory-Backup/memory`; one namespace, loaded every session |
| ~~F8~~ **STRUCK 2026-09-15 (S157 E16) — CORRECTED on the index row** | **SYM-046's own account is wrong**: "three closeouts cited it" — S100 never cited glass; its hollow claim is in its **ledger row**, which nobody corrected, and the correction appended to S100 corrects a claim that closeout never made. S97's glass line was also hollow and got no correction at all | `docs/45` F4 · `Observed 2026-09-15 (S157 E16)` — the correction appended to SYM-046's status cell (S157 E16): S100's closeout body names glass only in a relay note; the hollow `exit 0` citation is in its ledger row (`CLAUDE_README.md:1998`); the S101 correction on S100 answers a claim the closeout never made; S97:211's line was the same form and got no correction — the count stands as two closeouts and one ledger row (Observed: `grep -n glass` on both closeouts and the ledger) |
| ~~F9~~ **STRUCK 2026-09-16 (S161 E4) — PRIORITIZED, item by item (`sessions/S96` §18, seven items, each measured at HEAD): (1) the split ledger table — DONE, F1 struck S157 E45 (the header restored); (2) the four false OPENs — SYM-033/041/042 read FIXED-S94 (cells corrected S108 per F3), SYM-032 reads `open` and IS open (the watcher gate signed, not built) — DONE where it was false; (3) the S94 guards exercised — the single-instance half Observed at S102 (no twin), the eyes-on-the-glass half his (D8, resolved S161 E3) — CARRIED by D8; (4) `c5afd9edcf620fc6` held and vaulted — RESOLVED S117 (A41 struck S157 E40; the bundle re-shipped 2026-09-05, `held/c5afd9edcf620fc6--reshipped-20260905-211727`) — DONE; (5) S82's row date 08-16 vs its file 08-15 — the sitting crossed midnight UTC (the file is named by the local date it opened, the row by the UTC date it closed; the same shape as S159's 09-15 file / 09-16 row) — RETIRED, not a defect; (6) Stage 2/3 GO, the asset posture, the standing queue, acceptance red 38/39, the Linux suites UNREAD — SUPERSEDED by this register (§E carries the slate; acceptance reads 185/0 green today, armed in CI since S116; the Linux suites run in CI, armed 2026-09-10); (7) the library's remote — DONE (A43 struck S157 E4; the private remote since 2026-09-05). Nothing silently displaced: every item has a row, a strike, or a reason. Was:** | **S96's untouched next-entry list** was inherited `Historical` by S97 and **still requires explicit prioritization rather than silent displacement** | S97 §18.4 |
| ~~F10~~ **STRUCK 2026-09-15 (S157 E40) — ABSENT, never tracked — struck S157 E40** | **The stale pre-rewind `survival-audit-spec.md`** in the working dir — superseded by `docs/15`, deletable, Rab's call ⟨status 2026-09-04: STALE — 2026-09-04 verifier (HEAD bde007d): STALE — survival-audit-spec.md is absent from the tree and was never tracked (no deleting commit exists to cite; absence is the evidence). Strike.⟩ | memory `file-portal-project` · `Observed 2026-09-15 (S157 E40)` — `survival-audit-spec.md` is not in the working tree and not in `git ls-files` (2026-09-15); the row's 2026-09-04 verifier had already said so and recommended the strike; there is nothing for his call to delete |
| F11 | **This register** — work it down or delete it (see the header warning) | new today (S107) · `Observed 2026-09-17 (S197 E3)`: worked down, not deleted — the file's open rows read 96 (struck 187) across every section; the four surveyed sections (§B 11 · §D 3 · §F 2 · §J 41 = 57) after S196's four strikes carry their OWNERS, read by the S196 survey fleet from each row's own words and re-read by the coordinator (`sittings/S196/survey.json`): 42 his (a hand, a word, a signature), 10 the card's (a GPU run on his word), 2 Codex's (A49, J54 — nothing to Codex until lifted), 3 the lane's (F11 itself; the crammed row, withheld at one; J60, moot while the relay watch is OFF). §C's fifteen symptom rows were named to their owners at S195 (one the lane's, built). The header's warning is answered by the count moving — 61 → 57 surveyed rows this day; deletion stays his |
| ~~F12~~ **STRUCK 2026-09-15 (S157 E27) — ANSWERED S157 E27** | **`SYM-051(b)` cites "RAM 15.9 GB total"; the machine measures 31.9 GB.** Its whole memory-exhaustion argument rests on `0.7 GB free, 95.6 pct used` against a 15.9 GB total. Measured `2026-08-25T03:5xZ`: `Win32_OperatingSystem` reports **32,698 MB total, 11,121 MB free, 66.0 pct used**. Either RAM was added since 2026-08-21 or the original reading was wrong — **from here those are indistinguishable and a session may not guess.** SYM-039's class: a hardcoded number in prose contradicting a live probe. If it is an upgrade, it partially CLOSES SYM-051(b) and that row must say so | `Observed 2026-08-25` (S109 second sitting, process census). **Resolve by asking Rab whether RAM was added**, then correct the row in place with the date — do not silently overwrite evidence · `Observed 2026-09-15 (S157 E27)` — the machine's own record answers it: the memory library's `desktop-machine.md` records **32 GB RAM, 2×16 GB G.Skill F4-3600C18-16GVK, UPGRADED 2026-08-21 replacing 2×8 GB Corsair** — Rab's hand, the same day SYM-051(b) was measured; today's probe `wmic memorychip get Capacity,PartNumber,Speed` reads two DIMMs of 17,179,869,184 bytes, F4-3600C18-16GVK, 3200 — Observed. So the 15.9 GB reading was TRUE at its moment and the machine has since doubled; SYM-051(b)'s exhaustion argument is partially closed and its row now says so with the date |

| F14 | **THE AUTHORSHIP STAMP STOPPED BEING WRITTEN 89 SESSIONS AGO, and it is not decoration — it is what the MUSTER card's SYM-072 discriminator reads.** `Observed 2026-09-24 (S212 E5)`, counted rather than recalled by `stamp_census.py`: of the **123** session records since the convention began at S99 (2026-08-20), only **27** carry a `⟨claimed: … S<n> …⟩` stamp naming their own session, and the last one to do so is **S123 (2026-09-09)** — so **S124 through S212, eighty-nine consecutive sittings, carry none**. THE CONSEQUENCE IS LIVE, not cosmetic: `open.sh`'s SYM-072 branch decides "this session's closeout" versus "a stranger's file" on that delimited stamp, so **any sitting that runs past midnight reads COLLISION and the open exits 1** — which is what every open of S212 did once the date rolled over, and which I first read as a limitation of the card before reading its source. S212's own record is stamped now and the card reads clean (exit 0, `a later day of the same session (SYM-072)`), which is the guard FIRING rather than the guard relaxed. **The other 88 are deliberately NOT retro-stamped**: `coordination/authorship.md` rule 1 is *claim only what you wrote*, those records were written by other occupants in other sittings, and back-dating an authorship claim across 88 files is a record rewrite rather than a repair — it needs Rab's word, not a session's. What a session CAN do is stamp its own record at the open, and that belongs in SKILL.md Phase 6 beside the file it already tells you to create | `Observed 2026-09-24 (S212 E5)` · `stamp_census.py` · `coordination/authorship.md` · `open.sh:530-548` |
| F15 | **S210's LEDGER ROW COUNTS 18 DOCUMENTS; EIGHT OF THEM ARE S209's, ALREADY COUNTED IN S209's 23.** `Observed 2026-09-24 (S213)`, from `sourcing/results.jsonl`: 18 rows were queued on 2026-09-21 up to S210's close (14:37:26Z); **8 were queued before S209 closed at 06:13:03Z**, so **S210's own work is 10 documents**. The same 18 sits in S210's commit message and Desk closeout, and every lane of the S210–S212 review repeated it until its adversarial verifier re-derived it. **The S210 row is deliberately NOT edited** — a closed sitting's text stays what was believed at the time (S179's rule, sign-sheet item 3); the corrected reading lives here and in S213's record | `Observed 2026-09-24 (S213)` · `sourcing/results.jsonl` `queued_at` against S209's close · private `sittings/S213/retro_findings.json` |
| F16 | **A WARN-ONLY CI SUITE WAS RED FOR THIRTEEN RUNS AND NO CLOSE COULD SEE IT: `close.sh` [4] reads the run's `status` and `conclusion` only, and a `continue-on-error` step's conclusion is `success` whatever its log says (SYM-075).** The muster selftest went red on the runner at S212's `3e6a9de` (CASES 71 and 72, platform-shaped, added without the `on_windows` gate) and stayed red through S212's close, the ledger row, S213's open and three S213 commits. `ci_observe.py` printed it every time (`warn-only steps whose LOG says exit != 0: 1`); S213 read past it three times (S213 CORRECTIONS row 5). The cases are gated in S213; THIS row is the instrument gap: [4] should print each warn-only step's LOG verdict beside the conclusion, as a READING (the exit code untouched; arming A29 stays his). | `Observed 2026-09-24 (S213)` · run 36049946020's muster step log: `5 failed of 138`, CASES 71/72 with empty `got:` · the saved observations map it: `LOG clean` through `a548c52`, `LOG exit 1` from `3e6a9de` on · `close.sh` lines 246-258 read `status\|conclusion` and nothing else · private `sittings/S213/ci_step_text.py` reads one passed step's log |
| F13 | **`docs/15` has TWO sections numbered §9.5, and one of them carries S179's table with the refuted 21 % false-alarm rate in it — so a signed document now disagrees with the register.** The row reads, verbatim: *Ashby, An Introduction to Cybernetics (156, clean) | 3 | `A B A B B B A B A B …` (1,975 chars, zlib 0.023, trigram 326) — the book's own Markov-chain protocols (Ex. 1: "a protocol of 50 transitions")* — it QUOTES the book's own count of fifty and then classifies a 1,975-character block as that protocol. Eight blocks have since been read at their pages and all eight are loops; the real rate is 0 of 14 conversions, 0 of 6 books. **Editing a signed document is Rab's, not a session's** — it is item 9 of `sittings/S211/SIGN-BRIEF.md`, with the recommendation that the numbering and the rate be corrected TOGETHER, or the next reader fixes the numbering and leaves the wrong number sitting in a signed document. | `Observed 2026-09-23 (S211 E11/E12)` — opened by this sitting, not worked down by it, because it is his |
---

## §G WHAT IS *NOT* OPEN — so nobody re-opens it

- **The Opsroom prototype verdict.** Superseded — the Room shipped at S34. The memory open
  queue's "(a) verdict on the Opsroom" line is `Historical`; S96 corrected it.
- **The S32 installer question** — long closed.
- **The exporter supersede flow** — BUILT at S43 (`bd02fc0`), live-fired S50/S56. ~~*The Desktop
  half of the seam is what remains* (D7).~~ **CORRECTED `Observed` 2026-08-27: it does NOT remain.** Both halves have been in the tree since `e5bdd20` (2026-07-31), with a cross-side test at `assay.rs:646`. This bullet sits in §G — the section whose whole job is to stop work being re-opened — and it was itself the thing that needed closing. See D7.
- **F-09 semantics** (SYM-041) and **the card mutex** (SYM-042) — signed and built at S94,
  despite what the status column says.
- **`muster.sh` unversioned** — closed at S79.
- **SYM-030, SYM-048, SYM-052** — fixed.
- **`docs/15` §5 "a large delta is a flag"** — WITHDRAWN on evidence at S101. Do not cite it.
- **§9.1's "zero false alarms"** and **S82's four throughput ratios** — withdrawn.

---
- **Electron for the widget — PARKED (Rab, 2026-09-10: "keep electron on the backburner for now").** The assessment is `docs/59` (measured: Rust 5,292 lines / 42 commands; the front end runs on Chromium either way; 405 MB resident today, so memory is not the discriminator; S37's Job Object has no Node API). Re-open only on docs/59 §7's three conditions or his word; the four cheaper Tauri renovations there are the live alternatives (a CI build with the SHA-8 first).

## §H HOW THIS FILE STAYS TRUE

It will rot. `docs/45` Family 1 predicts exactly how: a line here will keep describing the
*neighbour* of what it names. Two cheap defences:

1. **Every close that resolves a line here strikes it in the same commit** — the same rule
   `docs/29` §5.4 applies to measurements.
2. **Never quote a status from this file into a claim.** Open the cited source. That is
   `docs/45` §6.2's open question in its smallest useful form.

---

## §I THE MAINTENANCE PROTOCOL — signed by Rab, S109 (2026-08-24)

His words: *"I need a task list that is dated, and that is updated in every session as part of
protocol, opening session checks it, closing sessions adds upon it, muster makes it mandatory to
check it."*

**Why it needed saying.** This register was built at S107 and **the protocol had never once
referenced it** — measured 2026-08-24: `OPEN-TASKS` appeared **0 times** in `muster/SKILL.md`,
`open.sh` and `close.sh`. It was a register nothing forced you through, which is a shrine, not a
spine. Its own header (line 26) already warned it "should either earn its keep by being worked
down or be deleted."

**Now mechanical, in three places:**

| where | what it does |
|---|---|
| `open.sh` `[2b]` | prints **counts, never a checkmark** — open items, symptom rows and open symptoms, relay entries — each with **when it was last written**. A register untouched for 2+ days prints `*** not written in Nd — is it still true? ***`. A tick would say the file exists; a count says how much is open, and **a count that never falls is visible as a count that never falls.** |
| `close.sh` `[8]` REGISTER | reports whether this file was written since the pin. **Every session either strikes an item or adds one; a session that did neither must SAY SO in its closeout rather than imply it.** Warn-only for one session, then armed. |
| `close.sh` `[8b]` DEBT | **§A21, finally built** — the open-SYM count now vs at the pin, with the direction named. It PRINTS, it does not block: a threshold that stopped a close on its first run would be tuned away by the second. |

**DATING RULE.** Every item carries the date it was **last verified**, not the date it was
written. `Observed <date>` means a command was run that day and its result quoted. `Historical`
means transcribed and **not** re-measured. An undated item is an item nobody has looked at.

**§A21 is now struck** — it proposed the debt gate on 2026-08-22 and sat unbuilt in the very list
it was designed to bound. Built S109.

---

## §J OPENED IN S109 (2026-08-24) — from the session's own gates, on their first run

*The register's first entries under its own protocol are the debts that protocol found. All
`Observed 2026-08-24` — each was printed by a gate, not recalled.*

| # | Item | Class | Source |
|---|---|---|---|
| ~~J1~~ | ~~STRUCK 2026-09-03T21:xxZ~~ **WAIVED.** `Observed 2026-09-03`: all three named constants now carry an inline `# lever-waiver:` comment on their own declaration line, naming Rab as the mover and the measured evidence that would move each — `BEAT_STALE_MIN` at `.claude/skills/relay-gate/gate.py:1113`, `GATE_TIMEOUT_S` and `MIRROR_MAX_ATTEMPTS` at `prototypes/relay-room/catcher.py:60-61`. `.claude/skills/muster/close.sh:145-158`'s LEVERS gate requires substance after the colon on that same line — a bare `# lever-waiver:` is rejected — so the waiver text was written against that exact case-pattern, re-read before writing. `python3.12 -m py_compile .claude/skills/relay-gate/gate.py` exits 0 after the edit. -- **3 threshold-shaped constants added with no lever and no waiver**: `BEAT_STALE_MIN`, `GATE_TIMEOUT_S`, `MIRROR_MAX_ATTEMPTS`. docs/18 §2: a number that decides something is a LEVER, not a constant | MECHANICAL | `close.sh` LEVERS, `Observed 2026-08-24` |
| ~~J2~~ **STRUCK 2026-08-27 — CLEARED** | **1 unsigned observability glitch since `4862be1`** — GLASS RED at close | MECHANICAL | `close.sh` GLASS `--enforce`, `Observed 2026-08-24` · `Observed 2026-08-27` — `glass_detector.py --since ab544d1 --enforce` → `no unsigned glitches`, exit 0. Scoped form per SYM-046. The detector's own caveat stands: *reads as clean is not the same as is clean* |
| ~~J3~~ **STRUCK 2026-09-15 (S157 E9) — BUILT, and it found something** | **SYM-054**: an agent fleet leaks live servers and a file-digest audit cannot see them. A fleet law needs a PROCESS census the way it already has a digest census | MECHANICAL | `SYMPTOM-INDEX.md`, `Observed 2026-08-24` · `Observed 2026-09-15 (S157 E9)` — private `agent-scripts/auditing/fleet_proc_census.py <since>` (+ brief; smoke 69/69): every process the tracker saw created in the window with no exit, checked against the live table (`tasklist` from Python, never `/FI` from a Git-Bash shell); exit 1 names each leak, never kills. First run over S157's own window: seven alive — five the machine's and Rab's (OTHER), and `llama-server.exe` — qwen3:8b loaded by the analyst SELFTEST at every run since S156 — eleven calls, five cold loads today (ERR-2026-09-15-106); unloaded by hand. Residue: the clause in docs/47 ("a process census after every fleet") is a signed law's text — his word; the lane's own protocol carries the step from this episode on |
| ~~J4~~ **STRUCK 2026-09-17 (S196 E2) — MET by the row's own done-when (re-time at the next close and name the step): re-timed at S157 E37 (72 s and 71 s, RUST skipped) and at the S160 close WITH the cargo path (≤ 75 s by the chain's own file times; the driver's 6 min 38 s is the wait for CI) — the step named: CI's wait is the bulk, the cargo path on a warm target costs seconds, the 8m37s is history. Residue, said: cargo on a COLD target is still unmeasured (no close has run one since the row was filed). Read by the survey fleet (S196 E1: reader + adversarial verifier, unrefuted) and by the coordinator's own read of the row and sessions/S157 E37 · S160:265** | **`close.sh` now takes 8m37s** (measured, `time`). It runs three suites that have grown from 22→83, 38→48, and 0→43 cases. A close nobody can afford to run is a close nobody runs ⟨status 2026-09-04: STALE — 2026-09-04 verifier (HEAD bde007d): STALE — close.sh never ran the three growing suites (at HEAD and at 623fe0a it runs only conditional cargo fmt/clippy/test + wiki selftest.sh, 11 tripwires); the muster/relay-gate/echo suites run per SKILL.md:257, not at close. The 8m37s is UNREAD read-only — re-time at the next close and name the step (cargo clippy on a cold target is the likely one).⟩ | MECHANICAL | `Observed 2026-08-24` · `Observed 2026-09-15 (S157 E37)` — re-timed: close.sh d0e193b runs in 72 s and 71 s (two rehearsals, RUST skipped — no widget change since the pin); the suites cost under a minute; the cargo path on a widget change is the unmeasured half · `Observed 2026-09-15 (S157 E56)` — this sitting touched `windows-widget/` (E53 JS, E55 Rust), so the close's `close.sh` will run fmt/clippy/test on a WARM target (E55's clippy+test just ran: 12 s + 0.13 s) — the re-timing the row asks for happens at the close, the step named then · `Observed 2026-09-16 (S160 close)` — the cargo half measured: this close touched `windows-widget/src/room.js`, so `close.sh` ran RUST fmt/clippy/test (all clean, warm target) and took ≤ 75 s by its own file times (the acceptance gate's end 18:00:40Z → its output 18:01:55Z) against S157 E37's 72 s without the cargo path; the whole close driver 6 min 38 s (18:00:15Z → 18:06:53Z), the wait for CI the bulk. The row's 8m37s is history; the step it asked to name costs seconds. |
| ~~J5~~ **STRUCK 2026-09-15 (S157 E59) — every NOT_YET tripwire exercised somewhere, each named** | **relay-room's `NOT_YET` list**: the live-server suite (T3/T4/T20/T21/T12), the catcher-cycle suite (T24-T27) and the end-to-end T28 are unbuilt. Declared, not hidden — but declared is not done | MECHANICAL | `prototypes/relay-room/test_room.py`, `Observed 2026-08-24` · `Observed 2026-09-15 (S157 E52)` — the row was STALE: T24–T27 live in `test_room_c.py` (a driven catcher against the real gate), T3/T4/T12/T21 and T28's end-to-end in `room.py selftest` (an ephemeral port, a catcher subprocess); only T20 (a live SSE client) is unexercised. RUN, the end-to-end read 15/19 with 4 FAIL and WROTE THE LIVE `state/` (room.md, status-fable.json, ack-fable.json, flight/, handoff/) while its own last check passed — the redirect set module attributes that `read_log(path=ROOM_MD)`/`append_entry(path=ROOM_MD)` had frozen at import, and the catcher subprocess never saw it (docs/32 §6, inside T28 itself). REPAIRED: the paths resolve at call time, `FP_ROOM_STATE` relocates a subprocess, the named lock derives from the log's current location; the contract's §5.4 trail OBJECT written once as `roomlog.render_trails` (room.py's routes and the catcher had passed the LogRead into `render_trail`'s `lane` — a 500 on claim, `state: error` every catcher cycle, `/api/flight` without `trails`); T28 now measures the live tree byte-for-byte. After: unittest 65/65 (7 errors at HEAD), `room.py selftest` 20/20, `selftest.py` 43/43, the live tree restored to HEAD's bytes and untouched since. Open for T20 only · `Observed 2026-09-15 (S157 E59)` — T20 joined `room.py selftest`: connect `/api/events` on the throwaway tree; `text/event-stream`; the FIRST frame `event: hello` with a JSON data line naming the cursor and the note; a say posted while the stream is open reaches it as an `entry` frame within 3 s; every data line JSON with no raw newline; the CONTROL the contract names (a hand-serialised payload with a raw newline would split a frame; `_emit`'s escape removes it) — 27/27. With E52: T24–T27 in `test_room_c.py`, T3/T4/T12/T21/T28 and now T20 in the end-to-end; `test_room.py`'s per-file list stays true of that file and says where the rest live |
| J6 | **`T-004`'s joint half** is blocked on the Codex lane's usage — **not on Rab**. Its EXTRACTED schema half is published for review ⟨status 2026-09-04: STALE — 2026-09-04 verifier (HEAD bde007d): STALE — Codex's usage returned in S113 (f045a66), so 'blocked on Codex's usage' is dead; the bus's last words are 'T-004 … Rab's alone' (relay.md:3302) and 'remains UNREAD' (:3390); the schema file still names Codex's review as the precondition. Attribution now: Rab's call, Codex's review first.⟩ | SEMANTIC (blocked) | `coordination/T-004-EXTRACTED-SCHEMA.md`, `Observed 2026-08-24` |
| J7 | **The disclosure standard and the private-layer doctrine** remain drafts. Only D2 of six triggers is mechanical; D1/D3/D4/D5/D6 are discipline | SEMANTIC | `coordination/DISCLOSURE-STANDARD.md`, `Observed 2026-08-24` |
| ~~J8~~ **STRUCK 2026-08-27 — CLOSED BY DECLARATION** | **`MSG-FAB-0018` names two entries on the bus, permanently** — the residue of the 17:25Z live-bus incident. Appends never erase; it is declared, not repairable | RECORD (closed by declaration) | `coordination/relay.md`, `Observed 2026-08-24` · `Observed 2026-08-27` — its own Class cell already said so; striking makes that visible instead of leaving it in the open count. `MSG-FAB-0018` still appears 11× in `relay.md` and always will — appends never erase |
| ~~J9~~ **STRUCK 2026-08-27 — PREMISE DEAD** | **The bus is DORMANT** — Codex out of usage. Its lane will read STALE indefinitely; `MSG-FAB-0029` records why so the silence is not read as a fault | RECORD | `Observed 2026-08-24` · `Observed 2026-08-27` — the bus is NOT dormant. Codex posted `MSG-CDX-0012` at 05:50Z, sent=12, and completed T-009 both directions |
| J10 | **C0 HAS STILL NOT BREATHED.** `drop/` empty, watcher not running, widget down. Recommended first book: Ashby, *An Introduction to Cybernetics* — the ancestor of every Beer volume already in the corpus | **THE ONLY ONE THAT MOVES THE SENTENCE** | `Observed 2026-08-24` · **PARTIALLY OVERTAKEN, `Observed 2026-08-27` — NOT struck.** The sentence "has still not breathed" is now FALSE: Ashby converted end-to-end and `events.jsonl` went 137 → **147** (last event `2026-08-25T05:29:46Z`). But C0's own done-when has three clauses and only two are met — the held/ bundles still have **no disposition**, and there are **five**, not four. Striking this row would claim C0 is closed. It is not. ⟨**SECOND BREATH 2026-09-06 (S117)**, Rab's book: *Designing Data-Intensive Applications 2e* (673 pp, born-digital) — drop 00:31:40Z → Marker 504 s (0.75 s/page, peak VRAM 9,015 MiB) → convert-phase `flag` 0.8559/144 pages → analyst 2 h 13 min, 481/11/0, `rejections {fence 8, survival 3, think_leak 0, inflation 0}` → analyst-phase **`fail` 0.9683 / 61 runs → `held/fc1f068c3a8eeb63`** with J33's first sidecar. Nothing to the vault. Held: 8. The same morning the FIRST held bundle reached the vault by the repair road (A41: Cybernetics `--reaudit` → bless → supersede `50896d1`). C0's third clause (held/ dispositions) now has a worked path and eight patients; the disposition of DDIA is Rab's (S117 §8 F2: re-run / tighter lever / Bench + reaudit).⟩ ⟨**THIRD BREATH 2026-09-09 (S118)**, Rab's "B": DDIA re-dropped under the 0.80 lever with J41 collecting — Marker skipped (A1 kept its slices), analyst 2 h 14 min, 472/20/0 (`survival 12`, `inflation 0`), analyst-phase **fail 0.9718 / 49** (was 0.9683 / 61) → held again, in place. The residual loss is sized (S118 §8): ≈half the audit's own blindness (SYM-076 + ligatures, J44, +0.009), ≈half content the model dropped or rewrote inside passing chunks (J45; program / model / D1). Held: 8.⟩ · `Observed 2026-09-15 (S157 E40)` — the held count in this row (five) was superseded at S142 E1 by nine, each triaged (A38 struck this sweep); C0 has breathed (J14); the first-book recommendation (Ashby) was taken at S109 and re-converted at S144 E5 (`held-books/reconvert-four`). What remains of this row is its history |
| ~~J11~~ **STRUCK 2026-08-27 — CORRECTED** | **A fabricated duration in `docs/46`'s opening line**: "spent a year" where the measured span is **60 days** (first commit `0a16117` 2026-06-25 → 2026-08-24). Inferred from artifact VOLUME — 109 sessions, 46 docs, 54 symptom rows — and rendered as elapsed TIME. Wrong by 6×, inside the document whose thesis is that every defect here is a category error; **session count ≡ elapsed time** is one. Corrected in place. **Found only because Rab asked "where are you getting the year timeline?"** — no probe in the session would have caught a number nothing was required to measure | RECORD (closed) | `docs/46:15`, `Observed 2026-08-24` · `Observed 2026-08-27` — `grep -c 'spent a year' docs/46-the-ontology-layer.md` → **0**; the text now reads `60 days` |
| ~~J12~~ **STRUCK 2026-09-15 (S157 E13) — the four clauses EXERCISED for reader fleets** | **`docs/47`'s four unexercised clauses.** The subagent orchestration law is SIGNED and only its GROUND clause has a tripwire (3/3, `wf_72b1dfce-055`). `NEGATIVE CONTROL`, `DECLARE YOUR RESIDUE`, `HONEST NULLS` and the verifier anti-correlation rule are `Intended` — signed, untested. By this repo's own rule each is a proxy with a birth certificate until it has a case that VIOLATES its property | MECHANICAL | `docs/47` §7, `Observed 2026-08-25` · `Observed 2026-09-15 (S157 E13)` — private `agent-scripts/auditing/fleet_law_check.py` (+ brief; smoke 72/72): BRIEF (the five clauses named) · PLANT (each lane's planted control in its defects — NEGATIVE CONTROL seen to fire) · RESIDUE (declared, non-empty) · NULLS (exactly the packet's ids — no padding, truncation, invention — HONEST NULLS) · TAGS · LENS (identical verdict vectors over a shared packet = one lens); `fleet_law_check_selftest.py` violates each on purpose and watches exactly its check go red — 11/11; the first live run over S157 E5's five skeptics: 25 checks, 0 red. Residue: the law's §7 sentence ("Intended — signed, unexercised") is a signed text — his to amend; the check binds reader/skeptic fleets with a packet shape, a builder fleet's residue (files, processes) is the process census's (E9) |
| J13 | **The phase-transition relay: TESTED, NOT ADOPTED** (Rab: "Don't implement that, just test it"). Works live and bidirectionally from both a background agent and a workflow lane. Six constraints recorded in `docs/47` §8. **Two are decisions, not facts:** (a) `SendMessage` is DEFERRED and a lane that does not `ToolSearch select:SendMessage` first reports a FALSE NEGATIVE — "no relay channel exists"; (b) `to:"main"` is documented "background subagents only" yet workflow lanes accept it, so the restriction is unenforced and **could start being enforced**. **UNTESTED: N concurrent lanes** — both probes were single-lane; ordering and interleaving are `Unknown`, and the mutation run had five | SEMANTIC (adoption is Rab's) | `docs/47` §8, `Observed 2026-08-25` |
| J14 | **C0 BREATHED — and the book failed its convert-phase audit.** `events.jsonl` moved off 137 for the first time since 2026-08-14. Ashby, *An Introduction to Cybernetics*, 156 PDF pages = **295 printed pages (2-up landscape, 842×595 pt)**. Convert: 1088.7 s wall, **6.98 s/PDF-page vs 4.719 promised**, peak VRAM 9427 MiB — and **contaminated**: a 24-agent fleet ran across its first half (SYM-035's hazard), so treat it as an UPPER BOUND, not a measurement. Audit: `doc_survival 0.8582`, **`degeneration: True`, verdict `fail`**. Then, in the analyst lane, chunks generating ~15 min against a 13.9 s/chunk mean (**60x**) at 93 % GPU / 286 W. **First attributed to SYM-003 (table-loop disease); that attribution is WITHDRAWN** -- one of the two measured specimens contains ZERO pipe characters. Root cause found and filed as **SYM-056**: the converter emits **40 unterminated `array` environments** in this book (79 begin / 39 end, 28 of 112 chunks), with 36-character runaway column specs. All 28 carry `status: passed` -- the audit scores content, not structural validity. **It is in the SHIPPED corpus too**: the anchored Investment Valuation bundle reads delta 3 | MECHANICAL / measurement debt | `Observed 2026-08-25`, events.jsonl 140-143 **CROSS-VENDOR CORRECTION 2026-08-27 (Codex lane, `MSG-CDX-0014`, independently reproduced from primary bytes):** the figure **60** is the **strict-literal** delta only (`\begin{array}` = 126 minus `\end{array}` = 66) and must be labelled as such wherever it appears. The **semantic** count is **61 unmatched opens** — a whitespace-aware byte-order stack finds 127 opens, 66 closes, **zero stray closes**. The literal rule misses one opener written as `\begin` + **CRLF** + `{array}` while still counting its closer, so it under-reports by one. **"60 unterminated arrays" as a semantic claim is FALSIFIED; the honest number is 61, which is worse by one.** Controls run by Codex: balanced canonical and CRLF-split fixtures each return zero unmatched; a planted missing close returns one; removing one real close moves 61→62 and appending one moves 61→60; a second independent implementation reproduced the hashes, the counts, the anomalous token at body line 4,183 / raw line 4,200, and all five controls. ⚠ **The causal link to the timed-out analyst chunks 103/106/115 is `UNREAD`, not established** — those chunks are absent from the journal by design. ⚠ Codex also asks that `held`, `anchor`, `delivered`, `shipped` and `vaulted` not be used interchangeably as phase claims without separate evidence. **Its standing note on the guard: the guard stays cost-justified, but its test must include whitespace-separated TeX commands or it inherits the same blind spot.** Body denominator: 691,965 bytes / 5,926 lines, sha256 `5880bed650…`. *(Filed by the Fable lane on Codex's evidence, per the 2026-08-27 split: Codex verifies, Fable edits its own registers. Same defect family as `ERROR-BIN.md` ERR-004 — a probe whose assumption about token adjacency was narrower than the data, on a CRLF file.)* · `Observed 2026-09-15 (S157 E36/E38)` — the prose probe on the held Ashby (`26bd434d`, convert survival 0.8582, analyst 0.9452): 231 of 8,182 prose lines missing, 27 pages with ≥ 3 (p.56's paragraph *The system that is specified only by states…* absent by `grep -F`; p.51's list re-lettered f/g where the page prints g/h — an OCR difference, not a loss; p.4 the contents page): the loss is MARKER's on 1956 print, the opposite shape from the University Edition's analyst-stage sections. The `array` runaway (SYM-056) is a second, structural class the probe does not see · `Observed 2026-09-15 (S157 E50)` — THE GUARD EXISTS: `fidelity_audit.latex_balance` (whitespace-aware `\\(begin|end)\s*\{`, LIFO, every key always present; report-only — it feeds no verdict) in the convert-stage audit block since S109's class was filed, with `latex_balance_selftest.py` (the 60-vs-61 CRLF decoy is its L2 case). MEASURED on the anchor shelf with the audit's own function (private `e50_j14_probe.py`): 143 unterminated environments of 891 begins across 25 bundles, in 6 bundles — Ashby 61 / 65 (Codex's 61 reproduced to the digit on the first copy; first unmatched opener at line 232), the Damodaran 4e 3 / 3, the UE 6 / 5, every other bundle 0; the 7 manifests audited since the guard carry its block and all 7 agree with the fresh reading; 25 − 7 predate it (ABSENT, not zero). Still open: a verdict or lever on the count (his — report-only today); the causal link to the analyst's timed-out chunks stays UNREAD · `Observed 2026-09-16 (S168 E3)` — the measurement debt paid on the shelf: SYM-056's cell carries the census (9 of 33 bundles INVALID; Ashby +61; the causal claim 2 of 3 under the run's own 182-chunk split) and the validator (`windows-converter/latex_structure.py`, 12 tripwires; wiring his restart window). |
| ~~J15~~ | ~~STRUCK 2026-09-03T21:xxZ~~ **FIXED.** `Observed 2026-09-03`: `.claude/skills/muster/close.sh:384` and `:385` BOTH now read ``sym_open=$(grep -coU '| \(`open`\|\*\*OPEN\)' "$SYMS_MD" ...)`` and the matching `sym_open_pin=$(... grep -coU '| \(`open`\|\*\*OPEN\)' ...)`` — the two-spelling pattern, applied to BOTH the current count and the pin count. The row's premise — that `close.sh:330` counts with a single literal — no longer holds; that logic now lives at `[8b]`, lines 384-385. `.claude/skills/muster/selftest.sh` carries a dedicated case for this fix: `ok   J15: both \`open\` and **OPEN** counted, and a prose mention is NOT (3 of 4)` and `ok   …and it reads neither 1 (backticks only) nor 4 (prose counted)` — `Observed 2026-09-03` re-running `bash .claude/skills/muster/selftest.sh`: `ALL TRIPWIRES FIRED — 54/54`, exit 0. -- **THE DEBT GATE UNDER-COUNTS BY FORMATTING.** `close.sh:330` counts open symptoms with ``grep -oU '| `open`'`` -- a single literal rendering. `SYMPTOM-INDEX.md` uses at least three (`` `open` ``, `**OPEN**`, `**OPEN.**`), so **this session's card read `open SYM 3 -> 4 (+1)` while the true figure was +3** (SYM-054 counted; SYM-055 and SYM-056 invisible). **Measured corpus-wide at filing: `` `open` `` appears 4x, `**OPEN**` 12x, `**OPEN.**` 1x -- the gate sees 4 of ~17 markers.** The gate convicted this session for adding debt AND under-reported that debt by two-thirds, in the same line. SYM-039's family, in a gate one day old. **Do not fix by reformatting the rows to suit the grep** -- that adapts the evidence to the instrument. Fix the pattern, then re-run against the pin to confirm the number moves. | MECHANICAL | `close.sh:330`, `Verified 2026-08-25` (pattern read + row formats counted) |
| J16 | **`widget:age_s` is unrendered and should not be.** `line.rs:106` computes, per PDF queued in `drop/`, the seconds since its mtime -- and no widget renderer reads it (`main.js`, `room.js`, `index.html`: zero references; the only other `age_s` in the tree is an unrelated key in `prototypes/relay-room/`). **It is neither INTERNAL nor DEAD**: *'this PDF has sat in drop/ for three hours'* is SYM-024's scenario exactly, and is the readout that would have shortened tonight's diagnosis while a book sat in `drop/` and `events.jsonl` did not move. Dispositioned **GLASS** in `observability/dispositions.json` with the debt named [**CORRECTION 2026-08-31 (NUM-7 audit): that entry is NOT in dispositions.json — either reverted or never landed; the age_s disposition now lives at census:N-099**] rather than classed INTERNAL to quiet the tool. Wiring site: a renderer need only display a value already present in the payload; it cannot affect the running adopted binary (4DCB73E2) until a rebuild. **A GLASS entry that outlives the session after next is itself a defect** -- this one has a name, a site and this row, so it can be collected ⟨correction 2026-09-05 (S115, the Atlas fleet's O5 lane, `research/S115-fleet/O5/MEASUREMENTS.md`): this row's citation is wrong at HEAD — there is no `age_s` at `line.rs:106` and no bare `age_s` key in the file (hits at 187, 234, 318, 341, 346, 700); the drop rows carry `wait_s` + `mtime_ns` (`line.rs:148`) and `room.js:173/:176` render them. The remaining gap is narrower: the Dock shows no age (`main.js:429`) and the Room's age is null exactly when the intake receipt is stale — the stalled-watcher case. Re-scope before building.⟩ | MECHANICAL (small; adoption is still Rab's hand) | `observability/dispositions.json`, `Verified 2026-08-25` (glass RED -> exit 0 after the disposition) · `Observed 2026-09-15 (S157 E53)` — BUILT at the re-scoped site: the Dock's drop readout carries the OLDEST wait (`main.js` lineLoop: `wait_s` when the intake receipt is fresh, else the age from the row's `mtime_ns` — `line.rs:148` ships it always; a queue with neither reads `wait UNREAD`), with `pfAge` (hours past ninety minutes — three hours is `3.0h`, not `180m`); the Room's queue row falls back the same way (`sat 3.0h` when `wait_s` is null — the stalled-watcher case the S115 correction named). Tripwired in `projection_selftest.py` (J16 cases + a negative control). Source only; adoption his |
| ~~J17~~ | ~~STRUCK 2026-09-03T21:xxZ~~ **FIXED.** `.claude/skills/muster/muster.sh` now applies the S109 DESCENDANT rule at its `is-ancestor` calls (`Observed 2026-09-03`: `grep -c is-ancestor .claude/skills/muster/muster.sh` = 5, spanning the reconcile, fork and out-of-repo branches at `muster.sh:267,274,286`). `.claude/skills/muster/selftest.sh:406-430` (CASE 30/31) asserts it both directions — `ok   S109 DESCENDANT: a TIME-STATE AHEAD of its ledger row reconciles (exit 0)` and `ok   S109 NEGATIVE: a genuine fork (ledger NOT an ancestor) STILL exits 1` — `Observed 2026-09-03` re-running `bash .claude/skills/muster/selftest.sh`: `ALL TRIPWIRES FIRED — 54/54`, exit 0. Today's own ledger diagnostic (`sessions/S114-ledger-diag-codex.md`; `coordination/ack-fable.json` doing-line) parsed **96/96 Desktop rows S16..S113 with 0 order faults, 0 unresolvable SHAs, 0 non-ancestors** (plus 4 other-lane, 21 pre-S16, all reachable) — the exact failure this row named (a close's ledger row invisible to the muster) is now under test on the live ledger and passing. -- **A CLOSE WROTE A LEDGER ROW THAT THE MUSTER COULD NOT SEE.** The parser takes **SHA = the last non-empty cell**, matching `^[0-9a-fA-F]{7,40}$` ON ITS OWN (`muster.sh:122-129`). A row carrying its SHA *inside* the prose cell parses as `no-sha`, is discarded, and **the muster silently selects the PREVIOUS session** -- which is exactly the failure `muster.sh:99-105` describes in its own comment: *"a bad close wearing a rewind's clothing."* It happened at THIS close: the row carried its SHA in prose, and the card came back `TIME-STATE S109 vs ledger S108 -- INCIDENT`. Correct shape is **5 content cells**: date | machine | prose | files-touched | SHA. **Nothing documents this.** `docs/21` and the muster SKILL.md say *"ledger row (<=80 words)"* and never mention the cell structure, so it is learnable only by reading the awk or by breaking a close. **Fix: state the shape in SKILL.md's close section, and have `close.sh` structurally validate the newest row BEFORE the push.** | MECHANICAL | `muster.sh:122-129`, `Observed 2026-08-25` (broke it, then reconciled) |
| ~~J18~~ | ~~STRUCK 2026-09-03T21:xxZ~~ **ANSWERED — IT FIRES.** `Observed 2026-09-03`: this ticket's own agent ran `bash .claude/skills/muster/selftest.sh` directly (not a relayed claim) and watched it print `ok   a malformed NEWEST row fires [3b], naming the row` (`.claude/skills/muster/selftest.sh:99`, `assert "a malformed NEWEST row fires [3b], naming the row" 1 'LEDGER PARSE.*✗.*UNPARSEABLE' ...`), inside `ALL TRIPWIRES FIRED — 54/54`, exit 0. The guard this row asked about — whether `[3b]`'s tail alarm fires on a malformed newest row — has now been watched fire, by name, per this row's own remedy ("resolve by reconstructing the malformed row ... and running muster.sh against it"). Do not re-open on a future UNREAD claim without re-running the suite. -- **UNREAD: did `[3b]`'s tail alarm fire on that malformed row?** It exists for exactly this case (`TAIL_ROWS=5`; the row was last of 117, so 117 > 112 should have tripped it). But I grepped `[3b]` OUT of my own output while reading the incident card, so **I never saw whether it fired** -- and a guard nobody has watched fire is a proxy with a reputation (`docs/32` §5). Resolve by reconstructing the malformed row in a scratch copy and running `muster.sh` against it. **Do NOT record this guard as working until someone has watched it.** | MECHANICAL (verification) | `Observed 2026-08-25` |

### §J continued — OPENED IN S113 (2026-08-31), at the Fable-lane close

*J-series continued rather than a new letter so `close.sh` [8]'s `[A-FJ]` id census can see
these additions; a K-row would read as ADDED 0. All `Observed 2026-08-31`.*

| # | Item | Class | Source |
|---|---|---|---|
| ~~J19~~ | ~~STRUCK 2026-09-02T05:5xZ~~ **COLLECTED AND REPORTED** (S114, 2026-09-01 ~05:15Z): landed 04:56:32Z, HELD `14c66834bdfeaa2e`, both audits FAIL (convert 0.9334 on the degeneration tripwire; analyst 0.9402 on two gates). First-ever goodput collected FROM THE MANIFEST, not from `analyst/done` -- that event never fires on the inline path (F3/F4): `chunks_generated 316 · chunks_resumed 641 · passed 928 · rejected 29 · failed 0 · duration_s 4634.4 · goodput_accepted_tok_s 59.37`; 641+316=957, 928+29+0=957. Report delivered to Rab (what he got / did it work / gained / lost / regressions named). **ONE CLAUSE NOT DISCHARGED, carried here by name:** the Gmail draft per the standing pipeline-run protocol -- Rab was home and reading the report live; Fable asked whether the draft was still wanted and got no answer. Waiver or go is his word. -- **The maiden-voyage final report is OWED.** The re-dropped Damodaran 4e was IN FLIGHT at the Fable-lane close: convert CLEAN (3,834.2 s · 2.78 s/pp · retry 0 · peak 9,395 MiB · zero stalls), convert-phase audit fail 0.9334 with the first honest denominator (**runs 25 of runs_total 531** — SYM-066 dead in production), analyst at 166/957 chunks · 16.5 s/chunk · eta ≈ 03:05Z. Next session collects: the analyst-phase audit + verdict + held/supersede outcome, the FIRST `goodput_accepted_tok_s` + `chunks_generated` records, then delivers Rab's commissioned report — "what I get, if it works, what I gained and lost", regressions named against commits — plus the Gmail draft per the standing pipeline-run protocol. The watcher is detached and survives the close; its 8 h outer cap bites 05:30Z | MECHANICAL (collection + report) | `~/ml/library/events.jsonl` + `.analyst-progress.json`, `Observed 2026-08-31` · commission verbatim in `sessions/S113-fable-lane-2026-08-31.md` §1.7 |
| ~~J20~~ | ~~STRUCK 2026-09-02T05:44Z~~ **ADOPTED.** Rab: *"Adopted, hash matches E7C5A47A"*; Fable re-measured the installed path with Get-FileHash: `E7C5A47A`, 10,333,696 bytes. The glass now carries M6-R1 + `12f0ca9`. Residue carried forward, not struck: J24's three `convert/blocks*` events are still absent from `event-vocab.js` (unsigned widget change), and the S94 guard exercise remains `Unknown`. History of this row, including the false 'does not exist' note and its retraction (ERR-048), is preserved below. — **Staged exe `1CF604CD` UNADOPTED** — the live accumulator rows (slices/pages/stalls/retry-spend this run) + the 40-event tail (commit `2296cc0`). Adoption was HELD mid-run because a widget close kills the watcher tree (S37 Job Object); with the pipeline idle it is one Copy-Item + Get-FileHash away. The adopted glass is `4C6B073B` ⚠ **RE-MEASURED 2026-09-02T05:3xZ (S114): THE STAGED ARTIFACT DOES NOT EXIST AND ITS SOURCE IS STALE.** `Observed`: `windows-widget/target/release/file-portal-widget.exe` is absent — there is nothing to Copy-Item; the installed glass is still `4C6B073B` (2026-08-31 17:17). `Verified`: two commits touch `windows-widget/` after the staged build's commit `2296cc0` — `57a5da6` (M6-R1) and `12f0ca9` (Codex's M6 retained-evidence fix) — so `1CF604CD` would ship without either even if it existed. And `event-vocab.js` has **0** references to `blocks`: J24's three new events (`convert/blocks`, `blocks_partial`, `blocks_error`) are unknown to the Room. **The window is open (widget + watcher are DOWN on 09-02, no Job Object to kill) but the ticket as written is dead.** Replacement: a fresh rebuild on current HEAD — `cargo fmt --check` LEADS (docs/19:68, SYM-020), then clippy `-D warnings`, test, build — that carries M6-R1 + `12f0ca9` and, if signed, J24's vocabulary; print its SHA-8; adoption stays Rab's hand (docs/19 §0.3). ** RETRACTION 2026-09-02T05:4xZ (S114), same session, ~10 minutes later: the sentence above beginning 'THE STAGED ARTIFACT DOES NOT EXIST' is FALSE.** `Observed`: `windows-widget/src-tauri/target/release/file-portal-widget.exe` was present, mtime 2026-08-31 18:11, 10,332,160 bytes, **sha-8 `1CF604CD`** -- the rebuild's own step [0] found it. The earlier `ls` checked `windows-widget/target/release/` (the wrong directory) and the whole-tree `find` from `/c/Users/Bndit` silently truncated before reaching `Projects/` (reproduced: the same predicate scoped to `windows-widget/` lists it; from the user root it does not; stderr was suppressed). Two failed probes were promoted to a negative that reached Rab and this register -- ERR-2026-09-02-048. **What still stands:** the artifact predates `57a5da6` and `12f0ca9`, so adopting it would omit M6-R1 and Codex's fix; a rebuild on HEAD is the right adoption target regardless. **What was lost:** the fmt-led rebuild (`fmt` clean, clippy clean, `node --check` OK, `cargo test` 36/36) was already running and overwrote this path; a preservation copy was attempted and came too late (mtime 09-02 01:39:59 local). `1CF604CD` was destroyed on a false premise. Recoverable only by `git checkout 2296cc0` + rebuild -- Rab's call, and not the artifact he should adopt anyway. **New staged build: `E7C5A47A`** (10,333,696 bytes, HEAD `5b52fab`, GREEN through all six gates) -- ready for Rab's hand. | SEMANTIC (adoption is Rab's hand — docs/19 §0.3) | `sessions/S113-fable-lane-2026-08-31.md` §2f, `Observed 2026-08-31` |
| ~~J21~~ | ~~STRUCK 2026-09-02T05:5xZ~~ **DONE.** Review posted `MSG-FAB-0065` (PASS + one MAJOR finding, M6-R1-F1: display cap applied to the manifest verdict). Codex reproduced it, found a SECOND boundary loss (`Bench.state()` sliced retained runs to 40 before the UI), fixed both in `12f0ca9` (`MSG-CDX-0043`). Fable re-ran the 60/531 positive and 101/100 negative controls independently on HEAD: PASS, producer-cap wording fixed, `display_cap=BENCH_RUN_DISPLAY_CAP` gone. Verdict posted `MSG-FAB-0066`. AUD-1 / M6-R2 remains blocked-on-rab (not widened). -- **M6-R1 independent review OWED to Codex.** `MSG-CDX-0042` delivers commit `57a5da6` (+ closeout `e4cb9fc`) for PASS-or-findings with five named attention points: mixed partial+UNREAD counts · under-cap legacy exactness · all-visible-addressed-while-unseen-remain · A1 remedy text · A2 widget/Bench grammar parity · no M6-R2 boundary crossing. Review only — no widening, no implementation | MECHANICAL (peer review; docs/47 law applies) | `coordination/relay.md` MSG-CDX-0042, `Observed 2026-08-31` |
| ~~J22~~ | ~~STRUCK 2026-09-03T21:xxZ~~ **BUILT, RESIDUE NAMED.** `Observed 2026-09-03`: `prototypes/mission-control/` holds `panel-a-opsroom.html`, `panel-b-telemetry.html`, `panel-c-small-multiples.html`, `voyage.js`, `voyage-manifest.json`, `voyage-events.jsonl`, `CONTRACT.md` and an adversarial `CRITIQUE.md`, all landed in commit `a02b02c` ("J22: three mission-control panel mocks + contract + adversarial critique" — `git log --oneline -1 -- prototypes/mission-control/` confirms). `CRITIQUE.md:316` recommends "graduate Panel C, with transplants," naming one severe defect **C1** (`:322`, three fix-first edits at Panel C's `:2768`, `:2814`, `:2910`) and a second finding **C3** (`:375`) among its residue. **RESIDUE, carried by name: graduation of Panel C pending Rab; C1/C3 edits pending.** No widget change was made — the `prototypes/` quarantine boundary held (no signature crossed it). -- **The UI research commission** (Rab at close: "start the research tasks"). Study the sources ranked in the 2026-08-31 conversation — Project Cybersyn opsroom design · NASA Open MCT (inspectable code) · Grafana dashboard best practices · Tufte sparklines/small multiples · mission-control UX pattern surveys — then build 2–3 panel-layout mocks in `prototypes/` against THIS run's real events.jsonl for Rab to pick from; the winner becomes a signed ticket. Boundary: `prototypes/` quarantine only; no widget change without signature | MECHANICAL (research) / SEMANTIC (any graduation) | Rab 2026-08-31, `sessions/S113-fable-lane-2026-08-31.md` §5.5 |
| J23 | **13 contested census dispositions await Rab's signature**: N-001 N-003 N-004 N-012 N-040 N-048 N-096 N-132 N-134 N-142 N-208 N-239 N-308 — the census rows where the NUM-7 audit and the existing disposition disagree. Sign or amend; the census_dispositions section holds them apart from the close gate | SEMANTIC | `observability/dispositions.json` + `docs/51-numeration-census.md` §11, `Observed 2026-08-31` |
| ~~J24~~ | ~~STRUCK 2026-09-02T06:0xZ~~ **SIGNED (Rab 2026-09-01: "Yes signed") AND BUILT** `850bdf3` — `marker_blocks.py` (413 lines) + `marker_blocks_selftest.py` 57/57 watched failing both ways; pre-existing converter suite 85/85; markdown byte-identical two ways; overhead 0.48 %; resume-safe; degrades to today. Upstream bug found: `FlatBlockOutput.page` is wrong in marker 1.10.2 (true page = id segment [2], absolute across slices). Its one unsigned glass key `converter:blocks_engine` dispositioned EVIDENCE `7a33c55`. **Residue carried, not struck:** (1) the supersede export path never copies `blocks.json` — **J28**; (2) three events `convert/blocks*` unknown to `event-vocab.js` (unsigned widget change); (3) `blocks.json` size on a real book UNREAD. Verified by the S114 fleet's ninth agent. — **THE CONVERTER DISCARDS EVERY BOUNDING BOX IT COMPUTES.** `convert_and_ship.py:849-851` calls `marker_single --output_format markdown`. `Verified 2026-09-01` on the installed marker-pdf **1.10.2** by importing its own renderers: `JSONBlockOutput` carries `id · block_type · html · polygon · bbox · children · section_hierarchy · images`, and `FlatBlockOutput` (the `chunks` renderer) carries the same **plus `page`**. Every polygon, bbox, page index, block type and section hierarchy is computed by Marker and thrown away on every page ever converted. **This is the missing instrument behind three open items**: **SYM-053** (a page 'looks covered' because an asset exists, but the asset is blank paper and the diagram's words are gone - a bbox lets the source region be rendered and compared); the **analyst audit's unfillable highlight** (this session's held manifest carries `run_page: null` on all 25 analyst runs - locatable only by excerpt text - while the error-structure protocol requires reason **+ highlight** + solution); and **figure_coverage.py**, which infers what a polygon would measure. **Scope:** emit a second renderer pass (or switch to `chunks` and render markdown from it), persist the block records beside the bundle, and anchor omission runs and degeneration zones to `page` + `bbox`. **Not in scope:** `--use_llm` (that is J26 and it costs the link fence), and any new model. No new dependency - the renderers are already installed | SEMANTIC (converter change - Rab's signature) | Rab 2026-09-01 ("whats the ticket being commissioned"), `sessions/S114-desktop-2026-08-31.md`; verified against `marker.renderers.json` / `marker.renderers.chunk` in `~/ml/marker-env` |
| J25 | **THE CONVERTER BAKE-OFF - Marker vs Chandra on the same corpus** (Rab 2026-09-01: *"lets get that too wtf. We can test different converters"*). **Chandra OCR 2** (`datalab-to/chandra-ocr-2`, HF) is Datalab's full-page document VLM: **5B params, BF16, safetensors, built on Qwen 3.5**, scoring **85.8 % on olmOCR-bench vs Marker v1.10.0's 76.5 %** (+9.3 pts, Datalab's own published figures - unverified by us). ⚠ **IT DOES NOT FIT AT FULL PRECISION**: 5B @ BF16 is ~10 GB of weights against **8,323 MiB free** on the RTX 3080 (`Observed 2026-09-01T05:2xZ`). Local paths, in order of trust: (a) official safetensors + local 4-bit/8-bit quantisation (official publisher, no third-party trust); (b) a community GGUF - **supply-chain caution: the ollama repost is not Datalab's, and the web results around it include at least one spam domain**; (c) CPU (unusable at 1,377 pp). ⚠ **LICENCE IS TIGHTER THAN MARKER'S**: modified OpenRAIL-M, free for personal use and startups under **$2M** (Marker's weights are $5M), plus an explicit *"cannot be used competitively with our API"*. Code is Apache 2.0. **Bake-off design must reuse what already exists**: the held/anchor corpus is the reference set, `fidelity_audit.py` is the scorer, and the comparison is only honest if both converters are scored by the same audit on the same books with the same denominators (docs/34). ⚠ Chandra's card does **not** state that it emits bboxes or per-block confidence - if it does not, J24's instrument comes from Marker regardless of who wins on accuracy ⚠ **REFINED 2026-09-04 by the docs/53 lead hunt (coordinator-verified):** Chandra-OCR-2 is **5.3B params** (`du -b model.safetensors` = 10,591,220,088 B ÷ 2 B/param; the HF API says 5.3B BF16) — not the 5B above and not lane D's 9B (that was Chandra v1); its `config.json` says `model_type qwen3_5` (hybrid Mamba/SSM + attention, `mamba_ssm_dtype float32`, `transformers_version 5.2.0`). The INT4 path the coordinator names is `C:/Users/Bndit/ml/docling-env` (`Verified` 2026-09-04 by Fable: transformers 5.14.1 with `models/qwen3_5` present, torch 2.11.0+cu128); a third-party GGUF (mradermacher, Q4_K_M 3,066,385,440 B + mmproj 366,894,368 B ≈ 3.43 GB) exists but whether llama.cpp runs the hybrid arch is Unknown. **Roster after the hunt** (rank / verdict): #2 Qwen3-VL-4B-Instruct-GGUF (official Qwen quant, Apache-2.0, Q4_K_M 2,497,281,664 B + mmproj Q8_0 453,974,304 B ≈ 2.95 GB — the cheapest real reconstructor test; the download is Rab's), #3 Chandra INT4, #4 nougat-base (MIT code, CC-BY-NC-4.0 weights, dormant 559 days; test it on ASHBY, the book with 61 unterminated arrays, not Damodaran's 3). **And Marker itself is a candidate:** upstream is at **2.0.0** (2026-07-20; installed 1.10.2, surya 0.17.1) — stage it as a separate `marker2-env` (marker 2.0.0 + surya 0.22.1 + transformers ≥ 5.12.1, the same transformers line Chandra needs; never upgrade `marker-env` in place). Protocol for every candidate: the held Damodaran's page 805 (130 `| |` empty-cell markers), scored by `fidelity_audit.py --pdf/--md` + `latex_balance` against Marker's page, negative control the filler md (0.0 measured). Licences at source: installed marker/surya are **GPL-3.0-or-later** (weights OpenRAIL-M, $2M) — the GROUND's 'Apache-2.0' was WRONG; upstream master reads Apache-2.0 / $5M per the coordinator's fetch (not hand-parsed). | SEMANTIC (new model + a converter swap - Rab's signature; the download itself is signed) | Rab 2026-09-01, `huggingface.co/datalab-to/chandra-ocr-2` + `github.com/datalab-to/chandra` |
| J28 | **BUILT `7a68be5` (OUT by Rab's decision) — DEPLOYED 2026-09-04T02:11Z, `Observed`.** First attempt at 02:1xZ found the ThinkPad offline (tailscale: last seen 2 h); Rab turned it on ("just turned it on"); over `tailscale ssh rab@archlinux`: `/home/rab/file-portal-src` pulled `3465ac2 → 1a91918` (fast-forward, dirty 0 — the first deploy since 2026-08-16), `SHIP_BLOCKS_TO_VAULT = False` present, `systemctl --user restart file-portal-converter` → active, `converter.main` PID 2117 watching `pipeline/` and `library/staging` → vault. **Still `Intended`, not exercised:** the manifest field first fires on the next supersede of a J24-era bundle. Row stays OPEN until that observation. — **THE SUPERSEDE EXPORT PATH NEVER CARRIES `blocks.json`** (J24's FIND-2, confirmed in source by the S114 verifier). `linux-converter/converter/exporter.py:450-453` copies `.md` + `assets/` + `manifest.json` on supersede and nothing else; `test_exporter.py` has **0** occurrences of `blocks`. Consequence: the first bundle to walk that path with J24 in production ships to the vault WITHOUT its block records, and the Damodaran re-convert is that bundle. Unexercised so far — no J24-era bundle has been superseded yet. Fix is small (one more copy + a test that plants a `blocks.json` and asserts it lands); the exporter is the ThinkPad's code and the vault's contract, so it is Rab's signature. Also the moment to decide whether `blocks.json` belongs in the vault git repo at all (size UNREAD on a real book; ~10 MB per 1,377 pp is Inferred) | SEMANTIC (exporter + vault contract — Rab's signature) | S114 fleet, `VERIFIED.md` §8 FIND-2; `sessions/S114-desktop-2026-08-31.md` |
| ~~J29~~ **STRUCK 2026-09-15 (S157 E4) — DONE** | **THE TABLE-AWARE DEGENERATION GATE (SYM-067) — the docs/53 lead hunt's build-first, rank #1 of the roster.** Strip markdown pipe-table rows before `degeneration()` runs in `windows-converter/fidelity_audit.py`. Acceptance, already measured on the pair on disk (`docs/53-lead-hunt/scripts/verify_sym067_pair.py`, 2026-09-04, the repo's own function): anchored Damodaran 2025 4e `flagged=true` / 26 blocks → `false` / 0; held University 4e `14c66834bdfeaa2e` 25 → **exactly 1** (raw line 8776, the 441× `{1 - t}` array runaway that is SYM-056's unterminated `\begin{array}` — it must STAY flagged); the Beer Brain-of-the-Firm anchor (max_trigram 2,267) must still trip; Diagnosing / Valentine / Ashby anchors keep their real hits. CPU-only, scored by the audit itself, on the verdict that is holding the maiden voyage. It changes verdicts, so it is a gate change ⟨status 2026-09-04: DONE — 2026-09-04 verifier (HEAD bde007d): DONE — BUILT `8aa8936`, amended `d9bfaaa` (fleet wf_1e69e60b-b45 GO), signed Rab 2026-09-04; SYM-067 reads FIXED. Strike.⟩ | **BUILT `8aa8936` — signed Rab 2026-09-04** (*"Signed: J29, SYM-072, push 7bcf001"*). Acceptance held as filed: 2025 4e 26 → 0 (both anchor copies and held `0d68f0e02293970c`), held Univ 4e 25 → 1 at line 8776, Beer ×2,267 / ×143 still trip, Diagnosing / Ashby / Valentine keep their verdicts; 34 files swept, 3 flips, all the one class. `degeneration_selftest.py` 22/22; `latex_balance` 27/27 and `convert_and_ship` 105/105 unchanged; A4 schema check PASS; `converter:table_rows_stripped` dispositioned EVIDENCE in the same commit; `docs/15` §9.3 written. Declared residue: a loop that emits only table rows is invisible (D5) | docs/53 `VERIFIED.md` build_first; coordinator rank #1 · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — 8aa8936 and d9bfaaa ancestors of HEAD; SYMPTOM-INDEX SYM-067 reads **FIXED** `8aa8936` |
| J30 | **MARKER'S ACTUAL READING ORDER vs THE DECLARED TREE — the measurement docs/52 §2.7c thought it had.** docs/52's "2/57 WTPDF, 15/60 ISO" compared the declared structure tree with pymupdf's raw block order (the content stream), not with Marker (`Verified` 2026-09-04 in the installed 1.10.2: `marker/builders/layout.py:140` orders by surya layout position; `marker/processors/order.py:17-22` re-sorts only sliced pdftext pages). Run `marker_blocks.py` (J24) on the WTPDF and ISO specimens (GPU), map each block to its tree element, count the pages where Marker's order ≠ the declared order, and only then restate §2.7c's prize. Until then no sentence may say how far Marker diverges from a declared order | MECHANICAL (a GPU run on two public specimens; no gate change; the GPU session is Rab's to schedule) | docs/53 coordinator residue (1); the docs/52 correction |
| ~~J31~~ **STRUCK 2026-09-15 (S157 E4) — BUILT + EXERCISED** | **RE-AUDIT A REPAIRED HELD BUNDLE — PROPOSE_AMENDED by the Fable verifier (`wf_d92d7e9a-f2d`), UNSIGNED.** Nothing today re-scores a held bundle after a Repair Bench repair, rewrites its manifest, or re-ships it (`Verified`: no path in `convert_and_ship.py`; the exporter never audits, it reads `fidelity.verdict` + `bless.json` + `source_sha256`). TWO TEXTS: the manifest's `fidelity.convert` (0.9334 / 241 flagged / 531 runs) is the PRE-analyst Marker body (`:1616`); the held md is the POST-analyst text (`:1633`, `:1670`; 0.9271 / 257 / 570) — SYM-073. The bench edits the held md, so "re-audit the same text" (my draft) can never see a repair. **Mechanism (amended):** `--reaudit <held sha16>` on a staging COPY: (1) `fidelity.final` = `audit_convert(pdf, repaired held md)` + `audit_analyst(Marker body, repaired held md)` — the repaired text against BOTH references, `text_audited: held-post-analyst` (the Marker body from J33's sidecar, or the slice cache while it survives); `convert`/`analyst` stay as history; (2) `verdict = compute_verdict(final.convert, final.analyst)`; (3) `fidelity.reaudit {ts, by, reason, text_audited, repairs_digest, from}`; (4) `audit/scored` with `stage: "audit"`, `phase: "final"`, `reason: "reaudit"` (`assay.rs::bless` matches `stage=='audit' && event=='scored'` literally; refuses unless verdict flag and no degeneration); (5) `_enforce_hold()` then `ship()` like both callers — `audit-mode.txt` reads **enforce** today, a still-fail re-ship is re-parked as `<sha16>--superseded-<stamp>`; `*.bench-bak` and `repairs.jsonl` EXCLUDED from the copy (held `b6fbdd75` carries 930 KB + 59 KB of them; the exporter's create path copies everything but `blocks.json`). **Measured on Univ 4e:** deleting the line-8776 runaway (21,870 chars — Marker's own seed is 3,005 chars at its line 8795; the analyst amplified it 7.7× in chunk 296, 673 words in / 5,164 out, ACCEPTED) flips degeneration 1 → 0 and convert-alone fail → flag; doc_survival / pages_flagged / runs_total do not move (held 0.9271/257/570, Marker 0.9334/241/531, before and after). **With the analyst block historical the verdict is fail for every variant, on this book and on 4 of the other 6 held bundles** — the verifier's D-1: the rule must be the repaired text against both references, and even then Univ 4e fails its analyst stage on real deletions (J32). Full amended wording + measurements: `docs/54-repair-road/README.md` §1 ⟨status 2026-09-05: **BUILT** `d3f69e8` (lane 1, Sonnet) on Rab's "J31 D-1 yes" — `--reaudit <held dir>` computes `fidelity.final` (repaired held body vs the PDF witness AND the Marker reference: sidecar, else slice cache), `fidelity.verdict = compute_verdict(final)`, `fidelity.reaudit` provenance, `audit/scored phase=final` in `assay.rs::bless`'s shape, `audit/reaudit` + `audit/reaudit_refused` verbs; still-fail writes in place and ships nothing; flag/pass stamps the supersede intent, ships the STAGING copy (bench files excluded) and renames the held dir `--reshipped-`; `--dry-run` emits nothing. Verified GO_AMENDED by `wf_6fca68fd-b1d` (T19 1–9 + T7-shaped vocab check, 175/175); remedies e4cccda: ID validation (no `..`, no absolute paths), the sidecar is trusted only when it matches `manifest.marker_body.sha256/bytes`, a manifest with no prior verdict is refused; docs/15 §16 notes that a `flag` re-audit needs a human bless before the exporter supersedes a vaulted note (exporter.py:402). Scratch dry-run on a COPY of held Cybernetics `c5afd9edcf620fc6`: fail → **flag** unrepaired (its July degeneration was the SYM-067 false positive J29 fixed) — a real `--reaudit` on it would SHIP the book; Rab's hand. The Univ 4e road is unchanged: repaired or not, its analyst stage fails on real deletions (J32).⟩ ⟨status 2026-09-05 (S116): **EXERCISED FOR REAL** on Rab's word — `--reaudit c5afd9edcf620fc6` at 21:17:25Z: `fail → flag` (reference None: no analyst block on that manifest), supersede intent carried, SHIPPED to ThinkPad staging, held renamed `c5afd9edcf620fc6--reshipped-20260905-211727`; five events (`audit/scored` final/reaudit, `flagged`, `reaudit`, `supersede`, `ship/shipped`). ThinkPad: `EXPORT-SUPERSEDE-HELD … 'flag' is not pass (and no valid bless) -- vault untouched, staging copy kept`; receipt `supersede-held`; vault tip unchanged `70c60e6`. The book waits on Rab's bless click (Assay card → bless.json into ThinkPad staging → the next sweep supersedes with provenance) — see A41.⟩ ⟨2026-09-06 (S117): **the road is complete end to end** — bless clicked 00:46Z, exporter restarted, `EXPORTED-SUPERSEDE` commit `50896d15`, vault tip `50896d1`. Residue: the exporter's log + vault commit say "fail→pass" where the manifest says a blessed flag (J40); `bless.json` is copied into the vault beside the note.⟩ | SEMANTIC — Rab's signature (the verdict rule D-1 is the policy) | docs/54; fleet `wf_d92d7e9a-f2d` · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — d3f69e8 an ancestor; `windows-converter/convert_and_ship.py:2280` `def reaudit(bundle_id, dry_run)`, provenance `by: convert_and_ship --reaudit` (:2431); exercised for real on Rab's word (S116, the row's own status) |
| ~~J32~~ **STRUCK 2026-09-15 (S157 E4) — BUILT (lever moved on his word)** | **THE ANALYST-STAGE GATE COUNTS ESCAPES, PUNCTUATION AND SPACING AS LOSS — AND THE MODEL DELETES (AND INFLATES) PARAGRAPHS. PROPOSE_AMENDED (`wf_d92d7e9a-f2d`), UNSIGNED.** Reference pairs on disk: SIX, not one — the Univ 4e slice cache (mirror = manifest: 0.9402 = 38,018 of 40,438 twelve-word windows, exact containment; 404 runs) + five `anchor/` pairs `<name>` vs `<name> [analyst-local]` that reproduce their manifests (2025 4e 0.9525, Valentine 0.9303, Diagnosing 0.9791, claude-code 0.9493 / 0.9547) + Brain of the Firm (0.9621). **Ladder, three independent implementations (builder / lane A / verifier):** unescape 0.9564 / 0.9562 / 0.9573; + punctuation-free 0.9695 / 0.9704 / 0.9696; + space-free, cumulative 0.9847 (646 of 42,235) / 0.9838 / 0.9848 — my draft's "0.9718" was a 2-step rung. Residue ≈1.5 % of windows; over-count ≈3.7× in windows (2,420 → 646) and ≈7× in runs (404 → 54–59). **On all six books the normalisation moves NO verdict** (0.960–0.989 vs the 0.995 gate; closest Diagnosing 0.9891) — a truer number and a shorter runs list, not a gate outcome. The residue is real damage by qwen3:8b under `readability`: chunks 23 / 78 lose 3 of 5 and 2 of 4 prose paragraphs (361 / 308 words, absent from the book); chunk 296 is the opposite hole (673 words in → 5,164 out, the runaway); the ⟦IMG⟧ fence sees neither. Spread: ≈68–69 % of still-failing windows locatable; 378–412 of 957 chunks carry loss, 80 % in 222–246; front matter 3 of 765. The surviving journal is the 2026-08-30 attempt's (today's run deleted its own on success); 16 of 29 rejected chunks identifiable, 6–10 loss windows there. **Proposal A** (mechanical, analyst stage only — `audit_convert` never calls `audit_analyst`): compare after unescape + punctuation-free + space-free on both sides, record the normalisation in the block, ship the regexes with a selftest pinned to 0.9847 / 646. **Proposal B** (the guard the deletions need): a per-chunk input-window survival check at accept time in `analyst.process` — measured on the 08-30 journal's 500 validated passed chunks: 5 chunks < 0.50 carry 13 % of located loss, 32 < 0.80 carry 31 %, all three named disasters < 0.50; threshold and reject-vs-flag are Rab's. Full wording: `docs/54-repair-road/README.md` §2 ⟨status 2026-09-05: **BUILT** — A `4e73b19`, B `ad5e667` (lane 2, Sonnet) on Rab's "J32 A + B (threshold 0.50, reject)". A: `text_norm.py` (pure ladder unescape → punct-free → space-free) shared by `audit_analyst` and the guard; `normalisation{…, regex_id}` in the block; `ANALYST_DOC_FAIL` 0.995 untouched; six-book table: NO verdict moves. B: `ANALYST_CHUNK_SURVIVAL_MIN = 0.50` in `analyst.process` after the fence; `rejections{fence, survival, think_leak}` on both `analyst/done` emits (T17) + `rejections_survival` in the frontmatter; journal records carry reason + survival. Verified GO_AMENDED by `wf_6fca68fd-b1d`; remedy e4cccda (R5): `punct_free` no longer deletes the backslash `unescape` was told to keep (`\rm` was comparing as `rm`), `regex_id` j32a-v2, the Univ 4e pin re-measured and re-stamped in docs/15 §9.4 (v1: 0.9838 / 641 of 39,461 / runs 59 / fail — the first report's 639 was wrong). Calibration on the 2026-08-30 journal (coordinator + refuter + verifier, three shapes): below 0.50 = chunks 23 (0.388) and 78 (0.422) ONLY — the deletions the threshold was signed for; chunk 296 (the runaway) survives at 0.791 — the guard is DELETION-ONLY by construction; inflation → **J34** (PROPOSED).⟩ ⟨**LEVER MOVED 2026-09-09 (S118)**, Rab's "B": `ANALYST_CHUNK_SURVIVAL_MIN` 0.50 → **0.80** after DDIA 2e held at analyst-phase 0.9683 with a 264-word paragraph deleted from a chunk that scored ≈0.56 and passed. Basis: the 09-05 measurement (3 / 6 / 14 of 500 hash-matched passed records below 0.50 / 0.80 / 0.90 under `j32a-v2`); the pre-move re-measure was impossible — A1's slice sweep took the 08-30 journal's Marker reference when DDIA converted on 09-06 (S118 §4). Selftests re-run at 0.80: the J32-B/J34 fixtures all sit outside 0.50–0.80 (survival ≈1.0 or < 0.45), so no case moved. First post-move measurement = the DDIA re-run's `chunk_scores` (J41).⟩ ⟨**MEASURED 2026-09-09 (S118), the re-run:** the 0.80 lever rejected 12 chunks for survival (0.50 had rejected 3), the 264-word deletion among them; document 0.9683 → **0.9718**, runs 61 → 49; still `fail` at 0.995. The 472 passing chunks still lose 3.04 % of windows between them (36 in [0.80,0.90), 55 in [0.90,0.95)); a lever that reaches that (≈0.95) rejects a fifth of the book. **Proposal A's ladder has a blind spot (SYM-076):** `prepare_output` strips markdown `_` before `unescape` sees `\_`, so every window through a Marker-escaped identifier dies (unit probe 0.0); fix + a ligature-blind rung measured offline on this bundle = +0.0090 → **J44**. The rest is content (J45 / program / model / D1).⟩ | SEMANTIC — Rab's signature (A and/or B) | docs/54; lane A `ladder.py`, `qual_diff.py`; verifier `v_c.py` · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — `windows-converter/text_norm.py` present; `analyst.py:77` `ANALYST_CHUNK_SURVIVAL_MIN = 0.80` with the lever-waiver naming his two words (0.50 on 2026-09-05, 0.80 on 2026-09-09); 4e73b19 and ad5e667 ancestors |
| ~~J33~~ **STRUCK 2026-09-15 (S157 E4) — BUILT + LIVE** | **RETAIN THE MARKER BODY WITH THE BUNDLE — PROPOSE_AMENDED (`wf_d92d7e9a-f2d`), UNSIGNED.** Where the pre-analyst body survives today: for a book analysed INLINE (the Univ 4e voyage) nowhere but `.chunk-work/<sha16>/slice-*/slice.md`, swept when the NEXT book starts chunking (`:1277-1280`, LATEST-BOOK, A1); for a book analysed through the pending card (`--defer-analyst` → `resume()`), the anchor written at `:1988` IS the pre-analyst bundle — five such pairs exist and reproduce their manifests (J32). Proposal: write the merged, link-rewritten Marker body beside the bundle md (held / anchor / pending + the shipped tar); keep it OUT of the vault by J28's shape — a `_skip_marker_md` copytree filter (today `_skip_blocks` filters only `blocks.json`, `:153-160`), `SHIP_MARKER_MD_TO_VAULT = False`, manifest `marker_md {present_in_bundle, shipped, bytes}`. **Every exactly-one-.md guard must learn the sidecar — SIX, not the two my draft named:** exporter supersede scan `:506-511`, bench open scan `bench.py:250-263`, bench picker `:1965`, `_anchor_copies` `convert_and_ship.py:1858`, `coverage_rescore.py:103`, `acceptance.py:45` (+ `room.rs:258`, cosmetic). **Alternative that sidesteps the class — Rab's slot:** a non-.md name (`<name>.marker.txt`) or `marker/<name>.md`. Cost 3.46 MB UTF-8 for this 1,377-page book ≈ 2.5 KB/page; 0 in the vault once the lever exists — a goal, not yet a property. Unlocks SYM-073, J31's analyst reference, J32 on every future book, a bench "restore from Marker" pane (later). Full wording: `docs/54-repair-road/README.md` §3 ⟨status 2026-09-05: **BUILT** `bbcff64` (lane 1, Sonnet) on Rab's "J33 name = <name>.marker.txt, vault OUT" — `_write_marker_body_safe` writes the pre-analyst, link-rewritten Marker body beside the bundle in `convert()` (before the analyst branch; never raises; `manifest.marker_body{file, bytes, sha256}`), it rides every copy path (anchor/pending/held/tar copy the whole dir); the exporter keeps it OUT by J28's shape (`SHIP_MARKER_BODY_TO_VAULT = False`, `_skip_marker_body`, the L12 gate skips it, `marker_body{present_in_bundle, shipped}` folded); the six exactly-one-.md guards are untouched because `.marker.txt` is not `.md` (T18 + direct reads). Verified GO_AMENDED by `wf_6fca68fd-b1d`; remedy e4cccda (R3): encode-then-atomic-rename so a fault leaves no 0-byte sidecar (T18(f)). Cost measured: 3,486,636 bytes for the 1,377-page Univ 4e. Exporter tests: 4 added; on Windows 2 of them fail in the pre-existing file-lock class (base 20 + 2), Linux CI is the executed evidence. Manifest keys are invisible to the glass harvester (subscript-assigned, like J24's `blocks`) — recorded in observability/README.md, not dispositioned.⟩ ⟨status 2026-09-05 (S116): **LIVE on the ThinkPad** — pulled `f6442cf → ba7c79f` (only `exporter.py` + its tests among the 40 commits touch the receiver; no requirements change), `file-portal-converter` restarted 21:09:25Z (PID 14619, watching pipeline + staging → vault.git); `grep -c "marker.txt\|MARKER_BODY" exporter.py` = 12. First real receive under J33 was the Cybernetics reaudit at 21:17:26Z — a pre-J33 bundle with no sidecar, so the vault-OUT rule is UNEXERCISED until a post-J33 bundle ships.⟩ | MECHANICAL + the sidecar-name slot + one vault-side OUT rule (Rab's, as blocks.json was) | docs/54; lane C; verifier `v_c.py` · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — `convert_and_ship.py:799` `_write_marker_body_safe`; `linux-converter/converter/exporter.py:91` `SHIP_MARKER_BODY_TO_VAULT = False` with the J33 waiver; bbcff64 an ancestor; the pipeline's own `.marker.txt` sits beside the anchor Valentine copy (S154 E1 read it) |
| ~~J34~~ **STRUCK 2026-09-15 (S157 E16) — BUILT, TWO LIVE BOOKS — its own status said strike at the next sweep** | **INFLATION GUARD AT ACCEPT TIME — PROPOSED (solution measured), UNSIGNED.** J32-B's survival guard (0.50, signed 2026-09-05) cannot see INFLATION: on the 2026-08-30 Univ 4e journal (516 validated / 500 passed, shipped `text_norm.chunk_survival`) the runaway chunk 296 (681 words in, 5,170 out) survives at **0.791** — its input windows are all still there; only chunks 23 (0.388) and 78 (0.422), the paragraph DELETIONS, fall under 0.50 (below 0.60: 2 · 0.70: 3 · 0.80: 5 of 500). Output/input word ratio over the same 500: chunk 296 = **7.59×**, next highest 1.18× (chunk 92), then 1.14 / 1.12 / 1.11; ratio > 1.25 / 1.5 / 2 / 3 = 1 of 500 each time. Proposal: in `analyst.process`, after the fence and survival checks, reject when output words / input words > 1.5 (reason `inflation`, riding `rejections`), ship the original — 0 false rejects on this journal. On this journal chunk 296's text also carries a think tag, so SYM-074's guard rejects it today; the ratio guard catches the class independently of that accident. Threshold and reject-vs-flag are Rab's. Scripts: `scratchpad/j32b_named_chunks.py`, `j32b_ratio.py` (S115; reproduces lane 2's 2 / 5 / 12 buckets) ⟨re-measured 2026-09-05 after remedy e4cccda (R5, `j32a-v2`: `punct_free` no longer deletes the backslash `unescape` keeps): the runaway chunk 296 now scores **0.159** under the shipped `chunk_survival` and IS rejected by the signed 0.50 guard (v1 read 0.791); buckets 3 / 6 / 14 of 500 below 0.50 / 0.80 / 0.90 (v1: 2 / 5 / 12). The deletion-only property stands: 2× and 7× verbatim duplication still score 1.0 (measured), so the ratio guard's case is duplication-shaped inflation, not this instance — the 7.59× vs 1.18× ratio measurement is unchanged. Re-measure against the shipped module before signing.⟩ ⟨status 2026-09-05 (S116): **SIGNED "J34 1.5x reject" and BUILT** — `text_norm.word_ratio` (raw whitespace-split words, output/input, None on 0 input words) + `ANALYST_CHUNK_INFLATION_MAX = 1.5` (lever-waiver inline) in `analyst.process` AFTER the survival check; reject reason `inflation`, the fourth bucket of `rejections` (both `analyst/done` emits, T17 four-bucket parity 206/206), frontmatter `rejections_inflation: n` on both paths, journal records carry `ratio` on inflation rejections AND passes. Re-measured against the SHIPPED function on the 08-30 journal: 1 of 500 rejected (chunk 296, 7.5918; its survival 0.1591), second highest 1.1750, 0 None. `analyst_selftest.py` 17/17: a 2× duplicate (survival 1.0) rejected as inflation; ~1.09× passes; lever→inf negative control watched; survival fires first; the edge is strict (1.4889 pass / 1.5111 reject); journal round-trip. Not exercised on a live run yet — the next analyst run is its first breath. **Verify fleet (3 Sonnet refuters, `wf_96a508be-498`, 3/3 decoys caught, GO_AMENDED ×3) → R1:** `word_ratio` was BLIND to CJK (no inter-word spaces → `str.split()` sees one word; a doubled CJK chunk read 1.0, `Observed`) — now counts non-space CHARACTERS when `is_cjk(input)`, English path unchanged, selftest (g) keeps the old reading as its watched control (18/18). **R2 (docs/34):** the "500 passed chunks" are the 500 HASH-MATCHED passed records of 623 passed in the raw 08-30 journal; the 123 whose input hash no longer matches today's chunking are unmeasured, not measured. The 1-of-500 / 7.5918 / 1.1750 numbers reproduce unchanged after R1 (the journal is English).⟩ ⟨**FIRST LIVE RUN 2026-09-06 (S117)**, DDIA 2e, 492 chunks, local qwen3: `rejections {fence 8, survival 3, think_leak 0, inflation 0}` — zero inflation rejects, zero false rejects; frontmatter `rejections_inflation: 0` on the held note. The live ratio DISTRIBUTION is unrecoverable: the S61 journal is `rmtree`d at assembly, so only the counts survive (J41). Strike at the next sweep.⟩ ⟨**SECOND LIVE RUN 2026-09-09 (S118)**, same book under the 0.80 survival lever, this time with J41: `inflation 0` of 492 again, and now the distribution is on record — top ratios 1.150 (chunk 4), 1.051, 1.005; nothing near 1.5. Two live books, 984 chunks, zero inflation rejects, zero false rejects: the lever is not the binding constraint on this model.⟩ | SEMANTIC — Rab's signature (a gate rule) | sessions/S115-desktop-2026-09-05.md §7 (2026-09-05) · `Observed 2026-09-15 (S157 E16)` — `analyst.py:91` `ANALYST_CHUNK_INFLATION_MAX = 1.5` with the lever-waiver naming his word ("J34 1.5x reject", 2026-09-05); `rejections.inflation` on both `analyst/done` emits; the two live runs (DDIA 492 chunks ×2: inflation 0, the ratio distribution on record since J41); `analyst_selftest` J34 (a)–(c) incl. the lever monkeypatched to inf as the negative control — 44/44 today |
| J35 | **UNREAD-WITH-NULL AS A SCHEMA RULE (The Atlas Plan, path 4) — never tracked until now.** The plan (2026-08-23) called it "the smallest honest first step of the whole atlas"; re-measured 2026-09-05 (`wf_f5173810-889`, measurer + refuter, CONFIRMED): NOT built — the law lives only as per-file discipline (`windows-widget/src/main.js:493,648`, `src-tauri/src/line.rs:148`, `windows-converter/visual_witness_capture.py`), there is no shared validator or schema line, and no register row carried it. Build: one rule every status surface inherits (a failed probe renders as null + UNREAD, never 0 / absent / healthy), enforced where status records are written and pinned by a selftest that plants a healthy-looking zero for a failed probe and watches it go red | MECHANICAL (the rule is the tag law's rule 4, already signed) | The Atlas Plan §2 path 4 (artifact `0012f564…`, re-measured 2026-09-05); docs/45 §6 · `Observed 2026-09-15 (S157 E53)` — RE-SCOPED, not built: the rule's enforcement point is the WRITER (`line.rs`, Rust — the status records are born there; a JS helper would only decorate the readers), so 'one rule every status surface inherits' is a Rust validator CI can compile and `cargo test` without an adoption, plus a selftest planting a healthy zero for a failed probe. E53 added the readers' discipline for the drop rows (`wait UNREAD`, `sat …`) as per-file cases in `projection_selftest.py`; the shared validator waits for the Rust episode (with U02) — its shape is a choice worth his word: a `Reading` type or a post-write check |
| ~~J36~~ **STRUCK 2026-09-15 (S157) — BUILT S157 E12** | **ADOPTION RECEIPTS (The Atlas Plan, path 8, the receipt half) — claimed shipped in S108, never built.** The inert-approval doctrine IS adopted in prose (`coordination/SIGNATURES.md:10`, `136cf2a`, 2026-08-24); the durable one-line receipt on every adoption (sha + evidence pointer + date) is not — exe adoption lineage still lives in memory prose (`installed-exe-state`), the exact class the path named. `windows-widget/src-tauri/src/receipts.rs` is Stage C2's vault-seam receipts (S58), unrelated. S108's own §3 lists "adoption receipts" among Wave 1's builds — a dated correction sits under that paragraph since 2026-09-05. Build: an `adoptions.jsonl` (or a row on the existing receipts tail) written by the adoption ritual (docs/19 §0.3) and read by the card that re-measures the installed exe at every open | MECHANICAL (the doctrine half is signed; the receipt's home is a small design choice) | The Atlas Plan §2 path 8; sessions/S108-desktop-2026-08-23.md §3; S115 · `Observed 2026-09-15` — public `coordination/adoptions.jsonl` (append-only JSON rows: `ts, recorded, state, sha8, bytes, exe, head, by, evidence, note, measured`) seeded with the five lineage rows the memory prose carried (Historical, `measured: false`) and the current one written by the tool itself — `C4559805`, 10,335,232 bytes, measured on the installed path; private `agent-scripts/auditing/adoption_receipt.py` (+ brief; smoke 70/70) refuses a claimed hash the exe does not measure (`DEADBEEF` → REFUSED, exit 1, nothing written); `.claude/skills/muster/open.sh` renders `adoption <sha8> (<ts>, by <who>) · installed <sha8> — MATCH | *** DRIFT *** | UNREAD` under the `installed exe` row; `selftest.sh` CASE 37 (a) MATCH on a planted exe + receipt, (b) DRIFT on a differing receipt, (c) UNREAD with no file, never MATCH — 106/106. The live card at HEAD: `adoption C4559805 (2026-09-04T02:3xZ, by rab) · installed C4559805 — MATCH`. Residue: the Historical rows' exact times are the prose's (`2026-08-2x`, `14:1xZ`); the current row's build commit is null (S114's record has it; not guessed) |
| ~~J37~~ **STRUCK 2026-09-15 (S157 E14) — BUILT** | **CIRCLE-SKILL RULES (The Atlas Plan, path 11) — claimed shipped twice, the file unchanged since 2026-08-13.** `~/.claude/skills/circle/SKILL.md` (untracked; mtime 2026-08-13, ten days BEFORE the plan) carries none of the three rules — frozen-commit immutable audit records, the aborted-lane law, "HELD" retired as a verdict word (line 101 still returns `HELD — checked and clean`). Two written claims say otherwise: S108 §3's Wave-1 list ("circle rules") and `sessions/S94-GUARD-EXERCISE-FORM.md:23` ('"held" is retired per the S108 circle rules') — both carry dated corrections since 2026-09-05. Build: the three rules into the skill, a tracked copy under `.claude/skills/circle/` so a re-measurement can read it from the repo, and a selftest that greps the retired word | MECHANICAL (the three rules were signed S108) | The Atlas Plan §2 path 11 + §8; S115 · `Observed 2026-09-15 (S157 E14)` — the three rules in the TRACKED `.claude/skills/circle/SKILL.md` (a new section, the wording of The Atlas Plan's path 11: frozen-commit immutable audit records; the aborted-lane law — a non-returning lane is NON-EVIDENCE, never absorbed; HELD retired — Phase 6's vocabulary reads WITHSTOOD / BLOCKED) and the user-level mirror overwritten byte-identical; `selftest.sh` beside it greps the rules in and the retired word out, with two negative controls (HELD planted back, the aborted-lane law removed — each reds) and the mirror compared — 4/4; the two written claims (S108 §3, the S94 form line 23) carry a dated note under their S115 corrections: as of today, true |
| J38 | **THE FILE PORTAL ATLAS v2 — NEXT ITERATION (F2's filed minors).** Built 2026-09-05 in `C:\Users\Bndit\Projects\file-portal-atlas-quarantine` on branch `fable/real-atlas` (no remote) by Rab's exact fleet (2 Fable · 5 Opus 5 · 3 Sonnet 5; `wf_a4cac9f7-695` + `wf_08e3978a-a30`); terms schema 2 `e570a81`; verified **GO_AMENDED**, the four MAJORs remedied `503a4d1` (fingerprint identity for the inbox; grep-true line numbers; `.github`/`.gitattributes`/`.gitignore` no longer dropped; working-tree measurements unpinned + gitignored files skipped). Filed, not built — `research/S115-fleet/F2-VERDICT.md` carries each with its exact change + tripwire: R5 the register row-count fact's numerator (distinct ids vs matched lines; docs/51 374 vs 553); R6 journey 'code site' needles (`def ` → `def audit_convert(`, the real export entry); R7 'highest row id' by natural order (J37, not J7); R8 `describe` parses a leading `<root_id>:` and gets the gated git; R9 one vocabulary for absence (registered path missing = Observed absent, not UNREAD); R10 pack-level progress for the ~50 s build phase (the bar parks at 100 %); R11 `explain`/`pack` still v1-fixture-only (migrate or label); R13 coverage below the top level (205 of 576 tracked files probed — tauri.conf.json's CSP, fp_paths.py, vault.rs/transfer.rs/watcher.rs/chat.rs, sessions/*.jsonl undescribed and uninboxed: a second-level sweep or a per-surface 'N of M described' fact); R14 O5's event-verb count 34 → 36 (two hyphenated intake verbs); R15 laws.py's Unknown why composed from the instrument. (The two `.gitattributes` inbox entries filed at the close are the checkout's and the memory library's — not a duplicate.) **Rab's slots (F2's list, unsigned):** the widget reads the TRACKED catalog (O5's D-1; docs/13) vs a launcher; DESIGN-v2 §15 decisions 1–9; O3-2; WIDGET-FEATURES §9 W1–W8; adoption stays `false` ⟨signed 2026-09-05 (S116), Rab verbatim: *"Atlas: widget reads the tracked catalog, §15 1-9 as designed, R1/R4b ratified, O3-2 no"* — recorded in the Atlas repo `research/RAB-SIGNATURE-2026-09-05-S116.md`: transport **T-A** / DESIGN-v2 §15 decision 4 (W5's READING — the widget reads the tracked `atlas/generated/catalog.v2.json`, never a launcher; the BUILD of W5 into the widget is a separate signature per the 2026-09-05 record's decision 8, and G9 stays unsigned: `adoption_authorized` false); §15 decisions 1–9 as designed; F2 remedies 1 (fingerprint identity on rescan) and 4b (gitignored files excluded) ratified as applied in `503a4d1`; O3-2 DECLINED (the loopback allowance is not widened). The eleven minors R5–R15 remain the mechanical half.⟩ ⟨signed 2026-09-05 (S117), Rab's "absolutely" on the S116 sign line: **D-1 = fail-closed-with-reason** for new Atlas surfaces (existing fail-silent sites untouched); **W5 build: not yet**; **G9: not yet** (`adoption_authorized` stays false). Recorded in the Atlas repo (`RAB-SIGNATURE-2026-09-05-S116.md` addendum; WIDGET-FEATURES §7/§9). Remaining ink on this row: T-B, W1–W4, W6–W8, and the minors' build.⟩ | MECHANICAL (the minors) + SEMANTIC (the slots) | Atlas repo: DESIGN-v2.md, research/S115-fleet/F2-VERDICT.md, research/WIDGET-FEATURES.md; sessions/S115-desktop-2026-09-05.md |
| ~~J39~~ **STRUCK 2026-09-15 (S157 E4) — DONE** | **THE WINDOWS-CONVERTER LINT DEBT — 50 ruff errors under a warn-only CI step.** `ruff check .` (0.15.20) in `windows-converter/` finds 50: E702 ×22 (semicolon-joined statements), E731 ×12 (lambda assignment), F401 ×10 (unused import), E741 ×3, E402, F541, F841 — reproduced locally (`uvx ruff==0.15.20 check .`; marker-env carries no ruff) and in run 33990130578's job LOG, where the step exited 1 while the jobs API reported `conclusion: success` (SYM-075). Arming the step (S108-9's last third) goes red until these clear: clear them in ONE commit that also drops `continue-on-error` from the step, green locally first. Mechanical, but each E702/E731 sits on a load-bearing line — read the S106 lever gate's note on constants before "fixing" a semicolon-joined assignment. ⟨status 2026-09-06 (S117): **BUILT and MERGED** — a Sonnet builder in an isolated worktree (`wf_ffc82c5e-d33`, reset to `b96f266` first: the S115 F4 harness fault reproduced, the worktree spawned at `7c006f2`) hand-edited all 50 (no `--fix`): F401 ×10 deleted after a getattr/globals/string grep each; F541 ×1; E741 ×3 renamed at every use (`llamacpp`, `line`, `layer`); E402 ×1 `# noqa: E402` per the analyst_selftest convention; F841 ×1 `WAIT_OBJECT_0` deleted into a comment (no branch ever tested it); E731 ×12 lambda→def same name/body (no loop-capture case); E702 ×22 split at identical indentation (the `stmt; continue` under an `if:` kept both inside). CI Lint step armed in the same commit. Refuter GO_AMENDED (two cosmetic notes: "1 failure + 5 errors" vs the 6 it re-measured; the comment's 09-06 date is UTC, the commit's 09-05 is local). Cherry-picked `47cd440`; in the main checkout ruff `All checks passed!`, backend_parity 9/9 · figure_coverage 30/30 · convert_and_ship 206/206 · analyst 18/18 · room_chat_acceptance 24/24 · visual_witness_capture 54/54. **Found on the way:** the fresh worktree's checkout corrupted the byte-exact `docs/contracts/*.json` fixtures (CRLF blob + autocrlf → a second CR per line; 6 of 54 GROUND-DRIFT there, 54/54 in main) — pinned `-text` in `.gitattributes` next to S115's `schemas.json` line. Strike at the next sweep once CI on the armed step is observed green.⟩ | MECHANICAL | `.github/workflows/ci.yml` (the Lint step's comment); sessions/S116-desktop-2026-09-05.md §4 · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — `uvx ruff@0.15.20 check .` in windows-converter → `All checks passed!` (this sitting, twice: E1 and E3 each fixed its own two findings before the push); the CI step armed (ci.yml:92-94, no continue-on-error); 47cd440 an ancestor; CI green on the armed step observed today (run 34937129934) |
| ~~J40~~ **STRUCK 2026-09-15 (S157) — STRINGS FIXED S157 E11 (the bless.json question stays Rab's)** | **THE EXPORTER CALLS A BLESSED FLAG "PASS".** On the first real supersede-with-bless (Cybernetics, 2026-09-06T00:47:28Z) the ThinkPad exporter logged `EXPORT-SUPERSEDE … (reaudit, fail->pass)` and wrote the vault commit message *"supersede: … (reaudit, fail→pass)"* — while the vaulted manifest says `fidelity.verdict flag`, `blessed{by rab, from_verdict flag}`, `supersede{from_verdict fail}`. The data is right; the two status strings outrun it (STATUS-THEATRE's shape, ERROR-BIN class table; the exporter's own comment at `exporter.py:412-416` says "the vault must never pretend a blessed note passed on its own merits"). Fix: render the EFFECTIVE verdict with its cause — `fail→flag (blessed by rab)` — in both the log line and the commit message; one test on the bless path asserting the string. Also observed, named not judged: `bless.json` is committed into the vault beside the note (a second copy of a control file inside the library; the manifest already folds it) — Rab's call whether the vault should carry it. | MECHANICAL (the strings) + one SEMANTIC question (bless.json in the vault) | `linux-converter/converter/exporter.py` (supersede log + commit message, ~:387-430); ThinkPad journal 2026-09-06T00:47:28Z; sessions/S117-desktop-2026-09-05.md §7 · `Observed 2026-09-15` — `linux-converter/converter/exporter.py`: `_supersede_replace(…, effective)` — the caller hands the EFFECTIVE verdict (`pass` on merit; `flag (blessed by <who>)` on a human bless) and both the `EXPORT-SUPERSEDE` log line and the vault commit subject render `<from>→<effective>`; `tests/test_exporter.py`: the bless-path test asserts the subject `… (audit-remedy, fail→flag (blessed by rab))`, a negative control asserts the merit path keeps `fail→pass`; ruff check + format clean; **CI green on 51edb34 (run 34942832685, num 376) — Linux is the executed evidence** (the Windows run fails in the pre-existing path class, 23 of 38 before the change). Residue, his: whether `bless.json` should ride in the vault beside the note; the ThinkPad deploy (pull + restart) is his hand |
| ~~J41~~ **STRUCK 2026-09-15 (S157 E4) — BUILT + FIRST LIVE READ** | **THE ANALYST JOURNAL DIES ON SUCCESS — AND THE GUARDS' CALIBRATION DATA WITH IT.** `analyst.process` ends with `shutil.rmtree(work_dir)` ("the journal has done its job"), so the per-chunk `survival` and `ratio` columns J32-B/J34 write to every record exist only while a run is in flight or after a crash. S115 could calibrate both guards ONLY because the 2026-08-30 Univ 4e run had died at chunk 641 and left `chunks.jsonl` behind; the DDIA run (2026-09-06, 492 chunks, first live J34) deleted its own on assembly, so "0 inflation rejects" cannot be checked against the distribution it came from. Proposal: on success, keep the columns, not the text — write `analyst.chunk_scores: [{i, survival, ratio, reason?}]` (≈20 bytes × chunks) into the manifest, or rename the journal beside the bundle with the chunk TEXT stripped (the text is the book; the numbers are the evidence). Rab's slot: which home. Until then every successful run is a calibration sample thrown away. ⟨2026-09-09 (S118), signed "J41 = manifest chunk_scores" (Rab's "B"): **the home is the manifest** — `analyst.chunk_scores` rows `{i, s, r, x?}` (short keys; ≈15–20 KB for a 492-chunk book; event untouched). Built by a Sonnet lane in a worktree + refuter (`wf_d3f7b9b4-41f`). **Why it could not wait:** the 08-30 journal's reference is GONE — A1's slice sweep (signed 08-31) ran when DDIA converted on 09-06, both Univ 4e anchors are analysed and its held bundle predates the sidecar, so the S115/S116 calibration (3 / 6 / 14 of 500 below 0.50 / 0.80 / 0.90) is now `Historical` and unreproducible on this machine; the DDIA re-run under the 0.80 lever will be the first measurement that survives its own success.⟩ ⟨**BUILT + FIRST LIVE READ 2026-09-09 (S118):** `07daaf5` (+ `aa93818` on the refuter's two findings — a real negative control, a dead branch removed); `_score_row` rows `{i, s?, r?, x?}` from the live loop, the failed branch and the resume branch; `converter:chunk_scores` EVIDENCE; analyst selftest 22/22, T17 206/206. On the DDIA re-run: **492 rows, 17,507 bytes**; survival on 484, ratio on 472; passed-row buckets [0.80,0.90) 36 · [0.90,0.95) 55 · [0.95,0.99) 173 · ≥0.99 208; Σ(1−s) over passed = 14.33 chunk-eq = 3.04 % (body 12.43, front 0.90, back 1.00). Every number in S118 §8 came off it. Strike at the next sweep.⟩ | MECHANICAL + one SEMANTIC slot (the home) | `windows-converter/analyst.py` (`process()` tail, `_append_journal`); sessions/S117-desktop-2026-09-05.md §8 F3 · `Observed 2026-09-15 (S157 E4, HEAD 6644ecc)` — 07daaf5 and aa93818 ancestors of HEAD; `windows-converter/analyst.py:443` `def _score_row`; `observability/dispositions.json:305` `converter:chunk_scores`; the Valentine anchor manifest carries no chunk_scores (analysed before J41 — the DDIA re-run of S118 is the row's own live read, 492 rows) |
| ~~J42~~ | ~~STRUCK 2026-09-12T19:5xZ~~ **DONE (S140, as C's road).** `reanalyze()` starts from the newest anchored copy whose J33 sidecar VERIFIES (sha256 + bytes); the work copy's body is the sidecar, its frontmatter and manifest lose the analyst block; stale or missing refuses as before, as an event; `convert_and_ship_selftest.py` J42 (a)–(c), 216/216. First live use: the S140 re-analysis of *Zero to One*. Original text:  **A SIDECAR-AWARE `--reanalyze`.** `convert_and_ship.py:1951 reanalyze()` refuses any source whose anchored copies all carry an `analyst` block ("re-analyzing an analyst pass compounds its damage — use re-convert"), which is every book that has been analysed once. J33's sidecar (`<name>.marker.txt`, hash-bound in `manifest.marker_body`) IS the pre-analyst body the refusal is protecting against losing — so the verb should accept a held/anchored bundle WITH a verified sidecar and start from it: copy the bundle, replace the body with the sidecar text, run `apply_analyst`, ship as a supersede. The S118 re-run had to be a re-drop instead; it cost nothing only because A1 happened to keep DDIA's slices. Eligibility stays: no sidecar or a sidecar whose sha does not match → refuse as today. Selftest: a bundle with a valid sidecar re-analyses; one with a stale sidecar refuses; one without refuses. | MECHANICAL | `windows-converter/convert_and_ship.py` (`reanalyze`, `_anchor_copies`, the J31 sidecar check in `reaudit` to reuse); sessions/S118-desktop-2026-09-09.md §4, §8 F8 |
| J43 | **THE ROOM'S CONVERT STATION BAR IS A PROMISE, NOT A COUNT.** `room.js:327-339`: `pct = min(95, elapsed ÷ Marker's promised eta_s)`, capped at 95 until `converted` — but the station stays RUNNING through the analyst, so after Marker finishes (or resumes from cache with a 0 s promise, S118) the bar reads **95 % for two hours** while the honest numbers sit in the numerations panel below (N067 slices x/4, N061·live pages, **N106 analyst chunks n/total**, N063 slices resumed, N064 true cost, N062 retry spend, N076 stalls). docs/34: name the numerator and denominator. Fix: drive the bar from the LIVE stage's count — slices x/total during Marker, chunks n/total during the analyst — label it, show the promise as its own line, never divide by a 0 s promise; move N067/N061/N106/N063/N064 into the station, leave the queue/tray rows in the panel. A widget change: `room.js` (one function), `room.rs` if a field is missing, rebuild, adoption by Rab's hand. Rab's question on 2026-09-09 ("isn't some of the numerations important to be represented in the converter station?") is the ticket's commission. | SEMANTIC — a widget change (Rab's sign) + MECHANICAL | `windows-widget/src/room.js` (`Convert station` block, the numerations list at :523-537); sessions/S118-desktop-2026-09-09.md §8 F7 |
| J44 | **MEASURED 2026-09-17 (S182 E1) — BUILT BEHIND A LEVER, OFF; the arming his:** `text_norm.prepare_for`, `ladder_lever.read_ladder` (roots.json `ladder` → `ladder.txt`; absent = `j32a-v2` byte for byte), `audit_analyst(…, ladder)` + the per-chunk guard on the same lever, `regex_id` = the ladder that ran; rungs 1 (escape-first incl. `_`) and 3 (cite-anchor, both sides) built — the ligature rung NOT (gated later); `analyst_audit_selftest` (m)–(q) 20/20 with the positive control that v2 moves nothing. **The real audit on held DDIA: v2 0.9718 / 581 failed of 20,589 / 49 runs (reproduces the manifest) → v3 0.9845 / 319 / 33** — 262 windows were the audit's own; the verdict unchanged (< 0.995). docs/15 §9.6. ONE LINE `j32a-v3` into `ladder.txt` arms it and `--reaudit fc1f068c3a8eeb63` re-scores for free. Struck when the lever moves on his word (J32's form). **LADDER v3 — ESCAPE-FIRST, LIGATURE-BLIND (SYM-076).** Two measured rungs for J32-A's normalisation, sized offline on held DDIA `fc1f068c3a8eeb63` with the real `fidelity_audit.audit_analyst` (the as-shipped run reproduces the manifest to the digit, 0.9718 / 49): (1) run `unescape` BEFORE `prepare_output`'s markdown strip and include `_` in the escape set (`\\(?=[^\w\s]|_)`) — `within\_recursive` vs `within_recursive` goes 0.0 → 1.0 while `\rm` vs `rm` stays 0.0 (R5 kept) — document **0.9718 → 0.9756, runs 49 → 46**; (2) a ligature-blind rung deleting `ffi|ffl|fi|fl|ff` on BOTH sides before windowing (Marker drops the glyphs — `rst`, `proles`, `gure` — and the model repairs them; the audit must not count a repair as a loss) — **0.9808 / 33**. Neither clears 0.995; together they are the audit's own share of the residual. Build: `text_norm.py` (the order + the two regexes; `regex_id` → `j32a-v3`), `analyst_audit_selftest` cases for each rung with `\rm` as the negative control and a named false-match risk for the ligature rung ("fine" → "ne"), docs/15 §3/§9 re-pinned, dispositions' `converter:regex_id` bumped in the same commit. Then **`--reaudit fc1f068c3a8eeb63`** re-scores DDIA for free (the sidecar is the reference; expect ≈0.981 / 33, still fail — the rest is J45's class). ⟨**A THIRD RUNG, 2026-09-09 (S119, both research lanes independently):** Marker's citation form `[\[n\]](#page-N-K)` — 1,396 in the DDIA sidecar, canonicalised by the model to `[n](#page-N-K)` in 844 places — is the LARGEST failed-window class (R1: link-syntax 175 of 581; R2: cite-anchors 29.8 % of 604) and neither rung above sees it, because the ladder's link-strip regex cannot see through the escaped brackets. Rung 3 = strip `(#page-[\d-]+)` / collapse `[[n\]](url)` and `[n](url)` alike on BOTH sides before the markdown strip. Measured with the real audit: v3b 0.9808/33 → **v3c 0.9897/20** (R2, `prototypes/analyst-lab/decoding/ladder_probe.py`, controls: a repaired citation 1.0, a deleted tail 0.5); on the acceptor's reconciled body (J46) v3c reads 0.9968/1 and CLEARS 0.995. Trap measured by R1: a ONE-sided cite normalisation (reference only) makes the audit WORSE (0.9234/283) — both sides or neither. Order of the rungs: unescape (incl. `_`) → cite collapse → markdown strip → ligature-blind.⟩ ⟨**Codex's preserved risk, accepted (MSG-CDX-0047, 2026-09-09):** a ligature-BLIND rung (delete `ffi|ffl|fi|fl|ff` on both sides unconditionally) turns the adversarial pair *fine → ne* from 0.0 into 1.0 — so 0.9808/33 and the v3c 0.9897/20 that includes it are COUNTERFACTUAL until the rung is **gated: apply it only where the INPUT span carries a garble glyph** (R1's acceptor already does exactly this — `edit-whitelist/common.py`'s ligature rung requires a mis-mapped code in the input; R1's per-font table lists the codes). Build the rung gated, then re-measure; the ungated number is not to be quoted as a gain.⟩ | MECHANICAL | `windows-converter/text_norm.py` (`prepare_output`, `_UNESCAPE`, `chunk_survival`), `fidelity_audit.py:39` (by-name import — the fix must live in text_norm); probes: S118 scratchpad `ladder_v3_probe.py`, `ladder_v3b_probe.py`; `prototypes/analyst-lab/decoding/ladder_probe.py`; sessions/S118-desktop-2026-09-09.md §8 F2, S119 §8 F3 · `Observed 2026-09-15 (S157 E50)` — THE DELTAS RE-MEASURED at HEAD 392a4cd with the row's own probe (`prototypes/analyst-lab/decoding/ladder_probe.py`, in-memory, the held DDIA `fc1f068c3a8eeb63`): v2 as shipped 0.9718 / 49 (= the manifest), v3 escape-first 0.9756 / 46, v3b + ligature-blind 0.9808 / 33, v3c + cite-anchor 0.9897 / 20 — every number in this row reproduces to the digit; the controls: v3c repaired cite → 1.0, deleted tail → 0.5 (a real deletion not rescued). The probe itself could NOT run as committed: `ddia_pairs.py` bound the S119 fleet's WORKTREE path, gone since — repaired to the repo's own windows-converter (FP_WC overrides). The build (the rungs into `text_norm.py`, the ligature rung GATED per Codex's preserved risk, dispositions bumped) is the switch — his; the ungated 0.9808/0.9897 stay counterfactual |
| ~~J45~~ **STRUCK 2026-09-15 (S157 E8) — ANSWERED BY J46 — struck S157 E8** | **THE MODEL REWRITES NUMBERS AND NOTHING SEES IT.** In DDIA chunk 81 (passed at 0.8077) qwen3:8b turned `person(100,` into `person(10,` and `the value 100` into `10` — a corruption inside a chunk the survival guard passed, invisible to the fence (no image token), to survival (one window of 52), to the ratio (0.998). The program says "Do NOT reword, add, or remove"; numerals are the cheapest place to check that it obeyed: **a per-chunk multiset of digit-bearing tokens in the input must be a subset of the candidate's** (reflow may reorder, never drop or alter a number) — reject reason `numeral`, the fifth `rejections` bucket, riding `chunk_scores` as `x`. First act: a CLEAN census — the S118 `rescore_ladder.py` count (145 chunks / 691 tokens) is NOISY because its output-span trimming cut tail numbers and is NOT quoted; align input and candidate per chunk from `chunk_scores` + the journal-shaped text and count properly, then set the rule (exact subset vs allow formatting like `1,000`/`1000`). Threshold-free by design; the semantic slot is Rab's reject-vs-flag. ⟨**CLEAN CENSUS 2026-09-09 (S119, both lanes):** **14 chunks / 48 content numerals** changed on the shipped DDIA run (e.g. chunk 81 `100→10` ×2, 337 `2124→2123`, 193 `232→32`) — S118's 145 / 691 was contaminated by `<span id="page-N-K">` digits and citation numbers moved out of links; the rule must strip span anchors, ⟦IMG⟧ tokens and link targets before counting. R2's greedy re-run of chunk 81 did NOT reproduce the halving (a draw), so part of this class is the sampler (J49) and the acceptor (J46) reverts the rest (321 numeral edits reverted on the 492 pairs, most of them citation numbers the model moved — the whitelist's `link` rung must decide those).⟩ | MECHANICAL (the census, the guard) + SEMANTIC (reject vs flag) | `windows-converter/analyst.py` (`process()` after survival/ratio), `text_norm.py`; sessions/S118-desktop-2026-09-09.md §8 F3, §10; `prototypes/analyst-lab/decoding/edit_taxonomy.py`, `edit-whitelist/classify.py` · `Observed 2026-09-15 (S157 E8)` — the acceptor promoted S140 on Rab's C+A (2026-09-12): `edit_whitelist.py:172-179` classifies a hunk `numeral` when the digits differ and the letters agree; `FULL` (:73) excludes it, so every numeral change is REVERTED at accept time and counted in `manifest.analyst.edits.reverted.numeral` (DDIA: numeral 10, digit for digit — docs/15 §12.2); a hunk mixing a good edit with a numeral change reverts whole. "Nothing sees it" no longer holds; the reject-vs-flag slot is moot under the signed whitelist (the original text ships for that hunk, no chunk is lost). Residue, J46's own: a citation number the model moved out of a link is the `link` rung's to judge; the census's `1,000`/`1000` formatting question stays unasked (a formatting change is a substitution today, reverted) |
| ~~J46~~ | ~~STRUCK 2026-09-12T19:5xZ~~ **DONE (S140, Rab signed C+A 2026-09-12).** A: `windows-converter/edit_whitelist.py` promoted from the S119 prototype and wired into `analyst.process` (FULL whitelist; punctuation/case reverted — Rab's slot); `manifest.analyst.edits` + `chunk_scores` `e`; dispositions; docs/15 §12.2; selftests 40/40 + analyst 30/30. Measured on the 492 DDIA pairs after a 3-lane review: FULL 0.9822/24 shipped ladder, 0.9969/1 v3c (prototype 0.9817/24, 0.9968/1); 1,914 accepted / 1,397 reverted; no non-whitelisted edit accepted; STRICT 1.0/0. C: the live re-analysis of *Zero to One* from its verified Marker-body sidecar (J42's path; 79 chunks; the first
  launch died with a session restart at chunk 44 and the relaunch resumed from the S61 journal — 521 s for the rest, 21:00Z):
  **71 passed · 8 rejected** (fence 3, survival 2, think_leak 3) · 0 failed; **21 edits accepted** (escape 11 · markup 7 ·
  reflow 2 · hyphen 1), **39 reverted** ( Original text:  **THE DIFF-WHITELIST ACCEPTOR — faithfulness by construction (S119 R1, measured).** After the fence, survival and ratio checks, `reconcile(chunk, candidate, whitelist)`: word-level opcodes (rapidfuzz `Levenshtein.opcodes` on whitespace tokens, already a dependency); accept an edit ONLY if the two spans normalise equal under a stated whitelist (escapes, link re-syntax with an equal URL multiset, heading/emphasis/table marks, hyphen joins, ligature-glyph repairs, reflow); revert everything else to the input span; ship the reconciled chunk. The model may then reflow, re-mark and repair; it can no longer reword, delete, insert or change a numeral. **Measured on the real 492 DDIA pairs with the real `fidelity_audit.audit_analyst`** (`prototypes/analyst-lab/edit-whitelist/acceptor.py`): shipped 0.9718/49 → **FULL whitelist 0.9817/24** under today's ladder, **0.9968/1 under ladder v3c (clears 0.995)**, **STRICT (revert ligature/escape/link repairs too) 0.9998/0** under every ladder; edits gated: accepted 1,213 (markup 526, escape 331, markup+escape 189, mixed 127, link 14, ligature 12, reflow 1), reverted 2,650 (deletion 1,006, substitution 989, numeral 321, punctuation/case 272, insertion 62). Controls: accept-all reproduces the shipped body BYTE-FOR-BYTE and its 0.9718/49; accept-none reproduces the sidecar (1.0/0). It caught what no guard did: `(minimizing` → `(minim,izing` (chunk 9), a `/no_think` leak inserted into chunk 3, the halved numeral (81). Build: a pure module beside `text_norm` (`reconcile`/`equivalent`/`norm`, ~250 lines, no I/O), one call in `analyst.process` before `out.append(candidate)`, accepted/reverted counts per class into the journal row and `chunk_scores` (a new `e` map), selftest pinned to the two controls + one case per rung with its negative control (`\rm` vs `rm` reverted; a URL change reverted; "fine" vs "ne" named). Cost ≈ microseconds per chunk, 0 GPU. Not enough to `--reaudit` afterwards — the acceptor changes the shipped TEXT, so a live run (J42's sidecar-aware `--reanalyze`, or a re-drop) measures it. **Rab's slot: the whitelist IS the policy (which edits the analyst may make); punctuation/case edits are reverted in the prototype and could be admitted.** Residue: STRICT leaves 4 windows (chunks 56/61/90, reconstruction seams); the ligature rung measured on DDIA only. ⟨S131 (2026-09-12), a second book's evidence for this ticket: on *Zero to One* the local analyst's readability pass, against Marker's own body, DROPPED "I thought,"; MISSPELLED a word it was fixing (Marker's drop-cap `E SCAPING` → `ESCAPPING`); CUT a 112-word paragraph mid-word ("even for the bi.") and swallowed the next heading's `###`; DROPPED a 50-word paragraph and a 16-word sentence tail; broke a line mid-word ("Rome, Rom" / "Romulus was"); and LEAKED its `/no_think` control token twice (SYM-115) — eight holes, ~560 words, found by a paragraph-level containment diff, of which the audit's run list surfaced three. Every one is an edit the whitelist would revert. Repaired by hand on the bench (`repairs.jsonl` seq 6–13).⟩ | MECHANICAL (the module) + SEMANTIC (the whitelist) | `prototypes/analyst-lab/edit-whitelist/` (acceptor.py, align.py, classify.py, common.py, DESIGN.md, results_*.json); `windows-converter/analyst.py` (`process()` accept branch); sessions/S119-desktop-2026-09-09.md §8 F6 |
| J47 | **WHAT IS THE ANALYST FOR — the stated four items, or "make it readable"? (S119 R2, measured; Rab's).** `prompts/readability.txt` asks for four things: join mid-word hyphenation splits, normalise heading levels, keep paragraphs intact, keep ⟦IMG⟧ tokens. On DDIA the input has **0** hyphenation splits (`\w-\n\w`) and 4 `w- w`; the LLM performed **0 hyphen joins and 0 heading-LEVEL changes** — and changed **426 of 451** aligned chunks anyway (348 headings removed, 383 added; 1,006 deletions, 989 substitutions, 321 numerals, 272 punctuation/case edits — the acceptor's reverted classes). A ~40-line rule pass (`prototypes/analyst-lab/rule-analyst/rule_analyst.py`: dehyphenate `w-\nw`; join `unexpect edly` only when the join is a frequent word of the book and one half is not; headings measured, not touched; everything else byte-identical) does the STATED job and scores **1.0 / 0 runs** on the real gate, at 0 GPU, saving ≈2 h 14 min per 670-page book; its selftest plants both split shapes into real prose and fires 1/1 each. **The decision is semantic and Rab's:** (a) the stated job → a `rules` backend closes the analyst stage at 1.0 and the LLM becomes optional; (b) readability as the product → the LLM stays and J46 makes it faithful, and D1 (`ship-with-losses-named` vs `audit-must-be-green`, open since S110) needs signing for whatever the acceptor cannot reach. Residue: the space-split guard is measured on one born-digital book with 0 splits; an OCR/scan book is UNREAD; list reflow and heading JUDGEMENT are things the rules cannot do that the LLM did. | SEMANTIC — Rab's (what the stage is for) | `windows-converter/prompts/readability.txt`, `analyst.py` (a `rules` backend or program); `prototypes/analyst-lab/rule-analyst/`; sessions/S119-desktop-2026-09-09.md §8 F5 |
| J48 | **THE REFERENCE IS DAMAGED AT pdfium's CHAR→UNICODE FALLBACK — repair upstream or before the analyst (S119 R1, Verified).** 224 ligature glyphs matched bbox-by-bbox on 527 DDIA pages: every one resolves to U+FB00–FB04 under MuPDF and to a code in `! " # $ & ' )` under pdfium, because the subset fonts (MinionPro-It, MyriadPro-SemiboldCond, GuardianSans…) name their ligature glyphs (`f_i`, `f_f_i`, `f_l`, `T_h`, `f_t`, `f_k`) at codes 0x21–0x29 with NO ToUnicode entry, and `pdftext/pdf/chars.py:18` (`FPDFText_GetUnicode`) returns the raw code — so Marker's body reads `rst`, `proles`, `%ere` (There), `so$ware`. The `!`-hyphen (782 `x! y` joins, 138 broken links) is the PDF's OWN ToUnicode (`bfrange <20><26><0020>` maps the hyphen glyph to `!`) — both extractors agree there. Sidecar census: 73 in-word garbles, 782 hyphen joins, 138 split links, 1,396 escaped citations. Two homes: (1) upstream — pdftext consults the glyph NAME when ToUnicode is silent (one line, `Inferred`, falsifier named: pdfium's own API may not expose it); (2) File Portal — `prototypes/analyst-lab/reference-repair/repair.py`'s vocabulary-gated repair (R-lig 58 words, R-hyph 716, R-link 138; the vocabulary from the pymupdf witness `fidelity_audit` already builds; an empty vocabulary changes nothing) applied to the Marker body BEFORE the sidecar is written and the analyst runs. Measured only after the fact (repaired reference vs an output produced from the unrepaired input): 0.9718 → 0.9726/50; the gain is expected on a RE-RUN (fewer repairs for the model to make, none for the audit to count). Trap measured: a one-sided cite rule makes the audit worse (0.9234/283). Sibling class not sized: diacritic glyphs (`Uğur`, `Salihoğlu`). | MECHANICAL (the repair) + a signature for where it lives (upstream vs pre-analyst) | `prototypes/analyst-lab/reference-repair/` (repair.py, DESIGN.md — the per-font glyph table); `windows-converter/convert_and_ship.py` (between the Marker merge and the sidecar write); marker-env `pdftext/pdf/chars.py:18`; sessions/S119-desktop-2026-09-09.md §8 F2 |
| J49 | **THE LOCAL ANALYST SAMPLES AT 0.6 AND NOTHING SAYS SO — pin the sampler and record it (S119 R2, Verified).** `analyst._generate`'s local request carries NO `temperature`/`seed` (`options: {num_ctx: 8192}` only; the Gemini path pins 0.2), so qwen3:8b decodes at its Modelfile's thinking-mode recipe (temperature 0.6, top_p 0.95, top_k 20, repeat_penalty 1; `ollama show qwen3:8b`, ollama 0.33.2) on a `think: false` copy task. One gated GPU experiment (the watcher's chat-hold written by the calling process; 1,633 → 7,987 → 1,661 MiB; ≤90 generations): on the 40 hardest DDIA chunks shipped vs `temperature 0 + seed 7` were byte-identical 28/40, same survival 36/40 (mean Δ +0.0034), greedy reproduced itself 5/5, **7 of the 12 paragraph deletions reproduce to the digit under both settings — the model deletes those on purpose**; 5 were draws, as was chunk 81's `100→10` (did not recur). The draw is worth ≤ 0.3 pp of the 3 % (`Inferred`, hardest-40 extrapolation). Build: `options {num_ctx, temperature: 0, seed: <fixed>}` with a lever-waiver line, and write the effective sampler into `analyst` meta so the manifest names its conditions (docs/34) — three lines; then a full greedy run's document number is the measurement this ticket owes. A preservation `system` line improved 2 of 5 chunks (n = 5, `Inferred`). No larger local model fits beside the desktop's 1.6 GB on the 10 GB card (qwen3:8b resides at 6.3 GB; 12–14B at Q4 ≈ 8–11 GB). ⟨status 2026-09-20 (S209 E6): **SIGNED** — Rab, the Desk 58ba5591 "Signed." on the lane's three-line list (f1d13d86: "If u need a signature, make it clear"): `ANALYST_SAMPLER = {"temperature": 0.0, "seed": 7}` in `analyst.py` from the next process on; `sampler_record` writes the values into the manifest's `analyst.sampler`; `analyst_selftest` 51/51. The sourcing batch splits here — four documents before, the rest after — the analyst survival read across the split at E5. The full greedy run's document number is still what this row owes.⟩ | MECHANICAL + a lever-waiver (Rab's word on the values) | `windows-converter/analyst.py` (`_generate`, the local options dict; `process()` meta); `prototypes/analyst-lab/decoding/` (decoding_experiment.py, results.json, DESIGN.md); sessions/S119-desktop-2026-09-09.md §8 F4, F7 · `Observed 2026-09-15 (S157 E6)` — the RECORDING half built: `ANALYST_SAMPLER` (None, None — the request unchanged) with its waiver, `_request_options()`, `sampler_record()` → manifest.analyst.sampler names what was sent or whose recipe applied; disposition converter:sampler; analyst_selftest S157-E6 (a)-(c) 43/43. Still his: the values (temperature 0 + a seed) and the full greedy run the ticket owes |
| J50 | **THE AUGUST THIRTEEN — 13 Codex ack-required entries unconfirmed by Fable (MSG-CDX-0029, 0030, 0033–0043; 2026-08-31 → 09-01).** NR-05 read 13 owed, oldest 232.6 h, at 2026-09-09T19:26Z; the watcher re-lists them at every arming and the handler is forbidden to take them (they are August tickets, several `RED` — MSG-CDX-0033 is a digest mismatch on MSG-FAB-0055). Options: **confirm-all** (a Sonnet handler pass writing an honest restatement per entry — "read 2026-09-09, no work follows"), **leave** (the debt stays a stated number), **strike** (one Fable notice declaring them superseded by S110–S119, cited by id; the sidecar still shows them owed). Codex may carry this to Rab itself (MSG-FAB-0079 ROUTE). | semantic — Rab's | S119 (13-or-15), S120 §5 NR-05, docs/56 **SIGNED 2026-09-10T00:35Z (Rab: confirm-all, receive-only) — executed S121: 11 confirmed, 2 law-4 reds (0029, 0030: the sender's claimed digests never matched the bytes it appended; unchanged since 8ee44bb/02e6a6b); those two stay owed and named unless struck.** |
| ~~J51~~ **STRUCK 2026-09-15 (S157 E40) — SIGNED and executed — struck S157 E40** | **AN ENTRY WORD CAP AS A REFUSAL — the succinctness lever.** NR-07 baseline: Fable median 536 words / p90 949, Codex 293 / 530; restatements 123 vs 34 (NR-08). After S120 `gate.py post` prints the body's word count (a meter, no threshold) and refuses only a missing envelope slot (CR-CDX-0002, signed). A cap — e.g. 250 words for a notice, 450 for a ticket, refused not warned — is a lever: his number, his word. Until signed, the meter is read at every close (docs/56 §2 is the baseline to beat). | lever — Rab's | S120 §5 NR-07/08, MSG-FAB-0079 ROUTE (1) **SIGNED 2026-09-10T00:35Z (Rab: meter-only, no refusal) — the meter prints at post; no cap; recorded in coordination/SIGNATURES.md.** · `Observed 2026-09-15 (S157 E40)` — SIGNED 2026-09-10T00:35Z (meter-only, no refusal); `gate.py post` prints the body's word count and refuses nothing but a missing envelope slot; recorded in `coordination/SIGNATURES.md`. A cap was his to name and he named none |
| ~~J52~~ **STRUCK 2026-09-15 (S157 E40) — SIGNED and executed S121 — struck S157 E40** | **A RELAY WATCH THAT OUTLIVES THE SESSION — and a lane that reads DEAD, not busy.** The 24/7 watch Rab ordered dies with the session process (S119 by design; S120's power loss at 20:29Z proved it: 49 min blind, re-armed by hand at 21:18Z; a second arm 21:29Z on the tracked script). The tracked `coordination/relay_watch.sh --as <lane>` is path-relative and logs to `coordination/private/relay-watch.log` (NR-13), so it can run under a Windows scheduled task / a service at logon — a standing process on his machine, so his word. Twin: after the loss Codex's state read `working` for 100+ minutes with a STALE beat — a dead lane renders busy; a rendering rule "STALE beyond N min = presumed dead" is a threshold (his). | semantic/infra — Rab's | S120 §7 (20:29Z, 21:18Z), §8 F2/F8, docs/56 NR-09/NR-13 **SIGNED 2026-09-10T00:35Z (Rab: per-logon supervision + STALE 45 = presumed dead) — executed S121: scheduled task "File Portal relay watch (Fable)" at logon, no time limit, WATCH ARMED 00:40:33Z; status renders presumed dead past 45 min.** · `Observed 2026-09-15 (S157 E40)` — SIGNED 2026-09-10T00:35Z (per-logon supervision; STALE 45 = presumed dead); executed S121 (the scheduled task "File Portal relay watch (Fable)" at logon; the board renders presumed dead past 45 min). The watch's later OFF state (Rab's word, S122) lives in J60, which stays open |
| J54 | **CODEX'S FOUR ITEMS FROM S122'S VERIFICATION — delegated to its lane on the bus (MSG-FAB-0083), not yet collected.** (1) **SYM-081** `lab_driver.py:258` compares the lab actor's pid to `Popen().pid`; under a uv venv launcher the pids differ and 9 of 115 tests error — 115/115 holds only on the interpreter its `VERIFICATION.md:175` pins (fix the check or name the pin in the claim); (2) **SYM-082** its documentation manifest (`sessions/S121-codex-reading-and-ticket-origins.md`) mixes git-blob (LF) and checkout (CRLF) hashes — 7 of 25 sampled rows mismatch the checkout; (3) the Operations Studio `test_http` teardown assertion (`connect_ex == 0` after stop) fails 4–5 of 34 on a launcher — a port-release race, count non-deterministic; (4) MSG-CDX-0060's RECAP states Rab's chat-only "Signed."/"Installation Signed!" as flat fact while its SIGNATURES.md marks them transcript-only. Everything else it claimed reproduced (scope, 0 deletions, relay append-only kept, EXE `C4559805`, JS 39/39, Rust 29/29 + fmt + clippy, acceptance 141/141, reader 47/48+1); UNREAD from outside: the DebugEXE hash and the 91/81 inventory. Collected when its next entry names each as fixed, restated, or disputed with a probe. · `Update 2026-09-19 (S208 E1)`: Codex is back online (his word 14:17Z); the door brief `coordination/BRIEF-S208-CODEX-RETURN.md` names this row and A49 as Codex's and re-issues NEITHER — his 2026-09-10 word parked them; his word to Codex re-issues them; the brief gives Codex its own lanes (codex · C<N>) so its work never lands in the Fable lane's folders | delegated — Codex's | S122 §4b/§8, MSG-FAB-0083, SYM-081, SYM-082 |
| ~~J55~~ | ~~STRUCK 2026-09-10T04:5xZ~~ **DONE (S124).** 24 SYM + 9 ERR + 5 J rows filed (SYM-086..109, ERR-…-069..077, J57..J61); 28 already-filed and 13 not-a-defect recorded in `sessions/S124-…` §8; census 77/77 · 109/109. **THE CLOSEOUT FINDINGS WITHOUT A ROW.** S123's third reader found ~40 §8/§10 findings across S104–S122 that name a failure mechanism and never became a SYM/ERR/J row (docs/58 §5); two recurring ones were given families with no row yet (S15 the-instrument-manufactures-the-finding: 7 findings; S16 guard-coarser-than-the-defect: 4). A sweep session files each as a row in its family or records why not; `error_families.py --census` then counts them. | mechanical — a docs/55-style sweep | S123 §8, docs/58 §2 S15/S16, §5 |
| ~~J56~~ | ~~STRUCK 2026-09-10T04:5xZ~~ **DONE (S124) — (c) both.** docs/47 §9 (the law) + `.claude/hooks/guard_git.py` (the hook, wired in `.claude/settings.json`) + `coordination/fleet-ground-template.md`; SYM-085 → fixed. Codex's harness is not covered by the hook (the law covers it). **A GUARD ON THE SHARED CHECKOUT AGAINST A LANE'S DESTRUCTIVE GIT VERB (ERR-068 / SYM-085).** An in-place fleet lane ran `git reset --hard` in the main checkout and destroyed Codex's uncommitted 0064 entry and sidecar writes. Options: (a) docs/47 amendment (Rab's signed law): a ground names its mode; a lane not in a worktree may not run `git reset/checkout/clean/stash/restore`; (b) a mechanical guard — a git `pre-*` hook or a harness permission that denies those verbs to subagents in the main checkout; (c) both. Codex's copy of 0064 lives only in its own transcript — its re-post is asked in MSG-FAB-0084. | Rab's — the law is his signature; the hook is a standing change to his checkout | S123 §7 02:40Z, ERR-068, SYM-085 |
| ~~J57~~ | ~~STRUCK 2026-09-10T05:2xZ~~ **DONE (S125) — the scheduled task at logon.** `File Portal widget (J57)`: AtLogOn for `DESKTOP-BNDIT\Bndit`, Interactive/Limited, no execution limit, IgnoreNew, no battery stops; action = the installed `AppData\Local\File Portal\file-portal-widget.exe`. The widget autostarts its watcher at boot (`main.js:1152`), so the task closes the 'clicked' half of S114 F6. Fired once by hand: result 0, the single-instance plugin fronted the running widget (still one instance). The card's `widget autostart` row re-measures it every open (muster selftest 81/81). **OBSERVED 2026-09-10 16:36Z (S128): the whole chain fired unattended — see J62.** What remained was the LOGON itself — auto-logon after a power cut is a security setting and Rab's hand; the unattended proof is the next logon. **NO UNATTENDED START PATH — the intake watcher is a child of the hand-launched widget (S114 §9/§10-F6).** After a reboot or power cut the machine sat powered, healthy and idle for 1 h 46 m until a human logged in and clicked; no journal can recover what nothing restarts. Options: a scheduled task at logon for the widget (J52's shape for the relay watch), or a watcher service independent of the glass. Touches remote-dispatch-vision. | Rab's — a seam he named as his signature | S114 §10-F6; docs/58 S6; filed S124 (J55) |
| ~~J58~~ | ~~STRUCK 2026-09-10T06:1xZ~~ **DONE (S126) — one click, exporter side.** The staging watch is recursive (`main.py`) and `ExportHandler` re-exports a bundle when a `bless.json` is written (IN_CLOSE_WRITE) or renamed directly inside a top-level bundle; every other nested event is ignored. Two tests; ThinkPad: 32 → 34 passed, unit restarted 06:04:31Z, both `watching` lines in the journal. **`Intended` until Rab's next real bless click** (the journal will show `EXPORT-BLESS-SEEN` then `EXPORTED-SUPERSEDE` with no restart between). **THE EXPORTER SWEEPS ONLY AT STARTUP and its staging watch is non-recursive (S117 §8-F5).** A `bless.json` written inside a bundle dir mid-session is honoured only at the next exporter restart (S117's own bless needed one). A top-level touch or a recursive watch removes the hand. Rab's product question: one click or two. | Rab's — product | S117 §8-F5; A41; filed S124 (J55) |
| ~~J59~~ **STRUCK 2026-09-15 (S157 E21) — CUMULATIVE STAGE BUILT (the lock remedy not taken; named)** | **`gate.py stage` HAS NO TRANSACTION LOCK AND IS NON-CUMULATIVE ACROSS LANES (S120 §10, L2's declared residue).** Two lanes staging in the same minute is untested; each call recomputes HEAD + its own entries, so the second replaces the first's index entry — correctness rests on "each lane commits before the other stages". Remedy: a lock across stage→commit, or a cumulative stage. | MECHANICAL | S120 §10; SYM-078/079 do not cover it; filed S124 (J55) · `Observed 2026-09-15 (S157 E21)` — `gate.py stage`: the base is the INDEX blob when it differs from HEAD — the peer's staged, uncommitted entries are kept in the working copy's order (matched as an ordered subsequence like HEAD's); an index that is not a faithful append of HEAD, or not a subsequence of the working copy, is REFUSED as UNREAD, the index untouched; the second stage prints what it kept of the peer's (`cumulative (J59)`). `relay-gate/selftest.py` + 5: Fable then Codex before any commit — both in the index; the reverse order; the working tree untouched; a forged index blob refused and left as it was — 204/204. Residue: two stages in the same instant are still serialised only by git's own index lock; the stage→commit lock the row also named is not built (the cumulative base removes the replacement the row measured, which was the harm) |
| ~~J60~~ **STRUCK 2026-09-20 (S209 E2) — REMEDY EXISTS (--stop-file per watcher)** | **THE RELAY-WATCH HANDOVER HAS A DARK WINDOW (S121 §10: 73 s, one signal missed).** One shared STOP file stops both the outgoing and the incoming watcher, so they cannot overlap. Remedy: a distinct STOP path per watcher so the new one arms before the old one stops. Moot while the watch is OFF (Rab's word, S122); live again the day it is re-armed. ⟨status 2026-09-20 (S209 E2): REMEDY EXISTS — `agent-scripts/navigation/watch_relay.sh --stop-file <path>` (S143) gives each watcher its own STOP file (default `coordination/private/watch-relay-<lane>.STOP`), so a handover with no dark window is a procedure, not a build: arm the NEW watcher with a fresh `--stop-file` first, then touch the OLD one's — the two never share a file. The watch is OFF on his word (S122); the day it is re-armed, that order is the rule. Strike.⟩ | MECHANICAL | S121 §10; J52; filed S124 (J55) |
| ~~J61~~ **STRUCK 2026-09-15 (S157 E8) — FIXED S157 E8** | **`selftest.py`'s `run()` decodes the child's stdout with the host locale (S121 §10).** A non-ASCII assertion (an em dash) cannot match and had to be split into ASCII substrings; decode UTF-8 explicitly. Test-only, small. **Second instance, S129:** the new reading-bench `selftest.py` did the same in reverse — the child printed in the locale (cp1252), the parent decoded it as UTF-8 with `errors=replace`, the U+FFFD then crashed the parent's own print on the cp1252 console (`UnicodeEncodeError`). Fixed there by `PYTHONIOENCODING=utf-8` in the child's env plus a `reconfigure` of the parent's stdout — the pattern for this row, and a filed item repeated the day after it was filed. | MECHANICAL | S121 §10; filed S124 (J55) · `Observed 2026-09-15 (S157 E8)` — `.claude/skills/relay-gate/selftest.py`: `CHILD_ENV` carries `PYTHONIOENCODING=utf-8`, `run()` / `spawn()` / the T53 `Popen` decode UTF-8 with `errors=replace`, the parent's stdout reconfigured (the reading-bench selftest's S129 pattern); a tripwire asserts the gate's literal em dash round-trips through `run()` (the J52 T1 assertion had been split into ASCII halves to dodge it) — 199/199. Negative control observed: a child on UTF-8 under the old locale decode renders `â€”` (the old `run()` under any session that exports PYTHONIOENCODING); the fix round-trips |
| ~~J62~~ | ~~STRUCK 2026-09-10T16:5xZ~~ **DONE (Rab's hand, S128 observed).** Rab: "Tried it. Nice." `Observed`: boot 16:36:07Z → auto-logon (console session `bndit`, logon 16:36) → the J57 task launched the widget at 16:36:29Z (pid 9096; the task's LastRunTime 16:36:36Z, state Running = its instance is the widget) → the widget's boot log: `boot: all loops launched` then `watcher_start running pid 14120` at 16:36:37Z → the watcher's parent is 9096. Thirty-one seconds from power to converting, no click. `AutoAdminLogon=1`, `DefaultUserName=Bndit`, no cleartext `DefaultPassword` (an LSA secret — a proper route). The card read `auto-logon ON (Bndit)` at open, unprompted. **AUTO-LOGON — J57's other half (Rab: "auto-logon to finish J57", 2026-09-10).** The J57 task launches the widget at logon; a power cut still ends at the logon screen. Auto-logon is a security setting that stores the account password (an LSA secret via `netplwiz` or Sysinternals Autologon; cleartext via the raw registry) — **not a session's act** (the safety rules; docs/19 §0.3c). **Read S127:** `AutoAdminLogon` unset, no cleartext `DefaultPassword`, `DevicePasswordLessBuildVersion=2` (the netplwiz checkbox is HIDDEN until that value is 0), account `Bndit` is local, last sign-in by **password** (no PIN trip), Win10 Pro 19045, BitLocker UNREAD (non-admin), Autologon.exe not on the machine. **Procedure (his elevated shell):** EITHER Sysinternals Autologon (`Autologon.exe DESKTOP-BNDIT Bndit <password>`; stores an LSA secret) OR set `HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\PasswordLess\Device\DevicePasswordLessBuildVersion` to 0, then `netplwiz` → untick "Users must enter a user name and password" → the password twice. **Caveats:** anyone who powers the machine gets Bndit's desktop (BitLocker state unknown; the seat may be his brother's — docs/19 §0.8); a later password change breaks it silently — the card's `widget autostart` row reads `auto-logon ON/OFF` every open. **Done-when:** the card reads ON, and a reboot brings the widget up with no click (the widget's boot log shows `watcher_start` at boot) — J57 → `Observed` then. | Rab's — a security setting and his password | S127 §4/§8; J57; docs/19 §0.3, §0.8 |
| ~~J63~~ | ~~STRUCK 2026-09-10T06:5xZ~~ **DONE (S127) — THE RECORD-FIRST TRIPWIRE.** ERR-063 (S120), ERR-078 (S126), ERR-080 (S127): three sessions in a row wrote and committed instruments before their record. Mechanized: `open.sh` publishes the session number in `coordination/private/session.current` (per machine, untracked); **`.claude/hooks/guard_record.py`** (PreToolUse on Edit/Write/MultiEdit/NotebookEdit) refuses a write to a tracked repo file while `sessions/S<N>-*.md` is absent (exempt: `sessions/` itself, `coordination/private/`, outside the repo, a root without `coordination/`); **`guard_git.py`** refuses a `git commit` in the shared checkout under the same condition, so a script's write is stopped at its commit; no marker = no open ran = refused. Selftests: guard_record 11/11, guard_git 149/149, muster CASE 47 (86/86). Its first live catch was its author: the hook went live on the settings.json write and refused the next edit until the card had written the marker. Gap named: a session that never runs open.sh and only writes through scripts is caught at the commit, not at the write. | MECHANICAL | S127 §8; ERR-063/078/080; docs/28 |
| ~~J64~~ | ~~STRUCK 2026-09-12T15:4xZ~~ **DONE (S137).** Shapes 6–11 as 17 cases in `guard_git_selftest.py` (nine false-deny regressions, eight negatives), then two narrowings in `guard_git.py`: `SHELL_WRAPPERS` (the recursion into a quoted argument only for shell wrappers) and `fold_data_heredocs()` (a heredoc body is data unless its receiver is a shell; a program-fed body that mentions git is UNREAD). 172/172; the specimens 15/15; the three shapes re-run live in the real checkout ALLOW. The same construct hid a bypass (a program-fed heredoc shelling out to git passed) — SYM-116, closed in the same commit. Original text:  **`guard_git.py`'s SIXTH false-deny shape: an inline `python -c "…"` whose code carries braces (S128–S129).** SYM-110's family — a read-only command in the shared checkout rendered UNREAD and denied. The mechanism is `Inferred` (the brace rule reading a quoted `-c` payload's `{`/`}` as bash brace expansion — quoted braces are data); the deny line itself is in the S128 transcript. Order of work: a FALSE-DENY REGRESSION case in `guard_git_selftest.py` that reproduces the deny FIRST, then the grammar, never a loosening (the fifth shape's fix found a real bypass by its negative control — run the negatives). The remedy meanwhile is the project's own rule: scripts in files (ERR-009). Small. ⟨S130 (2026-09-10/11): three MORE false-deny shapes, same family, each given its own call and then a script in a file — `%es` inside a `time -f` format string; an inline PowerShell `if { … }` block; a `[void][System.…]::` head. A `$R`/`$r`/`"$PY"` variable head is the DESIGNED deny (a head the shell expands is UNREAD by construction) and is not a case. The regression list for the suite is nine false shapes now; SYM-110's first cell carries them.⟩ | MECHANICAL | S129 §8 F5; SYM-110; S128 §10 |
| J65 | **THE WIDGET'S VAULT FETCH IS ONE SERIAL LOOP OF TWO SSH SESSIONS PER CYCLE (S130 §8 F1; `Observed` 2026-09-10/11 from the process feed, receipts in the private repo).** Every cycle the widget opens `tailscale ssh` twice — a `git fetch` of the vault, then a tail of the receiver's receipts — each with its own conhost, plus a local `git rev-list --count`; the loop is serial, so one slow fetch stretches the period (measured period 45.6 s; three fetches of 8–20 s seen in one evening). Cost: ~2 SSH sessions and ~6 processes per cycle, all day, whether or not anything changed. Remedy candidates: one SSH session per cycle (a single remote command returning both), or a receiver-pushed receipt; cadence and shape are Rab's. | MECHANICAL | S130 §8 F1 (private record); `windows-widget/` · `Observed 2026-09-15 (S157 E56)` — RE-MEASURED from this sitting's own tracker receipt (02:03–08:03 local, `proc_events/receipt-20260915-020335.jsonl`): 121 `ssh.exe` sessions under `tailscale.exe` in 6 h = 20/h — 55 `git-upload-pack 'file-portal/vault.git'` fetches + 65 `tail -n 60 ~/file-portal/receipts.jsonl` — WITH THE THINKPAD OFFLINE the whole window: the widget keeps opening two SSH sessions a cycle against a host that is not there. The remedy (one session per cycle, or a receiver push) is a `vault.rs` change → his rebuild; the number today is the argument |
| ~~J66~~ **STRUCK 2026-09-15 (S157 E43) — BUILT, private, with its selftest** | **A `desk` SKILL — the session-side contract for the Desk (S130).** The Desk (private repo, `relay-desk/`) is live: Rab posts from the phone, Fable answers with the CLI under a watcher. Missing is the protocol a session follows: at open, read the inbox and arm the watcher; every reply carries its tag; at close, stop the watcher and say so; the record names what came in. Rab's ask (2026-09-11): *"the desk app also turns on claude desktop app's proc state, so that I can then text claude in a session, to transfer itself to reading from … the desk service and its new inputs"* — the skill is the half a session can do; waking the desktop app from the Desk is J68. | SEMANTIC | S130 §4b, §18 (private record) · `Observed 2026-09-15 (S157 E43)` — the `desk` skill: source `relay-desk/skill/desk/SKILL.md` (private) mirrored to `~/.claude/skills/desk/` (the session loads it; `/desk`); the five obligations (the inbox at open via `desk_inbox.py` — Rab's entries not yet done; the watcher armed and its task id named; a reply TO HIS ID with tag and status; the lane check; stop-and-report at close); `selftest.sh` 6/6 with a negative control (a copy stripped of one obligation reads 4 of 5); `desk_post.py` marks its own posts done (128 closeouts had buried his inbox); the smoke 75/75. J68 (waking a session from the Desk) stays his, plan-mode |
| J67 | **GHOST — the Desk's backend log of every input and output with calls, sequences, episodes, sessions and the model in the seat (Rab, 2026-09-11).** His words: *"log every input and output, with all calls, sequences, episodes and sessions listed … labelling which model or models were control of that session, with them attributed to their specified calls, sequences and episodes."* The tracker's receipts already carry calls/sequences/episodes for a window; the Desk's log carries entries and replies; Ghost is the join keyed by session and occupant, and the attribution of a tracker episode to a Desk reply. Design first (an /echo of what "session" and "in control" mean to him); nothing built. | SEMANTIC | S130 §4b (private record); J66; the translator in `proc-feed/` (private) |
| J68 | **THE DISPATCHER — a Desk post that wakes or re-targets a Claude session on the desktop (Rab, 2026-09-11; plan-mode only, UNSIGNED).** Today the Desk needs a session already open and watching. Rab's ask is the other direction: a post from the phone starts a session that reads the Desk. To settle before a line of code: whether the desktop app exposes a launch/prompt surface a scheduled task could drive; the security shape (a phone post becoming a prompt is docs/14's dispatch-as-Claude-prompt — Tailscale-only, and Rab signs it); the cost of an always-on watcher. Nothing built, nothing signed. | SEMANTIC | S130 §4b (private record); docs/14; J66 |
| ~~J69~~ | ~~STRUCK 2026-09-12T08:27Z~~ **DONE (S132) — `row_check.sh <N>`: [3b]'s own awk over the newest row (case 51 asserts the two bodies byte-identical), the SHA a commit and an ancestor of HEAD; exit 1 blocks the push; selftest cases 48–52; SKILL.md step 4b. Its first live run: S131's row OK, S132's absent row fired.** THE LEDGER ROW IS VALIDATED BY ITS READER BEFORE THE PUSH (S130 post-close; SYM-113, ERR-085). `close.sh` runs before the row exists, so the row's only parser (`muster.sh` [3b]) runs at the NEXT open — S130's row was three cells with the SHA in prose, and the mock open read `no-sha`. Remedy: a `ledger-check` step (a re-runnable `[9]` in `close.sh`, or a two-line script the ledger step calls) that runs the same awk over the newest row and exits 1 on `skip`; a selftest case that plants a three-cell row and asserts the red (a guard born today gets its tripwire today). Small; the parser already exists. | MECHANICAL | S130 §20 addendum; SYM-113; ERR-085 |
| ~~J70~~ | ~~STRUCK 2026-09-12T07:35Z~~ **DONE (S131) — SIGNED by Rab 2026-09-12 ("signed, open S131 and ship the book"): THE NEAR-EXACT GATE MASKS A DEGENERATE REFERENCE BLOCK.** The specimen: *Zero to One* held `cb7e026f3fc02da8` — Marker looped on page 1 (a 2048-char `# INTERNATIONAL PROPERTY AND ROUTE AND ROUTE…` heading, zlib 0.028, trigram 202; the PDF's own text holds no "and route"); the Repair Bench cut it; the analyst gate then read the cut as a 420-word omission (0.9848 → fail) while keeping it failed on degeneration, and `bless` refuses a degeneration fail from either side — no repair could pass (SYM-114). Built: `fidelity_audit._degenerate_blocks` (uncapped, shared), `mask_degenerate_reference` (blanks every flagged block's lines before windowing; records `reference_masked`), `audit_analyst` masks first; docs/15 §12.1; `analyst_audit_selftest.py` (h)–(k) incl. the mask disabled as the watched negative control (12/12); `degeneration_selftest.py` 26/26 unchanged; refuted by a 3-lane fleet (identity · hostile shapes · the specimen). Measured: raw reference 0.9892 + one 420-word run → masked 0.9994, no run → **pass**; `--reaudit --dry-run` fail → pass. | MECHANICAL (built) + SEMANTIC (signed) | S131 §8; SYM-114; docs/15 §12.1 |
| ~~J71~~ | ~~STRUCK 2026-09-12T14:55Z~~ **DONE (S136).** `guard_git.py` `row_check_missing()`: a main-session `git push` whose `@{upstream}..HEAD` diff touches `CLAUDE_README.md` runs `row_check.sh <N>` (N from the session marker); exit 1 DENIES naming the row; everything the guard cannot judge (no upstream, no script, exit 2, an unreadable marker, a timeout) is UNREAD and allows. `guard_git_selftest.py` 155/155: a five-cell row passes, S130's three-cell shape is refused, a lane's push from its own worktree is untouched, a push that does not touch the ledger is not checked, no `row_check.sh` = UNREAD. First live push through it: S136's own close. WIRE `row_check.sh` INTO THE PUSH PATH (S132; the review fleet's finding).** J69 is a documented step (SKILL.md 4b) the closer runs by hand; nothing refuses `git push` when the newest ledger row is malformed or names a foreign SHA. Remedy: `guard_git.py` runs `row_check.sh <N>` (N from `coordination/private/session.current`) before allowing a push whose diff touches `CLAUDE_README.md`, and denies on exit 1 (exit 2 = UNREAD, allow with a warning); a case in `guard_git_selftest.py` that plants S130's three-cell row and asserts the deny. Small; the guard's deny shapes (SYM-110) are the risk to measure first. | MECHANICAL | S132 §8 F2; J69; SYM-113 |
| ~~J72~~ | ~~STRUCK 2026-09-12T21:0xZ~~ **DONE (S140, born the same sitting).** Rab (the Desk, 20:34Z): "all code implementations are written in a bin journal by the agent, explaining what it did by the time it's the sign off — it can be used against the agent by the refuters or by parent"; 20:46Z: "format and file tree absolutely organized and fool proof … scripts according to each category". Built in the PRIVATE library: `auditing/code_journal.py` (every commit of the session's range with its message, every file with +/− and the functions the hunks landed in, the agent's block per file — what · why · what proved it · what it can be held to — from `CLOSE_CODE_NOTES`; UNEXPLAINED named), the journal tree `journal/{calls,code,cost,usage,procs}/S<N>.md` with its README, `auditing/proc_receipt.py`, `auditing/journal_check.py` (five receipts per closed session or exit 1; strays named); the close chain runs the fifth receipt; `SEQUENCES.md` §3 carries the order. Backfilled S136–S139 (the code journals of past sessions carry their commit messages as the why and UNEXPLAINED per-file blocks — honest, not retro-written). | MECHANICAL | S140 §8; `journal/README.md` |
| — | *S133 (2026-09-12T08:4xZ): no item struck, none added — the session paid two S131 residue lines (the re-audit's `repairs_digest` from the bench ledger; the codepage run form) and its close chain pushed past a REGISTER red and a CI red (ERR-088, filed the same hour). This line is the register write the gate reads.* | — | S133 §15, §20 |
| — | *S134 (2026-09-12T09:1xZ): no item struck, none added — the session built the PRIVATE agent-scripts library (Rab's word this sitting; private commit `c17ba74`; the public guard `.gitignore` `agent-scripts/` + a junction). This line is the register write the gate reads.* | — | S134 §15 |
| — | *S135 (2026-09-12T14:xxZ): no item struck, none added — SYM-115 fixed in the symptom index (the leaked `/no_think` is a think-leak rejection); the library's `.gitattributes` (S134 residue). This line is the register write the gate reads.* | — | S135 §15 |
| — | *S136 (2026-09-12T14:55Z): J71 struck (the push hook, live); no item added — the two receipts (cost, scripts-used) and the projects census are in the private library and the record. This line is the register write the gate reads.* | — | S136 §15 |
| — | *S137 (2026-09-12T15:4xZ): J64 struck (the guard's grammar, 17 cases); no item added — SYM-116 filed and fixed in the same commit; ERR-089/090 filed. This line is the register write the gate reads.* | — | S137 §15 |
| — | *S138 (2026-09-12T16:1xZ): no item struck, none added — the Readability lane over the briefs was S134's owed review, not a J item; its 19 findings, `brief_check.py` and SYM-117 live in the private library and the record. This line is the register write the gate reads.* | — | S138 §15 |
| — | *S139 (2026-09-12T17:4xZ): no item struck, none added — Rab's commission (the tool census, every script exercised and fixed, the call sequences, the script journal) lives in the private library (`SEQUENCES.md`, `tool_census.py`, `library_smoke.py`, `script_journal.py`) and the record; SYM-119 filed and fixed; ERR-093. This line is the register write the gate reads.* | — | S139 §15 |
| — | *S140 (2026-09-12T19:5xZ): J46, J42 and J72 struck (J72 born and built the same sitting: the code journal + the journal tree) (the acceptor built, reviewed and proved on Zero to One; the sidecar-aware --reanalyze as its road); no item added — the renderer for `analyst.edits` and the ladder's hyphen blindness are Rab's calls in the record's §18, not tickets yet. This line is the register write the gate reads.* | — | S140 §15 |

## TICKETS (named) — S141, 2026-09-13: the lettering is retired

**Rab, 2026-09-12 (the Desk, b1796936): the A–J lettering is retired; new work is a named ticket `<category>/<verb-object>`
whose name says what it works towards.** The bodies (born of · works towards · change · contracts touched · what an agent
is handed · acceptance · tripwire · Goodhart pair) live in the private `measurement/TICKETS-2026-09-12.md`; this table is the
public register row: id, goal, status. A done ticket's id is ~~struck~~ (the census counts it like a lettered id); the
lettered rows above keep their ids until struck; no new letter id is minted after this line. `open.sh` [2b] counts
open named rows with the lettered ones; `close.sh` [8] counts struck and added named ids the same way.

| id | works towards | status | bodies |
|---|---|---|---|
| ~~truthful-numbers/classify-calls-by-head~~ | the calls journal's kind column counts what RAN, not what was named | done S141 E1 | private `measurement/TICKETS-2026-09-12.md` |
| ~~truthful-numbers/read-harness-exit-code~~ | the result column is the harness's exit code, not a substring | done S141 E1 | private `measurement/TICKETS-2026-09-12.md` |
| ~~truthful-numbers/journal-the-lanes~~ | a fleet's calls and denies are in the journal, attributed to their lane | done S141 E1 | private `measurement/TICKETS-2026-09-12.md` |
| ~~truthful-numbers/census-classify-by-run-not-text~~ | "N tools" counts programs that run | done S141 E1 | private `measurement/TICKETS-2026-09-12.md` |
| ~~truthful-numbers/recompute-cost-by-second-reader~~ | the cost receipt's "calls" and totals are VERIFIED, not DERIVED | done S141 E1 | private `measurement/TICKETS-2026-09-12.md` |
| ~~provenance/stamp-producer-and-sources~~ | every receipt header carries producer path + `git hash-object` blob + private HEAD + interpreter + harness version + source file(s) with bytes and sha256 at gen… | done S141 E2 | private `measurement/TICKETS-2026-09-12.md` |
| ~~provenance/extend-dump-ledger-not-invent-manifest~~ | one ledger family for hashed evidence — the receipts' manifest is `dump.sh`'s row shape with three new columns (producer blob, sources, coverage), written per c… | done S141 E2 | private `measurement/TICKETS-2026-09-12.md` |
| ~~provenance/coverage-lines-and-datasheet~~ | every receipt says what it did NOT observe | done S141 E2 | private `measurement/TICKETS-2026-09-12.md` |
| ~~provenance/occupant-per-session~~ | the occupant on the ledger row is CORROBORATED by the transcript every close | done S141 E2 | private `measurement/TICKETS-2026-09-12.md` |
| ~~corrections/ledger-and-r2-files~~ | a corrected receipt is a NEW file `S<N>-r2.md` `wasRevisionOf` the old | done S141 E3 | private `measurement/TICKETS-2026-09-12.md` |
| ~~corrections/instrumentation-bug-register~~ | instrumentation bugs are rows like SYM rows: id, instrument, wrong reading it produced, sessions affected, fix commit, receipts revised | done S141 E3 | private `measurement/TICKETS-2026-09-12.md` |
| ~~typed-failure/five-codes-in-policy~~ | `0` yes/done · `1` measured red · `2` usage/config · **`3` nothing found** · **`70` crash** — five codes, the deviation from sysexits stated | done S141 E4 | private `measurement/TICKETS-2026-09-12.md` |
| ~~cross-checks/journal-denies-equal-guard-log~~ | `journal_check --content`: guard_git DENIED rows (main) == `git-guard.log` DENY rows `agent=-/-` in the window (tolerance 0, one clock) | done S141 E5 | private `measurement/TICKETS-2026-09-12.md` |
| ~~cross-checks/tool-version-at-call-time~~ | a preloaded call between an uncommitted edit of that tool and its commit is FLAGGED in the journal (`moving-target`), from the transcript alone | done S141 E5 | private `measurement/TICKETS-2026-09-12.md` |
| ~~cross-checks/code-journal-paths-in-commit~~ | every explained path is in that commit's `--name-only` | done S141 E5 | private `measurement/TICKETS-2026-09-12.md` |
| ~~cross-checks/procs-join-via-receipt-to-tokens~~ | the procs receipt states `covered / matched / sub-grid-possible` for preloaded runs, from the existing joiner, with the grid as the stated tolerance | done S141 E5 | private `measurement/TICKETS-2026-09-12.md` |
| ~~guard-holes/cd-tilde-and-cd-dash~~ | an unresolvable or unknown `cd` target makes the rest of the command UNREAD → deny, the same as `$VAR` | done S141 E6 | private `measurement/TICKETS-2026-09-12.md` |
| ~~guard-holes/row-check-with-no-upstream~~ | a push with no resolvable upstream is UNREAD → the row is checked against the ledger file itself (`row_check.sh` needs no upstream), not skipped | done S141 E6 | private `measurement/TICKETS-2026-09-12.md` |
| ~~guard-holes/guard-record-selftest-shapes~~ | every matcher tool has a payload case | done S141 E6 | private `measurement/TICKETS-2026-09-12.md` |
| ~~guard-holes/retire-codex-hook-copy~~ | one copy of every hook | BLOCKED on Rab (delete vs junction; Desk eac27c03) → done S143 E2: a junction (two files backed up; one had drifted by a docstring line) | private `measurement/TICKETS-2026-09-12.md` |
| ~~unread-surfaces/read-the-transcript-system-rows~~ | the calls journal gets a **harness** section: retries, compactions (with their times — a coverage boundary), fallbacks, hook p50/p95, advisories count | done S141 E7 | private `measurement/TICKETS-2026-09-12.md` |
| ~~unread-surfaces/tool-runtime-from-transcript~~ | per-tool and per-script runtime p50/p95 in the usage receipt (mandate §6), from A alone, with the tolerance stated (result time = when the harness wrote the res… | done S141 E7 | private `measurement/TICKETS-2026-09-12.md` |
| ~~unread-surfaces/desk-server-log-format~~ | the Desk writes its access log as UTF-8 JSONL (`data/access.jsonl`), swallows 10054 as a counted event, and `boot.log` keeps its "address present after 91 s" la… | done S141 E7 | private `measurement/TICKETS-2026-09-12.md` |
| ~~unread-surfaces/watcher-stuck-drop-file~~ | the watcher moves sources by a handle it can resolve (`\\?\` long-path + the exact bytes it listed), logs a `move_failed` EVENT (not a bare traceback), and `pip… | done S141 E7 (the cause was not the colon; ERR-100) | private `measurement/TICKETS-2026-09-12.md` |
| ~~unread-surfaces/orphan-chunk-journal-dump-before-rmtree~~ | a chunk journal is ledgered (`dump.sh --category evidence`) BEFORE rmtree, every time | done S141 E7 | private `measurement/TICKETS-2026-09-12.md` |
| ~~unread-surfaces/scheduled-tasks-on-the-card~~ | `open.sh` prints the three tasks' Status / Last Run / Last Result (with `MSYS2_ARG_CONV_EXCL=*` — the lane's own trap) as a card row | done S141 E7 | private `measurement/TICKETS-2026-09-12.md` |
| unread-surfaces/workflow-journals-and-widget-boot-log | the calls journal's Workflow rows say `replayed-from-cache` when the key matched a prior run | journal half done S141 E7; widget half DEFERRED to a rebuild slot Rab names | private `measurement/TICKETS-2026-09-12.md` |
| ~~services/tracker-as-scheduled-task~~ | `proc_events.ps1` runs as `File Portal tracker` at logon, gated on `coordination/private/session.current` (present → record | built S141 E8; REGISTRATION on Rab's word (Desk eac27c03) → done S143 E2: registered Ready with his two lines as the consent, not started (arms at the next logon) | private `measurement/TICKETS-2026-09-12.md` |
| ~~services/desk-watcher-as-task~~ | `File Portal desk watch` at logon, writing to `data/watch.jsonl` | built S141 E8; REGISTRATION on Rab's word (Desk eac27c03) → done S143 E2: registered Ready with his two lines as the consent, not started (arms at the next logon) | private `measurement/TICKETS-2026-09-12.md` |
| ~~baseline-experiments/baseline-s141-s145~~ | `measurement/baseline/S<N>.json` per sitting (the corrected kinds incl | done S141 E9 (rows through S145 accrue at each close) | private `measurement/TICKETS-2026-09-12.md` |
| ~~baseline-experiments/h-001-sequences-pre-registered~~ | `measurement/hypotheses/H-001-sequences.md` committed BEFORE its window, RESULT empty until S150 | done S141 E9 (RESULT read S146–S150) | private `measurement/TICKETS-2026-09-12.md` |
| ~~retirement/census-columns-and-first-candidates~~ | `tool_census.py --list` gains `born` (`git log --diff-filter=A`), `uses` (direct + nested — `scripts_used.py` learns to read the chain's own log lines), `last u… | done S141 E10; seven candidates on Rab's word (Desk eac27c03) | private `measurement/TICKETS-2026-09-12.md` |
| ~~contracts/policy-and-sequences-rewrite-pass~~ | one pass over POLICY.md (§2 gains two brief headings: WHAT CAN MAKE IT WRONG · WHERE IT HAS FAILED; §5 five codes | done S141 E11 | private `measurement/TICKETS-2026-09-12.md` |
| ~~contracts/register-naming-transition~~ | `OPEN-TASKS.md` gains a section "TICKETS (named)" with one row per ticket id + goal + status, pointing at this private file for bodies | done S141 E11 (this section) | private `measurement/TICKETS-2026-09-12.md` |
| thinkpad/map-receiver-and-exporter-surfaces | the map's row for the ThinkPad: the receiver's log, the exporter's receipts, `held/`'s state, the seam receipts, the vault repo's log — each with producer, P/D,… | HELD (written S141 E12, run when Rab says test) | private `measurement/TICKETS-2026-09-12.md` |
| thinkpad/chunk-journal-and-held-state-provenance | a `held/` bundle carries a ledger row (what was held, why, the verdict, the sha) in the vault's own `LEDGER.md` shape (dump.sh's row) — so "what did the ThinkPa… | HELD (written S141 E12, built when Rab says test) | private `measurement/TICKETS-2026-09-12.md` |
| ~~held-books/triage-the-nine~~ | nine held bundles, each with its score at hold, its score under today's audit, and a decision with the command | done S142 E1 (decisions; the executions on Rab's word) | private `measurement/TICKETS-2026-09-13.md` |
| ~~held-books/damodaran-two-copies~~ | two editions are two books by the dedup key (`source_sha256`); neither held without a stated reason | done S142 E1 | private `measurement/TICKETS-2026-09-13.md` |
| ~~held-books/cjk-bundle-disposition~~ | retire the held copy: a 19-page front-matter PDF, one dropped 29-word span; no chunk scores exist to cite | done S142 E1 (the retire on Rab's word) | private `measurement/TICKETS-2026-09-13.md` |
| ~~vault-moves/reship-zero-to-one-pass~~ | the held pass copy blessed and superseding the vault's earlier copy, seam receipts on both ends | done S144 E4 as a NOOP, verified: the vault has held the pass body since 2026-09-12 07:43Z (`8d7cc85`, the FIRST reship); today's `--reaudit` ship nested inside the held-fail staging copy (SYM-128), was moved up by hand, and the exporter answered `supersede-noop` (byte-identical; receipt 21); the held-fail copy set aside as a dot-dir in staging | private `measurement/TICKETS-2026-09-13.md` |
| bench-doctrine/one-good-repair-recorded | one repair at the bench recorded end to end, its audit credit counted | WITH RAB | private `measurement/TICKETS-2026-09-13.md` |
| ~~guard-debt/sym-110-twelfth-shape~~ | the twelfth false-deny shape as a selftest case that was red against the pre-fix guard (178/181), green after (181/181) | done S142 E2 | private `measurement/TICKETS-2026-09-13.md` |
| ~~instruments-frozen/rows-only~~ **STRUCK 2026-09-15 (S157 E40) — EXPIRED by its own dates — struck S157 E40** | no new tool, no new law through S145; only `baseline.py <N>` at each close | standing S142–S145 | private `measurement/TICKETS-2026-09-13.md` · `Observed 2026-09-15 (S157 E40)` — "no new tool, no new law through S145" — S145 closed 2026-09-13; every instrument since (S147's bench layout, S150's layer, S157's four promoted tools and two probes) was built under his later words ("build the new analyst…", "all signed", "run autonomous"); the doctrine was time-bound and its window is past |
| ~~sequencing/compile-preload-brief-policy~~ | every ticket of the sitting sequenced, with PRELOAD (scripts by literal path) and BRIEFS (POLICY §2) slots | done S143 E1 | private `measurement/TICKETS-2026-09-13-S143.md` |
| ~~retirement/six-candidates~~ | the census's RETIRE? rows deleted, their briefs to `retired/` (six today: `bundle_audit.py` left the S141 list because S142 E1 ran it) | done S143 E2: six retired (98 tools / 29 briefs after); the checker learned `retired/` (IB-014) | private `measurement/TICKETS-2026-09-13-S143.md` |
| ~~contracts/persistence-as-policy~~ | the persistent method (a sitting closes in parts; each category an episode) in POLICY, the muster SKILL, docs/21 and a memory note | done S143 E2: POLICY §9 · muster SKILL · docs/21 §2 · a memory note | private `measurement/TICKETS-2026-09-13-S143.md` |
| ~~research/pdf-standard-vs-our-errors~~ | our conversion errors against ISO 32000-2, PDF/A, PDF/UA and the OCR literature — a cited, tagged research record | done S143 E4 (first version; the standard's own text UNREAD to the fetcher, said) | private `measurement/TICKETS-2026-09-13-S143.md` |
| ~~ml-gauge/scan-omission-observability~~ | scans-only PDFs: fewer omissions, more variability, more observability — six instruments with their denominators; first ground truth on Valentine's scan (465 pages, no JBIG2, ink median 4.2 % at 200 dpi) | done S143 E5 (the second witness and the GPU pass on Rab's word) | private `measurement/TICKETS-2026-09-13-S143.md` |
| ~~desk/two-boards-lane-switch~~ | the Desk has two switchable boards (orange Claude · greyish-white Codex); the switch selects who receives a post and where (`data/desk-codex.jsonl`) | done S143 E3 (a 3-lane Sonnet fleet; selftest 32/32; live 07:16Z; first Codex entry 20b6cc4a) | private `measurement/TICKETS-2026-09-13-S143.md` |
| ~~relay/pace-and-map-for-codex~~ | Codex told to pace itself (Rab's word) and given the reciprocal read-only map; its answer carried to Rab | done S143 E3 (MSG-FAB-0087/0088/0089; Codex's 0066/0067) | private `measurement/TICKETS-2026-09-13-S143.md` |
| ~~relay/watcher-scripts-private~~ | `watch_relay.sh` and `watch_desk.sh` in the private library with briefs; Codex led to them by path (Rab, Desk 795574c2) | done S143 E3 (two tools inside the frozen window, on Rab's word) | private `measurement/TICKETS-2026-09-13-S143.md` |
| ~~held-books/retire-cjk-copy~~ | the CJK held copy and its nine anchor duplicates MOVED to `library/retired/{held,anchor}/` (never deleted; ledgered first, D0092) | done S144 E1 (held 9 → 8, anchor 31 → 22) | private `measurement/TICKETS-2026-09-13-S144.md` |
| ml-gauge/valentine-marker-pass | Valentine's scan converted under today's Marker (block boxes + a sidecar), then the ink-coverage instrument read per page | open (S144 E2, on the card) | private `measurement/TICKETS-2026-09-13-S144.md` |
| held-books/reconvert-four | Ashby · Beer's Diagnosing · Damodaran 4e · Damodaran University Edition re-converted with the promoted acceptor, one GPU process at a time, scored beside their S142 numbers | open — 1 of 4: Ashby re-converted S144 E5 (14.8 s/page; the audit at birth as August's, fail; SHIPPED under `audit-mode.txt = report` and INGESTED by the exporter's guard-less first-ingest path — SYM-130; the vault note 54b471d7 is Rab's call); Beer's run stopped by hand at 11:33Z before its verdict; the two Damodarans on Rab's word (measured ~5.6 h each at today's rate) — all three to run only after the lever reads `enforce` or Rab says otherwise · `Observed 2026-09-15 (S157 E28)`: the prose-completeness probe (`measurement/probes/prose_loss.py`) puts numbers on why: the two University Edition bundles (08-31, 09-01, pre-acceptor) each lost DIFFERENT whole sections at the analyst stage (588, 576, 432 … words; p.1174's postscript body gone under its heading) — 349 and 277 prose lines missing of 27,087; the 4e (2025) bundle 145; today's acceptor reverts the class (edit_whitelist_selftest 54/54) · `Observed 2026-09-15 (S157 E29)`: the BEFORE exists — `sittings/S157/e29/<sha8>/` (prose_loss per page with the lines): IV 4e held 349 missing / 20 pages (p.1151's postscript gone), IV UE held 277 / 20, Ashby held 231 / 27, DDIA held 131 / 10; the post-acceptor Zero to One (09-12) reads 1 missing line of 3,383 | private `measurement/TICKETS-2026-09-13-S144.md` |
| ~~ml-gauge/second-witness-tesseract~~ | Tesseract 5 installed (Rab's word) in user scope + a separate venv; the agreement instrument read on Valentine's fresh output | done S144 E3 (Tesseract 5.4.0 by Rab's hand; a separate venv; the agreement instrument read on the HELD body: pair agreement 0.9300 over 442 of 465 pages, control 0.2524; the fresh-body reading follows Valentine's pass) | private `measurement/TICKETS-2026-09-13-S144.md` |
| ~~audit/verdict-weighs-denominator~~ | `compute_verdict` never passes a convert block whose `pages_scored / pages` is under a floor — flag, with the reason | done S144 E2 (`WITNESS_COVERAGE_FLOOR = 0.50` in `compute_verdict`; docs/15 §12.3; selftest case (m), 14/14) | private `measurement/TICKETS-2026-09-13-S144.md` |
| ~~transport/ship-replaces-held-staging-copy~~ | `ship()` never nests: an existing `staging/<name>` (a held copy the exporter kept) is replaced or the ship refuses and says so; a tripwire ships onto an existing name and asserts the exporter reads the NEW manifest | done S146 E4 — `ship()` moves an existing staging name aside (`.held-<sha16>-<UTC stamp>`, or exit 97) before the final mv; nothing deleted; the `shipped` event names the displaced copy (`replaced_staging`); T22 a–e; the registry regenerated; a live read-back waits for the next real ship | private `measurement/TICKETS-2026-09-13-S144.md` |
| ~~analyst/bound-chunk-generation~~ | the analyst's request carries a `num_predict` bound near 2× the chunk's token count and a runaway is a named rejection reason (`runaway`), so one looping chunk costs about a minute, not the 900 s client timeout; a tripwire feeds a never-ending fake backend and asserts the bound fires | done S146 E5 — `num_predict` = 2x the chunk's estimated tokens (floor 512, cap NUM_CTX; above the 1.5x reject) on every local call; a `length` stop is rejected as `truncated`, the original ships, the reason counted; S146-E5 (a)–(d); the registry regenerated; a live read-back waits for the next analyst run | private `measurement/TICKETS-2026-09-13-S144.md` |
| ~~exporter/ingest-verdict-guard~~ | the ThinkPad exporter's FIRST-INGEST branch reads `fidelity.verdict` and refuses a `fail` the way the supersede branch does (`EXPORT-INGEST-HELD … verdict 'fail'`, staging copy kept); a tripwire ships a fail fixture into staging and asserts the refusal and the receipt | done S146 E6 — the first-ingest path refuses an explicit `fail` (`EXPORT-INGEST-HELD`, receipt `ingest-held`, staging kept); `flag`/missing still ingest; four tests, 38 passed on the ThinkPad; deployed: the ThinkPad's checkout pulled to cc64dd2 (from S126's 119db7d), its suites 38 and 90 passed on the real checkout, `file-portal-converter.service` restarted 18:28:43Z and watching staging again (MainPID 29195); the undo is a checkout of 119db7d and the same restart | private `measurement/TICKETS-2026-09-13-S144.md` |
| ~~pipeline/audit-mode-provenance~~ | the audit-mode lever has provenance on the WRITE side (the card already prints it — C-024): a write to `audit-mode.txt` emits an event naming the writer and the time; every `shipped`/`held` event carries the mode the converter read | done S146 E2 — `set_audit_mode()` + `--set-audit-mode/--writer/--reason`: the one writer, the event `pipeline/audit_mode_set` with old/new/writer/reason/mtimes, refusals before writing (T12 a–h, 224/224); `shipped`/`held` name the mode; the lever flipped to `enforce` through it at 17:45:29Z on Rab's word bdc9415c (the undo: the same call with `report`) | private `measurement/TICKETS-2026-09-13-S144.md` |
| ~~muster/read-the-levers-aloud~~ | the open card prints its own `SHIPS ON VERDICT` line whenever `audit-mode.txt` is not `enforce`; the muster's Phase 5 carries the standing order (the levers row quoted in the open reply and named again before any launch that can ship); `selftest.sh` fires on a `report` fixture and stays quiet on `enforce` | done S145 E1 (the card's `SHIPS ON VERDICT` line; the standing order in SKILL.md Phase 5; selftest cases fire/quiet) | private `measurement/TICKETS-2026-09-13-S145.md` |
| ~~measurement/slice-falsifier~~ | a 100-page range through the sidecar (a measurement, no ship) timed beside the 50-page ranges (12.7 s/page) and the 200-page slice that ran past 20 s/page — the number behind the converter's scan-lane slice change (Rab's word) | done S145 E3 — the falsifier FALSIFIED: 100 pages in one run 10.0 s/page (one load) vs the 50-page ranges' 12.7 (a load each); the range's size is not why the 200-page slice died; the smaller-slice recommendation retracted; the cause moved to the card's occupancy that morning (9.5 GB vs 5.6–5.9 GB) — the ollama log CLEARS the analyst model (no load, no generate in the window), so `measurement/slice-200-direct` (E4: the exact 200-page slice through the sidecar) is the probe left; no converter change recommended for the slice yet | private `measurement/TICKETS-2026-09-13-S145.md` |
| ~~ml-gauge/i3-by-block-type~~ | the ink-coverage instrument split by Marker's block type over the Valentine artifact (figure / table / text per exhibit page); the `lever-waiver` comment beside `WITNESS_COVERAGE_FLOOR` naming Rab's word bf4d5d05 | done S145 E3 (I3 by block type over the Valentine artifact; the waiver comment beside the floor) | private `measurement/TICKETS-2026-09-13-S145.md` |
| ~~measurement/slice-200-direct~~ | the converter's EXACT 200-page slice (Valentine pages 0–199) through the sidecar directly on the free card, its wall, s/page and peak VRAM beside the 100-page probe (10.0 s/page, 5.9 GB) and the converter's own run (past 20 s/page, 9.5 GB): does 200 pages alone reproduce the death? | done S145 E4 — the exact 200-page slice through the sidecar directly: 1,701 s = 8.5 s/page, peak 5,843 MiB (sampled), against the converter's own run of the same slice (past 20 s/page, 9,515 MiB, killed): the direct runs do NOT reproduce the death; ollama unloaded by the launcher (OK-16), no stream/game process in the tracker's window; the cause is UNREAD and the probe left is the converter's own run under a memory sampler; no slice or budget change recommended | private `measurement/TICKETS-2026-09-13-S145.md` |
| ~~ml-gauge/evidence-card~~ | one per-page table over Valentine joining the four instruments' CSVs (second witness, two-rendering disagreement, ink coverage by text/any block, dominant block type) with the instruments' own flags, the pages worth an eye first; every column names its numerator/denominator | done S145 E5 (`scan_evidence_card.py` → `measurement/evidence/valentine-evidence-card.{md,csv}`: 465 pages, 442 scored, flags I1-LOW 47 · I4-UNSTABLE 11 · MISSING 8 · REORDER 32 · TYPED-EXHIBIT 32; the exhibits first; an instrument defect in its first cut caught and gated) | private `measurement/TICKETS-2026-09-13-S145.md` |
| ~~ml-gauge/evidence-overlays~~ | for the flagged pages: the page at 150 dpi with Marker's block boxes by type and the disagreeing words boxed — the map as a picture; one PNG a page + a contact sheet, linked from the card | done S145 E6 (`evidence_overlay.py`: the flagged pages rendered with Marker's blocks by type and the disagreeing words boxed; one PNG a page + a contact sheet under `measurement/evidence/valentine-overlays/`) | private `measurement/TICKETS-2026-09-13-S145.md` |
| ~~ml-gauge/surya-confidence~~ | surya's per-line confidence read out for the flagged pages (I2, the last unread instrument) as a column on the card; uncalibrated until a scan bench session | done S145 E7 (`surya_confidence_probe.py` on the twelve flagged pages: the dot-matrix checklists 134/133 at median confidence 0.66 with 87/104 lines under 0.8 — the engine itself unsure; the flowcharts, Contents, chart and tables at 0.96–0.997 — confident reads whose losses are layout; a column on the card; uncalibrated until a scan bench session) | private `measurement/TICKETS-2026-09-13-S145.md` |
| ~~measurement/converter-run-sampled~~ | the converter's OWN run of Valentine as a dry-run (converts and anchors, does not ship) under a 30-second memory sampler — the reproduction probe E4 left: does the converter's run reach 9.5 GB and the 20 s/page budget where the direct runs of the same slice did not? | done S145 E8 — the converter's OWN run of Valentine (dry-run, sampled): the slice that died this morning (0–199) COMPLETED inside the converter at 1,698.6 s against the direct run's 1,701 s — the same recognition curve at every checkpoint, steady 5.6 GB, one 9,256 MiB sample 65 s in; THE CONVERTER'S RUN IS THE DIRECT RUN — the death is not in the converter; the morning's kill row (100 % at 9,515 MiB) is the card's ceiling with a tenant no instrument named, and the S144 tracker's receipt holds no row for the pass itself (IB-018; E4's 'no stream/game' reading downgraded to UNREAD — C-025, ERR-104; SYM-132); the run continued into slices 2–3 (anchors, ships nothing); no slice or budget change; the residue → `converter/gpu-signature-per-process` | private `measurement/TICKETS-2026-09-13-S145.md` |
| ~~converter/gpu-signature-per-process~~ | `_gpu_signature()` reads the card's total (`nvidia-smi memory.used`), so a kill at the ceiling — the morning's `timeout` row: 100 % util, 9,515 of 10,240 MiB — cannot name its co-tenant; E8 ran the same slice in the converter's own run at 5.6 GB steady with the direct run's exact recognition profile, so what the morning held beyond the pass was a second tenant the signature cannot see (the tracker lists births, not memory). Add the WDDM per-process `Total Committed` (the `GPU Process Memory` counter set — `Dedicated Usage` drifts, 11 GB on a 478 MB process, and is not a reading) to the signature and to the `timeout`/`stalled` events; the measurement half first: the counters sampled through a direct run's first two minutes, where E8 read 9,256 MiB at the detection→recognition handoff | done S146 E3 — `_gpu_top_committers()` + `_gpu_signature(with_processes=True)` + `_note_ceiling()` at 0.80 of the card: the death certificates and the slice event name WHO held the card and when (T21 a–k, 235/235); the registry regenerated; the measurement half: the ceiling moment is the sidecar's OWN load transient — at +27–30 s of a direct 50-page run the sidecar commits 7,999 MB (the card 9,154–9,232 MB resident, the desktop's 1.2 GB beside it, no other tenant moved) for ~10 s, then 4,403 MB steady through recognition; both runs 333 / 332 s for 50 pages (6.7 s/page); the morning's 9,515 MiB at 4,003 s is not this transient and stays UNREAD until the next ceiling event names its split | private `measurement/TICKETS-2026-09-13-S145.md` |
| ~~journal/autonomy-experiment~~ | the journal Rab reads (`sessions/JOURNAL-autonomy-2026-09-13.md`: one dated entry per improvement — what he will notice, where to look, before/after with denominators, cost, the undo) and the vision beside his (`docs/60-the-factory-that-audits-itself.md`) | done S146 E1 (the journal `sessions/JOURNAL-autonomy-2026-09-13.md` with its first five entries; the vision `docs/60-the-factory-that-audits-itself.md` — eight additions, each with a measurable and a first ticket; F1: a scan-lane book cannot pass today's audit, its agreement witness is the embedded OCR layer a scan does not have → `audit/scan-lane-second-witness`) | private `measurement/TICKETS-2026-09-13-S146.md` |
| ~~measurement/ceiling-moment-per-process~~ | the WDDM `Total Committed` per process sampled at 1 s through a direct 50-page run's first two minutes — which stage and which process own the 9,256 MiB moment E8 read at +65 s (the measurement half of `converter/gpu-signature-per-process`) | done S146 E3a — the ceiling moment is the sidecar's OWN load transient — at +27–30 s of a direct 50-page run the sidecar commits 7,999 MB (the card 9,154–9,232 MB resident, the desktop's 1.2 GB beside it, no other tenant moved) for ~10 s, then 4,403 MB steady through recognition; both runs 333 / 332 s for 50 pages (6.7 s/page); the morning's 9,515 MiB at 4,003 s is not this transient and stays UNREAD until the next ceiling event names its split | private `measurement/TICKETS-2026-09-13-S146.md` |
| ~~vault/ashby-note-54b471d7~~ | the failing Ashby body that reached the vault under `audit-mode=report` (S144 E5): revert or keep, decided on the numbers (the shipped body's audit beside the held copy's) and written before the act | done S146 E7 — REVERTED on the numbers (the same body the machine held on 2026-08-25: survival 0.8582, 149/156 scored, 50 flagged, degeneration true; it entered through the lever and the ingest gap): vault commit f489189 on the ThinkPad's work clone, pushed to the bare, Rab's Obsidian clone fast-forwarded; the held and anchor copies keep the body; the undo a revert of f489189 | private `measurement/TICKETS-2026-09-13-S146.md` |
| ~~library/reconvert-dryruns-sampled~~ | the re-conversions one at a time as dry-runs that anchor (Beer; then a Damodaran ~5.6 h) with the VRAM and WDDM samplers beside them; the lever read aloud before each | done S146 E8 (Beer) — Beer converted inside the converter in 868.4 s (4.72 s/page; the audit fail on a true degeneration line; anchored, not shipped); the card at 9,480–9,565 MiB through recognition and the per-process sampler named the owner — the sidecar itself, 8,321 MB committed at batch 8 (Valentine's 4.4 GB), no neighbour: the morning's death signature with its tenant; the `converted` event now carries the ceiling (a gap found live, T21 l/m); the Damodarans (~5.6 h each) wait for a sitting that can hold the card | private `measurement/TICKETS-2026-09-13-S146.md` |
| converter/ceiling-crawl-triggers-the-ladder | the recovery ladder (OK-17) retries a slice at batch 4 only on a FROZEN progress (STALL_FROZEN_S 900 s) or after the budget's timeout; a run that CRAWLS at the card's ceiling (Beer: the sidecar 8,321 MB committed through recognition at batch 8, the card 9.5 GB) is neither — it pays the whole budget and dies the morning's death; the ceiling moment `_note_ceiling` already records (≥ 0.80 of the card) plus a recognition rate under a floor for N samples should trip the ladder early; the number behind it: Beer's rate at 9.5 GB vs the direct runs' at 5.6 GB | open (S146 E8's residue; product code, with its tripwire; the measurement first) | private `measurement/TICKETS-2026-09-13-S146.md` · `Observed 2026-09-15 (S157 E58)` — read: the ladder lives in `convert_and_ship.py` (the field's, never-edit this sitting) and 'the measurement first' is a run on the card (Beer at 9.5 GB vs the direct runs at 5.6 GB) — his card. Waits for both; nothing the lane can measure without them |
| ~~intake/watcher-restart-onto-current-code~~ | the live watcher (pid 13572, pre-S141 code) stopped by its own control and the current code started under the same settings; intake proven by the smoke and one drop; the old command line kept for the revert | DONE S147 E0 — by Rab's hand, S146's E9 closed at 19:27Z on 2026-09-13: asked in chat what to press, answered from the widget source (the titlebar's ⏻, `#watcher-btn`: click, wait five seconds, click); "done" 19:27:24Z; read back Observed 19:27:38Z — pid 13572 gone, the new watcher pid 16872 (trampoline 15368, parent the widget 8076, started 19:27:03Z, today's `watch_and_convert.py`), the intake state file written by 16872. History kept: open — attempted S146 E9 (18:45Z): the widget's ⏻ is the watcher's only control (the widget owns the pid); the screen-control request was declined (`user_denied`) and taken as Rab's word; a shell restart would leave a watcher that dies with the session and a ⏻ reading stopped, so not done; the watcher spawns the current converter per drop — only its own loop code is old; two clicks on the ⏻ (his) or a later grant close it | private `measurement/TICKETS-2026-09-13-S146.md` |
| ~~bench/stream-gestures-on-his-phone~~ | S148 (dab27e3): the Repair Bench's handles answer the stream — the wheel over a handle resizes it, two tap-steppers on every handle, an 18-px hit strip — built for Rab's Desk report ba04c67f (on the Moonlight stream a drag up or down did nothing, a scroll worked); every gesture Observed in the Browser pane by synthetic events; NOT yet felt on his phone over the stream | DONE S149 E1 — by his hand and his word (chat, 2026-09-13 ~23:4xZ): "the drag works! Perfectly!" on the phone over the stream; his ea93b672 asked for the steps without inference and got them; the loupe checked live the same night. History kept: open — his hand: one line on the Desk after he tries the two-finger scroll and the arrows on the phone closes it (Observed there, not here); if it fails there, the stream's touch mode is the next probe (a one-finger drag as a cursor move is Inferred) | sessions/S148-desktop-2026-09-13.md E1–E3; the journal entry 15 |
| ~~bench/repair-inside-a-table~~ | S149 (Rab's Obsidian discovery, 2026-09-13 ~23:5xZ): a crop, paste or transcription is inserted directly after the zone's line (`bench.py` repair(): `lines[at:at] = ["", embed, comment]`); when that line is a pipe-table row the table is split — Valentine's Exhibit 8.2 (continued) has carried the 2026-08-06 crop between its header row and its delimiter row ever since, and no renderer shows it as a table (Observed: the pre-repair backup renders as 16 rows, the live copy as none; the only such site in 33 library files). The bench showed the lines as fine; only a renderer showed the break | DONE S154 E3 (2026-09-15, Rab's `all signed` = the clear): the un-split of the held Valentine copy b6fbdd75f6242f53 run (`bench.py <id> --unsplit-tables`, no server): ONE block moved — the embed `_repair_p234_1.png` from inside the table at body lines 1579–1595 (the clean numbering) to after its last row, the record re-anchored (a record from before OK-0, no id); one gesture, two ledger rows (removal + addition, seq 8–9, 02:46:39Z), body sha 616b310f → 8e4e3553. The condition measured first: the widget's bench server on that bundle was alive since 2026-09-14 04:34Z (his click) but idle — no client on its port, last gesture 2026-08-15 — and the bench caches the manifest at start, so its next gesture would have overwritten the re-anchoring; the process tree was stopped from the shell (every gesture is persisted; he reopens with one click: the Room's 🔧 on the held row, or the Repair Bench surface button). Measured on a fresh dry run of the held copy (resolver none, chunks none): the census tables 80 → 81, orphan runs 2 → 0, orphan rows 17 → 0; the scorer on the 14 truths — p133-t1 held NO TABLE → table, TEDS 0.938 → 0.979, the other 13 unchanged, mean TEDS 0.730 → 0.733. Residue: the S149 guard (an insertion never lands inside a table) is in the bench already; this was the one trapped block in the one bundle known to carry one — the other held bundles were not scanned (the un-split is idempotent and prints moved == [] where nothing is trapped). History kept: open — B signed ("B then C"): the insertion lands after the table's last row and the record says so; an un-split through the ledger for Valentine; a viewer mode in the pane; then C the table-health check | sessions/S149-desktop-2026-09-14.md §1, E1 |
| ~~analyst/table-geometry-lever~~ | S150 (Rab, 2026-09-14: "build the new analyst, test that, and see what we gained, and lost, and what is still missing"): the table-geometry LAYER (`windows-converter/table_geometry.py`: a spanning title → a caption, a rotated rail's letters → the word on the run's first row, stray bullet glyphs → •, every repair held to `grid_invariant`) and the CLASS (`edit_whitelist.FULL_TABLES`, rung `table-geometry`: a table judged whole, accepted as one edit or restored whole — which also closes the markup rung's hole where a dropped pipe shipped a row one cell short) are built, tested (54/54 · 53/53 · 38/38) and run dry on Valentine; `analyst.ANALYST_TABLES = False` and `FULL` unchanged | DONE S154 E5 (2026-09-15, Rab's `all signed` over BRIEF-S153-OPUS §1.1): `ANALYST_TABLES = True` — the layer runs in the pipeline on the next converted book (not retroactive; drop 0 · pending 0 at the flip, nothing shipped by the act); the waiver in the code names his word and the measure; `table_geometry_selftest` 119/119, `edit_whitelist_selftest` 53/53, `convert_and_ship_selftest` 248/248, `analyst_selftest` 39/39 after four cases that had encoded the OFF default were made explicit (`tables=False`) and a new case S154 (a) asserts the default engages the layer — watched firing with the lever forced off (1 of 39 BAD, the 38 others green); ruff at CI's pin clean; docs/15 §12.4.8. Residue: the MEASURE is not yet taken — it needs the next book through the watcher (the census BEFORE/AFTER and the scorer on any truthed table); the evidence at the flip is one book, both copies; the rails' grid calls now ride the analyst's GPU run and their cost per book is unmeasured. History kept: open — Rab's slot (the whitelist is the policy, S119/S140): whether `table-geometry` enters the shipped whitelist and the pipeline runs the layer; a lever-waiver like the survival threshold — moves on his word only | sessions/S150-desktop-2026-09-14.md E3, E4, E5; docs/15 §12.4 |
| ~~analyst/rail-boundary-unread~~ | S150 E4: the rows a rotated label spans are UNREAD — the label goes on the letter run's first row, and where the OCR left no blank row between two labels the letters of both arrive as ONE run (the anchor copy's `6 O S T s M G M T V A Ā O N` = COSTS · MGMT · VALUATION over seven rows), so the resolver answers a phrase and the boundary stays unread; only the picture (the page image, a vision model) or a row-count heuristic from the block record could place it | DONE S154 E6 (2026-09-15, the mechanical half; the vision half is `analyst/vision-route-decision`): every rail proposal carries a `span` — the groups tile the body rows, each CENTRED on its letters, never past the next run's first letter; the word is lifted onto the span's first row over blank rows and stray marks (`_stray_mark`), never onto the header; `grid_invariant` admits only that shape (selftest §11: header, no-run, over-another-run, filled-cell negatives; 130/130). The truth: three Sonnet readers on the rendered pages, 21 of 21 span readings identical to the S151 majority. Measured on both copies with the scorer's new placement measure: labels on their first row 2 of 7 → 5 of 7 (p.107 TEDS 0.967 → 0.980, p.108 0.979 → 0.998, the 12 other tables unchanged); the first cut (a midpoint split) placed FINANCIAL a row early and was measured out. Residue: MGMT (not a word of the book's prose) and p.72's phrase rail in fragments — only the page places those; the centring rule is measured on two pages of one book. History kept: open — mechanical: a boundary rule (the block record's row groups; a blank-row heuristic; or the vision route, which is Rab's decision) | sessions/S150-desktop-2026-09-14.md E4a, E5 |
| ~~analyst/vision-route-decision~~ | S154 E6 (2026-09-15): the vision route's value, MEASURED — a per-book reading of the page image places 7 of 7 rotated labels (three Sonnet readers, 21 of 21 span readings identical to the S151 majority, the planted packet caught by all three), reads MGMT (an abbreviation the book's prose never spells) and calls p.342's quadrant a figure (3 of 3), where the text rule (§12.4.9) places 5 of 7 and cannot see either | DONE S156 (2026-09-15, Rab: `signed, use sub agents`): the route is SUB AGENTS — a reading of the page by a Sonnet reader panel in a sitting, a sidecar `vision.json` (`vision-reading/1`; private `measurement/truth/VISION.md`) the layer consults the way it consults the resolver (E1: a word lent, a span taken, a fragment label rebuilt, a figure call honoured; every claim through `grid_invariant`; selftest §12, 142/142) and the pipeline hands over from beside the dropped PDF or beside the bundle (E2: 254/254). Valentine's reading written from the S154 panel's 21-of-21 agreement (`vision_from_readers.py`) and measured on both copies (E3): rails on their first row 5/7 → 7/7, words 6/7 → 7/7 (MGMT), p.108 TEDS 0.998 → 1.000, p.72 0.892 → 0.994, the 12 other tables and every cell bucket unchanged; the p.342 quadrant silenced as a figure. Residue: a reading exists per book only where a sitting's panel read its pages (the next book's panel: `panel_packets_TEMPLATE.py`, `panel_reader_brief_TEMPLATE.md`, `panel_agreement.py`, `vision_from_readers.py`); the census reads the copy, not the sidecar (a figure is still a table to it); the live Valentine copies do not carry the reading yet — `pipeline/reanalyse-valentine-with-reading`. History: open — blocked-on-rab: the route — a vision model on his card at conversion (the GPU law), Sonnet readers by API per book (cost, no card), or neither; his word names it, the lane builds the hook (`geometry_pass` takes a reading of spans and words the same way it takes a resolver) | sessions/S154-desktop-2026-09-14.md E6 |
| pipeline/reanalyse-valentine-with-reading | S156 E3 (2026-09-15): Valentine's reading (`measurement/evidence/s156-valentine-after-vision/valentine.vision.json`) gives 7/7 rails and MGMT on both copies in the DRY run; the live anchored and held copies were not re-analysed — the reading must sit beside the bundle as `vision.json` and the J42 re-analysis (`convert_and_ship --reanalyze`, the widget's card) must run, which rewrites the copy's body and manifest under the lever | open — blocked-on-rab: writing into the live copies is a pipeline act (his word: `reanalyse Valentine`); the steps: copy the reading beside each bundle, run the re-analysis per bundle, the census and the scorer after, the held copy's audit | sessions/S156-desktop-2026-09-15.md E3 |
| ~~analyst/title-fragments-unread~~ | S150 E2: the first half of Valentine's Exhibit 8.2 carries its spanning title chopped into header cells (`Start`, `with th`, `s sour`, `ce to ir`…) — the title-row shape (one filled cell, or a fragment pair) does not see it, so no caption is proposed; the census names it as MISSING; the block record shows surya read it as fragments too (the anchor's `blocks.json`) | DONE S151 E3 (2026-09-14): the reading half sees the shape (`title_fragments`: three or more short filled cells above a fuller real header); the proposer reads the fragments' letters as one stream and captions the table by, in order of trust, (1) the book's own phrase holding the letters in order (a continued exhibit repeats its title — the anchor's 1552 and the held copy's 1556 both read "Start with this source to investigate before meeting management", 49 of 53 letters, the raw join kept on the record), (2) the book's words along the stream (only when they explain seven letters in ten, three words or more, five letters on average), (3) the fragments joined as read and said so; a fragment row whose cells are words the book uses (a regression's stacked `Standard | Lower | Upper` headings) is refused as headings — the 2502/2510/2564 tables kept their headers. Residue named: the headings guard is the lexicon's (a stacked heading in words the book never uses elsewhere would be joined as read); the stacked heading itself (`Standard` over `Error`, `P-` over `value`) is not merged — see `analyst/stacked-heading-rows`. History kept: open — mechanical: a fragment-row rule (a first row whose filled cells are short pieces that concatenate into words, above a real header) with its negative controls; the lifted caption is the same repair | sessions/S150-desktop-2026-09-14.md E2, E5 |
| ~~analyst/stacked-heading-rows~~ | S151 E3: Valentine's regression outputs (anchor 2502, held 2510 and 2564) carry a column heading split across two rows (`Standard` over `Error`, `t` over `Stat`, `P-` over `value`, `Lower` over `95%`) — a renderer shows the upper row as the header and the lower as a data row; the reading half sees the shape (`title_fragments`), the proposer refuses to caption it (the cells are words of the book), nothing merges the two rows into one header (`Standard Error`, `t Stat`, `P-value`). The merge needs the invariant to accept a row FOLDED into the header (cells joined column by column) — a rule not yet written and not yet measured. | DONE S152 E3 (2026-09-14): when the headings guard says a fragment row is a stacked heading, the proposer emits a HEADER FOLD — the header row and the first body row joined into one heading row by the page's typography (`_join_heading`: one space; a hyphen joins without a space, `P-value`; a lone non-letter upper piece above a two-word lower piece drops, `t Stat`); the invariant's fold clause accepts the exact heading join for the header row only; measured on both copies — p.179's headings 2/8 → 8/8 in the header row, the held p.181's 1/6 → 6/6; the anchor's p.181 lost its upper line in conversion (nothing to fold) and p.175's block is mid-table; selftest with the chopped title, a healthy header and a non-exact join as negatives. Residue: a lone digit over a one-word piece would join as read; the anchor's lost upper line is a conversion defect. History kept: open — mechanical: a stacked-heading rule (two header rows whose cells pair column by column into headings the book uses: `Standard Error`, `t Stat`, `P-value`) folded into one header row, the invariant extended to admit the fold, its negatives (a real data row under the header; a fragment title) | sessions/S151-desktop-2026-09-14.md E3 |
| ~~analyst/table-geometry-ground-truth-and-error-measures~~ | S150 E5 (Rab's two PDFs, DAS 2002 and Bateman 2008, read for File Portal — private `measurement/READING-DAS2002-BATEMAN2008-2026-09-14.md`): the sharpest answer to "what is still missing that we need to capture" — the layer has a BEFORE/AFTER census but no ground truth and no error taxonomy for its repairs. From the reading (Observed on the pages): cell-level buckets correct / split / merged / missed / spurious reported against BOTH denominators (Wang, Phillips & Haralick, DAS pp. 279–281); region matching with thresholds taken from pages ground-truthed twice and the truth's own reproducibility measured (Todoran et al., pp. 526–529: 16 pages × 4 runs × 2 evaluators, 97–99 %%); a table or figure with embedded text is ONE region and the truthing tool over-segments first, the human merges (Antonacopoulos & Meng, pp. 238–240); ground truth as boxes + identifiers only, omission / commission / precision (Wnek, pp. 465–466) | DONE S151 E4 (2026-09-14): private `measurement/truth/FORMAT.md` (one JSON per printed table), `probes/render_pages.py`, `probes/table_truth_score.py` (18/18: caption / header / rails / the DAS 2002 buckets against both denominators / TEDS, TEDS-S / a GriTS-style factored alignment, named as such); the truth of 14 tables on 10 pages of Valentine by a three-reader Sonnet panel from the rendered pages (a planted text alteration caught by all three; cell agreement 100 % on 13 tables, 206–207 of 208 on the dense matrix, the one disputed bullet settled by the verifier's zoom); BEFORE / E1 / E3 scored on both copies — the layer moves every measure the right way on the tables it touches (p.132 TEDS 0.866 → 0.967) and nothing elsewhere. Residue: the truth is 14 of 81 tables, chosen; the panel's format example leaked one row; the list-cell convention is open (see the rows below). History kept: open — mechanical, before the lever moves: a ground-truth format per table (title yes/no, each rail's rows and word, the stray cells), the Repair Bench as the truthing tool, a scoring script with the buckets against both denominators, the truth ground-truthed twice on a sample (Rab at the bench, the census as the second reader) and its variability reported, accuracy stratified by a per-table complexity; the modern measure (ICDAR table competitions, TEDS, GriTS — Inferred, unchecked) checked online by Fable before the format is fixed | sessions/S150-desktop-2026-09-14.md E5; the private reading note |
| ~~converter/wrapped-row-label-split~~ | S151 E4 (the panel): a row label printed on two or three lines (`lag1 (log of manufacturing index)`, `Compensation and Benefits`) arrives as TWO rows — the label's first line with no data, the rest with the data (anchor 2507–2508, 2559–2560; both copies) — 9 and 7 cells 'split' by the scorer | DONE S152 E1 (2026-09-14): a FOLD proposal in `table_geometry` (the pair seen by `_wrapped_pair`; `apply_table` joins the two rows column by column; `grid_invariant` admits a fold only as the exact join of two adjacent rows, a body fold joining only where one side is empty); measured on both copies — the two labels folded (`lag1 (log of manufacturing index)`, `Compensation and Benefits`), the diff against S151's output the two folds alone, the scorer's split cells 9 and 7 → 0; the first dry run folded nine INDEX entries (two-column entries ending in page numbers) — the rule tightened on that measurement (three columns, a letter-ending first line) with both shapes as negatives; 102/102. Residue: a two-column table with a genuinely wrapped label would not fold (said so); the stacked heading above these tables is E3's. History kept: open — mechanical: a geometry rule (a row with a label and no data, followed by a row whose label continues it and holds the data → one row), under the invariant, with the p.204/206 truths as the tripwire | sessions/S151-desktop-2026-09-14.md E4 |
| ~~converter/lost-table-p175~~ | S151 E4 (the panel): Exhibit 12.7's coefficients table (PDF page 200, printed p.175) is not a table in the anchor copy — its numbers sit as prose lines (2442–2444) and the label `index)` as `indev)`; the block record has it as `/page/200/Table/3`; the scorer reads 'not located' | DONE S157 E1 (2026-09-15, the lane's own task while his words wait): the SPLIT pass in `table_geometry.py` — a second stacked heading inside a body (two rows, or one row with `<br>`) splits the table, the tail re-joined from the prose only where the shape is exact (`split_invariant`; docs/15 §12.4.11; selftest §13, 155/155). Measured on both copies: p200-t3 anchor TEDS 0.149 → 0.996 (header 8/8, 17 of 18 cells), held 0.247 → 0.742 (header 7/8); the ANOVA tables 0.226 → 0.406 and 0.308 → 0.482; the twelve other tables unchanged. Residue: the label's OCR (`log /manufacturin indev)` for `log (manufacturing index)` — a lexicon route for a row label); the held copy's last row scattered over two prose lines with `10.03` lost by the OCR — left, never invented. History kept: open — the S149 §18 probe RUN (S155 E2, 2026-09-15, Rab's `move back and run`): `marker_single --html_tables_in_markdown` on PDF pages 195–207 (120 s on the card, one process) — the flag does NOT recover it: Marker fuses the coefficients block's header rows and its Intercept row INTO the ANOVA `<table>` (11 rows) and drops the `log (manufacturing index)` row to prose as before (`indev)` + `3.58 0.36 10.03 0.00 2.86 4.29 2.86 4.29`); scored (the HTML rewritten to pipes by `probes/html_tables_to_pipes.py`): p200-t3 TEDS 0.149 in the probe = 0.149 in the anchor copy, header 0/8, 18 unmatched; the loss is Marker's table detection, not the markdown rendering. The raw HTML output also reads WORSE than the pipeline's copy where the layer had repaired (p204-t3 1.000 → 0.560, p206-t3 0.842 → 0.745: the stacked headings unfolded). The fix is a LAYER rule, the lane's: a second header-shaped row inside a table's body (`Coefficients · Standard Error · t Stat · P-value · Lower 95% …`) SPLITS the table — the stacked-heading fold's cousin — and the prose row beneath the split table's last row with the table's column count re-joins it; measurable on p200 against p200-t3. Marker's `--use_llm` table route needs an LLM service on the card or by API — his. History: open — semantic: a conversion defect; the block-record route (S149 §18, the `--html_tables_in_markdown` probe) is Rab's card | sessions/S151-desktop-2026-09-14.md E4 |
| ~~library/damodaran-bundle-misplaced~~ | S153 E3 (a verifier lens's residue, re-read by the lane; Observed): the held Damodaran 4e bundle `0d68f0e02293970c` that S149 §10 read as gone from `held/` with no pipeline event is NOT gone — it sits nested inside another held bundle's folder, `held/c5afd9edcf620fc6--reshipped-20260905-211727/0d68f0e02293970c/`, intact (markdown, `.bench-bak`, `assets/`, manifest: converted 2026-08-01, lane clean, verdict fail); the nested folder's mtime 2026-08-06, the parent's 2026-09-14 03:01Z (the time S149 saw on `held/`); `events.jsonl` carries no event for either bundle at that hour (the Damodaran's last 2026-08-03: a ship failure, then held; the Beer bundle's a re-audit and re-ship on 2026-09-05) — the shape of a folder dragged into a sibling, not of the pipeline | DONE S155 E1 (2026-09-15, Rab's word `move back and run` — the first half): the nested folder moved back with one `mv` (`held/c5afd9edcf620fc6--reshipped-20260905-211727/0d68f0e02293970c` → `held/0d68f0e02293970c`), the condition measured first (no bench process, no pipeline event since 2026-09-13, the target absent); after: `held/` 8 entries (7 → 8), the bundle intact — its markdown, `.bench-bak`, `assets/`, `manifest.json` (converted 2026-08-01, verdict fail), 23 MB, its own mtime 2026-08-06 unchanged, body sha fe72c52f; the Beer bundle's folder back to its own three items. The move on the 14th at 03:01:12Z read as his hand in Explorer (S154 E4); his word does not say so in as many words — only that it goes back. History kept: open — semantic (Rab's yes/no): was the move his hand? On his word: one reversible `mv` of the nested folder up to `held/0d68f0e02293970c` with nobody holding the bundle open, then the held count re-censused (7 → 8); if not his hand, an incident (the widget's log and the folder's history read before anything is written) | sessions/S153-desktop-2026-09-14.md E3; coordination/BRIEF-S153-OPUS.md §1.8 · `Observed 2026-09-15 (S157 E35)` — `held/0d68f0e02293970c/` is at the top level of `held/` (folder mtime 2026-08-06); nothing nested under the Cybernetics hold (its mtime 2026-09-15T00:01Z); the id's pipeline events: pending 08-01, failed 08-03 (ship tar/ssh), resolved-held 08-03 — no move event. S153's nested reading is not reproducible from here |
| ~~analyst/spanning-caption-partial~~ | S151 E4 (the panel): p.97's Exhibit 6.1 prints `Quality of Sell-Side Analyst Based on Your Prior Experience` as a caption spanning the three source columns only; the copy's first row holds it beside other cells, the title shape (one filled cell, or a fragment pair) does not fire, the headings stay in body row 1 (TEDS 0.620, unchanged by the layer) | DONE S152 E2 (2026-09-14): the copy's first row was the caption chopped into three LONG pieces beside empty cells (`Quality of Self-Side | Analyst Based on Your I | Prior Experience`), a shape neither the title rule nor the fragments rule saw; `TableReading.title_pieces` (two or more pieces, an empty cell, the next row fuller, a continuation signal across the pieces) and the proposer's pieces route (the book's phrase, else the pieces joined as read; the headings guard first); measured on both copies — the caption above the p.72 table and its headings in the header row, nothing else changed; scored: caption missing → correct, TEDS up; 108/108 with the two-heading header (Lipstick) and single-word headings as negatives. Residue: the OCR's `Self-Side` and stray `I` ride in the caption (a glyph defect); a two-heading header carrying a continuation word would read as pieces (named). History kept: open — mechanical: a partial-span caption rule (a first row with ONE long phrase and empty cells under the rail/label columns, the next row the headings) with its negatives | sessions/S151-desktop-2026-09-14.md E4 |
| ~~truth/list-cell-convention~~ | S151 E4: a cell that prints a bullet list (Exhibit 17.3, p.264; Exhibit 6.1's cells) — the panel truthed p.264 as four rows (one bullet per row) where the copy has two rows of lists; FORMAT.md does not say which is the truth, so the scorer reports 'merged' cells that are a convention, not a defect | DONE S154 E2 (2026-09-15, Rab's `all signed` on the lane's recommendation): the rule is ONE CELL — a cell that prints a bullet list is one cell holding the list, the items in printed order each led by the bullet (`measurement/truth/FORMAT.md`, the list-cell rule); the majority truth p264-t1 re-cut to one row of two list cells (the readers' four-row files kept as read); the scorer on S152's last AFTER, both copies: merged 7 → 0, split 0 → 2, correct 1 → 0 — what it names now is the converter's real defect (Exhibit 17.3's two list cells spread over two rows, the bullet marks dropped, items run together), TEDS 0.438 → 0.559 anchor / 0.593 → 0.699 held under the new truth (the copy unchanged; the numbers are the scorer's reading of one copy under two conventions, not an improvement of the copy). Residue: the defect itself — a list cell split into rows — has no repair rule yet (a fold with bullets restored would need the page's list marks; the geometry pass sees none); p.72's Exhibit 6.1 is not in the truth set, so the rule is measured on one table. History kept: open — semantic (Rab's word): a list cell is one cell (the copy's shape) or one row per item (the page's layout)? The FORMAT rule follows | sessions/S151-desktop-2026-09-14.md E4 |
| ~~census/figure-and-index-read-as-tables~~ | S151 E4 (the panel): p.367's communication-quality quadrant is a FIGURE by 2 of 3 readers (the copy has it as a table with the HIGH/LOW rail the layer labels); p.446's index columns are two 36-row 'tables' in the copy (all three readers: no table) — page-level spurious tables the census counts as healthy | DONE S152 E4 (2026-09-14), the index half; the figure half carried: the block record calls the p.342 quadrant and the p.421 index columns Table (Observed — no Figure / TableOfContents block there), so no census column from it; the index is named by a text signature (`TableReading.index_like`: ≤ 3 columns, ≥ 10 rows, at least 35 % of the filled cells an entry — words, a comma, a page number or range; measured into shape on the anchor's 18 index tables, whose entry rate runs 0.39–0.74 because entries wrap) — on both copies every index-like table sits in the index pages, none before it, and none of the panel's fourteen truth tables is named; the census carries the count. The figure: a quadrant with a rail and cells of prose has Exhibit 6.1's text shape (a real table) — no text rule separates them; the call is the page's (the vision route, Rab's) or a panel's. History kept: open — mechanical: a census column `page_kind` from the block record (Figure / TableOfContents / Table) so a table block over a figure or an index is named, and the layer leaves the p.367 rail alone unless the block record says Table | sessions/S151-desktop-2026-09-14.md E4 |
| ~~analyst/rail-word-route-lexicon~~ | S150 E4b (Observed on the card, four runs): qwen3:8b cannot spell a rotated rail from its letters — it returns the letters shuffled (`SARTGEY` for STRATEGY, `TOSSTMGMTVAAON` for the seven-row run), an echo (`HGH`, `LLO`), a near-miss `letters_fit` refuses (`REVINUE`, `FANAN`) or a wrong word the one-confusion rule admits (`LABEL` for LOW); four labels went into the tables no better than the garble they replaced, inside the invariant. The letters-only bound is necessary, not sufficient | DONE S151 E1 (2026-09-14, the lexicon route: the book's own words, the OCR's confusion sets, the short-read cap, the rows' vocabulary as the tie-breaker, the run split at the cells where each word's letters end, proposals admitted one at a time): five dry runs on both copies, the fifth with ZERO wrong labels — STRATEGY, FINANCIAL, REVENUE, COSTS, VALUATION, HIGH right; residue named: MGMT (an abbreviation, no word of the prose) and LLO (LOW and LOG fit a two-letter read alike) stay unresolved rather than guessed — the reader panel (E4) or his eye decides them. The lever stays his. History kept: open — mechanical: a LEXICON bound — the label must be a word the book's own prose uses (Valentine says revenue, costs, management, valuation, strategy, financial, high, low), matched to the letters by `letters_fit`; the model chooses among the fitting candidates or the route is deterministic without it; short reads (≤ 3 letters) fit exactly after the doubled-letter collapse; an echo of incomplete letters is no answer; re-measured on both copies with the AFTER census; the lever stays off until the loss reads zero | sessions/S150-desktop-2026-09-14.md E4b, E5 |
| ~~analyst/stray-glyph-outside-a-matrix~~ | S150 E4a: the `dots` rule fixes a stray bullet glyph only inside a table that has a • matrix; the p.367 HIGH / LOW matrix carries a `·` in column 2 (held copy line 4545) beside no •, so it stays — what that glyph is (a bullet, a stray, a dropped letter) is UNREAD | CLOSED AS-IS S157 E4 (2026-09-15): the page WAS read — by the S154 E6 panel (three Sonnet readers on the rendered page 367, sittings/S154/e6/readers.json): all three call the block a FIGURE (a 2×2 quadrant of prose cells — Lipstick on a Pig / Reputation Builder / Reputation Killer / Lost Opportunity — under a three-box STEP 1/2/3 diagram; the rotated axis labels HIGH and LOW are what Marker read as `H<br>G<br>H` and `L` / `L<br>O`), and the reading `valentine.vision.json` carries `kind: figure` for it. Under the reading the layer gives a figure no repair at all (S156 E1) and the census counts it as a figure (S157 E2: 82 blocks → 81 tables + 1 figure); the `·` is a stray of the axis label and stays as the OCR wrote it — no one-glyph rule. Residue: the figure's prose survives only as forced table cells until Marker's figure route or his reanalyse word. History kept: open — read the page image once (the bench: page 367) and decide; a one-glyph rule follows or the row closes as-is | sessions/S150-desktop-2026-09-14.md E4a |
| analyst/stub-tables-are-prose-in-a-frame | S157 E5 (2026-09-15, the shelf probe: `measurement/evidence/s157-shelf-geometry/`): 64 of the 2,513 pipe-tables on the shelf (33 bundles) have NO filled body cell — a header row and nothing under it, or one all-blank row: Marker framing a figure caption (`Figure 7.5 Default Spreads…`), a chopped heading (`Phi` `loson` `hv` as three cells, `Announce` `ment Date`), a footnote, a bare `True False` as a table. The L4 skeptic of the trim pass named three of them (mechanically right to trim, odd to be tables). A rule would UNFRAME a stub — its header cells joined back into one prose line (a chopped heading through the fragments route of S151 E3) — under an invariant that nothing but the pipes and the delimiter leave; the census would count `stubs`. Mechanical; the lane's; no card. Measure: the shelf probe's stub count 64 → 0 with every joined line equal to the header's cells joined, and the 14 Valentine truths unchanged | open — mechanical, the lane's | sessions/S157-desktop-2026-09-15.md E5 · `Observed 2026-09-16 (S160 E6)` — BUILT, LEVER OFF, MEASURED OFFLINE: `table_geometry.find_stubs` / `propose_unframes` / `apply_unframe` / `unframe_invariant` / `unframe_pass`, wired into `geometry_pass` after the trim; the lever `UNFRAME_STUBS = False` (his word) — OFF the record names every stub (`stubs`, `unframes_proposed`, `stub_traces`) and the text is untouched, ON it applies what the invariant admits (one table before, none after, one line equal to the header's cells joined, every letter kept, every body cell blank). The shelf's 64 (33 bundles, 2,517 tables, the bundles never written) split by their own shape into **29 FRAMES** (0–1 blank rows, no cell stacked three deep: 11 chopped headings `Phi loson hv` / `True False` / `Effect On`, 6 short headings `Announcement Date`, 6 footnotes and question stems, 6 captions `Figure 7.5…`, `TABLE<br>25.2`) and **35 TRACES** — a header of 130–1,219 characters over a grid of 10–37 BLANK rows (framed footnotes and paragraph tails: table_rec's grid with its cells empty, docs/61 §10's padding), the crammed TABLE 8.2 (one cell of 99 `<br>` pieces over 25 blank rows — `converter/table-crammed-into-one-cell`, the unstack route's, not this one's) and a ten-column exercise head (6 pieces) — S157 E7's judgement kept in code: a trace is named and never unframed. Lever ON in memory: 29 applied · 0 refused · 0 frames remain · the 35 traces stand. Valentine: one frame per copy (`Phi loson hv`), the 14 truths are bodied tables and cannot be stubs. `table_geometry_selftest.py` 184 → 208 (the classes, the invariant's five refusals, the lever both ways, the traces byte-identical). Thresholds `STUB_TRACE_ROWS = 2`, `STUB_TRACE_PIECES = 3`, lever-waiver comments. Private `sittings/S160/e6/shelf_stubs.md` lists every stub with the line it would become. ARMING IS HIS: the row stays open for the lever. |
| converter/table-crammed-into-one-cell | S157 E24 (2026-09-15, the IV University Edition adjudication; Observed in the anchor copy `… -- Four`, `sittings/S157/e24/excerpts/p0308.md`): TABLE 8.2 *Betas and Operating Leverage — Shoe Companies* (p.308, 21 rows × 4 columns) is carried as ONE header cell of `<br>`-joined values (`Company Name<br>Beta<br>Market D/E<br>…`) over 26 empty body rows — the whole table inside a single cell, unusable as a table. Found by a reader lane's deviation clause, verified by the lane's own look. The class: Marker emitting a table's cells as one stacked cell; the census's `stubs` counter (E5) sees the empty rows, not the crammed cell — a `crammed` counter would (one non-empty cell whose `<br>` count ≥ the row count × columns). A rule would UNSTACK it (the header cell split on `<br>` into rows of `cols` cells) under an invariant that the joined text is unchanged. Mechanical; the lane's; no card. Measure: the shelf census's `crammed` count, and p.308 read back as 21 rows × 4 with every cell equal to the page's | open — mechanical, the lane's · `Observed 2026-09-15 (S157 E26)`: measured on the shelf — 1 of 1,662 pipe tables (this one, on both copies of the bundle; 24 stacked cells elsewhere are not the class); the census counts `crammed` now (`table_census.py`: largest cell ≥ 90% of the table's characters and ≥ 10 `<br>`); the unstack rule WITHHELD for one instance — built when the counter reads more than one · `Observed 2026-09-17 (S196 E3)`: re-measured on the shelf that grew — 32 bundles, 1,857 pipe tables, seven bundles landed 2026-09-17 (`sittings/S196/e3_crammed_probe.py`, `e3/crammed.json`; controls: TABLE 8.2 reads crammed 1, Ashby 0 / stacked 3): the census's criterion reads FOUR hits — TABLE 8.2 on both IVUE copies (the known one) plus two NEW frames the coordinator read by eye as NOT the class: *Automate the Boring Stuff*'s INDEX (a two-column index page as one 93-br cell in a 49×3 frame) and *Document Layout Analysis*'s TABLE OF CONTENTS (entries, leaders and page numbers as one 131-br cell with 31 other filled cells, 28×4) — prose in a frame (the stub class) that the share-and-br criterion also catches. The class's count stays ONE; the rule stays WITHHELD; the counter OWES a discriminator before it can be the trigger: a real table's stacked cell has no empty values and its value count divides by the declared columns (TABLE 8.2: 100 values / 4 = 25 rows; the index's 94 / 3 does not divide; the ToC's cell carries runs of empty values) — mechanical, the lane's, no card · `Observed 2026-09-17 (S196 E4)`: the discriminator BUILT — `table_census.py`'s `crammed_measure` returns `crammed_real` (crammed AND every br-split value non-empty AND the value count divides by the declared columns with ≥ 3 rows); `crammed` keeps E26's meaning; the summary carries `crammed_real` + `crammed_real_tables`. Its tripwire born the same episode: `measurement/probes/table_census_selftest.py` 5/5 (a TABLE 8.2-shaped stack real; an index-shaped stack and a ToC-shaped stack crammed-not-real; an ordinary table and a stacked-beside-filled cell neither) — it failed to unpack against the old code, so it distinguishes; the live read `sittings/S196/e4_live.py` on the three frames in their bundles: TABLE 8.2 real, the index and the ToC not — 3/3. THE ROW'S TRIGGER IS NOW `crammed_real` (reads ONE on the 32-bundle shelf); the unstack rule stays WITHHELD until it reads more than one | sessions/S157-desktop-2026-09-15.md E24 |
| converter/lost-paragraph-in-illustration-box | S157 E24 (2026-09-15; Observed): ILLUSTRATION 20.7 (p.757, *Valuing a Brand Name Using the Enterprise Value to Sales Ratio*) — the bundle carries the heading, both *Valuing* sub-heads and every equation, but NOT the box's opening paragraph (*Consider two firms that produce similar products … $2.5 billion next year*, six page lines; `grep -F` on three of its phrases: 0 hits; `sittings/S157/e24_text_loss.py`: 8 of the page's 11 prose lines absent — the only page of the 76 uncovered with three or more). A lost PARAGRAPH inside a shaded box, beside a lost table (S151's p.175) and lost figures (P-2). The measure that found it (every ≥40-char prose line of a page searched in the bundle, tags stripped, equations excluded) is cheap and runs on the CPU — over a whole book it is a prose-completeness instrument the project does not have (P-1 counts figures, the audit scores chunks). Semantic (why Marker drops a box's first paragraph — its layout model? the OCR lane?) + mechanical (the instrument). No card for the instrument; the re-conversion that would show the drop is the card's | open — the instrument the lane's; the cause his re-conversion · `Observed 2026-09-15 (S157 E28)`: the instrument half built — `measurement/probes/prose_loss.py` (whole book, per page, three 8-word windows, ligatures and link targets folded); p.757's paragraph is in BOTH copies' convert-stage runs (154 words, `year. valuing famous using these inputs`) — a MARKER loss, not the analyst's; the cause is his re-conversion's to show | sessions/S157-desktop-2026-09-15.md E24 |
| analyst/whole-sections-lost-pre-acceptor | S157 E28 (2026-09-15; Observed): the shelf's bundles converted BEFORE the S140 acceptor (J46, 2026-09-12) carry analyst-stage losses of whole sections — the IV University Edition's two conversions (08-31, 09-01; qwen3:8b readability; copy 1 with 900 of 955 chunks from the resume journal) each dropped different sections of 300–600 words (p.1174 *A Postscript on Value Enhancement*: heading kept, body gone; p.129, 788, 1037, 1022, 1036, 212, 917 on copy 1; p.44, 917, 670 on copy 2) while Marker's own largest run is 154 words. Found by `measurement/probes/prose_loss.py` (per page, with the lines) and confirmed by the manifests' two audit blocks (convert identical on both copies; analyst runs differ) and by `grep -F`. Today's acceptor reverts a dropped section whole (`edit_whitelist_selftest` case 29, 54/54), so conversions since S140 are guarded; the pre-S140 shelf is not. Remedy: re-conversion (`held-books/reconvert-four`, his card). What the lane can still do: run the probe on every held/vaulted bundle and list the pages, so a re-conversion has its before | open — the shelf sweep the lane's; the re-conversions his · `Observed 2026-09-15 (S157 E29)`: the held IV 4e (08-01) lost the SAME postscript (p.1151) the UE's copy 1 lost at p.1174 — one section, two editions, two conversions: the analyst's behaviour on that content, a specimen for its owner; the held post-acceptor Zero to One (09-12, pass) reads 1 missing line of 3,383 where the older anchor conversion read 11 | sessions/S157-desktop-2026-09-15.md E28 |
| linux-converter/port-the-table-row-blanking | S158 E1 (2026-09-15; Observed by comparison, L2 + the lane's regex): `linux-converter/converter/degeneration.py` is a PORT of the Windows `fidelity_audit.degeneration()` that carries the docs/15 §9 thresholds byte-identical but NOT the SYM-067/J29 table-row blanking (`_blank_table_rows`, no `table_rows_stripped` key) — a dense pipe-table paragraph on the Linux lane can still false-fire the loop detector (the Windows twin went 26 → 0 false blocks on the Damodaran 2025 4e). Report-only on that lane (never blocks publishing), so the harm is a false `degeneration: true` in the manifest. The port's own docstring says "if the Desktop recalibrates, port the numbers, do not fork them" — the blanking is the unported half | OPEN — his word (the ThinkPad is his; `docs/61` §4.8 names it) | private `measurement/TICKETS-2026-09-12.md` |
| docs/amend-15-s12-degeneration-rule | S159 E5 (2026-09-16; the Circle over the converter's evidence, `docs/62` §6 item 1): docs/15 §12 (SIGNED 2026-07-20) states the degeneration thresholds as `zlib < 0.20 OR max-word-trigram ≥ 40 per §9.1` while §9.2 amended the block rule to AND and the code implements AND (`fidelity_audit.py:254-258`; the Linux port too); §17 (S158) records the reading without amending. Every other post-signing change to §12 carries a numbered amendment (§12.1–§12.4); this one does not. The act: a §12.5 stating AND per §9.2 with the date — a SIGNED sentence, so his signature, not a lane's append | OPEN — his signature (TENSION; docs/62 §7) | public `docs/62-circle-converter-evidence.md` §6–§7 |
| ~~symptom-index/tripwires-owed-for-fixed-rows~~ **STRUCK 2026-09-17 (S196 E2) — EIGHT OF NINE LANDED and read in their rows' Guard cells today: SYM-006 (T23), SYM-041 (T24), SYM-009 (T25), SYM-125, SYM-030 (`test_generated_md.py`), SYM-052 (`test_table_health.js`), SYM-011 and SYM-012 (`linux-converter/tests/`, S166 E2); the ninth, SYM-023 (`main.rs`'s label guard), is Rust and rides the next rebuild's ritual — HIS hand, carried by the rebuild row; the lane's part of this row is complete** | S162 E3 (2026-09-16; the second Circle, `docs/63`; Observed by three Sonnet lanes and the lane's own re-check): nine FIXED rows name a fix that is PRESENT in the code at HEAD and has NO tripwire in any suite a close or CI runs — SYM-006 (`_kill_tree`, `convert_and_ship.py:294`), SYM-009 (`_enforce_hold`, `:637`), SYM-011 (the inotify `on_created` route, `linux-converter/converter/main.py:11-16`), SYM-012 (`FORCE_DROP_OLD`, `engines.py:78`), SYM-023 (the `window.label() == "main"` guard, `main.rs:772`), SYM-030 (`GENERATED_MD`, `bench.py:250`), SYM-041 (`_with_batch`, F-09), SYM-052 (`bench.html`'s removed `slice(0, 400)`), SYM-125 (`watch_and_convert.py:572`'s utf-8 logging). docs/32 §6: a guard without a test that violates its property is a proxy with a birth certificate. Six more FIXED rows are RULES with no code to test (SYM-004, 005, 018, 019, 021, 022 — docs/19's laws and the muster's standing orders): not owed. Measure: one case per row that plants the defect back and watches the suite go red; the row's cell then reads WITHSTOOD in the next Circle | open — mechanical, the lane's (a rebuild is not needed for the Python and shell ones; the two Rust/JS ones ride the next rebuild's ritual) | sessions/S162-desktop-2026-09-16.md E3 · `Observed 2026-09-16 (S163 E1–E4)` — FIVE of nine landed, each with its negative control watched: SYM-006 (T23), SYM-041 (T24), SYM-009 (T25) in `convert_and_ship_selftest.py` (254 → 268); SYM-125 in `watch_and_convert_selftest.py` (18 → 22); SYM-030 in `prototypes/repair-bench/test_generated_md.py` (new, 4). FOUR wait: SYM-011/012 (Linux `tests/` — a case written blind here is proved only by CI's ubuntu log, SYM-075), SYM-023 (Rust — the rebuild's ritual), SYM-052 (the bench's `test_table_health.js`). FOUND on the way: none of the converter's hermetic suites (`convert_and_ship_selftest`, `watch_and_convert_selftest`, `table_geometry_selftest`, the bench's tests) is run by CI (marker-env only; `figure_coverage_selftest` is the one exception) or by `close.sh` or by `library_smoke --public` — every tripwire above is run by hand at each touch. That is its own row: `close/run-the-hermetic-converter-suites`. · `Observed 2026-09-16 (S165 E1)` — SIX of nine: SYM-052 in `prototypes/repair-bench/test_table_health.js` (node, 18 → 24; the renderer read out of the page; two in-run plants and a file-level plant, all red). THREE wait: SYM-011/012 (CI's ubuntu log), SYM-023 (the rebuild's ritual). The JS suite has no runner (CI and `[3c] CONVERTER` run python suites only) — E2 of the same sitting if room, else its own line under the runner row. · `Observed 2026-09-16 (S166 E2)` — EIGHT of nine: SYM-011 (`TestAllocatorHop`, 5) and SYM-012 (`TestOcrMode`, 3) in `linux-converter/tests/`, proved on Linux by CI's ubuntu log (run 35152701408, eight ids PASSED — `ci_step_log.py --grep`, the reader written for it), two source mutants red in scratch copies. ONE waits: SYM-023 (Rust — his rebuild's ritual). SYM-117 (private, not on this row's list) got its selftest the same sitting (E1). |
| close/run-the-hermetic-converter-suites | S163 E4 (2026-09-16; Observed while landing the second Circle's tripwires): the converter's hermetic selftests — `convert_and_ship_selftest.py` (268 checks, every Marker run a stub, `FP_PIPELINE` quarantined), `watch_and_convert_selftest.py` (22), `table_geometry_selftest.py` (208), `marker_blocks_selftest.py`, `degeneration_selftest.py`, `analyst_selftest.py`, the bench's `test_table_boundary.py` and `test_generated_md.py` — run under marker-env only (fitz, pymupdf), so CI's ubuntu job excludes them all but `figure_coverage_selftest.py` (S108/S116) and `close.sh` runs none: every tripwire born S157–S163 is exercised only when a lane runs it by hand. A guard nobody runs is a proxy with a reputation (docs/32 §5). Build: a `[3c] CONVERTER` row in `close.sh` that runs the hermetic suites under marker-env when `windows-converter/` or `prototypes/repair-bench/` changed since the pin (the RUST row's shape: fmt leads, each suite a row, RED named), WARN-ONLY first (the S108 standard — arming is his signature), with a `selftest.sh` case that plants a failing suite and watches the row go red. Measure: the close's card carries the suites' counts; a red suite cannot close green | open — mechanical, the lane's (warn-only); arming his | sessions/S163-desktop-2026-09-16.md E4 · `Observed 2026-09-16 (S164 E1)` — BUILT, WARN-ONLY: `close.sh` gains `[3c] CONVERTER` in the RUST row's shape — when `windows-converter/` or `prototypes/repair-bench/` changed since the pin it runs the eight named hermetic suites under the marker-env interpreter (`convert_and_ship_selftest` 268 · `watch_and_convert_selftest` · `table_geometry_selftest` 208 · `marker_blocks_selftest` 57 · `degeneration_selftest` 26 · `analyst_selftest` 47 · the bench's `test_table_boundary` 14 + `test_generated_md` 4; `deferral_gate` and `card_mutex` OUT by name, as CI keeps them), one row per suite with the suite's own tally, a RED row that says warn-only, and the exit code unmoved until `FP_CONV_ARMED=1` (or the line) — ARMING IS HIS. `selftest.sh` 113 → 119: CASE 61 a planted failing suite reads RED and the exit is the SAME as with a green one (the differential reading); 61b ARMED, the same red stops the close (exit 1); 62 the POSITIVE CONTROL (a passing suite reads clean with its tally — it caught the fixture's first interpreter, the Store stub that exits 49, making the planted RED pass for the wrong reason); 63 an untouched converter reads skipped and runs nothing. Live against pin bbcaf58 (S163's tests): eight rows clean with their tallies in 57 s of `close.sh`. UNTIL ARMED the row is a reading on the card, not a gate. · `Observed 2026-09-16 (S165 E2)` — the row gains the one non-python suite, the bench's `test_table_health.js`, run under `node` from its dir (`FP_CONV_NODE` names the runner; a missing runner reads UNREAD, never clean; `FP_CONV_NODE_SUITES` the list); `selftest.sh` 119 → 125 (cases 64/64b/65: a planted failing node suite RED warn-only with the exit unmoved; a passing one clean with its tally; a missing runner UNREAD with the exit unmoved). LIVE on this tree (`close.sh 854182b`, 21:02Z): NINE rows clean — 268 · SELFTEST PASS · 208 · 57 · 26 · 47 · 14 · 4 · `TABLE HEALTH: 24/24 ok` — in 47 s; the row's first run inside a real close on a converter-touching tree. Still warn-only; arming his. |
| ~~closeout/lockstep-through-the-tool~~ **STRUCK 2026-09-16 (S169 E1) — MEASURE MET: S167's and S168's §20 each carry the tool's own line (`MEMORY_SHA … staged: MEMORY.md cookie-tally.md` — `grep -c` 1 in each record) and neither `sittings/S167/` nor `sittings/S168/` holds a `fold_S<N>.py` (`ls | grep -c fold_` → 0); S168's close also exercised the tool's size guard (refused by 3 bytes, nothing written, the block trimmed, landed) and S169 E0 folded the index's residue with `compact_memory.py` for the room the tool needs. Was:** | S167 E1 (2026-09-16; found by the register survey for the next mechanical row — every candidate his, withheld or moot): the memory library's close-time clock move has had a tool since S134 — private `agent-scripts/developing/lockstep.sh`, SEQUENCES.md step 7 — and every close since S140 wrote a per-sitting fold script instead (this lane's S160–S166: `fold_S<N>.py`); the hand-written folds inherited the tool's own slip, `git add MEMORY.md`, so the cookie logged just before each lockstep left its `cookie-tally.md` line for the NEXT cookie's commit (S164, S165; S166 §10). S167 E1: the tool stages the whole library (`git add -A`), takes `FP_MEMORY_DIR` (with `FP_MEMORY_MD` bound inside it so a fixture can never stamp the real index), prints the staged files, skips the real clocks under a fixture; `lockstep_selftest.py` (11 cases on a fixture library with a bare origin; the S139 line planted back and watched leaving the tally behind); the brief's STAGING paragraph; step 7's gotcha (the cookie BEFORE the lockstep). Measure: the next two closes' §20 carry the tool's `MEMORY_SHA … staged: MEMORY.md cookie-tally.md` line and no `fold_S<N>.py` exists in their `sittings/`; then strike | open — mechanical, the lane's (S167's own close is the first use since S139) | sessions/S167-desktop-2026-09-16.md E1 · private `agent-scripts/developing/lockstep.brief.md` |
| ~~hooks/err-054-half-live-proof~~ **STRUCK 2026-09-16 (S171 E1) — MEASURE MET: one live `python.exe -c "print('S171 E1 live probe: a\\b')"` in this harness (23:28Z) drew the hook's advisory into the call — quoted: `ERR-054 (QUOTING): this Bash call puts a program with '\\' inside a python -c string … a FILE written with the Write tool and run by its path. Not blocking` — and the E1 receipt's harness line reads `advisories 1` (`journal/calls/S171-ep1.md`; S170's read 0). The print itself showed the hazard: the shell ate the backslash (`a` printed, not `a\\b`). Was:** | S170 E2 (2026-09-16; the register's fourth cut — ERROR-BIN's rule column: 29 rules name a mechanism, ERR-053/054's was the one without): `.claude/hooks/warn_heredoc.py` gains ERR-054's half — a `python -c` / `python.exe -c` program carrying a backslash, a backtick or a `$` prints the ERR-054 warning (additive; warn-only; the heredoc rule and Rab's signed over-sensitivity byte-for-byte). PROVED by `warn_heredoc_selftest.py` (11 cases; the hook run as a subprocess and fed the PreToolUse payload shape the harness documents) — NOT YET by a live call in this harness: the calls journal's harness rows count hook `advisories`, and the first live `python -c` with a backslash after 3f7ecc0 either shows the ERR-054 text in the session (and the receipt's advisory count moves) or shows the hook's registration reads a different payload key than the selftest feeds. Measure: one receipt (`journal/calls/S<N>.md`) whose harness line carries an ERR-054 advisory, quoted in the record — then strike; if the live call is silent, the selftest's payload is the wrong shape and the row says so | open — the lane's (a live call, no hand of his) | sessions/S170-desktop-2026-09-16.md E2 · `.claude/hooks/warn_heredoc.py` |
| ~~close/ladder-and-acceptor-suites-on-the-runner~~ **STRUCK 2026-09-17 (S183 E3) — ADDED and MEASURE MET the same sitting:** S182's close ran ten converter suites and NOT `analyst_audit_selftest.py` — the suite that guards the ladder it had just built (J44; run by hand, 20/20) — nor `edit_whitelist_selftest.py` (J46's acceptor, 54). Both appended to `close.sh`'s `CONV_SUITES` (their tally shapes already read by the row's grep); `selftest.sh` 127/127 after; the `[3c]` row stays WARN-ONLY and `FP_CONV_ARMED` untouched — a list entry is not an arming (his). Measure: this sitting's own close card carries `CONV analyst_audit_selftest` and `CONV edit_whitelist_selftest` rows | the runner sees the suites that guard what the lane builds — a guard nobody runs is a proxy with a reputation (docs/32 §5) | done — warn-only; arming his | sessions/S183-desktop-2026-09-17.md E3 · `.claude/skills/muster/close.sh` line 156 |
| convert/estimator-figure-density-term | **ADDED 2026-09-17 (S183 E1/E4, Observed from `events.jsonl` on two books of his run end to end):** the convert `estimate` (basis `similar`, 3 samples) promised **68 s at 0.749 s/page** for *Document Layout Analysis* (92 pp, figure-heavy — Marker's recognizer ran over 750 regions) and the `converted` event read **402.5 s at 4.38 s/page — 5.9× under**; then, having learned that run, it promised **395 s at 2.38 s/page** for *Information Dashboard Design* (166 pp) and the run read **175.3 s at 1.06 s/page — 2.25× over**; **MEASURED again S184 (2026-09-17, three more books of his):** *Naked Statistics* 898 s promised / 282.7 s delivered (3.2× over), *Data Science with R* 948 / 286.4 (3.3× over), *Data Science for Business* 1,289 / 234.7 (5.5× over) — the `similar` basis, having learned one figure-heavy book at 4.38 s/page, now over-promises every text book after it; five of six runs outside 2×. **MEASURED S185 E1 (2026-09-17, read-only): the figure-density term this row proposed is FALSIFIED** — the slowest book (4.38 s/page) and the fastest (0.57) carry the same 0.35 images/page (pymupdf, 20 sampled pages each); the slow run's cause was the recognizer (750 regions sent to OCR on a `clean` book — unknowable before Marker runs). The weakness is the BASIS: `similar` with three samples learns the last slow book and over-promises the next three by 3–5×. Proposed instead: every ledger row as the basis, a median weighted by `chars_per_page`, and a promise that carries its spread (docs/34) — the Dock's bar a range, not a point. A change to `estimate` in `convert_and_ship.py` — the child's file: only with the card free. The Dock's ETA bar was wrong by minutes in both directions; the record stays honest only because `converted` carries the actual beside the promise (OK-17). The `probe` already counts `chars_per_page` and the OCR ratios but not images per page — the term the misses share. Build: a figure-density feature (images/page from the probe, or the recognizer's region count from the first pages) in the `similar` basis, sized offline on the conversion ledger's rows (every row carries `chars_per_page`, `s_per_page`, `pages`; the image count is the missing column — add it to the probe first, warn-only, then measure); a tripwire with a planted figure-heavy fixture. **MEASURED S188 E1 (2026-09-17, leave-one-out over the ledger's 30 rows, `sittings/S188/e1_estimator_loo.py`): the current basis as the field saw it — 13 of 23 books within 2× (CHRONO), 11 of 25 for a new book against the corpus; the whole-corpus median 15/23 · 16/25, the pooled rate 15/23 · 15/25 — a basis rewrite is worth +2…+5 books of ~24 inside 2× and no more: the day's 2.3–5.5× misses stand under every rule (the books were faster than any at their shape); a p25–p75 range holds the actual 10/23 · 16/25; the same-lane corpus spans 0.574–14.814 s/page and the SAME book ran 2.1× slower on another day — the predictor is not in the ledger, it is the run: the book's FIRST SLICE predicts its overall rate 6 of 6 within 2× (0.45–1.34; five of six 0.73–1.34), but only chunked books (> 600 pp clean) have slices. PROPOSED on the Desk S188 E2 in his form; the build his word. CORRECTED S188 E5 (the five synthetic live-proof rows of S175/S176 were in that corpus as books — retired to `conversion-ledger.retired.jsonl`; re-run on the 25 book rows: the current basis 14/23 · 13/25 within 2×, the whole-corpus median 15/23 · 16/25, the p25–p75 range holds the actual 10/23 · 7/25 — a basis rewrite gains one to three books in twenty-five, a range is right about a third of the time, the first-slice predictor 6/6 stands; the correction posted, Desk e9358ed7). The proofs themselves were the estimator's nearest neighbours for sparse and dense books (77 min promised for an 8-min book) — the lane's own leak, fixed E5.** | the Dock's promise within 2× of the actual on a figure-heavy book | open — mechanical, the lane's (a measurement first) | sessions/S183-desktop-2026-09-17.md E1 · `windows-converter/convert_and_ship.py` (`estimate`, the probe) · `conversion-ledger.jsonl` |
| ~~table/header-fold-admits-a-data-row (SYM-134)~~ **STRUCK 2026-09-17 (S187 E2) — BUILT with its tripwires (211/211 · 55/55; the field's before/after: the specimen alone moved to RESTORED)** | **ADDED 2026-09-17 (S186 E1):** the table-geometry rung admits a header row fused into the first DATA row as a stacked-heading fold (`_fold_allowed(header=True)` unconditional) — a corrupted table shipped in *Data Science for Business* that the plain acceptor would have reverted. Build (S187, the card free — `table_geometry.py` is the analyst child's import): in `_fold_allowed(header=True)`, refuse when the lower row's filled cells outside column 1 are all numeric ("the lower row is data — not a stacked heading"); tripwires: the Table 10-2 specimen in `table_geometry_selftest` (`grid_invariant` False, the reason naming data) and in `edit_whitelist_selftest` (`reconcile(FULL_TABLES)` restores the table whole), the regression stacked heading as the positive control (still admitted); then a read-only re-run of the acceptor over DSB's 50 tables to count how many fused rows it would now revert | a table's header never fuses into its data in a shipped note | open — mechanical, the lane's (a fix with its tripwires) | sessions/S186-desktop-2026-09-17.md E1 · `windows-converter/table_geometry.py` · SYM-134 |
| convert/analyst-runs-on-a-convert-phase-fail (lever) | **ADDED 2026-09-17 (S187 E5, Observed on two of his books):** the convert-phase audit's `fail` (degeneration) is known BEFORE the analyst starts, and the analyst runs anyway — *Data Science with R* 787 s, *Automate the Boring Stuff* 4,501 s and again 4,501 s after SYM-135's re-run — the card at full draw for a book `_enforce_hold` will park regardless (`verdict_with_phase` reads the convert block first). Today's design keeps it so the held bundle carries an ANALYSED body for the bench; the alternative parks the Marker body at once and lets `--reanalyze` (J42) run the analyst after the bench's repair — the analyst's ~10.5 s a chunk spent only on a body that will ship. A LEVER (it changes what a held bundle holds): his word; the measure first — how many held-on-convert books were later re-analysed vs repaired (the ledger and the vault's history), and the card-hours the skip would have saved on this corpus. **MEASURED S188 E3 (2026-09-17, read-only over the twelve bundles in `held/`, `sittings/S188/e3_analyst_on_fail_measure.py`): the signal known before the analyst starts is the convert block's `tripwires.degeneration` (`verdict_with_phase` checks the analyst gate FIRST, so a bundle failing both phases reads `analyst` — Automate does; the phase column alone under-counts): 9 of 12 held bundles carry degeneration; the analyst ran on 7 of them for 25,099 s = 6.97 card-hours — 68 % of the 36,960 s (10.27 h) the analyst has spent on every held book; of those seven, ONE (Zero-to-One, 14 repairs, reshipped twice) had its analysed body used by the bench, six sit unrepaired (`repairs.jsonl` absent); Equity Research (10 repairs, never reshipped) and the Cybernetics models book (reshipped once) held WITHOUT an analyst pass. Two older manifests carry `verdict` pass/flag beside `degeneration: true` (Zero-to-One, Cybernetics) — a reship's verdict beside the original tripwire; noted, not touched. The vault's own history UNREAD locally (the ThinkPad's repo; the `--reshipped` suffixes answer the export question). The lever's price on this corpus: ~6.4 card-hours saved against one analysed body the bench used.** | a held book costs the card only what its road needs | open — a lever, his; the measure the lane's | sessions/S187-desktop-2026-09-17.md E5 · `windows-converter/convert_and_ship.py` (the analyst step before `_enforce_hold`) · docs/15 §12 |
| ~~observability/the-36-unsigned-keys~~ **STRUCK 2026-09-17 (S191 E1–E3) — the whole-repo census reads `0 unsigned glitch(es) at 0 site(s) across 3 lanes`; 36 entries signed one judgment at a time, each read at its producer AND its consumer (the count was 16 bench · 13 widget · 7 converter, not the 19/11 this row's ADDED text says — corrected here): 7 converter (6 EVIDENCE, 1 INTERNAL; `dict_hit`'s acceptance row flipped GLITCH → evidence with the reason), 13 widget (11 EVIDENCE — the nine config-file fields are human-written inputs whose home is the file, rendered by presence; 2 INTERNAL — `unacked` below the signed M, `convert_frac` behind a monotonic bar by design), 16 bench (14 EVIDENCE — the repair ledger, the locate/find/undo/rescore responses; 2 REPORT — REPAIRS.md's `chars_added`, `entropy_pct`); `acceptance.py` 288/288; `--enforce` exit 0; SYM-027's criterion (3) MET** | **ADDED 2026-09-17 (S190 E2, Observed by the whole-repo `glass_detector.py` census):** the `--since` census is clean at every close, but the WHOLE-repo census reads **36 unsigned glitches at 39 sites** — 19 `bench:*` keys of the Repair Bench's JSON (`total_hits`, `needles`, `votes`, `seq`, `context_before`, `context_after`, `sha_before`, `sha_after`, `regions`, `chars_restored`, `matches_disk`, `delta_lines`, `para_lines`, `zones_with_repairs`, `chars_added`, `entropy_pct`) and 11 `widget:*` config keys (`acked`, `unacked`, `linux_host`, `remote_user`, `remote_inbox_root`, `vault_library_dir`, `gpu_pipeline_dir`, `gpu_python_exe`, `gpu_converter_dir`, `reader_obsidian`, `reader_zennotes`) — SYM-027's criterion (3), the standing §7 debt, NOT MET since S77. The work: each key a docs/29 §5.3 judgment read from its producer AND its consumer (`bench.py` beside `bench.html`; `config.rs` beside the Room's surfaces) — GLASS where a surface reads it, EVIDENCE where the manifest or a ledger is its home, INTERNAL for control flow, DEAD if nothing should produce it — committed as judged, never a batch; a wrong disposition signs silence, which is the defect the detector exists for | the whole-repo census reads 0 unsigned, each entry carrying its reason | open — the lane's, a sitting of its own (S190 §18) | sessions/S190-desktop-2026-09-17.md E2 · `observability/glass_detector.py` (bare run) · `observability/dispositions.json` |
| institution/the-copyable-institution-prototype | **ADDED 2026-09-17 (S193 E1–E7, Observed on a copy in Temp):** his word in chat — "prototype-institutions … the full thing … auto persistent … nothing missed" — built at `file-portal-private/institutions/` (the junction `prototypes/institutions`, gitignored at :121): the constitution, the three contracts, five hooks, four skills, six registers, the record template and journal, the memory index with ONE clock line, five observability tools, twelve tools, nine tripwire suites (24 runner lines, all green; the git guard 181/181 in a real copy), fourteen docs, the bootstrap that refuses S1 on any red or silent suite, the reference tenant (this converter's manifest) and the contract test — proved: a copy bootstrapped CLEAN, a broken copy refused. What stays: **publishing a copy (his, HUMAN.md §2 kind 6)**; a real tenant's `[[lanes]]` (T-002 in the copy); a copy's first sitting under a human's words | a copy is published on his word, or a real tenant occupies one and closes its S1 in lockstep | open — the build DONE; the publishing SEMANTIC (his) | sessions/S193-desktop-2026-09-17.md E1–E7 · `file-portal-private/institutions/README.md` · `sittings/S193/PLAN.md` |
| ~~tracker/sym-111-lands-at-his-next-boot~~ **STRUCK 2026-09-17 (S196 E2) — MET WITHOUT HIS BOOT, THE ROW'S PREMISE CORRECTED: the boot service does NOT run the old library until his next boot — `proc_tracker_service.ps1:46` spawns a fresh `proc_events.ps1` child at every session-gate open and the child dot-sources `proc_events_lib.ps1` from disk (`proc_events.ps1:52`), so the S195 build landed at this sitting's gate open (19:23:28Z; the service itself started once, 2026-09-15 22:51Z, never restarted). The done-when read (Verified: the survey fleet's verifier, S196 E1, and the coordinator's own count `sittings/S196/e2_receipt_read.py`): the S196 receipt (`receipt-20260917-152328.partial.jsonl`, the new library) carries `cmdline_state` on 1,239 of 1,239 records and NO empty command-line cell (310 read `UNREAD (no command line captured)`, 929 `read`; the md row 386 UNREAD lines); the S195 receipt (the old library, the negative control): 0 of 990 with the key, 271 EMPTY cells. SYM-111's FIRST marker moved to FIXED S196 E2. Residue: the S196 FINAL receipt is written at this sitting's gate close — S197 reads it; the service records in poll mode, so events mode's own live receipt under the new library stays unread until an events run (elevated — his)** | **ADDED 2026-09-17 (S195 E1, Observed by `proc_events_selftest.ps1` 42/42):** SYM-111's four mechanisms are built in the tracker's library (private `proc-feed/`), but his boot service (`proc_tracker_service.ps1`, installed by `install_tracker_task.ps1`) loaded the OLD library at this boot and runs it until the next — nothing running was edited underneath. What lands at his boot: receipts that say `UNREAD` for a command line the events mode could not read, a claim window on the post-trace read, an identity cache keyed pid|ticks, parents refused by creation time | the first receipt written by the new library carries `cmdline_state` and no empty command-line cell; then SYM-111's FIRST marker moves to FIXED | open — SEMANTIC (his boot; or his word to restart the service) | sessions/S195-desktop-2026-09-17.md E1 · `SYMPTOM-INDEX.md` SYM-111 (the S195 Update) |
| hooks/heredoc-deny-for-an-unbalanced-quote | **ADDED 2026-09-17 (S198 E2, Observed by `sittings/S198/e1_hook_census.py` over the day's transcripts):** the warn-only heredoc hook (`warn_heredoc.py`, ERR-009; its over-sensitivity his signature 2026-08-27) fired 26 times today — 21 flagged heredocs worked, four were the git guard's denials, ONE was the bin's own failure again (11:43Z, an unterminated quote). PROPOSED on the Desk in the e1bd1e1e form: ONE blocking predicate — a heredoc whose quotes do not balance is DENIED; everything else stays warn-only as signed; two selftest cases, one per side; the cost ~20 min of the lane and a nil false-deny price (an unbalanced heredoc fails anyway). The cheaper remedy, said beside it: the lane's own hand — the Write tool for every script, already the rule, which the count says did not hold alone | his word: signed → the hook edit + the two cases, warn-first is already the state; or "the warning is enough" → struck with his word | open — SEMANTIC (a gate change; his signature) | sessions/S198-desktop-2026-09-17.md E1–E2 · `ERROR-BIN.md` ERR-009's remedy cell · the Desk (S198 E2) |
| receipts/baseline-rows-s159-s181-recut-after-a-trial-diff | **ADDED 2026-09-17 (S199 E2, Observed by `sittings/S199/e2_journal.py`):** thirty-nine tracker receipts reached the journal (`procs/S159.md` … `S198.md`) once `proc_receipt.py` learned the service's gate label (S199 E1, its selftest 5/5); today's sixteen baseline rows (S182–S198) were re-cut with their own values and read a tracker coverage for the first time since S158; the twenty-three earlier rows (S159–S181) still read `tracker coverage UNREAD` with their receipts on disk — a re-cut recomputes EVERY field from the journal, the guard log and git, and a historical row must not drift silently | a trial: `baseline.py S159 --tickets <its JSON's n_tickets> --chain-exit <its> --out <a temp dir>` and a diff against `measurement/baseline/S159.json` ignoring `computed_at` and `tracker` — equal → the twenty-three re-cut in place (`e2_journal.py`'s scope widened to S159); any other field moved → that field's drift is the finding, named before any row is rewritten | open — MECHANICAL, the lane's; no card · `Observed 2026-09-17 (S200 E1)`: the trial RUN per row (`sittings/S200/e1_recut.py`, `e1_recut.json`; the walker's negative control: `--tickets` off by one reads MOVED): 5 of 23 re-cut in place — S177, S178, S179, S180, S181 (nothing but `computed_at` and `tracker` moved; tracker coverage 0.58–0.76); 18 LEFT: S159, S160, S161, S162, S163, S164, S165, S166, S167, S168, S169, S170, S171, S172, S173, S174, S175 carry a REVISED calls receipt (`S<N>-r2.md`, filed after the row was cut) — a re-cut would follow the revision (`inferred`/`preloaded` reclassified, `corrections_filed` up, the window's receipt name) and move 7–10 fields; S176 moved only on `corrections_filed` (later corrections naming it). The question the lane does not decide alone: does a baseline row FOLLOW its receipt's revision (the -r2 has its CORRECTIONS.md row by design) or stand as first cut? — STUDY §11's rule to name; until named, the eighteen stand, their tracker coverage readable in `e1_recut.json` (0.06–0.79) | sessions/S199-desktop-2026-09-17.md E2 · `measurement/baseline/README.md` |
| ~~baseline/verifier-field-refuted-of-candidates~~ **STRUCK 2026-09-17 (S202 E1–E2) — BUILT with its tripwire and USED: `baseline.py --verifier R/C` → `verifier: {refuted, candidates}` in the JSON, the README's last cell `tickets / refuter / verifier` (UNREAD when not given; a malformed value never a number); three cases in `baseline_selftest.py` (a KeyError crash against the old code — the negative control) and the suite's own tally found TYPED (23/23 under three new [ok] lines) and made a read count: 26/26; S196's row re-cut after a clean trial walk with `2/5` READ from `sittings/S196/survey.json` — the README's S196 row reads `4 / UNREAD / 2/5`. The refuter field stays UNREAD everywhere (no refuter lane ran)** | **ADDED 2026-09-17 (S201 E1, Observed by `sittings/S201/e1_refuter.py`):** the baseline row's `refuter` field is defined "reproduced/total" of a refuter lane's findings (S141, STUDY §11); a SURVEY-shaped fleet (S196 E1: readers, a struck-row control, an adversarial verifier per candidate) produces a different number — candidates REFUTED of candidates judged (2 of 5, `sittings/S196/survey.json`) — and it was not written under the other name. The instrument needs its own slot: `--verifier R/C` → `verifier: {refuted, candidates}` in the JSON and a column in `measurement/baseline/README.md`, `UNREAD` when not given; its selftest case beside `baseline_selftest.py`'s; then S196's row re-cut after its trial diff with `--verifier 2/5` READ from the survey's file | `baseline_selftest.py` green with the new case; `baseline.py S196 … --verifier 2/5` re-cut after a clean trial; the README's S196 row reads the verifier column | open — MECHANICAL, the lane's; no card | sessions/S201-desktop-2026-09-17.md E1 · `measurement/baseline/README.md` |
| ~~open/claims-reader-for-the-previous-sitting~~ **STRUCK 2026-09-18 (S207 E1) — MET by its own done-when: three opens in a row ran `proc_receipt.py S<N-1>` then `claims_reader.py S<N-1> <pin> <closing>` and carried the three readings into the record's §3 before the closing message repeated them — S205 §3 (S204's window), S206 §3 (S205's), S207 §3 (S206's): idle · untouched · nothing written each time, the receipt a clean stop each time. The reader stays in the open's rule (the carry-over names it); the row's job is done** | **ADDED 2026-09-17 (S204 E2, Observed by `sittings/S204/e2_day.py`):** the private `agent-scripts/auditing/claims_reader.py S<N> <pin> <closing>` reads, from files that exist, the two claims every closing message made without one — the card idle (the tracker receipt's Image census against a named model set), his tree untouched (the git diff against the code dirs), the pipeline unwritten (mtimes in the receipt's window) — each UNREAD when its source is missing; its tripwire 10/10; run over S193–S203 at S204: eleven idle, eleven untouched, one file in one window (the widget's own boot log). The reading belongs at the OPEN, after `proc_receipt.py S<N-1>` finalizes the receipt: the previous sitting's three lines into the record's §3, so the claim is Observed before the next closing message repeats it | the open's rule in the carry-over names the reader; the next three records' §3 carry the previous sitting's three readings (Observed); the closing message says 'the card idle' only with the reading behind it | open — MECHANICAL, the lane's; no card · `Observed 2026-09-17 (S205 E1)`: one of three — S205's §3 carries S204's three readings (idle · untouched · nothing written), the receipt finalized first · `Observed 2026-09-17 (S206 E1)`: two of three — S206 §3 carries S205 (idle · untouched · nothing written) | sessions/S204-desktop-2026-09-17.md E1–E2 · `agent-scripts/auditing/claims_reader.py` (private) |
